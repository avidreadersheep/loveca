---
description: 束 B（loveca_db）を実施する。U22 / D-11 / U10 / D65 の ord 移行 + D-8 + D-13
---

# 束 B の実施

`CLAUDE.md` の §7（作業ルール・止まる条件）と §8（現在地・未決の索引）を先に読むこと。

---

## U22 / D65 の確定内容

★★**正は `docs/決定事項一覧.md` の「D99 の詳細」。**★★
★**ここに写さない**（同じ内容が 2 箇所にあると必ず古くなる / **D-15**）。

---

## コミット分割

★**1 コミット = 1 論点。順序も守る。**

| # | 内容 |
|---|---|
| 1 | **doc のみ。** U22 を新決定として記録 + D65 の backfill 基準行と backfill SQL 行を差し替え + D65 に 1 行追記 + `deck_repository.dart` の doc を参照に置換。★★**実装と同じコミットにしない**★★ —— 混ぜると U22 が D65 の前提を動かしたことが履歴から消える（**D-15 (l)** / D73 の実例） |
| 2 | 比較器を `loveca_core` に置く + テスト |
| 3 | `schemaVersion` 2→3（ALTER / Dart の backfill / `save` が添字を書く / `byId` と `all` の両方に `ORDER BY ord`）+ `migration_test` |
| 4 | UI を `ord` に切り替え（`normalizedEntries` 撤去 / 予告撤去 / `isDirtyAgainst` / リセット / 挿入位置） |
| 5 | **D-8** の前提の測り直し（下記） |
| 6 | **D-13**（方式決定済み。実装のみ） |

★**U21（詳細検索）はこのセッションに含めない。** 論点 4 つを持つ大物で、索引追加で版がまた
上がりうるため、D65 の移行と別の版にする。

---

## D-8 について

`docs/決定事項一覧.md:1181` の「孤児は構造上 0 件」には **2026-08-24 の日付が付いている。**
★**結論ではなく条件が今も成り立つかを測り直す**（**D-15 (l)**）。

★**0 件は「無い」と「見えていない」の区別がつかない**（**D-10**）。
孤児を 1 件わざと作って計数が拾うことを**先に**確かめてから本番を測ること。
0 件なら決定どおり掃除経路は作らず、**測り直した事実だけ**記録する。

---

## テストの作法

★★**すべて陽性対照を対で置く。**★★

- **並び順**: 投入順とソート順が**食い違う**フィクスチャで測る。
  ★一致するデータだと比較器が no-op でも通る
- **backfill**: 無効化したら落ちることを**先に**確かめてから本体を測る
- **`migration_test`**: 「デッキが残る」を足す（`docs/決定事項一覧.md:1467` が既に要求している）。
  ★これは `card_search` の建て直し（D49）と違い**ユーザデータに触る移行**である
- **経路差**: `all` と `byId` が同じ順を返すことと、**片方だけ壊したら落ちること**の両方

---

## 新所見として記録すること

`onUpgrade` の backfill は `save` を通らないので `revision` を上げない。
かつ backfill は `cost` / `score` を引くので**マスタに依存**し、端末間で `masterDataVersion` が
違うと `ord` がずれうる。

★null を末尾に落とす規則があるので壊れはしないが、**端末間で並びがずれる経路が 1 本できる。**
→ D 番号を採って記録し、**Phase 4 の束 A** の検討材料として残す。★★**いま直さない。**★★

---

## 止まる条件

`CLAUDE.md` §7 の 6 条件に加えて、次に当たったら**止めて報告**すること。

- backfill 時にマスタから `cost` / `score` を引けない札が実データに存在した場合
  （★末尾に落とす規則自体は決定済みだが、**件数と内訳を報告してから**進む）
- 上の確定内容のいずれかが、既存の実装と噛み合わないことが分かった場合
- 上記以外で D 番号の記述を変える必要が出た場合

---

## 報告

★★**報告は `docs/セッション報告/YYYY-MM-DD_束B.md` に書くこと。**★★
チャットに出すだけだと残らない。★`CLAUDE.md` §7 の「止めずに進めた場合、何を選んだかと
理由を報告に必ず書くこと」は、**残らない場所に書いても要件を満たさない。**

書く内容:

1. **コミットごとの 1 論点が何だったか**
2. **止めずに進めた判断があれば、何を選んだかと理由**
3. **測った値と、その測定が実行可能だったか**（★実行できなかったものは
   「実行不能」と書く。**推測で埋めない** / **D-15 (k)**）
4. **相談役に見てほしい点**（自分で不安が残ったところ。★無ければ「無し」と書く）

---

## 提出前の自己検査

★**read-only の subagent に投げること。**実装した本体に自己採点させない。
★**ただしこれは設計判断のレビューではない。**機械的に検査できるものだけを見る
（思い込みは共有されるので、同じセッションでは検出できない / 引き継ぎメモ §9）。

- [ ] 1 コミットに 2 つ以上の論点が入っていないか
- [ ] 陽性対照が**対**になっているか（「出る側」だけのテストが無いか）
- [ ] doc に**機械が数えられない数**を書いていないか（**D-15**）
- [ ] 同じ内容を**2 箇所**に書いていないか
- [ ] 決定番号の参照先が**実在**するか（**D-5** と同じ型を作っていないか）
- [ ] ルールに触れるコードに**条番号**が併記されているか（**D-B**）
- [ ] `loveca_core` に Flutter / `dart:ui` / `dart:io` / `DateTime.now()` / seed なし `Random()` が
      入っていないか
- [ ] ★**この `.claude/commands/bundle-b.md` の「初回のみ」の節を削除したか**

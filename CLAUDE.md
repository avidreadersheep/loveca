# ラブカ シミュレーター — 作業指示書

> このファイルは Cowork の「フォルダ指示（Folder instructions）」および Claude Code の
> プロジェクト指示として読まれる。セッションを跨いで前提を引き継ぐための唯一の入口。
> **`★` の項目は実データで前提が覆された経緯がある。安易に変更しないこと。**

---

## 0. このプロジェクトは何か

ラブライブ！シリーズ オフィシャルカードゲーム（通称「ラブカ」）の
**非公式シミュレーターアプリ**。個人利用・非商用の範囲で開発する。

★★**この 1 行が何を含むかの読みは 2026-08-27 に記録した。正は `docs/決定事項一覧.md` の
`決定 D104` の詳細「確認の範囲」である**★★（★**範囲そのものをここに書かない** —— 同じ内容を
2 か所に置けば必ず食い違う / `ルール整合性チェック_v1.06.md` **D-15** の規約 3）。
★**きっかけは `決定 D105`**（利用者は複数）。★**上の 1 行は 1 文字も変えていない。**

```
カードを検索できる → デッキを作れる → ソロで回せる → オンラインで対戦できる
```

| | Windows (PC) | iOS / Android |
|---|---|---|
| カードリスト・検索・デッキ構築・同期 | ○ | ○ |
| ゲーム盤面（ソロ / ローカル対戦 / オンライン対戦） | ○ | **×** |

★★**上の表は 1 文字も書き換えていない。★★iOS の扱いは★この表の右列と同じではない★★**★★（2026-09-01 / **D-35**）——
★**右列は「iOS / Android」で★★2 つを 1 つにまとめている★★**が、★**iOS には★順序と条件が付いた方針が在る**。
★★**中身をここに書かない。★正は `docs/決定事項一覧.md` の **D134-1** である**★★（★経緯は `docs/同期設計メモ.md` §9-19 / **D-15**）。

---

## 1. 絶対に変更しない設計方針

### ★ カード効果の自動処理は実装しない

ルールエンジンによる効果の自動解決・誘発効果の自動発動・効果テキストの構文解析は**一切行わない**。
紙のカードと同じように、プレイヤー自身が判断して手動操作する。
アプリは「盤面・カード・山札・手札をデジタル上で操作するサンドボックス」である。

**実装してよいのは物理操作の補助のみ**：引く / シャッフル / 移動 / 上から見る /
表裏の反転 / 横向き / 重ね置き / 選択。

### ★ ルールに触れるコードには必ず総合ルールの条番号をコメントで併記する

```dart
// 総合ルール 8.3.10: ブレード合計は「アクティブ状態のメンバー」のみ
```

根拠のない数値・条件をコードに書かない。参照元は `docs/LoveLiveTCG_cr_1.06_260428.pdf`（ver 1.06）。

★**`docs/` の公式 PDF は git 管理外**（ブシロードの著作物）。クローン直後は各自で配置する。本文の抽出は `python docs/tools/extract_rules.py`。

### ★ `loveca_core` に Flutter / 日時 / 乱数 / IO を持ち込まない

純粋 Dart パッケージとして維持する。理由は 2 つ。
1. デッキ検証が二重実装になると「スマホでは合法、PC では不正」という事故が起きる
2. Phase 6 の権威サーバが同じ `reduce` / `redact` をコピーゼロで再利用する

**禁止する依存**：

| 禁止 | 理由 |
|---|---|
| `package:flutter` / `dart:ui` | 上記のとおり。サーバとスマホで共有できなくなる |
| `dart:io` | 同上。サーバ・Web・テストで挙動が割れる |
| **`DateTime.now()`** | 同じ入力から同じ結果が出なくなる。時刻は呼び出し側から渡す |
| **`Random()`（seed なし）** | 同上。**乱数は `DeterministicRng` 抽象を注入する** |

★禁止するのは**非決定な呼び出し**であって型ではない。
`DateTime` を値として持つのは可（`Deck` の `createdAt` / `updatedAt` は同期に要る）。

★**乱数はシャッフル（10.2.3）に要るが、実装を埋め込まない。**
`DeterministicRng` を注入して seed 再現性を確保する。理由は 2 つ。
(1) 同じ seed で盤面を再現できないと不具合を追えない。
(2) Phase 6 の権威サーバがシャッフル結果の権威を持つ必要がある。

★★**既知の違反は 2026-08-31 に解消した（決定 D116-2 / Phase 4 のレーン 1）。**★★
`loveca-core/lib/src/entities/deck.dart` の `Deck.copyWith` が
`updatedAt` の既定値に `DateTime.now().toUtc()` を、
`revision` の既定値に `this.revision + 1` を使っていた。
★★**違反は 1 つではなく 2 つだった**★★ —— ここには長く「1 箇所」と書いてあり、
`revision` の側は `docs/同期設計メモ.md` §1 の A-3 が 2026-08-30 に記録するまで数えられていなかった。
→ ★**どちらも「据え置き」に変えた**（★署名は 1 文字も変わっていない）。
★**3 つ組を決めるのは呼び出し側である**（決定 D116-1）。

★★**「同期経路が `copyWith` を通るか」は開いたままである。解消と混ぜないこと。**★★
書き込みの口は `決定 D115-4`（既存の `save` 1 本）で決まったが、
★**受け取った内容から `Deck` を組む手段**（明示コンストラクタ / `fromJson` / `copyWith`）**は決まっていない。**
★**経緯の正は `ルール整合性チェック_v1.06.md` **D-14** と `docs/同期設計メモ.md` §26-6。**

★★**下の検証は 0 件にならない。★当たるのは禁止そのものを説明した doc の行である**★★
（`reduce.dart` と `rng.dart`。★**D-30** —— 禁止対象を説明する文書は禁止対象と同じ字面を必ず含む）。
→ ★**「非 0 だから違反が残っている」と読まないこと。★当たった行を 1 件ずつ開くこと。**

検証：

```bash
grep -rnE "package:flutter|dart:ui|dart:io|DateTime\.now|Random\(\)" loveca-core/lib
```

`loveca_db` は Flutter 非依存だけを課す（`dart:io` は `lib/native.dart` と
`lib/src/native/` に閉じる）。

```bash
grep -rnE "package:flutter|dart:ui" loveca-db/lib          # 0 件であること
grep -rln "^import 'dart:io'" loveca-db/lib            # native 配下のみ
```

★★`loveca_server` の境界はここに書かない。★正は `loveca-server/lib/loveca_server.dart` の doc である**★★
（決定 **D126-4** / **D-15** —— ★**同じ内容を 2 か所に置けば必ず食い違う**）。
★**格が違うので §1 に混ぜない** —— ★ここは**絶対に変更しない設計方針**であり、
★サーバーの境界は **D126** で決めたものである（★開き直す条件が在る）。
★**見張りは `loveca-server/test/core_boundary_test.dart`**（★決定 **D115-7** の (c) ＝ 走査テスト）。
★★**`loveca_core` を `loveca-server/pubspec.yaml` に書いてはならない**★★（**D126-3**）——
★線 α は**空で始まる許可リスト**（**D115-6**）であり、★**書けば「呼べる」状態になって空であることを構造で守れなくなる。**

---

## 2. ディレクトリ構成

```
loveca-data/     Python: カードデータ取得・正規化パイプライン（開発者ツール）
  loveca_data/   config / constants / http_client / fetch / normalize / validate / build_dist / stats / cli
  tests/         run_all.py / test_imports.py / test_normalize.py
  data/          ★ローカル資産。zip に含まれない。絶対に消さない
    raw/         生レスポンス（取得は 1 回きり）
    normalized/  normalized.json
    dist/        配信物（version / manifest / cards/*.json / meta / images）

loveca-core/     Dart: ドメイン層（Flutter 非依存）
  lib/src/entities/   card.dart / deck.dart / product.dart
  lib/src/master/     master_data.dart（配信 JSON パース・差分更新の計画）
  lib/src/rules/      deck_validator.dart / deck_order.dart（★決定 D99 の比較器。UI と DB の両方が呼ぶ）
  lib/src/sync/       ★deck_edit_op.dart（デッキ編集の操作の語彙 / 決定 D110-1）
  lib/src/game/       GameState / 集計 / フェイズ進行 / 巻き戻し / reduce / redact
  test/fixtures/      Python が実際に生成した JSON のコピー
  tools/verify_contract.py   Python↔Dart のキー整合検証（Dart SDK 不要）

loveca-db/       Dart: ローカル DB 層（drift / SQLite。★Flutter 非依存）
  lib/loveca_db.dart      スキーマ・DAO・取り込み層。dart:io を含まない
  lib/native.dart         ★dart:io に触れるのはこのエントリの下だけ
  lib/src/schema/         drift のテーブル定義とコード生成物
  lib/src/dao/            Card / Deck / 取り込み状態の読み書き
  lib/src/search/         fold（表記ゆれ折りたたみ）/ FTS5(trigram) 検索
  lib/src/import/         MasterFileSource / MasterImporter（planUpdate を使う）
  test/fixtures/dist/     ミニ配信物。tool/build_fixtures.py が実データから生成
  tool/build_fixtures.py  ★相互にハッシュ参照するので一括生成する
  tool/probe_sqlite.dart  解決された sqlite3 の診断（FTS5 / trigram の可否）

loveca-ui/       Flutter: UI 層（Windows デスクトップ）
  ── ここから下は【本実装の土台】。Phase 2 後半がそのまま使う ──
  lib/           ★本実装はここに書く。今は placeholder の main.dart のみ
  windows/       ランナー。flutter create が生成した実プロダクトの一部
  pubspec.yaml   依存の正本
  ── ここから下は【技術検証の試作】。本実装から参照しない（決定 D51） ──
  spike/         ★lib/ の外に置いてあり、lib/ から import できない
    README.md    ★扱いの取り決め。触る前に読む
    common/      DB 起動 / 計測 / 合成ポインタ / パス解決
    main_probe.dart   sqlite3 の Flutter 経路の疎通
    main_grid.dart    試作1 仮想リスト
    main_search.dart  試作2 検索
    main_drag.dart    試作3-A デッキ編集
    main_board.dart   試作3-B 最小盤面
    main_search_variants.dart  検索改善案の比較（D49 の判断材料）
  spike/.cache/  ★DB ファイルと測定結果。git 管理外

loveca-server/   Dart: 同期サーバー（★Flutter 非依存。決定 D126）
  lib/loveca_server.dart   ★★境界の宣言（4 段）。★触る前に読む★★
  lib/src/boundary.dart    ★線 α の許可リストの正（★空で始まる / D115-6）
  test/core_boundary_test.dart  ★見張り（D115-7 の (c) ＝ 走査テスト）
```

★★**2026-09-02 追記: ★プラットフォームを足す★手順★は `docs/プラットフォームを足す手順.md` である**★★
（★**下の 3 行は 1 文字も書き換えない** / **D-35**）—— ★★**`.metadata` の件は★★そちらに移した★★**
（★**注記では★次に踏む人が★事前に読まない**。★★手順の在る場所に置く★★ / ★運転指示【0】(6) の 6）。

★★**2026-09-01 追記: `loveca-ui/android/` を足した**★★（★Phase 5 の着手 / ★`flutter create --platforms=android`）。
★★**上の構成図は 1 文字も書き換えない**★★（**D-35** —— ★書いた時点では正しい）。
★**`windows/` と★同じ格である**（★ランナー。★`flutter create` が生成した実プロダクトの一部）。
★**`android/.gitignore` が★`local.properties` / `gradlew` / 生成物を無視する**（★§3 の見張りは★★0 件のままである★★ / ★実測）。
★★**`.metadata` は★`windows` の行を★手で戻した**★★ —— ★**`flutter create` が★★置き換えてしまった★★**（★実測）。

★**`lib/` `windows/` `pubspec.yaml` と `spike/` は扱いが違う（決定 D51）。**
前者は本実装そのもの。後者は D42〜D50 の数値の再現手段であり、
**Phase 3b 完了時に削除を再判断する。**
`spike/` にはテストが無く、本実装を変えても静かに腐る
（D49 で実際に `main_search.dart` の計測が実態と食い違った）。
**本実装の変更で spike の計測が意味を失ったら、その場で注記を直すか消すこと。**

★`loveca_db` は Flutter に依存させない。`drift_flutter` / `sqlite3_flutter_libs` /
`path_provider` は採らず、`QueryExecutor` を呼び出し側から受け取る。
`dart test` だけで検証できる状態を保つことが目的で、Phase 6 のサーバでも使える。
`dart:io` 禁止は **`loveca_core` に対する制約**であり `loveca_db` には及ばないが、
汚染範囲は `lib/native.dart` と `lib/src/native/` に閉じてある。

---

## 3. コマンド

```bash
# Python 側（取得・正規化・検証は標準ライブラリのみで動く）
cd loveca-data
python tests/run_all.py                       # テスト（ネットワーク不要）
python -m loveca_data stats                   # 統計（ネットワーク不要）
python -m loveca_data normalize               # 段階4（ネットワーク不要）
python -m loveca_data validate --complete     # 段階5（ネットワーク不要）
python -m loveca_data build --data-version 1 --min-app-version 0.3.0 --skip-images   # 段階6（JSON のみ・数秒）
#   ★★`--min-app-version` は必須である（決定 D118-5 ＝ 既-3 / D-7）。既定値は無い★★
#   ★アプリが実際に読める最小版を毎回考えて渡す。★綴りを間違えても例外は出ない
#     （数にできない部分は 0 として比較されるので「最小版なし」に化ける）
#   ★★上の 0.3.0 は★決めた値であって★今日のアプリの版ではない★★ ——
#     ★決めた値の正は `決定 D122-1`、★いまのアプリの版の正は
#     `loveca-ui/lib/src/app_info.dart`（★今日は `1.0.0`。★下げるのは
#     §32-6 のレーン 2 の末尾で、★まだ実施していない）
python -m loveca_data fetch --all             # ★段階0-3: 公式サイトへ実アクセス

# Dart 側（ドメイン層）
cd loveca-core
dart test
dart analyze                                  # ★テストでは検知できない指摘が出る
python tools/verify_contract.py               # Dart SDK 不要

# Dart 側（DB 層）
cd loveca-db
dart pub get
dart run build_runner build                   # drift のコード生成（*.g.dart はコミットする）
dart test
dart analyze                                  # ★同上
dart run tool/probe_sqlite.dart               # sqlite3 の FTS5 / trigram の可否
LOVECA_DIST_DIR=/path/to/dist dart test       # 実データの場所を変える
../loveca-data/.venv/Scripts/python.exe tool/build_fixtures.py   # ミニ配信物の再生成

# Dart 側（同期サーバー）。★今日は境界の宣言しか無い
cd loveca-server
dart pub get
dart test
dart analyze                                  # ★同上

# Flutter 側（UI）。★計測は profile ビルドで行う
cd loveca-ui
flutter pub get
flutter analyze
flutter run -d windows -t spike/main_probe.dart      # sqlite3 経路の疎通確認
flutter build windows --profile -t spike/main_grid.dart
SPIKE_AUTOEXIT=1 ./build/windows/x64/runner/Profile/loveca_ui.exe  # 計測して自己終了

LOVECA_DIST_DIR=/path/to/dist flutter run -d windows          # 実データで起動（D60 段1・本番の設定経路）
LOVECA_SEARCH_LIMIT=50 flutter run -d windows                 # ★検証用。検索の打ち切り（D50）を実データで再現する
```

★★**2026-09-01 追記: ★プラットフォームを問わない試作を 1 つ足した**★★
（`loveca-ui/spike/main_sqlite_caps.dart` —— ★★`main_probe.dart` は★現物を読むので Android では走らない★★）。
★★**上のコマンド欄は 1 文字も書き換えない**★★（**D-35**）。

```bash
# Windows —— ★★exe を直に走らせ、★出力をファイルへ落とす（★`flutter run` は返ってこない / 実測）★★
cd loveca-ui && flutter build windows --debug -t spike/main_sqlite_caps.dart --dart-define=SPIKE_AUTOEXIT=1
./build/windows/x64/runner/Debug/loveca_ui.exe > caps.txt 2>&1   # ★★外から止める（★自分で終わらない / 実測）★★

# Android —— ★★エミュレータを起こしてから★★
flutter emulators --launch <id>
cd loveca-ui && flutter build apk --debug -t spike/main_sqlite_caps.dart --dart-define=SPIKE_AUTOEXIT=1
adb install -r build/app/outputs/flutter-apk/app-debug.apk
adb logcat -c && adb shell am start -n com.example.loveca_ui/.MainActivity && adb logcat -d | grep SQLITE_CAPS
```

★`LOVECA_SEARCH_LIMIT` は **`LOVECA_DIST_DIR` と同格ではない**（決定 D64）。
前者は検証用の口、後者は D60 が定めた本番の設定経路である。
実データの最大ヒットは `ー` の 1,034 種で、**既定 2000 では打ち切りが起こらない**ため、
上限を下げないと `truncated` の表示を実データで確かめられない。
不正値（0 / 負数 / 非数値）は既定に戻したうえで起動時に警告する。

★`loveca_ui` は `sqlite3_flutter_libs` を採らない（決定 D45 の詳細・
`docs/UI技術検証メモ.md` §1-2）。`sqlite3` のビルドフックがそのまま Flutter でも働き、
FTS5 / trigram つきの SQLite 3.53.4 がバンドルされることを実測で確認済み。
採ると SQLite が二重調達になり、どちらを掴んだか分からなくなる。

★**`dart test` はリントもアナライザも走らせない。** `dart analyze` を別に流すこと。
テストが全通過していても指摘は検知されない。
★★**`loveca-core` の `analysis_options.yaml` は 2026-08-24 に接続した（D-2 の解消）。**★★
3 パッケージとも `dart analyze` / `flutter analyze` が 0 件で通る状態になっている。
★**D-2 が想定していた `constant_identifier_names` と `StepId.s8_4_13` の衝突は起きない**
（実測で確認。除外設定は 1 つも無い）。

### ★ 意図せず無視されたファイルが無いか（`ルール整合性チェック_v1.06.md` D-10）

★★**本命は `.gitignore` 側のアンカーであって、この見張りではない。**★★
ディレクトリ規則を `/dir/`（1 箇所）か `**/dir/`（意図した再帰）で書く取り決めにしてあり、
**素の `dir/` を書かない限りこの事故は起こらない**（root の `.gitignore` 冒頭）。
下のコマンドは**将来また未アンカーの規則が足されたとき**の受けである。

★**手動の見張りは忘れられる。** D-2（`loveca-core` にリントが 1 つも効いていなかった）が
その前例で、走らせる人がいなければ何も検知しない。だから (a) を本命に置いている。

```bash
git ls-files --others --ignored --exclude-standard \
  | grep -vE "^(loveca-(core|db|server|ui)/\.dart_tool/|loveca-ui/build/|loveca-ui/windows/flutter/ephemeral/|loveca-ui/spike/\.cache/|loveca-data/\.venv/|loveca-data/data/|\.claude/settings\.local\.json|\.claude/worktrees/)" \
  | grep -vE "(^|/)__pycache__/" \
  | grep -E "\.(dart|py|md|ya?ml|json)$"
```

**0 件であること。** 何か出たら、それは**ソースに見えるのに無視されているファイル**である。

★★**除外は「ディレクトリ名」ではなく「パス」で書くこと。**★★
`(^|/)build/` のような**名前**での除外にすると、
**まさに警戒している `lib/src/rules/build/` まで一緒に見逃す。**
このコマンドの最初の版が実際にそれで、模擬事故を検知できなかった。

★★**除外が仮定していることを 1 行で書くこと —— 形式が正しくても範囲は古くなる。**★★
上の「パスで書く」は**形式**の話で、それを守っていても事故は起きる。
★実例（**D-25**）: 除外に `\.claude/` と書いてあった。**パスで書かれており形式は正しい**が、
仮定は「`.claude/` に本実装に効くものは無い」で、**その仮定がどこにも書かれていなかった。**
2026-08-27 に `.claude/commands/bundle-b.md`（作業指示そのもの）が入った瞬間に誤りになり、
**最も失うと困るものが最も見えない場所**に置かれた。★**書いた人に落ち度は無い** ——
書いた時点では正しく、**あとから前提が動いた**（型は D-10 ではなく **D-15 (l)**）。
→ ★**いま除外に残る `.claude/` は `settings.local.json` の 1 ファイルだけ**である
（グローバル ignore `**/.claude/settings.local.json` に対応する分）。
★**`/.claude/worktrees/` も除外する**（2026-08-27 / 相談役の判断で**方針を変えた**）。
★★**仮定: ワークツリーの中身は本体で追跡済みのファイルのチェックアウトであり、
誤って無視されたソースがあれば本体側の走査にも必ず出る。**★★
→ ★**この見張りがワークツリーを見る利得は無い。**
★**変えた理由**: この見張りの契約は「**0 件であること**」である。
ワークツリーがあるあいだ**非 0 が常態**になると、**本物の 1 件がノイズに埋もれる。**
「隠すより見えるほうが安全」（`.gitignore` 冒頭）は**見える側が読まれる**ことを前提にしており、
**常に非 0 なら誰も読まない。**
★以前はこの逆の判断を書いていた（`ルール整合性チェック_v1.06.md` **D-25**）。
★**仮定が破れる場合**: ワークツリーの中にだけ新しく作られ、本体に一度も現れないソース。
★**ただし無視されている以上コミットできない**ので、
成果を本体へ取り込む時点で**欠落として現れる**（0 件の見張りより遅いが、静かには落ちない）。

★★**動作確認は「新規の未追跡ファイル」で行うこと。**★★
**すでに追跡されているファイルは `.gitignore` の影響を受けない**ので、
規則を元に戻すだけでは事故が再現しない。確かめるときは、
飲み込まれるはずの場所に**新しいファイルを置いて** `git status` に出ないことを見ること。

### ★ テスト件数の正はここ

**他の文書に書かれた件数は執筆時点のスナップショットであり、参照に留める。**
食い違ったらこの表を優先し、この表が古ければここを直す。

| パッケージ | 件数 | 確認コマンド |
|---|---|---|
| `loveca-data`（Python） | **53** | `python tests/run_all.py` |
| `loveca-core`（Dart） | **511** | `dart test` |
| `loveca-db`（Dart） | **247**（★skip 0） | `dart test` |
| `loveca-ui`（Flutter） | **1025**（★skip 0） | `flutter test` |
| `loveca-server`（Dart） | **240**（★skip 0） | `dart test` |

（`loveca-core` は **Phase 4 のレーン 3 の commit 15（起きたことを残す値）** 時点 / 2026-09-02。
`loveca-db` は **Phase 4 のレーン 5 の commit 22（N-10 の器 / `schemaVersion` 5）** 時点 / 2026-09-01。
`loveca-data` は **Phase 4 のレーン 2 の commit 4（画像だけのマニフェストを生成）** 時点 / 2026-08-31。
`loveca-ui` は **配信物を取りに行く口を置いた時点** / 2026-09-02。
`loveca-server` は **上限の枠と値を測った結果に合わせた時点** / 2026-09-02。★**5 件とも実測で確認済み**）

★★**器を引く経路（§32-6 の 23 の一部 / 決定 D140）で `loveca-db` が 227 → 247**★★
（**+20** = `deck_sync_mark_dao_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-server` / `loveca-ui` / `loveca-data` はこの commit で触っていない（★**511 / 240 / 1025 / 53 を実測で確認した**）。
★★**「門が開けば下流が着手できる」は★★4 回目も半分だった**★★ —— ★**W-14 の待っている門は「14 / 15」と書かれていたが、★★§24-8 がもう 1 つ下に在った★★**（★先例は **D126** / **D129** / **D130**）。
★★**§24-8 の訂正ではない**★★ —— ★**あちらは「決めていない。★★器の形に効かない★★」であり、★★器の形については正しい★★**。★★**引く経路に効くことを述べていないだけである**★★。★**字面は 1 文字も書き換えていない**（**D-35**）。
★★**目印の値の意味を決めた ＝ 印-1（最後に送った操作の id）**★★ —— ★**2 軸とも 印-1 の側で決まった**（★**(甲)** 印-2 は★★書く時点で値が確定していない★★ ／ ★**(乙)** 印-2 は★★「最初の id は 1 である」という前提を持ち込む★★）。★**好みが 1 つも入っていない**。
★★**「印-2 が劣る」とは書かない**★★ —— ★**同じ情報を 1 だけずらして持つ形であり、★到達する**。
★★**索引を足していない。★測って決めた**★★ —— ★**規約は「索引は★引く経路と一緒に足す」で、★この commit がその引く経路である**（★当てた）。★★**測ったら★索引を足しても速くならなかった**★★（★2026-09-02 / ★10,000 件 / ★**167.1 µs → 168.4 µs**）。★★**「速い」とも「索引が要らない」とも書かない**★★（**D-28**）。
★★**書く前に「0.05 ms」と書いた。★★偽だった★★**★★（**D-15 (j)** —— ★**測って直した。★出す前である**）。
★★**実測した対は 11 通り**★★（2026-09-02 / ★**戻してからハッシュを突き合わせた** / ★不一致 0 件）——
★★**(A) 印-2 にする** = 3 件★★（★★**D140-1** の対★★）／ ★**(B) デッキで絞らない** = 1 件 ／ ★**(C) 行が無くても空の基準を返す** = 3 件 ／
★**(D) `hasOpsSinceMark` を常に false にする** = 3 件 ／ ★**(E) `latestLogMark` がデッキで絞らない** = 1 件 ／ ★**(F) ログ 0 件で 1 を返す** = 1 件 ／
★**(G) 最初の id を返す** = 2 件 ／ ★**(I) `forget` が全部消す** = 1 件 ／ ★**(J) `forget` がログも消す** = 1 件 ／ ★**(K) `record` が `decks` も触る** = 1 件 ／ ★**(L) `record` がログを 1 件足す** = 5 件。
★★**対が 0 件の守りは 1 つも無かった**★★（**D-27**）。
★★**(K) と (L) は★最初 D-39 を踏んだ**★★ —— ★**式の本体を文にする仕込みで★★閉じ括弧が合わなくなった★★**（★通った件数が 247 → 4 に落ちた）。→ ★**本体ごと置き換える形に直して測り直した**。
★★**「同期が動くようになった」と読まないこと**★★ —— ★**呼ぶ側が★★1 人も居ない★★**。★**23 の残り 3 つ（★送る口 / ★預ける口を条件つきにする / ★配線）は★★1 行も無い★★**（★**D139-4** の口の細目が未決）。

★★**門 ウ（線 β）を洗って決めた（★★決定 D139★★）—— ★★件数は 5 パッケージとも動いていない★★**★★
（★**コードを 1 行も書いていない**。★**決めただけである**）。
★★**線 β ＝ サーバーが比べることを★★許す★★**★★（★β-2）—— ★**サーバーは★★衝突を 1 つも解かない★★**（★「取ったときの印が合わなければ断る」だけ）。★**解くのは依然アプリ側**（★主-1 / **D121-4**）。
★★**D105-2 の訂正ではない**★★ —— ★**あちらは「衝突解決はアプリ側」であり、★★『サーバーは一切比較しない』とは書いていない★★**（★§13-1 (b) が自らそう書き、★★その線が引かれていないことを **N-14** の論点 (2) にした★★）。★**線を引いたのである**（**D138** が **D108-2** の穴を埋めたのと同じ形）。★**字面は 1 文字も書き換えていない**（**D-35**）。
★★**塞いでいたのは「第 3 の書き込み」である**★★ —— ★**主-1 は「★取る → 解く → 書く」の 3 手を踏み、★★そのあいだの他端末の書き込みが黙って踏み潰される★★**。★★**これは (f-1) の衝突ではない**★★（★判定は★取った時点の版しか知らない）。
★★**決め手は「下流に新しい問いを作らないこと」である**★★ —— ★**消えないことを満たすのは β-2 と β-3 だけで、★★β-3（上書きされた版を残す）は★問いを 2 つ作る★★**（★捨てる規則 ＝ **N-16** と同型 ／ ★取り出す口 ＝ ★★利用者判断★★）。★**D119-1 が 後-1 を採ったのと同じ物差しである**。
★★**「β-3 が劣る」とは書かない**★★ —— ★★**到達する**★★（**D117-6** と同じ形）。★**利用者判断として **Q-09** に立てた**（★既定値は「残さない」＝ 今日の形）。
★★**これで **N-14** が閉じた**★★（★4 論点とも答えが在る）。
★★**口の細目は決めていない**★★ —— ★**印の字面 / ★状態コード / ★再試行の回数の 3 つは★★§32-6 の 23 を書くときに決まる★★**（★預ける口の署名が変わる）。★★**再試行の回数は **N-27** と当たる**★★。

★★**§32-6 の 15（解決で起きたことを残す値 / 決定 D119-1 ＝ 後-1）で `loveca-core` が 508 → 511**★★
（**+3** = `deck_edit_op_test.dart` の「語彙の範囲」の群。★**内訳はファイルを見ること**）。
★`loveca-db` / `loveca-server` / `loveca-ui` / `loveca-data` はこの commit で触っていない（★**227 / 240 / 1025 / 53 を実測で確認した**）。
★★**足したのは★語彙の 1 つだけである**★★（`DeckEditOpKind.resolveConflict`）—— ★**列も引数も 1 つも要らない**。★**`deck_edit_ops.kind` は TEXT なので★★`schemaVersion` は上がらない★★**（★確かめた）。
★★**勝った側で 2 つに分けない**★★ —— ★**「どちらが勝ったか」を種類に埋めると★★濃さが増える★★**。★**§29-4 が「引数を足せば濃くなる。★★倒さない★★」と定めており、★種類で足すのも同じである**。→ ★★**到達する。★落ちたのではない**★★（★§17-9-2 の 2 と同じ棚）。★**対で固定した**。
★★**キーは★記録点のメソッド名ではない**★★ —— ★**解決を書き込む所が★★1 つも無い★★**（★23 / 24 は未着手）。→ ★**出来事の名前を採り、★★記録点が付いてもキーを変えないことを対で固定した★★**（★変えると★送信済みのログが意味を失う）。
★★**「11 個目の操作」とは書かない**★★ —— ★**これは操作ではない**。★**同じ列挙に入れる理由は `deleteDeck` とまったく同じ**（★ログに 1 件残す点が同じ）。★**列挙の見出しの 2 行は 1 文字も書き換えていない**（**D-35**）。
★★**実測した対は 6 通り**★★（2026-09-02 / ★**戻してからハッシュを突き合わせた**）——
★★**(A) 種類を語彙から外す** = ★★対を組めない★★（★下）／ ★**(B) キーを識別子と違う字面にする** = 3 件 ／ ★★**(C) 勝った側で 2 つに分ける** = 1 件★★ ／
★**(D) `tryFromKey` を空にする** = 2 件 ／ ★**(E) キーを既存のものと同じ字面にする** = 6 件 ／ ★**(F) キーを空文字にする** = 6 件。
★★**(A) は★対を組めない。★型が守っている**★★ —— ★**列挙から値を外すと★★そのテストがコンパイルを通らない★★**（★**D-39** の判別法 1 が教えた ＝ ★★通った件数が 511 → 494★★）。★**改名しても★参照が一緒に動くので★何も落ちない**。→ ★**先例は §32-7 の commit 4 の (B)(D)**（★「★型でしか見えないものは、★型でしか守れない」）。★**その旨をテストの doc に書いた**。
★★**「衝突解決が動くようになった」と読まないこと**★★ —— ★**書き込む所も★見せる所も★★1 行も無い★★**（★23 / 24 / 25 は未着手）。

★★**§32-6 の 14（衝突解決を純粋関数として置く / 決定 D138）で `loveca-core` が 495 → 508**★★
（**+13** = `deck_resolution_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-db` / `loveca-server` / `loveca-ui` / `loveca-data` はこの commit で触っていない（★**227 / 240 / 1025 / 53 を実測で確認した**）。
★★**待っていた門を★★洗って開いた**★★（★**W-17** の「洗えば決められる。★洗っていない」の分 / **D115-8** ＝ ★(f-6) の決着層）。
★★**D108-2 の訂正ではない**★★ —— ★**あちらは「決着層は `decks.updatedAt`」であり、★★同値のときどうするかを 1 文字も述べていない★★**（**D115-8** が自らそう分けている）。★**足したのは★★2 番目の鍵★★である**。★**字面は 1 文字も書き換えていない**（**D-35**）。
★★**決め手は「収束するか」である。★好みではない**★★ —— ★**同-1（自分が勝つ）は★★収束しない★★**（★2 台が交互に同期すると★★永久に入れ替わり続ける★★ / ★1 手ずつ書いた）。★**「劣る」ではなく★★要求を満たさない★★**。
★★**残った 2 つの決め手は★従属である**★★ —— ★**同-2 の「相手側」は★★解く側から見た向き★★なので、★★軸 A が倒れると決着の向きが黙って裏返る★★**（**D121-9**）。★**同-3 は内容だけで決まる**。★**D121-4 が 主-1 を採ったのと同じ形である**。
★★**新しい量を 1 つも要らない**★★（★内容ハッシュは **D115-1** で既に在り、★器が 1 列で持つ / **D114-1**）—— ★**D125-2 が 帰-2 を採ったのと同じ形**。
★★**向き（大きい / 小さい）は★好みである。★隠さない**★★（**D138-2**）—— ★**根拠は無い**。★★**ただし利用者判断ではない**★★（★★2 つの向きは利用者から区別できない★★）。★★**しかも一度配ったら変えられない**★★（★向きの違う 2 版が同時に走ると★収束しない / **D105-5**）。
★★**柵が 1 つ増えた**★★（**D138-4**）—— ★**決着に渡す `updatedAt` は★★DB から読んだ値でなければならない★★**（★`save` は保存後に読み直さない / **D115-8** の測定 2）。★**破る経路は今日 0 本**。★★**柵そのものは実装で守れない**★★（★関数は値の出どころを知りようが無い）→ ★**破ったときに何が起きるかを★対で固定した**。
★★**D-27 の契機を 2 つとも引いた**★★ —— ★**(甲): 対は 2 つとも本番の関数を通る**（`resolveDeckConflict` / `deckContentHash`）／ ★**(乙): 柵を書いた瞬間に 1 段遡ると★★呼び出し側が 0 本 ＝ (乙) である★★** → ★**合成の入力で対を作った**。
★★**期待値を手で書かない**★★ —— ★**16 進の並びは★★中身と無関係である★★**ので、★**勝つ側は★テスト側でも計算して求める**（★手で書くと★★実装の写しになる★★）。
★★**実測した対は 10 通り**★★（2026-09-02 / ★**10 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(A) 段 2 を外す（＝ 同-1）** = 4 件 ／ ★**(B) 段 2 を常に受け取った版にする（＝ 同-2）** = 1 件 ／ ★**(C) 向きを反転する** = 2 件 ／
★**(D) 段 1 と段 2 の順序を入れ替える** = 6 件 ／ ★**(E) 内容が同じ場合を段 2 に畳む** = 3 件 ／ ★**(F) `revision` を段 2 の前に見る（＝ 同-4）** = 1 件 ／
★**(G) 削除された側を勝たせる** = 1 件 ／ ★**(H) 勝った側を作り直して返す** = 1 件 ／ ★**(I) 段 1 の向きを反転する** = 4 件 ／ ★**(J) 段 2 の理由を段 1 と同じにする** = 3 件。
★★**対が 0 件の守りは 1 つも無かった**★★（**D-27**）。★★**10 通りとも読み込みの失敗は 0 件である**★★（**D-39**）。
★★**測定の道具が★改行を書き換えた。★記録する（★手当てしていない）**★★ —— ★**Python が読み書きすると LF が CRLF になる**（★この機械 / `core.autocrlf=true`）。★**新しく置いたファイルだけがハッシュの突き合わせで当たった**（★既存のファイルは元から CRLF）。★**中身は 1 文字も変わっていない**（★確かめた）。
★★**「衝突解決が動くようになった」と読まないこと**★★ —— ★**呼ぶ側が 1 行も無い**（★§32-6 の 23 / 24 / 25 は未着手）。★★**起きたことを残す値（コミット 15）も入っていない**★★。

★★**§32-6 の 19（同定の量を持たせる / `schemaVersion` 5 → 6）で `loveca-db` が 208 → 227**★★
（**+19** = `migration_test.dart` の **v5 → v6 の群 19**。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-server` / `loveca-data` はこの commit で触っていない（★**495 / 240 / 53 を実測で確認した**）。
★`loveca-ui` は **1025 のまま**（★**走査テストの許可リストを 1 → 6 にしただけ**。★**テストを 1 件も足していない**）。
★★**待っている門が★まだ生きているかを先に当てた**★★（**D-15 (l)**）—— ★**待ち行列は **D125-3**（「残さない」の手段）と書いていたが、★★器については従属していない★★**。
★**残-1 でも 残-2 でも★★器の形は 1 文字も変わらない★★**（★従属しているのは★★切り替えの操作★★であって器ではない）。★**もう 1 つの入口（**D128-4** ＝ 端末の鍵）は★**D129** が消している**。
★★**ただし従属は消えていない。★★別の量に移っている★★**★★ —— ★**端末の同定は★★この器に入れていない★★**（★§34-7 の (2-a) が「実装の時点で決める」と書き、★★その時点は §32-6 の 26 ＝ 門 オ ＋ 門 ク の下流である★★）。★**入れると★残-2 の下で消える** ＝ ★★それは決定である★★（**D114-7** の理由 2）。
★★**新しく決めたのは 2 つだけである**★★ —— ★**(1)「まだ名乗っていない」は★★行の不在★★で表す**（★空文字を印にしない / ★先例は **D114-3**）／ ★**(2) 単一行を★★形で守る★★**（`CHECK (id = 0)`）。
★★**書く前の断定が★偽だった。★対が教えた**★★（**D-15 (j)** の (j-甲)。★**(j-乙) が働いた ＝ ★★出す前に走らせて分かった★★**）——
★**「既定値のままなら 2 行目は入らない」は★★偽である★★** —— ★**`INTEGER PRIMARY KEY` は★★rowid の別名で、★DEFAULT が無視される★★**（★実測: ★2 行入った）。
★★**先例が★同じリポジトリに在った**★★ —— ★**`master_state_dao.dart` の `setVersion` が「★id を明示する★」と★★同じ罠を doc に書いている★★**（★あちらは規約で守る）。→ ★**こちらは `CHECK` で塞いだ**（**D115-5**）。★**`master_states` は 1 文字も触っていない**。
★★**D-27 の★契機を★この回で初めて引いた**★★（★1 つ前の commit で決めた分）—— ★**守り（`CHECK`）を書いた瞬間に 1 段遡ると、★★上流の呼び出し側が 1 本も無い ＝ (乙) である★★**。→ ★**合成の入力で対を作った**。★★**1 例である。「作法が働く」とは書かない**★★（**§7-11**）。
★★**実測した対は 12 通り**★★（2026-09-02 / ★**12 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(A) `m.createTable(syncIdentities)` を外す** = 11 件 ／ ★★**(B) 巻き戻しから `DROP TABLE` を外す** = 1 件★★（★★要石★★）／ ★**(C) 枝が `backfillOrd()` を呼ぶ** = 3 件 ／
★**(D) 枝が `decks` を書き換える** = 5 件 ／ ★**(E) 枝が `deck_edit_ops` を消す** = 2 件 ／ ★**(F) 枝が `deck_sync_marks` を消す** = 1 件 ／
★★**(G) 枝が★既定の行を 1 つ入れる** = 7 件★★ ／ ★**(H) `CHECK (id = 0)` を外す** = 2 件 ／ ★**(I) 利用者名を nullable にする** = 1 件 ／
★**(J) 端末の同定の列を足す** = 1 件 ／ ★**(K) `decks` への外部キーを張る** = 2 件 ／ ★**(L) 主キーを `{id, userName}` にする** = 3 件。
★★**対が 0 件の守りは 1 つも無かった**★★（**D-27**）。
★★**(I) は★★最初 D-39 を踏んだ★★**★★ —— ★**nullable にすると companion の署名が変わり、★★構築点 7 か所がコンパイルを通らなくなった★★**。★**1 度目の「1 件」は★★読み込みの失敗だった★★**（★通った件数が 227 → 159 に落ちた ＝ **D-39** の判別法 1）。→ ★**構築点だけを機械的に直して測り直した**（★★assertion は 1 文字も触っていない★★）。
★★**測定の道具に不具合が 1 つ在り、★直した**★★ —— ★**同じファイルに 2 つ以上仕込むと、★★戻すときに★途中の状態を書き戻していた★★**（★ハッシュの突き合わせが検知した）。★**最初の中身だけを保つ形に直した**。
★★**「同期が動くようになった」と読まないこと**★★ —— ★**書き込み点も切り替えの操作も★★1 行も無い★★**（★★これは「空の器」である★★ / **D114-7** の理由 1 / 2 は解けていない）。

★★**D-27 に★契機を 2 つ足した（★★件数は 1 件も動かない★★）**★★ ——
★**引き金は相談役の指摘である**（★運転指示【0】(4)）—— ★「★★D-27 を作法にした同じ回に 2 回踏んだことは、★作法の弱さの証拠である★★」。
★★**2 件を分類した ＝ ★2 件とも (乙) である**★★（★★分類は働いた★★ —— ★2 件とも同じ型で、★同じ処置で直った）。
→ ★★**足りなかったのは分類ではなく「いつ当てるか」である**★★（★相談役の判定どおり）。
★★**契機を型ごとに 1 つずつ決めた**★★ —— ★**(甲) は★★対を 1 本書いた瞬間★★に「★その対が呼ぶものが本番も呼ぶものか」** ／
★**(乙) は★★守りを 1 つ書いた瞬間★★に「★上流に同じ判定が無いか。★★1 段だけ遡る★★」**。
★★**「測ったあと」ではなく「書いた瞬間」である。★そこが変わった点である**★★（★仕込みが 0 件だったあとに引く手順は、★★引く機会が来ない限り引かれない★★）。
★★**「1 段だけ」は★2 件で決めた規則である。★『2 段目には無い』とは書かない**★★（**D-28** —— ★標本は 2 点）。
★★**機械にできない。★言い切った**★★（★**D-27** の本文と同じ理由）。→ ★**だから契機を「書いた瞬間」に置いた**（★**D-2** —— ★手動の見張りは忘れられる）。
★★**D-27 も 1 つ目の追記も★訂正していない**★★ —— ★**本文も分類も見つけ方も 1 文字も動かない**（**D-35**）。
★★**前のセッションで置いた見張りが★また落ちた**★★（★**2 本**）—— ★**認証の走査の許可リストを 2 → 3 にした**（★理由つき / **D-30** —— ★★「名乗らない」と書いた行が語で当たる★★）／
★**日付の検査が★1 行を捕まえた**（→ ★★節の見出しに日付を書いた★★。★台帳の数は上げていない）。

★★**上限の枠と値を★測った結果に合わせた ★で `loveca-server` が 228 → 240**★★
（**+12** = `rate_limit_test.dart` の「枠」の群 **7** ＋ `sync_burst_test.dart` の書き直し **+5**。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-db` / `loveca-ui` / `loveca-data` はこの commit で触っていない（★**495 / 208 / 1025 / 53 を実測で確認した**）。
★★**引き金は相談役の判定である**★★（★運転指示【0】(3)）—— ★「★★『デッキ 5 つで断られる』は★値が間違っていることの証拠であって、★利用者判断を待つ理由ではない。★値を直すのに好みは要らない。★★現に断られる状態を残さないこと★★」。
★★**§57 が置いた合図が★鳴った**★★ —— ★**あの節は「値を動かすと `sync_burst_test.dart` が落ちる。★★それが合図である★★」と書いた**。→ ★**群を書き直した**（★**D-24** / **W-24** と同じ形の受け）。
★★**値を★測定から導いた。★好みを 1 つも入れていない**★★ —— ★**上限 ＝ 60000 ms ÷ ★★1573 ms★★（★2026-09-02 実測 / ★この機械 / ★本番の回数 600000）＝ ★★38 回★★** ／ ★**人が押す枠は据え置き 5** ／ ★★**同期の枠 ＝ 38 − 5 ＝ 33**★★。
★★**「上限を超える値は★もはや上限ではない」が★導き方の根拠である**★★ —— ★**窓に入りきらない仕事を通すと★★待ちが際限なく伸びる★★**。
★★**枠を 2 つに分けた**★★（**N-27** の 論点 (2)。★★あの節自身が「技術判断」と判定している★★）—— ★**合計（38）は分けても分けなくても同じで、★★変わるのは配り方である★★**。★**分けないと★38 回すべてを `/accounts` に使え、★★§54-2 が測った場面が 7.6 倍悪くなる★★**。
★★**覆う範囲は★入力ではなく★帰結である**★★ —— ★**1 台なら★デッキ 32 個まで ／ ★同じ住所の 2 台なら★15 個まで**。
★★**「もう断られない」とは書かない**★★ —— ★**デッキの数に上限は無い。★★D を大きくすれば必ず破れる★★**（★★形の帰結であって★値の問題ではない★★）。★**対で固定した**（★デッキ 33 個で断られる）。
★★**N-27 を閉じない**★★ —— ★**論点 (1)（★何を「同じ相手」とするか）と (3)（★一括で取る口）は★★1 ミリも動いていない★★**。★**問いは開いたままである**（**Q-07**）。
★★**「値が正しい」とは書かない**★★（**D135-5** は 1 ミリも動かない）—— ★**書けるのは「★★測った結果から導いた★★」までである**。
★★**差し替え点: ★機械が変われば値が変わる**★★ —— ★**1573 ms は★この機械の値である。★★60000 ÷ 測り直した値★★を上限として引き直すこと**（★規則は機械に依らない）。
★★**実測した対は 11 通り**★★（2026-09-02 / ★**11 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(A) 枠を 1 つに戻す** = 9 件 ／ ★**(B) 同期の枠の値を 5 に戻す** = 10 件 ／ ★**(C) 人が押す枠の値を 33 にする** = 8 件 ／
★**(D) 知らないパスを同期の枠に入れる** = 2 件 ／ ★**(E) 枠を無視して 1 つの勘定にする** = 9 件 ／ ★**(F) 上限を配る口より前に置く** = 8 件 ／
★★**(G) 同期の枠を合計の上限より大きくする（34）** = 5 件★★（★★導き方そのものの対★★）／ ★**(H) 枠ごとの勘定を同じ方針にする** = 4 件 ／
★**(I) `uniform` が同期の枠だけを埋める** = 6 件 ／ ★**(J) `of` がどの枠でも人が押す枠を返す** = 8 件 ／ ★**(K) 配る口の振り分けを外す** = (F) と同じ仕込み。
★★**対が 0 件の守りは 1 つも無かった**★★（**D-27**）。★★**11 通りとも読み込みの失敗は 0 件である**★★（**D-39**）。★**戻したあとハッシュを突き合わせ、★不一致は 0 件である**。
★★**置いた回のうちに★日付の検査が★自分の編集を捕まえた**★★ —— ★**doc に足した実測 3 行が日付を持っていなかった**。→ ★**台帳の数を上げず、★行に日付を書いて直した**。
★★**「同期が動くようになった」と読まないこと**★★ —— ★**呼ぶ側は★依然 0 行である**（★23 / 24 / 25 は未着手）。★★**変わったのは「もし呼んだら断られる」が「もし呼んだら通る」になったことだけである**★★。

★★**配信物を取りに行く口（アプリ側）を置いた ★で `loveca-ui` が 1011 → 1025**★★
（**+14** = `test/data/http_master_file_source_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-db` / `loveca-server` / `loveca-data` はこの commit で触っていない（★**495 / 208 / 228 / 53 を実測で確認した**）。
★★**§32-6 の 8 の★4 番目である**★★（**D137-6** の表）。★**16 / 18 / 21 の口と同じ形**（**D131-3** / **D132-4**）—— ★**`HttpClient` と `Uri` を★呼び出し側から渡す**。★★**`AppSettings` に足さない**★★（★それは画面の仕事）。
★★**D43 を踏まない。★1 文字も動かさない**★★ —— ★**この口は★★取り込みの側★★であって、★★読む側ではない★★**。★**読むのは今日どおり `CardImageSource` の 1 本**（**D137-1** ＝ 画経-4）。
★★**「期限が切れたから何でもよい」とは書かない**★★ —— ★**1 本という形が理由である。**
★★**答えを 3 つに分けない。★投げる**★★（★16 / 18 / 21 とは形が違う）—— ★**`MasterFileSource` の契約が「読めなければ投げる」で、★`MasterImporter` が★ファイルごとに受けて 1 件ずつ記録する**（★どちらも実読）。→ ★★**3 つに分けると★取り込み層が 1 つも見ない。★選んだのではない**★★。
★★**理由は畳まない**★★ —— ★**状態コードを字面で持つ**（★404 と 500 の理由が別であることを★対で固定した）。★★**名乗らない**★★（**D133-7** —— ★資格情報を 1 バイトも送らない / ★対で固定した）。
★★**やり取りの形が★2 か所に書かれる**★★（**D126-3** の代償）—— ★**前置が両側で同じことを★走査で見張る**（★★引用符ごと見る★★ / **D-37 の裏**）。
★★**サーバーの実装を 1 度も呼ばない**★★（**D126-3**）—— ★**自前の待ち受けを立てて★TLS 越しに往復した**。→ ★★**「端から端まで 1 回で通した」とは書かない**★★（★両側とも★自分の側の試験を持つ）。
★★**実測した対は 6 通り**★★（2026-09-02 / ★**6 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(A) 前置を変える** = 10 件 ／ ★**(B) 住所のパスをそのまま使う** = 1 件 ／ ★**(C) 状態コードを見ない** = 3 件 ／
★**(D) 理由を畳む** = 2 件 ／ ★**(E) 読んだ path の記録を消す** = 2 件 ／ ★**(F) 資格情報を送る** = 1 件。
★★**6 通りとも読み込みの失敗は 0 件である**★★（**D-39** —— ★**14 件が毎回すべて走った**）。
★★**前のセッションで置いた見張りが★また落ちた**★★ —— ★**許可リストに 1 行足した**（★理由つき / **D-30**）。
★★**「配信が動くようになった」と読まないこと**★★ —— ★**呼ぶ側は★★1 行も無い★★**（★§32-6 の 8 の 1 / 2 / 3 は未着手 ＝ ★端末の置き場 / ★保存する実装 / ★取り込みへの配線）。

★★**配信物を配る口（サーバー側）を置いた ★で `loveca-server` が 198 → 228**★★
（**+30** = `test/dist_endpoint_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-db` / `loveca-data` はこの commit で触っていない（★**495 / 208 / 53 を実測で確認した**）。
★`loveca-ui` は **1011 のまま**（★**走査テストの許可リストに 1 行足しただけ**。★**テストを 1 件も足していない**）。
★★**§32-6 の番号を持たない**★★ —— ★**一覧を返す口（§55-3）と同じ格である**。★**8 が「取りに行く相手」を要るのに★★1 つも無かった★★**。
★★**門 キ が開いたから書けた**★★（**D137** —— ★2026-09-02）。★**決まっていたのは **D120-1**（同じサーバーに置く）／ **D130-7**（パスで分ける）／ **D133-7**（配信は名乗りを要求しない）である**。
★★**この口だけ★名乗りを要求しない。★この口だけ★上限を掛けていない**★★ —— ★**名乗りを 1 度も見ないので★★固める処理（1.5 秒）を 1 度も通らない★★**。★★**守っていない。★隠さない**★★（★**N-27** の入力にした）。
★★**柵は 2 段**★★（★区切りごとに見る ／ ★繋いだ結果が根の下に在ることを確かめる）—— ★**D134-7** と同じ型（★受け取った字面をそのまま繋がない）。
★★**測って断定が 1 つ偽だった**★★（**D-15 (j)**）—— ★**「`Uri.path` は復号済みで渡る」は★★偽である★★**（★実測: 符号化されたまま返る）。→ ★**`Uri.pathSegments` へ直した**。★**字面は書き換えず隣に置いた**（**D-35**）。★★**出る前に対が教えた**★★。
★★**D-27 を踏んだ。★柵の対が★柵に 1 度も届いていなかった**★★ —— ★**`Uri.parse` が★★上へ抜ける段を自分で畳む★★**（★実測）。→ ★**柵を丸ごと外しても★★21 件が全部通った★★**。
→ ★**手当てを 3 つ打った**（★素の口で当てる ／ ★★`DistFileStore.read` に直に当てる★★ ／ ★「根の中なら読める」の対）。
★★**対が 1 つも無い守りが★もう 1 つ見つかった。★足した**★★ —— ★**「先頭の段が `dist` か」は★振り分けを通る限り必ず真である**（★外しても 30 件が全部通った / ★先例は §32-7 の (L)）。→ ★**何でもこの口へ流す待ち受けを立てて当てた**。
★★**重ねた手当ての片方だけを外しても落ちない。★隠さない**★★ —— ★**段 1 だけ = 0 件 ／ ★段 2 だけ = 0 件 ／ ★★両方 = 1 件★★**。★**段 2 だけを見る対は★置けなかった**（★段 1 を通って★なお根の外を指す段が★作れない）。★**記録した。★手当てはしていない**（**D-28**）。
★★**実測した対は 9 通り**★★（2026-09-02 / ★**9 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(A) 段 1 の呼び出しを外す** = 0 件 ／ ★**(B) 段 2 を外す** = 0 件 ／ ★★**(C) 両方外す** = 1 件★★ ／
★**(D) 段ごとの判定を空回りさせる** = 5 件 ／ ★★**(E) 先頭の段の判定を外す** = 2 件★★ ／ ★**(F) GET の判定を外す** = 1 件 ／
★**(G) 段を 1 つ食べない** = 8 件 ／ ★**(H) 上限の対象にする** = 1 件 ／ ★**(I) 中身を文字列に畳む** = 2 件。
★★**9 通りとも読み込みの失敗は 0 件である**★★（**D-39** —— ★**30 件が毎回すべて走った**）。
★★**依存が 2 件目になった**★★ —— ★**`path`**（★配信物の中の相対パスを安全に繋ぐ）。★**手で書ける。★書かない理由を pubspec に書いた**（**D115-2** の作法）。
★★**前のセッションで置いた見張りが★また落ちた**★★ —— ★**許可リストに 1 行足した**（★理由つき / **D-30** —— ★★「名乗らない」と書いた行が語で当たる★★）。
★★**【要確認/法務】引き金が 4 つ目になった**★★ —— ★★**カードのデータと画像が★他人の機械経由で渡る**★★。★**動かす前に **D104** の札を引き直すかを利用者が判断すること**（★**書く前ではない** / **D133-5**）。
★★**2026-09-02 訂正: ★「4 つ目」は★★列を取り違えている。★上の行は 1 文字も書き換えない**★★（**D-35**）—— ★★**別の列（★著作の列）の 1 つ目である**★★。★**3 軸とも割れた**（★誰の物か / ★誰が損なうか / ★★やめれば戻るか★★）。★**正は `docs/同期設計メモ.md` §61 と `決定 D104` の詳細である**（**D-15**）。★★**§60-6 の訂正ではない**★★（★引き金であることについては正しい）。★★**この列のほうが★早く発火しうる**★★（★配る口は名乗りを要求しないので、★★アカウントが 1 つも無くても配れる★★）。
★★**「配信が動くようになった」と読まないこと**★★ —— ★**取りに行く側は★★1 行も無い★★**（★§32-6 の 8 の 1 / 2 / 3 / 4 とも未着手）。

★★**§7 の 6（タッチで `Tooltip` が読めるか）を測った ★で `loveca-ui` が 1003 → 1011**★★
（**+8** = `test/ui/touch_tooltip_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-db` / `loveca-server` / `loveca-data` はこの commit で触っていない（★**495 / 208 / 198 / 53 を実測で確認した**）。
★★**直していない。★判断もしていない**★★ —— ★**§7 の 6 が「★判断は Phase 5。★★いま決めない★★」と★自ら書いている**。★**いまの挙動を固定した**（★先例は **D-24** / **W-24**）。
★★**実測**★★（2026-09-02 / ★**本番の `CardGrid` を通した**）—— ★**マウスを乗せると出る**（PC）／ ★★**触って長押しすると★出る**★★（モバイル）／ ★**触って叩くだけでは出ない**。
★★**「タッチでは `Tooltip` が出ず」は★読みによって真偽が変わる**★★（**§7-7**）—— ★**(i)「出す手立てが目に入らない」は★真** ／ ★★**(ii)「出す方法が 1 つも無い」は★偽である**★★。
★★**「同じ形が R2〜R6 にもある」も★読みで割れる**★★（★走査 / ★陽性対照つき）—— ★**(i)「`Tooltip` でしか読めない情報」は★真**（★盤面の外に 2 件）／ ★★**(ii)「無効なボタンの理由」は★偽である**★★（★盤面の外は★★0 件★★）。
★★**「§7 の 6 が誤り」とは書かない**★★ —— ★**あちらは★どの読みかを述べていない**。★**字面は 1 文字も書き換えていない**（**D-35**）。
★★**エミュレータは要らなかった**★★ —— ★**`tester.longPress` とマウスのポインタで足りた**（★★実機の指の速度・面積は★この検査に入らない —— ★それは **W-26** の相手である★★ / **§7-10**）。
★★**実測した対は 4 通り**★★（2026-09-02 / ★**4 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(A) `card_grid.dart` の `Tooltip` を外す** = 6 件 ／ ★**(F) 文言から刷り番号を落とす** = 3 件 ／
★**(C) 走査の字面を括弧なしに広げる** = 1 件 ／ ★**(D) 盤面かどうかの前置で分けない** = 1 件。
★★**4 通りとも読み込みの失敗は 0 件である**★★（**D-39** —— ★**8 件が毎回すべて走った**）。
★★**D-38 を★測っている途中で踏んだ**★★ —— ★**道具の経路で★★2 つ続けた逆斜線が 1 つに畳まれ★★、★仕込みの当て先が 2 度とも見つからなかった**。
→ ★**`chr(92)` で組み立てて直した**。★★**本番のファイルは 1 バイトも壊れていない**★★（★確かめた）。

★★**「測った日付が併記されていない実測」を数える検査を置いた ★で `loveca-ui` が 995 → 1003**★★
（**+8** = `test/docs/measurement_date_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-db` / `loveca-server` / `loveca-data` はこの commit で触っていない（★**495 / 208 / 198 / 53 を実測で確認した**）。
★★**引き金は相談役の指示である**★★（★運転指示【0】(6) の 7）—— ★「★走査する価値が在る。★★ただし『古い数』を機械で見つける手段は無い★★。★『測った日付が併記されていない実測』を探す形にすること」。
★★**「古い数」は探せない**★★ —— ★**古くなったことは★★字面に 1 文字も現れない★★**（★実例は `docs/UI設計メモ.md` §5-5 の 191 MB → 571 MB。★**doc の側は 1 バイトも変わっていない**）。
★★**0 件の検査ではない。★増やさないための検査である**★★ —— ★**いま在る件数は台帳（`_baseline`）が持つ。★★一斉に直さない★★**（**D-28** が「既存の測定値の一斉監査はしない」と自ら定めている）。
★★**完全一致にした。★「増えたときだけ落ちる」形には穴が在る**★★ —— ★**72 から 70 へ減り、★次に 71 へ戻ると、★★71 は 72 を超えないので新しい 1 件が隠れる★★**。
★★**代償を隠さない**★★ —— ★**doc に値つきの実測を 1 行足すたびに落ちる**（★型は **D-25** の別の形）。→ ★**手当て: ★★落ちたときの文言が行番号を並べる★★**ので、★**直し方は「日付を書く」であって「数を合わせる」ではない**（★実測で確かめた）。
★★**走る木を絞った。★範囲外を書いた**★★（**D-31**）—— ★**`docs/相談役への報告/` と `docs/セッション報告/` は見ない**（★★ファイル名が日付であり、★増え続けるので非 0 が常態になる★★ / **D-25**）。
★★**数字を持たない「実測」は拾わない**★★ —— ★**値を持たない行は★★古くなりようが無い★★**（★対で固定した）。
★★**日付は「節」で見る**★★（★直前の見出しからその行まで）—— ★**節の見出しに日付を書く形が実物に多い**。★**見出しを跨いだ日付が効かないことも対で固定した**。
★★**台帳の数を★別の実装で検算した**★★ —— ★**Python で独立に数え、★14 ファイルとも一致した**（**D115-2** と同じ作法）。
★★**実測した対は 5 通り**★★（2026-09-02 / ★**5 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(A) 節の区切りを外す** = 2 件 ／ ★**(B) 数字の要求を外す** = 2 件 ／ ★**(C) 日付が何にでも当たるようにする** = 3 件 ／
★★**(D) doc に★日付なしの実測を 1 行足す** = 1 件★★（★**本命の受け**。★★文言が行番号を出した★★）／ ★**(E) 走る木から `docs` を落とす** = 1 件。
★★**5 通りとも読み込みの失敗は 0 件である**★★（**D-39** —— ★**8 件が毎回すべて走った**）。
★★**「実測が古くないと分かった」と読まないこと**★★ —— ★**分かるのは★★日付が書かれているかだけである★★**。
★★**置いた回のうちに★自分の編集を捕まえた**★★（2026-09-02 / ★**同じセッション**）——
★**§7 の対応づけを待ち行列に書いたとき、★★「x86_64 のエミュレータで実測」の行が★日付を持っていなかった★★**。
→ ★★**台帳の数を上げず、★行に日付を書いて直した**★★（★**この検査が自ら定めた直し方そのものである**）。
★★**「検査が働くことの証明」と書かない**★★ —— ★**証明ではない。★★1 件当たっただけである★★**（**§7-11**）。

★★**既定の上限が★1 回の同期と当たることを測った（★**D135** ＝ 門 ソ）で `loveca-server` が 192 → 198**★★
（**+6** = `test/sync_burst_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-db` / `loveca-ui` / `loveca-data` はこの commit で触っていない（★**495 / 208 / 995 / 53 を実測で確認した**）。
★★**実装は 1 行も変えていない**★★（★`rate_limit.dart` も `auth_endpoint.dart` も。★**いまの挙動を固定しただけである**）。
★★**引き金は相談役の指示である**★★ —— ★「★『同じ相手 ＝ つないできた住所』が、★★既に返っている利用者の答えと当たっていないか★★を確かめること。★★当たると断じていない。★在るかを見る★★」。→ ★★**在った**★★。
★★**上限は★全パスに効いている**★★（★実読: ★振り分けの★★前★★で見る ／ ★実測: ★上限を外すと 20 回通る ＝ **D-10** の陽性対照）。
★★**1 台の 1 回の同期は「1 ＋ デッキの数」回投げる**★★ —— ★**サーバーは内容ハッシュを持たない**（**D114-1** / **D124-7**）ので★★中身を取らないと比べられない★★。★**呼ぶ側に再試行は 0 件**（★走査）。
★★**デッキが 5 つあると★1 台の 1 回の同期が断られる**★★（★実測 / ★TLS 越しに本物の待ち受けへ投げた）。
★★**同じ住所の 2 台が★デッキ 2 つずつ同期すると断られる**★★（★実測 / ★**対: 1 台だけなら通る**）—— ★**D107-2**（同じデッキを 2 台で編集する）と★正面から当たる。
★★**「代償を書いてあったから既知である」とは書かない**★★（**D135-3**）—— ★`rate_limit.dart` は「同じ出口を通る人が 1 つの枠を分け合う」と★★書いていた★★が、★★**その枠で★1 回の同期が通るかは★1 度も測っていなかった**★★（★型は **D-28**）。★**代償の字面は 1 文字も書き換えていない**（**D-35**）。
★★**直していない。★いまの挙動を固定した**★★（**D135-4**）—— ★**値を動かすと★この群が落ちる。★★それが合図である★★**（★先例は **D-24**）。
★★**「値が正しい」とは書かない。★「置いた」とだけ書く**★★（**D135-5** —— ★相談役がそう課した）。
★★**「同期が壊れている」とも読まないこと**★★ —— ★★**今日★実害は 0 である**★★（★**呼ぶ側が 0 行** ＝ §32-6 の 23 / 24 / 25 は未着手 ／ ★**本番では動かしていない**）。
★★**実測した対は 4 通り**★★（2026-09-02 / ★**4 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(A) 上限を★作る口・名乗る口だけにする** = 3 件 ／ ★**(B) 既定の回数を 60 にする** = 3 件 ／
★**(C) 数える鍵を★口ごとに分ける** = 3 件 ／ ★**(D) 断った要求も数える** = 4 件。
★★**4 通りとも読み込みの失敗は 0 件である**★★（**D-39** —— ★**6 件が毎回すべて走った**）。
★★**門は 13 → 14 になった**★★（★**§32-5 / §32-6 の表も過去の追記も 1 文字も書き換えない** / **D-35**）。

★★**§7 の 1（`thumb` がモバイルの物理セル幅を下回るか）を測った ★で `loveca-ui` が 989 → 995**★★
（**+6** = `test/ui/mobile_image_tier_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-db` / `loveca-server` / `loveca-data` はこの commit で触っていない（★**495 / 208 / 192 / 53 を実測で確認した**）。
★★**エミュレータは要らない**★★ —— ★**`tester.view.physicalSize` と `devicePixelRatio` を渡せば済む**（★`card_art_test.dart` が★DPR 3 で同じことをしている）。
★★**実測**★★（★電話の画面 1080 × 2400 / DPR 2.625）—— ★**3 列 / ★セル論理 129.14 / ★★物理 339★★ / ★デコード 200**。
→ ★★**§7 の 1 の断定「モバイルは… 原寸 200px を超える」は★真である**★★（★あちらは「300〜400」と幅で書いていた。★**実測を固定した**）。
★★**書きかけた断定が 1 つ偽だった。★対が教えた**★★（**D-15 (j)**）—— ★**「デコード幅が 200 を超える」と書いたが、★★`cacheWidthPx` は原寸で切られる★★**（★実読）。
→ ★**測るのは★★物理セル幅★★であって★デコード幅ではない**。★**「デコードは 200 で頭打ち → 339 へ引き伸ばされる」が★★ぼやける の中身である★★**。
★★**手当ては★既に在る。★一覧が呼んでいないだけである**★★ —— ★**`cardImageSizeFor`（**D82**）は★同じ入力で `normal` を選ぶ**（★対で固定した）。★**呼ぶのは盤面（`board_slot.dart`）だけである**。
★★**直していない。★いまの挙動を固定した**★★ —— ★**直すと★この群が落ちる。★★それが合図である★★**（★先例は **D-24**）。
★**直すには★§3-3 のキャッシュの見積り（1 枚 74 KB / 1000 枚）を★★測り直す必要がある★★**（★§7 の 1 が自らそう書いている）。
★★**対は 4 つ置いた**★★ —— ★**PC / DPR 1 では原寸を超えない**（★陽性対照）／ ★**PC では `cardImageSizeFor` も `thumb` を選ぶ**（★「何を渡しても `normal`」で通らないこと）／ ★**デコードは原寸で頭打ち**／ ★**段は `thumb` のまま**。

★★**§32-6 の 21 の★口★を実施した（★相手の版を取りに行く）で `loveca-ui` が 968 → 989**★★
（**+21** = `test/data/deck_sync_client_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-db` / `loveca-server` / `loveca-data` はこの commit で触っていない（★**495 / 208 / 192 / 53 を実測で確認した**）。
★★**口だけである。★呼ぶ側は 1 行も無い**★★ —— ★**いつ取りに行くか / ★どう見せるかは★★23 / 25 であり、★どちらも未着手★★**（★**18 の口と同じ形** / **D132-4**）。
★★**2 つの口を呼ぶ**★★ —— ★**一覧**（★どのデッキが預けてあるかを知る手段が他に無い）＋ ★**1 つ取る**（★内容を受け取る / **D124-7** ＝ 門 イ の (c)）。
★★**答えを 4 つに分けた**★★ —— ★取れた / ★名乗れなかった / ★★**そのデッキが無い**★★ / ★通信が失敗した。
★★**「そのデッキが無い」を★通信の失敗に畳まない**★★ —— ★**「まだ預けていない」は★★正しい答えである★★**（★★これが分からないと初回の同期が書けない★★）。★**対で固定した**。
★★**一覧の 404 は★別である**★★ —— ★**一覧の口に 404 は無い**（★空の一覧は 200）ので、★**返ったら「知らないパス」であり★期待どおりでない**。★**対で固定した**。
★★**中身を 1 バイトも解釈しない**★★（**D105-2**）—— ★**文字列のまま返す**。★**`Deck` に組み立てない**（★組み立て方は未決 / **D116-2** の理由 3）。
★★**429 も通信の失敗である**★★ —— ★**上限に当たったとき★資格情報は正しい**。★**「名乗れなかった」に畳まない**。
★★**サーバーの実装を 1 度も呼ばない**★★（**D126-3**）—— ★**自前の待ち受けを立てて★TLS 越しに往復した**。
→ ★★**「端から端まで 1 回で通した」とは書かない**★★（★両側とも★自分の側の試験を持つ）。
★**パスと鍵の名前が両側で同じことを★走査で見張る**（★引用符ごと見る / **D-37 の裏**）。
★★**実測した対は 14 通り**★★（2026-09-01 / ★**14 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(A) 一覧の 404 も「無い」にする** = 1 件 ／ ★**(B) 1 つ取る口の 404 を通信の失敗にする** = 1 件 ／ ★**(C) 401 を通信の失敗に畳む** = 1 件 ／
★**(D) 200 以外を全部「名乗れなかった」に畳む** = 3 件 ／ ★**(E) `ok` の判定を外す** = 1 件 ／ ★**(F) 一覧のパスを変える** = 1 件 ／
★**(G) 1 つ取るパスを変える** = 1 件 ／ ★**(H) 送る鍵の名前を変える** = 2 件 ／ ★**(I) 一覧でも deckId を送る** = 1 件 ／
★**(J) 中身を JSON として解釈する** = 1 件 ／ ★**(K) 列の非文字列を通す** = 1 件 ／ ★**(L) つながらないときに投げる** = 2 件 ／
★★**(M) 住所の scheme を https に書き換える** = 1 件★★ ／ ★★**(N) 取り出しの結果を見ない** = 1 件★★。
★★**(M) と (N) は★最初 0 件だった。★★2 件とも★対の形が悪かった★★**★★（**D-27** —— ★§51 に続いて★★同じセッションで 2 度目である★★）——
★**(M)** は★**誰も待ち受けていない港へ投げていた** → ★★**書き換えても書き換えなくても★つながらないので★同じ答えになる**★★ → ★**TLS で本当に待ち受けている港へ★素の HTTP で投げる形に直した**（★対つき） ／
★**(N)** は★**型だけを見ていた** → ★★**取り出しの失敗を★型の変換の例外に落としても★同じ `SyncUnreachable` になる**★★ → ★**理由の字面まで見る形に直した**。
★★**3 つの原因に 1 つずつ当てた**★★ —— ★**2 件とも (a) 対の形である**（★本命は空振りしていない ／ ★仕込みは弱くない）。
★★**14 通りとも読み込みの失敗は 0 件である**★★（**D-39**）。
★★**前のセッションで置いた見張りが★また落ちた**★★（★**3 日で 7 回目**）—— ★**パスで免除した**（★`deck_endpoint.dart` と★★同じ理由 —— ★あちらがサーバー側、★こちらがアプリ側★★）。
★★**「同期が動くようになった」と読まないこと**★★ —— ★**取りに行く口が在るだけで、★★呼ぶ側も★送る口も★1 行も無い★★**。

★★**写しの突き合わせを機械に移した（`docs/利用者への問い.md` の受け 2）で `loveca-ui` が 958 → 968**★★
（**+10** = `test/docs/user_question_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-db` / `loveca-server` / `loveca-data` はこの commit で触っていない（★**495 / 208 / 192 / 53 を実測で確認した**）。
★★**引き金は★新しい文書が★写しを持ったことである**★★ —— ★**問いの文言の正は `docs/同期設計メモ.md` の各節で、★★移すと既存の記録を書き換えることになる★★**（**D-35**）。
→ ★**写しを置き、★★食い違いを機械で見る**★★（★受け 1 の「正はここと書く」は★★人の規律であり忘れられる★★ / **D-2**）。
★★**見るのは候補の行だけである。★背景は見ない**★★ —— ★**背景は言い換えられるが、★★候補は選ばれたものを指す字面である★★**。
★★**除外を別に書かない**★★ —— ★**「この節が正である」の見出しは★★そもそも当たらない★★**（★見出しが「写し。★正は」を要求する）。
★**最初は除外の判定を別に置いた。★★どの入力でも 1 度も効かないことが分かって外した★★**（★型は **D-20**）。
★★**実測した対は 10 通り**★★（2026-09-01 / ★**10 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(A) 写しの候補を 1 文字変える** = 1 件 ／ ★**(B) 候補を 1 行落とす** = 1 件 ／ ★**(C) 「差し替え点」の行を落とす** = 1 件 ／
★**(D) 見出しの字面を変える** = 4 件 ／ ★**(E) 見出しから「写し。★正は」の要求を外す** = 2 件 ／
★**(F) 候補の判定を常に false にする** = 4 件 ／ ★**(G) 正のファイルを別のものにする** = 2 件 ／
★**(H) 突き合わせを常に真にする** = 1 件 ／ ★**(I) 問いの節の取り出しを空にする** = 1 件 ／ ★**(J) 引用の判定を外す** = 1 件。
★★**(H) と (J) は★最初 0 件だった。★★2 件とも★対の形が悪かった★★**★★（**D-27** —— ★§47 / §51 に続く）——
★**(H)** は★**対が `contains` を★★直に呼んでおり、★本番の突き合わせを 1 度も通っていなかった★★** → ★**同じ関数を通す形に直した** ／
★**(J)** は★**実物に「引用の外の表が★写しの節の中に在る」形が★★今日 1 つも無い★★** → ★**合成の入力で固定する対を足した**。
★★**3 つの原因に 1 つずつ当てた**★★ —— ★**(H) は (a) 対の形**（★本命は空振りしていない ＝ (A) が落ちる）／ ★**(J) も (a) 対の形**（★仕込みは弱くない ＝ 判定を丸ごと緩めている）。
★★**D-38 を実際に踏んだ**★★ —— ★**改行のエスケープを含むコードを★道具の経路で書こうとして、★★本物の改行に化けた★★**。
→ ★**このファイルには★★エスケープを 1 つも書かない形にした★★**（★繋ぎの字を全角にした）。★**doc にそう書いた**。
★★**10 通りとも読み込みの失敗は 0 件である**★★（**D-39**）。

★★**呼ばれる回数の上限を置いた（★**N-26** の★★既定値★★）で `loveca-server` が 176 → 192**★★
（**+16** = `test/rate_limit_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-db` / `loveca-data` はこの commit で触っていない（★**495 / 208 / 53 を実測で確認した**）。
★`loveca-ui` は **958 のまま**（★**走査テストの許可リストに 1 行足しただけ**。★**テストを 1 件も足していない**）。
★★**これは★決定ではない。★★答えが返るまでの仮の値である**★★（★**N-26** の (1) は★★利用者判断のまま★★）。
★★**根拠と差し替え点の正は `docs/利用者への問い.md` の **Q-01** である**★★（**D-15**）。
★★**限-1 を★★値として含む形にした**★★（**D114-7** の理由 2 を踏まない）—— ★**「断らない」は★同じ型の 1 つの値である**。★**限-1 が答えなら★渡す値を変えるだけで戻る**。
★★**限-4 には届かない。★隠さない**★★ —— ★**呼ぶ側に手間を課す形は★アプリ側の実装が要る**（★§32-6 の 18 の口が変わる）。
★★**「同じ相手」＝ つないできた住所である**★★ —— ★**利用者名では数えられない**（★★呼ぶ側が自由に決める★★ / **D133-4**）。★**代償 3 つを doc に書いた**（★同じ出口を通る人が枠を分け合う ほか）。
★★**上限は★名乗りを見る★★前★★に効く**★★ —— ★**あとに置くと守っていない**（★対で固定した ＝ ★壊れた要求でも 429 になる）。
★★**断った要求は数えない**★★ —— ★**数えると★窓が埋まったままになる**（★押し続けた相手が永久に入れない）。★**対で固定した**。
★★**時間を測る検査は 1 つも無い**★★（**D-28**）—— ★**時刻を★渡して動かす**（`serveApi` / `RateLimiter` とも `clock` を受け取る）。
★★**doc に書いた断定が 1 つ偽で、★★対が教えた**★★ —— ★**「覚えている量は窓で頭打ちになる」は★★引かれた相手だけを掃除する形では偽である★★**（★二度と来ない相手が永久に残る）。
→ ★**全部の相手を掃除する形に直した**。★★**型は D-15 (j)。★★出す前に見つかった★★**（★走らせて分かった。★書く前ではない）。
★★**実測した対は 13 通り**★★（2026-09-01 / ★**13 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(A) 上限を見ない** = 6 件 ／ ★**(B) 上限を振り分けのあとに置く** = 5 件 ／ ★**(C) 既定を「断らない」にする** = 1 件 ／
★**(D) 断った要求も数える** = 4 件 ／ ★**(E) 掃除を引かれた相手だけにする** = 1 件 ／ ★**(F) 掃除を外す** = 4 件 ／
★**(G) 相手ごとに分けない** = 2 件 ／ ★**(H) 断らない方針でも数える** = 1 件 ／ ★**(I) 既定の回数を 1 つ増やす** = 2 件 ／
★**(J) 断らない方針の `refuses` を true にする** = 3 件 ／ ★**(K) 断るときに送られた値を返す** = 1 件 ／
★**(L) 429 ではなく 401 を返す** = 6 件 ／ ★**(M) 待ち受けごとではなく要求ごとに数え直す** = 6 件。
★★**対が 0 件の守りは 1 つも無かった**★★（**D-27**）。★**13 通りとも読み込みの失敗は 0 件である**（**D-39**）。
★★**前のセッションで置いた見張りが★また落ちた**★★（★**3 日で 6 回目**）—— ★**許可リストに 1 行足した**（★理由つき / **D-30**）。
★★**「同期が動くようになった」と読まないこと**★★ —— ★**呼ぶ側は依然 0 行である**。

★★**一覧を返す口を置いた（★§55-3 の穴）で `loveca-server` が 153 → 176**★★
（**+23** = `test/deck_list_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-db` / `loveca-ui` / `loveca-data` はこの commit で触っていない（★**495 / 208 / 958 / 53 を実測で確認した**）。
★★**§32-6 の番号を持たない**★★ —— ★**20 の続きであって 21 ではない**（★21 は★★呼ぶ側★★の話である）。
★★**§55-3 が「一覧を返す口が無い。★どのデッキが預けてあるかを★呼ぶ側は知りようが無い」と書いた分である**★★（★型は **D-15 (l)** の受け）。
★★**返すのは `deckId` だけである。★選んだのではない**★★ —— ★**サーバーは内容ハッシュも目印も持たない**（**D114-1** / **D124-7**）。★★**返せるものが他に無い**★★。
★★**並びは字面の昇順。★規則順ではない**★★ —— ★**規則順は `loveca_core` の `deck_order.dart` が持つ**（**D99**）が、★★線 α は空のままである★★（**D115-6** / **D126-3**）。
★**並べる理由は★2 台が同じ集合を持っても並びが違うと★呼ぶ側が比べられないからである**（★保管した順を返さない / ★対で固定した）。
★★**空の一覧は 200 である。★404 にしない**★★ —— ★**「1 つも預けていない」は★★答えであって不在ではない★★**。★**返す口の 404 とは★意味が違う**（**§7-7**）。
★★**振り分けは完全一致である**★★ —— ★**`/decks/list` が `/decks` に吸われない**（★接頭辞で振り分けていない）。★**対で固定した**。
★★**実測した対は 12 通り**★★（2026-09-01 / ★**12 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(A) 並び替えを外す** = 3 件 ／ ★**(B) 引数の利用者名を見ない** = 8 件 ／ ★**(C) 空の一覧を 404 にする** = 3 件 ／
★**(D) 中身も返す** = 1 件 ／ ★**(E) 名乗りを見ない** = 2 件 ／ ★**(F) 振り分けからパスを落とす** = 17 件 ／
★**(G) 振り分けが預ける口へ送る** = 13 件 ／ ★**(H) 一覧でも deckId を必須にする** = 12 件 ／
★**(I) 空の判定を丸ごと外す** = 2 件 ／ ★**(J) 常に空を返す** = 10 件 ／ ★**(K) メソッドの検査を外す** = 1 件 ／
★**(L) deckId ではなく中身を返す** = 10 件。
★★**対が 0 件の守りは 1 つも無かった**★★（**D-27**）。
★★**D-39 の受けを測定の道具に組み込んだ**★★ —— ★**通った件数と★読み込みの失敗を毎回記録し、★戻したあとハッシュを突き合わせる**。★**12 通りとも読み込みの失敗は 0 件である**。
★★**「同期が動くようになった」と読まないこと**★★ —— ★**呼ぶ側は★依然 0 行である**（★21 / 23 / 24 / 25 は未着手）。

★★**§32-6 の 20（デッキを預かる / 返す口）を実施した（決定 D134-9）で `loveca-server` が 120 → 153**★★
（**+33** = `test/deck_endpoint_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-db` / `loveca-data` はこの commit で触っていない（★**495 / 208 / 53 を実測で確認した**）。
★`loveca-ui` は **958 のまま**（★**走査テストの許可リストに 2 行足しただけ**。★**テストを 1 件も足していない**）。
★★**デッキの保管を決めた ＝ ★利用者 1 人につき JSON のファイル 1 つ**★★（**D134-6** —— ★§35-9 が「まだ決まっていない」と書いていた分）。
★★**D131-4 の形をそのまま広げない**★★（**§7-7**）—— ★**デッキは★★同期のたびに変わる★★ので、★全件 1 ファイルだと★1 デッキ直すたびに★★全利用者ぶんを書き直す★★**。
★★**柵: 受け取った字面をファイル名にしない**★★（**D134-7**）—— ★**利用者名は★呼ぶ側が決める**（**D133-4**）ので、★★そのまま使うと別の場所を指す名前を送れる★★。★**SHA-256 の 16 進にする**（★利用者名はファイルの中に持つ）。
★★**409 を持ち込まない**★★ —— ★**上書きが正しい**（★同じデッキを何度でも預ける）。★**17-2 の 409 とは★★意味が違う★★**（**§7-7**）。★**対で並べて固定した**。
★★**中身を 1 バイトも見ない**★★（**D105-2**）—— ★**線 α は 0 件のまま**（**D115-6** / **D126-3**）。★**pubspec は 1 文字も変えていない**。
★★**【要確認/法務】ここが「★デッキが他人の機械へ渡る★最初の瞬間」である**★★（★§35-6）—— ★★**動かす前に★**D104** の札を引き直すかを★利用者が判断すること**★★。★**書く前ではない**（**D133-5** と同じ形）。
★★**呼ばれる回数に上限が 1 つも無い**★★（**N-26** ＝ ★★門 セ★★）—— ★**預ける / 返すの★毎回★1.5 秒かかる**（★実測 / §54-2）。★**塞いでいない**（★決まっていない）。
★★**実測した対は 13 通り**★★（2026-09-01 / ★**13 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(A) 預ける口が名乗りを見ない** = 2 件 ／ ★**(B) 返す口が名乗りを見ない** = 1 件 ／
★**(C) 新規と上書きを分けない** = 4 件 ／ ★**(D) 2 度目を 409 で断る** = 2 件 ／
★**(E) 空を通す** = 4 件 ／ ★**(F) 空の content だけ通す** = 1 件 ／
★**(G) 利用者名をそのままファイル名にする** = 1 件 ／ ★**(H) ファイルに利用者名を書かない** = 1 件 ／
★**(I) 利用者で分けず 1 ファイルにまとめる** = 4 件 ／ ★**(J) 無いデッキに 200 を返す** = 4 件 ／
★**(K) 振り分けが返す口を預ける口へ送る** = 11 件 ／ ★★**(L) 一時ファイルを使わず直に書く** = 1 件★★ ／
★**(M) 中身を JSON として解釈してから保管する** = 18 件。
★★**(L) は最初 0 件だった。★対の形が対象を見ていなかった**★★（**D-27**）——
★**「`.tmp` が残らない」は★★直に書く実装でも真である★★**。→ ★**一時ファイルの場所を塞いで★★元の 1 件が残ることを見る★★対に作り直した**（★待ち受けを通さず★保管だけを見る）。
★★**3 つの原因に 1 つずつ当てた**★★ —— ★**(a) 対の形である** ／ ★(b) 本命は空振りしていない（★他の対が落ちる）／ ★(c) 仕込みは弱くない（★間接を丸ごと外している）。
★★**D-39 を先回りして潰した**★★ —— ★**測定の道具が★★通った件数と読み込みの失敗を毎回記録する★★**。★**この回は 0 件である**。
★★**「同期が動くようになった」と読まないこと**★★ —— ★**呼ぶ側が 1 行も無い**（★21 / 23 / 24 / 25 は未着手）。★★**一覧を返す口も無い**★★（★どのデッキが預けてあるかを★呼ぶ側は知りようが無い）。

★★**§32-6 の 17-2（アカウントを作る口）を実施した（決定 D133-10）で `loveca-server` が 101 → 120**★★
（**+19** = `test/account_endpoint_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-db` / `loveca-ui` / `loveca-data` はこの commit で触っていない（★**495 / 208 / 958 / 53 を実測で確認した**）。
★★**§32-6 の漏れだった**★★ —— ★**§46-14 の 1 / §48-12 の 1 / §49-3 が★★3 回書いた分★★**（**D132-1** が立て、★**D133-4** が★門 ス を閉じた）。
★★**【要確認/法務】この口は★誰でも呼べる**★★（**D133-4**）—— ★★**動かす前に★**D104** の札を引き直すかを★利用者が判断すること**★★（**D133-5**）。★**書く前ではない**（★証明書が無ければ誰も作れない）。
★★**空は断る。★長さの下限は決めない**★★（**D133-9**）—— ★**空を許すと「パスワード無し」と同じで★**D105-3** が成り立たない**。★**下限は好みで決まる**（★測っていない / **D-28**）。
★★**409 が★存在を漏らすことを隠さない**★★ —— ★**認証の口は漏らさないが、★★作る口は避けられない★★**（★§46-14 の 3 / §48-12 の 3 の続き）。★**対で固定した**。
★★**方針の切り替えを持たない**★★（**D114-7** の理由 2 —— ★決まっていない分岐を先に置かない）。
★★**`serveAuth` を `serveApi` に改名した**★★ —— ★**1 つの待ち受けが★★2 つのパスを持つ★★ようになった**（**D130-7**）。★**「認証だけを待ち受ける」という名前が★実物と食い違った**（**D-15 (l)**）。
★★**実測した対は 12 通り**★★（2026-09-01 / ★**12 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(A) 重複を断らない** = 5 件 ／ ★**(B) 重複を 201 で返す** = 2 件 ／ ★**(C) 空のパスワードを通す** = 2 件 ／
★★**(D) 長さの下限を入れる** = 11 件★★（★**下限を決めていないことの対**）／ ★**(E) 平文をそのまま保管する** = 5 件 ／
★**(F) 塩を決め打ちにする** = 1 件 ／ ★**(G) 201 ではなく 200 を返す** = 4 件 ／ ★**(H) 壊れた要求でも 201 を返す** = 6 件 ／
★**(I) メソッドの検査を外す** = 1 件 ／ ★**(J) 振り分けが accounts を auth へ送る** = 6 件 ／
★**(K) 知らないパスを auth へ送る** = 3 件 ／ ★**(L) 保管が直に書く** = 1 件。
★★**(A) は★最初★仕込みが★コンパイルを通らなかった**★★（★存在しないメソッドを呼んだ）—— ★**落ちた件数が★★対ではなく読み込みの失敗だった★★**。
→ ★**通る形に直して測り直した**（★**対が落ちないときの 3 通り**のうち★★(c) 仕込みが弱い★★の隣にある「★仕込みが★そもそも走らない」形である）。
★★**前のセッションで置いた見張りが★また落ちた**★★（★**2 日で 5 回目**）—— ★**合図を書き直した**（★**16 / 17-2 / 18 の口は実施済み。★残るのは画面と 20 以降**）。

★★**§32-6 の 18 の★口★を実施した（決定 D132-4 / D132-6）で `loveca-ui` が 945 → 958**★★
（**+13** = `test/data/auth_client_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-db` / `loveca-server` / `loveca-data` はこの commit で触っていない（★**495 / 208 / 101 / 53 を実測で確認した**）。
★★**口だけである。★画面は 1 行も無い**★★ —— ★**何を残すかは★**N-21** の (4)(ii) 待ちである**。
★★**16 と同じ形を採った**★★ —— ★**`HttpClient` と `Uri` を★呼び出し側から渡す**（**D131-3** と同じ）。★**門 シ の (2) は★この口に現れない**。
★★**答えを 3 つに分けた**★★（**D105-6**）—— ★**名乗れた / ★名乗れなかった / ★★通信が失敗した★★**。★**2 つに畳むと★★「つながらなかった」が「パスワードが違う」に化ける★★**。
★**壊れた応答は★通信の失敗に畳む**（★**分ける材料が無い** / **D-28**）。
★★**本物の待ち受けに当てた**★★（**D-10**）—— ★**`loveca-server` の試験用の証明書を借り、★★TLS 越しに往復した★★**。
★★**サーバーの実装は 1 度も呼ばない**★★（**D126-3** —— ★依存は永久に 0 本）。★**自前の待ち受けを立てて★振る舞いを真似た**。
→ ★★**「端から端まで 1 回で通した」とは書かない**★★（★両側とも★自分の側の試験を持つ）。
★★**やり取りの形が 2 か所に書かれる**★★ —— ★**D126-3 が買った代償である**。★**鍵の名前が両側で同じことを★走査で見張る**（★引用符ごと見る / **D-37 の裏**）。
★★**D43 を踏まない。★1 文字も動かさない**★★ —— ★**理由は★★画像である★★**。★**この口は画像を 1 バイトも運ばない**。
★★**実測した対は 11 通り**★★（2026-09-01 / ★**11 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(A) 401 を通信の失敗に畳む** = 1 件 ／ ★**(B) 通信の失敗を「名乗れなかった」に畳む** = 2 件 ／ ★**(C) 400 を「名乗れなかった」に畳む** = 1 件 ／
★**(D) 壊れた応答を成功にする** = 1 件 ／ ★**(E) 利用者名が無くても成功にする** = 1 件 ／ ★**(F) 送る鍵の名前を変える** = 3 件 ／
★**(G) パスを変える** = 3 件 ／ ★**(H) メソッドを GET にする** = 4 件 ／ ★**(I) 住所のパスをそのまま使う** = 2 件 ／
★★**(J) 成功で★送った字面を返す** = 1 件★★ ／ ★★**(K) サーバー側の鍵の名前を★全部変える** = 1 件★★。
★★**(J) と (K) は★最初 0 件だった。★2 件とも★★対の形が悪かった★★**★★（**D-27** —— ★**指示が「★実装ではなく対の形を先に疑う」と★★先に書いていた★★**）——
★**(J)** は★**送る字面と返る字面を★★同じにしていた★★**（★§47 の (J) と同じ形。★★2 度目である★★）→ ★**違える形に直した** ／
★**(K)** は★**仕込みが 1 か所だけの置換で、★★サーバー側に同じ鍵が★他にも在った★★**（★★仕込みが弱かった★★ / ★本命は空振りしていなかった）→ ★**全部置き換える形に直した**。
★★**前のセッションで置いた見張りが★また落ちた**★★（★**2 日で 4 回目**）—— ★**合図を書き直した**（★**16 と 18 の口は実施済み。★残るのは画面と 17-2 である**）。

★★**§32-6 の 16（認証の口・サーバー側）を実施した（決定 D131-8）で `loveca-server` が 61 → 101**★★
（**+40** = `auth_endpoint_test.dart` 14 ＋ `account_file_store_test.dart` 18 ＋ `tls_fixture_test.dart` 8。★**内訳はファイルを見ること**）。
★★**認証のレーンで★番号を持つ最初のコミットである**★★（★これまでの 3 回は「前段」「判定」で★番号を持たなかった）。
★`loveca-core` / `loveca-db` / `loveca-data` はこの commit で触っていない（★**495 / 208 / 53 を実測で確認した**）。
★`loveca-ui` は **945 のまま**（★**見張りの許可を直しただけ**）。
★★**待っていた 3 つが揃った**★★ —— ★**判定**（**D130-14**）／ ★**保管**（**D131-4** ＝ JSON のファイル 1 つ）／ ★**待ち受け**（**D131-3** ＝ 証明書は渡される）。
★★**待ち受けを★本当に張って確かめた**★★（**D-10**）—— ★**試験用の自己署名の証明書をリポジトリに置いた**（**D131-7** / ★`test/fixtures/tls/`）。
★★**飛ばす検査（skip）を作らないためである**★★ —— ★**作るたびに生成すると `openssl` が無い機械で飛ばす**（`CLAUDE.md` §3）。
★★**柵: 試験用の証明書は `lib` から 1 度も参照されない**★★（★走査で見張る / **D131-7**）。★★**「安全である」とは書かない**★★（★秘密鍵がリポジトリに在る）。
★★**柵: 素の HTTP へ落ちない**★★（**D131-2**）—— ★**`serveAuth` が `SecurityContext` を★★必須で受け取る★★**。★**`lib` に `HttpServer.bind(` が 0 件であることも走査で見張る**。
★★**状態コードを分けた**★★（**D131-6**）—— ★**200 / 400 / 401 / 404 / 405**。★**401 と 400 を取り違えない**（★**400 は「送り手の作りが違う」、★401 は「資格情報が違う」**）。
★★**失敗の 2 つは★状態コードでも本文でも区別しない**★★（★対で固定した）。
★★**保管の柵 3 つ**★★（**D131-5**）—— ★一時ファイルへ書いて置き換える ／ ★重複は断る ／ ★置き場は呼び出し側が渡す。
★★**実測した対は 14 通り**★★（2026-09-01 / ★**14 通りとも仕込んで走らせ、戻してから本番を測った**）——
★★**(A) 待ち受けを素の HTTP にする** = 15 件★★ ／ ★**(B) 壊れた要求を 401 にする** = 1 件 ／ ★**(C) 名乗れなかったときを 200 にする** = 2 件 ／
★**(D) 知らないパスでも判定へ進む** = 2 件 ／ ★**(E) メソッドの判定をパスより先にする** = 1 件 ／ ★**(F) 失敗の応答に送られた値を入れる** = 5 件 ／
★**(G) 保管が重複を断らない** = 2 件 ／ ★**(H) 保管が直に書く** = 1 件 ／ ★**(I) 壊れたファイルを空として開く** = 1 件 ／
★**(J) 知らない版を通す** = 1 件 ／ ★**(K) 保管が利用者名を小文字に畳む** = 1 件 ／ ★**(L) 書き出しの並びを足した順にする** = 1 件 ／
★**(M) 試験用の証明書を `lib` から参照する** = 1 件 ／ ★★**(N) 試験用の証明書を 1 つ壊す** = 14 件★★。
★★**対が★本命の空振りを見つけた**★★（**D-27** の★★逆向きの働き★★）—— ★**証明書の走査で `RegExp.escape` の★★戻り値（文字列）をそのまま `Pattern` に渡しており、★何にも当たらなかった★★**。
★**「`lib` に 0 件」は通っていた**（★★空振りだから★★）。→ ★**「`test` には在る」の対が落ちて分かった**。★**正規表現に包んで直した**。
★★**素の HTTP の対も★最初は空振りだった**★★（★§45 の (R) / §47 の (J) と同じ形）—— ★**`getUrl` は★要求の器を作るだけで、★★つなぎに行くのは `close()` である★★**。★**`close()` まで進めて直した**。
★★**前のセッションで置いた見張りが★また落ちた**★★（★**2 日で 3 回目**）—— ★**合図を書き直した**（★**16 は実施済み。★残るのは 18 である**）。

★★**認証の判定を置いた（決定 D130-14）で `loveca-server` が 43 → 61**★★
（**+18** = `test/auth_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-db` / `loveca-data` はこの commit で触っていない（★**495 / 208 / 53 を実測で確認した**）。
★`loveca-ui` は **945 のまま**（★**見張りの許可を直しただけで、★テストを 1 件も足していない**）。
★★**§32-6 の 16 ではない。★16 の★判定★である**★★ —— ★**口は 1 つも開いていない。★待ち受けも保管も 1 行も無い**。
★★**16 が待っている 2 つを★1 つも要らないことを 1 つずつ当てた**★★（★門 シ ＝ 待ち受け / ★アカウントの保管 ＝ §35-9）——
★**判定は★受け取ったあとに走る** ／ ★**保管を★★渡される口★★にした**（★先例は `loveca_db` の `MasterFileSource`）。
★★**§32-6 の 13（衝突判定を純粋関数として置く）と同じ形である**★★。★**§45 で置いた固める関数の★★最初の消費者になった★★**。
★★**柵: 失敗の理由を分けない**★★ —— ★**「利用者名が無い」と「パスワードが違う」を区別すると★★利用者名の存在が漏れる★★**。
★★**ただし片側しか塞げない**★★ —— ★**アカウントを作る口は重複を断る**（**D130-9**）ので★そちら側では必ず漏れる。
★**その口は §32-6 のどのコミットにも無い**（★§46-14 の 1）。★**塞ぎ方はその口を作るときに一緒に決めること**。
★★**利用者名を★正規化しない**★★ —— ★**規則がどの決定にも書かれていない**（★§46-14 の 2）。★**「同じ」の定義は★保管の実装が持つ**（★対で固定した）。
★★**実測した対は 10 通り**★★（2026-09-01 / ★**10 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(A) 保管を引かず常に成功する** = 9 件 ／ ★**(B) 失敗の応答に理由を足す** = 1 件 ／
★**(C) 要求に端末の同定を足す** = 2 件 ／ ★**(D) 要求の鍵の名前を変える** = 2 件 ／
★**(E) 空文字も投げるようにする** = 1 件 ／ ★**(F) 鍵の欠落を通すようにする** = 1 件 ／
★**(G) 成功で★要求の字面を返す** = 1 件 ／ ★**(H) 固めた値ではなく素の比較にする** = 5 件 ／
★**(I) 常に失敗にする** = 4 件 ／ ★★**(J) `lib` が利用者名を小文字に畳む** = 1 件★★。
★★**(J) は★最初 0 件だった。★golden の★字の向きが悪かった**★★（**D-27** / ★★§45 の (R) の 2 例目★★ —— ★**指示が「対が落ちないときは golden が足りていない可能性を先に疑え」と★先に書いていた**）——
★**保管を `Mitsuki`、★要求を `mitsuki` にしていたので、★★lib が小文字へ畳んでも★結果も引いた字面も 1 つも変わらなかった★★**。
→ ★**向きを逆にした**（★保管が小文字、★要求が大文字混じり）。★**引いた字面そのものも見る形に足した**。
★★**`crypto` 以外の依存は 1 つも増えていない**★★（★JSON は `dart:convert`）。★**線 α は空のままである**。
★★**前のセッションで置いた見張りが★また落ちた**★★（★**2 日で 2 回目**）—— ★**合図を書き直した**（★**N-21 / N-22 / N-23 は閉じ、★残るのは「口」の側だけである**）。
★★**合図は★落ちるたびに古くなる**★★（**D-15 (l)**）。

★★**パスワードの保存の形を置いた（決定 D129-3 / D129-5）で `loveca-server` が 24 → 43**★★
（**+19** = `test/password_hash_test.dart` の新設。★**内訳はファイルを見ること**）。
★★**あわせて `loveca-ui` が 943 → 945**★★（**+2** = `auth_mechanism_test.dart` の免除の受け 2 件）——
★★**前のセッションで置いた見張りが★実際に落ちた。★置いた翌日である**★★（★**D-10** の受けが働いた実物）。
★**合図を書き直し、★許可の形を 2 つに分けた** —— ★**「たまたま語が当たる行」**（★件数で見る）と ★**「認証そのもののファイル」**（★★パスで免除し、★件数は見ない★★）。
★★**分けた理由**★★ —— ★**doc を 1 行直すたびに落ちる検査は★★数字を合わせる作業に化ける★★**（**D-25** の別の形）。
★`loveca-core` / `loveca-db` / `loveca-data` はこの commit で触っていない（★**495 / 208 / 53 を実測で確認した**）。
★★**§32-6 の 16 ではない。★16 の★前段である**★★ —— ★**口は 1 つも開いていない**。
★★**16 が待っている 3 つを★1 つも要らないことを 1 つずつ当てた**★★（**N-21** の (4) / **N-22** / **N-23**）——
★**何を送るか**は「届いた値を固める」だけなので効かない ／ ★**何に載せるか**は固めるのが受け取った★あとなので効かない ／ ★**識別子**は★★塩に混ぜない★★ので効かない。
★★**「空の器」ではない**★★（**D114-7** の理由 2 と混ぜない）—— ★**入力を受けて値を返す関数である**（★§32-6 の 11 / 12 / 13 と同じ形）。
★★**PBKDF2-HMAC-SHA256 を採った**★★（**D129-3** —— ★軸 5 つで全欄埋めた。★**平文 / 素のハッシュ / 塩つきの速いハッシュは落ちる**）。
★★**「Argon2id が劣る」とは書かない**★★ —— ★**(戊) では強い**。★**差は依存の数である**。★**開き直す条件を 1 つ書いた**。
★★**`crypto` が `loveca-server` の★最初の依存になった**★★（**D115-2** が定めた作法で pubspec に代償として記録した）——
★**`loveca_core` の最初の依存と同じパッケージだが、★★別の理由で入っている★★**（★あちらは内容ハッシュの SHA-256）。
★★**線 α には触れない**★★ —— ★**`loveca_core` ではないので許可リストは空のままである**（**D115-6**）。
★★**境界の宣言の「許可」の段に行を 1 つ足した。★段は 4 のままである**★★（**D126-4** の訂正ではない）。
★★**golden を★別の実装で検算した**★★ —— ★**Python の `hashlib.pbkdf2_hmac` で独立に計算して一致を確かめた**（★**D115-2** と同じ作法）。
★**うち 2 本は RFC 7914 §11 の公表値とも一致する**（★同じく確かめた）。
★★**柵を 3 つ置いた**★★（**D129-5**）—— ★**回数と塩を保存した値の中に持つ** / ★**塩は `Random.secure()` から作る** / ★**照合は時間の差が出ない比較にする**。
★★**繰り返し回数の重さを測った**★★（2026-09-01 / ★**この機械で** —— ★**600000 回で 1465 ms、★100000 回で 259 ms**）。
★★**何回名乗るかに従属する**★★（**N-21** の (iii) が未決）—— ★**保存した値が回数を持つので★あとから上げられる**（★柵 1）。
★★**時間を測る検査は置いていない**★★（**D-28** —— ★**機械の状態で揺れる**。★固定できるのは答えが正しいことまでである）。
★★**実測した対は 9 通り**★★（2026-09-01 / ★**9 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(L) PBKDF2 の XOR を落とす** = 1 件 ／ ★**(M) 平文をそのまま保存する** = 4 件 ／
★**(N) 塩を決め打ちにする** = 1 件 ／ ★★**(O) 保存した値から回数を落とす** = 5 件★★（★柵 1 が最も広く見られている）／
★**(P) 比較を短いほうで打ち切る** = 1 件 ／ ★**(Q) 壊れた値で投げずに false を返す** = 1 件 ／
★★**(R) ブロックの通し番号を常に 1 にする** = 2 件★★（★**1 ブロックしか見ない golden では 0 件だった** —— ★**64 バイトの golden を足して初めて見えた**）／
★**(S) 既定の回数を下げる** = 1 件 ／ ★**(T) 照合を常に true にする** = 1 件。
★★**対が 0 件の守りは 1 つも無かった**★★（**D-27** / ★§41 / §43 に続いて **3 回連続**）。
★★**免除の受けも 4 通り測った**★★ —— ★**(U) 免除のパスを実在しないものにする** = 3 件 ／ ★**(V) 免除から外す** = 2 件 ／
★**(W) 語を 1 つも含まないファイルを免除に足す** = 1 件（★陽性対照）／ ★★**(X) 免除したファイルの中身を変える** = 0 件★★（★**意図どおり —— ★免除は件数を見ない**）。

★★**線 α の走査を「解決」まで広げた（決定 D126-3 の受け）で `loveca-server` が 21 → 24**★★
（**+3** = `core_boundary_test.dart` の「解決」の群。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-db` / `loveca-ui` / `loveca-data` はこの commit で触っていない（★**495 / 208 / 943 / 53 を実測で確認した**）。
★★**引き金は実物である**★★ —— ★**committed の `pubspec.lock` が `loveca_core` を「direct main」として持っていた**（★`pubspec.yaml` には無い / ★実読）。
★★**最初は lock の字面を見る検査を書いた。★測ったら★★対象を見ていなかった★★**★★（**D-27** / ★仕込んで **0 件**）—— ★理由は 2 つで、★**どちらも実測である**:
★**(1) `dart test` は走る前に解き直し、★lock を書き直す**（★仕込みが消える）／
★★**(2) そもそも lock だけでは★呼べる状態にならない**★★（★pub は `pubspec.yaml` から木を作り、★lock は★★その木の版を留めるだけ★★である。★木に無い項目は落ちる）。
→ ★★**lock の検査は★置かずに外した**★★（★**§39 で「対の無い守り」を見つけて★★足した★★回の逆である —— ★今回は★★外した★★**）。
★★**残したのは「解決」だけである**★★ —— ★`.dart_tool/package_config.json` は★★いま呼べるものそのもの★★である（★git 管理外だが、★テストが走る時点では必ず在る）。
★★**stale な lock は直した。★害は無かった**★★ —— ★**解決には入っていなかった**（★確かめた）。★**直さないと、★次に依存を足した commit が★★黙って消してしまう★★**。
★★**実測した対は 6 通り**★★（2026-09-01 / ★**6 通りとも仕込んで走らせ、戻してから本番を測った**）——
★★**(A) committed の lock を訂正前に戻す** = 0 件★★（★**これが「外した」根拠である**）／
★**(G) 解決に `loveca_core` を 1 件足す** = 1 件 ／ ★**(H) 解決から自分自身を外す** = 1 件（★**D-10** の受け）／
★★**(I) 走査する名前から 1 つ外す（かつ解決に足す）** = 1 件★★（★**D-31** の受けが働いた。★**受けを置く前は 0 件だった**）／
★**(J) 取り出しを空集合にする** = 1 件 ／ ★★**(K) リポジトリに Dart パッケージをもう 1 つ置く** = 1 件★★（★同じ受け）。
★★**D-31 の受けを 3 つ目の走査テストに置いた**★★ —— ★**`auth_mechanism_test.dart` と `package_boundary_test.dart` に続く**。★**残り 2 つ（`decision_number_test.dart` / `legacy_design_number_test.dart`）は★手で足す規約のままである**。

★★**§1 の境界の見張りを機械に移した（決定 D128-3 / D-D）で `loveca-ui` が 932 → 943**★★
（**+11** = `test/docs/package_boundary_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-db` / `loveca-server` / `loveca-data` はこの commit で触っていない（★**495 / 208 / 21 / 53 を実測で確認した**）。
★★**§32-6 のコミットではない**★★ —— ★**16 / 18 / 20 / 21 / 23 / 24 / 25 は★7 つとも着手できない**（**D128-9** —— ★**16 / 18 が★**N-21** の (4) と **N-22** の 2 つを待っている**）。
★★**引き金は決定 D128 である**★★ —— ★**候補 3（端末が鍵を作って名乗る）が選ばれたので、★★鍵を作るコードがいずれ書かれる★★。
★**鍵は推測できてはならないので `Random.secure()` を引く** → ★**`loveca_core` には★★そもそも書けない★★**（**D128-3**）。
★★**それを見張っていたのは★上の手動 `grep` 1 本だけだった**★★（★走査した）。
★★**上の検証コマンドは消していない。★格が違う**★★ —— ★**手で 1 回走らせるものと、★毎回走るものは別である**（★§3 が **D-2** を前例に「手動の見張りは忘れられる」と書いている）。
★★**見張るのは 2 つで、★見ているものが違う**★★（**D-27**）—— ★**指示行の走査**（`package:flutter` / `dart:ui` / `dart:io`。★**doc の中の同じ字面を拾わない** / **D-30**）と
★**本文の走査**（`DateTime.now` / `Random()`。★★**指示行では 1 件も見えない**★★）。★**許可リストは 2 件で、★2 件とも★禁止を説明した doc の行である**。
★★**`Random()` を `Random(` に広げない**★★（**D-37 の裏** —— ★**`Random(seed)` は禁止ではない**。★`rng.dart` に実在する / ★**対で固定した**）。
★★**走査する木は 2 つだけである。★絞った理由と、絞って範囲外になるものを書いた**★★（**D-31**）——
★**§1 / §2 が境界を課しているのが `loveca-core` と `loveca-db` だからである**。★**`loveca-server` は自分の走査テストを持ち、★`loveca-ui` は制約を持たない**。
★★**受けを置いた**★★ —— ★**`lib/` を持つパッケージを実際に数え、★★見張りの割り当てとずれたら落ちる★★**（★「見ない」と書いてもよい。★**書かないことだけができない**）。
★★**実測した対は 13 通り**★★（2026-09-01 / ★**13 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(A) core に `dart:io` の import を足す** = 1 件 ／ ★**(B) core に flutter の export を足す** = 1 件 ／
★**(C) core に `DateTime.now()` を書く** = 1 件 ／ ★**(D) core に seed なし `Random()` を書く** = 1 件 ／
★**(E) db の native 以外に `dart:io` を足す** = 1 件 ／ ★**(F) db に flutter を足す** = 1 件 ／
★**(G) 走査から `export` を落とす** = 1 件 ／ ★★**(H) 指示行の走査から行頭の判定を外す** = 1 件★★（★**§39 で「対が 1 つも無い守り」が見つかった所そのものである**）／
★**(I) 本文の字面を `Random(` へ広げる** = 2 件 ／ ★**(J) 許可リストの件数を 1 つ変える** = 1 件 ／
★**(K) native の許可リストから 1 件外す** = 1 件 ／ ★★**(L) パッケージの割り当てから 1 件外す** = 1 件★★（★**D-31 の受けが働いた**）／
★**(M) core に同じ名前の `.dart` をもう 1 つ置く** = 2 件。
★★**対が 0 件の守りは 1 つも無かった**★★（**D-27** —— ★**§39 の再発は 0**。★§41 に続いて **2 回連続**）。
★★**(B) が要る**★★ —— ★**(A) だけでは「import を見ていること」しか分からない。★★`export` は再公開であり、★import と同じだけ線を跨ぐ★★**（★`loveca-server` の走査テストと同じ判断）。
★★**(I) が実在のコードに当たった**★★ —— ★**広げると `rng.dart` の `Random(seed)` が当たり、★★許可リストの完全一致も同時に落ちる★★**（★**合成の対だけではなく、★本番のコードが守りを支えている**）。
★★**測定の道具は★番兵へ置換する形にした**★★（★§41-5 の 2 の手当てをそのまま適用した）——
★**空文字への置換を禁じ、★戻したあと★★ハッシュを突き合わせ、★番兵が 1 つも残っていないことを確かめた★★**（★`git status` でも確かめた）。

★★**認証の仕組みの走査を機械に移した（決定 D127 / 未決 **N-21**）で `loveca-ui` が 921 → 932**★★
（**+11** = `test/docs/auth_mechanism_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-db` / `loveca-server` / `loveca-data` はこの commit で触っていない（★**495 / 208 / 21 / 53 を実測で確認した**）。
★★**§32-6 のコミットではない**★★ —— ★**16 / 18 / 20 / 21 / 23 / 24 / 25 は★7 つとも着手できない**（**D127-8**）。
★**これは★手で書いた断定の受けである** —— ★★**「仕組みを述べた行が `docs` にも `lib` にも 1 つも無い（★走査した）」が★偽だった**★★（**D-15 (j)** / ★§37 / §39 に続いて **3 回連続**）。
★★**見張るのは 2 つで、★見ているものが違う**★★ —— ★**4 パッケージの `lib`**（★落ちたら「**N-21** を先に閉じよ」の合図）と ★**原典**（★落ちたら「原典が動いた」/ **U31**）。
★★**`docs/` の他の `.md` は走査しない。★理由が実在することを対で固定した**★★（**D-25** —— ★**記録が増えるたびに当たるので非 0 が常態になる**。★**仮定も 1 行で書いた**）。
★★**`token` を語彙に入れていない**★★（**D-37** —— ★**全文検索の分かち書きの設定と★共有形式の正規化が当たる**。★**対で固定した**）。
★★**根をリポジトリから導く受けを置いた**★★（**D-31** —— ★★**`_libRoots` を手で足す規約に頼らず、★`lib/` を持つパッケージを実際に数えて突き合わせる**★★。
★**先行する 2 つの走査テスト（`decision_number_test.dart` / `legacy_design_number_test.dart`）は★この受けを持っていない**）。
★★**実測した対は 8 通り**★★（2026-09-01 / ★**8 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(A) サーバーの `lib` に仕組みの語を 1 行足す** = 1 件 ／ ★**(B) 許可リストから 1 行外す** = 1 件 ／
★**(C) 語彙から「アカウント」を外す** = 4 件 ／ ★**(D) 語彙に `token` を足す** = 3 件 ／
★**(E) 大文字小文字の無視を外す** = 1 件 ／ ★★**(F) 根からサーバーを外す** = 1 件★★（★**D-31 の受けが働いた**）／
★**(G) 原典に当たる行を 1 行足す** = 1 件 ／ ★**(H) 走査しない理由の対の相手を別の木にする** = 1 件。
★★**測っている途中で、★対の字面が広すぎて 2 行当たった**★★（**D-37** / ★**直した**）—— ★**決定表の 1 行を「必須化」で照合しており、★審査の話の行も当たっていた**。★**語を 1 段長くして分けた**。
★★**測定の道具が★本番のファイルを壊した**★★（★**§39-5 の 4 の 2 例目。★今度は空振りではなく★★破壊である★★**）——
★**仕込みを「空文字への置換」で書いたため、★戻すときに全文へ挿し込まれた**。★**git で追跡されていない新規ファイルだったので★差分では見えなかった**。
→ ★**手当て: 仕込みは★番兵へ置換し、★戻すときに番兵が在ることを確かめる**（★**空文字への置換を禁じた**）。
★★**この事故が本体に及ばなかったのは偶然ではない**★★ —— ★**ほかの仕込みはすべて非空の置換で、★`git status` が clean を返した**（★確かめた）。

★★**Phase 4 のレーン 4 の commit 17（線 α の許可リストと走査テスト / 決定 D115-6 / D115-7 の (c)）で
`loveca-server` が新設 —— **21**★★
（★**このパッケージの最初のテストである**。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-db` / `loveca-ui` / `loveca-data` はこの commit で**件数が動いていない**
（★**495 / 208 / 921 / 53 を実測で確認した**。★`loveca-ui` は 2 つの走査テストの `_roots` に 2 行ずつ足しただけ）。
★★**パッケージの誕生と同じ commit である。★離せない**★★ —— ★**D115-6** が「★空で始めれば、★最初の 1 件に必ず理由が付く」と書いており、
★**割ると「対象が存在するのにリストが無い commit」が 1 つできる**（★§32-7 が 17 を 16 の直後に置いた理由そのもの）。
★★**「空の器」ではない**★★（**D114-7** の理由 2 と混ぜない）—— ★**あちらは書き込み点が来るまで何も守らないが、★★こちらは今日から落ちる★★**（★下の対）。
★★**依存は 0 本である。★`loveca_core` を pubspec にも書かない**★★（**D126-3**）—— ★**書けば「呼べる」状態になり、★線 α が空であることを構造で守れなくなる**。
★★**走査を守っているものは 2 つ在り、★見ている対が違う**★★（★**実測して分けた** / **D-27**）——
★**行頭の判定**（★行コメント・doc・表の中の字面を止める）と ★**コメント外し**（★ブロックコメントの中の、行頭から書かれた指示行を止める）。
★★**実測した対は 12 通り**★★（2026-09-01 / ★**12 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(A) lib に core の import を足す** = 1 件 ／ ★**(B) lib に core の export を足す** = 1 件 ／
★**(C) lib に flutter の import を足す** = 1 件 ／ ★**(D) lib に `dart:ui` の import を足す** = 1 件 ／
★**(E) lib に `loveca_db` の import を足す** = 1 件 ／ ★**(F) pubspec に core の依存キーを足す** = 2 件 ／
★**(G) 許可リストに 1 件足す** = 2 件 ／ ★**(H) 走査から `export` を落とす** = 2 件 ／
★**(I) コメント外しを恒等関数にする** = 2 件 ／ ★**(J) 文字列を跨がないようにする** = 1 件 ／
★**(K) YAML のコメント外しを恒等関数にする** = 1 件 ／ ★**(L) 行頭の判定を外す** = 1 件。
★★**(B) が要る**★★ —— ★**(A) だけでは「import を見ていること」しか分からない。★★`export` は再公開であり、★import と同じだけ線を跨ぐ★★**。
★★**測っている途中で、★対が 1 つも無い守りが見つかった**★★（**D-27** / ★**足した**）——
★**(L) が★最初 0 件だった**。★**行コメントも doc も、★コメント外しだけで止まっており、★行頭の判定は 1 件も見られていなかった**。
→ ★**行の途中に在る文字列リテラル**（★コメント外しは文字列を残すので走査に届く）で対を作り直した。
★★**もう 1 つ、★守りが働いていない所を見つけた**★★（★**分けた**）—— ★**pubspec の「キー行」の検査にはコメント外しが効かない**
（★行頭が `#` の行は「字下げ ＋ 識別子 ＋ コロン」に当たらない）。→ ★**コメント外しが効く検査（本体の字面）を別に置いた**（★(K) がその対）。
★★**D-31 を 2 か所で踏み、★2 か所とも直した**★★ —— ★**`CLAUDE.md` §3 の見張りの除外**（★実測: パッケージを足すと **2 件**出る。★除外を直して 0 件に戻した）と、
★**`loveca-ui` の 2 つの走査テストの `_roots`**（★実測: `loveca-server` に偽の番号を 1 つ置くと **3 件**落ちる ＝ 決定番号 / 所見番号 / 旧設計番号）。
★★**footgun を 1 つ記録した（★手当てしていない）**★★ —— ★**行頭から書かれた文字列リテラルの中の指示行は拾ってしまう**。
★**`lib` に該当は 0 件**（★走査した）。★**doc とテストに書いた**（**D-28** —— ★推測で埋めない）。

★★**Phase 4 のレーン 5 の commit 22（N-10 の器 / `schemaVersion` 4 → 5 / 決定 D114-1）で
`loveca-db` が 194 → 208**★★
（**+14** = `migration_test.dart` の **v4 → v5 の群 14**。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-ui` / `loveca-data` はこの commit で触っていない（★**495 / 921 / 53 を実測で確認した**）。
★★**この commit は「書き込みが 1 件も無い器」である**★★ —— ★`lib` から `deck_sync_marks` へ INSERT する経路は 0 本で、
★**入れているのはテストだけ**（★「新表が空である」と★「空でない状態は作れる」を対で置いてある / **D-10**）。
★★**D114-7 の理由 1 / 2 は解けていない。★★これは「空の器」である★★**★★ ——
★**記録点はコミット 23（送信）であり、★門 エ の下流である**。★**doc とテストにそう書いた。**
★**理由 3 / 4 のほうは解けている** —— ★**D115-1**（列は 1 本）と ★**D124-7**（(c) ＝ 器に列が増えない）。
★★**列は 3 つ**★★（`deck_id` / `log_mark` / `baseline_hash`）—— ★**相手側の目印もアカウントも端末も無い**（**D124-7** / **D125-9**）。
★★**目印の値の意味（「最後に送った」か「次に送る」か）を列名でも言わない**★★（★§24-8 が未決）。
★★**`decks` への外部キーを張らない。★`deck_edit_ops` の理由は写していない**★★（★§24-8 が「写すな。問いを立て直せ」と定めた分）——
★**張ると (2-c) の「残さない」の★手段（**D125-3** が未決）を先に決めてしまう**（★cascade なら消える / RESTRICT なら消せない）。
★★**巻き戻しは各段が「自分の版が足したもの」を落とす形にした**★★ —— ★`_rewindToV4` を新設し、
★**`_rewindToV3` / `_rewindToV2` / `_rewindToV1` は上の段に任せる**（★`_rewindToV3` の doc が「そのときは新設せよ」と予告していた分）。
★★**`ALTER TABLE ... DROP COLUMN` は使っていない**★★（★表ごと落とす）。
★★**実測した対は 10 通り**★★（2026-09-01 / ★**10 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(A) `m.createTable(deckSyncMarks)` を外す** = 7 件 ／ ★**(B) 枝が `backfillOrd()` を呼ぶ** = 2 件 ／
★**(C) 枝が `decks` を書き換える** = 4 件 ／ ★**(D) 枝が `deck_entries` を 1 行消す** = 3 件 ／
★**(E) 枝が `deck_edit_ops` を消す** = 1 件 ／ ★★**(F) 巻き戻しから `DROP TABLE deck_sync_marks` を外す** = 1 件★★ ／
★**(G) ハッシュの列をもう 1 本足す** = 1 件 ／ ★**(H) `baselineHash` を nullable にする** = 1 件 ／
★**(I) `decks` への外部キーを張る** = 1 件 ／ ★**(J) 主キーを `{deckId, logMark}` にする** = 1 件。
★★**(F) がこの群の要石である**★★（★v3 → v4 とまったく同じ理由）—— ★drift の `Migrator.createTable` は
**`CREATE TABLE IF NOT EXISTS`** を吐くので、★**巻き戻しが表を落とし損ねると移行は例外も出さずに空振りし、★群ごと無言で無意味になる。**
★**落ちるのは「前提: 巻き戻した DB は v4 の形」の 1 件だけであることを★名前まで実測した。**
★★**(B)(C)(D)(E) は「ユーザデータに触らない」側の対である**★★ —— ★**(A) を仕込んでも 4 通りとも通る**ので、
★**移行を無効化するだけでは、それらが対象を見ているか分からない**（**D-27**）。★だから壊し方を 4 通り分けて測った。
★★**(E) は目印の前提そのものを見ている**★★ —— ★**目印はログの位置である**（**D114-2**）ので、
★**この枝がログに触れば目印の指す先が移行で動く。**

★★**Phase 4 のレーン 3 の commit 13（衝突判定 ＝ 候補 L / 決定 D111-2）で
`loveca-core` が 480 → 495**★★
（**+15** = `deck_conflict_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-db` / `loveca-ui` / `loveca-data` はこの commit で触っていない（★**194 / 921 / 53 を実測で確認した**）。
★★**判定だけである。★解決も送信も 1 行も無い**★★ —— ★**解決（コミット 14）は入れていない**（★下）。
★★**基準は 1 つの record にした**★★（`DeckSyncBaseline`）—— ★**D114-1** が「目印と基準ハッシュを同じ行に」と決め、
★**D114-4** の 1 が「片方だけ在る状態を作れない」を利得に挙げている。→ ★**型でも片方だけ渡せない。**
★★**目印そのものは渡さない。★有無に畳んでから渡す**★★（★G が見るのは有無である / §18-2-4）。
→ ★**§24-8 が未決にした「目印の値の意味」がこの型に効かない。**
★★**相手側は内容ハッシュだけで見る**★★（**D124-7** ＝ (c)）—— ★**器に相手側の列は 1 つも無い。★引数にも無い。**
★★**器の行が無い場合は `neverSynced` を返して★何をするかを答えない**★★（**D114-3** / ★門 カ）。
★★**落としたことを 1 ビットで外へ出す**★★（`droppedLocalOpsByContent`）——
★**捨てると「操作が 1 件も無い」と「L が落とした」が区別できなくなる。**
★★**論理削除は必ずそこに入る**★★（**D116-12** —— ★`deletedAt` は内容ハッシュの 5 フィールドに無い）。
★★**扱いはここで決めない。★L の定義に削除の例外を足すことは (f-1) を開き直すことである**★★（★していない）。
★**実物の `Deck` で「削除しても内容ハッシュが変わらない」ことを★この群自身が確かめてから判定に流す**（★仮定を借りてこない）。
★★**両側が同じ内容へ変わった場合も `conflict` のままにした**★★ —— ★**L が落とすのは「基準と一致するもの」であって
「両側が一致するもの」ではない**。★**足すと定義を変えることになる。★解決は空振りになるが害は無い**（★粒-1 / **D112-1**）。
★★**実測した対は 8 通り**★★（2026-09-01 / ★**8 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(A) L の段を外す（＝候補 G に戻す）** = 6 件 ／ ★**(B) G の段を外す（＝内容だけで見る）** = 2 件 ／
★**(C) 落とした 1 ビットを常に false にする** = 4 件 ／ ★**(D) 行の不在を「変わっていない」に畳む** = 1 件 ／
★**(E) 相手側にも操作の有無を掛ける** = 3 件 ／ ★**(F) 両側が同じ内容なら衝突にしない** = 1 件 ／
★**(G) `localOnly` と `remoteOnly` を入れ替える** = 5 件 ／ ★**(H) 自分側を基準ではなく相手と比べる** = 4 件。
★★**(A) と (B) の両方が要る**★★ —— ★**(A) だけでは「L の段が在ること」しか見ておらず、
★★G の段が在るかは分からない**★★（**D-27**）。★2 つは同じ論理積の別の項である。

★★**Phase 4 のレーン 3 の commit 12（内容ハッシュを 1 個作る / 決定 D115-1 / D115-2）で
`loveca-core` が 466 → 480**★★
（**+14** = `deck_content_hash_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-db` / `loveca-ui` / `loveca-data` はこの commit で触っていない（★**194 / 921 / 53 を実測で確認した**）。
★★**`crypto` が `loveca_core` の★最初の依存になった**★★（**D115-2** —— ★**§25-4 が「記録すること」と書いた分**）。
★**§1 の禁止 5 つに当たらないことを★§1 の検証コマンドそのもので確かめた**（★`crypto` の `lib` 配下は **0 件** /
★**陽性対照つき** —— ★同じ走査が `loveca-core/lib` では当たる（**D-30** の説明行））。★**推移的な依存は `typed_data` 1 つだけ。**
★★**「当たらない」と「増やしてよい」は別である。★増やしたこと自体を代償として pubspec に記録した。**★★
★★**golden を★別の実装で検算した**★★ —— ★**Python の `hashlib.sha256` で独立に計算して一致を確かめた**（2 値とも）。
★**自分の出力を写しただけの golden は「自分と同じであること」しか言わない**。★**2 台が交換する値なので、実装をまたぐことが要求そのものである。**
★★**実測した対は 7 通り**★★（2026-09-01 / ★**7 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(a) 固定した表現を通さず `Deck` から直に組み立てる** = 3 件 ／ ★**(b) `toJson()` を素直に畳む** = 8 件 ／
★**(c) 接頭辞を落とす** = 5 件 ／ ★**(d) 接頭辞の字面を変える** = 3 件 ／ ★**(e) md5 に差し替える** = 4 件 ／
★**(f) 16 進を大文字にする** = 4 件 ／ ★**(g) 前半 32 桁に切り詰める** = 4 件。
★★**測っている途中で、★自分の対が対象を見ていないことが 1 つ出た**★★（**D-27** / ★**直した**）——
★**「入力は `canonicalDeckContentBytes` そのものである」の対が、★「5 フィールドが同じなら同じ値」しか見ていなかった。**
★**それは `Deck` から直に組み立て直す実装でも通る**（★(a) が最初 2 件）。→ ★**関係を直に見る形に置き換えた。**
★★**doc に書いた断定が 1 つ偽だった**★★ —— ★測る**前**に「この 1 件だけが落ちる」と書き、★**測ったら偽だった**。★**型は D-15 (j)**。★**書き直した。**

★★**Phase 4 のレーン 3 の commit 11（内容ハッシュの入力の正規化された表現 / 決定 D115-3 の柵 1・柵 2）で
`loveca-core` が 440 → 466**★★
（**+26** = `deck_content_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-db` / `loveca-ui` / `loveca-data` はこの commit で触っていない（★**194 / 921 / 53 を実測で確認した**）。
★★**ハッシュを 1 バイトも作らない。★依存も 1 つも増やしていない**★★（★SHA-256 は commit 12）。
★★**置き場を決めた —— `loveca_core` である**★★（★**§32-6 が「12 の時点で決める」と書いた分**。★理由 3 つはファイルの doc に書いた）。
★★**表現は「値ごとに UTF-8 のバイト数を前置し、列は件数を前置する」**★★ ——
★**区切り 1 文字の連結では、★自由文（`name` / `memo` / `tags`）に区切りを入れて別のデッキと同じ表現が作れる**（★**D115-3** の柵 2）。★**規約ではなく形で守る。**
★★**掴むのは 5 フィールドだけ**★★（**D111-4**）—— ★`deckId` / `createdAt` / `updatedAt` / `deletedAt` /
`revision` / `lastDeviceId` / `masterDataVersion` は**1 つも入らない**。★**対で固定した**。
★★**削除は 1 ビットも現れない**★★（**D116-12**）—— ★`deletedAt` が 5 フィールドに無いので、
★**論理削除だけが起きたデッキは出力が 1 バイトも変わらない**。★**欠陥ではなく D111-4 の帰結であり、★対で固定した。**
★★**実測した対は 12 通り**★★（2026-09-01 / ★**12 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(a) `Deck.toJson()` を素直に使う** = 11 件 ／ ★**(b-1) 素の連結・刷り番号をまとめて書く** = 6 件 ／
★**(b-2) 素の連結・刷り番号と枚数を交互に書く** = 6 件 ／ ★**(c) 長さ前置だけ落とす** = 4 件 ／
★**(d) 件数の前置だけ落とす** = 2 件 ／ ★**(e) null と空文字を同じに書く** = 2 件 ／
★**(f) 版タグを落とす** = 2 件 ／ ★**(g) entries を並べ替えてから書く** = 1 件 ／
★**(h) tags を並べ替えてから書く** = 1 件 ／ ★**(i) 長さを文字数にする** = 2 件 ／
★**(j) 文字列を小文字に畳む** = 3 件 ／ ★**(k) 枚数を長さ前置なしで書く** = 2 件。
★★**測っている途中で、★自分の対が対象を見ていないことが 2 つ出た**★★（**D-27** / ★**直した**）——
★**(1) (d) が★最初 0 件だった** —— ★`tags:['']` と `tags:[]` を比べており、★**長さ前置だけで割れる**。
★**件数の前置でしか割れない対**（★タグ 2 つ ⇔ エントリ 1 件）に置き換えた ／
★★**(2) (b) が★最初 2 件しか落ちなかった**★★ —— ★境界に入れる文字を**空白 1 文字に決め打ち**していたので、
★**NUL で連結する実装には当たらなかった**。★**区切りの候補 10 個を境界 6 か所すべてに当てる形へ直した。**
★★**(k) は対を組めなかったので★形を直に見た**★★ —— ★**枚数は十進の数字列で後続が「桁数 + `:`」なので、
★衝突を作るには 21 桁の枚数が要る**（★int の範囲外）。★**推測で埋めず、★golden と 1 件で形を固定した。**
★★**golden を 1 件置いた**★★ —— ★**「衝突しにくいこと」ではなく「表現を固定すること」が D115-3 の要求である**。
★**この 1 件は 12 通りのうち 10 通りで落ちる**（★落ちないのは (g)(h) —— ★golden のデッキは並べ替えても順序が変わらない）。

★★**Phase 4 のレーン 2 の commit 3（最小アプリ版を必須引数に / 決定 D118-5 ＝ 既-3 / D-7 のやること 1）で
`loveca-data` が 33 → 40**★★
（**+7** = `tests/test_build_dist.py` の新設。★**`tests/run_all.py` に登録した**。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-db` / `loveca-ui` はこの commit で触っていない（★**431 / 170 / 921 を実測で確認した**）。
★★**`cli.py` から `build_parser()` を切り出した**★★ —— ★**`main` の中に閉じていると、
必須引数を確かめるだけで `args.func` まで走って実データに触れる**。★`test_imports.py` の必須シンボルにも足した。
★★**実測した対は 6 通り**★★（2026-08-31 / ★**6 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(a) `build` に既定値を戻す** = 2 件 ／ ★**(b) CLI の `required=True` を外す** = 1 件 ／
★★**(c) `cmd_build` が値を渡さず `1.0.0` を埋める** = 1 件★★（★**D-7 の症状そのもの**。★**(a)(b) は通ったまま**）／
★★**(d) CLI の引数名を綴り違いにする** = 2 件（対の 2 件）★★（★**「必須である」の検査は通ったまま**）／
★**(e) `with_images` の既定値を消す** = 1 件 ／ ★**(f) `data_version` に既定値を入れる** = 1 件。
★★**測っている途中で検査の側の欠陥が 2 つ出た**★★（★**直した**）——
★**既定値を戻すと `AttributeError`、★引数名を変えると `SystemExit`（★`Exception` では捕まらない）で、
★★どちらも「ファイルごと落ちて残りの検査が 1 つも走らない」★★**。★**壊して測らなければ見つからなかった**（**D-10** / **D-27**）。
★★**footgun を 1 つ記録した（★手当てはしていない）**★★ —— ★**`compareVersions` は数にできない部分を 0 として扱う**ので、
★**`--min-app-version` の綴りを間違えると例外も出さずに「最小版なし」に化ける**。★**必須の自由入力にしたので今日から踏める。**
★★**D-7 は解消していない**★★ —— ★**やること 3（dist を作り直す）/ 4（アプリの版を戻す）は 1 つも実施していない**。

★★**Phase 4 のレーン 2 の commit 4（画像だけのマニフェストを生成 / 決定 D121-1 ＝ 画-5 / N-2 の画像側）で
`loveca-data` が 40 → 53**★★
（**+13** = `tests/test_image_manifest.py` の新設。★**`tests/run_all.py` に登録した**。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-db` / `loveca-ui` はこの commit で触っていない（★**431 / 170 / 921 を実測で確認した**）。
★★**生成側だけである。★読む側は 1 行も無い**★★（★順序の 5〜8）。
★**入れたもの**: `build_image_manifest`（★行のハッシュは**ディスク上のバイト列**から作る / **D121-2** の柵）＋
`image_manifest.json` の書き出し ＋ `version.json` の `imageManifestPath` / `imageManifestHash`。
★★**決めたことが 2 つある**★★（★§31 が字面まで定めていない分。★**理由はコードの doc に書いた**）——
★**(1) 画像のマニフェストに `dataVersion` を載せない**（★載せるとカードの版を上げるたびに画像側のバイト列が変わり、
★**画-5 の利得（カードの変更と画像の変更が独立に運べる）を自分で潰す**）/
★**(2) `--skip-images` では書かず、★版の情報の列も出さない**（★★**空の物を書くと「画像が 0 枚である」という宣言になり、
★削除の計画を足したとき『全部消せ』と読める**★★。★「まだ無い」と「0 枚である」を書き分ける）。
★★**D-4 は半分しか閉じない。★閉じない側もテストで固定した**★★ ——
★**生成側は出力先が既に在れば作り直さない**ので、★段の設定を変えても新しいバイト列が作られず、★行のハッシュも変わらない。
★**「変わらないこと」を検査に置いた** —— ★**閉じたと誤読させないためであり、★直した人はそのテストが落ちるのを合図にできる**。
★★**実測した対は 9 通り**★★（2026-08-31 / ★**9 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(a) 行のハッシュを名前から作る** = 2 件 ／ ★**(b) 重複を落とさない** = 1 件 ／ ★**(c) 原本 PNG を拾わない** = 1 件 ／
★**(d) 存在しないファイルも行にする** = 7 件 ／ ★**(e) 画像側に `dataVersion` を載せる** = 1 件 ／
★**(f) `--skip-images` でも列を出す** = 2 件 ／ ★**(g) 画像の行をカードのマニフェストにも足す** = 2 件 ／
★**(h) 版の情報の列を書かない** = 1 件 ／ ★**(i) 宣言するハッシュを別物にする** = 1 件。
★★**測っている途中で、★自分の対が 2 つとも対象を見ていないことが分かった**★★（**D-27** の実物）——
★**(e) と (g) は最初★0 件だった**。★**組み立てた値を見ていて、★`build` が★書いた物を見ていなかった**。
★**書かれたファイルを読む形に直してから測り直した。**

★★**Phase 4 のレーン 2 の commit 5（画像マニフェストの列を読む / 決定 D121-1 の受け取り側 1／4）で
`loveca-core` が 431 → 440**★★
（**+9** = `master_data_test.dart` の 2 群。★**内訳はファイルを見ること**）。
★`loveca-db` / `loveca-ui` / `loveca-data` はこの commit で触っていない（★**170 / 921 / 53 を実測で確認した**）。
★**入れたもの**: `VersionInfo` の `imageManifestPath` / `imageManifestHash`（★どちらも null 可）＋
`hasImageManifest` ＋ `Manifest.parseImages`。★★**取りに行かず、取り込みもしない。★読むだけである**★★。
★★**「まだ無い」と「0 枚である」を型で書き分けた**★★ —— ★**null は「まだ無い」**。
★**片方だけ / 空文字も「無い」扱いにする**（★場所だけあってハッシュが無ければ取り直す判断ができない。★推測で埋めない）。
★★**カード側の `parse` は緩めていない**★★ —— ★`dataVersion` は必須のまま。
★**緩めると壊れたカードのマニフェストが 0 として通る**。★**対で固定した**（★`parseImages` が「ただ緩めただけ」なら落ちる）。
★★**実測した対は 4 通り**★★（2026-08-31 / ★**4 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(a) 列を読まない** = 2 件 ／ ★**(b) `hasImageManifest` を片方だけで真にする** = 2 件 ／
★**(c) `parseImages` をカード側と同じ厳しさにする** = 2 件 ／ ★**(d) カード側の `parse` を緩める** = 1 件。
★★**(d) がこの群の要石である**★★ —— ★**(c) だけでは「`parseImages` が緩いこと」しか見ておらず、
★★カード側が厳しいままかは分からない★★**（**D-27**）。

★★**Phase 4 のレーン 2 の commit 6（取得手段がバイナリを運ぶ / 決定 D121-1 の受け取り側 2／4）で
`loveca-db` が 170 → 181**★★
（**+11** = `test/master_file_source_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-ui` / `loveca-data` はこの commit で触っていない（★**440 / 921 / 53 を実測で確認した**）。
★**入れたもの**: `MasterFileSource.readBytes`（★**インタフェースに足したので実装は全部が書かねばならない**）＋
`MapMasterFileSource` の `binaries` ＋ `LocalDirectoryMasterFileSource.readBytes`。
★★**取り込み層は 1 行も変えていない。★運べるようにしただけである**★★（★振り分けは commit 7）。
★★**壊れ方の断定を 1 つ実測で直した**★★ —— ★doc とテストに「テキスト経路では**置換文字に化ける**」と書いたが、
★**`dart:io` の `readAsString` は★★投げる★★**（`FileSystemException` / 既定は `allowMalformed: false`）。
★**「黙って壊れる」ではなく「読めない」である**。★**書いたテストが落ちて分かった**。★**両方の壊れ方を doc に併記した。**
★★**対を「UTF-8 にできないバイト列」で置いた**★★ —— ★**テキストで往復させると文字列に畳んでも差が出ないので、
★★文字列経由の実装でも通ってしまう★★**（**D-27**）。
★★**実測した対は 4 通り**★★（2026-08-31 / ★**4 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(a) `readBytes` を `readAsString` 経由にする** = 2 件 ／ ★**(b) `binaries` を無視する** = 3 件 ／
★**(c) `readBytes` が `readPaths` を記録しない** = 1 件 ／ ★**(d) 無い path で投げずに空を返す** = 1 件。

★★**Phase 4 のレーン 2 の commit 7（振り分けに画像を足す / 決定 D121-1 の受け取り側 3／4）で
`loveca-db` が 181 → 194**★★
（**+13** = `test/master_image_import_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-ui` / `loveca-data` はこの commit で触っていない（★**440 / 921 / 53 を実測で確認した**）。
★**入れたもの**: `MasterImageSink`（★**置き場は決めない** / 門 キ ＝ **N-1**）＋ `MasterImporter` の画像の計画・取り込み・削除。
★★**置き場を決めていない**★★ —— ★**保存先は呼び出し側が渡す。★渡さなければ今までどおり「未対応」に落ちる**
（★**置き場が決まっていないことで取り込み全体が止まる形にしない**）。
★★**カードの計画に画像を混ぜない**★★ —— ★`planUpdate` の削除計画は「カードのマニフェストに無いローカルの path」なので、
★**画像の記録をそのまま渡すと★★画像が全部消える★★**。★取り込み済みのハッシュを前置で 2 つに分けてから渡す。
★★**「まだ無い」と「0 枚である」を混ぜない**★★ —— ★**マニフェストが `null` なら 1 件も計画しない**
（★生成側が `--skip-images` のとき書かないので、★この状態は実際に作れる）。★**空のマニフェストなら消す**（★対で固定）。
★★**版は画像の失敗で止めない**★★ —— ★`data_version` を上げる判定はカードのマニフェストだけを見る。
★**画像はハッシュだけで計画するので、★失敗しても次回また計画に残る**（★版に従属しない）。
★★**実測した対は 6 通り**★★（2026-08-31 / ★**6 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(a) マニフェストが `null` でも空として扱う** = 1 件 ／ ★**(b) カードの計画から画像の記録を除かない** = 4 件 ／
★**(c) 画像をテキスト経路で読む** = 9 件 ／ ★**(d) 削除の振り分けが画像を無視する** = 2 件 ／
★**(e) 保存先が無くても計画を立てる** = 1 件 ／ ★**(f) 書き込みの失敗を握り潰す** = 1 件。
★★**(a) は最初 0 件だった。★自分の対が対象を見ていなかった**★★（**D-27** の 2 例目）——
★**テストの補助が `imageManifest ?? dist.imageManifest` と書いてあり、★`null` を渡しても★★黙って実物にすり替えていた★★**。
★**別の引数（`noImageManifest`）に分けてから測り直した。**
★★**未登録の隙間を 1 つ記録する（★採番しない / ★手当てしない）**★★ ——
★**版の情報の `imageManifestHash` を★覚える場所が無い**。★**画像のマニフェストは毎回読み直す**。
★**今日は害が無い**（★配信元はローカルのディレクトリで、★§32-6 のレーン 2 に★線を通す commit が 1 つも無い）。
★**線を通すときに要る** —— ★**覚えるなら取り込み状態の表に列が 1 つ増え、★`schemaVersion` が上がる**。

★★**Phase 4 のレーン 1 の commit 2（版ゲートを「より小さい」に / 決定 D118-3 ＝ 版-3 / D-32）で
`loveca-core` が 429 → 431 / `loveca-db` が 168 → 170**★★
（core: **−1** 「同じ dataVersion なら更新不要」を撤去、**+3**。
db: **−1** 「同じ dataVersion なら 1 件も読まない」を撤去、**+3**。★**内訳はファイルを見ること**）。
★`loveca-ui` / `loveca-data` はこの commit で**件数が動いていない**（★**921 / 33 を実測で確認した**）。
★★**§28-6 の予告「§23-3 が挙げた 2 テストが落ちる」は当たっていた**★★ ——
★**実測でもその 2 件だけで、★`loveca-ui` は 0 件だった**（★予告は「ほかに在るかは測っていない」と併記していた）。
★★**ui の「0 件」には陽性対照を取った**★★ —— ★`planUpdate` を常に `appTooOld` にすると
`loveca-ui` が **8 件**落ちる（`import_issue_test.dart`）。★**ui は `planUpdate` を実際に踏んでいる**（**D-10**）。
★★**実測した対は 2 通り**★★（2026-08-31 / ★**2 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**(A) ゲートを `<=` に戻す** = ★**同値を見ている 4 件が落ちる**（core 2 / db 2）／
★**(B) ゲートを丸ごと外す** = ★★**降格を見ている 2 件だけが落ちる**★★（core 1 / db 1）。
★**(B) がこの群の要石である** —— ★**(A) だけでは「降格の対が降格を見ているか」が分からない**（**D-27**）。
★★**`upToDate` が返る場面が変わった。★名前は変えていない**★★ ——
★**返るのは降格のときだけになった**。★変えると `loveca_db` / `loveca-ui` の分岐まで動く（1 コミット = 1 論点）。
★★**`dataVersionAdvanced` の名前が実態と食い違うようになった**★★ ——
★**同値で取り込むと「揃った」が真になるが版は動いていない**。★**害は無い**（★消費者は
`boot_controller` の「失敗があり、かつ揃わなかったとき」だけ）。★**いまの挙動をテストで固定した。★直すのは別の論点である**。
★★**`loveca_db` の doc から理由が 1 つ消えた**★★（★型は **D-15 (l)**）——
「1 件でも欠けたまま版を上げると次回 `upToDate` で二度と再取得されない」は**偽になった**（★同値では止まらない）。
★**規約そのもの（全ファイルが揃ったときだけ上げる）は変えていない** —— ★実態と食い違う版は画面の表示を狂わせる。

★★**Phase 4 のレーン 1 の commit 1（`copyWith` の既定値を据え置きに / 決定 D116-2 / A-3 / D-14）で
`loveca-core` が 425 → 429**★★
（**−1** = 「copyWith で revision が増える」を撤去。**+5** = `deck_validator_test.dart` の
「copyWith の既定値は据え置き」の群。★**内訳はファイルを見ること**）。
★`loveca-db` / `loveca-ui` / `loveca-data` はこの commit で**件数が動いていない**
（★**168 / 921 / 33 を実測で確認した**。★`loveca-ui` はフェイクにコメントを 4 行足しただけ）。
★★**§32-6 の予告「1 で落ちるはずのもの ＝ フェイクが `revision` を上げなくなる」は★実測 0 件だった**★★
（★**予告自身が「それを見ているテストが在るかは測っていない」と書いていた**）。
★★**予告に無い 1 件が落ちた**★★ —— ★`loveca-core` の「copyWith で revision が増える (決定 D101)」。
★**「落ちるはず」の落ち方である**（★仕様が変わった側）。★**D101 の訂正ではない** —— ★+1 する**主体**が
呼び出し側だと決まっただけである（**D116-1** / **D116-8**）。
★★**実測した対は 8 通り**★★（2026-08-31 / ★**8 通りとも仕込んで走らせ、戻してから本番を測った**）——
★**先に測った 4 通り**: **(A) 両方を据え置きに** = core 1 / db 0 / ui 0 ／
**(B) `revision` だけ** = 同じ 1 件 ／ **(C) `updatedAt` だけ** = ★**3 パッケージとも 0 件** ／
★★**(陽性対照) `copyWith` に `deletedAt` を無視させる** = ui が 1 件落ちる★★
（★**これが無いと ui の「0 件」は「見えていない」と区別できない** / **D-10**。
★ui が `Deck.copyWith` を踏む経路はフェイクの `softDelete` 1 本だけである）。
★**あとに測った 4 通り ＋ 1**: ★**新しい 5 件は 1 つずつ独立に落ちる** ——
既定値を戻す 2 通り / 引数を無視させる 2 通り / `masterDataVersion` を固定する 1 通りで、
★**5 通りとも「対応する 1 件だけ」が落ちた**（**D-27**）。

★★**束 C の commit 6（穴 (a) を塞ぐ / A-i / 決定 D110-2）で `loveca-ui` が 913 → 921**★★
（+4 = `deck_meta_dialog_test.dart` の「R2 も `DeckEditStore` を通る」の群。
+4 = `test/data/deck_save_call_site_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca-db` / `loveca-data` はこの commit で触っていない（★**425 / 168 / 33 を実測で確認した**）。
★★**§17-9-7 の 6 コミットはこれで終わりである**★★ —— ★**入ったのは器と記録点だけで、★同期の送受信は 1 行も無い**（★下の「動くようになったもの / まだ動かないもの」）。
★★**予告どおり挙動の差は 3 つだった**★★（★§15-7-3 の測定 / ★**3 つとも実測で確かめた**）——
★**差 1** `canSave` の門ができた（★**無変更のまま決定しても保存されない**）/
★**差 2** 失敗表示が 1 本落ちる（★**塞いだ**）/ ★**差 3** 一覧の読み直しを明示に足した。
★★**差 2 の「受け」の所在を確かめた —— 穴 (a) には無かった**★★ ——
★§15-7-6 (5) の #2 が挙げている `deck_list_page_test.dart:143` は **`failSoftDelete`（削除側）**であり、
★**`failSave` を使うのは R3 の 1 件だけ**（★再走査）。→ ★**§15-7-3 の「見ているテストが 0 件」が正しかった。**
★★**差 1 は予告どおり 1 件も反転しなかった**★★（★**測定の引用ではなく実測**した ——
★R2 経路の「決定」を押す 8 件は 8 件とも通ったまま）。
★★**観測できない差が 2 つ増えていた**★★（★§15-7-3 の 3 つに無い分。★**隠さないために書く**）——
★**(4) `DeckListStore` の再入防止（`busy`）に参加しなくなった**（★`busy` は R2 が 1 度も描かない / ★走査。
★入口はすべてモーダルの後ろなので届かない）/ ★**(5) 保存の開始時に `actionError` を消さなくなった**
（★表示した時点で消える経路が別に在る）。★**どちらも画面に出ない。**
★★**実測した対は 5 つ**★★（2026-08-30 / ★**5 つとも仕込んで走らせ、戻してから本番を測った**）——
★★**(A) 差 2 の塞ぎを外すと 1 件だけ落ちる**★★（★差 2 の受け）/
★★**(B) 差 3 の `load()` を外すと 1 件だけ落ちる**★★（★差 3 の受け）/
★★**(C) `_editMeta` を A-i の前（`DeckRepository.save` へ直行）に戻すと 3 件落ちる**★★
（★走査の完全一致 ＋ 差 1 ＋ 差 2。★★**`deck_edit_log_test` の「穴 (a) は塞がった」は落ちない**★★ ——
★**その test は「画面がこの手順を組むこと」を 1 文字も見ていない**。★**doc にそう書いてある** / **D-27**）/
★★**(D) `DeckListStore` に `saveMeta` を書き戻す（＝ A-ii の形）と 2 件落ちる**★★（★走査 2 件とも）/
★★**(E) `applyMeta` の記録を外すと 2 件落ちる**★★（★9 操作の群の 1 件 ＋ 「穴 (a) は塞がった」）。
★★**予告されていなかったテストが 1 件落ちた**★★ ——
`test/docs/legacy_design_number_test.dart` の「実測は許可リストと完全一致する」。
★**不具合ではなく台帳の保守そのもの**である（★撤去した `saveMeta` の doc がペイン番号を 1 つ持っていた ——
★**減った側**で、そのテスト自身が「許可リストからその行を消す」と手順を書いている）。
★**件数だけ直さず、消えた理由を許可リストに残した。**

★★**このコミットで動くようになったもの / まだ動かないもの**★★（★**「同期が動く」と読ませない**）——

| | |
|---|---|
| ★**動くようになった** | ★**デッキ編集の 10 操作**（9 ＋ 削除）**が、どの入口から来ても DB の編集ログに残る**。★R2 のメタ編集も R3 と同じ記録点を通る |
| ★**動くようになった** | ★**本体とログが同じトランザクションで動く**（保存 / 削除とも）。★片方だけ残る状態が作れない |
| ★★**まだ動かない**★★ | ★★**同期の送受信は 1 行も無い。**★★ ★ログを**読む**コードも `lib` に 0 行（★検査だけが表を直に引く） |
| ★★**まだ動かない**★★ | ★**衝突の判定 (f-1) / 解決の粒度 (f-2) / 解決の主体 (f-3) は開いたまま**（★正は同期設計メモ §17-10） |
| ★★**まだ動かない**★★ | ★**N-16**（ログをいつ捨てるか）/ **N-17**（失ったとき何をするか）。★★**削除の寿命が N-16 に従属する**★★（**D110-4**） |
| ★**穴は 3 つのうち 2 つが塞がった** | ★**(a)** = このコミット / ★**(c)** = commit 3。★★**(b)（`backfillOrd`）は開いたまま**★★ —— ★**A-2 / D-26 と同じ問い**であり、束 A で決める |

★★**束 C の commit 5（9 操作を貯めて渡す / 決定 D110-2）で `loveca-db` が 165 → 168 / `loveca-ui` が 895 → 913**★★
（db: +3 = `deck_dao_test.dart` の「★save は渡された操作列をログに書く」の群。
ui: +18 = `test/data/deck_edit_log_test.dart` の新設。★**内訳はファイルを見ること**）。
★`loveca-core` は **425 のまま**（★1 行も触っていない。★実測で確認した）。★`loveca-data` も **33** のまま。
★★**ここで初めて `ops` に判別力が付いた**★★ —— ★**commit 4 で 0 件だった 3 つの壊し方のうち 1 つが落ちるようになった。**
★★**残る 2 つは 0 件のままである。★これは繋がっていないのではなく、★型の話だからである**★★（★下の (B)(D)）。
★★**実装は 3 層にまたがる**★★ —— ★`DeckEditStore`（貯める / 記録点 = **D110-2**）→ `DeckRepository.save`（通す）→ `DeckDao.save`（**本体と同じトランザクションで書く**）。
★**§17-9-2 の 3（いつ書くか）が決まった** ——★★**保存の時点で書く**★★（★「操作のたびに書く」は採らない。★理由は同期設計メモ §17-9-2）。
★★**2 / 4 は 1 文字も動いていない**★★（★引数を持たせない / ★`sortByRule` は「規則順に戻した」という事実だけで**どの規則かは書かない**）。
★★**決めたことが 2 つある**★★（★上の 4 行が触れていない分。★理由は同期設計メモ §17-9-2）——
★**(1) 貯めた列は「保存が通ったときだけ」捨てる**（★失敗は巻き戻るので、捨てるとやり直しが本体だけを書く）/
★**(2) 断られた `addCard` は記録しない**（★`softDelete` の「当たる行が無ければ記録しない」と同じ形）。
★★**5-3 に相当する問いは立たない**★★ —— ★**R3 に巻き戻しの口が 1 つも無い**（★走査した。★`undo` / `redo` は盤面の Store だけ）。
★★**実測した対は 12 通り**★★（2026-08-30 / ★**12 通りとも仕込んで走らせ、戻してから本番を測った**）——
★★**(C) `DeckRepository.save` が `ops` を DAO へ渡さず `const []` を渡すと 16 件落ちる**★★（★**commit 4 では 0 件だった**。★内訳は `deck_edit_log_test` 15 ＋ `deck_delete_log_test` 1）/
**(B) `DeckDao.save` の `ops` を既定値つきにすると 0 件**（★commit 4 と同じ）/
**(D) `DeckRepository.save` の `ops` を既定値つきにすると 0 件**（★同上）/
★★**(E) 9 操作の記録を 1 つずつ外した 9 通り —— 9 通りとも「9 操作がそれぞれ 1 件残る」群の★対応する 1 件が落ちた**★★
（★**群の中で落ちるのはその 1 件だけである**。★群の外では合成のテストも落ちるので、★落ちた総数は **1〜6 件** ——
`setName` 6 / `setMemo` 4 / `addCard` 4 / `sortByRule` 3 / `removeCopy` 2 / `moveEntry` 2 /
★**`replaceEntries` / `applyMeta` / `removeEntry` はちょうど 1 件**）。
★★**(B)(D) が 0 件のままであることは「繋がっていない」ではない**★★ ——
★**既定値にしても既存の呼び出し元は全部が明示で渡すので、挙動が 1 つも変わらない。**
★**commit 4 の記録「型でしか見えないものは、型でしか守れない」がそのまま成り立っている。**
★★**予告どおり既存が 2 件落ちた**★★（★どちらも「落ちるはず」の落ち方）——
**(a) `deck_delete_log_test.dart` の「対: 削除以外は 1 件も残らない」**（★そのテスト自身が「コミット 5 で書き換わるはず」と書いていた。
★**対を緩めず、見る対象を「行が在るか」から「`deleteDeck` の行が在るか」へ移した**）/
**(b) `reduce_call_site_test.dart` の走査**（★★**D-30 を再び踏んだ**★★ —— ★`DeckEditStore` の doc に
盤面のクラス名を書いたら、★`lib` をその字面で走査しているテストが拾った。★**言い換えて避けた**）。
★★**テストの doc に書いた断定が 1 つ偽だった**★★ —— ★測る**前**に「1 つ外したらその 1 件**だけ**が落ちる（実測で確かめてある）」と書き、
★**測ったら偽だった**（★合成のテストも落ちる）。★**型は D-15 (j)**（検証していない完了報告）。★**書き直した。**

★★**束 C の commit 4（`save` が操作列を受け取る / ★署名だけ）で `loveca-core` が 421 → 425 / `loveca-db` が 162 → 165**★★
（core: +4 = `deck_edit_op_test.dart` の `DeckEditOpRecord` の群。
db: +3 = `deck_dao_test.dart` の「★save が操作列を受け取る」の群。★`loveca-ui` は **895 のまま** ——
★**呼び出し元を 26 箇所直したが、テストを 1 件も足していない**）。★**内訳はファイルを見ること。**
★★**§17-9-7 の予告「4 の時点では挙動が 1 つも変わらない」は当たっていた**★★ ——
★**既存の 895 / 165 / 425 が 1 件も落ちていない**（★落ちるとすれば署名変更そのものの誤りである、と予告が書いている）。
★★**§15-7-4 の見積り（20 箇所）は 3 種の理由で外れていた**★★ —— ★**正は新所見 D-37 と D-31 の 2 例目**。
★**実測は 57 箇所**（lib 5 / test 51 / `FakeDeckRepository` の override 1）。
★★**うち安全を買っているのは lib の 5 箇所だけである**★★（★残り 52 は `required` の性質上ついてくる費用）。
★★**引数の型は `loveca_core` の `DeckEditOpRecord` ＝ record の typedef である**★★
（★`({DeckEditOpKind kind, DateTime at})`。★**名前つきクラスにしない** —— ★クラスは
フィールドを 1 行足すだけで増やせるが、★**record は形が型なので足した瞬間に全構築点が落ちる**）。
★**持つのは kind と at の 2 つだけ**（★`id` は DB が採り、★`deck_id` は既に引数で決まっている）。
★★**実測した対は 4 つ**★★（2026-08-30 / ★**4 つとも仕込んで走らせ、戻してから本番を測った**）——
★★**(A) 型に引数を 1 つ足すと `deck_edit_op_test.dart` が丸ごと走らなくなる**★★
（★**コンパイルで落ちる。★実行時ではない** —— ★`invalid_assignment` が **2 件**。★`loveca_db` / `loveca-ui` は **0 件**で、
★**構築点が在る場所でだけ落ちる**。★いまの構築点はテストの中だけである）/
★★**(B) `DeckDao.save` の `ops` を既定値つきにしても 0 件落ちる**★★ /
★★**(C) `DeckRepository.save` が `ops` を DAO へ渡さず `const []` を渡しても 0 件落ちる**★★ /
★★**(D) `DeckRepository.save` の `ops` を既定値つきにしても 0 件落ちる**★★。
★★**(B)(C)(D) が 0 件であることがこの commit の要点である。**★★
★**「渡した列が無視されていないこと」は★このコミットでは確かめようが無い** —— ★中身を 1 行も書いていないので
`ops` は**証明可能に未読**であり、★**観測できる差が 1 つも無い**。★**推測で埋めない。**
→ ★**「空の列でログが増えない」の群は、★commit 4 では判別力を持たない**（**D-27**）。★**その旨をテストの doc に書いた。**
→ ★★**`required` の利得も同じくテストで固定できない**★★（(B)(D)）—— ★**型でしか見えないものは、型でしか守れない。**
★`loveca-data` はこの commit で触っていない（★**33 を実測で確認した**）。

★★**束 C の commit 3（穴 (c) を塞ぐ / C-iv / 決定 D110-3）で `loveca-db` が 155 → 162 / `loveca-ui` が 890 → 895**★★
（db: +7 = `deck_dao_test.dart` の「削除は編集ログに 1 件残る」の群。
ui: +5 = `test/data/deck_delete_log_test.dart` の新設。★**内訳はファイルを見ること**）。
★★**§17-9 の予告「署名を 1 つも変えない唯一のコミット」は当たっていた**★★ ——
★`DeckDao.softDelete(String deckId, DateTime at)` は名前も引数も返り値も 1 文字も動かず、
★**削除された既存行は実装側 8 行（旧 `softDelete` の本体）＋ テスト側 1 行（drift の `show` 節）だけ**である（★走査）。
★**既存の test が 1 件も書き換わっていない**（★`FakeDeckRepository` も 1 文字も触っていない）。
★★**入れ子は「測った」のではなく構造で確かめた**★★ —— ★`DeckDao.softDelete` は自前のトランザクションを持っておらず、
★呼ぶのは `DeckRepository.softDelete` だけで、★`loveca-ui/lib` の `.transaction(` は **0 件**（★再走査）。
★**§16-8 #5（入れ子がこの executor で成り立つか）は未測定のまま残る**（★避けただけである / **D-28** の作法）。
★★**D110-4 は `deck_dao.dart` の doc に置いた**★★（★「ここに残る 1 行が削除の寿命そのものになる」）—— ★**N-16 を決める人が読む場所**である。
★★**「衝突が消えた」ことはテストで固定できない。★固定したのは前提までである**★★ ——
★検出層を**読む**コードが 1 行も無い（同期の送受信は §17-9-7 の 6 コミットのどれにも入っていない）ので、
★**固定できるのは「1 回の削除が `decks` の行とログの両方に同時に残る」ところまで**である（★テストの doc に範囲を書いた）。
★★**決めていないことを 1 つだけ判断した**★★ —— ★**当たる行が無ければログを書かない**（`updated == 0` で return）。
★理由: 存在しない `deckId` でも UPDATE は 0 行で**例外を出さない**（★既存の挙動。★変えていない）ので、
★無条件に書くと**起きていない削除が履歴に残り、検出層がそれを配る**。
★**列（`kind` / `at` / `deck_id`）の話ではなく「記録点がいつ発火するか」の話**であり、★このコミットの論点そのものである。
★★**実測した対は 5 つ**★★（2026-08-30 / ★**5 つとも仕込んで走らせ、戻してから本番を測った**）——
**(A) ログの INSERT を丸ごと外すと db が 5 件 / ui が 3 件落ちる**（★db で落ちない 2 件は「対: 削除以外は残さない」と「同時性: UPDATE が失敗したらログも残らない」/ ★ui で落ちない 2 件は「前提: 空」と「対: 削除以外は残らない」）/
★★**(B) `db.transaction` の包みだけ外すと「対: ログの INSERT が失敗したら削除も残らない」の 1 件だけが落ちる**★★ /
**(C) `updated == 0` の門を外すと「当たる行が無ければ記録しない」の 1 件だけが落ちる** /
**(D) `kind` に `.key` ではなく Dart の識別子（`.name`）を入れると 3 件が落ちる**（★**D110-1** がわざと違えた 1 件を見張る対）/
**(E) ログを書かないまま ui を測ると 3 件が落ちる**（★上の (A) の ui 側。★**入口 2 本と合計の 3 件**）。
★★**(B) がこの群の要石である。**★★ ★**(A) を仕込んでも「同時性: UPDATE が失敗したらログも残らない」は通る** ——
★包まなくても「UPDATE が失敗すれば INSERT へ進まない」ので、**その 1 件だけでは包んだ証拠にならない**（**D-27**）。
★**逆向き（ログの INSERT を失敗させて UPDATE が巻き戻ることを見る）だけが包みを見ている。**
★`loveca-core` / `loveca-data` はこの commit で触っていない（★**421 / 33 を実測で確認した**）。

★★**束 C の commit 2（ログの表 / `schemaVersion` 3 → 4 / 決定 D110-1）で `loveca-db` が 142 → 155**★★
（+13 = `migration_test.dart` の **v3 → v4 の群 12** ＋ **v1 の群 +1**。★**内訳はファイルを見ること**）。
★★**この commit は「書き込みが 1 件も無い器」である**★★ —— ★`lib` から `deck_edit_ops` へ INSERT する経路は 0 本で、
★**入れているのはテストだけ**（★「新表が空である」と★「空でない状態は作れる」を対で置いてある / **D-10**）。
★★**§17-9 の予告「既存テストが 2 件落ちる」は外れていた —— 実測は 3 件**★★。
★**走査が `_userVersion(db)` と★変数名まで含んでおり、`_userVersion(first)` / `(second)` を飛ばしていた**（★型は **D-31**）。
★**外れたのは件数だけで、予告の中身は当たっている** —— ★**3 件とも `Expected: <3> Actual: <4>` で、「落ちるはず」の落ち方**である。
★**経緯の正は `docs/同期設計メモ.md` §17-9-1。★件数をここに書き足さない。**
★★**実測した対は 5 つ**★★（2026-08-30 / ★**5 つとも仕込んで走らせ、戻してから本番を測った**）——
**(A) `m.createTable(deckEditOps)` を外すと 8 件が落ちる**（★v3 → v4 の群 7 ＋ v1 の群 1）/
**(B) 枝が `backfillOrd()` を呼ぶと「並び（ord）が 1 つも動かない」の 1 件だけが落ちる** /
**(C) 枝が `decks` を書き換えると 3 件が落ちる**（★新設分は「決定 D109: updatedAt / revision が動かない」）/
**(D) 枝が `deck_entries` を 1 行消すと 9 件が落ちる**（★新設分は 2 件）/
★★**(E) 巻き戻しから `DROP TABLE deck_edit_ops` を外すと「★前提: 巻き戻した DB は v3 の形」の 1 件だけが落ち、★残り 11 件は全部通る**★★（★群は 12 件）。
★★**(E) がこの群の要石である。**★★ ★drift の `Migrator.createTable` は **`CREATE TABLE IF NOT EXISTS`** を吐く（★実読）ので、
★**巻き戻しが表を落とし損ねると移行は例外も出さずに空振りし、★群ごと無言で無意味になる。**
★`migration_test.dart` の `_rewindToV1` が予告していた「★より悪い場合は黙って通って**移行を一度も検証しないテストになる**」の実物である。
★★**(B)(C)(D) は「ユーザデータに触らない」側の対である**★★ —— ★**(A) を仕込んでも 3 件とも通る**ので、
★**移行を無効化するだけでは、それらが対象を見ているか分からない**（**D-27**）。★だから壊し方を 3 通り分けて測った。
★`loveca-core` / `loveca-ui` / `loveca-data` はこの commit で触っていない（★**421 / 890 / 33 を実測で確認した**）。

★★**束 C の commit 1（語彙を `loveca_core` へ / 決定 D110-1）で `loveca-core` が 411 → 421**★★
（+10 = `deck_edit_op_test.dart` の新設）。
★★**この commit は「決めていないこと」を型で決めないためのテストを含む**★★ ——
★引数の型を 1 つも持たないこと / ★`tryFromKey` が `null` を返すのは**寛容にするという決定ではない**こと。
★★**実測した対**★★: **`deleteDeck` のキーを識別子と同じ字面に戻すと 2 件が落ちる**
（「キーは Dart の識別子の写しではない」と「★対: 知らないキーは null」）。
★**2 件目は予想していなかった** —— ★識別子で引けるようになるので、`tryFromKey` の対が同時に崩れる。
★`deleteDeck` のキーだけ記録点のメソッド名 `softDelete` にしてある。
★`loveca_db` / `loveca-ui` / `loveca-data` はこの commit で触っていない。

★★**束 C の commit 0（A-0 の残務 ＋ **U32**）で `loveca-ui` が 874 → 889**★★
（+15 = `test/docs/legacy_design_number_test.dart` の新設 **7** ＋
`test/docs/decision_number_test.dart` の新設 **8**。★**内訳はファイルを見ること**）。
★`loveca-core` / `loveca_db` / `loveca-data` は**件数が動いていない**（★コメント 1 行ずつ直しただけ。★実測で確認した）。
★★**実測した対は 2 つ**★★（2026-08-30）——
**(A) 書き換え前の形を実ファイルに 1 つ戻すと「実測は許可リストと完全一致する」が落ちる**
（★`deck_id.dart` が余分な鍵として出る）/
**(B) 台帳に無い番号を実ファイルに 1 つ置くと「決定番号は 1 つ残らず台帳に在る」が落ちる**
（★どのファイルが引いているかまで出る）。
★★**どちらも仕込んで走らせ、戻してから本番を測った**★★（**D-10** —— ★0 件は「無い」と「見えていない」の区別がつかない）。

★★**その次の commit（U32 の走査の穴）で `loveca-ui` が 889 → 890**★★
（+1 = 太字の飾りが付いた参照を拾うことの陽性対照）。
★★**穴は実在した**★★ —— ★`決定 **DNN**` の形が corpus に在り、★**最初の版は黙って飛ばしていた**。
★**型は D-31**（★狭めた走査はそのまま穴になる）。★**置いた当日に自分で踏んだ。**

★★**束 B の commit 2 で `loveca-core` が 385 → 411**★★
（+26 = `deck_order_test.dart` の新設。区分順 3 / 区分ごとの軸 5 / null の末尾 4 /
副次キー 2 / `sortedByDeckOrder` 3 / 挿入位置 6 / 比較器の性質 3）。
★★**no-op の比較器を入れると 26 件中 21 件が落ちることを実測した**★★（2026-08-27）。
★**さらに、フィクスチャを整列済みに直すと `_expectScrambled` が落ちる**ことも実測した
—— **投入順と期待順が一致していると、比較器が何もしなくても `sort` が入力順を保つので通る。**
★`loveca_db` / `loveca-data` / `loveca-ui` はこの commit で触っていない。

★★**束 B の commit 3（`schemaVersion` 2 → 3 / 決定 D65 / D99）で `loveca-db` が 127 → 139**★★
（+12 = `migration_test.dart` の v2 → v3 の群。前提 2（保存順 / 規則順 / `printingId` 昇順が
3 つとも違う・巻き戻した DB が v2 の形）/ 移行の結果 7 / 2 回目に走らないこと 1 /
★**マスタが空でも落ちないこと** 1 / ★**`all` と `byId` が同じ並びを返すこと** 1）。
★★**実測した対は 2 つ**★★（2026-08-27）——
**(A) backfill を無効化すると 22 件中 6 件が落ちる** /
**(B) `all` の `ORDER BY` だけを落とすと「経路差」の 1 件だけが落ちる**
（★片方だけ壊して 1 件だけ落ちることを見ないと、両方壊れていても通る検査になりうる）。
★`loveca-ui` は 861 のまま（**1 件を差し替えただけ**——
「開き直すとカード番号順に戻る」は D65 の事実の固定で、D99 で**向きが逆になった**）。

★★**束 B の commit 4（画面を `ord` に切り替え / 決定 D99）で `loveca-ui` が 861 → 875**★★
（+14 = `test/state/deck_edit_store_test.dart` の新設 **10** / `deck_repository_test.dart` **+3** /
`deck_edit_page_test.dart` **+1**。★**内訳はファイルを見ること**）。
★★**2026-08-27 訂正: ここは 9 / +4 と書いてあった**★★——
**合計 14 が合っていたので気づかなかった**（9+4+1 も 10+3+1 も 14）。
★**自己検査で実測して見つけた。**★**内訳は合計が合っていても間違いうる。**
★★**実測した対は 3 つ**★★（2026-08-27）——
**(A) 挿入位置の計算を外して末尾に足すと 2 件が落ちる** /
**(B) `isDirtyAgainst` を `Map` 比較（D65 の形）に戻すと 4 件が落ちる** /
**(C) 開いた直後に並べ直す（撤去した `normalizedEntries` の代わり）と 3 件が落ちる**。
★`loveca-core` / `loveca_db` / `loveca-data` はこの commit で触っていない。

★★**束 B の commit 6（D-13 の根治）で `loveca-db` が 139 → 142 / `loveca-ui` が 875 → 874**★★
（db: +3 = 別のハッシュで直すと 0 件に戻る / ★対: 成功のあとの失敗は未解消のまま /
別のファイルを巻き込まない。ui: ★**2 件を 1 件に置き換えた**ので **−1** ——
「そのあと取り込めています」の出る側と出ない側が、**出る状態そのものが作れなくなった**）。
★★**実測した対**★★: `recordFile` の `DELETE` を外すと `master_importer_test` の 2 件が落ちる。

★★**M-B7 で増えたのは `loveca-core`（380 → 385）と `loveca-ui`（826 → 861）**★★
（core: +5 = R-S1 の集合と `stopsAutoAdvance` の導出 / 8.4.12 に立てないこと /
★**止まらないものの対の表** / `requiresOpponent` と独立に立つこと。
ui: +35 = 自動進行の番人 24（`board_auto_advance_test.dart` 新設）/
D-21 の本体 4 / 整理の帯の畳み込み 4 / U19 の新しい測定 1 / 通過と止まった理由の行 2）。
★★**うち 2 件は「いまの挙動をそのまま固定する」ためのものである**★★ ——
新所見 **D-24**（10.6 の警告がエール処理中の札にも立つ）を直したときに
**押下回数が変わることが検査で分かる**ようにしてある。
★`loveca_db` / `loveca-data` は触っていないので 127 / 33 のまま（★どちらも実測で確認した）。

★以下は D96 / D97 時点の記録（残す）——
★★**D96 / D97 で増えたのは `loveca-core`（376 → 380）と `loveca-ui`（777 → 826）**★★
（core: +4 = 入口が 0 枚のエネルギーデッキ（例外が出ないこと / 1 枚なら 1 枚だけ出る対 /
0 枚では乱数を消費しないこと / 消費する対）。
ui: +49 = 補完の撃ち分け 14 / 走査テスト 7 / 設定の往復 6 / 開始ダイアログ 11 /
R3 の軸 2 が 5 / R6 と D-23 が 6）。
★★**うち 1 件は実機で見つけた不具合の受け**★★ —— R6 が「いま補うカード」を
**起動時のスナップショット**で出していた。★ウィジェットテストでは出ない
（`env.settings` と `settingsStore` が最初から一致しているため）ので、
**両者をわざと食い違わせる**形で固定した。
★`loveca_db` / `loveca-data` は触っていないので 127 / 33 のまま（★どちらも実測で確認した）。

★以下は D95 時点の記録（残す）——
★★**D95 で増えたのは `loveca-core`（351 → 376）と `loveca-ui`（767 → 777）**★★
（core: +25 = 保存の不変条件と走査の判別力 / 動かせない札の扱い / 10.4.1 の 2 か所目 /
数え方 / 10.5.1・10.5.2 の陽性対照。ui: +10 = 理由の撃ち分け / 盤面のタグ / 常設の帯 / U19 帯あり）。
★`loveca_db` / `loveca-data` は触っていないので 127 / 33 のまま（★どちらも実測で確認した）。

★以下は M-B6 時点の記録（残す）——
★**M-B6 で増えたのは `loveca-core`（336 → 351 / 6.2.1.6 のマリガン）と
`loveca-ui`（693 → 754 / マリガンの画面・整理・11.10 / 11.11・ライブ勝敗・写像の適用）**。
★★**M-B6 の実機確認（決定 D94）でさらに `loveca-ui` が 754 → 767**★★
（+7 表示文字列の走査 / +6 整理の抑止条件。★`loveca-core` は 1 行も触っていないので 351 のまま）。
`loveca_db` は変更していないので 127 のまま（★skip 0。実データが在る状態で実測した）。
`loveca-data` も触っていないので 33 のまま（★実測で確認した）。

★★**`loveca-ui` は 587 と書かれていたが実測 590 だった（2026-08-25 に訂正）。**★★
表を書いたコミット（`7fba617`）の**あと**に `board_progress_test.dart` へ 3 件足した
コミット（`5cbbe47`）があり、**表だけが更新されなかった。**
★**この表は「他の文書より優先する正」なので、古いまま放置すると
「増減しないはずの作業で増えた」という誤った読みを生む。**
→ ★**テストを足したコミットで必ずここを直すこと。**同じ数を 2 箇所に書く問題
（`ルール整合性チェック_v1.06.md` D-15）と同じ型で、**ここでは「コードと表」の 2 箇所である。**

★★**2026-08-25: 上にそう書いたのに、また同じことが起きた。**★★
M-B3 完了時の表（`loveca-core` **293** / `loveca-ui` **590**）が
**M-B4 でテストを足したときに更新されず**、実測は **307 / 638** だった。
★**「必ずここを直すこと」と書いても守られない。**手で書く数である以上、また古くなる。
→ ★★**読む人は数を信じる前に確認コマンドを実行すること。**★★

★★**ただし、ここでは「数を書くのをやめる」という手は採れない。**★★
`ルール整合性チェック_v1.06.md` の **D-15 (f)**（`CLAUDE.md` §8 の `M-B4` の行数）は
**正が `docs/盤面設計メモ.md` §14-7 にあった**ので数そのものを消した。
**ここは違う。この表が正であり、対になるのはコードそのもの**（`dart test` / `flutter test` で
機械が数えられる）なので、**消さずに直す。**★どちらの手を採るかは
「もう 1 つの側が文書か、機械で数えられるものか」で決まる。

★`loveca-db` の 127 件には **移行テスト（`migration_test.dart` / 10 件）**を含む。
`onUpgrade` が走ること・`decks` が残ること・索引が建て直ることを固定している
（`ルール整合性チェック_v1.06.md` D-6 の解消）。

★`loveca-ui` のテストは **M1 で初めて置いた**（決定 D58）。
固定しているのは `PaneScaffold` の 1/2 ペイン切替（D61）・`AppInfo.version` と
pubspec の突き合わせ・**起動ゲートの段ごとの失敗の区別**・dist の 3 段解決と不在の検出（D60）・
一覧の絞り込み（D48）。
★**M2 で足したのは**「保存 → **DB を開き直す** → 内容が一致する」（実 DB を使う。M2 の目的そのもの）・
`revision` が**保存回数**で増えること・`updatedAt` が `Clock` 由来であること・
論理削除が一覧から消えて DB には残ること・**検証が DB へ行かないこと**（DB を閉じたあとでも
`validate` が答える = 決定 D55 の機械的な証明）・ホームが R2 であること。
★★**M3 で足したのは「縮退が実際に起きる入力で、表示に使う値が立つこと」である。**★★
D-10 の教訓（「検知手段を書いたら『見つかるはずのもの』を仕込んで動かす。
0 件は『無い』と『見えていない』の区別がつかない」）を、
**縮退を見せる仕組み自体に適用した。**
`test/data/card_search_degradation_test.dart` は**実 DB**に対して
`limit` を下げて**本当に打ち切り**（D50）・2 文字の語で**本当に `likeFallback` へ落とし**（D40）・
`deleteExpansion` で**本当に孤児を作る**（D-8 / D63）。
★★**「起きない入力で出ないこと」も必ず対で固定してある。**★★
出る側だけ見ると、**常に出す実装**でも通ってしまう。
フェイクを使うのは画面側（デバウンス回数・3 つの縮退が別々の行として読めること・
失敗が 0 件表示にすり替わらないこと）だけで、**役割を混ぜない。**

★★**M4 で足したのは「守らないと静かに壊れる知見を、外すと落ちるテストで固定すること」である。**★★
`test/common/card_drag_test.dart` は**対照実験**を持つ——
色を持たない素の `Draggable` を同じ形で並べ、**行の余白では掴めないこと**を固定する。
★これが通らなくなったら「ウィジェットテストでは D46 が再現しない」という意味であり、
**成功側のテストが何も証明していない**ことを示す。
★★**2 つの手当ては「外すと落ちる」ことを実際に確かめてある。**★★
`dragAnchorStrategy` を外したら「掴んだ位置が違っても判定は変わらない」が
**trailing / leading に割れて落ちた**。★最初は落とす点が端だったため**外しても通っていた**
（D-10「検知手段自身が同じ罠を踏む」の実例）。
`DragStartMode` は **immediate / longPress × マウス / タッチ の 4 経路**を通す
（PC で使われない経路は `spike/` と同じ性質で静かに腐る / D51）。
ほかに固定したのは 3 操作（一覧→デッキ / 並べ替え / ゴミ箱）・
**4 枚制限がメインデッキだけに効くこと**（★エネルギーで効かない側も対）・
**カードの増減で `revision` が跳ねないこと**・
**ドラフトの検証が DB を閉じても答えること**（決定 D55）・
**U8 の実測**（`test/ui/deck_pane_width_test.dart`）。

★★**M5 で足したのは「実データの多様性を写した fixture で見ること」である。**★★
M4 の誤検知（区分をまたぐと縮退が出る）は**テストデータが単純すぎて通った**例だった。
`test/support/real_shaped_catalog.dart` は**実在の 6 種 / 9 刷り**を dist から写してある——
複数キャラ名 3 + 複数グループ名 3（★実データで 6 種しかない）/ 同じ cardNumber の 3 刷り
（パラレル 2・商品もまたぐ）/ ★**色ブレードハートと `bladeHeartEffects` の同居**（59 種）/
`SCORE` / `ALL` / `GRAY` / ★**全フィールドが空のエネルギー**（567 種すべてこの形）。
固定しているのは **2 ペインと 1 ペインで同じ `CardDetailPane` が出ること**・
**R3 / R4 の両方から開けること**・`normal` の `cacheWidth` が原寸 500px を超えないこと
（★DPR 3 で**画面から通した経路**。クランプを外すと 1080 で落ちる）・
`imageHash` が空ならプレースホルダのまま（★空でなければ絵が出る、と対）・
★**色ハートとアイコンが別見出しで出ること**・**ライブの枠が横長になること**。
★★**`ListView` は画面外を作らない。**★★「出ないこと」を見る検査は
**末尾までスクロールしてから**行う（送らずに見ると常に通る / D-10 と同じ形）。

★★**U11 の解消（決定 D72）で足したのは「箱は変えず、中の枠だけ種別で選ぶ」ことの固定である。**★★
`test/ui/card_art_test.dart` は**5 箇所すべて**（一覧セル / デッキ行 / 一覧セルの feedback /
デッキ行の feedback / P3 カバー選択）で「ライブは横長 / ★対 メンバー・エネルギーは縦長のまま」を見る。
★★**同時に「決定 D42 の測定条件が動いていない」も機械で固定してある**★★——
タイルの寸法が種別で変わらないこと、**`ResizeImage.width` がライブとメンバーで一致し
タイル論理幅 × DPR に等しいこと**。これが「再測定は不要」の根拠であり、**文章より強い。**
★掴める / 押せる矩形が帯を含むことは `test/ui/deck_edit_page_test.dart`（D46 の群）が持つ——
**掴む点が絵の外であることを先に検査してから**掴む（そうしないと絵の上を掴んで通ってしまう）。

★★**M-B2 で足したのは「D46 / D47 / D72 が盤面で成立することを、外すと落ちる形で固定すること」である。**★★
`test/board/board_drag_test.dart` は**盤面の 5 か所**（手札 4.11 / メンバーエリア 4.5 /
ライブカード置き場 4.6 / 控え室 4.12 / 解決領域 4.14）で
**ライブの札の「帯」（絵の外・箱の中）から掴めること**を見る。
★**掴む点が絵の外であることを先に検査してから掴む**（帯 > 10px。枠線 1 + 余白 2 では満たせない値）。
★**対で、メンバーも同じ相対位置から掴める**ことを固定する（種別依存の不具合を疑うのだから対が要る）。
★★**対照実験は 2 段ある。**★★ (1) 常設 —— 同じ形を素の `Draggable` で包むと帯で掴めない
（★対: 絵の上なら掴める）。(2) 手作業 —— `card_drag.dart` の `ColoredBox` を外すと
**このファイルが 19 件落ちる**ことを 2026-08-25 に実測した（決定 D85）。
★ほかに固定したのは **`DropEdge` の撃ち分け**（★順番を管理しない領域では帯が出ない・
メンバーが 0 人でも出ない・**メンバーを掴んだときも出ない**）・
**メンバーが 2 人以上いるエリアへの「下に置く」で選択が出ること**
（★**末尾ではないメンバーを選んで**確かめる。末尾を選ぶと「黙って末尾に入れる実装」でも通る）・
**`DragStartMode` の両値 × マウス / タッチ**・
**1 操作につき履歴がちょうど 1 件増えること**（= 直接 `GameState` を書き換えていない証拠）・
★**`GameSession` に触れるファイルが `game_store.dart` だけであること**
（`test/board/reduce_call_site_test.dart`。★**陽性対照つき** —— 同じ走査が `loveca_core` では当たる）。

★★**M-B5 で足したのは「巻き戻しが本当に戻ることと、戻らないものが戻らないこと」である。**★★
`test/state/board_session_test.dart` は**決定 D51 が戒めていた負債の返済**——
D78 の根拠（巻き戻しを含むログを同じ seed で再生すると一致する / 落とすと一致しない）は
**リポジトリの外の使い捨てスクリプト**で確かめたままだった。
★**再生は本番の口**（`dispatchAll` / `undo` / `undoStep`）を通す。
★★**対を 4 つ置いた**★★ ——(1) 巻き戻しを落とすと一致しない (2) **モードが違うと一致しない**
(3) ★**2 周目の 8.4.9 から 1 ステップ戻ると 1 周目の 8.4.12 に着く**（静的グラフの逆辺なら 8.4.8 で落ちる）
(4) ステップ内で操作していたら**そのステップの入口**に着く（カーソルは変わらない）。
★`GameState` に `==` が無いので**構造の署名**を作って比べる（領域は `Zone.values` から回す）。
★ほかに固定したのは **`CountingRng` が乱数列を変えないこと**（陽性対照つき）・
**合成が履歴を 1 件しか増やさないこと**（★画面から通せる 6 経路すべて）・
**途中で投げたら丸ごと捨てること**・**`maxDepth` 到達で帯が出ること**（★**本当に到達させる**。対つき）・
**乱数を消費した undo でだけ注記が出ること**（★対: `FlipCard` では出ない）。
★★**`CountingRng` の `_count++` を外すと 3 ファイル 8 件が落ちる**ことを 2026-08-25 に実測した。★★

★**フレーム統計は測らない。** profile ビルドでしか出ず、それは `spike/` の資産（D51）。

★`loveca-db` のテスト結果を報告するときは **skip 件数も併記すること。**
実データ（`loveca-data/data/dist/`）を使うテストは `data/` が git 管理外のため、
未配置なら `markTestSkipped` で理由を明示して飛ばす。
「全通過」の報告に skip が埋もれると、検証しているつもりで検証していない状態になる。

**`fetch` は明示的な指示がない限り実行しない。** 公式サイトへの実アクセスを伴う。

---

## 4. 公式サイトへのアクセス制約（絶対に緩めない）

| 項目 | 設定 |
|---|---|
| 並列度 | 1（直列のみ） |
| 間隔 | 1.5 秒 |
| リトライ | 指数バックオフ 最大 3 回 / 連続 5 回失敗で自動停止 |
| `per_page` | 100（増やさない） |
| User-Agent | 素性と連絡先を明記 |
| 実行頻度 | 新商品発売時のみ。定期巡回しない |

取得と正規化は完全に分離してある。**正規化ロジックを何度直しても公式サイトを再度叩かない。**

---

## 5. ★ 実データで判明した落とし穴

### (1) API のフィールド名を信用しない

公式 API は CMS の汎用カラムを流用しており、名前と中身が乖離している。
**`card_kind` による分岐が必須。**

| JSON フィールド | メンバー | ライブ |
|---|---|---|
| `cost` | コスト | ブレードハート（の一部） |
| `heart` / `heartNN` | 基本ハート | **必要ハート** |
| `attack` | ブレード（数値） | **ブレードハート** |
| `blade_heart` | ブレードハート | **スコア** |

### (2) 色マッピングが 3 系統あり、共用してはいけない

```
系統A（heartNN フィールド）: 01桃 02赤 03黄 04緑 05青 06紫
系統B（heart_NN.png 画像）  : 01桃 02緑 03青 04赤 05黄 06紫   ★A と 02/03/04/05 が食い違う
系統C（日本語文字列）        : 桃/赤/黄/緑/青/紫/無/ALL
```

効果テキストのアイコン置換に系統 A を使うと**カードテキストの色表示が全件誤る**。
公式の `meta.text_icons` 自体にバグがある（黄と緑が同じファイルを指す）。
`meta` は実行時に信用せず、新アイコン検知にのみ使う。

### (3) 画像 URL を組み立ててはいけない

```
PL!N-bp1-034-PE＋  → PL!N-bp1_034-PE2.png        ハイフンがアンダースコア
PL!N-bp1-999-SEC＋ → SEC2-PL!N-bp1-999-SEC2.png  接頭辞つき
```

`picture` フィールドの実値をそのまま使う。

### (4) パラレル判定は「刷り単位」

同じカードが複数商品に再録されると**通常刷りが複数になる**。
`isParallel`（刷り単位）が正。`isBasePrinting`（cardNumber ごとに代表 1 枚）という概念は**誤りとして廃止済み**。
`parallel_param` は基本刷りフラグではない（実測で否定済み）。判定は公式検索 `parallel=normal` の集合で行う。

**パラレル表示 OFF = `isParallel == false` の刷りを「すべて」表示する。**

### (5) 表記ゆれが実データ内に存在する

`みらくらぱーく！`（全角）と `みらくらぱーく!`（半角）が分裂していた。
グループ名・ユニット名は総合ルール付録 A の表記に寄せ、レアリティは NFKC で半角統一。
**未知の名前は勝手に変えない**（新作品・新ユニットを壊さないため）。

### (6) カード番号の切り出しはホワイトリスト化しない

```python
printing_id = unicodedata.normalize("NFKC", card_number).strip()
card_number = printing_id.rsplit("-", 1)[0]
```

`PRproteinbar` のような公式レアリティ一覧に無い接尾が実在する。機械的な `rsplit` に徹する。

### (7) 同じ意味を公式が複数の書き方で入れてくる

数値やトークンのパースで**数字や括弧を必須にしない**。実測の内訳：

| 意味 | 実在する書き方 |
|---|---|
| ブレードハートのドロー | `ドロー1` 24 刷り / `ドロー` 48 刷り / `[ドロー]` 3 刷り |
| ブレードハートのスコア | `スコア1` 19 刷り / `スコア` 27 刷り / `[スコア]` 1 刷り |
| ALL のブレードハート | `ALL1` / `[全ブレード]`（パラレル刷りのみ 5 刷り） |
| ターン 1 回 | `[ターン1回]` 271 刷り / `［ターン1回］` 全角 9 刷り |

数字必須の正規表現だったため 59 種でブレードハートが、
半角括弧のみの照合だったため 4 種で `TURN_1` が無言で欠落していた。
**色は例外で、5,367 刷り分すべてが数字を伴っていた。**

さらに**同一 cardNumber でも刷りごとに書き方が揺れる**。
`PL!-bp4-022` は `-L` が `スコア`、`-SECL` が `スコア1`。
`normalize_all` は先勝ちなので、壊れた刷りが勝つと丸ごと落ちる。V15 がこれを検出する。

---

## 6. ★ 集計で最も間違えやすい点（Phase 3a の核心）

```
ブレード合計 = メンバーエリアの「アクティブ状態のメンバー」のブレード     （8.3.10）
ハート合計   = メンバーエリアの「全メンバー（ウェイト含む）」のハート      （8.3.14）
エールハート = 共有解決領域の「ownerId == 自分」のカードのブレードハート （8.3.14）
```

**参照範囲が違う。** ウェイト状態のメンバーからはブレードを参照できない。

解決領域は**両プレイヤー共有で 1 つだけ**（4.14.1）。先攻パフォーマンス後も先攻のエールカードが
残ったまま後攻パフォーマンスに入るため、`ownerId` での絞り込みが必須。

ブレードハートには色以外のアイコンがあり、**配信 JSON では色と別フィールドに分かれている**。

```jsonc
"bladeHearts":       { "BLUE": 1 },   // 8.3.14 のハート合計に合算するのはこちらだけ
"bladeHeartEffects": { "DRAW": 1 }    // 合算しない
```

- ドローアイコン → カードを 1 枚引く（8.3.12.1）。**ハート集計 8.3.14 より前**
- スコアアイコン → スコア合計に +1（8.4.2.1）

★同じ `Map<HeartColor,int>` に同居させ直さないこと。
参照範囲も処理する時点も違うため、型で分けておかないと集計で取り違える。
実在数は DRAW 59 種 / SCORE 37 種で、**いずれもライブカードのみ**（メンバーには 1 件も無い）。

---

## 7. 作業ルール

1. **`data/` を消さない・移動しない。** コード更新は既存フォルダへの上書き展開で行う。
2. **コードを編集したら `python tests/run_all.py` を必ず実行する。**
   過去にパッチ適用で関数 7 つが消失した事故があり、`test_imports.py` はその再発検知が目的。
3. **`dist` を再生成したら必ず `verify_contract.py` を通す。**（形式を変えたときだけではない）
   `loveca-core` の `HeartColor.fromKey` / `BladeHeartEffect.fromKey` は未知キーで例外を投げる。
   この厳格さは「配信前に門番が毎回機能すること」が前提で成立している。
   終了コード **2 は「dist が無く限定検査しかしていない」** の意味であり、成功ではない。
   `test/fixtures/` は相互にハッシュ参照するミニ配信物なので、1 ファイルだけ直すと壊れる。一括で更新する。
   なお `build --skip-images` は **`imageHash` も空になる**（画像を読まないため）。
   配信物として使う dist を作るときは `--skip-images` を付けない（`requirements.txt` の Pillow が要る）。
4. **1 コミット = 1 論点。** 複数の修正を混ぜない。
5. 判断に迷う点は勝手に決めず、選択肢と根拠（条番号つき）を提示して確認を取る。
   ★**ただし「迷っていない点」まで確認を取らない**（下の「確認を求めずに進めてよい条件」）。
6. 権利・利用規約に関わる論点は `【要確認/法務】` と明記し、技術的可否と法的可否を分けて書く。
7. ★★**推奨がある選択肢は、確認を求めずに推奨で進めてよい。**★★
   下の 6 条件のいずれかに当たるときだけ止める。

### ★★ 確認を求めずに進めてよい条件 ★★

★**背景**: これまで **20 回以上**の選択肢のうち、**推奨と異なる判断が採られたのは 4〜5 回**だった。
選択肢が出るたびに待つ手間のほうが大きい。

★★**推奨がある選択肢は、確認を求めずに推奨で進めてよい。**★★
★ただし次のいずれかに当たる場合は**必ず止めて確認する**。

| # | 止める場合 | 中身 |
|---|---|---|
| 1 | ★**利用者にしか答えられないこと** | 仕様の希望 / 使い方の想定 / どの絵柄か / 何を「完成」と呼ぶか。★**条文にも実データにも根拠が無く、好みで決まること** |
| 2 | ★**条文に反する、または条文が定めていない状態を作ること** | ★**D-B**（脇置きの落とし先を置くと「条文が定めていない状態」を作れてしまう / 決定 D93-2）が実例 |
| 3 | ★**取り返しがつかないこと** | データの削除 / `data/raw/` への書き込み / git 履歴の破棄 / **`fetch` の実行**（§4） |
| 4 | ★**既存の決定を訂正すること** | D 番号の記述を変える / 確定済みの設計を覆す |
| 5 | ★**範囲が指示を超えること** | 触るなと言われたパッケージに手を出す / 別のマイルストーンに踏み込む |
| 6 | ★**推奨案に自分で不安があるとき** | 「どちらとも言えない」なら止める |

★★**止めずに進めた場合、何を選んだかと理由を報告に必ず書くこと。**★★
★**事後に判断を追える形にする**（推奨で進めたこと自体が黙ると、後から「なぜそうなったか」を辿れない）。

★★**この規約は「確認を減らす」ためのものであって「判断を省く」ためではない。**★★
★**推奨を出す前の検討は従来どおり行う。**
★**却下した案の理由も従来どおり記録する**（決定に相当するものの正は `docs/決定事項一覧.md`）。

---

## 8. 現在のフェーズ

| フェーズ | 状態 |
|---|---|
| Phase 1-A データパイプライン（Python） | **完了**（1,708 種 / 2,527 刷り / 検証エラー 0。Python テストで担保） |
| Phase 1-B `loveca_core` エンティティ・DeckValidator | **完了**（`loveca-core` の **Dart** テストで担保。件数は §3 の表） |
| Phase 2 PC ローカル DB・カードリスト・デッキ構築 | **完了**（第一段階: drift スキーマ + マスタ取り込み層 = 完了。UI 着手前の技術検証 = 完了 / `docs/UI技術検証メモ.md`。検索の改善と移行機構 = 完了 / D49・D50。UI 本実装の設計 = 完了 / `docs/UI設計メモ.md`・D52〜D61。**M1 土台 + R1 + R4 = 完了。M2 R2/R3 最小版 + デッキの書き込み = 完了 / D62・`docs/UI設計メモ.md` §9-4。M3 検索 = 完了 / D63・D64・`docs/UI設計メモ.md` §9-5。M4 R3 デッキ編集 + P1 = 完了 / D65・`docs/UI設計メモ.md` §9-6〜§9-8（★U8 を実測で解消・D-11 / D-12 を記録）。M5 R5 カード詳細 = 完了 / D66・§9-9。★M6 R6 設定・診断 + P3 メタ編集 + 共有形式の入出力 + 複製 = 完了 / D67〜D71・§9-10 / §9-11（★U7 を解消・D-13 / D-14 を記録）。★**U11 の解消 = 完了 / D72・§9-12（★D42 の測定条件は動かしていない・U12 を記録）**） |
| Phase 3a GameState / 集計 / 進行 / 巻き戻し / reduce・redact | **完了**（`loveca-core` の Dart テストで担保。件数は §3 の表）。★★**2026-08-25: 実機で見つかった 4.1.2.1 未実装を修正した（決定 D91）**★★——領域の属性（**4.1.2.1** 表示面 / **4.3.1** 向き / **4.10.2** 置き場所）は**引き継がず、移動のたびに着地先の領域から決める**。写像は `card_move.dart` の`placedIn` / `positionIn` の 2 つだけ。★例外は条文の根拠つき（**4.6.2** / **4.13.2** / **4.1.4**）。★★**2026-08-26: 整理（10.4 / 10.5）で行き先を決められない札を消していた（D-22）のを直した（決定 D95）**★★——**動かさず元の置き場に残す**。★走査で **10.4.1 にも同じ形**があり、★**種別を確かめずに断定している箇所は 2 種類**あった（種別不明 / ライブの孤児）。★検知は「整理の前後で盤面の札が 1 枚も増減しない」不変条件 |
| Phase 3b PC 盤面 UI | **完了**（設計は `docs/盤面設計メモ.md`。★**モードは §14 / 決定 D88**）。★**M-B1 = 完了 / 決定 D80・D81**（R7 + `GameStore` + 6.2.1 の開始手順 + 静的な盤面描画）。★**M-B2 = 完了 / 決定 D82〜D85**（物理操作のドラッグ + `DropEdge` + 反転 / 向き / 重ね置きのメニュー。★**U5 / U16 / U17 を実測で解消**）。★**M-B3 = 完了 / 決定 D86・D87**（進行 `AdvanceStep` + 8.3.6・8.4.12 の分岐 + 集計 + 警告 + 引く / シャッフル / 上から見る。★**のべ 73 ステップを 1 つ残らず通した**。★D83 を測り直し、★**縦の下限を初めて測って新設 U19**）。★**M-B4 = 完了 / 決定 D88・D89 の実装**（モード + 廃止した語の置き換え + dist の 3 層。★**U20 を測って解消**。★**新所見 D-17 / D-18**）。★**M-B5 = 完了 / 決定 D90**（巻き戻し + 合成コマンド + 乱数の注記 + セッションのログ。★**列挙をやめて実測にした**。★**新所見 D-19**）。★**M-B6 = 完了 / 決定 D93**（マリガン 6.2.1.6 + 11.10 / 11.11 の補助コマンド + 整理 `Tidy` + `SetLiveJudgement`。★**範囲を 4.1.6 から導き実データ 29 刷りで裏を取った / 脇置きの落とし先は置かないと確定し doc 3 箇所を訂正（新所見 D-15 (i)）/ 整理ログの器を複数件にした（D-21 の一部を解消）/ U19 が初めて動いた**）。★**M-B6 の実機確認 = 完了 / 決定 D94**（★**実装の誤りが 2 件出た** —— 表示文字列の Markdown 装飾記号 D94-1 / 整理の空振りを警告が抑止していた D94-2。★**見ていない経路 6 つは `docs/盤面設計メモ.md` §16**。★**新所見 D-22 / D-15 (j)**）。★★**M-B7 = 完了 / 決定 D92 の実装 + 決定 D98**★★（自動進行「次へ」＝次の停止点まで + 「1 ステップ」ボタン + 1 押下 = 履歴 1 件 + 通過 / 止まった理由 / 分岐 / 整理の見せ方。★**新所見 D-21 を全件解消**。★**D92 の記述を 3 か所訂正**（D-15 (l)）。★**新所見 D-24**（10.6 の警告がエール処理中の札にも立つ / 未解消）。★**押下回数と U19 を実測**）。★**M-B7 の実機確認 = 完了 / 2026-08-27**（★**実装の誤りは 0 件**。★**§16 の 6 番を確認して台帳が空になった** —— 結果は「盤面はキーボードで操作できない」で**新設 U30**）。★★**Phase 3b は完了である。**★★ |
| Phase 4 認証・同期 | ★★**着手済み**★★（2026-08-31〜）。★★**どこまで進んだかをここに書かない。★正は `docs/同期設計メモ.md` §32-6 の順序と §33 / §37 の実装の記録である**★★（**D-15**）。★★**「同期が動くようになった」と読まないこと** —— ★**送受信は 1 行も無い**★★ |
| Phase 5 スマホ / Phase 6 対戦サーバ | 未着手 |

Dart SDK は導入済み（3.11.1 stable / Flutter 3.41.4）。
Python は **`loveca-data/.venv/Scripts/python.exe`（3.13.12）を使う**。
Git Bash の `python` は MSYS2 の 3.14.3 を掴むため使わない。

### ★★ 盤面のモードは 3 つ。「一人回し」という語は使わない（決定 D88）★★

| モード | 領域 | 秘匿 | 手番 |
|---|---|---|---|
| **ソロ** | ★**自分側のみ** | `redact` を掛けない（★隠す相手が居ない） | ★**常に先攻** |
| **ローカル対戦** | 両側 | `redact` を掛けない（★1 人が両方を操作する） | 8.4.13 で入れ替わる |
| **オンライン対戦**（Phase 6） | 両側 | ★サーバが `redact` を掛けて配る | 同上 |

★★**D88 以前の文書・コード・コミットメッセージに出てくる「一人回し」は、すべてローカル対戦を指す。**★★
語を残すと**同じ語が時期によって別の意味**になり `grep` で新旧を区別できないため廃止した。
★★**免除されている文書をここに列挙しない。正は `docs/決定事項一覧.md` の D88-1 の
grep の除外行 1 箇所である。**★★ 検査もそこが正で、**除外に挙がっていない場所で 0 件であること**を見る。
★★**2026-08-27 訂正: ここには「廃止を記録している 3 つの文書」と数が書いてあり、偽になっていた。**★★
免除される文書は**その後に増えていた** —— `docs/相談役への引き継ぎ.md`（廃止を記録する用法）/
★**原典 `docs/引き継ぎドキュメント.md`** と `docs/引き継ぎドキュメント_注意.md`。
★★**新しい数をここに書き直さない。**★★ 数を書けばまた古くなる
（`ルール整合性チェック_v1.06.md` **D-15** の規約「数を書くなら機械が数えられる形にする」）。
★**同型の記述が `docs/決定事項一覧.md` §3 の「D36〜… は使用済み」にもある**（そちらは**その行が正**なので、消さずに直す）。
★★**免除の理由は 1 種類ではない。**★★「**廃止そのものを記録している**」文書と、
「**廃止より前に書かれた資料そのもの**」（原典）とで理由が違う（`docs/引き継ぎドキュメント_注意.md` §5）。
★**理由が違うものを 1 行にまとめない**（内訳は D88-1 の除外表）。
★★**コード・テストの置き換えは M-B4 で完了した（2026-08-25）。走査は 0 件である。**★★
★★**2026-08-26: この `CLAUDE.md` 自身に「live な記述」として 4 箇所残っていた**★★
（§0 の 2 箇所 / §8 の U13・U17 の行）。`docs/盤面設計メモ.md` は訂正済みで、**ここだけ取り残されていた。**
★**走査では出ない** —— `abolished_term_test.dart` は `.dart` だけを見ており
（`loveca-ui/lib` `loveca-ui/test` `loveca-core/lib` `loveca-core/test`）、**`.md` を 1 つも読まない。**
`docs/決定事項一覧.md` は「語の取り出し元」として読むだけで、走査対象ではない。
★★**だから `.md` 側は機械に見えていない。**★★ 免除の一覧が古くなり
**D88-1 の grep が非 0 を返す状態になっても、テストは緑のまま**である。
★**実際に 2026-08-27 まで誰も気づかなかった。**★**`.md` 側を見るのは D88-1 の手動 grep だけ**で、
★**手動の見張りは忘れられる**（§3 が **D-2** を前例として書いている）。
→ ★**免除される文書を増やしたら、その場で D88-1 の除外行を直すこと。**
★**次に動いたときも、機械は同じように黙っている。**
→ ★**免除が免除なのは「廃止を記録するため」または「原典だから」であって、
live な記述に使ってよいという意味ではない。**★免除と使用許可を混ぜないこと。
★**戻ってこないことは `loveca-ui/test/board/abolished_term_test.dart` が機械で見ている**
（★語そのものはテストに書かず、D88-1 の文書から取り出す。陽性対照つき）。
★★**ただし見ているのは `.dart` だけである。**★★

★★**2026-08-27（M-B7 / 決定 D98）: §16 は 6 項目すべてが片づき、Phase 3b 時点で未確認の経路は 0 件になった**★★
（`docs/盤面設計メモ.md` **§16**）。★**未決ではない。「まだ見ていない」もの**なので下の表には載せない。
★★**表そのものは残してある**★★ —— **何を・どうやって確かめたか**と**実行可能性の欄**（D-15 (k)）の記録であり、
★**3 番の「実行不能」を消してはならない**。★Phase 5 で新しい経路が出たらここに足す。
★**6 番（キーボード編集）の結果は「操作できない」** —— **合成キー入力は届く**（R4 の検索欄に文字が入る）が、
盤面では `Tab` を 8 回押しても **1 px も変わらず**、メニューは `Esc` で閉じない。
★**不具合ではなく手当てが 1 つも無いという事実**であり、**未決 U30** として登録した。

★以下は M-B6 完了時の記述（残す）—— ★**1 / 2 / 5 は実機で見た**（10.5.2 / 10.5.3 / 10.5.4 を種別ごと /
10.6 の警告 / 両袖のボタン）。★**4 は測った**（帯あり 718）。★★**3 は実行不能と確定した**★★——
`GameSetup.begin` が未知の刷りで投げるので盤面が開かない（**D-15 (k)**）。
★**代わりにライブ孤児で D95 の修正を実機で確かめた。**

**次の一手**: ★★**Phase 3b は完了した。次は Phase 4（認証・同期）である。**★★
~~★**着手の前に `ルール整合性チェック_v1.06.md` の D-5 を片づけること**~~
→ ★★**2026-08-27 に片づいた。**★★ 原典（`docs/引き継ぎドキュメント.md`）が見つかり、**`P4` / `P5` は最初から `決定 D11` / `決定 D35` として採番済みだった**ことが分かった（★**D-5 の「`P4` だけは手がかりが 1 つも無い」は誤りだった** / 型は新所見 **D-29**）。`P1` / `P2` / `P3` は **`決定 D100` / `D101` / `D102`** として新規採番した。★★**2026-08-27 訂正: ここには「参照の書き換えは走査 0 件まで済んでいる」と書いてあったが、偽の完了報告だった。**★★ 丸括弧つきの走査は確かに 0 件だが、★**括弧なしの旧番号が `lib` と `test` の両方に生きている**（型は新所見 **D-31**）。★**残務の登録先は `docs/同期設計メモ.md` §1 の A-0。**★★**2026-08-30 に片づけた（束 C の commit 0）**★★ —— ★**括弧なしも書き換え、走査を `.dart` と `.md` の両方へ広げた**。★★**「0 件」は完了条件にできない**★★（★旧番号を説明する文が同じ字面を含む / **D-30**）ので、★**許可リストとの完全一致で見張る**（`loveca-ui/test/docs/legacy_design_number_test.dart`）。★**箇所数をここに書かない。★正はそのテストである。**★★**「走査を `.dart` と `.md` の両方へ広げた」は 2 通りに読める。★字面は変えない**★★ —— ★**指しているのは★一度きりの書き換え作業で、★そのテストは `.dart` だけを見る。**★**偽ではないが読み違えられる。**★正は `docs/同期設計メモ.md` の **N-8** の追記である。
★★**同時に見るもの（束 A）の正は `docs/同期設計メモ.md` §1 である。**★★
★**内訳をここに書かない。**★★**2026-08-27 訂正: 束 A の内訳は 3 か所で食い違っていた**★★ ——
ここは `U13` を挙げて **U9** と **D-26** を落とし、`docs/相談役への引き継ぎ.md` §5 は
**U9** を挙げて **D-26** と `U13` を落とし、**D-26** 自身と `決定 D101` の詳細は
**自分を束 A** と書いていた（`ルール整合性チェック_v1.06.md` **D-15**）。
→ ★**合併集合を作り、正を 1 か所に寄せた。**★**索引と内容を二重に持たない。**
★**`U13` は Phase 6 が正**で、Phase 4 で決めるのは**同期対象に含めるかの 1 点だけ**である
（`docs/同期設計メモ.md` §2-1）。
★★**2026-08-27: その 1 点は決着した**★★（`決定 D103-1` ＝ 対戦記録は**同期しない**）。
→ ★**Phase 4 では何も決めない。`U13` も `U29` も Phase 6 のまま動かない。**

★★**Phase 3b で残した未決は 6 つ**★★（下の表に載っている）——
**U13 / U18**（Phase 6）/ **U19**（縦の解き方。★**測定値は更新済み**）/ **U26 / U27 / U28 / U29 / U30**。
★★**未解消の所見が 1 つある: D-24**★★（10.6 の警告がエール処理中の札にも立つ）。
★**直すと押下回数の検査が落ちる**ようにしてあるので、直した人はそれを合図にできる。

★★**M-B6 で片づいたもの（読み直さなくてよい）**★★:
マリガン 6.2.1.6（`GameSetup.mulligan` / D93-4。`MulliganNotImplemented` は枝ごと消した）/
11.10 / 11.11 の補助コマンド（D93-1 / D93-3）/ 整理 `Tidy` の口 + 整理ログの器（D93-5）/
`SetLiveJudgement`（D25 の訂正どおり欄を 2 つに）/ §14-7 の**持ち越し制約 4 つとも**。

★★**脇置き（6.2.1.6）の落とし先は盤面に置かない（決定 D93-2）。**★★
★**ここには以前「置くと 7 つ目の合成経路が通るようになる」と書いてあったが、それは誤りだった。**
`zone.dart` が「脇置きは **6.2.1.6 の手順内にのみ存在する**」と定めており、
置くと**条文が定めていない状態**を作れてしまう（D-B）。→ **恒久的に 6 経路である。**
★7 つ目は `test/board/board_move_apply_test.dart` が**写像の答えを本番の口へ流して**担保する。
経緯は `ルール整合性チェック_v1.06.md` **D-15 (i)**。

★★**マイルストーンを 1 つずつ繰り下げた（決定 D88 / 2026-08-25）。**★★

| 新 | 内容 | 旧 |
|---|---|---|
| **M-B4** | ★**モード**（新設）+ dist の可視化 | — |
| **M-B5** | 巻き戻し（`undo` / `undoStep`）+ 合成コマンド + 乱数の注記 | **旧 M-B4** |
| **M-B6** | 11.10 / 11.11 の補助コマンド + 整理 + `SetLiveJudgement` **+ ★マリガン 6.2.1.6** | **旧 M-B5** |
| ★**M-B7**（新設 / 決定 D92） | ★**進行の粒度 —— 自動進行**（「次へ」＝止まるまで進む + 「1 ステップ」ボタン + 1 押下 = 履歴 1 件） | — |

★★**M-B7 は末尾に足しただけで、M-B6 は繰り下げていない。**★★
D88 は**途中に挿し込んだ**ので付け替えが要ったが、**末尾に足すだけなら付け替えは 0 件**である。
★**M-B7 を M-B6 より後に置く根拠は `docs/盤面設計メモ.md` §15-12**
（8.4.6 / 8.4.7 の停止は `SetLiveJudgement` が来て初めて意味を持つ）。
★★**M-B6 が D92 から負っていた制約（整理ログの器を「複数件」対応で作ること）は
2026-08-26 に満たした**（決定 D93-5）。★★
`BoardState.tidy` → **`tidies`（`List`）**。★**M-B7 は器を作り直さなくてよい。**
§14-7 の持ち越し表に結果を書いてある。

★**既存のコミットメッセージに `M-B4` / `M-B5` は 1 件も無い**（未実装だから）ので**履歴は壊れない**。
★**M-B1 / M-B2 / M-B3 は完了済みなので付け替えない。**
★★**コード・テストの中の `M-B4` / `M-B5` は M-B4 で付け替えた（2026-08-25）。**★★
残るのは `M-B4` = モード / D89 の実装、`M-B5` = 巻き戻しを指す行だけである
（マリガンと整理はすべて **M-B6** へ移した）。

★★**件数はここに書かない。正は `docs/盤面設計メモ.md` §14-7 の走査表 1 行だけである。**★★
★**完了条件は件数ではなく分類である。**`M-B4` の行は**この実装を指す doc が増えるたびに増える**ので、
数だけを写すと次に数えた人が「増えた」と誤読する（`ルール整合性チェック_v1.06.md` **D-15**）。
★★**実際にここは古くなっていた**★★——§14-7 が正しく数え直したあとも、
ここだけが書いた当時の数のまま残っていた。**「§14-7 を見よ」と書き添えても、数がここにある限り古くなる。**
→ ★**数そのものを消した。**§8 から「次の D 番号」を消したのとまったく同じ対処である。

★★**M-B5 で `loveca-ui/test/state/board_session_test.dart` を置くこと。**★★
§8-3 の 2 件（undo を含むログを同じ seed で再生すると最終 `GameState` が一致する /
★**対: undo を落とすと一致しない**）は**リポジトリの外の使い捨てスクリプト**で確かめたまま、
再現手段が無い。★**出る側だけでなく対も必ず一緒に置くこと**（出る側だけだと
何もしない再生でも通る）。

★★**エネルギーデッキ置き場（4.9）に「上から見る」の口を作ってはならない。**★★
★**M-B3 で実装済み**——4.9 のメニューには**理由 2 つだけ**が出る。
★**格が違うので混ぜないこと**: (1) 上から見る = 5.7 / 10.2.2.2 は 4.8 の規定で
**条文が定めていない**（禁止条があるのではない）/ (2) シャッフル = 5.5.1 は 4.9 を
**除外していない**ので**出さないのは実装の判断**（4.9.2 により観測差が無い）。

★**M-B5 で片づいたもの**（読み直さなくてよい）:
**§8-3 の 2 件の再現手段**（`test/state/board_session_test.dart`）/ **U19 の測り直し**（★1 つも動かなかった）/
**§7-3 の「2 段になる」経路**（★7 経路とも合成にした）/ **D78 の表示条件**。
★★**D-19 も 2026-08-25 に解消した**（決定 D91 で `loveca_core` を触った機会に。D-12 も同時）。★★

★**M-B1 / M-B2 / M-B3 / M-B4 で片づいたもの**（読み直さなくてよい）:
★**M-B4**: モード（D88）/ 廃止した語（コードに **0 件**）/ dist の 3 層（D89）/
**U20**（ソロの最小幅 988）/ **D-17** / **D-18** /
★**§11-5 の測定 3 項目**（帯なしは変わらず / 帯ありの U19 = 560）。★以下は M-B1〜M-B3 のぶん ——
`DrawEnergy`（D73）/ ゲーム開始手順 6.2.1（D79 / D80）/ **D-15 の (a)(b)(c)(d) 全件** /
**D-2**（`loveca-core` にリント接続）/ **U5**（D82）/ **U16**（D83 / ★M-B3 で測り直し）/ **U17**（D84）/
**12 フェイズ 73 ステップの通し**（D86）/ **`DrawEnergy` の袖の非対称**（D87）。

★★**M-B1〜M-B4 で決めたことは `docs/決定事項一覧.md` D80〜D89 と「D88 / D89 の実装」にある。やり直さないこと。**★★
★★**ただし D77 / D81 / D84 は D88 で訂正した。下の 2 / 5 / 7 は訂正後の字面である。**★★
1. **6.2.1 は 2 段**（`GameSetup.begin` → ★**マリガンが入る** → `dealInitialEnergy`）。
   ★順は**型で守ってある**（コンストラクタが private）
2. ★**R7 の入口は R2 のデッキメニュー 2 本**（「ソロ」「ローカル対戦」/ ★**D88 で訂正**。D81 は 1 本と書いていた）。
   ★**6.2.1.4 の 2 段を保つのはローカル対戦だけ**（ソロは常に先攻で、1 人では条文の手順が成立しない）。
   ★**`DrawEnergy` ボタンは恒久**（足場ではない）
3. ★**帯を出す条件をフラグで持たない**（D85）。**上下で写像の答えが違うときだけ出す**
4. ★**画像の段は物理幅で決める**（D82）。**定数ではなく DPR の関数**
5. ★**相手の手札は常時表示**（D84）。★**ただしローカル対戦の話**（D88 で訂正）——**ソロでは相手の帯そのものが無い**。
   成立条件は未決 **U18**
6. ★**進行の分岐は 2 つで扱いが違う**（D86）。8.3.6 は選ばせず、8.4.12 は 2 択。
   ★**選択肢の文言も遷移先も UI に書かない**（`GameStore.transitions` から取る）
7. ★**「エネルギーを1枚出す」は両プレイヤーの袖**（D87）。★**ソロは袖が 1 つ**（D88 のモード条件）
8. ★★**モードは 3 つ。ソロは自分側だけを描き、相手を要求する 31 ステップを通らない**★★（D88 / `docs/盤面設計メモ.md` §14）。
   ★**モードは `GameState` ではなく `ReduceContext` に置く**。★**`players` は 3 モードとも 2 人**
9. ★**dist が解決できていないことを 3 層で見せる**（D89 / `docs/UI設計メモ.md` §11）。
   ★★**M-B4 で実装・測定済み**★★ —— 帯は `BootGate` 1 箇所（**R2 から消えたのではなく全画面に出た**）/
   プレースホルダは**設定の問題**と**データの問題**を撃ち分ける / 「詳細」から R6 へ飛べる
10. ★★**ソロの最小幅は袖 1 本ぶんだけ狭い**（U20 の解消 / `kSoloBoardMinWidth = 988`）★★。
    ★**判断基準を測る前に宣言してから測った。**測る前の予測は外れていた（(b-3) は下がった）
11. ★**開発時に `LOVECA_DIST_DIR` を毎回付けない。**R6 で一度保存すれば段 2 が効く（D89 への追記 / **D-18**）
12. ★★**乱数を消費したかは「列挙」ではなく「実測」で判定する**★★（D90-1 / `state/counting_rng.dart`）。
    ★**列挙は 3 回数え直して 3 回とも漏れた**（D-15 → D-16 → **D-19**）。
    ★`AdvanceStep` は消費したりしなかったりするので、列挙では精度も足りない
13. ★★**「いったん手札などへ戻してから」の拒否は条文由来ではなかった**★★（D90-2）。
    ★**7 経路すべて合成にした**（画面から通せるのは 6。脇置きは M-B6）。
    ★**4.1.7 の拒否は残す**（中継先は必ずオーナー自身の手札）
14. ★**着地先の条番号はボタンのラベルに出す**（D90-3）。**Tooltip はマウスを乗せないと出ない**。
    ★`maxDepth` 到達は Tooltip ではなく**帯**（状態であって出来事ではない）

★★**2026-08-26（M-B6 / 決定 D93-4）: 6.2.1.6 を実装した。**★★
`GameSetup.begin` → **`mulligan`** → `dealInitialEnergy` の 3 段になり、
`BoardNotice.MulliganNotImplemented` は**枝ごと消した**。
★**画面はタップ選択**（盤面が開く前なので `GameStore` が無く、ドラッグの器を使えない）。
★**0 枚なら乱数を 1 つも消費しない**ことを実測で固定してある（対つき）。

★**盤面のスロットの箱は 1 つの比に統一する**（**D76**）。
★★**盤面のドラッグは `ui/board/board_drag.dart` の写像 1 か所を通る（D85）。**★★
落とし先で `GameAction` を組まない。**帯を出す条件もそこから導く**（上下で答えが違うときだけ）。
★**素の `Draggable` / `DragTarget` を書かない**（`ui/common/card_drag.dart` のラッパ経由 / D46）。
★★**D72 の Phase 3b 節「4.6 / 4.10 はライブ専用」は 4.6 について誤り**★★——
8.2.2 / 8.2.4 は「手札の**カード**」でブラフが正規戦術（総評の表にも記録がある）。**そのまま信じないこと。**

★★**Release 1 を「完了」と呼ぶ前に §7 の項目が残っている**（`docs/UI設計メモ.md` §7）。
モバイルは**設計だけ通した**状態であり、実機検証は Phase 5 である。

★**`loveca_db` を次に触るときにまとめて片づく所見が 3 つある**（D-8 / D-11 / **D-13**）。
いずれも**方式まで決めてある**（決定 D63 / D65 / D-13 本文）ので、そのとき検討をやり直さないこと。

★**Release 1 の残り（次に UI を触る人が読むこと）**
1. §2-7(1) **FAQ を出すなら**: `Faq.cardNumbers` の中身は
   **cardNumber ではなく printingId**。名前を信じて結合すると**例外も出ずに 0 件**になる
   （★M5 / M6 とも触っていない。Release 1 の範囲外）
2. §7 の項目（モバイル）。★**未了である限り Release 1 を「完了」と呼べない**

★**盤面の物理操作は M-B2 でできている。** 掴む側は `BoardPiece`、落とす側は
`BoardDropSlot` / `BoardDropRegion`。**新しい置き場を足すときはこの 2 つを通す。**
★**山（4.8 / 4.9）を押したときのメニューは `board_deck_menu.dart`**（M-B3）。
`HiddenPile` は**押したとき何が出るかを持たない** —— 4.8 と 4.9 で中身が違うので、
呼び出し側（`board_layout.dart`）が渡す。**同じ見た目の山に 2 つの振る舞いを埋めない。**

★**M4 / M5 / M6 で骨格と器はできている。** R3 / R4 / R5 / R6 は同じ一覧ペインと
同じ詳細ペインを器だけ替えて置いているので、**ルートを増やして分岐させない**（§2-2）。
★P3 のメタ編集ダイアログも R2 / R3 で**同じものを器だけ替えて**開いている。
設計は `docs/UI設計メモ.md`、描画の実測は `docs/UI技術検証メモ.md`。
★**盤面（Phase 3b）の設計は `docs/盤面設計メモ.md`。**
そこから外れる場合は理由を示すこと。
★★**この 3 つに決定番号の範囲を書かない。正はその doc 自身である。**★★
★**doc が扱う決定は増え続ける**ので、範囲を書いた時点で古くなることが確定している。
★★**2026-08-27 訂正: ここには「決定 D52〜D71」「決定 D42〜D48」「決定 D73〜D79」と
範囲が書いてあり、3 つとも偽になっていた**★★（★**§8 のフェーズ表 Phase 3b の行にも
同じ「決定 D73〜D79」があり、同時に消した**）。★**範囲を書き直さず、範囲そのものを消した**
（先例は §8 の D-15 の行から「(a)〜(k)」を消したもの）。
`loveca-ui/spike/` は検証用であり、**本実装から参照しない。**
★**盤面は R7 として新しいルートを立てる。**一覧・詳細ペインの変種ではなく別の能力だから（決定 D75）。
★**`PaneScaffold` を使わない**——盤面は 1 ペインに縮退できず、「1 ペインのときどうなるか」に答えが無い。

決定事項の参照先は `docs/決定事項一覧.md`（`決定 DNN` / `決定 D-X` の実体）。
★★**次の番号はここに書かない。正は `docs/決定事項一覧.md` §3 の 1 行だけである。**★★
未記録番号を再利用しないこと。
★過去に D72 を採番したときこちらだけが更新され、あちらが「次は D72」のまま残った
（`ルール整合性チェック_v1.06.md` **D-15**）。**同じ数を 2 箇所に書けば必ず食い違う。**
★★**2026-08-25: 実際にまた食い違った。**★★ D88 / D89 を採番したときあちらは直したが、
ここは「D88 以降を使う（D36〜D87 は使用済み）」のまま残っていた。
**「正はあちらだ」と書いてあっても、数がここにある限り古くなる。**
→ ★**数そのものを消した。**「正はあちら」と書くだけでは足りない。

### ★ 未決項目（着手フェイズから辿るための索引）

★★**この表に載せる基準: 判断を先送りした項目のうち、判断すべきフェイズが特定できるものはすべてここに載せる。**★★
基準を書かずに個別に足していくと同じ漏れが再発する
（実際 `ルール整合性チェック_v1.06.md` D-1 / D-2 / D-4 / D-5 が長く漏れていた）。

★**ここは索引であって内容ではない。** 詳細は各文書に置き、この表には 1 行だけ書く。
両方に内容を書くと、片方だけ直されて食い違う。

| いつ判断するか | # | 論点 | 詳細 |
|---|---|---|---|
| ★**Phase 4 の着手前 / 着手時** | ★**束 A** | ★★**同期の前提。論点表の正は `docs/同期設計メモ.md` §1 である。**★★ ★**内訳をここに書かない**（3 か所で食い違った実例がある / 上の「次の一手」）。★**9 項目**を合併集合にしてあり、**A-0**（D-5 の残務 / ★★**2026-08-30 に片づいた**★★）/ **A-1** D-9 / **A-2** D-26 / **A-3** D-14 / **A-4** D-1 / **A-5** D-4 / **A-6** D-7 / **A-7** U9 / **A-8** U13 が載る。★★**2026-08-27: 利用者が ①② に回答した（`決定 D103`）。法務にも判断が出た（`決定 D104`）。★さらに N-7 の 5 問にも回答を得た（`決定 D105`）。★サーバーの同一性と認証にも回答を得た（`決定 D106`）。**★★ ★★**2026-08-28: 2 台で食い違ったときの振る舞いにも回答を得た（`決定 D107`）。★2026-08-29: 方式の一部を初めて採番した（`決定 D108`）。★2026-08-30: 「更新」の日付を動かす主体にも回答を得た（`決定 D109`）。★★同日、検出層が決着した（`決定 D110`）。★同日、(f-1) 衝突判定も決着した（`決定 D111`）。★同日、(f-2) の解決の粒度も決着した（`決定 D112`）。★★**★★ ★★**2026-08-31: 選ばせる口を置くかと、通知が編集を止めてよいかにも回答を得た（`決定 D113`）。**★★ ★★**同日、N-10（前回同期時点の目印と基準ハッシュを持つ器）の★形も決着した（`決定 D114`）。★これは利用者判断ではなく技術判断である**★★（★**器は `decks` とは別の表 1 つ / 目印はログの位置 / 行の不在が「まだ一度も同期していない」を表す**。★★**中身をここに書かない。★正は同期設計メモ §24 と `決定 D114` の詳細である**★★ / **D-15**）。★★**「同期が動くようになった」と読まないこと**★★ —— ★**書き込み点は 1 つも立っておらず、★`schemaVersion` も上げていない**（★理由 4 つは **D114-7**）。 ★★**同日、N-18（内容ハッシュ）と (f-3) の軸 B と N-14（役割の分離）も決着した（`決定 D115`）。★これも技術判断である**★★（★**ハッシュはデッキ 1 つにつき 1 個で SHA-256 / 軸 B は「解決結果を送り返す」/ 役割の分離は同居を許し境界を型と走査テストで支える**。★★**中身をここに書かない。★正は同期設計メモ §25 と `決定 D115` の詳細である**★★ / **D-15**）。★★**「N-14 が閉じた」と読まないこと**★★ —— ★**線 β（サーバーが「比べる」ことを許すか）は★(f-3) の軸 A に従属して残る**（**D115-9**）。★★**決着の同値は解けないままである**★★（**D115-8**。★**解くには (f-6) の決着層を開く必要がある**）。 ★★**同日、束 A の A-1 / A-2 / A-3 と N-16 / A-7 も決着した（`決定 D116`）。★これも技術判断である**★★（★**3 つ組を決めるのは呼び出し側 / `softDelete` も移行も `revision` を動かさない / `Deck.copyWith` の既定値は据え置きに変える / ログは規則が決まるまで捨てない**。★★**中身をここに書かない。★正は同期設計メモ §26 と `決定 D116` の詳細である**★★ / **D-15**）。★★**「`revision` を使わないと決めた」と読まないこと**★★ —— ★**言えるのは「決まった 4 段に消費者が 1 人も居ない」までで、★それ以上書くと `決定 D101` の訂正になる**（**D116-8**）。★★**実装は 5 つとも動いていない**★★（**D116-14**。★**A-3 だけは動きうるが、★挙動が変わるので対を測ってから入れる**）。★★**利用者判断が 2 つ開いた。★止まらずに問いを作った**★★（**D116-11** —— ★**ログを恒久的にどうするか / 論理削除を戻す口を置くか**。★**問いの文言は同期設計メモ §26-11 の 1 か所だけ**）。 ★★**同日、その 2 問に回答が返った（`決定 D117`）。★これは利用者判断である**★★（★**ログはすべての端末が受け取ったことを確かめてから捨てる / 消したデッキを戻す操作は置かない**。★★**中身をここに書かない。★正は同期設計メモ §9-9 / §27 と `決定 D117` の詳細である**★★ / **D-15**）。★★**「もう捨てている」と読まないこと**★★ —— ★**実装の材料が 4 つとも無く、★`決定 D116-6`（規則が決まるまで捨てない）は 1 文字も動かない**（**D117-5**）。★**変わったのは「いつまで捨てないか」の★終わりが決まったことだけである**。★★**「戻す口は永久に置かない」とも読まないこと**★★ —— ★**「いまの材料では置かない」であり、★開き直す条件を 3 つ書いた**（**D117-6**）。★★**2 問とも★推奨が先に出ている。★独立の裏づけには使えない**★★（**D117-2** / 同期設計メモ §7-12 —— ★**`決定 D113` と同じ格で、★`決定 D107` / `決定 D109` とは違う**）。★★**D110-4 は従属が残り、★条件が変わった**★★（**D117-7** —— ★**危険は新しい論点 **N-19** へ移った。★消えたのではない**）。★★**新しい論点を 1 つ立て、★止まらずに問いを作った**★★（**D117-8** ＝ **N-19** 使わなくなった端末をどう扱うか。★**問いの文言は同期設計メモ §27-3 の 1 か所だけ**）。 ★★**同日、束 A の配信系 5 つ（A-4 / A-5 / A-6 / N-2 ＋ D-32）も洗って選んだ（`決定 D118`）。★これは技術判断である**★★（★**JSON の単位は商品単位のまま / 版ゲートは「より小さい」に変える / `min_app_version` の既定値は置かず必須引数 / `minAppVersion` は使い続ける**。★★**中身をここに書かない。★正は同期設計メモ §28 と `決定 D118` の詳細である**★★ / **D-15**）。★★**「配信系が決まった」と読まないこと**★★ —— ★**決まったのは 2 つ半で、★3 つ半は利用者判断待ちである**（★**画像の単位 / A-5 / A-6 の (III) は★問い 1 待ち、★A-4 は★問い 2 待ち**。★**2 問とも同期設計メモ §23-14 に文言が在り、★作り直していない**）。★★**実装は 1 行も動いていない**★★（**D118-17**。★**版ゲートも既定値も方向を決めただけで、★挙動が変わるので対を測ってから入れる**）。★★**塊 B（①(f) の残りと器 —— (f-4) の (iii) / (f-3) の軸 A / N-13 / N-15 / N-17 / N-19）は★別セッションに送った**★★（**D118-12**。★**塊のあいだに依存が無いことを 3 本当てて確かめた** —— ★**§23-6 の条件つきの辺は `決定 D115-4` が★決定で閉じた**）。 ★★**同日、塊 B（①(f) の残りと器）も洗って選んだ（`決定 D119`）。★これも技術判断である**★★（★**(f-4) の (iii) は「解決してから、起きたことを残す」/ N-15 は「送信側の `updatedAt` をそのまま採る」/ N-17 は「器の行を消して『まだ一度も同期していない』に戻す」/ N-13 の論点 1 は器-5 を採らない**。★★**中身をここに書かない。★正は同期設計メモ §29 と `決定 D119` の詳細である**★★ / **D-15**）。★★**「①(f) が決まった」と読まないこと**★★ —— ★**(f-3) の軸 A と N-13 の論点 1（残り）と N-19 は開いたままである**（★**いまの状態の索引は同期設計メモ §22-6 の 1 か所** —— ★**この回で (f-4) の (iii) の 1 行だけが動いた**）。★★**「(iii) が決まれば軸 A と N-13 が連鎖して決まる」は成り立たなかった**★★（**D119-7**。★**狭めるが、★どちらに倒れても 1 つには決まらない**）。★★**軸 A と N-13 の論点 1 を決めなかったのは、★§21 の表が「同上」で埋まっていて横に読めないからである**★★（**D119-8** —— ★**D-35 が 2 件。★`決定 D118` と合わせて 3 例目**。★**表は 1 文字も書き換えていない**）。★★**実装は 4 つとも動いていない**★★（**D119-14**。★**受信経路も器もまだ 0 行である**）。★★**N-17 と N-19 は 1 点で重なる**★★（**D119-9** —— ★**相手が捨てた場合には効かない**）。 ★★**2026-08-31（同日）: 利用者が 3 問に回答した（`決定 D120`）。★これは利用者判断である**★★（★**配信物は開発する人が作り、デッキを預けるのと同じサーバーに置く / 未知の値はその値のところだけを飛ばして残りを取り込み、飛ばしたことを見せる（**A-4** の 軸 1）/ 使わなくなった端末は一定の期間つながらないと自動で外す（**N-19** の (1) と (2) の半分）**。★★**中身をここに書かない。★正は同期設計メモ §9-10 / §30 と `決定 D120` の詳細である**★★ / **D-15**）。★★**「配信系と N-19 が決まった」と読まないこと**★★ —— ★**問い 1 待ちだった 4 つは★前提が立っただけで 1 つも決まらず**（★**N-2 の画像側 / A-5 / A-6 の (III) / D-32 の 版-5**。★**A-5 は (c) の逃げ道だけが閉じた**）、★**A-4 は★軸 1 だけが閉じて 厳-2 / 厳-3 が残り**、★★**N-19 は 4 論点のうち 1 つ半しか閉じていない**★★。★★**この回の格は★1 つの問いの中で 2 つに分かれる**★★（**D120-7** / §7-12 の 3 例目 —— ★**「開発する人が作る」は★利用者が枠の外から絞り、★「同じサーバーに置く」は★推奨への同意である**。★**問い 2 / 問い 3 には推奨を 1 つも出していない**。→ ★**混ぜて引かない**）。★★**D118-8 の「厳-2 は弱まる」の重みが戻った。★D118 の訂正ではない**★★（**D120-9**）。★★**D106-2 に 4 つ目の役割（カードマスタの配信 ＝ 配る。判断しない）が加わる。★訂正ではない**★★（**D120-13**）。★★**D110-4 の危険は N-19 に留まり、★(3) に集約された**★★（**D120-10**）。★★**利用者判断が 1 つ開いた。★止まらずに問いを作った**★★（**D120-12** —— ★**「一定の期間」を誰がどう決めるか**。★**問いの文言は同期設計メモ §30-7 の 1 か所だけ**。★**新しい論点は立てていない**）。★★**実装は 3 つとも動いていない**★★（**D120-16**）。 ★★**同日、残っていた未選択 6 つも洗い直して選んだ（`決定 D121`）。★これは技術判断である**★★（★**画像の差分の単位は「画像だけ別のマニフェスト」/ `imageHash` は原本のハッシュのまま（3 案とも採らない）/ アプリの版は実態に戻す / 衝突を解くのは送信側 / 未送信・通信の失敗・衝突は寿命で系統を決める / 外れた端末が戻ってきたら「まだ一度も同期していない」に戻す**。★★**中身をここに書かない。★正は同期設計メモ §31 と `決定 D121` の詳細である**★★ / **D-15**）。★★**(4)(5) は §21 の表が「同上」で横に読めなかったので、★字面を 1 文字も書き換えず、★語を揃えた表を別に作ってから選んだ**★★（★**§18 → §19 で採った処置と同じ**）。★★**「D-4 が解消した」と読まないこと**★★ —— ★**閉じたのは★受け取り側だけで、★★生成側の飛ばしは開いたままである★★**（**D121-2**）。★★**「線 β が閉じた」とも読まないこと**★★ —— ★**開いたままで、★★従属先が★軸 A から「サーバーが上書きされた版を残すか」へ変わっただけである★★**（**D121-5**。★**D115 の訂正ではない** / **D111-1** と同じ形）。★★**「N-19 が閉じた」とも読まないこと**★★ —— ★**閉じたのは (3) の★遷移先だけで、★引き金と (4) は **N-11** 待ちである**（**D121-7**）。★★**柵が 2 つ増えた**★★（★**画像のマニフェストの行のハッシュは配る WebP のバイト列から作ること / アプリの版の定数だけを単独で下げてはならない**。★**どちらも破る経路は今日 0 本** / **D111-4** と同じ形）。★★**利用者判断が 1 つ開いた。★止まらずに問いを作った**★★（**D121-8** —— ★**実態の版は何番か**。★**問いの文言は同期設計メモ §31-6 の 1 か所だけ**。★**新しい論点は立てていない**）。★★**実装は 6 つとも動いていない**★★（**D121-14**）。 ★★**同日、その問いに回答が返り、あわせて Phase 4 の実装の順序を出した（`決定 D122`）。★版の値は利用者判断、★順序は技術判断である**★★（★**実態の版は `0.3.0` / ★順序は 1 コミット = 1 論点でレーン 6 本 ＋ 門 8 つ**。★★**中身と数をここに書かない。★正は同期設計メモ §9-11 / §32 と `決定 D122` の詳細である**★★ / **D-15**）。★★**「A-6 が閉じた」「D-7 が解消した」と読まないこと**★★ —— ★**(II-b) が開いており、★★やること 1〜4 は 1 つも実施されていない★★**（**D122-3**）。★★**版-B は今日実施できない**★★ —— ★**3 つ同時 ＋ ★★柵が見ていない相手がもう 1 つ在る★★**（★同じデータ版のまま作り直すなら版ゲートが先に要る / **D122-4**）。★★**N-11 は★サーバーに触れるコミット全部と N-19 の残りを★同じ門で塞いでいる。★認証を「あとで足す」ことができない**★★（**D105-3** / **D122-6**）。★★**止まる条件が 1 つ当たった**★★（**D122-7** —— ★**サーバーの実装の置き場がどの論点にも登録されていない**。★**決めず・採番せず・格も判定していない**）。 ★★**2026-08-31（同日）: ★実装に入った。★レーン 1 の commit 1 で **A-3**（D-14）が片づいた**★★（★**`Deck.copyWith` の 2 つの既定値を据え置きに** / **D116-2**）。★★**`決定 D116` の「実装は 5 つとも動いていない」は★A-3 についてのみ古くなった。★字面は書き換えない**★★（**D-35**）—— ★**残る 4 つ（D116-1 / D116-3 / D116-4 / D116-6）は★決めた中身が「動かさない」なので★コミットが 0 行である**（★正は同期設計メモ §32-10）。★★**順序の正は同期設計メモ §32-5 / §32-6 の表である。★どこまで進んだかをここに書かない**★★（**D-15**）。 ★★**2026-08-31（同日）: ★門 ア（**N-11** 利用者の同定）を決め、★8 つの門に 1 つずつ当てた（`決定 D123`）。★これは技術判断である**★★（★**利用者と端末を別の量として持つ / Phase 6 の対戦相手の同定とは粒度を分ける / 端末の同定は端末が自分で持つ / 利用者の同定はローカルにも持つ**。★★**中身をここに書かない。★正は同期設計メモ §34 と §10 の **N-11** と `決定 D123` の詳細である**★★ / **D-15**）。★★**「N-11 が閉じた」と読まないこと**★★ —— ★**`decks` の行ごとに持つかは★利用者判断である**（**D123-8**）。★★**「認証が実装できる」とも読まないこと**★★ —— ★**決まったのは★何を同定するかであって、★仕組みではない**（★コミット 16 / 18 は★門 エ の下流でもある）。★★**「D106-4 / D105-3 を訂正した」とも読まないこと**★★ —— ★**認証が 1 つであることと盤面の中の同定が座席であることは両立し**（**D123-2**）、★**同定とアクセス制御は別である**（**D123-4**）。★★**新しい論点を 1 つ登録した（**N-20** サーバーの実装の置き場）**★★（**D123-5** —— ★**§32-12 が「どの論点にも登録されていない」と判定した分。★★登録しただけで中身を 1 つも決めていない★★**）。★★**8 つの門の状態は★読みを 2 つ宣言して別々に数えた。★数と内訳をここに書かない。★正は同期設計メモ §34-9 の表である**★★（**D-15**）。★★**利用者判断を 2 つ開いた。★止まらずに問いを作った**★★（**D123-8** —— ★**1 台に複数のアカウントが同居しうるか / 用意するサーバーで何が動かせるか**。★**問いの文言は同期設計メモ §34-11 の 1 か所だけ**）。★★**実装は 1 行も動いていない**★★（★**コードもテストも 1 行も変えていない**）。 ★★**2026-08-31 回答 / 2026-09-01 記録: ★利用者が 4 つに回答し、★あわせて 門 イ を決めた（`決定 D124`）。★回答は利用者判断、★門 イ は技術判断である**★★（★**1 台につきアカウントは 1 つ / 用意するサーバーでは任意のプログラムが動かせる（★サーバーは利用者の友人が用意する）/ 使わなくなった端末を外す期間は 10 日 / 門 イ は「相手の内容を受け取って比べる」**。★★**中身をここに書かない。★正は同期設計メモ §9-12 / §35 と `決定 D124` の詳細である**★★ / **D-15**）。★★**回答の日と記録の日が分かれた初めての回である。★どちらも消さない**★★（**D124-9** —— ★**日付は「前提が動いた順序」を追う唯一の手がかりである** / 同期設計メモ §9-3 の 8）。★★**「D103-3 と食い違った」と読まないこと**★★ —— ★**「サーバーは利用者が用意する」は 2 通りに読めるが、★★どちらの読みでも D103-3 の働きは 1 ミリも動かない★★**（**D124-4**）。★**字面は 1 文字も書き換えていない**。★★**ただし D104 に札を 1 つ付けた ＝ 【要確認/法務】**★★ —— ★**確認の範囲は「利用者以外がサーバー経由で★利用する形」を述べており、★★「運用する形」を 1 文字も述べていない★★**。★**引き直すかは利用者が判断する。★判断の時期はレーン 4 の手前である**。★★**「D105-3 が守れなくなった」とも読まないこと**★★ —— ★**アクセス制御が守る相手は★他の★利用者であって、★サーバーを運用する人ではない**（★層の断りを足しただけ）。★★**問い 3 の格は §7-12 の 5 例目で、★過去 4 例のどれとも違う**★★（**D124-5** —— ★**推奨は出ているが★★同意ではなく、★利用者が値を動かした★★**。★**相談役は「根拠が無い」と明示していた** —— ★★**理由を添えない推奨はこの回が初めてである**★★。→ ★**独立の裏づけは在る**）。★★**「Dart で書くと決めた」と読まないこと**★★ —— ★**境界 1 は守れると確かめたまでで、★★倒していない★★**（**D124-6** —— ★**Phase 4 のデッキ同期のサーバーは `loveca_core` を 1 つも呼ばない** / **D115-6**）。★★**「N-11 が閉じた」とも読まないこと**★★ —— ★**(2-c) は★半分である**（**D124-8** —— ★**同居しないことは閉じたが、★★入り直したとき前のデッキが残るかは返っていない★★**。★**3 候補はこれでは割れない**）。★★**「N-19 が閉じた」とも読まないこと**★★ —— ★**期間の値と測り方は決まったが、★★誰が決めるかは返っておらず、★(3)(i) / (4) は開いたままである★★**。★★**門 イ が決まったことで動いたものが 2 つある**★★（**D124-7** —— ★**門 ウ に入る従属が 1 本消えた** / ★**門 ク の材料 3 が★門 イ 待ちではなくなった**。★★**門の数は 8 のままである**★★）。★★**利用者判断は 1 つ減り、★同時に 1 つ開いた。★止まらず問いの文言まで作った**★★（★**別のアカウントで入り直したとき前のアカウントのデッキをどうするか**。★**問いの文言は同期設計メモ §35-14 の 1 か所だけ**）。★★**実装は 1 行も動いていない**★★（★**コードもテストも 1 行も変えていない**）。 ★★**2026-09-01: ★利用者が (2-c) の残り半分に回答し、★あわせて (2-c) を決めた（`決定 D125`）。★回答は利用者判断、★(2-c) は技術判断である**★★（★**別のアカウントで入り直したとき前のアカウントのデッキは端末に残さない / (2-c) は「DB 全体で 1 つのスカラとして持つ」**。★★**中身をここに書かない。★正は同期設計メモ §9-13 / §36 と `決定 D125` の詳細である**★★ / **D-15**）。★★**回答を「消す」と縮めないこと**★★ —— ★**決まったのは「★手元に残らないこと」までで、★★手段（行の物理削除 / DB ごと作り直す）は決めていない★★**（**D125-3**）。★★**「論理削除する」とも読まないこと**★★ —— ★**論理削除は検出層に 1 件残って同期で伝播するので、★★サーバー側の前のアカウントのデッキまで消える★★**（**D110-1** / **D110-3**）。★**相談役の補足「サーバーには残る」と正面から当たる**。★★**「D102 と食い違った」とも読まないこと**★★ —— ★**字面は 2 通りに読めるが、★★どちらの読みでも D102 の働き（削除が同期で伝わること）は 1 ミリも動かない★★**（**D125-4**。★**守る対象も向きも違う**）。★**字面は 1 文字も書き換えていない。★台帳の D102 の詳細に追記だけを置いた**。★★**ただし行の物理削除を選ぶ段では★広い読みを先に倒す必要がある**★★（★この回の範囲ではない）。★★**N-11 が閉じた**★★（**D125-6** —— ★**5 論点とも答えが在る**。★決め手は **D123-4** が既に要求している量で足りることで、★★帰-2 は新しい量を 1 つも要らない★★）。★**「帰-1 / 帰-3 が劣る」とは書かない**（★帰-3 は★回答と食い違い、★帰-1 は★比べて採らなかった）。★★**「認証が実装できる」とも読まないこと**★★ —— ★**決まったのは同定の量と置き場であって、★仕組みではない**（**D123** の断りが 1 文字も動かない）。★★**柵が 1 つ増えた**★★（**D125-5** —— ★**入り直しの拭き取りは★1 つのトランザクションで行うこと**。★途中で落ちると★残った行が★新しいアカウントのものになる。★**破る経路は今日 0 本** / **D111-4** と同じ形）。★★**未送信の編集は 1 つも決まっていない**★★（**D125-8** —— ★**未送信のログと未送信の論理削除は同型で、★★D117-1 の字面に 1 点で当たる★★**。★★**訂正ではない**★★ —— ★D117-1 は「アカウントが変わったときどうするか」を 1 文字も述べていない。→ ★**N-17 に入口を 1 つ足した**）。★★**N-10 の器はアカウントごとでも端末ごとでもない ＝ デッキごとである**★★（**D125-9** —— ★**主キーが `deckId` 単独**。★**列は 1 つも増えず、★D119-5 の遷移先がそのまま使える**）。★★**この回の格は §7-12 の 6 例目で、★推奨に同意した形である**★★（**D125-7** —— ★**入口は分からない**。★**理由 3 つのうち 2 つは★字面より広い**。★★**補足「元のアカウントで入り直せば戻る」は★初回同期の設計に従属し、★成り立つとも成り立たないとも書けない**★★）。★★**利用者判断は 1 つ閉じ、★同時に 1 つ開いた。★止まらず問いの文言まで作った**★★（★**切り替えのとき、まだ他の端末に届いていない編集をどうするか**。★**問いの文言は同期設計メモ §36-10 の 1 か所だけ**）。★★**実装は 1 行も動いていない**★★（★**コードもテストも 1 行も変えていない**）。 ★★**2026-09-01: ★利用者が「サーバーは Dart で書く」と回答し、★あわせて **N-20**（サーバーの実装の置き場）を決めた（`決定 D126`）。★β は利用者判断、★α と境界は技術判断である**★★（★**ソースはこのリポジトリの新しいパッケージ `loveca-server/` に置く / ★依存の向きは 6 本とも書いて今日は 0 本 / ★`loveca_core` を pubspec にも書かない / ★境界の宣言は 4 段**。★★**中身をここに書かない。★正は同期設計メモ §9-14 / §38 と `決定 D126` の詳細である**★★ / **D-15**）。★★**「サーバーが実装できる」と読まないこと**★★ —— ★**決まったのは★置き場と言語であって、★★何を送り合うか★★ではない**。★★**認証の仕組みは★どの論点にも登録されていない**★★（★**§32-12 の 1 ＝ 門 エ と★形が同じ**。★**6 コミットを塞いでいる**。★**採番していない** / `CLAUDE.md` §7 の止まる条件 **5**）。★★**「門 エ が開いたから下流が着手できる」とも読まないこと**★★ —— ★**§32-6 の 28 コミットに 1 つずつ当てた結果、★★今日着手できるのは 17（線 α の許可リストと走査テスト）だけである★★**（**D126-8**。★**数と内訳をここに書かない。★正は同 §38-11**）。★★**この回の格は §7-12 の 7 例目で、★推奨と名乗らない入力は初めてである**★★（**D126-6** —— ★**入ったのは「帰結 2 つ」**。★**独立の裏づけは★在る**が、★★**§7-8 の型に当たり、★3 つ目の形が落ちていた**★★ / **D126-7**）。★★**`決定 D122` の「止まる条件が 1 つ当たった」はこれで解けた**★★ —— ★**`決定 D123` が採番し、★この回が中身を決めた。★字面は 1 文字も書き換えない**（**D-35**）。 ★★**2026-09-01（同日）: ★認証の仕組みを洗い、★論点として登録した（`決定 D127` ＝ **N-21** / ★門 ケ）。★これは技術判断である。★★ただし中心の 1 つは利用者判断だった★★**★★（★**軸 6 つを宣言して候補 5 つを全欄埋めた結果、★★割れるのは「用意する人が秘密を持つか / 第三者に依存するか / 2 台目を足す手間」の 3 つだけで、★どれも使い方の想定である★★**。★**止まらず問いを作った** —— ★**問いの文言は同期設計メモ §40-12 の 1 か所だけ**）。★★**決めたのは★従属しない 4 条件だけである**★★（★**利用者を区別できない形は落ちる / ★名乗る操作は編集の前提にしない / ★認証は `loveca_core` を 1 つも呼ばない / ★版のずれは **N-12** に入口を足した形で持つ**。★★**中身をここに書かない。★正は同期設計メモ §40 と `決定 D127` の詳細である**★★ / **D-15**）。★★**「認証が実装できる」と読まないこと**★★ —— ★★**§32-6 の 7 コミット（16 / 18 / 20 / 21 / 23 / 24 / 25）とも★着手できない**★★（**D127-8** —— ★**口の形そのものが 4 候補で違う**）。★**「空の器」を先に置かないと判断した**（★開き直す条件を 1 つ書いた）。★★**「Phase 6 と同じ仕組みか」は★既に決まっている**★★（**D127-7** —— ★**`決定 D106-4` を★**N-11** と **D123-2** が★★2 か所で「1 つの仕組みで確かめる」と読んでいる★★**。★★**新しい決定ではない。★確認である**★★。★**分けることは `決定 D106-4` の訂正になるので★避けた**）。★★**門は 8 → 9 になった**★★ —— ★**§32-5 / §32-6 の表も、★過去の追記の「門の数は 8 のままである」も 1 文字も書き換えない**（**D-35**）。★★**指示の前提が 2 件外れた**★★ —— ★**(a)「D106-4 は仕組みが同じとは言っていない」は実物と食い違った** ／ ★★**(b) `決定 D126` の回に書いた「仕組みを述べた行が `docs` にも `lib` にも 1 つも無い」は★偽だった**★★（★**D-15 (j)**。★★原典 `docs/引き継ぎドキュメント.md` §1.5 に 3 行在る★★ —— ★**2 つは「決定として使えない番号」、★1 つは使える番号だが★台帳が写したのは別の側**）。→ ★**該当箇所を書き直し、★引き金を残した**。★★**U31 に実物がもう 1 つ挙がった**★★（★**採番しない**）。★★**実装は 1 行も動いていない**★★（★**7 コミットとも着手できない**）。 ★★**2026-09-01: ★利用者が「何で名乗るか」に回答し、★あわせて候補 3 が要求するものを洗った（`決定 D128`）。★回答は利用者判断、★整理と門の判定は技術判断である**★★（★**サーバーに名乗る方法は「端末が鍵を作って名乗る」/ ★鍵の生成は `loveca_core` の外 / ★2 台目に写す文字列は人が写せること / ★通信の運び方を **N-22** ＝ 門 コ として登録した**。★★**中身をここに書かない。★正は同期設計メモ §9-15 / §42 と `決定 D128` の詳細である**★★ / **D-15**）。★★**代償を縮めないこと**★★ —— ★**2 台目を足すときは 1 台目で出した文字列を写す手間が要る**（★**候補 1 / 2 / 4 にはこの手間が無い**）。★★**「小さい」とも「大きい」とも書かない**★★（★長さは口の形に従属する）。★★**「N-21 が閉じた」と読まないこと**★★ —— ★**論点 (1)(2) は閉じ、★(3) は 1 ミリも動かず、★★(4) 口の形が新設された★★**（**D128-6** —— ★**対称 / 非対称の 2 形と 2 台目の結び付け方 3 形を全欄埋めた結果、★★決まっている決定は 1 つに絞らない★★**）。★★**「門 ケ が閉じた」とも読まないこと**★★ —— ★**読みを 2 つ宣言して別々に数えた**（**D123-7** と同じ処置）。★★**門は 9 → 10 になった**★★（★**§32-5 / §32-6 の表も過去の追記も 1 文字も書き換えない** / **D-35**）。★★**理由 3 つを根拠として引き直さないこと**★★（**D125-7** —— ★**とくに「用意する人が秘密を持たない」から「口は非対称である」を導かない**）。★★**この回の格は §7-12 の 8 例目で、★★2 段になっている★★**★★（**D128-10** —— ★**1 段目「任せます」は★判断そのものを委ねた形で、★過去 7 例に 1 つも無い**。★**2 段目は★★相談役が自分の推奨に反対材料を出したうえでの選択★★**）。★★**2 段目は独立の裏づけになる。★ただし★★候補 1 に対してだけである**★★（**D128-11** —— ★**候補 2 / 4 に対する回答は★理由に従属したままである**）。★★**1 アカウントが複数端末を持つことは★前提として既に決まっている**★★（**D128-12** —— ★**根拠は **D123-1** / **D120-3** / **D121-7** で、★★**D105-4** は別のことを述べている★★**。★**指し先だけを直した**）。★★**鍵の置き場は決まらない**★★（**D128-4** —— ★★**D125-3**（「残さない」の手段）に従属する★★。★**§32-6 の 19 の待ちは 1 ミリも減らない**）。★★**§32-6 の 7 コミットとも着手できない**★★（**D128-9** —— ★**16 / 18 が★**N-21** の (4) と **N-22** の 2 つを待つ**）。★★**「門が開けば下流が着手できる」は★2 回連続で半分だった**★★（★**D126** の回と★この回。★**§38-11 も §40-11 も 1 文字も書き換えない** —— ★**偽ではなく★足りないだけである**）。★★**利用者判断が 1 つ閉じ、★1 つ開いた。★止まらず問いを作った**★★（★**全部の端末の鍵を失ったときどうするか**。★**問いの文言は同期設計メモ §42-12 の 1 か所だけ**）。 ★★**§32-6 の番号を持つ実装は 0 である。★§1 の境界の見張りを機械に移した**★★（★**引き金は **D128-3**。★★件数と対の内訳は §3 の記録が正である★★**）。 ★★**2026-09-01（同日）: ★利用者が「何で名乗るか」の答えを★★覆した★★（`決定 D129`）。★★`CLAUDE.md` §7 の止まる条件 4 に当たる。★利用者が確認したうえで指示している★★**★★（★**名乗る方法は★★利用者名とパスワード★★ / ★パスワードは PBKDF2-HMAC-SHA256 で保存する / ★アドレスの確認はしない / ★識別子を **N-23** ＝ 門 サ として登録した**。★★**中身をここに書かない。★正は同期設計メモ §9-16 / §44 と `決定 D129` の詳細である**★★ / **D-15**）。★★**経緯は 6 段。★縮めない**★★ —— ★**利用者が「メールアドレスを鍵として用意するのはどうか」と★★枠の外から案を出し★★、★(a) アドレスだけで名乗る形は★★鍵にならないと分かり★★**（★アドレスは秘密ではない / **D105-3** と正面から当たる）、★**利用者が「入力は楽な方がいい」と★条件だけを述べ、★相談役が候補 1 を推し、★利用者が受けた**。★★**訂正の形は先例を 3 つ探して決めた**★★（**D129-10** —— ★**D25** は同じ番号のまま字面を書き換えた（★★最初から誤りだった★★）／ ★**D88** は新しい番号が訂正し★★原文を「以下は原文 →」で残した★★ ／ ★**D99** は訂正と呼ばなかった）。→ ★★**D88 の形を採った。★理由 4 つ**★★（★**D128 は書いた時点で誤っていない** / ★**12 項目のうち直接の影響は 4 つだけ** / ★**格も経緯も日付も別である** / ★**D-36 の規約 (2) が同じ向きを指す**）。★★**D-35 / D-36 と「決定の訂正」は別である。★確かめた**★★ —— ★**D-36 は★★コミット本文★★についてであり、★★台帳は編集できる★★**。★**D-35 は「いまも正しい」ものについてであり、★★§9-15 / §40-8 / §42-12 には当たるが★D128-1 には当たらない★★**。→ ★★**記録は書き換えない。★決定は覆せる**★★。★★**「候補 3 が落ちた」と読まないこと**★★ —— ★**§40-8 の軸表の (丙)(丁)(戊) は 1 ミリも動いていない。★★動いたのは利用者が何を重く見るかである★★**。★★**「アドレスとパスワードで確定した」とも読まないこと**★★ —— ★**§40-8 の 名-1 の字面は「利用者名」であり、★★アドレスとは 1 文字も書かれていない★★**（★指示の前提が 1 件外れた）。→ ★**N-23** ＝ 門 サ。★★**門は 10 → 11 になった**★★（★表も過去の追記も 1 文字も書き換えない / **D-35**）。★★**D128 の 12 項目に 1 つずつ当てた**★★ —— ★**覆るのは 3 つ、★★消えるのは 1 つ★★、★生き残るのは 8 つ**（★**D128-3** は生き残り、★対象が★鍵から★★塩★★に入れ替わった）。★★**N-22 に要求が 1 つ増えた ＝ 経路が秘匿でなければならない**★★（**D129-6** —— ★**候補 3 の非対称の形には無かった。★★訂正の代償であり、★縮めない★★**）。★★**§42-12 の問い（鍵を失ったら）は★答えなくてよくなった**★★（**D129-7** —— ★**事象そのものが消えた**。★**問いは 1 文字も書き換えず残す**）。★★**§32-6 の 7 コミットとも着手できない**★★（**D129-11** —— ★**16 は★**N-21** の (4) ＋ **N-22** ＋ **N-23** の 3 つを待つ**）。★★**ただし 16 の★前段（保存の形）は★3 つとも待たない**★★ → ★**実装した**（★**件数と対の内訳は §3 の記録が正である**）。★★**利用者判断が 1 つ閉じ、★2 つ開いた。★止まらず問いを 2 つ作った**★★（★**識別子** / ★**パスワードを忘れたとき**。★**文言は同期設計メモ §44-13 の 1 か所だけ**）。★★**この回の格は §7-12 の 9 例目で、★★§7-8 の検知手段が初めて働いた回である★★**★★（**D129-12** —— ★**枠の外の案が出て、★★倒れた★★**）。★★**独立の裏づけは「入力は楽な方がいい」までである**★★ —— ★**その条件は 4 候補のうち★★3 つを残す★★**ので、★**候補 1 を選んだのは推奨である**。★**既存の決定との突き合わせは 17 行に当てて、★★訂正は D128 の 3 項目だけである★★**。 ★★**2026-09-01（同日）: ★利用者が 2 問に回答し、★あわせて **N-22**（運び方）を洗って決めた（`決定 D130`）。★回答は利用者判断、★運び方と門の判定は技術判断である**★★（★**識別子は★★利用者名★★ / ★復旧の口は作らない / ★通信は★★HTTP ＋ TLS★★ / ★サーバーが待つ / ★配信と同じ口をパスで分ける / ★**N-24** ＝ 門 シ を登録した**。★★**中身をここに書かない。★正は同期設計メモ §9-17 / §46 と `決定 D130` の詳細である**★★ / **D-15**）。★★**代償を縮めないこと**★★ —— ★**全端末とパスワードを同時に失うと、★★そのアカウントに二度と戻れない★★。★サーバーに預けたデッキは★消えずに残るが、★★取り出す手段が無い★★**（★「消える」とは書かない）。★**手元のデッキと共有形式の書き出しは残る**（★**実物に当てた**）。★★**「小さい」とも「起きにくい」とも書かない**★★（★確率を測っていない）。★★**アドレスの案に「落ちた」と読まないこと**★★ —— ★**§44-8 の 6 欄は 1 ミリも動いていない。★★動いたのは (丁)（個人情報が残るか）の重みである★★**。★★**識別子と復旧の口は★輪になっている**★★（**D130-11** の柵 —— ★**理由 2 つが★どちらも **D129-4** に従属し、★**D129-4** の開き直す条件が **N-23** の答えに従属する**。→ ★**開き直すときは 2 つ同時に開くこと**）。★★**16 の待ちは 3 → 0 になった。★それでも着手できない**★★（**D130-14** —— ★**塞ぐものが★★2 つ入れ替わった★★**: ★**門 シ**（待ち受けられるか）＋ ★**アカウントの保管**（★§35-9 が未決））。★★**「門が開けば下流が着手できる」は★3 回連続で半分だった**★★（★**D126** / **D129** / この回）。★★**ただし 16 の★判定★は★2 つとも待たない。★実装した**★★（★**§32-6 の 13 と同じ形**。★**件数と対の内訳は §3 の記録が正である**）。★★**門は 2 つ閉じ、★1 つ増えた**★★（★表も過去の追記も 1 文字も書き換えない / **D-35**）。★★**読み A と 読み B が★初めて一致した**★★（★過去 6 回はすべて割れていた）。★★**利用者判断が 2 つ閉じ、★1 つ開いた。★止まらず問いを作った**★★（★**経路の秘匿をどう用意するか**。★**文言は同期設計メモ §46-13 の 1 か所だけ**）。★★**この回の格は §7-12 の 10 例目で、★**D117** と同型である**★★（**D130-15**）—— ★★**1 点だけ新しい ＝「1 つの返事に畳まれている」ことが★分かっている**★★（★**D117 は分からない**。★**「畳まれていなかった」とは書かない**）。★**既存の決定との突き合わせは 17 行に当てて訂正 0 件**。 ★★**2026-09-01（同日）: ★16 を塞いでいた 2 つを洗って決めた（`決定 D131`）。★技術判断である。★利用者に 1 つも聞いていない**★★（★**アカウントの保管は★★JSON のファイル 1 つ★★ / ★**N-24** の (3) と従属しない部分を決めた / ★★§32-6 の 16 を実施した★★**。★★**中身をここに書かない。★正は同期設計メモ §48 と `決定 D131` の詳細である**★★ / **D-15**）。★★**§35-9 の判定が★期限切れだったことを先に確かめた**★★（**D-15 (l)** —— ★**「DB の有無は **N-20** の β の下流」と書かれていたが、★★β は **D126-1** で決まった★★**。★**§35-9 の字面は 1 文字も書き換えず追記を置いた**）。★★**「DB が要ると分かった」には当たらない**★★ —— ★**5 候補とも★友人に聞く必要は出なかった**（★★SQLite も★埋め込みであって★サーバーではない★★。★**「DB が要る」と「DB サーバーが要る」を混ぜない**）。★★**`loveca_db` を採らなかった理由がもう 1 つ在る**★★ —— ★**`schemaVersion` は★★クライアントの DB の版である★★ので、★共有すると★★理由の無い移行が走る★★**。★★**「保-1 が劣る」とは書かない**★★（★利用者が増えたときは強い）。★**D126-4 の 3 段目は「決めていない」のまま残した**。★★**N-24 は閉じない**★★ —— ★**(1) 証明書をどう得るかは★★利用者判断のまま★★**（★問いは作り直していない）。★★**16 については従属が解けた**★★（**D131-3** —— ★**証明書は★呼び出し側から渡されるので、★(1) の答えが何であれ★★コードは 1 文字も変わらない★★**）。★★**柵が 5 つ増えた**★★（★素の HTTP へ落ちない ／ ★一時ファイルへ書いて置き換える ／ ★重複は断る ／ ★置き場は呼び出し側が渡す ／ ★試験用の証明書は `lib` から参照されない）。★★**開けた門が新しい門を生むかを★進む★前★に確かめた**★★ —— ★**6 件当てて★★新しい門は 0 だった★★**（★「門が開けば下流が着手できる」が **D126** / **D129** / **D130** と★3 回連続で半分だったため）。★★**§32-6 の 16 を実施した ＝ 認証のレーンで★番号を持つ最初のコミットである**★★（**D131-8**。★**件数と対の内訳は §3 の記録が正である**）。★★**「同期が使えるようになった」と読まないこと**★★ —— ★**アカウントを★作る口が★§32-6 のどのコミットにも無い**（★事実として置いた。★採番しない）。★**18 は **N-21** の (4)(ii) ＋ 門 シ の (2) を、★20 は 18 ＋ **D104** の札 ＋ ★★デッキの保管★★を待つ**（★**アカウントの保管を決めても★デッキの保管は決まらない** —— ★量が違う）。★★**利用者判断は 1 つも閉じず、★1 つも開かなかった**★★。★**既存の決定との突き合わせは 14 行に当てて訂正 0 件**。 ★★**2026-09-01（同日）: ★§32-6 の漏れを補い、★残りに 1 つずつ当てた（`決定 D132`）。★技術判断である**★★（★**アカウントを作る口を★**17-2** として立てた / ★その口が★新しい門を生んだ（**N-25** ＝ 門 ス）/ ★★18 の★口★を実施した★★**。★★**中身をここに書かない。★正は同期設計メモ §50 と `決定 D132` の詳細である**★★ / **D-15**）。★★**位置は★依存から導いた**★★（★**16 より後**（待ち受けと保管の上に載る）＋ ★**20 より前**（デッキを預けるには利用者が居る）→ ★★制約を満たす最も早い位置（17 の直後・18 の手前）★★）。★★**番号を振り直さない**★★ —— ★**§32-6 の番号は 8 つの記録から参照されており、★★振り直すと過去の記録が全部ずれる★★**（**D-35** の精神）。★**表は 1 文字も書き換えず追記で足した**。★★**「16 と同じ形」は★17-2 には採れなかった**★★ —— ★**証明書は★★運用の入力★★（どの形でもコードが同じ）だが、★★方針は分岐そのものである★★**（★開いた登録と招待では口の形が違う）。★**確かめたら同型ではなかった**（**§7-7**）。★★**18 には採れた**★★（**D132-4**）—— ★**`HttpClient` を呼び出し側から渡せば★門 シ の (2) を要らない**。★**N-21 の (4)(ii) は★★口の外である★★**。→ ★★**口だけ実施。★画面は書かない**★★。★★**D43 を踏まない。★1 文字も動かさない**★★ —— ★**理由は★★画像である★★**。★**認証の口は画像を 1 バイトも運ばない**。★★**「期限が切れた」に寄りかからない**★★（★画像の経路は★門 キ のまま）。★★**やり取りの形が★2 か所に書かれる**★★ —— ★**D126-3**（通信は HTTP 越しであってコード依存ではない）が★★買った代償である★★。★**消えない。★走査で見張るだけである**。★★**D104 の札の引き金は★デッキだけではない**★★ —— ★**§35-6 は「デッキが渡る最初の瞬間 ＝ 20」と書いたが、★★パスワードは 18 で渡る★★**。★**§35-6 の字面は偽ではない**（★デッキについて正しい）。★**事実として置いた。★採番しない**。★★**この回で書くのはコードであって、★1 バイトも送らない**★★（★16 も★証明書が無ければ 1 度も待ち受けない）。★★**門は 12 → 13 になった**★★。★★**利用者判断は 1 つも閉じず、★1 つ開いた。★止まらず問いを作った**★★（★**誰がアカウントを作れるか**。★**文言は同期設計メモ §50-11 の 1 か所だけ**）。★**既存の決定との突き合わせは 14 行に当てて訂正 0 件**。 ★★**2026-09-01（同日）: ★2 つの回答をまとめて記録し、★§32-6 の 17-2 を実施した（`決定 D133`）。★格は 3 つに分かれる**★★（★**N-24 の (1) の判定 ＝ 技術判断 / ★**N-25** の答えとアカウントの任意性 ＝ ★★利用者判断★★ / ★門の判定と口の細目 ＝ 技術判断**。★★**中身をここに書かない。★正は同期設計メモ §9-18 / §52 と `決定 D133` の詳細である**★★ / **D-15**）。★★**「証明書が用意された」と読まないこと**★★ —— ★**友人が答えたのは「★★必要になったら用意できる★★」であり、★★手段も形も期限も述べられていない★★**（**D133-1**）。→ ★**N-24 の (1) は★★閉じない★★**。★★**実装の従属は★1 つも解けない**★★（**D133-2** —— ★**16 / 18 / 20 とも★★この回答より前から解けていた★★**）。★★**柵: 「用意できる」を「用意した」と読み替えない**★★（**D133-3** —— ★動かす日に実際に在ることを確かめる）。★★**N-25 は閉じた ＝ 誰でも作れる**★★（**D133-4**）。★★**経緯に★撤回が 1 件在る**★★ —— ★**「招待された人だけ」が★★台帳に入る前に撤回された★★**。★★**決定の訂正ではない**★★（★**D129** が **D128** を覆したのとは格が違う）。★★**入ったのは推奨ではなく★前提の訂正である**★★（★同期は「★★同じアカウントの端末どうし★★」であって「PC 版と Android 版」ではない / **D123** / **D120**。★**訂正が実物と合うことを 3 件とも当てた**）。★★**格は §7-12 の 11 例目で、★2 点新しい**★★（**D133-11** —— ★**撤回** / ★**前提の訂正**。★どちらも過去 10 例に無い）。★★**D104 の札が★発火した。★時期は「★動かす前」である**★★（**D133-5** —— ★**書いても★証明書が無ければ誰も作れない**）。★★**引き金が 3 つになった**★★（★デッキが渡る / ★パスワードが渡る / ★★見ず知らずの人がアカウントを作れる★★）。★**最も早いのは 3 つ目**。★**§35-6 の字面は 1 文字も書き換えない**（★デッキについて正しい）。★★**アカウントは任意である**★★（**D133-7**）—— ★**アカウント無しでも★カードリスト・検索・デッキ構築・共有形式・盤面・設定・★★カードマスタの取り込み★★が使える**。★**使えないのは★同期・預ける・端末の名簿である**。★★**D127-4 の範囲を広げた。★同じことを言っているのではない**★★（★**D127-4** は「名乗る★操作★」についてで、★★アカウントが存在しない状態を 1 文字も述べていない★★。★**1 文字も動かない**）。★★**Phase 5 に効く**★★ —— ★**Android だけの人も★アカウント無しで使え、★★2 台持てば同期も使える★★**。★**§0 の表と食い違わない**（★表は「使える」と書いており「使うか」を述べていない）。★★**デッキの保存・編集を両方に実装することは★既に決まっていた ＝ 確認である**★★（**D133-8**）。★★**§32-6 の 17-2 を実施できる**★★（**D133-10** —— ★**進む★前★に 5 件当てて★★新しい門は 0 だった★★**）。★★**「アカウントの任意性を実装した」とは書かない**★★ —— ★**実装するものが 1 つも無い**（★認証を要求する経路が★★今日 0 件である★★ ＝ いまの状態そのもの）。★**既存の決定との突き合わせは 12 行に当てて訂正 0 件**。★**新しい論点は 1 つも立てていない**。 ★★**2026-09-01: ★記録 4 件と、★新しい門 1 つ（`決定 D134`）。★あわせて §32-6 の 20 を実施した。★iOS の方針は利用者判断、★残り 5 つは技術判断である**★★（★**iOS は「すべての実装が完了し、Android 版と Windows 版のリリースが終わったあと、余裕があれば設計・実装する。現時点では対応しない」/ ★N-26 ＝ 門 セ を登録 / ★型 D-39 / ★索引 U33 / ★デッキの保管は利用者 1 人につき JSON のファイル 1 つ / ★20 の口の細目**。★★**中身をここに書かない。★正は同期設計メモ §54 / §55 / §9-19 / §10 の N-26 と `決定 D134` の詳細である**★★ / **D-15**）。★★**「新しい門は 0 だった」と読まないこと**★★ —— ★★**過去 3 回は 0 だったが、★この回で初めて非 0 である**★★（★**進む★前★に 3 つ当てた** —— ★★呼ばれる回数の上限が★1 つも無い★★）。★★**「招待制の撤回が原因である」とも読まないこと**★★ —— ★**上限が無いことは★★撤回の前から真である★★。★撤回が変えたのは★★呼べる相手の集合★★であり、★同じ実装が★★守る相手を失った★★**（**§7-7**）。★★**測って断定が 1 つ偽だと分かった**★★（**D134-2**）—— ★★**「認証の口は漏らさない」は偽である。★★時間で漏れる★★**★★（★実測: ★在る利用者名 1526 ms / ★無い利用者名 1 ms。★★どちらも 401★★）。★**型は D-15 (h) と (j) の両方**。★★**7 回連続で途切れていた (j) を★この回で踏んだ**★★。★**字面は 1 文字も書き換えず隣に置いた**（**D-35**）。★★**「ちらつきに新しい所見の番号を立てた」とも読まないこと**★★ —— ★**D-34 の★★2 例目である★★**（★あの節自身が「別の番号にすると 1 回目と 2 回目が結びつかない」と書いている）。★**立てたのは★索引の **U33** だけである**。★★**作法は★既に書かれていた。★守られなかった**★★（★型は **D-2** の前例そのもの。★★新しい作法を書き足していない★★ / **D-15** の規約 3）。★★**「20 で同期が動くようになった」とも読まないこと**★★ —— ★**入ったのは★サーバー側の口だけで、★★呼ぶ側は 1 行も無い★★**（★21 / 23 / 24 / 25 は★1 つも着手していない）。★★**柵が 1 つ増えた**★★（**D134-7** —— ★**受け取った字面をファイル名にしない**。★**破る経路は今日 0 本**）。 ★★**決まったものと残っているものの内訳をここに書かない**★★（★★**2026-08-31: 列挙そのものを消した。★ここには内訳が書いてあり、`決定 D110` で偽になって一度直したが、`決定 D111` で 3 度目に古くなる**★★ / ★**数と内訳をここに書かない。★正は同期設計メモ §17-10 / §19-8 / §20-9 / §24-11 / §25-12 / §26-14 と `決定 D110` の D110-6 / `決定 D111` の D111-5 / `決定 D112` の D112-6 / `決定 D113` の D113-6 / `決定 D114` の D114-8 / `決定 D115` の D115-10 / `決定 D116` の D116-13 である** / ★★**いまの状態の索引は同期設計メモ §22-6 の 1 か所である**★★（★**決定ごとの節は「その回に何が起きたか」、★§22-6 は「いま何が閉じ何が開いているか」。★役割が違うので混ぜない**）。★★**ただし §22-6 が覆うのは ①(f) の分割だけである**★★ —— ★**N-10 も N-13〜N-18 もその表に載っていない**（★**§22-6 自身が「N-13 は ①(f) の分割に入っていない。この表に混ぜない」と定めており、★N-10 も同じ**）。→ ★**それらの状態の正は同期設計メモ §10 の各節である** / **D-15** / ★**D105 で「避けられない」だった衝突は D107 で「日常」に強まった** / ★**回答が開いた論点は同期設計メモ §10** / ★**判断順序は §11。★本線の先頭は N-5 = ①(f) のまま** / ★★**検出層は 2026-08-30 に決着した（`決定 D110` ＝ 操作の履歴 / 案 6）**★★（★★**回数と内訳をここに書かない。★正は同期設計メモ §14-8 / §15-6 / §16-7 / §17 である**★★ —— ★**2026-08-29 訂正: ここには「2 度止まった」と 2 件の内訳が書いてあり、同日 3 度目で偽になった**。★**同じ数を 2 箇所に書けば必ず食い違う** / **D-15**）。★★**決め手は「案 4 が劣ること」ではない**★★ —— ★**2 案は「どちらも決め手なし」で並んでおり**（★§15 が案 4 の決め手を崩し、§16 が案 6 の決め手を崩した）、★★**穴 (c) を塞ぐ費用を測って初めて比べられるようになった**★★。★**案 4 の代償は 1 つも重くなっていない**（★**「却下した」と書かないこと** / `決定 D110`）。★★**根拠は測定であって利用者の語ではない**★★ —— ★**「最新の★履歴」から案 6 を導かない**（**D107-3** の柵 / **D110-5**）。★**相談役の作法として立てた節は同期設計メモ §7-3 の追記 / §7-9 / §7-10**）。★**共通の問い**（`save` を通らない書き込みで `決定 D101` の 3 つ組をどう動かすか）もそこに在る。~~★★**利用者にしか答えられない論点は 0 になった**★★（★**残りはすべて技術判断である**）~~ → ~~★★**2026-08-30 訂正: また非 0 になった。★A-2 の調査で 1 件出た**★★（★**画面の「更新」の日付と一覧の並び順。★同期を 1 行も書かなくても起きる**）~~ → ★★**2026-08-30（同日）: 回答を得て再び 0 に戻った（`決定 D109`）**★★ → ★★**2026-08-31: また非 0 になり、★同日に回答を得て再び 0 に戻った（`決定 D113`）**★★（★**これで 3 度目である。★§5-3 が「次に論点が出ればまた偽になる」と書いたとおりに起き続けている**）。 → ★★**2026-08-31（同日）: 4 度目に非 0 になり、★今度は★回答を待たずに続けた（`決定 D116`）**★★（★**2 件 —— ログの寿命 / 復元の口**。★**止まる条件が緩められ、★問いの文言まで作って残りを続けた**。★★**回答は返っていない。★開いたままである**★★ / **D116-11**）。 → ★★**2026-08-31（同日）: 5 度目に非 0 になり、★今度も★回答を待たずに続けた（`決定 D121`）**★★（★**1 件 —— アプリの実態の版は何番か**。★**問いの文言は同期設計メモ §31-6 の 1 か所だけ**。★★**方向（版-B）は止まっていない。★値だけが待ちである**★★ / **D121-8**）。 → ★★**2026-08-31（同日）: その 1 件に回答が返った（`決定 D122`）。★★ただし 0 には戻らない★★**★★（★**N-19 の「一定の期間」が開いたままである** / **D120-12** / **D122-8**。★★**過去 3 回（`決定 D109` / `決定 D113` / `決定 D117`）は返った時点で 0 になったが、★今回は違う**★★）。 → ★★**2026-08-31（同日）: 6 度目に非 0 が増えた。★今度も★回答を待たずに続けた（`決定 D123`）**★★（★**2 件 —— 1 台に複数のアカウントが同居しうるか / 用意するサーバーで何が動かせるか**。★**問いの文言は同期設計メモ §34-11 の 1 か所だけ**。★★**開いている利用者判断の★数と内訳をここに書かない。★正は同期設計メモ §35-14 の表である**★★（★★**2026-09-01: ここには「3 つになった」と書いてあり、★決定 D124 で偽になった。★先例どおり★数そのものを消した**★★） / **D-15** / **D123-8**）。★★**数と内訳をここに書かない。★正は `docs/同期設計メモ.md` §5-3 である**★★（★**§5-1 が「①と② の 2 つだけ」と数えたあと動き続けている。★数はここで数えない**）。★★**D109 が決めたのは「更新」の日付だけである**★★ —— ★**`revision` には及ばない**ので **A-1**（D-9）と **A-2** の `revision` の論点は開いたまま（★**判定表は同 §5-4**）。★**新しい論点を 1 つ開いた**（**N-15** 同期で受け取ったときの「動かす」の中身）。★★**`決定 D110` はさらに 2 つ開いた**★★（**N-16** ログをいつ捨てるか / **N-17** ログを失ったとき何をするか）—— ★★**N-16 には「ログの寿命が★削除の寿命になる」が張り付いている**★★（**D110-4**。★**捨てる規則を決める時点でこの制約が効く**）。★**実装の前に要るものと 1 コミット = 1 論点の順序は同期設計メモ §17-9**（★★**6 コミットとも 2026-08-30 に終わった**★★ —— ★**入ったのは器と記録点だけで、★同期の送受信は 1 行も無い**。★**穴 (b) は開いたまま**で、★**A-2 / D-26 と同じ問いである**） | `docs/同期設計メモ.md` |
| ★**原典を根拠に使うとき**（★**いま監査しない**） | ★**U31** | ★★**`docs/引き継ぎドキュメント.md` は D-5 の分以外が未監査である。**★★ ★**監査したのは `P1`〜`P5` の定義（531-535 行 / 3965-3969 行）だけ**。★★**監査の対象は 2 つある**★★ —— (1) ★**原典 §1.5「確定済みの設計判断（D1〜D36）」の未監査項目**（★**`docs/決定事項一覧.md` §3 が「未記録」と書いていた番号がここに在る**）/ (2) ★**残り約 5,300 行**。★**いま監査しない**（範囲が爆発する）。★**既知の誤りは 3 件だけ判明している**（13フェイズ / 廃止した語「一人回し」/ `P1` の「v4」）が、★★**これは監査の結果ではない**★★ —— D-5 の作業でたまたま出たものだけである。★**「追跡下にある = 検証済み」と読まないこと**。★★**2026-08-27 追記: 実物が 1 つ挙がった**★★ —— ★**原典 §1.2「確定済み」に、`docs/決定事項一覧.md` に採番されていない決定群がある**（★**`P1`〜`P5` と同じ形**。★**中身はここに写さない**。扱いは `docs/同期設計メモ.md` §6-3） | `docs/引き継ぎドキュメント_注意.md` |
| ★**全体試験が落ちたとき**（★★次に出たとき★★） | ★**U33** | ★★**`flutter test` が★ときどき落ち、★どの検査かが特定できていない。**★★ ★**3 例**（★2026-08-28 に 1 件 / ★2026-09-01 に 3 件 / ★★**同日にもう 1 回 —— ★このときだけ★名前が残った**★★）。★★**3 例とも次の走りから通り、★1 行も直していない**★★（★1 例目は 4 回連続 / ★2 例目は 5 回連続 ＋ ★同時実行も試した）。★★**「再現しない」で閉じない**★★（**D-10** —— ★通ったことは★無いことの証明ではない）。★★**3 例目で★★機構まで見えた**★★ —— ★**落ちた 2 件とも★★実 DB を一時ディレクトリに作る検査★★で、★`TimeoutException`（30 秒）のあと★★後片づけが Windows のファイルの掴みで失敗している★★**（★**因果は分からない** / **D-28**）。★★**1 例目 / 2 例目と同じ原因かは★言えない**★★（★あの 2 回は★★生出力が残っていない★★）。★★**残っていないもの: ★並び順の種 / ★同時に走っていたもの**★★。★**残っているもの: ★3 例目の生出力と名前と例外**。★★**§3 の `loveca-ui` の件数は★★通った回★★のものである**★★（★落ちた回の件数ではない）。★★**作法は★既に書かれている。★守られなかった**★★ —— ★**全体試験は★★生出力をファイルに落としてから読むこと★★**（`flutter test --file-reporter json:...`。★★渡し方 3 通りの実測つき★★）。★★**中身をここに書かない。★正は右の **D-34** である**★★（★**新しい所見の番号を立てない** —— ★あの節自身が「別の番号にすると 1 回目と 2 回目が結びつかない」と書いている） | `ルール整合性チェック_v1.06.md` **D-34** |
| 済 | ★**U32** | ~~★★**コード中の決定番号・設計番号が台帳に実在するかを、テストで突き合わせていない。**★★~~ → ★**2026-08-30 解消**（束 C の commit 0）。`loveca-ui/test/docs/decision_number_test.dart`。★**見るのは `決定 DNN` と `D-NN` の 2 形だけ**で、★★**素の `DNN` は見ない**★★（★実データのカード番号と字面が同じで機械には分けられない。★**その字面が実在することもテストが固定している**）。★★**置いた初日に索引の欠落を見つけた**★★ —— ★**詳細節は在るのに §1 の表に行が無い決定がある。★番号も件数もここに書かない。★正はテストである**（★機械が数える側に置いた / **D-15**）。★以下は発見当時の記述 —— ★**D-29 の検知手段そのもの**（`(P4)` はこれで捕まった —— 参照が 0 件である一方 `決定 D11` は台帳に在る、という対を並べれば「置き換え済み」と読める）。★**方式まで書いてある。そのとき検討をやり直さないこと**。★★**条番号（総合ルール）側には同じ手が使えない**★★ —— PDF が git 管理外（§1）で、**突き合わせる相手がリポジトリに存在しない**。★**効くのは台帳がリポジトリ内に在る番号体系だけ** | **解消**（`ルール整合性チェック_v1.06.md` D-29 / `loveca-ui/test/docs/decision_number_test.dart`） |
| 済 | **B-4** | ~~ポジション / フォーメーションチェンジに補助コマンドを置くか~~ → ★**決定 D74 で解消。置く。**`MoveMemberBetweenAreas` の合成で表現でき **`loveca_core` の変更は要らない**。★11.10.2 / 11.11.2 の制約は**コマンドの中だけ**に置き、素のドラッグには課さない | **解消**（`docs/盤面設計メモ.md` §3 / 決定 D74） |
| 済 | **B-2** | ~~エネルギーデッキは「順番が管理されない」~~ → ★**決定 D73 で解消。無作為 1 枚抽出。**★結論は当初の提案どおりだが**根拠が違う**——「非公開領域なので観測上の差が無い」は**成立しない**（6.2.1.3 がシャッフルしないので index 0 はデッキリスト順＝プレイヤーが選べる）。決め手は **4.9.3** と **10.5.4 の戻り経路** | **解消**（`docs/盤面設計メモ.md` §2 / 決定 D73） |
| 済 | **D-2** | ~~★**`loveca-core` にリントが 1 つも効いていない**~~ → ★**2026-08-24 解消（M-B1）。**`analysis_options.yaml` を接続し `lints ^6.1.0` に揃えた。★**想定していた `constant_identifier_names` と `StepId.s8_4_13` の衝突は起きない**（実測。除外設定 0）。実測 7 件はすべて処置済み | **解消**（`ルール整合性チェック_v1.06.md` D-2） |
| 済 | **U5** | ~~盤面のカード表示に `thumb` で足りるか~~ → ★**決定 D82 で解消**（2026-08-25）。★**答えは定数ではなく DPR の関数**（`cardImageSizeFor`）。スロット論理 76 は DPR 3 で 228px になり初めて `normal` が要る。★**D42 のキャッシュ見積りはやり直しにならない**——`ResizeImage(width:)` は物理幅であって段ではない | **解消**（`docs/盤面設計メモ.md` §6-6） |
| 済 | **U17** | ~~相手の手札を常時出したままにするか~~ → ★**決定 D84 で解消**（2026-08-25）。**常時表示。**★**ローカル対戦**は 1 人が両プレイヤーを操作するので隠すとドラッグできない（D77 / ★語は D88-1） | **解消**（`docs/盤面設計メモ.md` §4-3） |
| **盤面の縦の見た目を次に触るとき** | **U19** | ★**盤面の最小の高さ。**進行バー・整理の帯・常設の帯・集計は**縦にスクロールしない**ので、窓を縦に狭めると固定の段が溢れる。★**縦をどう解くかはまだ決めていない**——幅（D75）は「盤面ごと横スクロール」で解いたが、**縦を同じ形にすると `BoardLayout` の中のスクロールと入れ子になる**。★★**測定値をここに書かない。**★★ M-B3 の初測から M-B6・D95 まで**マイルストーンごとに動いており**、索引に置くと**最も食い違いやすいものを二重に持つ**ことになる（D-15 (f)(g)）。★**同時に測り直すもの: U20 / U26**（袖幅と文言が測定値の入力である）。★★**2026-08-27（M-B7）に 10 値を測り直した** —— 9 値は不変で、**「最も混んだ押下」の測定を新設**した★★ | ★**正は `docs/盤面設計メモ.md` §13 の未決一覧の `U19` の行**（測定値・内訳・経緯はすべてそこに在る） |
| ★**盤面の袖の見た目を次に触るとき**（★**U19 / U20 の測り直しと同時**） | ★**U26** | ★**袖の「エネルギーを1枚出す」が語の途中で折り返す**（★切れてはいない）。★★**袖幅も文言も U19 / U20 / D83 の測定値の入力である**★★——`kSoloBoardMinWidth = kBoardMinWidth − kBoardSleeveWidth`。**どちらを変えても測り直しが要る** | `docs/盤面設計メモ.md` §13 |
| ★**盤面の操作の入口を次に触るとき**（**U28 と同時**） | ★**U27** | ★**ポジションチェンジ 11.10 の入口が 4 段と深く、札がタップできる手がかりが無い。**11.11 は袖にボタンがあり**非対称**。★**非対称そのものは条文由来**（11.10.1「そのメンバーを」/ D93-3）だが、★★**「4 段」と「手がかりが無いこと」は実装の都合である**★★。混ぜて「条文どおりだから直せない」と読まないこと | `docs/盤面設計メモ.md` §13 |
| ★**盤面の操作の入口を次に触るとき**（★**U27 / U28 と同時**） | ★**U30** | ★★**盤面がキーボードで操作できない。**★★ ★**合成キー入力は届く**（R4 の検索欄に文字が入る）が、盤面では `Tab` を 8 回押しても **1 px も変わらず**、メニューを開いて `Esc` を押しても**閉じない**。★走査でも `loveca-ui/lib` に `Shortcuts` / `Focus(` / `LogicalKeyboardKey` が **0 件**。★★**不具合ではなく「手当てを 1 つも持っていない」という事実**★★。★**論点は 3 つあり、`Esc` だけは独立に置ける** | `docs/盤面設計メモ.md` §13 |
| ★**U27 と同時** | ★**U28** | ★**フォーメーションチェンジの拒否理由が、そこから到達できないボタンを指す。**「先に『整理する 10.4 / 10.5』を押してください」と出すが、**そのボタンは進行バーにありダイアログを閉じないと押せない。**★「黙って効かないボタンを作らない」は満たしているが、**示した次の一手に到達できない** | `docs/盤面設計メモ.md` §13 |
| ★**U13 を決めるとき（Phase 6）** | ★**U29** | ★**盤面の `←` が確認なしにセッションを捨てる**（`AppBar` に `leading` を置いていないので Flutter 既定の戻る）。★**U13 のとおり保存も同期もしない**ので戻らない。★R2 のデッキ削除は確認 1 枚を持つのに、**失うものがはるかに大きい経路に確認が無い**。★★**U13 に一方向で依存する**★★が、**確認ダイアログ自体は独立に置ける** | `docs/盤面設計メモ.md` §13 |
| 済 | ★**D-22** | ~~★★**カタログに無い孤児カードが盤面から消える。**★★~~ → ★**決定 D95 で解消**（2026-08-26）。**動かさず元の置き場に残す**（10.1.2「条件を満たしている場合に実行されます」を満たせないため）。★★**走査で同じ形がもう 1 か所出た** —— **10.4.1** も束を無条件に捨てていた★★。★★**種別を確かめずに断定している箇所は 2 種類あった** —— 種別**不明**（データの問題）に加え、**ライブカードの孤児**（種別は分かるが 10.5.3 / 10.5.4 のどちらにも当たらない = 条文の問題）★★。理由を 2 つに分けて出す。★**決定 D35 を盤面へ広げただけ**で新しい原則ではない。★**直したら A-3 に退化しかけた**（`break` の前に記録を取り込んでいなかった） | **解消**（`ルール整合性チェック_v1.06.md` D-22 / 決定 D95） |
| 済 | **U20** | ~~★**ソロの最小幅・最小高さ**~~ → ★**2026-08-25 に測って解消**（M-B4）。**`kSoloBoardMinWidth = kBoardMinWidth − kBoardSleeveWidth = 988`**（★**袖 1 本ぶんだけ狭い**）。実測: (b-1) 394 / (b-2) 496 / **(b-3) 584** / 縦 392。★★**判断基準を測る前に宣言してから測った**★★——「袖 1 本（112）以上小さいときだけ分ける」。★★**予測（「(b-3) 696 は条文由来なので不変」）は外れていた**★★——**(b-3) が測るのは窓の幅**で、袖が 1 本消えれば同じ帯が 112 狭い窓に収まる | **解消**（`docs/決定事項一覧.md` の「D88 / D89 の実装」） |
| ★**Phase 6** | **U18** | ★**`redact` された `CardInstance` が手札の帯に混ざったときの描画。**D84 の「Phase 6 では UI に分岐が要らない」が成立する条件がこれ。`HiddenPile` は枚数しか受け取らない（D77）が、**手札の帯は `CardInstance` のリストを受け取る**。★**いま決めなくてよいが、書いておかないと Phase 6 で「話が違う」になる** | `docs/盤面設計メモ.md` §13 |
| 済 | **U16** | ~~盤面の最小ウィンドウ幅~~ → ★**決定 D83 で解消**（2026-08-25）。**1100 据え置き。**(a) 外部標準は**無い**（D75 がペイン縮退を禁じている）/ (b-1) 溢れの下限 506・530 / (b-2) ★**条文由来の下限 696**（6.2.1.5 の初期手札 6 枚が同時に見える）/ (b-3) ★**札の大きさは窓幅で変わらない**ので可読性は `kBoardSlotWidth` の話。★**測定手法に欠陥があり U8 の記録値も 151 → 198 に訂正**。★★**M-B3 で測り直した**（506 変わらず / 530 → 496 / 696 変わらず）。**1100 は据え置き** | **解消**（`docs/決定事項一覧.md` D83 / D86） |
| 済 | **D-19** | ~~★**「乱数を消費するアクション」の列挙に `LookAtTop` が無い**~~ → ★**2026-08-25 解消。**列挙に足し、★**条件つき（10.2.2.2 の不足時のみ）であることも書いた**。★★**あわせて「列挙方式そのもの」への注記を隣に置いた**★★——3 度数え直して 3 度とも漏れている（D-15 → D-16 → D-19）ので、**「ここを直せば済む」と読ませない**。判定が要るなら列挙ではなく**観測**（`CountingRng` / D90-1）。★断定の中身はテストで固定した | **解消**（`ルール整合性チェック_v1.06.md` D-19） |
| ★**`Zone` に属性を足すとき** | **D-20** | ★**属性を定義したのに `lib/` の消費者が 1 人もいない。**★**`Zone.visibility` は決定 D91 で解消済み**（`card_move.dart` が消費している）。★**いま残っているのは `Zone.isShared` だけ**——`Zone.visibility` は 3a-1 から在ったのに**宣言以外に `lib/` の参照が 0 件**で、結果として **4.1.2.1 が 1 度も実装されないまま M-B5 まで来た**、というのが発見の経緯である。★★**検知手段は無い（実測で確認）**★★——`dart analyze` は**公開フィールドの未使用を報告しない**（模擬事故を仕込んで `No issues found!` を確認）。★**同種がもう 1 件ある: `Zone.isShared`**（ただし共有であることは型で表現済みなので不具合ではない）。★**教訓は「使われない」だけではない**——**条文由来の属性は、その条文が要求する振る舞いも同時に実装されたかを見ること** | `ルール整合性チェック_v1.06.md` D-20 |
| 済 | **D-21** | ★★**2026-08-26 に `tidy` の器だけ解消（決定 D93-5）**★★ —— `BoardState.tidy` を `tidies`（`List`）にし、`dispatchAll` が全件を積む。★**残るのは `advance` / `skipped` で、M-B7 で同じ形へ揃えるだけ**である。★以下は発見時の記述 —— ★**`GameStore.dispatchAll` の合成は「報告を持つアクション」を 2 つ以上含められない。**`refreshCount` だけが積まれ、`tidy` / `advance` / `skipped` は**最後の 1 件で上書き**される。★**いまは実害が無い**（合成 7 経路は `MoveCard` 系で、`AdvanceStep` は 1 件版からしか渡らない。走査で確認）が、★★**決定 D92 の自動進行は 1 押下に `AdvanceStep` を複数入れるので直接踏む**★★——**警告の観測が落ちると停止判定まで誤る**（表示だけの問題ではない）。★**doc が実装より広い型**（D-15 (h) と同じ向き。「N 個のアクション」と種類を限定していない）で、★**走査では出ない**。→ ★★**2026-08-27 に全件解消**（M-B7 / 決定 **D98-2**）★★ —— `executed` / `taken` の単数をやめ **`BoardStepLog` の列**にし、`skipped` も連結した。★**`cursorBefore` と `taken` が別のステップを指す問題も型ごと消えた**（各件が自分の `cursor` と `taken` を対で持つ）。★**着地先 `cursorAfter` は新設**（`steps` から導けない）。★**doc 側の限定の無い書き方も同時に直した** | **解消**（`ルール整合性チェック_v1.06.md` D-21 / 決定 D98-2） |
| ★**引き金を踏んだとき** | **U15** | ★**巻き戻し後に乱数が張り直されない**（undo は乱数を戻さないので、戻して引き直すと違う札が出る）。★★**退避先の中身も引き金 2 つも決めてある**——(1) ★**ソロ / ローカル対戦**で実際に問題として報告された (2) Phase 6 で undo を成立させる方式が見つかった。★**そのとき検討をやり直さないこと**（U12 と同じ形）。★★**M-B5 で器の前半が揃った**★★——`CountingRng` が `nextInt` の回数を数えている。**足すのは「`SeededRng(seed)` を張り直して N 回空回しし、`ReduceContext` を差し替える」部分だけ**（決定 D90-1） | `docs/盤面設計メモ.md` §8-3 / 決定 D78 / D90-1 |
| ★**`loveca_core` の `RuleProcessWarningKind` を触るとき**（★**U25 と同時**） | ★**D-24** | ★★**10.6 の警告が「エール処理中」の札にも立つ。**★★ `warningsFor` は `state.resolution.isNotEmpty` だけを見るが、★**10.6.1 は「…または**エール処理中（8.3.11）である**以外のカード」と書いており、エール中の札は対象外**である。★`docs/PhaseEngine設計メモ.md` §11 は「エール処理中はカーソルから観測できる」と**自分で書いている** —— **観測できると書いたほうを実装は観測していない**。★**M-B3 から在る**が、M-B7 で**停止条件**になって初めて挙動として現れた（押下回数が 12 / 7 → 13 / 8）。★**誤検出であって見落としではない**（盤面は壊していない）。★**直さない理由 3 つと判断の時期は参照先にある**。★★**いまの挙動をテストで固定してあるので、直すと押下回数の検査が落ちる**★★ | `ルール整合性チェック_v1.06.md` D-24 |
| ★**取りこぼしが報告されたとき** | **U25** | ★**自動進行の「新規の警告」を種類だけで見てよいか**（決定 D92 / R-S2）。`RuleProcessResult.warnings` は `List<RuleProcessWarningKind>` で**対象カードを 1 つも持たない**ので、**10.6 の対象が入れ替わっても「同じ警告」と見なして止まらない**。★**今の型でできるのは種類の比較だけで、持っていないものを比較に使わないという判断**であって、正しさを確かめたわけではない。★対象を持たせるなら `loveca_core` の変更が要る。★**そのとき検討をやり直さないこと**（U12 / U15 と同じ形） | `docs/盤面設計メモ.md` §13 / §15-5 |
| **`loveca_core` を次に触るとき** | **U14** | ★★**2026-08-25 追記: 根拠が条文で確定した（決定 D91）**★★——4.1.2.1 + 4.5.3 により**メンバーエリアに置かれたカードは表向きになる**（下に重ねたカードも同じ）。★**U14 自体は未決のまま。**★**メンバーエリアのカードを裏向きにできない。**`FlipCard` が `Zone.memberArea` を弾き、`PlaceMemberInArea` が `faceUp` 固定なので**構造的に経路が無い**。4.5 に表示面の規定が無く 4.5.3 は公開領域なので Phase 3b では出さない | `docs/盤面設計メモ.md` §7-2 |
| 済 | **D-18** | ~~★**`LOVECA_DIST_DIR` の位置づけが D60 と D64 で食い違う**~~ → ★**2026-08-25 発見・同日解消。**D64 と CLAUDE.md §3 が段 1 を「本番の設定経路」と書いていたが、D60 本文と `dist_locator.dart` は段 1 =「開発と検証」/ 段 2 =「R6 が書く」。★**実際に段 2 が一度も使われず、`LOVECA_DIST_DIR` を毎回付ける運用が常態化していた** | **解消**（`ルール整合性チェック_v1.06.md` D-18 / 運用は D89 への追記） |
| 済 | **D-17** | ~~★**盤面設計メモ §14-5 の「相手側に何を入れても自分側は同一」**~~ → ★**2026-08-25 発見・同日解消。**6.2.1.7（エネルギー）については**誤り** —— 6.2.1 は**手順ごと**に両プレイヤーを回すので、相手のデッキ枚数が変われば自分の 4.9.3 の抽出がずれる。★**実装は正しく、断定のほうが誤っていた。**★**「同じはず」で済ませずテストを書いたら 1 件外れた**（D86 に続き 2 度目） | **解消**（`ルール整合性チェック_v1.06.md` D-17） |
| 済 | **D-15** | ~~★**doc の「列挙による断定」が実装より古い**~~ → ★**(a)〜(k) をその都度解消済み。**★★**ただし手法は生きている**★★——型は 1 つではなく、**走査では出ないものがある**（doc が実装より**広い** (h) / doc が**誤った方針**を書く (i) / **検証していない完了報告** (j) / **実行できない確認予定** (k) / ★**未実装の決定の前提が、別の決定に動かされていた** (l) / ★★**内訳が誤っていても合計が合う** (m)★★ / ★★**捨てるつもりの複製が残った** (n)★★）。★★**(l) は向きが新しい**★★——(a)〜(g) は実装が動いて doc が取り残されたが、(l) は **doc（D92）が実装を待っているあいだに別の決定（D93-5 / D95）が前提を動かした**。→ ★**未実装の決定を実装するときは「前提が動いていないか」を先に確かめること。**★**次に何かを列挙したら同じ走査を掛けること。範囲は `lib` だけでは足りない**——`docs/*.md` / `CLAUDE.md` / `test/` を必ず含め、★**陽性対照を置いてから走ること**（D-16 の盲点）。★**各型の内訳と実例はここに写さない**（正は右の参照先） | `ルール整合性チェック_v1.06.md` D-15 / D-16 |
| **Phase 6**（D-9 / D-5 と同時に） | **U13** | ★**ゲームの勝敗・投了（1.2.1 / 1.2.2 / 1.2.3）の記録場所が `GameState` に無い。**`LiveJudgementRecord` は**ライブの勝敗**であって別物。Phase 3b は UI 側の Store に持つ（★**ソロもローカル対戦も保存や同期をしない**。★ここは D88-1 で廃止した語が残っていた最後の 1 箇所で、2026-08-26 に直した）。★★**U29（`←` の確認）はこれに依存する。U13 を決めるとき一緒に見ること**★★ | `docs/盤面設計メモ.md` §7-2 |
| **Phase 3b 完了後**（★SQL を足すなら `loveca_db` を触る時点 = D-8 / D-11 / D-13 と同時） | **U21** | ★**詳細検索（R3 / R4）。**いま引けるのは 5 軸だけ（フリーワード / 商品 / 種別 / コスト / パラレル表示）で、**スキーマにあるのに引けない軸が多数ある**（★索引が建っている 3 本を参照するクエリが **0 件**）。★論点は 4 つ ——**D48（メモリ上）か SQL か**（★`cards` / `printings` の列は D48 で済むが**子テーブル由来は「1 行 = 1 刷り」の投影に収まらない**）/ 種別依存の軸は素朴に絞ると他種別が全消え /現コストフィルタは実データの **52% を区別できない** / キーワードは内部語彙 | `docs/UI設計メモ.md` §12-1 |
| 済 | **U22** | ~~★**デッキの並び順をメンバー = コスト降順 / ライブ = スコア降順にする**~~ → ★**2026-08-27 確定（決定 D99）。**★**手動並べ替えを残して `ord` に保存する。**★★**規則そのものはここに書かない。正は `docs/決定事項一覧.md` の D99 の詳細である**★★（★索引に規則を写すと、片方だけ直されて食い違う）。★★**M4 の却下案「並べ替えを出さない」の却下理由は期限切れになっていた**★★（D46 は本実装のテストで固定済み。型は **D-15 (l)**）。★**予告どおり D65 の訂正ではなく新決定にした** | **解消**（`docs/決定事項一覧.md` D99） |
| 済 | ★**D-23** | ~~★**設定の保存がどの項目でも「次の起動から」と言う**~~ → ★**2026-08-26 発見・同日解消**（決定 D97 の実装と同時）。★`_saveSettings` が無条件に `_needsRestart` を立てていたが、根拠のコメントは `distDir` 1 項目ぶん（**D56**）しか無かった。★★**既存 2 項目がどちらも再起動を要したので偶然に正しかっただけ**★★で、即時に効く 3 項目目を足した瞬間に嘘になる。★型は **D-15 (h)** の親戚だが**向きが逆**（doc が実装より広いのではなく、**理由が実装より狭い**）。★**走査では出ない** | **解消**（`ルール整合性チェック_v1.06.md` D-23） |
| 済 | **U23** | ~~★**エネルギーデッキ 0 枚での保存**~~ → ★**2026-08-26 に確定（決定 D96）。**補完は**盤面の開始時**に行い、保存時には行わない。★**却下理由も決定文に残してある** | **解消**（`docs/UI設計メモ.md` §12-3 / 決定 D96） |
| 済 | **U24** | ~~★**既定のエネルギーカード**~~ → ★**2026-08-26 に確定（決定 D97）。**`LL-E-002-SD` を `AppSettings` の既定値に置き、設定で変更できる。★★**カード番号のハードコード 0 件の不変条件はこれで「1 箇所だけ」になる**★★（走査テストで見張る） | **解消**（`docs/UI設計メモ.md` §12-4 / 決定 D97） |
| 済 | **D-5** | ~~★**`P1`〜`P5` の参照先が存在しない。**~~ → ★★**2026-08-30 解消**★★（束 C の commit 0）。★**括弧なしの旧番号を書き換え、★走査を `.dart` と `.md` の両方へ広げた**（**D-31** / **N-8**）。★★**「0 件」は完了条件にできない**★★ —— ★旧番号を説明する文は同じ字面を必ず含む（**D-30**）。→ ★**許可リストとの完全一致で見張る**（`loveca-ui/test/docs/legacy_design_number_test.dart` / 先例は **決定 D97**）。★**箇所数と走査範囲をここに書かない。★正はそのテストの許可リストである。**★★**第 3 の体系を見つけた**★★ —— `docs/同期設計メモ.md` §16-4 の**前提番号**（★ペイン番号でも同期の設計番号でもない）。★以下は発見当時の記述 —— ★**採番は済んだ**（`決定 D100`〜`D102` / `P4` `P5` は `D11` `D35` として既に採番済みだった）。★★**しかし「解消 / 走査 0 件」と書いてあったのは偽の完了報告だった**★★ —— 丸括弧つきの走査は 0 件だが、★**括弧なしの旧番号が `lib` と `test` の両方に生きている**（型は新所見 **D-31**）。★**残務の正は `docs/同期設計メモ.md` §1 の A-0**（★**方式は未決**）。★以下は発見当時の記述 —— 指す先は `deckId` / `revision` / `deletedAt` / `masterDataVersion` ——**同期の中核**。★★**`P4` / `P5` は最初から採番済みだった**★★（`決定 D11` / `決定 D35`。2026-08-27 訂正。型は **D-29**）。★**箇所数はここに書かない**（増え続けるため / D-15 (g)）—— ★**走査コマンドは参照先の D-5 に置いてある**（索引に写すと 2 箇所になる）。★**完了条件は 0 件**。★★**`P1`/`P2`/`P3` は UI のペイン番号（`docs/UI設計メモ.md` §2-3）と同名**★★ なので、**素の `P1` で機械的に置換すると壊れる** | **解消**（`ルール整合性チェック_v1.06.md` D-5 / `loveca-ui/test/docs/legacy_design_number_test.dart`） |
| **Phase 4** | **D-1** | `fromKey` の厳格性を維持するか寛容化するか。新アイコン追加で**既存インストール済みアプリのマスタロードが全滅**しうる（決定 D39 は取り込み層の隔離だけで、本判断は残っている） | 同 D-1 |
| **Phase 4** | **D-4** | ★**`imageHash` は原本 PNG のハッシュで配信 WebP のハッシュではない。**画像を差分で配る経路を作った瞬間に、古い画質が**無言で**残る。★★**2026-08-31 追記: ★半分だけ閉じた（`決定 D121`）**★★ —— ★**画像の差分の単位が「画像だけ別のマニフェスト」に決まり**（**D121-1**）、★★**マニフェストの行のハッシュが★配られるバイト列を名指すので、★★受け取り側では★この症状が起きない★★**★★（**D121-2**）。★★**「解消した」と読まないこと**★★ —— ★**生成側が★出力先の存在で飛ばすほうは★開いたままで、★手当てが別に要る**。★★**左の症状の字面は 1 文字も書き換えない**★★（★**書いた時点で誤りではない** / **D-35**）。★**正は同期設計メモ §31-5** | 同 D-4（= `docs/UI設計メモ.md` U2） |
| **Phase 4** | **D-7** | ★**`min_app_version` が CLI に露出せず 1.0.0 固定。**既存の dist はすべてアプリ 1.0.0 以上を要求する。M1 は**アプリ版を上げて**回避した——★**データに合わせてアプリを変える本来と逆の向き**。★★**2026-08-31 追記: ★3 つの未決のうち (III) が決まった（`決定 D121`）**★★ —— ★★**アプリの版を実態に戻す（版-B）**★★（**D121-3**）。★**(I) は 既-3 のまま**（**D118-5**）/ ★**(II-b) は待ち**（**D118-6**）。★★**柵: アプリの版の定数だけを★単独で下げてはならない**★★ —— ★**下げた瞬間に取り込みが全件拒否になる**（★左の M1 の落ち方そのもの）。★**3 つ同時**。★★**版の★値は★利用者判断である**★★（**D121-8**。★**問いの文言は同期設計メモ §31-6 の 1 か所だけ**）。★**実装は今日していない**。 ★★**2026-08-31 追記: ★値が返った（`決定 D122`）**★★ —— ★★**`0.3.0`**★★（**D122-1**）。★★**それでも (II-b) が開いているので A-6 は閉じない。★やること 1〜4 は 1 つも実施されていない**★★（**D122-3**）。★★**柵が見ていない相手がもう 1 つ在る**★★ —— ★**同じデータ版のまま作り直すなら★版ゲート（**D118-3**）が先に要る**（**D122-4**）。★**柵は 1 文字も動かさない**（★足りないのではなく★見ている対象が違う）。★**実装の順序は同期設計メモ §32-6 のレーン 2** ★★**2026-08-31 追記: ★やること 1 を実施した（Phase 4 のレーン 2 の commit 3）**★★ —— ★**`min_app_version` の既定値を消し、★CLI へ `--min-app-version` として必須で露出した**（**D118-5** ＝ 既-3）。★★**「解消した」と読まないこと**★★ —— ★**やること 3（dist を作り直す）/ 4（アプリの版を戻す）は 1 つも実施していない。★(II-b) も開いたまま**。★★**「やること 2 を実施した」とも読まないこと**★★ —— ★**既定値を実態に合わせたのではなく、★★既定値そのものを消した★★**。★★**footgun を 1 つ記録した（★手当てはしていない）**★★ —— ★**綴りを間違えると例外も出さずに「最小版なし」に化ける**（★`compareVersions` は数にできない部分を 0 として扱う）。★**必須の自由入力にしたので今日から踏める**。 | `ルール整合性チェック_v1.06.md` D-7 |
| 済 | **U1** | ~~モバイルの画像供給経路（同梱 / 初回取得）。**D43「UI にネットワーク取得の口を作らない」に触れる**~~ → ★★**2026-09-02 解消**★★（`決定 D137` ＝ ★門 キ / **N-1** と同時）。★★**一本化する。★読む実装は 1 本のまま**★★（★画経-4 —— ★★読む先は「1 つのディレクトリ」で、★そのディレクトリを決める★段を 1 つ足す★★）。★★**D43 の害は起きない**★★ —— ★**読む実装が 1 本のままなので「どちらから来た画像か」が★★読む側に現れない★★**。→ ★**印を出す手段が要らない**。★★**「§32-6 の 8 が全部書ける」と読まないこと**★★ —— ★**5 つに割って当てた結果、★★書けるのは 3 つと口が 1 つで、★1 つは書かない★★**（★いつ取りに行くか / どう見せるかは★★モバイルの UI の形である★★）。★★**中身をここに書かない。★正は `docs/同期設計メモ.md` §59 である**★★（**D-15**） | **解消**（`決定 D137` / `docs/同期設計メモ.md` §59 / `docs/UI設計メモ.md` §5-5） |
| 済 | **D-12** | ~~★**`DeckValidator.hasUnknownCards` の doc「編集は禁止し読み取り専用で表示する」は過剰**~~ → ★**2026-08-25 解消**（D91 と同じ機会に `loveca_core` を触ったため）。★**外した理由もコメントに残した**ので、同じ断定が書き戻されない | **解消**（`ルール整合性チェック_v1.06.md` D-12） |
| 済 | **D-11** | ~~★**デッキの並び順が保存されない**（`deck_entries` に順序列が無く、★**`all` は entries に `ORDER BY` を持たない**ので経路で並びが違う）~~ → ★**2026-08-27 解消**（束 B）。`schemaVersion` 3 / `ord` 列 / ★**`byId` と `all` の両方**に `ORDER BY ord` / `save` が添字を書く。★**backfill の値と書き方は決定 D99 が差し替えた**（`printing_id` 昇順 → 規則順 / SQL → Dart）。★**画面側の当座の手当て 4 つも撤去した**。★**新所見 D-26 を同時に記録** | **解消**（`ルール整合性チェック_v1.06.md` D-11 / 決定 D65 / **D99**） |
| 済 | **U10** | ~~★**デッキの並び順が保存されない**~~ → ★**2026-08-27 解消。**★★**D-11 と同じ事象に 2 つの番号が付いている**★★（こちらは `docs/UI設計メモ.md` §10 の採番）。★**内容は D-11 の行を見る。ここに写さない** | **解消**（★**実体は D-11** / 決定 D65 / **D99**） |
| **`loveca_db` を次に触るとき** | **D-8** | ★**方式は決定済み（D63: `filesToDelete` が空でないときだけ掃除する）。残るのは実装だけ。**M3 は UI 側で「表示できないカードがある」と出すところまでを引き受けた。★**この経路が実在することは実 DB のテストで確認済み**。★★**2026-08-27（束 B）に前提を測り直した**★★ —— ★**結論ではなく条件を測った**（**D-15 (l)**）。実データで孤児 **0 件**、★**対: `deleteExpansion('BP01')` で 88 件**（**D-10**: 0 件は「無い」と「見えていない」の区別がつかない）。`manifest.json` は **25 ファイル / 商品 22** で `normalized` との差は**両向きとも空集合**、★**更新は 2026-08-23 で D-8 を書いた日より前**（作り直されていない）。→ **掃除経路は作らない。実装は 1 行も変えていない** | `ルール整合性チェック_v1.06.md` D-8 / 決定 D63 |
| 済 | **D-13** | ~~★**直った取り込み失敗が「未解消」のまま永久に残る。**~~ → ★**2026-08-27 解消**（束 B）。`MasterStateDao.recordFile` が同じ path の過去の失敗を**同じトランザクションで消す**（決定済みの案 (a) をそのまま実装。★`schemaVersion` は上げていない）。★★**方式表の「`supersededByNewerFile` は残っていても害はない」は誤りだった**★★ —— 根治後に `currentHash != hash` になるのは「**古い**版が取り込まれていて**新しい**版で失敗した」ときだけで、「そのあと別の版で取り込めています」は**向きが逆**になる。★**撤去した** | `ルール整合性チェック_v1.06.md` D-13 |
| 済 | **D-14** | ~~★**`Deck.copyWith` の既定値違反は UI から到達しなくなったが、本体は未修正。**~~ → ★★**2026-08-31 解消**★★（Phase 4 のレーン 1 の commit 1 / **決定 D116-2** ＝ 廃-3）。★**2 つの既定値を「据え置き」に変えた**（★署名は 1 文字も変わっていない）。★★**違反は 1 つではなく 2 つだった**★★ —— ★`updatedAt` の `DateTime.now()` に加え、★**`revision` の `+ 1`** も既定値だった（★§1 は長く「1 箇所」と書いていた）。★★**「同期経路が `copyWith` を通るか」は開いたままである**★★ —— ★**書き込みの口は `決定 D115-4` で決まったが、★受け取った内容から `Deck` を★どう組むかは決まっていない**（**D116-2** の理由 3）。★**解消と混ぜないこと**。★★**実測した対は 8 通り。★件数と内訳をここに書かない。★正は §3 の記録である**★★ | **解消**（`ルール整合性チェック_v1.06.md` D-14 / 決定 D116-2） |
| ★**Phase 4 の束 A**（★**D-9 / D-5 / D-14 と同時**。★**同じ問いである**） | ★**D-26** | ★★**`ord` の backfill が `revision` を上げず、マスタに依存する。**★★ `schemaVersion` 2 → 3 の `onUpgrade`（決定 D65 / **D99**）は `DeckDao.save` を通らないので、**デッキの中身（`ord`）が変わるのに `revision` が動かない**。★**さらに規則順は `cost` / `score` を引く**ので、マスタ未取り込みの端末では`printingId` 昇順だけになり、★**同じ `revision` で中身が違う**状態ができる。★**壊れはしない**（null を末尾に落とす規則があるので全順序は決まる。「マスタが空でも落ちない」を `migration_test` が固定）。★**型は D-15 (h)**（doc が実装より広い。D65 の「`ord` は `revision` に乗る」に限定が無い）。★**走査では出ない** | `ルール整合性チェック_v1.06.md` D-26 |
| **Phase 4**（D-5 と同時に） | **D-9** | ★**`DeckDao.softDelete` が `revision` を上げない。**決定 D101「更新のたびに +1。同期の差分検出に使う」と食い違う。**「更新したのに revision が動かない唯一の経路」** | `ルール整合性チェック_v1.06.md` D-9 |
| 済 | ★**U9** | ~~★**論理削除したデッキを戻す口が無い。**~~ DB には残る（決定 D102）が UI から復元できない。★★**M6 では入れなかった。忘れたのではない**——`softDelete` が `revision` を上げない（**D-9**）が未解決だった。→ ★★**2026-08-31 に確定（`決定 D117-4`）。★口は置かない**★★。★**先に D-9 が閉じている**（**D116-3** ＝ 動かさない）ので、上の「先に要る」は満たされた。★★**却下ではない**★★ —— ★**「いまの材料では置かない」であり、★開き直す条件を 3 つ書いた**（★★**1 つは「好み」なので `決定 D113-3` とは 1 つ違う**★★）。★★**D102 が 3 つの条件すべての前提である**★★（★**行が残っているから「復元」になりうる。★`決定 D117-4` は行を 1 ビットも減らさない**）。★★**内容も条件もここに書かない。★正は `決定 D117` の **D117-6** と `docs/同期設計メモ.md` §27-5 / §27-6 である**★★ | **解消**（`決定 D117-4` / `docs/UI設計メモ.md` §10 の U9） |
| **Phase 5 着手時** | **§7 の項目**（★**U3** = `thumb` 200px / ★~~**U1** = 画像供給経路~~ ★**U1 は 2026-09-02 に解消**（`決定 D137`）。ほかは無番） | ★**内訳と件数はここに書かない。正は §7 の表**（★項目は増える —— M-B5 / D90-3 でタッチの `Tooltip` が足された）。★**未了である限り Release 1 を「完了」と呼べない** | `docs/UI設計メモ.md` §7 |
| **Phase 5** | **U4** | 一覧の投影クエリを `loveca_db` へ移すか（スマホが同じ投影を要すると確定したとき） | `docs/UI設計メモ.md` §4-5 |
| **Phase 5** | **U8** | ~~★**ペインの切替しきい値 840 論理px は暫定値**~~ → ★**M4 で実測して確定**（採用 320 / 444+1+320 = 765 ≤ 840）。**残るのは実機のタブレットでの妥当性だけ**。★★**測定値の格が 2 段ある。混ぜないこと**★★——**格(1)** はウィジェットテストの**溢れない下限**（テスト用フォント / 構造の話）、**格(2)** は実機の実データで**刷り番号の行が読める下限**（可読性の話）。★★**採用値 320 の根拠は格(2) であり、余裕を計算するときも格(2) から引く**★★（★**束 B が格(1) から引いて「余裕 32」と書いたのが実例**。2026-08-27 訂正）。★★**測定値をここに書かない。正は `docs/UI設計メモ.md` §9-7 の表**★★（★**格(1) と格(2) にたまたま同じ数が入った時期があり、索引に写すと最も取り違えやすいものを二重に持つ**）。★**2026-08-27 に格(2) を測り直した**（D83 の「測定値を持つ項目は同時に測り直す」）—— **D61 の結論は変わらない** | `docs/UI設計メモ.md` §9-7 / `docs/決定事項一覧.md` D83 |
| 済 | **U7** | ~~共有形式インポートの既定の刷りの選び方~~ → ★**M6 で解消（決定 D68）。**非パラレルのうち `printingId` 昇順の先頭。UI で差し替えられる | **解消**（`docs/UI設計メモ.md` §2-5 / §9-11） |
| 済 | **U11** | ~~★**一覧はライブのセルを `cover` で切っている**~~ → ★**決定 D72 で解消。**箱の寸法は種別で変えず、中に `cardAspectRatioOf` の枠を作って中央に置く。**タイルは 200:279 のままなので D42 の測定条件は 1 つも動いていない** | **解消**（`docs/UI設計メモ.md` §9-12 / 決定 D72） |
| **一覧の見た目を次に触るとき** | **U12** | ★**ライブだけに絞った一覧は、行間の空白が札の高さより広くなる**（`maxCellExtent 140` の限り常に真）。★★**退避先まで決めてある**——1 種別だけのとき `childAspectRatio` をその種別の比にする（**セル高さしか変えないので D42 は動かない**）。採らなかったのは「グリッドに寸法の前提が 2 つ生まれる」ためであって性能ではない。★**そのとき検討をやり直さないこと** | `docs/UI設計メモ.md` §10 の U12 / §9-12 |
| 要求が出たとき | **U6** | 画像の先読みを採る条件（「スクロールが止まったら 1 画面分」などの具体値） | `docs/UI技術検証メモ.md` §10-4 |
| 済 | ★**D-28** | ~~★★**「実測」と記録された値に、刻んで測ったものと 2 点だけ見たものが混ざっていた。**★★~~ → ★**2026-08-27 発見・同日規約化**。★実例は **U8 の格(2)** —— 「240 で切れる / 288 で読める」の**2 点だけ**から「288 付近」と書かれ、以後**下限として引用**されていた（288 で読めるのは本当。**下限ではなかった**）。★★**D83 とは別の型**★★ —— D83 は**測定手法の欠陥**、こちらは**手法は正しく標本が足りない**。★**§4-6「根拠の格を分ける」は根拠の種類を分けるが、「実測」の中に精度の軸が無い。**★★**規約: 実測値を正として記録するなら刻み幅・標本点・判定手段を併記する。「〜付近」は下限ではない**★★。★**既存の測定値の一斉監査はしない** —— 束 D で測り直すときに適用する（`docs/相談役への引き継ぎ.md` §5）。★**汎用の検知手段は無い**（問題は字面ではなく「引用されたこと」で、引用の側に印が残らない） | **解消**（`ルール整合性チェック_v1.06.md` D-28） |
| 済（★**A-0 を片づけた**） | ★**D-31** | ★★**走査を安全にするために狭めたことが、そのまま盲点になった。**★★ ★**2026-08-27 発見・同日記録**（束 A）。D-5 の走査を**丸括弧つき**に限ったのは **UI のペイン番号を壊さないため**で、★**その判断は正しかった**。★★**正しい安全策が、そのまま検出範囲の穴になった**★★ —— 括弧なしの旧番号が `lib` と `test` の両方に残っていた。★★**D-30 とは別の事故である**★★ —— D-30 は**除外を足した**せいで穴が空いたが、★**D-31 は除外を 1 つも足していない**（だから D-30 の回避策が効かない）。★**D-29 とも違う**（D-29 は見る対象の片側落ち / D-31 は同じ対象の中の取りこぼし）。★★**機構: 完了条件が走査で定義されている場合、走査の穴はそのまま完了条件の穴になる**★★（**D-10** の再来）。★**検知手段は部分的にしか無い** —— 走査を 1 段広げれば非 0 になるが、★**同期の設計番号と UI のペイン番号を分けられるのは人だけ**である。→ ★**「表と一致すること」を検証にせず、生の出力を残す**（**D-15 (j)**）。★★**2026-08-30: 走査を `.dart` と `.md` の両方へ広げて残務を片づけた**★★（★**受けは `loveca-ui/test/docs/legacy_design_number_test.dart`**）。★**機構そのものは残る** —— ★**完了条件が走査で定義されている限り、走査の穴は完了条件の穴になる** | **解消**（`ルール整合性チェック_v1.06.md` D-31） |
| 済 | ★**D-32** | ★★**版が上がらない限り、直したカードデータが無言で落ちる。**★★ `planUpdate` は `dataVersion` が上でなければ **manifest のファイルを 1 件も見ずに `upToDate` を返す**。→ ★**同じ版のまま `cards/*.json` を作り直しても取り込まれない**。★★**Phase 4 を待たず今日成立する**★★（dist を丸ごと差し替えても `localDataVersion` は DB 側に残る）。★**型は D-4 と同じ「痕跡を残さず更新を落とす」だが機構が違う** —— D-4 は**ファイル名が中身を名指せていない**、こちらは**ゲートが手前で切る**。→ ★**D-4 の走査では出ない**。★★**テストが守っている側である**★★（`master_data_test.dart` が「同じ `dataVersion` なら更新不要」を仕様として固定）ので、★**「テストが緑だから正しい」では出ない** → ★★**2026-08-31 解消**★★（Phase 4 のレーン 1 の commit 2 / **決定 D118-3** ＝ 版-3）。★**版ゲートの比較を「以下」から「より小さい」に変えた** —— ★**同値は通し、★降格は止める**。★★**降格の防止が「副作用」から「意図」に変わった**★★。★★**「版-5 も決まった」と読まないこと**★★ —— ★**生成側で担保する案は★待ちのままで、★版-3 と排他ではない**。★★**`Deck.masterDataVersion` への影響も消えていない**★★（★同じ版が端末ごとに違う中身を指しうる状態は★作れる）。★★**実測した対は 2 通り。★件数と内訳をここに書かない。★正は §3 の記録である**★★ | **解消**(`ルール整合性チェック_v1.06.md` D-32 / 決定 D118-3) |
| ★**A-5（D-4）を決めるとき**（★**訂正は済んでいる**） | ★**D-33** | ★★**`loveca_core` の doc が D-4 に反する安全性を主張していた。**★★ 「この名前は中身を指すので不変」「差し替え時の無効化問題が構造的に起きない」と**積極的に書いてあった**。★**D-4 に照らして偽**（`imageHash` は原本 PNG のハッシュ）。★★**2026-08-27 にコメントを訂正済み**★★ —— ★**偽を消すのに案の選択は要らない。訂正と判断は別である**。★**残るのは D-4 の案 (a)(b)(c) だけ**。★**なぜ残ったか**: `imageUrl` は `lib` から **0 件**で呼ばれていない（**D-20** の系）。★**訂正を書くときに D-30 を踏み、その規約 (a) を適用した** | `ルール整合性チェック_v1.06.md` D-33 |
| 済 | ★**D-30** | ~~★★**禁止した字面を説明する注意書きを、その字面の走査が拾った。**★★~~ → ★**2026-08-27 発見・同日規約化**（D-5 の書き換えの仕上げで踏んだ）。★★**D-10 の裏返しである**★★ —— D-10 は**除外が広すぎて**最も警戒すべき対象を隠し、こちらは**除外が狭くて**説明を拾った。★**軸は「除外の広さ」ひとつで、どちらへ倒しても別の失敗が待つ**。★★**機構: 禁止対象を説明する文書は、禁止対象と同じ字面を必ず含む。走査と説明が同じ空間にある限り、説明は必ず偽陽性になる**★★。★**回避策は在る**（退避先が走査範囲の**外**なら穴が空かない / 中なら除外＝穴になる）が、★★**「ここにこれを書くな」という局所の注意書きだけは退避できない**★★ —— 残る手 3 つと代償は参照先。★★**検知手段は無い**★★（再発と注意書きは**バイト単位で同一**。「注意書きは除外する」規則も書けない —— 名乗れば黙らせられる） | `ルール整合性チェック_v1.06.md` D-30 |
| 済 | ★**D-37** | ~~★★**走査の字面を「実際に書かれている行」から写したため、そこに入っていたローカル変数名がそのまま検出範囲になった。**★★~~ → ★**2026-08-30 発見・同日規約化**（★束 C の commit 4 の前に呼び出し元を測り直して踏んだ）。★★**同じ文書の中で 2 回起きている**★★ —— ★1 度目は `_userVersion(db)`（★予告 2 件 → 実測 3 件 / `docs/同期設計メモ.md` §17-9-1 の 5。★**そこでは番号を与えていなかった**）、★2 度目は `repository.save(`（★見積り 8 件 → 実測 25 箇所 / 同 §15-7-4）。★★**D-31 と混ぜない —— 狭める判断が 1 つも無い**★★（★D-31 の規約「絞った理由と一緒に、絞ったことで何が範囲外になったかを書く」は**絞ったと自覚した瞬間**を前提にしており、★**この型にはその瞬間が無いので 1 文字も動かせない**）。★★**D-10 とも違う** —— 0 件ではなく「もっともらしい非 0」が返る★★ので、★**仕込んだ 1 件が写した綴りと同じなら D-10 の受けも通ってしまう**。★★**検知手段は在る。★しかも機械にできる**★★（★走査の字面に対象と無関係な識別子が入っていないかを見て、★**外して走り直し件数が変わらないことを確かめる**）。★**ただし万能ではない** —— ★メソッド名だけに縮めると同名の別クラスを拾い、★**型で絞る前置を足すと D-31 に戻る**（★同じ軸の両端。★片方だけを選べない） | **解消**（`ルール整合性チェック_v1.06.md` D-37） |
| 済 | ★**D-38** | ★★**doc に写した実装のコード片のエスケープが解釈され、制御文字が入って表示経路がそこから先を落とした。**★★ ★**2026-08-31 発見・同日規約化**（★渡した文書が 3 回とも同じ位置で切れた）。★**実装の側は正しい** —— ★ソースの上では**エスケープの数文字**だが、★**写した先の書き込み経路がそれを解釈して制御文字 1 バイトになった**。★★**書いた本人の手元では完全に見える。★落ちるのは受け取る側だけである**★★。★★**D-30 / D-31 / D-37 のどれとも混ぜない**★★ —— ★**D-30 は走査が説明を拾う話で、★こちらは走査が届く★前に文字が消えている**（★**内訳は参照先**）。★★**検知手段は「無い」ではなく「化ける」**★★ —— ★**`analyze` にも `git status` にも出ない**が、★**`git diff` は「binary だ」と言って差分そのものを消し**、★★**`grep` は「Binary file … matches」に化けて★行を出さないまま終了コード 0 を返す**★★（★**D-10 の裏返し** —— ★あちらは「0 件が 2 つの意味を持つ」、★こちらは「**非 0 が中身を持たない**」）。★**規約は 3 つ**（★**内訳は参照先**）。★**テストで見張る道は在るが、この回では足していない** | **解消**（`ルール整合性チェック_v1.06.md` D-38） |
| 済 | ★**D-36** | ~~★★**コミット本文は「その commit の時点」でしか正しくなく、あとから訂正できない。**★★~~ → ★**2026-08-29 発見・同日規約化**（★未 commit の作業を時系列で畳んだ `6e0ba22`〜`7f4a5f5` で踏んだ。★**7 件とも 23:23〜23:24**）。★実例は 2 つ —— ★**commit 3 / 4 が指す「検出層が止まった回数」と「穴の数」を、commit 5 / 7 が覆した** → ★**その 1 件だけを見た人は「穴は 2 つ」「止まったのは 1〜2 回」と読む**。★★**畳んだことの副作用ではない**★★ —— ★**時系列で積む以上どうやっても起きる**（★一括の 1 commit なら起きないが★論点が混ざる / `CLAUDE.md` §7-4 / ★同型の却下は `決定 D108-6`）。★★**D-15 (l) と向きが違う**★★ —— ★**あちらは doc が実装に追随しなかった（★追随できたのにしなかった＝直せる）。★こちらは履歴が原理的に追随できない**（★書き換えは止まる条件 3 / ★`git notes` はこの運用に入っていない）。→ ★**「直す」という手当てが存在しない**。★★**規約は 3 つ**★★（★**内訳は参照先**）。★★**汎用の検知手段は無い**★★（★古くなったことが字面に出ない —— ★**本文はいまも、その commit については正しい**） | **解消**（`ルール整合性チェック_v1.06.md` D-36） |
| 済 | ★**D-35** | ~~★★**比較表の欄が、宣言した物差しの語で揃っていなかったため、字面での抽出が 1 案を落とした。**★★~~ → ★**2026-08-29 発見・同日規約化**（`決定 D108` の検算で踏んだ）。★実例は `docs/同期設計メモ.md` §13-2 —— ★**「物差しは 3 つ」と宣言した表の 7 案のうち、★案 6 だけが別の軸の語（「最も精度が高い」）で書かれ、★「満たせる」の字面で横に抜いたときに落ちた**。★★**書いた人に落ち度は無い**★★ —— ★**案 6 にとってはその語が最も正確で、書いた時点で誤りではない。★壊れるのは「あとから横に読んだとき」だけである**（★**D-15 (l) に似ているが★何も動いていない。動いたのは読み方である**）。★★**規約: 案の一覧から「◯◯を満たす案」を抜いたら、抜いた結果ではなく★全欄の字面を並べて示す**★★（★**型は D-15 (j) / D-31 と同じ「生の出力を残す」**）。★★**汎用の検知手段は無い**★★（★語が揃っているかは意味の問題で、★**`.md` を見る機械はこのリポジトリに 1 つも無い** / `CLAUDE.md` §8）。★**D-10 / D-15 / D-27 / D-30 / D-31 のどれとも混ぜない**（★**内訳は参照先**） | **解消**（`ルール整合性チェック_v1.06.md` D-35 / 決定 D108-4） |
| 済 | ★**D-27** | ~~★★**対を置いても、その対が対象を見ていないことがある。**★★~~ → ★**2026-08-27 発見・同日解消**（束 B の自己検査）。★**§4-5（陽性対照）の一段先** —— 「出る側だけのテストは何も証明しない」の**次**にある問いで、★**置いた対そのものが対象を見ているか**は誰も言っていなかった。★実例では対が掴んでいたのが**区分の見出し**（「表示できないカード」）で、**縮退の描画を丸ごと殺しても通っていた**。★★**規約: 対を置いたら、対象を壊して対が落ちることを実測する**★★。★**汎用の検知手段は無い**（`findsOneWidget` は「誰が出したか」を見ない。★**部分一致はとくに危うい**）。★**D-10 の系**（陽性対照そのものが陽性対照を要る。★**入れ子は 1 段で止まる**） | **解消**（`ルール整合性チェック_v1.06.md` D-27） |
| 済 | ★**D-25** | ~~★★**§3 の見張りが `.claude/` を構造的に見ていない。**★★~~ → ★**2026-08-27 発見・同日解消。**除外の `\.claude/` を `\.claude/settings\.local\.json` に狭めた。★★**型は D-10 ではない。**★★ D-10 は `.gitignore` の未アンカー規則、§3 の「最初の版が `(^\|/)build/` だった」は**形式**の誤りだが、こちらは**形式は正しく範囲が古くなった**もので **D-15 (l)**（前提が動いた）である。★**書いた時点（2026-08-24 / `676a69c`）では正しく、`.claude/commands/bundle-b.md` が入った 2026-08-27（`69a43f5`）に誤りになった。**★**陽性対照つき** —— 一時的に `/.claude/commands/` を ignore に足し、★**新規の未追跡ファイル**を置いて旧 grep が見逃し新 grep が検知することを実測した（`settings.local.json` は前後どちらでも除外されるのでテストにならない）。★**`/.claude/worktrees/` は当初「残る穴」として除外しなかった**が、★★**2026-08-27 に相談役の判断で除外へ回した**★★（見張りの契約は「0 件であること」で、非 0 が常態になると本物が埋もれる）。★**仮定も §3 に書いた。**★**陽性対照も対で取り直した** | **解消**（`ルール整合性チェック_v1.06.md` D-25） |
| 済 | **D-10** | `.gitignore` の未アンカー規則が本実装を隠していた件。規則のアンカー化（本命）と §3 の見張り（受け）で**両方適用済み** | **解消**（`ルール整合性チェック_v1.06.md` D-10） |
| 済 | B-1 / B-3 | フェイズ構成（リーフフェイズ **12 個**）/ 重ね置きの解消先 | **確定** |

★**D-5 は Phase 4 の「着手前」である。** 同期の設計を始めてからでは、
参照の書き換えが同期の実装と混ざる。

B-1（フェイズ構成）と B-3（重ね置きの解消先）は**確定済み**。根拠と設計は
`docs/PhaseEngine設計メモ.md` を参照する。
★**B-2（エネルギーデッキの順序）と B-4（ポジション / フォーメーションチェンジ）も確定済み**で、
根拠と設計は `docs/盤面設計メモ.md`（決定 D73 / D74）にある。
**B 節は 4 つとも決着した。**
B-1 / B-3 の要点だけ再掲する。

- **リーフフェイズは 12 個**（7.1.2 / 7.3.3 / 8.1.2）。★**過去に書いていた「13 フェイズ」は誤記**
- 8.4.13（先攻入れ替え）と 8.4.14（ターン終了）は**ライブ勝敗判定フェイズ 8.4 の内部手順**であり、
  フェイズではない。フェイズの下に**条番号をそのまま ID とするステップ層**を持つ
- フェイズはロール（先攻/後攻）で定義し、実プレイヤーは `firstPlayerId` から解決する。
  8.4.13 で入れ替わるため、**フェイズに実プレイヤー ID を埋めない**

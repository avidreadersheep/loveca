# プラットフォームを足す手順（`loveca-ui`）

作成日: 2026-09-02（★**運転指示【0】(6) の 6 —— ★★手順の在る場所に置くこと★★**）

---

## 0. ★ なぜこの文書が在るか

★★**`flutter create` は★既に在るものを★黙って書き換える。**★★
★**次に踏む人が★★事前に読む場所★★が要る**（★`CLAUDE.md` の注記では★★足したあとにしか読まれない★★）。

★★**相談役が「★★`CLAUDE.md` の注記では次に踏む人が事前に読まない。★手順の在る場所（Android / iOS を足す手順の隣）に置くこと★★」と述べた分である。**★★
★**同じ判断を【0】(0) が★報告の規約についても述べており、★★同じ型の処置である★★**（`docs/相談役への報告/README.md` の 0 節）。

★★**この文書は★中身を持たない索引ではない。★★手順そのものである★★。**★★
★**ただし★★決定と測定値は持たない★★** —— ★正は `docs/決定事項一覧.md` と `CLAUDE.md` §3。

---

## 1. ★ 手順

```bash
cd loveca-ui
flutter create --platforms=<ios|macos|linux|web> --project-name=loveca_ui .
```

★**`--project-name=loveca_ui` を★★必ず付ける★★**（★付けないと★ディレクトリ名から推測される）。

---

## 2. ★★ 足したあとに★必ず確かめる 6 つ ★★

| # | 何 | ★どうやって | ★なぜ |
|---|---|---|---|
| ★★**1**★★ | ★★**`.metadata` の★★既存のプラットフォームの行が消えていないか**★★ | ★`git diff loveca-ui/.metadata` | ★★**下の 3 節。★実際に踏んだ**★★ |
| ★**2** | ★**雛形の検査が生えていないか** | ★`git status` に `test/widget_test.dart` が出るか | ★★**`MyApp` を参照するので★コンパイルも通らない**★★（★Android で実際に生えた） |
| ★**3** | ★**`windows/` と `pubspec.yaml` が★動いていないか** | ★`git diff --stat` | ★**既存のビルドを壊さない**（★運転指示【5】） |
| ★**4** | ★**`CLAUDE.md` §3 の見張りが★0 件のままか** | ★§3 の `git ls-files --others --ignored` のコマンド | ★★**生成された `.gitignore` が★ソースを飲み込みうる**★★（**D-10**） |
| ★**5** | ★**既存のビルドが★通ったままか** | ★`flutter build windows --debug` | ★同 3 |
| ★**6** | ★**`flutter test` / `flutter analyze`** | ★`CLAUDE.md` §3 | ★**件数表を直す**（★あれが正である） |

---

## 3. ★★ 踏んだこと 1 —— ★`.metadata` の既存の行が★★置き換えられる★★ ★★

★★**実測**★★（2026-09-01 / ★`--platforms=android` を足したとき）——
★**`migration.platforms` の `windows` の行が★★`android` に置き換えられた★★。★手で戻した。**

★★**戻さないと何が起きるか**★★ —— ★**次に `windows` の移行（`flutter migrate`）を掛ける手段が消える。**

★★**次に `--platforms=ios` を足す人も★同じように踏む**★★（★★Flutter 側の挙動であり、★こちらに手当てが無い★★）。

★**いまの `.metadata` は★`root` / `android` / `windows` の 3 段を持つ**（★実読 / 2026-09-02）。
→ ★**足したあと★★段が減っていないこと★★を `git diff` で見る。**

★★**「Flutter の不具合である」とは書かない**★★ —— ★**そう判定していない**（★★測ったのは★行が消えたことだけである★★ / **D-28**）。

---

## 4. ★★ 踏んだこと 2 —— ★`applicationId` が `com.example.…` のまま生成される ★★

★**Android で実際にそうなった**（`loveca-ui/android/app/build.gradle.kts`）。
★★**配布するときに要る**★★（★Google Play は `com.example` を受けない）。
★★**今日は何も塞いでいない**★★ —— ★配布は待ち行列に 1 行も無い。

★**正は `docs/作業待ち行列.md` の **W-33**（★★利用者判断 —— ★アプリの同定は好みで決まる★★）。**
★★**iOS を足すときは★同じものが `PRODUCT_BUNDLE_IDENTIFIER` として生える**★★（★★未確認 —— ★足していない★★ / **D-28**）。

---

## 5. ★ この文書が覆わないもの

| # | 何 | ★正はどこか |
|---|---|---|
| ★**1** | ★**iOS を足すかどうか** | ★★**`決定 D134-1`**★★（★★現時点では対応しない★★） |
| ★**2** | ★**ビルドと計測のコマンド** | `CLAUDE.md` §3 |
| ★**3** | ★**モバイルの未了項目** | `docs/UI設計メモ.md` §7 |
| ★**4** | ★**件数** | `CLAUDE.md` §3 の表 |

---

## 6. ★★ 2026-09-02 追記: ★★端末に dist を置く（★開発用。★引き金を持たない）★★

★★**アプリのコードを 1 行も要らない**★★ —— ★**読む段は★既に 2 つ在る**
（★段 1 ＝ 環境変数 ／ ★★段 2 ＝ `settings.json` の `distDir`（**D60** —— ★「★手でも置ける」と自ら書いている）★★）。
★**試作は `--dart-define=LOVECA_DIST_DIR=…` で直に指せる。**

★★**手順の正は `docs/UI技術検証メモ.md` §3-3-2 の「置く経路」である**★★（**D-15** —— ★ここに写さない）。
★**この文書に置くのは★★踏んだこと 1 つだけである★★。**

### ★★ 踏んだこと 3 —— ★`/sdcard/Android/data/<pkg>/files/` は★アプリから読めない ★★

★**`adb push` は★★通る★★**（★shell は `ext_data_rw` に属する）。
★**しかしアプリが読むと★★`Permission denied`（errno = 13）★★**（★2026-09-02 実測 / ★API 36）。
→ ★**アプリ専有の内部領域（`/data/data/<pkg>/files/`）へ★★`run-as` で写す★★。**
★**`adb root` は使えない**（★`adbd cannot run as root in production builds` / ★実測）。

★★**「置けない」と読まないこと**★★ —— ★**置ける。★★置き場が違うだけである★★。**

# 束 B（`loveca_db`）— 作業報告

日付: 2026-08-27。ブランチ `feature/phase3b-board`。
対象: **U22 / D-11 / U10 / D65 の `ord` 移行 + D-8 + D-13**。

★**この文書は報告であって正ではない。**決定の正は `docs/決定事項一覧.md`、
所見の正は `ルール整合性チェック_v1.06.md`、件数の正は `CLAUDE.md` §3 の表。
食い違ったらそちらを優先する。

---

## 0. 束 B の前に片づけた 2 件

★**指示の順序どおり、束 B の作業とは別のコミットにしてある。**

| # | コミット | 論点 |
|---|---|---|
| 0-1 | `4f8615d` | §3 の見張りの除外を `.claude/` から `settings.local.json` へ狭める（★**新所見 D-25**） |
| 0-2 | `fcd04d4` | `docs/相談役への引き継ぎ.md` を追跡下に入れる（内容は変更していない） |

### 0-1 の要点

★**型は D-10 ではなく D-15 (l)。**除外は `^\.claude/` と**パスで書かれており**、
§3 自身の「パスで書くこと」の規約は守っている。**壊れたのは形式ではなく範囲**である。
書いた時点（2026-08-24 / `676a69c`）では `.claude/` に本実装に効くものが無く**正しかった**。
`.claude/commands/bundle-b.md` が入った 2026-08-27（`69a43f5`）に**前提のほうが動いた。**

★**陽性対照を置いた。**`settings.local.json` は狭める前後どちらでも除外されるので
テストにならない。§3 自身の「動作確認は新規の未追跡ファイルで行うこと」に従い、
一時的に `/.claude/commands/` を ignore に足し、**新規の未追跡ファイル**を置いた。

| | 結果 |
|---|---|
| 旧（`\.claude/`） | ★**0 件。見逃す** |
| 新（`\.claude/settings\.local\.json`） | ★**検知した** |

---

## 1. コミットごとの 1 論点

| # | コミット | 論点 |
|---|---|---|
| 1 | `b72ad63` | **doc のみ。** U22 を決定 **D99** として記録 + D65 の backfill の基準行・SQL 行を差し替え + D65 に 1 行追記 + `deck_repository.dart` の doc を参照に置換 |
| — | `06b8983` | `/bundle-b` の「初回のみ」の節を D99 への参照 1 行に置き換える（★節そのものが「commit 1 が終わったら削除」と指示していた） |
| 2 | `e46139f` | 比較器を `loveca_core` に置く（`deck_order.dart` + `deck_order_test.dart`） |
| 3 | `d0e21df` | `schemaVersion` 2 → 3（`ord` 列 / Dart の backfill / `save` が添字を書く / `byId` と `all` の両方に `ORDER BY ord`）+ `migration_test` |
| 4 | `ef6333c` | 画面を `ord` に切り替え（`normalizedEntries` / `DeckOrderNotPersisted` / `isReordered` 撤去 / `isDirtyAgainst` / 規則順に戻す / 挿入位置） |
| 5 | `db632f2` | **D-8** の前提の測り直し（★**実装は 1 行も変えていない**） |
| 6 | `09c1054` | **D-13** の根治（方式決定済み。実装のみ） |

---

## 2. ★ 止めずに進めた判断

★**`CLAUDE.md` §7 の 6 条件のいずれにも当たらないと判断したもの。**
★**当たると判断して止めたものは無い**（＝ 6 条件に触れる場面が出なかった）。

### (a) commit 3 に `loveca-ui` のテスト 1 件の差し替えを含めた

★**「1 コミット = 1 論点」との兼ね合いで迷いうる点なので書く。**

`deck_repository_test.dart` の
「並べ替えて保存しても、開き直すとカード番号順に戻る」は
**DB の挙動（D65 の事実）を固定していたテスト**である。
commit 3 が `ord` を入れた瞬間に**このテストだけが落ちる**ので、
commit 3 の中で向きを逆にした（「並べ替えて保存すると、開き直しても並びが残る」）。

★**画面側の撤去（`normalizedEntries` など）は commit 4 に残してある。**
★理由: commit 3 の論点は「DB が並びを保存するようになったこと」であり、
このテストはまさにそれを見ている。**別コミットにすると commit 3 が赤いまま残る。**

### (b) commit 4 で D-11 / U10 を「解消」として記録した

★**厳密には D-11 の実体は commit 3 で解消している。**
commit 4 に置いたのは、**D-11 の当座の手当て（画面側の 4 つ）が消えるのが commit 4** だからである。
★D-11 の行に「画面側の手当ても撤去した」と書いてあるので、
**commit 3 の時点で「解消」と書くと、その時点では嘘になる。**

### (c) commit 6 で `ImportIssue.supersededByNewerFile` を撤去した

D-13 の方式表は「不要になる（★**消してよい。残っていても害はない**）」と書いていた。
★**「害はない」は誤りだった。**実装して初めて分かった。

根治後に `currentHash != hash` が成り立つのは
★**「古い版が取り込まれていて、新しい版で失敗した」**ときだけである。
そこに「このファイルは、そのあと**別の版で取り込めています**」と出すと**向きが逆**になる。
→ ★**死んだだけでなく意味が反転していたので撤去した。**
★`currentHash` 自体は「いま取り込まれている版」という手がかりなので**残した**。

★**これは「D 番号の記述を変える」（§7 の条件 4）に当たるか迷った。**
判断: **当たらない。**D-13 の**方針（案 (a)）は 1 文字も変えていない**。
変えたのは「UI 側」の欄の**付随的な見込み**であり、
★**その見込みが外れたこと自体を D-13 に追記して残した**（消していない）。

### (d) 挿入位置のテストを `test/state/deck_edit_store_test.dart` に新設した

`deck_repository_test.dart` に置くと `DeckEditStore` を import することになり、
★**そのファイルの doc が「役割を混ぜない」と明言している**のに反する。
→ **`state/` に新しいファイルを作った。**

### (e) `migration_test.dart` の巻き戻し手法を拡張した

★**これは判断というより、既存の doc の指示に従っただけである。**
`_rewindToV1` の doc が
「★**次に実テーブルを動かす移行を入れたら、この手法は使えなくなる。
そのときは v1 の実テーブル定義そのものを凍結して持つ必要がある。
この注記を読まずに流用しないこと。**」と書いていた。
**その「そのとき」が来た**ので、`_rewindDeckEntriesToV2` を足し、
v2 の `deck_entries` の DDL を**凍結**した。

★**`ALTER TABLE ... DROP COLUMN` は使わなかった**（SQLite 3.53.4 なら使える）。
★理由: それは**現在の形から 1 列を引く**書き方で、
**将来 v4 が別の列を足したときその列は残ったまま**になる。
凍結した DDL で建て直せば、残るのは常に「凍結した v2 の形」である。

---

## 3. ★ 測った値と、その測定が実行可能だったか

★**実行できなかったものは「実行不能」と書く。推測で埋めない**（**D-15 (k)**）。

### 3-1. 陽性対照（★すべて実行できた）

| # | 何を壊したか | 落ちた件数 | 実行可能性 |
|---|---|---|---|
| 1 | `compareDeckOrder` を `return 0`（no-op）にする | ★**26 件中 21 件** | 実行した |
| 2 | フィクスチャの投入順を期待順と同じにする | ★**`_expectScrambled` が落ちる** | 実行した |
| 3 | `onUpgrade` の `backfillOrd()` を外す | ★**`migration_test` 22 件中 6 件** | 実行した |
| 4 | ★**`all` の `ORDER BY` だけ**を落とす | ★**「経路差」の 1 件だけ** | 実行した |
| 5 | `addCard` の挿入位置の計算を外して末尾に足す | ★**2 件** | 実行した |
| 6 | `isDirtyAgainst` を `Map` 比較（D65 の形）に戻す | ★**4 件** | 実行した |
| 7 | 開いた直後に並べ直す（撤去した `normalizedEntries` の代わり） | ★**3 件** | 実行した |
| 8 | `recordFile` の `DELETE FROM import_issues` を外す | ★**2 件** | 実行した |

★**4 が重要である。**「`all` と `byId` が一致する」だけを見ると、
**両方壊れていても通る。**片方だけ壊して**1 件だけ落ちる**ことを確かめた。

### 3-2. D-8 の前提（★実行できた）

★**結論ではなく条件を測った**（**D-15 (l)**）。

★★**先に計数が機能することを確かめた**★★（**D-10**: 0 件は「無い」と「見えていない」の
区別がつかない）。実データの dist（22 商品）を取り込んだ DB で:

| | 値 |
|---|---|
| `cards` | **1,708** |
| 孤児（本番相当） | **0** |
| ★`deleteExpansion('BP01')` の直後 | ★**88** |

判定基準そのもの（★**見るべきは手元の DB ではなく配信側の履歴**）:

| 見たもの | 値 |
|---|---|
| `manifest.json` のファイル数 | **25**（うち商品 **22**）。2026-08-24 の記録と同じ |
| `normalized` と `dist` の商品集合の差 | ★**両向きとも空集合** |
| `manifest.json` の更新 | **2026-08-23 00:05**（★D-8 を書いた日より前） |
| `normalized.json` の更新 | **2026-08-10 10:25**（同上） |

→ ★**dist は D-8 を書いてから一度も作り直されていない。**削除が起きる余地が無い。
→ **掃除経路は作らない。実装は 1 行も変えていない。**

★**測定手段はリポジトリに残していない。**実データの dist は git 管理外なので、
テストにすると**未配置の環境で必ず skip になり**、
「検証しているつもりで検証していない」状態を増やす。

### 3-3. 実データで `cost` / `score` を引けない札（★止まる条件だった。★該当なし）

指示の止まる条件に「backfill 時にマスタから `cost` / `score` を引けない札が
実データに存在した場合、件数と内訳を報告してから進む」とあった。

★**該当しなかった。**`docs/UI設計メモ.md` §12-2 の事実 2（メンバー 916 種で
`cost == null` が 0 件 / ライブ 225 種で `score == null` が 0 件）は今も当たる。
★**それでも null を末尾に落とす規則は実装してある**（決定 D99）——
`Card.cost` / `Card.score` は `int?` であり、**新商品で現れたときに黙って先頭へ来る**のは事故だから。

★**「引けない札」がもう 1 種類ある**ことに注意 —— **マスタに無い刷り**（決定 D35）。
これは実データの話ではなく**デッキ側の話**で、
`migration_test` の「マスタが空でも落ちない」と「マスタに無い刷りも消えない」が固定している。

### 3-4. テスト件数（★4 パッケージとも実測した）

| パッケージ | 束 B の前 | 束 B の後 |
|---|---|---|
| `loveca-data` | 33 | **33**（触っていない） |
| `loveca-core` | 385 | **411** |
| `loveca-db` | 127（skip 0） | **142**（★**skip 0**） |
| `loveca-ui` | 861 | **874** |

★`loveca-ui` が **875 → 874** に**減った**のは commit 6 —— D-13 の当座の手当ての
「出る側」と「出ない側」の 2 件を、**その状態そのものが作れなくなった**ので 1 件に置き換えた。

★`loveca-db` の skip 0 は**実データが在る状態で実測した**（`loveca-data/data/dist/` 配置済み）。

### 3-5. ★★ U8 が動いた —— 198 → 288 論理px ★★

★**報告を書きながら「測っていない」と書いたので、測った。**

| いつ | 値 | 何が起きたか |
|---|---:|---|
| M4（2026-08-24） | 151 | ★**誤り**（二分探索が溢れを取りこぼしていた） |
| M-B2（2026-08-25 / **D83**） | **198** | ★**測定手法の訂正。**判断ではなく数値が変わった |
| ★**束 B（2026-08-27）** | ★**288** | ★★**実装が変わった。**★★「規則順に戻す」のボタン 1 つぶん |

★★**原因を切り分けてある**★★ —— ボタンを外して測り直すと**198 に戻る**ことを実測した。
★**測定手法の訂正（D83）と混ぜないこと。**あちらは数値の訂正、こちらは実装の変化である。

★★**採用値 320 の余裕が 122 → 32 に減った。**★★
★**回帰ガードは通っている**（288 ≤ 320）ので **決定 D61 の結論は変わらない**
（一覧 3 列 444 + 仕切り 1 + 320 = 765 ≤ 840）。

★**(1) と (2) が同じ 288 になったのは偶然である。**格が違う
（(1) はテスト用フォント / (2) は実機の実データ）。**同じ数だからと束ねてはいけない。**

★★**実機の下限は測り直していない。**★★ テスト用フォントは日本語の実幅ではないので、
**実機の下限は 288 より広い可能性がある。**
→ ★**§9-7 とテストファイルの doc の両方に、次の一手を書いた。**

### 3-6. 実行不能だったもの / やっていないもの

★**実機（`flutter run -d windows`）での確認は行っていない。**
★**「実行不能」ではなく「やっていない」である。**区別して書く。
束 B の指示に実機確認の項目が無く、変更の中心が DB 層とその移行だからだが、
★**画面に見える変更が 2 つある**ので、次に実機を触る人は見てほしい:

1. ★**「規則順に戻す」ボタン**（デッキ編集ペインのヘッダ、メモ欄の下）
2. ★**並べ替えると保存ボタンが光るようになった**（以前は光らなかった）

★★**幅は測った（上の 3-5）。**★★ 実機で見ていないのは**折り返しと可読性**である。

---

## 4. ★ 新所見（この束で見つけて記録したもの）

| # | 中身 | 状態 |
|---|---|---|
| **D-25** | §3 の見張りが `.claude/` を構造的に見ていない | ★**解消**（束 B の前） |
| **D-26** | `ord` の backfill が `revision` を上げず、マスタに依存する | ★**いま直さない**。Phase 4 の束 A |

### D-26 の要点

`onUpgrade` の backfill は `DeckDao.save` を通らないので **`revision` を上げない。**
かつ規則順は `cost` / `score` を引くので**マスタに依存する** ——
マスタ未取り込みの端末では `printingId` 昇順だけになり、
★**同じ `revision` で中身が違う**状態ができる。

★**壊れはしない**（null を末尾に落とす規則があるので全順序は決まる）。
★**症状は「端末間で並びがずれる」であって、データの破損ではない。**

★**いま直さない理由**: **D-9**（`softDelete` が `revision` を上げない）と
★**同じ問い**（`save` を通らない書き込みで `revision` をどう扱うか）である。
片方だけ決めると規則が 2 つになる。→ **Phase 4 の束 A で D-9 / D-5 / D-14 と一緒に判断する。**

★**型は D-15 (h)**（doc が実装より広い。D65 の「`ord` はデッキの中身なので `revision` に乗る」に
限定が無く、移行経路を含めて読める）。★**走査では出ない。**

---

## 5. ★ 相談役に見てほしい点

★**「無ければ無しと書く」——無しではない。3 つある。**

### (1) ★★ 「規則順に戻す」の置き場 —— 幅の余裕が 122 → 32 に減った ★★

★**自分で不安が残ったのはここである。**

決定 D99 は「入口 1 つ・2 段以内」としか定めていない。
実装は**デッキ編集ペインのヘッダに `TextButton.icon`（ラベル「規則順に戻す」）を常時出す**とした。

| 選んだ理由 | |
|---|---|
| アイコンだけにしなかった | ★`Tooltip` はマウスを乗せないと出ない（**決定 D90-3**）。**U27**（手がかりが無い）を繰り返さない |
| メニューに入れなかった | 2 段になる。D99 は「2 段以内」なので許されるが、1 段で足りるなら 1 段 |

★★**測った（3-5）。198 → 288 論理px。**★★ ボタンを外すと 198 に戻ることも確かめてある。
★**回帰ガードは通っており D61 の結論は変わらない**が、**採用値 320 までの余裕が 32 しか無い。**

★★**見てほしいのはここ**★★ —— ★**実機の下限は測り直していない。**
テスト用フォントは日本語の実幅ではないので、実機ではもっと広い可能性がある。
★**U26**（袖の「エネルギーを1枚出す」が語の途中で折り返す）と**同じ型**の問題が
ここでも起こりうる —— 「規則順に戻す」は 6 文字あり、袖の文言より短いだけである。

★**逃げ道は用意してある**（どれも D99 の「入口 1 つ・2 段以内」を満たす）——
(a) ラベルを短くする / (b) 行を分ける / (c) 幅が足りないときだけアイコンに落とす。
★**(c) は「Tooltip はマウスを乗せないと出ない」（D90-3）に触れる**ので、
**採るなら U27 の反省とどう両立させるかを決めてから**にしたい。

### (2) ★ 挿入位置の定義が「手動順が混ざったデッキでは正しくない」ことの見せ方

D99 は「比較器で自分より後ろに来る最初の札の直前」と定義し、
★**手動順が混ざったデッキでは規則順の正しい位置とは限らない**と明記している。

★**その性質をテストで固定してある**（`deck_order_test.dart` の
「★手動順が混ざったリストでは『規則順の正しい位置』とは限らない」）。

★**利用者にはこれを何も言っていない。**
★**言うべきかどうかの判断を求めたい。**
言わない側の根拠: 「規則順に戻す」を押せば直るので、**行き止まりではない**。
言う側の根拠: `DeckOrderNotPersisted` を撤去した以上、
**並びについて画面が何も言わない状態**になった。

### (3) ★ `migration_test` の巻き戻し手法が「差の列挙」に依っている

`_rewindDeckEntriesToV2` を足したことで、この手法は
★**「v1 / v2 と v3 の実テーブルの差が、巻き戻している分に尽きること」**に依るようになった。

★**注記は書いた**（「次に実テーブルを動かす移行を入れたら、また巻き戻しを足すこと」）。
★**しかし、これは手で守る規約である。**
足し忘れると `ALTER TABLE ... ADD COLUMN` が
**「duplicate column name」で落ちる**か、より悪い場合は**黙って通って移行を一度も検証しない。**

★**機械で見張る手が思いつかなかった。**
drift の `SchemaVerifier` は `card_search`（仮想テーブル）を知らないので使えない、
というのが D-6 の解消時の記録である。
→ ★**「落ちるほうに倒れる」設計にできないか**が論点だと思うが、自信が無い。

---

## 5-2. ★★ 提出前の自己検査（read-only の subagent）★★

★**実装した本体に自己採点させない**ため、read-only のエージェントに
**機械的に確かめられる 8 項目だけ**を投げた（設計判断のレビューではない）。

| # | 項目 | 結果 |
|---|---|---|
| 1 | 1 コミット = 1 論点 | ★**OK** |
| 2 | 陽性対照が対になっているか | ★★**問題あり 1 件**★★ |
| 3 | doc に機械が数えられる数を書いていないか | ★★**問題あり 3 か所**★★ |
| 4 | 同じ内容を 2 箇所に書いていないか | ★★**問題あり 2 か所**★★ |
| 5 | 決定番号の参照先が実在するか | ★**OK**（D99 / D-25 / D-26 とも実在。次は D100） |
| 6 | ルールに触れるコードの条番号 | ★★**問題あり 1 件**★★ |
| 7 | `loveca_core` の禁止依存 | ★**新しい違反は 0**（既知の D-14 が 1 件残るのみ） |
| 8 | 件数表が実測と合っているか | ★**表は OK / 内訳に問題あり 2 か所** |

★★**指摘は 5 件とも自分で再現してから直した。**★★
★**エージェントの報告をそのまま信じない**（**D-10** と同じ構え —— 検知手段自身が誤りうる）。

### ★★ 直したもの（2 コミット）★★

| コミット | 中身 |
|---|---|
| `8485900` | ★**陽性対照が対になっていなかった**（項目 2） |
| `69ec54c` | ★**doc の食い違い 4 件**（項目 3 / 4 / 6 / 8） |

### ★★ 項目 2 —— 直す途中でもう 1 段踏んだ ★★

★**これがこの検査でいちばん価値のあった発見である。**

`deck_edit_page_test.dart` の「並べ替えの予告はもう出ない」は
**`findsNothing` だけで構成されており、陽性対照が 0 本**だった。
★コメントは「**★対: 別の縮退はまだ出る**」と言っているのに `findsNothing` を見ており、
フィクスチャに未知の刷りが無いので**通っていた。**

★★**1 回目の直しも間違っていた。**★★
未知の刷りを混ぜて `find.textContaining('表示できないカード')` を対にしたが、
★**それは区分の見出し（`_section`）であって縮退の帯ではない。**
縮退の描画を `SizedBox.shrink()` に置き換えて測ると、**その対は通ったまま**だった。
→ `_DeckDegradationLine` が出す文言（「カードデータが未取得の刷りが」）に変え、
**殺すと落ちる**ことを実測した（直す前は 1 件、直したあとは 2 件が落ちる）。

★**教訓**: 「対を置いた」だけでは足りない。**その対が本当に対象を見ているか**を、
**対象を壊して確かめる**まではただの飾りである。

### ★ 項目 8 —— 合計が合っていたので気づかなかった

`loveca-ui` の +14 の内訳を「9 / +4 / +1」と書いていたが、実測は「**10 / +3 / +1**」。
★**9+4+1 も 10+3+1 も 14 になる**ので、合計を見ても検出できない。
→ ★**内訳は合計が合っていても間違いうる。**

### ★ 確かめられなかったと報告されたもの（★正直に残す）

- ★**「落ちる件数」の分子**（no-op で 21 件 など）は**実装を壊す必要がある**ので
  read-only では検証できない。★**分母（26 / 22）は検証済み**。
- ★**条番号の正しさ**は**総合ルール PDF が git 管理外**なので条文原文と突き合わせていない。
  ★項目 6 で直した 2.2.2 も、**`CardType` の doc との整合**を根拠にしている。

---

## 6. ★ この束に含めなかったもの

★**指示どおり `U21`（詳細検索）は含めていない。**
論点 4 つを持つ大物で、索引追加で版がまた上がりうるため、D65 の移行と別の版にする。

★**`U22` の「規則順に戻す」以外の UI の作り込みも入れていない**
（並び順の切替 UI・区分ごとの折りたたみなど）。D99 が定めていないので範囲外。

# 2026-08-27 束 A —— 論点の洗い出し（★決定は行っていない）

★★**この文書は「走査の生の出力」を残すための記録である。**★★
★**論点の正は `docs/同期設計メモ.md`**。★**ここに判断を書かない。**

★**束 B の記録（`docs/セッション報告/2026-08-27_束B.md`）と同じ扱い** ——
台帳（`docs/同期設計メモ.md` / `ルール整合性チェック_v1.06.md`）は**数と生データを写さず**、
必要ならこちらを指す。

---

## 1. このセッションでやったこと

| # | 成果物 |
|---|---|
| 1 | `docs/同期設計メモ.md` を新設（束 A の論点表。★**方式は 1 つも決めていない**） |
| 2 | `ルール整合性チェック_v1.06.md` に **D-31** を新設 |
| 3 | ★**D-5 の偽の完了報告を未完了に戻した**（文書 6 か所。★**一部だけ直さない**） |
| 4 | `CLAUDE.md` §8 の索引を更新（★**束 A の内訳をここに書かない**形にした） |

★★**コードは 1 行も変えていない。**★★

---

## 2. ★ 走査 A —— 丸括弧つき（D-5 の完了条件に使われたもの）

```bash
grep -rnE '[（(]P[1-5][）)]' --include=*.dart loveca-*/lib | grep -v '\.g\.dart'
```

★**出力: 0 件**（2026-08-27 / 実行して確認）。

→ ★★**この 0 件が「完了」と読まれたのが D-31 である。**★★
走査 B が示すとおり、**対象は消えていない。**

---

## 3. ★★ 走査 B —— 括弧なしまで広げたもの（生の出力）★★

```bash
grep -rnE '(^|[^A-Za-z0-9_])P[1-5]([^0-9A-Za-z_]|$)' --include=*.dart \
  loveca-core/lib loveca-db/lib loveca-ui/lib \
  loveca-core/test loveca-db/test loveca-ui/test | grep -v '\.g\.dart'
```

★★**判定を挟んでいない。**★★
出力には**同期の設計番号**と **UI のペイン番号**（`docs/UI設計メモ.md` §2-3）が混ざっている。
★**分けられるのは人だけ**であり、★**「表と一致するか」で確かめると
同じ思い込みで表と判定の両方を作ってしまう**（**D-15 (j)**）。
→ ★**生のまま残す。次に読む人が自分で分けること。**

```
loveca-core/lib/src/entities/deck.dart:12:/// ★★ 旧番号 P1〜P5 はこの 5 つに置き換えた (2026-08-27) ★★
loveca-core/lib/src/entities/deck.dart:13:///   参照先の無い体系だったため。P4 / P5 は最初から D11 / D35 として
loveca-core/lib/src/entities/deck.dart:14:///   採番済みで、新規採番は P1〜P3 の 3 つだけ
loveca-db/lib/src/schema/tables.dart:240:/// ★★ 旧番号 P1〜P5 を置き換えた (2026-08-27) ★★
loveca-db/lib/src/schema/tables.dart:241:/// ★ここは以前「P2 revision・updatedAt」と書いており deletedAt が落ちていた。
loveca-ui/lib/src/data/deck_id.dart:1:/// `deckId` の生成（決定 D62 / `loveca-core/lib/src/entities/deck.dart` の P1）.
loveca-ui/lib/src/data/deck_id.dart:3:/// `Deck.deckId` は **UUID v4** と定められている（P1「連番にすると端末間で衝突する」）が、
loveca-ui/lib/src/data/deck_repository.dart:85:  /// P3 のメタ編集（M6）。★`deck_tags` に `ord` つきで保存される。
loveca-ui/lib/src/data/deck_repository.dart:88:  /// P3 のメタ編集（M6）。★デッキの中のカードから選ぶ。無ければ null。
loveca-ui/lib/src/data/deck_repository.dart:431:          deckId: _newDeckId(), // ★UUID v4（P1 / 決定 D62）
loveca-ui/lib/src/data/deck_repository.dart:436:          // ★P5: 作成時のカードマスタ版。未知カード検出に使う（決定 D35）。
loveca-ui/lib/src/data/deck_repository.dart:452:  /// **カバーを外せない。** P3（M6）でカバーの編集口を作る以上、外す口が要る。
loveca-ui/lib/src/data/deck_repository.dart:504:  /// P5 は「作成時のカードマスタ版。未知カード検出に使う」。
loveca-ui/lib/src/data/deck_repository.dart:506:  /// 「今の版で作ったのに未知」という説明不能な状態になり、P5 の用途を壊す。
loveca-ui/lib/src/data/deck_share.dart:275:  /// → 取り込む前に見せ、取り込んだあとは P1 の検証に違反として出る。
loveca-ui/lib/src/data/master_repository.dart:139:  /// 未解消の取り込み失敗の一覧（R6 / P2）。
loveca-ui/lib/src/state/deck_edit_store.dart:177:  /// P3 のメタ編集（M6）。★`_apply` を通すので `revision` は動かない。
loveca-ui/lib/src/state/deck_list_store.dart:64:  /// P3 のメタ編集を R2 から行う（M6）。
loveca-ui/lib/src/ui/deck/deck_edit_page.dart:4:/// 探す（R4 と同じ一覧ペイン）→ 入れる（ドラッグ / 「+」）→ 検証を見る（検証パネル **P1**）が
loveca-ui/lib/src/ui/deck/deck_edit_page.dart:11:/// | 論理削除（決定 D102） | ★検証パネル **P1** の常設 |
loveca-ui/lib/src/ui/deck/deck_edit_page.dart:13:/// ★★ この段落の `P1` は UI のペイン番号（`docs/UI設計メモ.md` §2-3）である ★★
loveca-ui/lib/src/ui/deck/deck_edit_page.dart:14:/// 同期の設計番号だった旧 `P1`〜`P5` とは別の体系で、後者は 2026-08-27 に
loveca-ui/lib/src/ui/deck/deck_edit_page.dart:16:/// (`ルール整合性チェック_v1.06.md` D-5)。★丸括弧で囲んだ `P1`〜`P5` は書かないこと ——
loveca-ui/lib/src/ui/deck/deck_edit_page.dart:219:  /// P3 メタ編集（M6）。★R2 と同じダイアログを器だけ替えて開く（§2-1）。
loveca-ui/lib/src/ui/deck/deck_list_page.dart:11:/// `deletedAt` を立てるだけなので DB には行が残る（P3。物理削除すると
loveca-ui/lib/src/ui/deck/deck_list_page.dart:97:  /// P3 メタ編集（M6）。★R3 と同じダイアログを器だけ替えて使う（§2-1）。
loveca-ui/lib/src/ui/deck/deck_list_page.dart:238:/// R6 への導線と、取り込み失敗のバッジ（決定 D39 / §2-3 の P2）。
loveca-ui/lib/src/ui/deck/deck_meta_dialog.dart:1:/// P3 デッキのメタ編集（`docs/UI設計メモ.md` §2-3 / M6）.
loveca-ui/lib/src/ui/deck/deck_pane.dart:17:/// | 6 | P1 検証パネル | ★**常設**（§2-3）。別画面だと構築の最中に効かない |
loveca-ui/lib/src/ui/deck/deck_pane.dart:94:  /// P3 メタ編集を開く（M6）。★ダイアログは画面側が出す。
loveca-ui/lib/src/ui/deck/deck_pane.dart:172:  /// P3 メタ編集を開く（M6）。★ダイアログは画面側が出す。
loveca-ui/lib/src/ui/deck/deck_pane.dart:203:                // ★P3（M6）。R2 と同じダイアログを器だけ替えて開く（§2-1）。
loveca-ui/lib/src/ui/deck/deck_validation_panel.dart:1:/// P1 検証パネル（`docs/UI設計メモ.md` §2-3 / 決定 D28 / D55）.
loveca-ui/lib/src/ui/settings/import_issues_section.dart:1:/// P2 取り込み失敗の詳細（決定 D39 / `docs/UI設計メモ.md` §2-3 / §2-6）.
loveca-core/test/deck_validator_test.dart:194:  test('copyWith で revision が増える (同期の差分検出 P2)', () {
loveca-db/test/deck_dao_test.dart:108:    // ★P3: 物理削除すると削除が同期で伝播しない。
loveca-ui/test/data/deck_repository_test.dart:174:      // ★P5: 作成時のカードマスタ版が残る（決定 D35 の未知カード検出に使う）。
loveca-ui/test/data/deck_repository_test.dart:278:      // P2「更新のたびに +1」と食い違うが、直す先は loveca_db 側であり
loveca-ui/test/data/deck_share_test.dart:256:          reason: '取り込んだあと P1 の検証に違反として出る');
loveca-ui/test/ui/card_art_test.dart:323:  group('★★ P3 カバー選択（幅 56 の箱）★★', () {
loveca-ui/test/ui/deck_edit_page_test.dart:443:  group('★★ P1 検証パネルは DeckValidator の結果を出す（決定 D28 / D55）★★', () {
loveca-ui/test/ui/deck_meta_dialog_test.dart:1:/// P3 デッキのメタ編集（`docs/UI設計メモ.md` §2-3 / 決定 D70 / M6）.
loveca-ui/test/ui/deck_pane_width_test.dart:15:/// （名前+保存 / メモ / 中身の一覧 / ゴミ箱 / P1 常設）。
loveca-ui/test/ui/settings_page_test.dart:427:  group('★ R2 のバッジ（§2-3 の P2）', () {
```

★**読むときの手がかり**（★**判定ではない。分けるのは読む人**）——

- ★**同期の設計番号**は `deckId` / `revision` / `updatedAt` / `deletedAt` /
  `masterDataVersion` / 論理削除 / 差分検出のいずれかを語っている
- ★**UI のペイン番号**は `検証パネル` / `取り込み失敗の詳細` / `メタ編集` のいずれかを語っている
  （`docs/UI設計メモ.md` §2-3）
- ★**旧番号の廃止そのものを説明している行**もある（`deck.dart` / `tables.dart` /
  `deck_edit_page.dart` の注意書き）。★**これは再発ではない** —— ★**D-30 の型**であり、
  ★★**走査の出力からは再発と説明が区別できない**★★

---

## 4. ★ 走査 C —— `revision` が上がらない経路（陽性対照）

```bash
grep -rnE "into\(db\.decks\)|update\(db\.decks\)|into\(db\.deckEntries\)|update\(db\.deckEntries\)" \
  --include=*.dart loveca-db/lib | grep -v '\.g\.dart'
```

```
loveca-db/lib/src/dao/deck_dao.dart:76:        await db.into(db.decks).insertOnConflictUpdate(
loveca-db/lib/src/dao/deck_dao.dart:125:    await (db.update(db.decks)..where((d) => d.deckId.equals(deckId))).write(
loveca-db/lib/src/dao/deck_dao.dart:255:          await (db.update(db.deckEntries)
```

★★**陽性対照として、既知の 3 か所が出ることを確かめた**★★（**D-10**）——
`save` / `softDelete`（**D-9**）/ `backfillOrd`（**D-26**）。
★**分類と共通の問いは `docs/同期設計メモ.md` §3。**

---

## 5. ★ コードを変えていないことの確認

```bash
git diff --stat HEAD -- 'loveca-core/' 'loveca-db/' 'loveca-ui/' 'loveca-data/'
```

★**出力は空**（2026-08-27 / 実行して確認）。
★**テスト件数は動いていない**ので `CLAUDE.md` §3 の表は更新しない。

---

## 6. ★ 次のセッションへ

★**正は `docs/同期設計メモ.md`。** ここには書かない。
★**このセッションで決めたことは 1 つも無い**（論点の分離と、偽の完了報告の巻き戻しだけ）。

# Android デッキ構築 UI —— 決定の申し送り

作成: 2026-09-02 / 出所: UI 設計セッション（利用者との対話）
★**2026-09-02 に全面更新**（★引き継ぎ資料 §5 の 10 項目に加え、★そこから派生した論点を全部含む）

★★**2026-09-03 追記: ★受け取った字面を★★1 文字も変えずに★ここへ置いた★★**★★ ——
★**洗った結果・訂正・行立ては★★§11 以降に★別の節として置く★★**（**D-35** —— ★★受け取った記録は書き換えない★★）。

---

## 0. ★ この文書の位置づけ

★**UI 設計セッションで利用者と決めた内容の全部**である。
★**リポジトリには 1 バイトも書いていない**（§7-2）。
★**設計レビュー相談役が Claude Code へのプロンプトに載せること。**

★**決定番号は採番していない。** 予約すると `docs/決定事項一覧.md` §3 と食い違う（**D-15**）。
★**このセッションが引き取った未伝達分**（引き継ぎ資料 §2 の同期 UI 4 件）は **§5** に写してある。

---

## 1. ★★ 既存の決定を覆す項目（5 件）★★

★★**ここを最初に読むこと。**★★ ★5 件とも**既に実装済みの Windows の挙動を変える。**

### 1-1. ★★ エネルギー 0 枚で 6.1 の警告を出さない ★★

| 枚数 | 表示 |
|---|---|
| ★**0 枚** | ★**何も出さない** |
| ★**1〜11 枚** | ★**警告を出す** |
| ★**12 枚** | 出さない |

★★**書き換えが要る箇所は 3 つ。**★★

1. ★**決定 D96** —— 「0 枚でも 6.1 の警告は出す」という部分
2. ★`docs/UI設計メモ.md` **§12-3** —— 「★解けない側」の記述と「R3 検証パネルの表示要件」
3. ★`ルール整合性チェック_v1.06.md` **D-15 (i)** —— ★§12-3 が「**D-B の先例として引用している**」と書いている箇所

★★**§12-3 は逐語で次のように書いており、それを覆すことになる。**★★

> ★**解けない側**: 「0 枚を**完成**と呼ぶ」は **6.1.1.3 に反する。**
> 分けられるのは**保存の可否**までであって、**「6.1 を満たしている」という表示は分けられない。**
> ★**ここを「解けます」と書かないこと。**

★**利用者はこれを承知のうえで選んだ。**★D96 の要望も同じ利用者から出たものなので、変更は正当である。

★★**注意: この申し送りを書いた側は D-15 (i) の実物を読んでいない**★★（★リポジトリを読めない）。
★★**D-15 (i) の中身は実物で確かめること**★★（**§7-3**）。

★**D96 の「盤面の開始時に 12 枚を補完する」は変わらない。**★変わるのは**警告の表示だけ**である。
★★**画像出力にも同じ規則が及ぶ**★★ —— ★エネルギー 0 枚のデッキは、★画像に**エネルギーの塊を出さない**（§3-9）。

### 1-2. ★ 絞り込みから種別を外し、カテゴリタブへ移す（★Windows も）

★Android にカテゴリタブ（すべて / メンバー / ライブ / エネルギー）を置く。
★★**Windows にも同じタブを置き、両方の絞り込みパネルから「種別」を外す。**★★
★理由は利用者の指定 —— 「**Windows 版も似た UI にしたい**」。
★現行の `filter_panel.dart` の種別 `Dropdown` が対象。★R3 / R4 が共有する 1 箇所である。

### 1-3. ★ 4 枚制限で「+」を無効にするのをやめ、警告に変える（★Windows も）

★現行（`docs/UI設計メモ.md` §9-6 #7 の実測）は
「メンバー 4 枚で「+」が無効。★パラレル違いも合算」である。
★★**これをやめる。**★★ ★押せる数を制限せず、★**合計が 4 を超えたらデッキ編集画面に警告を出す。**

★★**4 枚超が起きる経路を取り違えないこと。**★★
★一覧の 1 行は**1 刷り**だが、★4 枚制限は**カードナンバー単位**（6.1.1.2）。
★`-001-P` 3 枚 + `-001-R` 2 枚 と入れると、★各行は 4 以下なのに**合計 5 枚**になる。
★★**1 行の帯は 1 2 3 4 のままでよい**（★5 枚以上を 1 行で押す口は要らない）。

★向きは **決定 D69**（4 枚超過は弾かず丸めず、入れて `DeckValidator` に警告させる）と同じ。
★ただし D69 は**共有形式インポート**の話であり、★**手で押す経路は別件である**（**§7-7**）。

### 1-4. ★ 種別依存の軸を常に出す（★§4-2 の案 (a) を覆す / Windows も）

★現行は `docs/UI設計メモ.md` §4-2 の案 (a) ——
「コスト絞り込みを出すのは**種別がメンバーのときだけ**」。
★★**これをやめ、常に出す。**★★ ★絞ると他の種別が消えることを受け入れる（★§2 の穴 1）。

### 1-5. ★★ カードデータの更新を、再起動せずに行う ★★

★★**決定 D56 を覆す。**★★

> ★D56: 実行中に取り込むと `MasterCatalog` が**静かに古くなる。**★間違った答えを返すが例外は出ない＝ **A-3 型。**
> → 「データを更新」は**再起動を伴う操作**にする。
> ★これは性能の話ではなく、★**無効化漏れを構造的に不可能にするための制約**である。

★**Android では「その他」タブに更新ボタンを置き、★進捗を出しながら取り込み、★再起動しない。**
★理由 —— ★★**Android ではアプリが自分を再起動するのが行儀の悪い挙動になるため。**★★
★★**受け入れた穴は §2 の穴 2 に書いた。**★★

---

## 2. ★★ 受け入れた穴（4 件 / 利用者に伝達済み・了承済み）★★

★★**いずれも「例外は出ないが間違った答えを返す」型である。**★★
★このプロジェクトが A-3 型として繰り返し戒めてきたものを、★**判断のうえで受け入れた。**

### 穴 1. 種別依存の軸で、他の種別が黙って消える

★コスト / ブレード / 所持ハート / ハート合計 で絞ると**ライブとエネルギーが**、
★スコア / 必要ハート / 必要ハート合計 で絞ると**メンバーとエネルギーが**、
★**何の断りもなく結果から消える。**

★**D50**（打ち切りを黙らない）/ **D40**（検索経路を出す）と**一貫しない。**
★**緩和材料は 2 つ** —— ★カテゴリタブで種別を選べば気づけること、
★チップが有効なときは色が変わるので**絞っていること自体は見えること。**

### 穴 2. 更新後、編集中のデッキだけが古いカタログを見続ける

★**編集中のデッキがある状態で更新を押しても、そのデッキは開き直さない。**

| # | 何が古くなるか |
|---|---|
| 1 | ★編集中のデッキが**更新で消えた刷り**を持っていても、検証が古い判定を出す |
| 2 | ★**新しく増えたカード**が、開いている一覧に出ない |
| 3 | ★`DeckValidator` が古い `RuleConfig` を使う |

★★**いずれも例外は出ない。**★★
★**実装の緩和として**: ★カタログを作り直す箇所を 1 つに決め、★編集中でない画面はそれを見る形にすること。

### 穴 3. 取り込み失敗を、後から見に行けない

★**Android には「取り込み失敗の詳細」を見る画面を置かない。**

| 場面 | 出るもの |
|---|---|
| ★更新した直後 | ★Notice バー + ★**完了ダイアログに件数**（「更新しました（1 件を取り込めませんでした）」） |
| ★後日 | ★★**何も残らない**★★ |

★`import_issues` は**次に成功するまで残る永続の状態**だが、★**Android からは見に行けない。**
★**D39** の「黙って落とさない」は**その場の告知だけ**で満たすことになる。

### 穴 4. Android では共有形式を書き出せない

| | Android |
|---|---|
| ★共有形式から**取り込む** | ★**ある**（デッキ一覧の追加ボタン） |
| ★共有形式へ**書き出す** | ★★**無い**★★ |

★書き出しは **Windows でのみ**行える。
★★**URL 共有の実装を延期した結果である**（§3-11）。

---

## 3. ★ Android UI の決定

### 3-1. 画面の構成

```
┌──────────┬──────────┬──────────┐
│ カード検索 │ デッキ構築 │  その他   │  ← 下段タブ 3 つ
└──────────┴──────────┴──────────┘
```

★**WS の「新商品」は落とす**（★個人利用なので新弾告知は要らない）。

★**デッキ編集は 2 画面を行き来する**（★WS と同じ形）——

```
デッキ詳細  ⇄  カード検索
```

★Windows の 2 ペイン（一覧 + デッキ + 検証パネルを同時）とは**別の割り方**である。
★★**Android は 1 ペインになる**★★（★電話は **411 論理px** で、しきい値 840 を大きく下回る）。

### 3-2. ★★ カード検索の一覧 ★★

★★**器は縦リストである**（1 行 1 枚。左に絵、右に文字）。★★
★★**Windows の格子（`GridView` / タイル比 200:279）とは別の形である。**★★

**メンバー**
```
┌────────┬──────────────────────┐
│ コスト  │ カード名（大きい文字）    │  0行目
│ (色BH) │ 作品名, ユニット          │  1行目
│ (D/S)  │ (ブレード)数字            │  2行目
│   絵   │ (ハート)数字 (ハート)数字 │  3行目
└────────┴──────────────────────┘
```

**ライブ**
```
┌────────┬──────────────────────┐
│ スコア  │ カード名（大きい文字）    │  0行目
│ (色BH) │ 作品名, ユニット          │  1行目
│ (D/S)  │ 必要ハート((ハート)数字…) │  2行目
│   絵   │                          │
└────────┴──────────────────────┘
```

**エネルギー**
```
┌────────┬──────────────────────┐
│   絵   │ カード名（大きい文字）    │  0行目
│（重ねる │                          │
│ ものなし）│                          │
└────────┴──────────────────────┘
```

| 項目 | 決定 |
|---|---|
| 絵の左上 | ★**メンバー = コスト / ライブ = スコア** |
| その下 | ★**色付きブレードハート → ドロー・スコアのアイコン** を縦に積む |
| エネルギー | ★**絵に何も重ねない。右はカード名だけ** |
| ハートの符号 | ★**(ハートアイコン画像)数字** を全角スペース区切りで並べる |
| 作品名の溢れ | ★**1 行のまま末尾を「…」で切る**（折り返さない） |
| 素材 | ★**ハート / ブレードハートのアイコン画像は設計ファイルに在る**（★無色・任意色も含む） |

★★**色は 6 色ではなく 8 通りある**★★ —— ★桃 / 赤 / 黄 / 緑 / 青 / 紫（2.1.1.1）/ ★**無色**（2.1.1.2）/ ★**任意色**（2.1.1.3）。

### 3-3. ★ 縦の積み方

```
┌────────────────────────┐
│ 件数 / 並び順            │
├────────────────────────┤
│                        │
│   カードの縦リスト        │
│                        │
├────────────────────────┤
│ すべて メンバー ライブ …   │ ← カテゴリタブ（横スクロール）
├────────────────────────┤
│ [キーワード][登場作品]…    │ ← 絞り込みチップ（横スクロール）
├────────────────────────┤
│ メ 12/48 ラ 3/12 エ 0/12 │ ← 3 本のカウンタ
├────────────────────────┤
│ カード検索 デッキ構築 その他 │ ← 下段タブ
└────────────────────────┘
```

★★**WS と同じ積み方である**★★（★チップとカテゴリタブは**画面の下**にある）。

### 3-4. ★ 3 本のカウンタ

| 項目 | 決定 |
|---|---|
| 置き場 | ★**一覧の下端に帯。3 本を常時出す** |
| 段 | ★**1 段に入れば横 1 段。入らなければ 3 段。★2 段の状態は作らない** |
| ラベル | ★**縮めない**（★入らなければそのまま 3 段へ行く） |
| しきい値 | ★★**実装時に実測して決める**★★ |
| 超過 | ★**数字を赤にする** |
| 判断点 | ★**カウンタ帯の中に閉じる。**`PaneScaffold` のしきい値を増やさない（**D61** は動かさない） |
| テスト | ★**1 段の状態と 3 段の状態を対で固定する**（§9-6 / **D-10**） |

★**実測の作法**（**§9-7** / **D-28** の先例）——
★「入る / 入らない」の境目を記録するときは、★**刻み幅・標本点・判定手段を併記すること。**
★「〜付近」と書いた値は下限にならない。

### 3-5. ★ カテゴリタブ

★**すべて / メンバー / ライブ / エネルギー**（★横スクロール）
★**「お気に入り」は置かない。**
★★**「すべて」があるので、3 種別は同じ一覧に混ざって出る**★★（§5-6 の答え）。

### 3-6. ★ 絞り込みチップ（11 / 左から）

```
キーワード → 登場作品 → ユニット → コスト → パラレル
→ ブレードハート → ブレード → スコア
→ 所持ハート → 必要ハート → 商品
```

| チップ | 中身 |
|---|---|
| ★★**キーワード**★★ | ★★**フリーワード検索**★★ ——★カード名 / テキスト / カード番号 などを語で引く（★FTS5 / **D40** / **D50**） |
| ★**所持ハート** | ★**合計**と**各色**の範囲指定（★下記） |
| ★**必要ハート** | ★同じ形 |
| ★**ブレードハート** | ★**色のみ**（★ドロー / スコアのアイコンでは絞らない） |

★**採らない軸**: キャラクター名 / ドロー・スコアのアイコン / **レアリティ** / イラストレーター / ★**効果の種類**（`card_keywords`）
★**種別は置かない**（★カテゴリタブへ移した / §1-2）

★★**「効果の種類」を置かないので、内部語彙（`ENTER` / `LIVE_SUCCESS`）の日本語写像は要らなくなった**★★（★U21 の論点 4 は Android では不要）。

#### ★★ 帰結: U21 の論点 1 を前倒しで判断することになる ★★

★段 B（**子テーブル由来**で「1 行 = 1 刷り」の投影に収まらない軸）が **5 つ**入った ——
★**登場作品 / ユニット / 所持ハート / 必要ハート / ブレードハート。**
★「**投影の形を変える**」か「**cardNumber の集合を返す SQL を足す**」かの決着が要る。
★後者なら `loveca_db` に手が入るので **D-8 / D-11 / D-13 と同じ時点**になる。

### 3-7. ★★ 数値の指定 —— リストから選ぶ ★★

★★**数値を指定する軸はすべてこの形にする**★★（★コスト / ブレード / スコア / 合計ハート / 各色ハート / 合計必要ハート / 各色必要ハート）。

```
合計ハート   [ 未指定 ]個 ～ [ 未指定 ]個
桃ハート     [   0   ]個 ～ [   4   ]個
赤ハート     [ 未指定 ]個 ～ [ 未指定 ]個
 …（8 色ぶん）

                ↑ 欄を押すと
        ┌────────────────┐
        │ [絞り込み      ] ✕ │  ← リストそのものを絞る
        │ ☐ 未指定         │
        │ ☐ 0             │
        │ ☐ 1             │
        │  …              │
        │ キャンセル  クリア  │
        └────────────────┘
              ★1 つ選ぶ
```

★**キーボードで打たせない。**★★**リストから選ぶ。**★★
★★**リストの終端は実データの最大値**★★（★§7 の測定項目）。

### 3-8. ★★ 枚数の帯 ★★

| 状態 | 帯 |
|---|---|
| ★**0 枚のとき** | ★**1 2 3 4**（★0 のボタンは**無い**） |
| ★**1 枚以上のとき** | ★**0 1 2 3 4**（★0 が左に現れ**灰色**。★選択中の数は**オレンジ**） |

| 項目 | 決定 |
|---|---|
| 右 | ★**大きな数字で現在の枚数**（★0 枚では出さない） |
| エネルギー | ★末尾に「**…**」。押すと**最大 12 枚**のフォームが開く |
| メンバー / ライブ | ★**「…」は付けない。**4 枚が上限 |
| 他の刷りで埋まっている分 | ★**押せる数を制限しない**（§1-3） |
| 同じ数字を再度押す | ★**何も起きない** |

★★**「…」がエネルギーだけに要る理由**★★ —— ★**6.1.1.2 は「メインデッキには」と書いている。**
★★**エネルギーデッキに 4 枚制限は無い**★★（§6 の 3）。

### 3-9. ★ デッキ一覧

| 項目 | 決定 |
|---|---|
| 1 行 | ★**カバー画像 + デッキ名 + 枚数 + 更新日時** |
| 上 | ★**デッキ名の絞り込み 1 本** + **件数** + **並び順** |
| 並び順 | ★軸は**更新日時 / 作成日時 / デッキ名**。★既定は**更新日時の新しい順**。★**同じ軸を再度選ぶと反転** |
| 行の右端 | ★**歯車** → 「**デッキ情報編集 / 複製 / 削除 / キャンセル**」 |
| 作る口 | ★**右下に浮くボタン** |

★**削除したデッキを戻す口は置かない**（★**決定 D117-4** で確定済み）。

★**追加ボタンを押すと、新規作成の画面が開く**（★モーダルではなく**画面**として開く）。

```
┌──────────────┬──────────────────┐
│  新規作成     │ 共有形式から取り込む │
└──────────────┴──────────────────┘

  デッキ名 [                    ] ✕

        キャンセル      確定
```

★★**共有形式の取り込みの入口はここ 1 本だけである。**★★
★実装メモ §9-11 は「入口は **R3 の 1 本だけ**」と定めていたが、
★★**Android では R2 に移る。★入口の数は 1 のまま**★★で、方針は守られる。

★**新規作成に置くのはデッキ名だけ。**★メモ / タグ / カバーは置かない
（★★**カバーはデッキが空なので選べない**★★）。

### 3-10. ★ デッキ詳細

```
┌────────────────────────────┐
│ ← マイデッキ一覧   デッキ詳細  ⚙ │
│ デッキ名                  [保存] │  ← 保存は未保存のときだけ
├────────────────────────────┤
│ 確認 │ 画像出力 │ デッキログへ入力 │  ← タブ 3 つ
├────────────────────────────┤
│ メンバー 45/48  ライブ 12/12    │
│ エネルギー 12/12                │
│ コストの分布  ▂▄█▆▂  ← 横棒あり  │
│ ブレードハートの色の分布          │
│ ブレードハートの数                │
├────────────────────────────┤
│ ⚠ メンバーカードは 48 枚にして…   │
├────────────────────────────┤
│ [規則順 ▾]      [手動並べ替え]   │
├────────────────────────────┤
│ メンバー                       │
│  [絵][絵][絵][絵]              │
│   ×1  ×1  ×4  ×1              │
│ ライブ                         │
│  [絵][絵][絵][絵]              │
│ エネルギー                      │
│  [絵]                          │
│  ×12                           │
├────────────────────────────┤
│ [カードを追加]                  │
│ ⚠ デッキを空にする               │
└────────────────────────────┘
```

| 項目 | 決定 |
|---|---|
| タブ | ★**3 つ: 確認 / 画像出力 / デッキログへ入力** |
| カードの並べ方 | ★**4 列の格子**。★各カードの下に **×N** |
| 区切り | ★★**種別のみ**★★（メンバー / ライブ / エネルギー）。★登場作品やユニットでは区切らない |
| 並び順 | ★**規則順 / カード番号順**の 2 つ |
| 手動並べ替え | ★★**ボタンを押すと、長押し + ドラッグで並べ替えられるモードになる**★★ |
| 警告の印 | ★★**4 枚超過と未知の刷りに「!」を付ける**★★ |
| 下部 | ★**カードを追加 / デッキを空にする**（★★取り消せないので確認を挟む★★） |
| 保存 | ★**ヘッダ。**未保存のときだけ出す |

★★**手動並べ替えをモードに分ける形は、実装メモ §7-2 の問題を回避する**★★ ——
★タッチでは**縦のスクロールとドラッグが競合**し、スクロール側が勝ちやすい。★モードを分ければ競合しない。

★**規則順の中身は 決定 D99 が正**（★この文書を書いた側はその実物を読んでいない）。
★★**手動で並べ替えたあと規則順を選ぶと `ord` が上書きされる**★★（★Windows の「規則順に戻す」と同じ役）。

### 3-11. ★ 統計

| # | 出すもの | 横棒グラフ |
|---|---|---|
| 1 | ★3 本のカウンタ（メンバー / ライブ / エネルギー） | ★無し |
| 2 | ★**コストの分布** | ★★**あり**★★ |
| 3 | ★ブレードハートの色の分布 | ★無し |
| 4 | ★ブレードハートの数 | ★無し |

★★**「所持ハートの色の分布」は一度採ったあと、利用者が撤回した。**★★
★**次に統計を触る人が同じ検討をやり直さないよう記録する。**

### 3-12. ★ 画像出力

| 項目 | 決定 |
|---|---|
| 中身 | ★**デッキ名 + カードの格子**（★統計は入れない） |
| 枚数 | ★各カードの**右下に数字** |
| 区切り | ★種別ごとに塊を分ける。★★**エネルギーは行を改めて、別の塊として離す**★★ |
| エネルギー 0 枚 | ★★**エネルギーの塊を出さない**★★ |
| 行き先 | ★**画面に表示するだけ。**★依存を増やさない |
| 保存 / コピー | ★★**Android 固有の機能に任せられるかを実機で確かめる**★★ |

★★**技術的な事実**★★ —— ★Flutter の画像ウィジェットは**長押しメニューを持たない。**
★Android 標準の「画像を長押し → 保存 / 共有」は**ブラウザの中の機能**であり、★アプリの画像には自動では付かない。
★→ ★**実機で確かめる項目**（§8）。

### 3-13. ★★ デッキログへ入力 ★★

★★**アプリ内でブラウザ版デッキログを開き、自動で入力する**★★（★WebView + スクリプト注入）。

★★**利用者に伝達済みの懸念（3 つ）**★★

| # | 懸念 |
|---|---|
| **1** | ★★**相手のページ構造に依存する。**★★ ★デッキログの HTML が変われば**黙って壊れる。**★★A-3 型そのもの。**★★ |
| **2** | ★**依存が 1 つ増える**（`webview_flutter` 等）。★このプロジェクトは `shared_preferences` / `package_info_plus` / `sqlite3_flutter_libs` を**いずれも却下**してきた |
| **3** | ★**D43 の射程が要確認。**★実装メモの引用では理由が「画像の取得経路が 2 つになると切り分けできない」なので★当たらないと考えられるが、★**実物を読んでいない** |

★★**手当て（必須）**★★: ★**失敗したら出す** ——「**デッキログの画面が変わった可能性があります**」。

★**規約については利用者が判断する。**★仰っているのは**データを取る**のではなく
**自分のデッキを自分のアカウントに入れる**操作である。

### 3-14. ★ カード詳細

```
┌────────────────────────────┐
│ ← 戻る       カード詳細 3 / 25 │
├────────────────────────────┤
│ カード名                  x4 │
├──────────┬─────────────────┤
│          │ レアリティ  R      │
│  [絵] ⤢  │ 種別       メンバー │
│          │ コスト      3      │
│          │ ブレード     2     │
│    ▼     │ ハート  (桃)2(青)1 │
├──────────┴─────────────────┤
│ グループ名 / ユニット            │  ← WS の「特徴」の位置
├────────────────────────────┤
│ テキスト                    ▼ │
│ 【登場】あなたは…              │
├────────────────────────────┤
│ ほかの刷り  [絵][絵][絵]        │
├────────────────────────────┤
│ （カード情報は長押しでコピーできます）│
├────────────────────────────┤
│ 4 / 4    0  1  2  3  4       │  ← デッキ編集から入ったときだけ
└────────────────────────────┘
```

| 項目 | 決定 |
|---|---|
| 隣のカードへ | ★**左右スワイプで移る。**★位置を「**3 / 25**」で出す |
| 属性の表 | ★★**絵の右横**★★（★縦に並べない） |
| 「特徴」の位置 | ★★**グループ名とユニット**★★（★条文 2.12.2 が『』で参照すると定めている 2 つ） |
| テキスト | ★**折り畳める**（▼） |
| 絵 | ★**拡大できる**（⤢） |
| 長押し | ★**カード情報をコピーできる** |
| ★**ほかの刷り** | ★**足す**（★Windows にある機能） |
| ★**「4 / 4」** | ★**出す**（★4 枚制限に達していることを赤字で） |
| 発売日 | ★**置かない**（★`products` を読む DAO が存在しないため） |
| 効果タグ | ★**置かない**（★配信データに無い） |
| 参照カード / お気に入り / カート | ★**置かない** |

★★**エネルギーは数値の欄ごと消える**★★（★実装メモ §9-9 #9 の実測と同じ）。

### 3-15. ★ カード検索の上部

★**件数と並び順だけ。**★★検索ボタン（詳細検索）もカメラも置かない。★★

| 項目 | 決定 |
|---|---|
| 並び順の軸 | ★**収録商品 / カード番号 / カード名 / コスト・スコア** |
| ★**コスト・スコア** | ★★**コスト順とスコア順を統合した 1 つの軸**★★（★メンバーはコスト、ライブはスコア） |
| 「すべて」タブのとき | ★★**種別で束ねてから並べる**★★（メンバー → ライブ → エネルギー） |
| 昇順 / 降順 | ★**同じ軸を再度選ぶと反転**（★デッキ一覧と揃える） |

### 3-16. ★ 「その他」タブ

| # | 項目 |
|---|---|
| 1 | ★**カードデータの更新**（状態の表示 + 更新ボタン + 進捗 + 完了ダイアログ） |
| 2 | ★**総合ルール**（公式サイトへのリンク） |
| 3 | ★**アカウント**（同期のログイン / ログアウト）★★方式は未決★★ |
| 4 | ★**カード検索の初期条件**（パラレル非表示 —— ★カード検索とデッキ編集で**別々に持つ**） |
| 5 | ★**アプリバージョン** |
| 6 | ★**ライセンス** |

★**外したもの**: カードのテキストを表示する / **取り込み失敗の詳細**（★§2 の穴 3）/ データ版 /
カードデータの場所 / ★**既定のエネルギーカード**（★**PC のみの機能**）

★**更新があるときは下段タブにバッジを出す**（★WS と同じ。★更新すると消える）。

### 3-17. ★ 警告の文言

★★**命令形で統一する**★★（★WS と同じ）。

| # | 文言 |
|---|---|
| 1 | ★「**メンバーカードは 48 枚にしてください。**」 |
| 2 | ★「**ライブカードは 12 枚にしてください。**」 |
| 3 | ★「**エネルギーカードは 12 枚にしてください。**」（★★**1〜11 枚のときだけ**★★ / §1-1） |
| 4 | ★「**LL-bp1-001「日野下花帆」は 4 枚までです。**」 |
| 5 | ★「**LL-bp1-001-P のカードが見つかりません。**」（★★命令形にしない★★） |

★★**カードの名指しは「カードナンバー +（カード名）」**★★。
★★**「同名カード」という言い方は使えない**★★ ——
★カード名が同じでもカードナンバーが違えば**別カード**（制限は別々に掛かる）、
★同じカードナンバーの**別の刷り**は**同名**（制限は**合算**される）。

★★**警告 5 だけ命令形にしない理由**★★ ——★マスタから消えたカードなので**カード名が引けず**、
★利用者が何をすべきかも状況で変わる（★取り除くのか、カードデータを更新するのか）。
★**D35** は「黙って削除しない」と定めているので、★アプリが勝手に消すことはできない。

★**作法**（実装メモ §9-5 / M3）——
★**内部語彙を出さない** / ★**1 件 = 1 行で、対処まで書く** / ★3 つが同じ見た目だと「なんか出てる」で無視される。

### 3-18. ★ 置かないもの

| # | 何を | 決定 |
|---|---|---|
| 1 | ★**構築ルールを確認しない**（逃げ道 / §5-8） | ★**置かない。**警告は常に出る。★**ただし警告が出ていても保存できる**（現行と同じ） |
| 2 | ★**試し引き**（§5-9） | ★**置かない** |
| 3 | ★**参照カード**（§5-10） | ★**いまは実装しない**（★下記） |
| 4 | ★お気に入り | ★置かない |
| 5 | ★**URL 共有** | ★★**実装を延期**★★（★下記） |
| 6 | ★詳細検索（虫眼鏡）/ カメラ検索 | ★**将来実装するかもしれない** |
| 7 | ★効果の種類（`card_keywords`）で絞る軸 | ★**将来実装するかもしれない** |

#### ★★ 参照カード —— 実装しないが、切り分けは残す ★★

★**利用者の判断**: 「★**現時点でカード名を参照する効果は無いため実装しない。
★ただし後々実装するかもしれないので、その旨を記載しておくこと。**」

★★**次に着手する人のために、洗った結果を残す。**★★

★**括弧の中を拾って照合するだけなら、D-A（効果テキストの構文解析をしない）には当たらないと考えられる。**
★「」『』は**規則が区切り記号として定めたもの**であり（2.3.2.2 / 2.4.3.1 / 2.12.2 / 2.12.3）、
★中身を取り出すのは**字句の切り出し**であって、★**文の構造を読むのではない。**

★★**ただし罠が 2 つある。**★★

| # | 罠 |
|---|---|
| ★**1** | ★★**注釈文（2.12.4）の中にも「」『』が入りうる。**★★ ★注釈文は**ゲームに影響しない**ので、そこから拾うと**存在しない参照**が出る。★★**しかし除外するには（）の入れ子を解く必要があり、それは構文の解析に踏み込む。**★★ |
| ★**2** | ★**カード名自身に引用符が含まれる場合**、参照と誤認する |

★**着手するときの候補は 4 つあった**（★利用者はどれも選んでいない）——
1. 作らない / 2. 拾って照合する（注釈文を除外しない）/ 3. 拾って照合する（注釈文を除外する）/
4. ★**照合しない。**括弧の中の語を叩くと、★**その語でカード検索を実行するだけ**にする

#### ★★ URL 共有 —— 延期 ★★

| 項目 | 決定 |
|---|---|
| いま | ★★**実装しない**★★（★全機能の実装後、余力があれば） |
| 延期の理由 | ★**サーバーの強度が読めない**ため |
| 将来の方針 | ★**サーバーにデッキを置いて ID を URL に載せる。**★行指向のテキストは採らない |

★★**判断に使った材料（次に検討する人へ）**★★

★**D67 と当たる** ——★D67 は「行指向のテキスト。★JSON を採らない」と定め、
★理由を「**手で貼る前提**で、1 文字壊れて全体が読めなくなる形は用途に合わない」とした。
★★**URL は「1 文字壊れると全体が読めなくなる形」そのものである。**★★

★**URL にデッキを載せる道は 2 つしかない**——

| | 中身 | 代償 |
|---|---|---|
| ★**(a) URL 自体に載せる** | ★デッキを圧縮して URL に埋める | ★URL が長い（★25 種類なら **200〜350 文字**の見込み。★載る） |
| ★**(b) サーバーに置いて ID を載せる**（★採用） | ★同期サーバー（**D120-1**）に置く | ★★**公開になる**★★（★同期は「自分の端末どうし」の想定だった） |

★★**ドメインが要る。**★★ ★同期サーバーのドメインを使えば App Link にできるが、
★無い場合は `loveca://` のような**独自のスキーム**になり、
★★**アプリを持っていない人には「開けません」としか出ない。**★★

---

## 4. ★ 引き継ぎ資料の訂正（3 件）

★**引き継ぎ資料 §3 の WS 分析は、実物の画面と 3 か所で食い違っていた。**
★利用者から追加のスクリーンショットと画面録画を受け取って確かめた。

| # | 引き継ぎ資料の記述 | ★実物 |
|---|---|---|
| **1** | §3-4「**0 / 1 / 2 / 3 / 4** のボタン列」 | ★**1 2 3 4** の 4 つ。★0 は無い |
| **2** | §3-4「**0 枚のときは「0」が灰色**」 | ★★**逆である。**★★ ★**1 枚以上のときだけ 0 のボタンが現れ**、灰色で表示される |
| **3** | §5-5「ラブカも**同一カードナンバー 4 枚まで**」 | ★★**メインデッキだけである**★★（§6 の 3） |

★**この文書を書いた側も 1 つ取り違えた**（★記録として残す）——
★★**WS のチップ帯には「キーワード」と「カードキーワード」が別々にある。**★★
★**「キーワード」はフリーワード検索**（★§3-9 のモーダルが**カード名 / テキスト / 特徴 / カード番号 / レアリティ**を対象にしている）で、
★`card_keywords`（効果の種類）ではない。★**一度これを取り違えて候補を作った。**

---

## 5. ★ 同期 UI（★引き継ぎ資料 §2 の未伝達 4 件を引き取り）

★★**以下は 2026-09-02 の会話で利用者が答えたが、まだリポジトリに 1 文字も記録されていない。**★★
★**このセッションが引き取り、ここに写した。**

| 項目 | 決定 |
|---|---|
| **同期の起動** | ★アプリを開いたとき / 閉じるときに**自動**。手動ボタンは置かない |
| **衝突の決着** | ★**自動**（`updatedAt` が新しい方）。選択画面は出さない。★**D108-2 / D113-1 は動かさない** |
| **衝突の告知** | ★同期が終わったとき、**画面下に短く出て自動で消える**。「N 件が相手の版に置き換わりました」 |
| **消えた側** | ★**残さない。**中身も編集元も見せない（★Q-09 の既定値がそのまま確定） |
| **同期の失敗** | ★**何も出さない。**次に開いたとき黙って再試行 |

★**経緯**: 利用者は当初「衝突したら両方の時刻を出して人が選ぶ」を希望したが、
★それが **D108-2** と **D113-1** と正面から当たることを説明したところ、
「新しい方に合わせるなら自動で構わない」と回答した。★**決定は動かない。**

★**帰結**: §32-6 の 25 は、★**6 つある結果の型のうち画面に出るのは衝突の 1 つだけ**になる。
★**新しい画面は 1 つも要らない。**

★★**残る懸念（利用者に伝達済み・了承済み）**★★:
★失敗を何も出さないと、サーバーが止まっている間に同期していないことに気づけない。
★繋がった瞬間にまとめて衝突する。★ただし告知は出るので **D107** は守られる。

★★**ログインの方式は未決である**★★（§9 の 2）。

---

## 6. ★ 条文の新所見（3 件）

★**総合ルール ver. 1.06 の実物から逐語で引いた。**★`ルール整合性チェック_v1.06.md` に載せる価値がある。

### ★ 1. 『』の指す先が条文どうしで食い違う

> **2.4.3.1.** テキスト中、『』で囲まれた名称は、その**グループ名称**を持つカードを意味します。
> **2.12.2.** カードテキストで『』で指定した名称のカードを参照する場合、それは**グループ名（2.4）またはユニット名（2.5）**の名称がその名称であるカードを参照します。

★**2.4.3.1 はグループ名のみ、2.12.2 はグループ名またはユニット名。**★後者のほうが広い。

### ★ 2. 「」の一致条件が条文どうしで食い違う

> **2.3.2.2.** テキスト中、「」で囲まれた名称は、その名称を**カード名の一部に持つ**カードを意味します。
> **2.12.3.** カードテキストで「」で指定した名称を参照する場合、それはその名称を**持つ**カードを参照します。

★**2.3.2.2 は部分一致（「一部に持つ」）、2.12.3 は「持つ」で曖昧。**

### ★★ 3. 4 枚制限はエネルギーデッキに掛からない ★★

> **6.1.1.1.** メインデッキは、メンバーカード **48 枚ちょうど**と、ライブカード **12 枚ちょうど**を含む必要があります。
> **6.1.1.2.** ★**メインデッキには**、カードナンバーが同一であるカードをそれぞれ **4 枚まで**含むことができます。
> **6.1.1.3.** エネルギーデッキは、エネルギーカード **12 枚ちょうど**を含む必要があります。

★★**6.1.1.2 は「メインデッキには」と限定している。**★★
★エネルギーデッキには**同一カードナンバーの上限が無い。**

★**実装側は既にそうなっている** —— ★**決定 D97 が 12 枚すべてを `LL-E-002-SD` で補完する設計**であり、
★4 枚制限が掛かっていれば成立しない。★**条文と実装は整合している。**

★★**食い違っていたのは引き継ぎ資料のほうである**★★（§4 の 3）。
★★**`DeckValidator` が本当にエネルギーを 4 枚制限から除外しているかは、実物で確かめること**★★（**§7-3**）。

★★**メインデッキは 60 枚である**★★（48 + 12）。★会話中に「40 枚」と書かれた箇所があったが、★書き違いとして扱った。

### ★ 参考: ハートの構造（§3-2 の根拠）

> **2.9.3.** メンバーのハート表記では、複数のハートアイコンの列が表記されることがあります。
> その場合、このメンバーは**それぞれの色と数**のハートアイコンをすべて有します。
> **2.11.2.1.** 各ハート音符は、縦につながったアイコンで表記され、
> **上が必要なハートの色、下が必要なその色のハート数**を意味します。

★★**所持ハートも必要ハートも、データとしては「(色, 数) の組の列」という同じ形になる。**★★
★カード上の絵は違う（重なったハート / 縦につながった音符）が、★**行に重ねる符号は同じ設計で足りる。**

> **2.1.1.1.** 桃、赤、黄、緑、青、紫の各色
> **2.1.1.2.** 特に色を指定しないハートアイコン（★**無色**）
> **2.1.1.3.** 6 色のいずれか 1 つの色として**任意に扱うことができる**ハートアイコン（★**任意色**）

★**色は 6 色ではなく 8 通りある。**★引き継ぎ資料 §4 の記述は**条文と一致している。**

★★**条文から読める限りでは、無色と任意色は出る場所が違う**★★ ——
★**無色**は必要ハート側（2.11.3 が「必要ハートのうち、無色でない各ハート音符」と書いている）、
★**任意色**は所持ハート側（2.1.1.3 がライブ成功判定 8.3.15.1.1 を参照している）。
★★**断定はしない。★実データで確かめること**★★（§7 の 7）。

---

## 7. ★★ 実データの測定項目（7 件）★★

★**UI 設計セッションはリポジトリを読めないので測れなかった。**★Claude Code に測らせること。
★**測る前に形を確定させないこと。**

| # | 測ること | 何が決まるか |
|---|---|---|
| **1** | ★**メンバー 1 枚が持つハートの色数**の最大と分布 | ★3 行目に収まるか。★収まらなければ符号を変える |
| **2** | ★**ライブ 1 枚が持つハート音符の数**の最大と分布 | ★同上（2 行目） |
| **3** | ★**メンバーに色付きブレードハートが在るか**（何種か） | ★★**メンバーのコストの下が常に空になるなら、置く意味が無い**★★ |
| **4** | ★**作品名 / ユニットを 2 つ以上持つカードの種数** | ★1 行目の「…」がどれだけ発生するか |
| **5** | ★**3 本のカウンタが 411 論理px に 1 段で入るか** | ★§3-4 のしきい値 |
| **6** | ★★**コスト / ブレード / スコア / 各色ハート / 各色必要ハート の実データ上の最大値**★★ | ★§3-7 の数値リストの終端 |
| **7** | ★**無色は必要ハート側だけか / 任意色は所持ハート側だけか** | ★§3-7 のフォームに 8 色すべてを出すか |

★**3 について**: `docs/UI設計メモ.md` §12-1 は
「ブレードハートのアイコン（DRAW / SCORE）| 実データ DRAW 59 / SCORE 37 種（★**いずれもライブのみ**）」
と記録している。★**色付きブレードハート（`card_hearts` kind=`bladeHearts`）の分布は書かれていない。**
★★**利用者は「ライブカードにもブレードハートは存在する」と述べた。**★★
★**メンバー側の分布が未確認である。**

★**測定の作法**（**D-28**）: ★**刻み幅・標本点・判定手段を併記すること。**★「〜付近」は下限にならない。

---

## 8. ★ 実機・外部の確認項目（2 件）

| # | 確かめること | なぜ |
|---|---|---|
| **1** | ★**表示した画像を、Android の標準機能で保存 / コピーできるか** | ★★Flutter の画像ウィジェットは**長押しメニューを持たない**★★。★できなければ依存を足すことになる |
| **2** | ★**デッキログのページ構造で、自動入力が成立するか** | ★★成立しなければ §3-13 は作れない★★ |

★**実装メモ §7 の「Phase 5 で最初に潰す項目」と同じ扱いにすること。**

---

## 9. ★ 残る未決（2 件）

| # | 論点 |
|---|---|
| **1** | ★★**U21 の論点 1**★★ ——★子テーブル由来の軸を「**投影の形を変える**」か「**SQL を足す**」か。★★**前倒しが確定した**★★（§3-6）。★後者なら `loveca_db` に手が入るので **D-8 / D-11 / D-13 と同じ時点**になる |
| **2** | ★★**同期のログインの方式**★★ ——★§5 で決めたのは**同期の挙動**だけ。★確かめられたのは **D120-1**（画像もデッキも同じサーバー）まで。★**Phase 4 の実装と同時に決める** |

★**引き継ぎ資料 §5 の 10 項目は、すべて片づいた。**

---

## 10. ★ このセッションで踏んだ型（★次に同じ役をする人へ）

| # | 何が起きたか |
|---|---|
| **1** | ★★**1 つの候補に 2 つの軸を畳んだ**★★ —— 「一覧の下端に」と「横に並べて」を 1 案にまとめ、利用者が「1」と答えたあとに並べ方を問い直すことになった（**§7-8**） |
| **2** | ★★**前提を確かめずに候補を作った**★★ —— カード一覧が Windows では**格子**なのに、WS の**縦リスト**を暗黙の前提にして「右のテキスト欄」という存在しない場所を候補に書いた |
| **3** | ★★**説明せずに専門語を使った**★★ —— 「行」「格子」「縦リスト」「中身」を図示せずに問い、利用者が 2 度「意味が分からない」と答えた |
| **4** | ★★**決まっていない話を持ち込んだ**★★ —— 4 枚超が「1 行で 5 枚以上押す」経路で起きると誤認し、「…」の要否を余計に問うた。★実際は**カードナンバー単位の合算**で起きる |
| **5** | ★★**同型としてまとめる前に実物を確かめなかった**★★（**§7-7**）—— WS のチップ帯に「キーワード」と「カードキーワード」が**別々にある**のに、★「キーワード」を `card_keywords` と対応づけた。★★**その結果、フリーワード検索の口が一度消えかけた。**★★ |
| **6** | ★★**「WS 固有」と断定した**★★ —— 「デッキログへ入力」をラブカに相当物なしと書いたが、★**ラブカにもデッキログはある** |
| **7** | ★★**候補に入れ忘れた**★★ —— WS の「特徴」に当たるものとして**ユニット**を候補に入れず、★利用者から指摘を受けた |

★**いずれも「実物を確かめてから使う」（§7-3）と「問いの形が答えの形を決める」（§7-8）の型である。**

# ★★ `引き継ぎドキュメント.md` は「正」ではない ★★

> このファイルは `docs/引き継ぎドキュメント.md`（以下「原典」）への**警告**である。
> ★**原典には 1 文字も足していない**（見出しも注記も入れていない）。
> 警告を原典の中に書くと、**原典が「当時そう書かれていたもの」でなくなり、
> 資料としての値が落ちる**ため、別ファイルに分けた。

---

## 1. これは何か

★**2026 年の初期資料**である。Phase 1 に着手する前の設計の出発点であり、
**現在の設計の正ではない。**

現在の正は次のとおりで、食い違ったら**必ずこちらを採る**。

| 何の正か | 正はどこか |
|---|---|
| セッションを跨ぐ前提・現在のフェーズ | `CLAUDE.md` |
| 決定（`決定 DNN` / `決定 D-X` の実体） | `docs/決定事項一覧.md` |
| 条文との突き合わせ・所見（`D-N`） | `ルール整合性チェック_v1.06.md` |
| 各層の設計 | `docs/PhaseEngine設計メモ.md` / `docs/UI設計メモ.md` / `docs/盤面設計メモ.md` / `docs/UI技術検証メモ.md` |
| テスト件数 | `CLAUDE.md` §3 の表 |

## 2. なぜ追跡下に置いたか

`ルール整合性チェック_v1.06.md` **D-5**（`P1`〜`P5` の参照先が存在しない）の
**案 (a)「復元する」が成立した**ため。

★**成立したのは「番号の定義が読める」ところまで**である。
**原典の記述内容が現在も正しい、という意味ではない。**

## 3. ★★ D-5 の分以外は未監査である ★★

★**監査したのは `P1`〜`P5` の定義（531-535 行 / 3965-3969 行）だけ**である。
原典は **5,310 行**あり、全体を現在の設計と突き合わせると範囲が爆発する。

→ ★**未監査であること自体を未決 **U31** として登録してある**（`CLAUDE.md` §8 の索引。★**監査の範囲もそこに書いた**）。
★**「追跡下にある = 検証済み」と読まないこと。**

★★**とくに原典 §1.5「確定済みの設計判断（D1〜D36）」を決定として読まないこと。**★★
★**`docs/決定事項一覧.md` が「未記録」と書いていた番号の中身はここに在る**が、
★★**未監査である**★★ —— 下の §4 の 2 件（`D5` の「13フェイズ」/ `D15` の「一人回し」）は
**この表の行そのものが既に覆されている**実例である。
★**「未監査」と「決定」を同じ札にしないこと。**

## 4. ★ 既知の誤り・古い記述

★**下の表は「監査の結果」ではない。**
D-5 の作業のついでに**たまたま判明したもの**だけを並べてある。
**ここに無いから正しい、とは読めない。**

| 原典の所在 | 原典の記述 | 現在の正 |
|---|---|---|
| 55 / 140 / 179 / 244 行 | **「13フェイズ」** | ★**リーフフェイズは 12 個**（7.1.2 / 7.3.3 / 8.1.2）。`ルール整合性チェック_v1.06.md` **B-1** / `docs/PhaseEngine設計メモ.md`「★「13 フェイズ」は誤記」 |
| 20 / 47 / 254 行 | **「一人回し」** | ★**語ごと廃止**（決定 **D88-1**）。→ 下の §5 |
| 531 行（`P1` の定義） | 「`deckId` は **UUID**」 | ★**「v4」は原典の定義には無い。**「v4」は同じ原典の**コード見本**（4011 行）にのみある。格が違う（→ `docs/決定事項一覧.md` の `P1` の決定文） |

## 5. ★ 廃止した語 —— 免除であって使用許可ではない

決定 **D88-1** で廃止した語が、原典に **3 箇所**ある（20 / 47 / 254 行）。
★**原典は廃止より前に書かれたものなので、直さない**（直せば原典でなくなる）。

検査の正は `docs/決定事項一覧.md` の **D88-1** の grep で、
**除外に挙がっていない場所で 0 件であること**を見る（`CLAUDE.md` §8 は数を持たず、そこを指すだけ）。
★★**除外された文書の免除理由は「廃止を記録するため」であり、原典の免除理由は
「廃止より前に書かれた資料そのものだから」で、理由が違う。**★★
★**この違いは D88-1 の除外表に書いてある**（2026-08-27）。
★★**それまで原典は除外に入っておらず、この grep は非 0 を返していた。**★★
★**免除と使用許可を混ぜないこと**（`CLAUDE.md` §8 が同じ注意を書いている）。

★**走査（`loveca-ui/test/board/abolished_term_test.dart`）は `.dart` だけを見る**ので、
このファイルが増えても落ちない。★**実測で確認済み**（4 件とも通過 / 2026-08-27）。
→ ★**落ちないということは、この免除は機械には見えていない。**この文書が唯一の記録である。

## 6. ★ 原典の中にも語の衝突がある

`P1`〜`P5` は**同期の設計番号**（531-535 行）だが、
原典 **1155 行**の `(P2: 取得と正規化の分離)` は**別の体系**で、同期とは関係が無い。

★`docs/UI設計メモ.md` §2-3 の**ペイン番号** `P1` / `P2` / `P3` を加えると、
★★**同じ字面が 3 体系で使われている。**★★
→ ★**素の `P1` で機械的に置換しないこと**（`ルール整合性チェック_v1.06.md` **D-5** の「語の衝突」）。

# 同期設計メモ — Phase 4（認証・同期）の論点

位置づけ: `docs/UI設計メモ.md`（Phase 2 後半 = デッキ構築）と
`docs/盤面設計メモ.md`（Phase 3b = 盤面）の続き。
**Phase 4 に着手する前に「何が決まっていて、何が決まっていないか」を分離した文書。**
作成日: 2026-08-27

★★**この文書は論点表であって設計ではない。**★★
★★**方式は 1 つも決まっていない。**★★
★★**2026-08-29 訂正: ここには「選択肢も出していない」と書いてあった。★§13 で偽になった。**★★
★**前提が動いた**（型は `ルール整合性チェック_v1.06.md` **D-15 (l)**）——
★**N-5 の利用者判断が `決定 D107` で揃い、残りが技術判断だけになった**ので、
★**案を並べる段に入った**（§13）。★**並べただけで、1 つも選んでいない。**
★**§1〜§12 は今も「選択肢を出していない」。★出しているのは §13 だけである。**
決定の実体は `docs/決定事項一覧.md` に置き、ここからは**番号で参照する**（§8）。

> ★★**2026-08-27 追記: 利用者が §5 の ①② に回答した（決定 **D103**）。**★★
> ★**あわせて §A-5 の上流にあった法務にも判断が出た**（決定 **D104**）。
> → ★**回答そのものと、回答が開いた論点は §9 / §10。束 A の判断順序は §11。**
> ★★**スコープが決まっただけで、方式は 1 つも決まっていない。**★★
> ★**「①② に答えが出た＝設計が決まった」と読まないこと。**

★★**束 A の正はこの文書である。**★★
`CLAUDE.md` §8 と `docs/相談役への引き継ぎ.md` §5 は**索引として 1 行で指す**。
★**内容を両方に置かない**（`docs/相談役への引き継ぎ.md` §4-9）。
★**そうしないと食い違う** —— 実際に束 A の内訳は**3 か所で違っていた**（§0-2）。

---

## 0. この文書ができた経緯

### 0-1. 束 A とは何か

`docs/相談役への引き継ぎ.md` §5 が未決を 4 束にまとめたうちの 1 つで、
**「同期の前提」= Phase 4 の着手前 / 着手時に見るもの**である。

★**束は「いつ判断するか」ではなく「何を触るときか」で切ってある。**
同じパッケージを開けるついでに片づく設計であり、**束を割ると往復が増える。**

### 0-2. ★★ 束 A の内訳が 3 か所で食い違っていた ★★

| 出典 | 中身 |
|---|---|
| `docs/相談役への引き継ぎ.md` §5 | D-5 / D-9 / D-14 / D-1 / D-4 / D-7 / **U9**（★**D-26** と **U13** が無い） |
| `CLAUDE.md` §8「同時に見るもの」 | D-9 / D-14 / **U13** / D-1 / D-4 / D-7（★**U9** と **D-26** が無い） |
| `CLAUDE.md` §8 の D-26 の行 / `docs/決定事項一覧.md` D101 の詳細 | ★**D-26 も束 A** と自称している |

★**D-15 の「同じ内容を 2 箇所に書けば必ず食い違う」の実例がまた出た。**
★**D-26 が漏れたのは前提が動いたため**（**D-15 (l)**）——
`docs/相談役への引き継ぎ.md` §5 が書かれたあと、同じ日に束 B が D-26 を記録した。

→ ★**§1 の表を合併集合とし、正をここに一本化する。**

### 0-3. ★ この文書を作る過程で見つかった 5 つの事実

★**どれも束 A の項目そのものではなく、束 A の記述の側の問題である。**

| # | 事実 | 処遇 |
|---|---|---|
| 1 | ★★**D-5 の完了報告が偽だった**★★（括弧なしの旧番号が `lib` / `test` に生きている） | ★**D-31** を新設し、断定を巻き戻した。残務は §6-1 |
| 2 | 束 A の内訳が 3 か所で食い違う | ★上記 §0-2。この文書を正にした |
| 3 | ★**`Deck.copyWith` の既定値違反は 1 つではなく 2 つ**（`revision` の自動 +1 に記録が無い） | ★§3 の Q2 に組み込んだ |
| 4 | ★**Phase 4 の技術選定が原典に「確定済み」と書かれているが台帳に無い** | ★§5 の②に「参考（根拠に使わない）」として置き、**U31** から参照できるようにした（§6-3） |
| 5 | ~~★★**【要確認/法務】D-4 の上流に Phase 0-5 の未決がある**★★~~ → ★**2026-08-27 に解けた**（**D104**）。★**技術側も ①(d) で解けた**ので **A-5 は両側とも満たされて発火した** | ★§2-2 と §5 の①(d)、A-5 |

---

## 1. 束 A の論点表

★★**この表は `D-31` の実物として、発見当時（2026-08-27）の記録である。**★★
★**`ルール整合性チェック_v1.06.md` D-5 の表と同じ扱いにする** ——
★★**正は走査であって表ではない。**★★
書き換えが進めば表は必ず古くなるので、突き合わせる前に必ず走査し直すこと（走査は §6-1 と §3-1）。

★**9 項目。3 か所の食い違いを解消した合併集合である。**

| # | 項目 | ★症状か方式の未決か |
|---|---|---|
| **A-0** | **D-5 の残務**（括弧なしの旧番号 / 偽の完了報告） | ★**残務。判断ではない** |
| **A-1** | **D-9** `DeckDao.softDelete` が `revision` を上げない | 症状（実測あり）+ 方式の未決 |
| **A-2** | **D-26** `ord` の backfill が `revision` を上げず、マスタに依存 | 症状（実データで観測）+ 方式の未決 |
| **A-3** | **D-14** `Deck.copyWith` の既定値違反 | 症状（★**向きは決定済み**） |
| **A-4** | **D-1** `fromKey` の厳格性 | 方式の未決 |
| **A-5** | **D-4** `imageHash` は原本のハッシュ | 方式の未決 + ★**法務従属** |
| **A-6** | **D-7** `min_app_version` が 1.0.0 固定 | 症状（★**やること 1〜3 は決定済み**） |
| **A-7** | **U9** 論理削除を戻す口が無い | ★**従属**（D-9 + ①） |
| **A-8** | **U13** 勝敗・投了の記録場所 | ★**索引は Phase 6。Phase 4 で決めるのは 1 点だけ**（§2-1） |

★**A-2 は `docs/相談役への引き継ぎ.md` §5 に無く、A-7 は `CLAUDE.md` §8 に無い。**
どちらも束 A である（D-26 は自称、U9 は索引が「D-9 を決めたとき（Phase 4）」と書いている）。

---

### A-0. D-5 の残務

| 欄 | 中身 |
|---|---|
| **症状か方式か** | ★**どちらでもない。書き換えの残務**である |
| **決まっていること** | `P1` / `P2` / `P3` → **決定 D100 / D101 / D102**（新規採番）/ `P4` / `P5` → ★**決定 D11 / D35**（最初から採番済み）/ ★**丸括弧つきの走査は 0 件**（実行して確認） |
| **決まっていないこと** | ★**括弧なしの旧番号をどう扱うか**（書き換える / 走査を広げる / 放置する）。★**走査を広げるなら UI のペイン番号を除外する必要が出て D-30 の穴を踏む**（§6-1） |
| **依存** | ★**無い。**単独で片づく |
| **条文由来か実装判断か** | ★**どちらでもない**（文書の整合） |
| **利用者にしか答えられないか** | ★**いいえ** |

★★**なぜ束 A の先頭に残るか**★★ ——
D-5 が「着手前に判断する」と定めた理由は
**「Phase 4 を実装する人が `P4` とは何かに突き当たる」**ことだった。
★**括弧なしの旧番号が残っている限り、その状況は解消していない**
（`loveca-ui/lib/src/data/deck_id.dart` を読む人は `P1` に出会い、`決定 D100` へ辿れない）。

---

### A-1. D-9 —— `softDelete` が `revision` を上げない

| 欄 | 中身 |
|---|---|
| **症状か方式か** | ★**症状は確定**（M2 の実機で実測: 作成 0 → 保存 1 → **論理削除 1 のまま**）。★**直し方が未決** |
| **決まっていること** | **D101**（`revision` / `updatedAt` / `deletedAt` の 3 つ組で差分検出）/ **D102**（論理削除）/「`revision` を管理するのは呼び出し側の責務」（`docs/UI設計メモ.md` §9）/ ★**現状の挙動をテストで固定してある**（`loveca-ui/test/data/deck_repository_test.dart`）/ ★**選択肢 (a)(b)(c) は D-9 に書いてある** |
| **決まっていないこと** | §3-2 の **Q1**（差分検出の正）と **Q2**（責務の層）。★**それが決まらないと (a)(b)(c) を選べない** |
| **依存** | ★★**A-2 と同じ問い**★★（§3）/ **A-7（U9）がこれに一方向で依存する** / ①(a)「削除の伝播を同期するか」 |
| **条文由来か** | ★**実装判断**（総合ルールは同期を定めない） |
| **利用者にしか** | ★**いいえ**（内部整合の問題） |

★★**3 つ組のうち `updatedAt` だけが動いている。**★★
`softDelete` は `deletedAt` と `updatedAt` を書き、`revision` に触れない。
→ ★**D101 が「3 つで 1 組の不変条件」と定めたものが、内部で割れている状態**である
（`docs/決定事項一覧.md` D101 の詳細が「既知の例外 1」として名指ししている）。

---

### A-2. D-26 —— `ord` の backfill が `revision` を上げず、マスタに依存

| 欄 | 中身 |
|---|---|
| **症状か方式か** | ★**症状は確定**（実データで観測。読みだけの推論ではない）。★**直し方が未決** |
| **決まっていること** | **D65**（`ord` 列と移行方式）/ **D99**（規則順・null は末尾・backfill は Dart で書く）/ ★**壊れはしない**（全順序は決まる。「マスタが空でも落ちない」を `migration_test.dart` が固定）/ ★**いま直さない理由 3 つが D-26 に書いてある** |
| **決まっていないこと** | (1) 移行を「更新」と数えるか（§3-2 の **Q3**）/ (2) 移行で `revision` を上げると同期から見て「**全端末が同時に全デッキを更新した**」ことになるが、それが望ましいかは**衝突解決の方式次第**で、★**その方式そのものが未決**（①(f)）/ (3) ★**マスタ依存を許すか** —— 許さないなら「マスタが揃うまで移行しない」= **起動をマスタ取り込みに従属させる**という別の大きな判断になる |
| **依存** | ★★**A-1 と同じ問い**★★（§3）/ (2) は ①(f) / (3) は起動設計 |
| **条文由来か** | ★**実装判断** |
| **利用者にしか** | ★**いいえ。**★**ただし (3) を「マスタが揃うまで移行しない」側へ倒すと起動の体感が変わる**ので、そのときは利用者に見える判断になる |

★**型は D-15 (h)**（doc が実装より広い）—— D65 の「`ord` はデッキの中身なので `revision` に乗る」に
限定が無く、**移行経路を含めて読める**が、実装は `save` の経路にしか当てはまらない。
★**走査では出ない。**

---

### A-3. D-14 —— `Deck.copyWith` の既定値違反

| 欄 | 中身 |
|---|---|
| **症状か方式か** | ★**症状。**★**修正の向きは既に決まっている**（`CLAUDE.md` §1「Phase 4 の設計時に呼び出し側から渡す形へ直す」） |
| **決まっていること** | 境界 2（`loveca_core` に非決定な呼び出しを書かない）/ ★**UI から踏む経路は 0 件**（**D70**）/ ★**本体は未修正**（「解消済み」と読まない） |
| **決まっていないこと** | ★★**「Phase 4 の同期経路が `Deck.copyWith` を通るか」**★★（§4）/ 直し方（引数を必須にする / 廃止する / 別コンストラクタにする） |
| **依存** | ★★**§4 のとおり同期経路の形に依存する。単独では決められない**★★ / §3-2 の **Q2** が決まれば従属して決まる |
| **条文由来か** | ★**実装判断**（プロジェクト規約 `CLAUDE.md` §1 由来） |
| **利用者にしか** | ★**いいえ** |

#### ★★ 既定値違反は 1 つではなく 2 つある（★この文書で新たに記録する）★★

```dart
// loveca-core/lib/src/entities/deck.dart の copyWith
updatedAt: updatedAt ?? DateTime.now().toUtc(),   // ← D-14 が記録している違反
revision:  revision  ?? this.revision + 1,        // ← ★記録が無い
```

★**後者は「境界 2 の違反」ではない**（`DateTime.now()` でも seed なし `Random()` でもない）。
★★**しかし「`revision` を管理するのは呼び出し側の責務」（A-1 の前提）と矛盾している。**★★

★**D101 の「更新のたびに +1」の実装は 2 か所にある。**

| 場所 | 状態 |
|---|---|
| `loveca-core/lib/src/entities/deck.dart` の `copyWith` | ★**`lib` からの呼び出しが 0 件**（D70 の副次効果）。★**規則はここに書いてあるが誰も通らない** |
| `loveca-ui/lib/src/data/deck_repository.dart` の `save` | ★**実際に +1 しているのはこちら**（明示コンストラクタで `base.revision + 1`） |

→ ★**同じ規則が 2 か所にあり、片方は「内側で決める」形、もう片方は「呼び出し側で決める」形**である。
★**§3-2 の Q2 はこの矛盾を含めて問う。**

---

### A-4. D-1 —— `fromKey` の厳格性

| 欄 | 中身 |
|---|---|
| **症状か方式か** | ★**方式の未決**（案 2 つが D-1 に書いてある） |
| **決まっていること** | **D39**（取り込み層で商品ファイル単位に隔離し `import_issues` に記録。UI から検知できる）/ ★**`loveca_core` の `fromKey` は 1 文字も変えていない**/ ★**寛容化するなら「読み飛ばした事実を UI に出す」までを含める**（A-3 型の無言の欠落を避ける） |
| **決まっていないこと** | 厳格を維持するか寛容化するか |
| **依存** | ~~★★**①(a)「Phase 4 が配信経路を含むか」に依存する**★★~~ → ★★**2026-08-27 に満たされた**★★（**D103-1** ＝ カードマスタの配信を含む）。D-1 は「**配信経路が固まった時点**で決める」と自ら書いており、★**その時点が来た**。→ ★**厳格 / 寛容の本判断が Phase 4 で生きる** |
| **条文由来か** | ★**実装判断** |
| **利用者にしか** | ★**部分的に。**「新アイコンの追加で既存インストール済みアプリが起動不能になりうる」を受け入れるかは運用方針。★**ただし推奨は技術的に出せる** |

---

### A-5. D-4 —— `imageHash` は原本のハッシュであって配信物のハッシュではない

| 欄 | 中身 |
|---|---|
| **症状か方式か** | ★**方式の未決**（案 (a)(b)(c) が D-4 に書いてある） |
| **決まっていること** | ★**現時点で実害 0**（`manifest.json` は画像を 1 件も持たず、`planUpdate` は画像を知らない。dist は丸ごと差し替え）/ **D57**（ファイル名を組み立てる場所を `CardImageSource` の 1 箇所に閉じる）/ ★**壊れる時点は「画像を差分で配る経路を作った瞬間」** |
| **決まっていないこと** | ★**(a)(b)(c) のどれを採るか**（★**「画像を差分で配るか」（①(d)）は決まった** —— **配る**）。★**同時に見るもの**: **N-2**（差分の単位）/ **D-32**（版ゲートで更新が無言で落ちる。★**同じ失敗の型なので手当ても揃う**） |
| **依存** | ~~★★**①(d) → Phase 0-5（法務）**★★~~ → ★★**2026-08-27 に両側とも解けた**★★（技術側 = **D103-2** の (d) ＝ 画像も差分 / 法務側 = **D104**）。→ ★**Phase 4 の実判断へ昇格。(a)(b)(c) の選択が要る**。★**残る依存は N-2（差分の単位）**で、案 (b) は `printings.image_hash` が 1 列しかないので**スキーマと配信形式の変更**を伴う（`loveca_db` = 束 B の領域） |
| **条文由来か** | ★**実装判断** |
| **利用者にしか** | ★★**いいえ。**★★ §5-2 の判定を見よ —— ★**これは差分検出の論点であって画質の論点ではない** |

#### ★★ D-4 の上流にあった法務は解けた（2026-08-27 / 決定 **D104**）★★

★**技術的可否と法的可否を分けて書く規約は維持する**（`CLAUDE.md` §7-6）。
★★**外したのは札だけである。**★★

| | |
|---|---|
| **技術的可否** | ★**可能。**案 (a)(b)(c) はいずれも実装できる |
| ★**法的可否** | ★★**判断が出た**★★（**D104**）——「**利用規約を確認し、専門家の確認を得た判断。2026-08-27 時点**」。★**「解決済み」とだけ読まないこと** —— ★**根拠が利用規約である以上、改定時は引き直しが要る** |

★★**依存が 1 つ残っている。★中身は 2026-08-27 に変わった。**★★
~~★**「個人利用・非商用の範囲で開発する」（`CLAUDE.md` §0）が前提に入っているかは未記録**である。~~
→ ★★**確認の範囲が記録された**★★ ——「**利用規約の確認は、利用者以外がサーバー経由で
利用する形を想定した範囲で行われた（2026-08-27 時点）**」（**D105** の回答 3 と同じ機会）。
★**`CLAUDE.md` §0 とは食い違わない**（★**根拠と経緯の正は `docs/決定事項一覧.md` の D104 の詳細**）。
→ ★**残る依存は「範囲を超える形（公開 / 商用）になったら引き直し」**である。
★**人数の上限も公開の可否も述べられていない**（★**推測で埋めない**）。

★★**依存を `D-D` に結び付けない。**★★
★**`D-D` は「`loveca_core` を Flutter に依存させない」**であって別物である。

→ ★★**D-4 の発火条件は両側とも満たされた**★★（技術側 = **D103-2** / 法務側 = **D104**）。
★**以下は発火前の記述として残す** —— 原典は CDN の決定を「**Phase 0 の権利方針判断後に決定**」と従属させ、
Phase 0 の表は **0-5「画像を自前配信するかの決定」= 未決**としていた。
追跡下の `loveca-data/README.md` も独立に「Phase 0 を完了させてください」と書いていた
（★**同じコミットで書き換えた**）。

---

### A-6. D-7 —— `min_app_version` が CLI に露出せず 1.0.0 固定

| 欄 | 中身 |
|---|---|
| **症状か方式か** | ★**症状。**★**やること 1〜3 は方式まで書いてある** |
| **決まっていること** | やること 1〜4 が D-7 に書いてある / ★**現在の回避が本来と逆向き**（データに合わせてアプリの版を上げた）/ ★**`1.0.0` は意味を持って設定された値ではなく関数の既定値**/ ★**`--skip-images` は使えない**（`imageHash` も空になる） |
| **決まっていないこと** | やること 4「アプリの版を実態に戻すか」/ 既定値を何にするか / ★**そもそも `minAppVersion` を使い続けるか** |
| **依存** | ~~★★**①(a) に依存**★★~~ → ★★**2026-08-27 に満たされた**★★（**D103-1**）。D-1 と同じ発火条件だった。→ ★**やること 1〜3 は機械的に実施できる段階** |
| **条文由来か** | ★**実装判断** |
| **利用者にしか** | ★**いいえ**（§5-2 の判定）。★**①(a) が決まれば従属して決まる** |

---

### A-7. U9 —— 論理削除を戻す口が無い

| 欄 | 中身 |
|---|---|
| **症状か方式か** | ★**症状 + 従属**。★**M6 で入れなかったのは忘れたからではない**（U9 が理由を書いている） |
| **決まっていること** | **D102**（論理削除。DB には行が残る）/ ★**確認ダイアログ 1 枚で誤操作を防いでいる**/ ★**復元時の `revision` の扱いは D-9 の判断が先に要る** |
| **決まっていないこと** | ★**利用者に見える復元操作を置くか** / 置くなら復元時の `revision` の扱い |
| **依存** | ★★**A-1（D-9）に一方向で依存**★★ / ①(a)「削除の伝播を同期するか」 |
| **条文由来か** | ★**実装判断** |
| **利用者にしか** | ★★**部分的に。**★★「消したデッキを戻せる必要があるか」は仕様の希望だが、★**①(a) と D-9 が決まれば論点として消える可能性がある**（§5-2） |

---

### A-8. U13 —— 勝敗・投了の記録場所

| 欄 | 中身 |
|---|---|
| **症状か方式か** | ★**方式の未決。**★**ただし判断時期は Phase 6 である**（`docs/盤面設計メモ.md` §7-2 / §13） |
| **決まっていること** | ★**Phase 3b は UI 側の Store に持つ**（`GameState` に置き場が無く、`LiveJudgementRecord` は**ライブの勝敗**であって別物）/ ★**Phase 6 では `GameState` に要る**/ ★**ソロでは 1.2.1 のゲーム勝敗が成立しない**（1.2.1.1 が比較相手を要求する） |
| **決まっていないこと** | ~~★**Phase 4 で決めるのは 1 点だけ**（§2-1）~~ → ★★**2026-08-27 に決着した**★★（**D103-1** ＝ 対戦記録は**同期しない**）。→ ★**Phase 4 では何も決めない。Phase 6 のまま無傷である** |
| **依存** | **U29**（盤面の `←` の確認）が U13 に依存する。★**ただし確認ダイアログ自体は独立に置ける** |
| **条文由来か** | ★**記録の要否は実装判断。**★**ソロで 1.2.1 が成立しないことは条文由来**（1.2.1.1） |
| **利用者にしか** | ★**はい**（「対戦記録を残したいか」）。★**ただし①(b) に含まれる**ので独立の問いにしない |

---

## 2. Phase 4 で判断が要るか要らないかの切り分け

★**「要る／要らない」の二分ではなく、要る部分だけを切り出す。**

### 2-1. U13 —— Phase 4 で決めるのは 1 点だけ

★**索引どおり Phase 6 が正である。**
Phase 3b の結論「UI 側の Store に持つ」は
★★**「ソロもローカル対戦も保存や同期をしない」という前提**★★に立っている。

→ ★★**その前提を握るのは Phase 4 である。**★★

| ①(b) の答え | U13 |
|---|---|
| ★★**ゲームセッションを同期しない ← 2026-08-27 にこちらが選ばれた**★★（**D103-1**） | ★**Phase 6 のまま無傷。**この文書では何も決めない |
| ~~★**同期する**~~ | ~~★**前倒しになる。**記録場所（`GameState` か UI の Store か）を Phase 4 で決める必要が出る。★**U29 も動く**~~ ★**選ばれなかった** |

★**Phase 4 で決めるのは「同期対象にゲームセッションを含めるか」の 1 点だけ**であり、
**記録場所そのものは含めない。**
→ ★★**その 1 点は決着した。U13 も U29 も動かない。**★★

### 2-2. D-1 / D-4 / D-7 —— 3 つとも同じ前提に立っている

★★**3 つとも「配信経路が固まった時点で決める」と自ら書いている。**★★

| | 記述 |
|---|---|
| **D-1** | 「Phase 4（認証・同期）で**配信経路が固まった時点**で、次のどちらを取るか判断する」 |
| **D-4** | 「★**Phase 4 で『画像を差分で配る』経路を作った瞬間に壊れる**」 |
| **D-7** | 「`minAppVersion` は**配信経路と一体の仕組み**であり、D-1 と同じく『配信経路が固まった時点』でまとめて決めるのが筋」 |

~~→ ★★**しかし「Phase 4（認証・同期）が配信経路を含むか」は、どこにも決まっていない。**★★~~
→ ★★**2026-08-27 に決まった（D103-1）。含む。**★★

| ①(a) の答え | D-1 / D-4 / D-7 |
|---|---|
| ~~同期の対象は**デッキだけ**~~ | ~~★**3 つとも Phase 4 の判断から外れる。**判断時期を「**配信経路を作るとき**」へ移す~~ ★**選ばれなかった** |
| ★★**カードマスタの配信も含む ← こちらが選ばれた**★★ | ★**3 つとも Phase 4 で生きる。**さらに ①(d) が「配る」なら **D-4 はブロッカー** |

★**①(d) も「配る」だった**（**D103-2**）。→ ★**D-4 は Phase 4 の実判断である。**
~~★**D-4 はさらに Phase 0-5（法務）に従属する**（A-5）。~~
→ ★★**法務も 2026-08-27 に解けた**★★（**D104**）。★**両側とも満たされたので発火した。**

### 2-3. ★ 束 A のうち、①②の答えを待たずに進められるもの

| # | 進められる理由 |
|---|---|
| **A-0** | ★**文書とコメントの書き換え。**同期の設計に一切依存しない |
| **A-1 / A-2 の Q2** | ★**「責務はどの層か」は DB 内の整合の問題**で、同期の相手にも衝突解決にも依存しない（§3）。★★**2026-08-27 にこの行は 2 回動き、2 回目で元へ戻った**★★ —— ②（アカウント制 / **D103-3**）で「サーバーが決める」が 3 つ目の候補として立ちうるとして一度取り消したが、**D105-2**（サーバーは保管庫。判断しない）で**その候補が消えた**。→ ★**候補は「呼び出し側」「DAO / エンティティの内側」の 2 つに戻り、Q2 は本線から外れた**（経緯の正は §10 の **N-5**）。★★**但し書き**★★: **(f) が新しい列を要求すると Q2 が扱う対象は増えうる**（**N-11**）。★**答え（層）は動かないが対象は増えうる** |
| **A-3** | ★**境界 2 の規約違反としては単独で成立する。**★**依存するのは優先度だけ**（§4） |
| **A-6 のやること 1〜3** | ★**方式が既に書いてある。**①(a) が決まれば機械的 |

---

## 3. ★★ `revision` が上がらない経路 —— 共通の問い ★★

### 3-1. ★ 件数を書かない。走査できる形にする

```bash
grep -rnE "into\(db\.decks\)|update\(db\.decks\)|into\(db\.deckEntries\)|update\(db\.deckEntries\)" --include=*.dart loveca-db/lib | grep -v '\.g\.dart'
```

各ヒットを「**D101 の 3 つ組（`revision` / `updatedAt` / `deletedAt`）を動かすか**」で分類する。

★★**陽性対照は取ってある**★★（**D-10**: 0 件は「無い」と「見えていない」の区別がつかない）——
2026-08-27 に実行し、**既知の 3 か所が出ることを確かめた。**

| 出た場所 | 3 つ組を |
|---|---|
| `loveca-db/lib/src/dao/deck_dao.dart` の `save` | ★**呼び出し側が渡した値をそのまま書く** |
| 同 `softDelete` | ★**`deletedAt` と `updatedAt` だけ書き、`revision` に触れない**（**D-9**） |
| 同 `backfillOrd` | ★**`decks` に一切触れない**（**D-26**） |

★**分類の結果はここに書かない**（走査すれば出る）。
★**完了条件は「動かさない側が 0 件、または 1 件ごとに理由が書かれていること」**である。

### 3-2. ★★ 共通の問い ★★

> ★★**「`DeckDao.save` を通らない `decks` / `deck_entries` への書き込みで、
> D101 の 3 つ組をどう動かすか」**★★

★**個別に直すと規則が割れるのは、下の 3 つが分かれるためである。**

| # | 下位の問い | どこに現れるか |
|---|---|---|
| **Q1** | ★**差分検出の正は何か**（`revision` か `updatedAt` か 3 つ組全体か） | **D-9** の案 (c)「同期を `updatedAt` だけで行い `revision` を使わない」 |
| **Q2** | ★**3 つ組を決める責務はどの層か**（呼び出し側か、DAO / エンティティの内側か） | **D-9** の (a) vs (b) / **D-14** の直し方 / ★★**`Deck.copyWith` の `+1` が既に「内側」に在る矛盾**★★（A-3） |
| **Q3** | ★**利用者の編集でない書き込みを「更新」と数えるか** | **D-26**（移行 / backfill）。★**数えると「全端末が同時に全デッキを更新した」ことになる** |

### 3-3. ★ いま分かっている食い違いの一覧（★方式は決めない）

| 場所 | 何が起きているか | どの問いに触れるか |
|---|---|---|
| `softDelete` | ★**3 つ組のうち `updatedAt` だけが動く**（内部で割れている） | **Q1** と **Q2** |
| `backfillOrd` | ★**中身が変わるのに 3 つ組が 1 つも動かない** | **Q3** |
| `Deck.copyWith` | ★**内側で `+1` する**（呼び出し側の責務という前提と矛盾） | **Q2** |
| `DeckRepository.save` | ★**呼び出し側で `+1` する**（同じ規則の 2 か所目） | **Q2** |

★★**4 つとも「どこで 3 つ組を決めるか」の答えが違う。**★★
★**片方だけ決めると規則が 2 つになる**（D-26 の論点 1 が同じことを書いている）。

---

## 4. D-14 の依存 —— 同期経路が `Deck.copyWith` を通るか

> ★★**「Phase 4 の同期経路が `Deck.copyWith` を通るか」を先に決めないと、
> D-14 を直す意味が決まらない。**★★

| 同期経路が `copyWith` を | D-14 の意味 |
|---|---|
| ★**通らない**（受信も明示コンストラクタで畳む。**D70** と同じ形） | 「呼び出し元 0 件の規約違反」のまま残る。★**直す動機は境界 2 の遵守だけ**になり、優先度は下がる |
| ★**通る**（受信したデッキを既存に重ねる） | ★★**同期そのものが壊れる。**★★ `updatedAt` の既定値が**受信時刻**で上書きされ、**送信側の時刻が消える**。→ **Phase 4 のブロッカー** |

→ ★**D-14 は束 A の中で単独では決められない。** §3-2 の **Q2** が決まれば従属して決まる。

★**「直すかどうか」ではなく「いつ直すか」が依存している**ことに注意する ——
★**規約違反であること自体は同期の設計に依存しない。**

---

## 5. ★ 利用者にしか答えられないもの

### 5-1. ★★ ①と② の 2 つだけである ★★

★**当初 5 つ挙がったが、3 つは従属していた**（§5-2）。
★**選択肢は書かない** —— 論点表を作る前に方式を決めることになるため。

> ★★**2026-08-27: ①② とも回答を得た（決定 **D103**）。**★★
> ★**以下の表は「決まっていること」欄に答えを畳み込んである。**
> ★**回答そのものは §9。**★**回答が開いた新しい論点は §10。**
> ★★**回答は「利用者にしか答えられないもの」を消したのであって、方式を決めてはいない。**★★

#### ① Phase 4 のスコープ —— 何を同期するか

| | |
|---|---|
| **決まっていること** | フェイズ名が「認証・同期」であること / 対象に**デッキ**が入る（**D100** / **D101** / **D102** / **D11** / **D35** が `Deck` に**先行実装済み**。理由は「後付けが極めて高コスト」）/ ★**dist は現在は丸ごと差し替え**で `manifest.json` は画像を 1 件も持たない / dist の解決は 3 段（**D60** / **D89** / **D-18**）/ ★**D43「UI にネットワーク取得の口を作らない」** |
| ★**2026-08-27 に決まったこと**（**D103**） | ★**(a)** カードマスタの配信を**含む。差分でダウンロードする** / ★**(b)** ゲームセッションは**含めない** / ★**(c)** 設定は**含む。アカウントごとに揃える**（★**範囲は「既定のエネルギーカード」しか言われていない** → §10 の **N-6**） / ★**(d)** 画像も**差分にする** |
| **決まっていないこと** | (e) **同期の相手の詳細**（★**②で「利用者が用意するサーバー」までは決まった**。★**何を前提にしてよいかは未確認** → §10 の **N-7**） / (f) ★★**衝突解決の方式**★★（★**未採番**。D-26 の論点 2 が「その方式そのものが未決」と書いている。★★**②がアカウント制になったので必須になった** → §10 の **N-5**★★） |
| ★**答えで何が分岐したか**（★**過去形。答えが出た**） | (a)「含む」→ ★**D-1 / D-4 / D-7 が Phase 4 で生きる**（§2-2）/ (b)「含まない」→ ★**U13 は Phase 6 のまま無傷**（§2-1）/ (d)「配る」→ ★**D-4 は実判断**（★**法務も D104 で解けた** / A-5）/ ★**(f) は未決のままなので D-26 の論点 2 も決められない** / ★★**(a) の「削除の伝播」はまだ決まっていない**★★ので **U9 は縮んでいない**（★**「デッキを含む」は元から決まっており、削除をどう伝播するかは (f) の側の問い**） |
| ★**答えなくても進める部分** | ★**A-0 / A-3 / A-6 の 1〜3**。★★**Q2 は外れた**★★ —— ②が前提を動かしたため（§2-3 / **N-5**） |
| ★**問いの文言**（★**2026-08-27 に回答済み。記録として残す**） | 「Phase 4 で同期するのは何と何ですか。デッキだけですか。**カードマスタの配信**・**ゲームの対戦記録**・**アプリの設定**は含みますか。**画像を差分で配る**予定はありますか」 |

#### ② 認証は何のためか

| | |
|---|---|
| **決まっていること** | ★★**追跡下の文書には 1 行も無い**★★（走査で確認。`CLAUDE.md` はフェイズ名を書くだけ）/ ★**`Deck.lastDeviceId` という列が先行実装されている**（`loveca-db/lib/src/schema/tables.dart` / 既定は空文字） |
| ★**参考（★根拠に使わない）** | ★**原典 `docs/引き継ぎドキュメント.md` §1.2「確定済み」に、台帳に採番されていない決定群がある**（§6-3 / **U31**）。★**技術名はここに写さない** |
| ★**2026-08-27 に決まったこと**（**D103-3**） | ★★**アカウント制。サーバーは利用者が用意する**★★ |
| **決まっていないこと**（★**2026-08-27 に 4 つ減った**） | ~~認証が守るのは何か（自分のデッキの秘匿 / サーバ資源 / Phase 6 の対戦相手の同定）~~ → ★★**D105-3 で「他人に見せないためのアクセス制御」まで確定した**★★（★**サーバ資源の保護は述べられていない** —— ★**推測で足さない**）/ ~~★**Phase 6 の認証と同じものか**~~ → ★★**認証は 1 つ**★★（**決定 D106-4** / §9-5。★**サーバーも同一である** —— **D106-1**。★**ただし「同一のサーバー」から「認証は 1 つ」は導けない。★別に述べられたことである**）/ ~~★**`lastDeviceId` が何を指すか**~~ → ★★**§10 の N-11 へ送った**★★（★**D105-4 で「端末の見分けでは足りない」ことが確定したため。★走査の結果は N-11 に写した**）/ ~~★★**サーバーに何を前提してよいか**★★~~ → ★★**N-7 は回答済み**★★（**決定 D105**） |
| ★**答えで何が分岐したか**（★**過去形**） | ~~アカウントを持たない（端末間直結・ファイル同期）→ ★**サーバが要らず、(f) が「最後に書いた者勝ち」で済みうる** → **D-26 の論点 2 が軽くなる**~~ ★★**この枝は選ばれなかった**★★ / ★★**持つ ← こちらが選ばれた**★★ → **Phase 6 の認証と共通化する判断が前倒しで要る**。★★**(f)（衝突解決）が必須になった**★★（§10 の **N-5**） |
| ★**答えなくても進める部分** | ★★**訂正: §3 は認証に依存しないとは言えなくなった**★★ —— **Q2** は責務の層の候補にサーバーが加わりうる（§2-3 / **N-5**）。★**§4 は依存しない**（`copyWith` を通るかは同期経路の形の話）/ ★**D-1 / D-7 も依存しない**（配信経路の話） |
| ★**問いの文言**（★**2026-08-27 に回答済み。記録として残す**） | 「認証は何を守るためのものですか。アカウントを作って複数端末から同じデッキを見るためですか、それとも端末どうしを直接つなぐだけですか。Phase 6 の対戦相手の同定と同じ仕組みにしますか」 |

### 5-2. ★ 要回答から外した 3 つの判定

★**独立した好みではなく、①②に従属している。**
★**先に聞くと、あとで前提が動いて聞き直しになる**（人間相手に **D-15 (l)** を作ることになる）。

| # | 論点 | 判定 |
|---|---|---|
| ③ | 削除したデッキを戻せる必要があるか（**U9**） | ★**従属。****D102**（論理削除）は決定済みで、残るのは「**利用者に見える復元操作を置くか**」だけ。★**①(a) の「削除の伝播を同期するか」と D-9 の答えで決まる** |
| ④ | 画質を後から調整したいか（**D-4**） | ★★**差分検出の論点であって画質の論点ではない**★★（本文を読み直した判定）。D-4 の失敗の型は「**痕跡を残さず更新を落とす**」で、機構は `{imageHash}.webp` が**中身を名指せていない**こと。案 (a) の代償「画質の調整が事実上できなくなる」は**差分配信を導入したあとにだけ生じる**（★**現運用は丸ごと差し替えなので全再配信が既定である**）。→ ★**利用者判断ではない。①(d) に従属し、その (d) は法務（Phase 0-5）に従属する**。★★**2026-08-27: (d) は「配る」、法務は D104 で解けた。→ 発火した**★★（★**判定そのものは維持する** —— **差分検出の論点であって画質の論点ではない**） |
| ⑤ | アプリの版番号をどう振るか（**D-7**） | ★**従属。**`minAppVersion` は同期・配信の**互換性**の話で、やること 1〜3 は方式決定済み。★**①(a) が決まれば 4 も従属して決まる**。★★**2026-08-27: ①(a) が決まった。→ やること 4 も従属して決まる段階に入った**★★ |

---

## 6. 残務と、この文書が開いた論点

### 6-1. A-0 —— 括弧なしの旧番号

★★**箇所数を書かない。**★★ 書き換えが進めば必ず古くなる
（**D-5** 自身が「箇所数を書かない。増え続けるため」と書いているのと同じ状況で、
★**ここに数を書けば偽の断定をもう 1 つ足すことになる**）。

★**正は走査である。**

```bash
grep -rnE '(^|[^A-Za-z0-9_])P[1-5]([^0-9A-Za-z_]|$)' --include=*.dart loveca-*/lib loveca-*/test | grep -v '\.g\.dart'
```

★★**この走査は判定を返さない。**★★
出力には**同期の設計番号**と **UI のペイン番号**（`docs/UI設計メモ.md` §2-3）が混ざっており、
★**分けられるのは人だけ**である。→ ★**生の出力を見て、その場で分けること。**
★**「表と一致するか」で確かめない** —— ★**同じ思い込みで表と判定の両方を作れば一致してしまう**
（**D-15 (j)**「実装と doc が同じ誤解を共有していると、突き合わせても一致する」）。

★★**発見当時（2026-08-27）の生の出力は `docs/セッション報告/2026-08-27_束A.md` §3 に残してある。**★★
★**判定を挟んでいない**ので、次に読む人が自分で分けられる。★**箇所数はそこにも書かない。**

★**選択肢（★方式はここで決めない）**

| 案 | 代償 |
|---|---|
| 括弧なしも書き換える | ★**UI のペイン番号を壊さないよう 1 件ずつ人が判定する**必要がある |
| ★**走査を括弧なしまで広げる** | ★★**UI のペイン番号を除外する必要が出て、D-30 の穴を踏む**★★（除外を足すと免除の一覧が増え、★**その一覧自身が古くなる**） |
| 放置する | ★**D-5 が着手前に判断すると定めた理由（`P4` とは何かに突き当たる）が解消しない** |

### 6-2. ★ U32 は束 A と同時にやれる

**U32**（コード中の決定番号・設計番号が `docs/決定事項一覧.md` に実在するかをテストで突き合わせる /
**D-29** の検知手段）は、束 A が台帳を触る作業なので**同じ機会に置ける**。

★★**ただし判断項目ではなく検知手段の実装である。**★★ §1 の表には載せない。

### 6-3. ★ U31 の具体的な中身（★内容は写さない）

★**§0-3 の事実 4 は `U31` の実物である。**

> ★**原典 `docs/引き継ぎドキュメント.md` §1.2「確定済み」に、
> `docs/決定事項一覧.md` に採番されていない決定群がある。**

★★**書くのはここまでで、技術名は写さない。**★★
写せば**台帳の外の決定を、根拠なしに台帳の中へ持ち込む**ことになる
（`P1`〜`P5` とまったく同じ形であり、★**U31 が「追跡下にある = 検証済み と読まないこと」と
書いているとおり**である）。

★**同じブロックに「13フェイズ」という既知の誤りが同居している**ことも、
根拠に使わない理由として記録しておく。

---

## 7. ★ 運用の記録

### 7-1. 「利用者にしか答えられない論点が 3 つを超えたら止まる」は空振りした

★**5 つ挙がったが、3 つは従属しており、独立した論点は 2 つだった**（§5）。
→ ★★**数で止めると、数える前に分離できていない場合に働かない。**★★

> ★★**2026-08-27 訂正（★§7-1 自身が「条件の訂正は次のセッション」と書いていた。これがそれである）**★★
>
> ★★**条件を「数」から「分離してから数える」に変える。**★★
>
> | | |
> |---|---|
> | ~~旧~~ | ~~利用者にしか答えられない論点が **3 つを超えたら**止まる~~ |
> | ★**新** | ★**論点が出たら、まず**従属していないか**を判定する。★**独立と判定できたものだけを数える。**そのうえで数が閾値を超えたら止まる** |
>
> ★**数そのものをやめない。**★**やめると「いくつまでなら聞いてよいか」の目安が消える。**
> ★★**変えたのは「数える前に分離する」を条件の一部にしたことである。**★★
> ★**今回この手順を踏んだ結果、5 → 2 に減り、閾値に触れなかった。**
> → ★**分離が働けば、そもそも止まる場面は減る。**

### 7-2. 指示の書き方が読み分けられなかった

★**同じ依頼の中で【1】が「D-5 本文の訂正も次のセッション」と書き、
【2】が「D-5 の完了報告を未完了に戻す / ★一部だけ直さないこと」と書いていた。**
→ ★**「D-5 の訂正」が、分析の訂正と偽の完了報告の巻き戻しの
どちらを指すかが字面から決まらなかった。**
★**具体的な側（【2】）を採り、対象を後者に限ると解釈して確認を取った。解釈は正しかった。**
★★**確認が要った時点で記録に値する**★★ —— 同じ書き方をすればまた止まる。

### 7-3. ★★ 相談役の作法 ★★

> ★★**相談役の指示に含まれる決定番号・行番号・引用は、実物を確かめてから使うこと。**★★

★**根拠**: 相談役の指示に書かれた前提が実物と食い違った件が積み上がっている
（★**型は §9-3 の表に残してある**）。★**結論を変えたものもある**ので、
確かめずに書いていれば誤った論点が立っていた。

★★**件数をここに書かない。★正は §9-3 の表であり、数えたければ行を数えること。**★★

```bash
sed -n '/^### 9-3\./,/^### 9-4\./p' docs/同期設計メモ.md | grep -c "^| "   # ★見出し行を 1 引く
```

★**ここには「2026-08-27 の指示で 4 件」「うち 2 件は結論を変えた」と書いてあった。**
★★**2026-08-28 に数え直したら、どちらも偽になっていた。**★★
★**§9-3 に 5 と 6 が足されたときに、この 2 か所が更新されなかった**
（★**どちらも 2026-08-27 の指示であり、「4 件」の範囲の中である**）。
★★**§9-3 は 1 つ下で「見出しに件数を書かない —— 足すたびに 2 か所を直すことになる」と
定めている。★その規約が 1 つ上のここに適用されていなかった。**★★
★**型は `ルール整合性チェック_v1.06.md` の D-15**（同じ数を 2 箇所に書いた）。
★**新しい数を書き直さない**（★**書けばまた古くなる** / §3-1 と同じ処置）。

★**1 件ずつ直すと同じことを繰り返す**ので、作法として置く。
★★**「相談役が間違えた」ではなく「引くべき文書を引かずに書いた」が原因である。**★★

#### ★★ 2026-08-29 追記: 「実物」には★自分が書いた条件も含まれる ★★

> ★★**節が条件つきで書かれているときは、★その条件を当ててから結論を使うこと。**★★

★**事実**（**決定 D108** の検算で出た / 2026-08-29）——
★**相談役が「(f-3) の軸 B『各端末が独立に解く』は落とす」と推奨した。**
★**根拠に挙げたのは §13-5 の「D99 の比較器はマスタを引く」である。**

★★**§13-5 は条件つきで書いてある。**★★

> ★**「解決の規則がカードマスタを参照するなら、軸 B の『独立に解く』は成立しない。**
> ★**参照しない規則なら成立しうる。★どちらかはまだ決まっていない。」**

→ ★★**確かめるべきは「★この推奨の解決の規則がマスタを参照するか」だった。★確かめていない。**★★
→ ★**実読すると★参照しない**（★推奨の 3 段は D99 の比較器を 1 度も呼ばない / **§14-7**）。
→ ★★**落とす根拠は成立していなかった。**★★

★★**上の本文と型は同じである。★対象だけが違う。**★★

| | 何を確かめずに使ったか |
|---|---|
| ★**本文**（2026-08-27 まで） | ★**他人が書いた実物**（決定番号・行番号・引用） |
| ★★**この追記**★★ | ★★**自分が書いた節の★条件**★★（★**§13-5 は相談役自身が前日に書いた節である**） |

→ ★★**自分で書いた節ほど、条件を落として結論だけを引きやすい。**★★
★**書いた本人は「何を書いたか」を覚えているが、★「どんな条件を付けたか」は覚えていない。**
★**§7-9 と混ぜない** —— ★**あちらは「表から抜くとき」、★こちらは「条件つきの結論を使うとき」である。**

### 7-4. ★★ 相談役の作法 —— 分離した問いが、他の推奨の前提になっていないか ★★

> ★★**複数の問いを同時に投げたら、1 つの答えが別の問いの推奨を動かさないか確かめること。**★★

★**§7-3 と同じ扱いで「相談役の作法」として置く。**

#### ★ 事実

★**相談役は当初、N-7 の問い 2 を「保管庫」とだけ推奨した。**
★**そのとき問い 3（利用者は 1 人か）が「1 人」で暗黙に前提されていた。**
★回答 3 が「**複数**」だったため、推奨は
「**保管庫（衝突解決はアプリ側）**」と「**アクセス制御はサーバー**」に分けられた
（→ **D105-2** / **D105-3**）。

#### ★★ 原因 —— 分離したこと自体が見落としを作った ★★

★**相談役は問い 1・2・4・5 に推奨を出し、★問い 3 だけ「これは答えてもらう必要があります」と
分離した。**
→ ★★**分離したことで、3 が他の推奨の前提になっていることを見落とした。**★★

★**分離の判断そのものは正しい**（問い 3 は利用者にしか答えられない / §5-2 の判定と同じ作法）。
→ ★★**正しい分離が、そのまま盲点になった。**★★
★**機構の向きは D-31 と同じ**（★**走査を安全にするために狭めたことが、そのまま盲点になった**）。

#### ★★ §9-3 の型との違い —— 検知手段が違う ★★

| | **§9-3**（→ §7-3） | ★**§7-4** |
|---|---|---|
| 何が違っていたか | ★**指示に書かれた決定番号・行番号・引用が実物と違う** | ★**書かれていないものが効いていた**（暗黙の前提） |
| 検知手段 | ★**実物を読めば判定できる**（★**§7-3 の作法がそれ**） | ★★**読んでも当たらない**★★ —— ★**どの文書にも書かれていない** |
| だから | ★**作法 = 実物を引く** | ★★**作法として明示する以外に検知手段が無い**★★ |

★**2 つを 1 つの作法にまとめない。**★**引けば直るものと、引いても出ないものを混ぜない。**

### 7-5. ★ 訂正に引き金を書いておくと巻き戻せる（★★成功例★★）

★**§2-3 の 2026-08-27 の訂正は、こう書かれていた** ——

> ★**先に決めると N-7 の答え次第で決め直しになる**

★★**自分を巻き戻す条件を、文中に書いていた。**★★
→ ★**N-7 が返った時点で、機械的に巻き戻せた**（§11-3 の表）。

★★**ここでは D-15 (l) が起きなかった。**★★
★**(l) は「前提が動いたのに doc が追随しない」型**だが、
★**この訂正は「何が動いたら自分が古くなるか」を自分で名指ししていた。**

→ ★★**可能性を根拠に訂正するときは、その可能性が閉じる条件を同じ文に書くこと。**★★
★**「〜が加わりうる」だけ書いて引き金を書かないと、
可能性が閉じたときに巻き戻す人がいない**（★**誰も間違えていないのに古くなる**）。

### 7-6. ★★ 相談役の作法 —— 箇所数を渡すときは走査した範囲も渡す ★★

> ★★**箇所数を渡すときは、走査した範囲も併せて渡すこと。**★★
> ★★**範囲を言わない箇所数は、次の人に「それで全部」と読ませる。**★★

★**根拠**: 2026-08-27 の「doc が扱う決定の範囲」の訂正で、★**相談役は箇所数だけを渡した**
（数は `28e9843` の件名に残っている）。★★**走査は `CLAUDE.md` に限られていた**★★ ——
★**同じ断定が別の文書にもあることを確かめていない。**
→ ★**受け取った側は 1 か所ずつ実物を確かめたが、範囲そのものは疑わなかった。**
★**実際に別の文書へ残っており、次のコミットの検算で出た。**

★**同型**: **D-5** の完了報告（★**走査を丸括弧つきに限ったので、括弧なしが残った** / **D-31**）。
★**§7-3 と混ぜない** —— ★**7-3 は「渡された実物が違う」**、
★**ここは「渡された数は正しく、その数が何を数えたかが言われていない」**。

★**新しい数をここに書かない**（★**直せば古くなる** / §3-1）。★**正は走査そのものである。**

#### ★★ 追記 —— 規約を書かせた直後に、同じことが起きた（2026-08-27）★★

★**§7-6 を書かせた次の指示で、★相談役はまた対象の行だけを渡した**
（`c6fd0a5` が直したもの）。★★**走査範囲は言われていない。**★★
→ ★★**規約を書くことと、規約に従うことは別である。**★★
★**書いた本人が直後に踏むのだから、「知らなかった」では説明がつかない。**
★**§7-3 の「実物を確かめる」と違って、これは読めば直るものではない。**
→ ★★**いま効いている手当ては 1 つだけ —— 受け取った側が走査範囲を自分で決め、
その範囲を報告に書くこと。**★★（★**実際にそれで同型がさらに出た。**）

★★**同じ機構は §7-8 でも起きた**★★（★**規約を書かせた次の指示で、その規約が破られた** / 2026-08-28）。
★**中身はそちらにある。★検知手段が違うので棚は分けてある**（§7-4 / §7-7）。★**件数はここで数えない。**

#### ★ 走査範囲（★次に同じ走査をする人が再現できるように）

★**2026-08-27 の「doc が扱う決定の範囲」の訂正は、★★リポジトリ全体の `.md`★★ を走査した結果である。**

```bash
git grep -nE "D[0-9]+〜D[0-9]+" -- '*.md'      # ★範囲はこれ。件数は書かない
git grep -nE "D[0-9]+〜D[0-9]+" -- . ':(exclude)*.md'   # ★対で一度は見ること
```

★★**非 0 が正常である。**★★ 当たりの大半は**台帳の節見出し**・**採番の正**・
**マイルストーンの完了記録**・**出自の主張**（`spike/`）であり、
★**直す対象は「その doc が扱う決定の範囲」を断定している行だけ**である。
→ ★★**分けるのは人である**（**D-30**。走査の出力からは区別できない）。

### 7-7. ★★ 相談役の作法 —— 同型としてまとめる前に、1 件ずつ実物を確かめる ★★

> ★★**同型としてまとめる前に、1 件ずつ実物を確かめること。**★★

★**§7-3 / §7-4 / §7-6 と同じ扱いで「相談役の作法」として置く。**

#### ★ 事実（★実測。★走査を再現できる形で残す）

`28e9843`（「`CLAUDE.md` §8 の doc が扱う決定の範囲 4 か所を消す」）で扱った 4 か所は、
★**相談役から「同型（範囲が古くなった）」としてまとめて渡された。**
★★**1 件だけ型が違った。**★★

```bash
grep -cE '(^|[^0-9A-Za-z_])D4[2-8]([^0-9]|$)' docs/UI技術検証メモ.md   # ★0
```

| doc | 書かれていた範囲 | 実測 | 型 |
|---|---|---|---|
| `docs/UI設計メモ.md` | 決定 D52〜D71 | ★**D52〜D71 は全件実在**（★**範囲の外にも多数**） | ★**A: 範囲が古くなった** |
| `docs/盤面設計メモ.md`（2 か所） | 決定 D73〜D79 | ★**D73〜D79 は全件実在**（★**実際は D72〜D98**） | ★**A** |
| `docs/UI技術検証メモ.md` | 決定 D42〜D48 | ★★**0 件。素の字面すら無い**★★ | ★★**B: 最初から誤った参照**★★ |

#### ★★ 機構 —— 処方が同じだと、型の違いが結果から検出できない ★★

★**処方は 4 か所とも同じ（範囲そのものを消す）。★だから結果は 4 か所とも正しい。**
→ ★★**型が違っても、結果を見るかぎり何も起きていないように見える。**★★

| | ★**型 A** | ★★**型 B**★★ |
|---|---|---|
| 参照先 | ★**正しい。範囲だけが狭い** | ★★**そもそも指す先が無い**★★ |
| ★**書き直せば救えるか** | ★**救える** | ★★**救えない**★★ |
| 再発の原因 | ★**増え続けるものに範囲を書いた** | ★**引用元を確かめずに範囲を書いた** |
| ★**次に同じ型が出たときの手当て** | ★**範囲を書かない** | ★★**引用を書く前に引用元を開く**★★ |

★★**`28e9843` のコミットメッセージは型 B を正しく記録している**★★（「素の字面すら 1 件も無く」）。
→ ★**失われたのは実測ではなく、「4 件を同型としてまとめた」という判断の側である。**

#### ★ §7-3 / §7-6 / §7-4 と混ぜない（★検知手段が違う）

| | 何が違っていたか |
|---|---|
| **§7-3** | ★**渡された実物が違う**（決定番号・行番号・引用） |
| **§7-6** | ★**渡された数は正しく、何を数えたかが言われていない** |
| **§7-4** | ★**書かれていないものが効いていた**（暗黙の前提） |
| ★★**§7-7**★★ | ★★**渡されたものは全部正しく、まとめ方が誤っていた**★★ |

★**型としては §7-4 / D-31 の系である**（★**正しく見える抽象化が、そのまま盲点になる**）。
★★**ただし §7-4 は「読んでも当たらない」もので、こちらは「1 件ずつ開けば当たる」。**★★
★**検知手段が違うので同じ棚に置かない**（§7-4 が自ら定めた分け方に従う）。

### 7-8. ★★ 相談役の作法 —— 問いの形が答えの形を決めていないか ★★

> ★★**二択で聞く前に、「どちらでもない」が答えになりうるかを確かめること。**★★

★**§7-7 と同じ回（2026-08-27 / 決定 **D106**）に出た。★別の型なので節を分ける。**

#### ★ 事実

★**相談役は指示【2-2】で、`CLAUDE.md` §1 の境界 1 について
「デッキ同期にも適用されると読めないか確かめ、★**読めるなら**読めない形に直す必要があるかを論点にする」
と書き、★★止まる条件に「境界 1 の記述を直す必要が出た場合」を置いた。**★★
→ ★**選択肢は「読める（直す）」と「読めない（何もしない）」の 2 つになっていた。**

★★**実際の答えはどちらでもなかった。**★★

| | 中身 |
|---|---|
| ★**境界 1 は読めない** | → ★**`CLAUDE.md` §1 は直さない**（根拠 4 つは §10 の **N-14**） |
| ★★**しかし §1 は禁じてもいない**★★ | → ★★**柵は別の場所に要る**★★（**N-14** / **D106-3**） |

→ ★**「読めない」で答えを終えていれば、★柵が要ることが丸ごと落ちていた。**

#### ★★ §7-4 との違い —— 問いの分け方か、問いの形か ★★

| | **§7-4** | ★★**§7-8**★★ |
|---|---|---|
| 何が原因か | ★**問いを分離したこと**（分離した 1 つが他の推奨の前提だった） | ★★**問いの形**★★（★二択が答えの空間を狭めた） |
| 何が落ちたか | ★**暗黙の前提**（どの文書にも書かれていない） | ★★**選択肢そのもの**★★（「直さないが、別の場所に要る」） |
| ★**検知手段** | ★★**読んでも当たらない**★★ —— 作法として明示する以外に無い | ★**答える側が二択の外に出れば当たる** |
| ★**だから** | ★**分けた問いが他の推奨の前提でないか確かめる** | ★★**「どちらでもない」が答えになりうるかを確かめる**★★ |

★**2 つを 1 つの作法にまとめない**（★**§7-4 が自ら定めた分け方に従う** —— ★**引けば直るものと、引いても出ないものを混ぜない**）。

★★**この作法は答える側にも効く。**★★
★**二択で聞かれても、二択の外が答えなら外を答えること。**★**問いの形は答えの形を決めない。**

#### ★★ 追記 —— 規約を書かせた次の指示で、また二択で聞かれた（2026-08-28）★★

★**§7-8 を書かせた次のセッションの指示【1-1】は、こう書かれていた** ——

> ★**(f-1) と Q1 が混ざっていないか確かめる**

★★**これは「混ざっている / 混ざっていない」の二択である。**★★
★**実際の答えはどちらでもなかった** —— ★★**混ざってはいない。★間に 1 つ抜けていた。**★★
（★**「両側とも変わったか」を問う衝突判定に番号が無い** / §12-4）

→ ★**「混ざっていない」で答えを終えていれば、★抜けているものが丸ごと落ちていた。**

★★**§7-6 の追記とまったく同じ構図である**★★ ——
★**規約を書かせた直後に、書いた本人がその規約を破った。**
★**§7-6 が「規約を書くことと、規約に従うことは別である」と既に書いており、
★ここはその 2 例目にあたる。**
→ ★**§7-6 には 1 行だけ指す。★中身は写さない**（★**検知手段が違うので棚は分ける** / §7-4 / §7-7）。

##### ★ なぜ §7-6 ではなくここに置いたか（★判断した理由）

★**候補は 2 つあった。**

| 候補 | 何を根拠にするか |
|---|---|
| **§7-6** の追記に足す | ★**再発の機構**が同じ（「規約を書かせた直後に、書いた本人が破った」） |
| ★★**§7-8**（ここ）★★ | ★★**検知手段**が同じ（「答える側が二択の外に出れば当たる」）★★ |

★★**この文書は棚を「検知手段」で分けると自分で定めている。**★★
§7-4 が「★**引けば直るものと、引いても出ないものを混ぜない**」と書き、
§7-7 が「★**検知手段が違うので同じ棚に置かない**（§7-4 が自ら定めた分け方に従う）」と繰り返している。
→ ★**今回の事象の検知手段は §7-8 のものである。**
★**§7-6 の検知手段（受け取った側が走査範囲を自分で決めて報告する）は、今回まったく働いていない。**
→ ★★**「再発の機構が同じ」で §7-6 へ寄せると、機構で分けたことになり、自分で定めた分け方に反する。**★★

##### ★★ 検知手段は 1 つしかない ★★

★**今回効いたのは「二択で聞かれた側が『どちらでもない』を答えとして返せた」ことだけ**である。
★★**相談役の側に検知手段は無い**★★（★**§7-6 の結論と同じ** —— ★**読めば直るものではない**）。

#### ★★ 追記 —— 二択で聞いてよいと判定してから聞いた（2026-08-28 / ★★成功例★★）★★

★★**上の追記とは向きが逆である。**★★
★**規約を破った記録ではなく、★規約を適用した記録である**（★**先例は §7-5** —— ★**成功例も残す**）。

##### ★ 事実

★**相談役は、利用者の第 1 の回答（「最新の履歴に合わせてください」）に対して
「消えてよいか / 消えるのは困るか」という★二択で聞き直した**（★経緯は §9-6 の段 1〜4）。
★★**聞く前に「どちらでもない」が答えになりうるかを確かめている。**★★

| 確かめたこと | 結果 |
|---|---|
| ★**軸に中間があるか** | ★**無い。**★**「消える」か「消えない」かの 2 値で、★間の状態が定義できない** |
| ★**「場合による」が落ちていないか** | ★**落ちていない。**★**§12-8 の問い 1 が既に「場合による」を提示しており**（★名前だけ違うとき / 入っているカードが違うとき）、★**利用者はそれを選ばずに答えている** |

→ ★**この二択は §7-8 の型に当たらない。**

##### ★★ ただし、答えは二択の外に出た ★★

★★**利用者は「困る」と答えたが、既定は「最新の履歴に合わせる」のまま残った。**★★
→ ★**返ってきたのは「消えてよい」でも「消えない」でもなく、
★★「消えてよい。ただし黙っては困る」★★である。**

| | 何が起きたか |
|---|---|
| ★**軸そのもの** | ★**中間は無かった。★判定は正しい** |
| ★★**答え**★★ | ★★**軸を 1 本足した**★★（★「消えるか / 消えないか」に、★**「黙ってか / 知らせてか」**が足された） |

→ ★★**「中間が無いことを確かめた」だけでは §7-8 を満たしたことにならない。**★★
★**§7-8 が警戒しているのは「選択肢が落ちること」であって「中間が無いこと」ではない。**
★★**今回それを救ったのは、答える側が軸を足したことである**★★
（★**§7-8 が「この作法は答える側にも効く」と書いた、まさにその働き方**）。

##### ★★ 検知手段 —— 上の追記と同じく、相談役の側には無い ★★

★**「軸に中間があるか」は聞く前に確かめられる。**
★★**「軸が足りないか」は確かめられない**★★ —— ★**足りない軸は、足されるまで名前を持たない。**
→ ★**確かめてよい。★★確かめたことを「安全になった」と読まないこと。**★★
★**§7-8 の本文の結論（「相談役の側に検知手段は無い」）は 1 文字も弱まらない。**

---
### 7-9. ★★ 相談役の作法 —— 表から案を抜くとき、抜いた結果を先に見ない ★★

> ★★**案の一覧から「◯◯を満たす案」を抜くときは、★抜く前に全欄の字面を並べること。**★★
> ★★**そして、自分が立てた絞り込み基準は★全案に当てること。★落とした案だけに当てない。**★★

★**§7-3 / §7-4 / §7-6 / §7-7 / §7-8 と同じ扱いで「相談役の作法」として置く。**

#### ★ 事実（2026-08-29 / **決定 D108** の検算で出た）

★**相談役が §13-2 の 7 案から推奨を出した。★理由の 1 行目は**
★★**「D107-1 後半を単体で満たせるのは案 3 / 4 / 7 だけ」**★★**である。**

★**誤りが 2 つ、同じ 1 つの行に入っていた。**

| # | 何を誤ったか |
|---|---|
| **(a)** | ★★**表を横に読むと言いながら、列の語が揃っているか確かめなかった。**★★ ★**「満たせる」の字面で抜いた**が、★**案 6 の欄は「最も精度が高い」で、同じ軸の語ではない**（★**意味では案 4 より強い**） |
| ★★**(b)**★★ | ★★**自分が立てた絞り込み基準と、自分の結論が食い違っていた。**★★ ★**基準は 3 つ**（N-11 を本線へ昇格させるか / N-14 に掛かるか / **N-10 は全案共通なので新しい依存ではない**）。★**一貫して当てると残るのは {案 4, 案 6} である** |

★**(b) のほうが重い。**
★**(a) は「1 案が視野に入らなかった」だが、★(b) は「視野に入れても同じ結論にならない」**。
→ ★★**(a) を直しても (b) は直らない。★2 つ書く。**★★

#### ★★ 機構 —— 抜いた結果は、抜き方の誤りを隠す ★★

| | |
|---|---|
| ★**抜く前に全欄を並べる** | ★**欄の語が揃っていないことが★目に入る**（★**案 6 だけ別の語**） |
| ★★**抜いた結果だけを見る**★★ | ★★**「3 / 4 / 7」は 3 つとも正しい。★正しいものしか残らないので、★落ちたものは見えない**★★ |

→ ★★**偽陰性は、結果を検算しても出てこない。★「3 / 4 / 7 は本当に満たせるか」を確かめても全部通る。**★★
★**捕まえるには「どの欄が★どの語だったか」を 1 件ずつ並べ直すしかない。**

★**(b) の機構は別である** —— ★**基準を書いた順序が効いている。**
★**相談役は「落としたい案」に基準を当てて落とし、★残した案に当て直していない。**
→ ★★**基準は「落とすため」に使われ、「残すため」には検算されなかった。**★★

#### ★ 検知手段（★**部分的にある**）

| 手当て | 効くもの |
|---|---|
| ★★**抜く前に全欄の字面を並べる。★抜いた結果を先に見ない**★★ | ★**(a)** |
| ★★**絞り込み基準を書いたら、落とした案だけでなく★全案に当てた表を出す**★★ | ★**(b)** |

★★**どちらも自動化できない**★★ —— ★**`.md` を見る機械はこのリポジトリに 1 つも無い**（`CLAUDE.md` §8）。
★**残るのは「生の出力を残す」ことだけである**（★**型は `ルール整合性チェック_v1.06.md` **D-15 (j)** / **D-31**）。

#### ★★ **D-35** と混ぜない —— 書く側と読む側である ★★

| | どちら側の話か | 手当て |
|---|---|---|
| ★**D-35** | ★★**書く側**★★ —— ★**比較表の欄を、宣言した物差しの語で揃える** | ★欄の語彙を先に決める。★**警告を隣に置く** |
| ★★**§7-9**★★ | ★★**読む側**★★ —— ★**揃っていない表から抜いてしまう** | ★**抜く前に全欄を並べる。★基準を全案に当てる** |

→ ★★**表が正しく書かれていても、読む側は同じ誤りを起こしうる**★★（★**逆も真** —— ★D-35 の手当てだけでは (b) は防げない）。
★**2 つで 1 対である。★片方だけ適用しないこと。**

#### ★ §7-3 / §7-6 / §7-7 と混ぜない（★検知手段が違う）

| | 何が違っていたか |
|---|---|
| **§7-3** | ★**渡された実物が違う**（決定番号・行番号・引用） |
| **§7-6** | ★**渡された数は正しく、何を数えたかが言われていない** |
| **§7-7** | ★**渡されたものは全部正しく、まとめ方が誤っていた** |
| ★★**§7-9**★★ | ★★**渡されたものは全部正しく、★抜き方が誤っていた**★★（★**しかも抜いた結果は正しい**） |

★**§7-7 に最も近いが、★向きが逆である** ——
★**§7-7 は「違うものを同じとしてまとめた」、★§7-9 は「同じ軸で読めないものを同じ軸で読んだ」。**
★**§7-7 は 1 件ずつ開けば当たる。★§7-9 は開いても、★結果だけ見ていれば当たらない。**


## 8. ★ この文書の完了条件

★**論点表としての完了条件は「束 A の全項目に §1 の欄が埋まっていること」**であり、
★★**方式が決まっていることではない。**★★

★**決定が出たら、実体は `docs/決定事項一覧.md` に置き、この文書からは番号で参照する。**
★**この文書に決定文を書かない**（索引と内容を二重に持たない）。

★★**§9 以降は 2026-08-27 の回答ぶんである。**★★
★**節の順は「完了条件」で終わっていない** —— ★**番号を振り直すと `CLAUDE.md` §8 などの
既存の参照（§1 / §2-1 / §6-3 …）が全部ずれる**ので、**足すほうを後ろに置いた。**

---

## 9. ★ 利用者の回答（2026-08-27）

★★**回答そのものはここが正。決定としての実体は `docs/決定事項一覧.md` の **D103** / **D104** である。**★★
★**この節に決定文を書かない**（§8）。★**ここに置くのは「何を聞き、何が返り、何が返らなかったか」**である。

### 9-1. 回答そのもの

| 問い | 回答 |
|---|---|
| ①(a) 同期の対象にカードマスタの配信を含むか | ★**含める。★差分でダウンロードする** |
| ①(b) ゲームの対戦記録を含むか | ★**含めない** |
| ①(c) 設定を含むか | ★**アカウントごとに揃える**（★**端末ごとではない**） |
| ①(d) 画像を差分で配るか | ★**差分にする** |
| ② 認証は何のためか | ★**アカウント制。★サーバーは利用者が用意する** |

★★**返らなかったもの**★★ —— ★**聞いていないので返っていない**、が正しい読み方である。

| 何 | なぜ返っていないか |
|---|---|
| ★**衝突解決の方式**（①(f)） | ★**§5-1 で「(f)」として挙げてはいたが、問いの文言に入っていなかった**（★§5-1 ①の「問いの文言」を見よ）。★**②の答えが「必須」に変えた** → **N-5** |
| ★**設定のどの項目を揃えるか** | ★**利用者が名指ししたのは「既定のエネルギーカード」だけ** → **N-6** |
| ★**サーバーに何を前提してよいか** | ★**②の問いの文言に入っていなかった** → **N-7** → ★★**2026-08-27 に第 2 回として聞き、5 問とも回答を得た**★★（**§9-4** / **決定 D105**） |

### 9-2. 法務の記録（★決定 **D104**）

| 欄 | 中身 |
|---|---|
| **判断** | ★**利用規約を確認し、専門家の確認を得た判断** |
| **時点** | ★★**2026-08-27 時点**★★ |

★★**「解決済み」とだけ書かない。**★★ ★**依存が 2 つ残る。**

1. ★**利用規約は改定されうる。根拠が規約である以上、改定時は引き直しが要る**
2. ★★**確認の範囲を超える形になりうる**★★（★**2026-08-27 に「未記録」から書き換えた**）——
   「**利用規約の確認は、利用者以外がサーバー経由で利用する形を想定した範囲で行われた
   （2026-08-27 時点）**」。★**`CLAUDE.md` §0 とは食い違わない。**
   ★**人数の上限も公開の可否も述べられていない**ので、そこを超えたら引き直し。
   ★★**根拠と経緯の正は `docs/決定事項一覧.md` の D104 の詳細**★★（★ここに写さない）

★★**依存 2 を `D-D` に結び付けない。**★★
★**`D-D` は「`loveca_core` を Flutter に依存させない」**（`docs/決定事項一覧.md` §2）であって別物である。
★**追跡下の正は `CLAUDE.md` §0 の 1 行**（「個人利用・非商用の範囲で開発する」）。

★**技術的可否と法的可否を分けて書く規約（`CLAUDE.md` §7-6）は維持する。**
★★**外したのは A-5 に付いていた札 1 つだけ**★★であり、
★**`loveca-data/README.md` の「robots.txt に書かれていないこと ≠ 許可」も §4 のアクセス制約も 1 文字も緩めていない。**

### 9-3. ★★ 指示の前提が実物と食い違った件 ★★

★**この節は §7-3（作法）の根拠である。**★**個々の訂正は済んでいるので、ここには型だけ残す。**

| # | 指示の記述 | 実物 | 確かめ方 |
|---|---|---|---|
| 1 | 「**D-D**（非公開・個人利用）」 | ★**D-D は「`loveca_core` を Flutter に依存させない」** | `docs/決定事項一覧.md` §2 の表 |
| 2 | 「`loveca-data/README.md:16` の**「画像の自前配信は未決」**」 | ★その行の字面は「**Phase 0（権利・利用規約の確認）を完了させてください**」。画像の自前配信を名指しするのは**原典側** | 当該行を読む |
| 3 | 「**D43** と ①(a) は正面から矛盾しうる」 | ★★**矛盾しない**★★。D43 は「**Phase 4 で配信経路が固まるまで**」と**自ら期限を切っている** | D43 の本文 |
| 4 | 「(a) が差分 →**現運用の「丸ごと差し替え」から変わる**」 | ★**コードは既に差分**（`planUpdate` がファイル単位でハッシュ比較 / `MasterFileSource` は Phase 4 の HTTP を見越した抽象）。「丸ごと差し替え」は**人が dist ディレクトリを置き換える運用**の話 | `master_data.dart` / `master_file_source.dart` |
| ★**5**（2026-08-27 / ★**N-7 の回答時**） | 「**N-6 の** `lastDeviceId`「何を指すか未決」」 | ★**`lastDeviceId` の未決は §5-1 ②の「決まっていないこと」に在る。**★**N-6 は設定の同期範囲**で、`AppSettings.distDir` と `settings.json` の話しか書いていない | §5-1 ② と N-6 の両方を読む |
| ★**6**（同上） | 「4 が「同時に上げない」なので **A-6（D-7）が生きる**」 | ★★**A-6 は既に発火済み**★★ —— §1 の A-6 の「依存」欄が「→ 2026-08-27 に満たされた（**D103-1**）」と書いている。★**回答 4 が足したのは発火ではなく「もう 1 つの軸」**（**N-12**） | §1 の A-6 の行 |
| ★**7**（2026-08-28 / ★**N-5 の分解時**） | 「**D-9 / D-26 / D-14（A-1 / A-2 / A-3）が N-5 の下流**である。§11 がそう書いている」 | ★**A-1 / A-2 は下流。**★★**A-3 は違う**★★ —— §11-1 / §11-5 は **A-3 の「向き」を Q2（並行 1）**に置き、本線に従属するのは**「優先度」だけ**である（★§11-5 が「**向きと優先度を混ぜない**」と自ら書いている） | §11-1 の図と §11-5 のレーン 1 |

★★**どれも「実物を読めば分かる」ものである。**★★
★**1 件ずつ直しても同じことが起きる**ので、作法として §7-3 に残した。

★★**見出しに件数を書かない**★★ —— ★**足すたびに見出しと表の 2 か所を直すことになる**
（`ルール整合性チェック_v1.06.md` **D-15** の規約 3「同じ数を 2 箇所に書かない」）。
★**先例は `CLAUDE.md` §8 の「廃止を記録している 3 つの文書」**（★**数を消した側**）。
★**表が正であり、数えたければ行を数えること。**

★**3 と 4 は結論を変えた** —— 3 は「矛盾するなら論点として立てる」という指示の分岐を
**立てない側**へ倒し（代わりに **N-1** を立てた）、4 は「差分は新設」という読みを
**点火**へ変えた（★**D103-2 に書いてある**）。

★**6 も結論を変えた** —— 「発火した」と書いていれば **A-6 の状態を二重に発火済みと記録**し、
★**回答 4 が本当に足したもの（N-12 の軸）が丸ごと落ちていた。**
★**5 は結論を変えていない**（論点そのものは実在した）。★**場所だけが違った。**

### 9-4. ★★ N-7 の回答（2026-08-27 / 第 2 回）★★

★**§9-1 とは別の回で聞いたので節を分ける**（★**同じ表に混ぜると「いつ返ったか」が消える**）。
★★**回答そのものはここが正。決定としての実体は `docs/決定事項一覧.md` の **D105** である。**★★

| 問い | 回答 |
|---|---|
| 1 常時動いているか / 落ちているあいだ編集を続けられるべきか | ★**常時稼働は前提にしない。編集は続けられる**（★**手元に貯めて後で送る**） |
| 2 サーバーは「保管庫」か「判断する側」か | ★**保管庫。**★**ただしアクセス制御（誰のデータか）はサーバーが持つ。**★**衝突解決（どちらが新しいか）はアプリ側** |
| 3 使うのは 1 人か、他の人も使うか | ★**利用者は複数。開発者 1 人ではない** |
| 4 サーバーとアプリを同時に上げられるか | ★**同時に更新できる前提にしない。**★**古いサーバーに新しいアプリがつながる状況はある** |
| 5 通信の失敗を利用者に見せるべきか | ★**見せる。**★**ただし編集は止めない** |

★**あわせて、`D104` の確認の範囲についても回答を得た**（★**問い 3 の裏返し**）——
「**他の人が使うことも想定した確認だった**」。→ ★**実体は `docs/決定事項一覧.md` の D104 の詳細「確認の範囲」。**

#### ★★ 回答 2 は 1 行にまとめない ★★

★**「保管庫」と「アクセス制御はサーバー」は理由が違う。**
★**まとめると、どちらが何を根拠にしているかが消える** —— 理由の正は
`docs/決定事項一覧.md` の **D105-2 / D105-3**（★ここに写さない）。

#### ★★ 返らなかったもの —— 0 件 ★★

★**5 問とも返った。**★**§9-1 のときと違い、聞いていない項目が残っていない。**
★**ただし「答えが出た＝方式が決まった」ではない** —— ★**開いた論点は §10 の N-10〜N-13。**

---

### 9-5. ★★ 第 3 回の回答（2026-08-27 / 決定 **D106**）★★

★**§9-1 / §9-4 とはさらに別の回である**（★**回ごとに節を分ける** —— ★**同じ表に混ぜると「いつ返ったか」が消える**）。
★★**回答そのものはここが正。決定としての実体は `docs/決定事項一覧.md` の **D106** である。**★★

| 何が返ったか | 回答 |
|---|---|
| ★**サーバーの同一性**（★**問いに立てていない**。**D105-7** が「未決」と書いていただけ） | ★★**デッキ同期のサーバーと Phase 6 の権威サーバは同一のものを使う**★★ |
| ★**認証**（★同上。§5-1 ② が「未決」と書いていただけ） | ★★**認証は 1 つ**★★ |

#### ★★ この回は「聞いていないものに答えが返った」★★

★★**§9-1 とちょうど逆向きである。**★★

| | §9-1 | ★**§9-5** |
|---|---|---|
| 何が起きたか | ★**問いの文言に入っていなかったものが返ってこなかった**（衝突解決の方式 / 設定の範囲 / サーバーの前提） | ★★**一度も問いに立てていないものに答えが返った**★★ |
| 何が問いを兼ねたか | ★**問いの文言そのもの**（§9-1 / §9-4 に残してある） | ★★**未決の宣言**★★ —— **D105-7** の末尾と §5-1 ② の「決まっていないこと」 |
| ★**読み取れること** | ★**問いに立てなければ返らない** | ★★**未決と書いておけば、問いに立てなくても返ることがある**★★ |

→ ★★**「未決」と書くことは問いを立てることの代わりにはならないが、無駄にもならない。**★★
★**D105-7 は「同一のサーバーかどうかは未決のまま」と 1 行書いただけだった。★その 1 行が回答を呼んだ。**
★**逆に、書かれなかった未決は誰の目にも触れない**（★**向きは U31 / D-20 と同じ** —— ★**宣言だけでは実装ではないが、宣言が無ければ見つからない**）。

#### ★★ 返らなかったもの —— 方式 ★★

★**聞いていないので返っていない**、が正しい読み方である（§9-1 と同じ）。

| 何 | どこへ行ったか |
|---|---|
| ★**役割の分離をどう保つか** | → **N-14**（★**同一のプロセスに同居させるか / サーバーが `loveca_core` の何を呼んでよいか / それを機械で見られるか**） |
| ★**同定の粒度**（Phase 6 の対戦相手と同じでよいか） | → **N-11** の論点 (3)（★**D106-4 が入力を足した**） |

#### ★ 回答が指しているもの（★ここに写さない）

★**役割の内訳・矛盾しない理由・警告を維持する根拠は `docs/決定事項一覧.md` の D106-2 / D106-3 にある。**
★**ここに写さない**（§8 —— ★**この文書に決定文を書かない**）。
★★**この節に置くのは「何が返り、どう返ったか」だけである。**★★

---

### 9-6. ★★ 第 4 回の回答（2026-08-28 / 決定 **D107**）★★

★**§9-1 / §9-4 / §9-5 とはさらに別の回である**（★**回ごとに節を分ける** —— ★**同じ表に混ぜると「いつ返ったか」が消える**）。
★★**回答そのものはここが正。決定としての実体は `docs/決定事項一覧.md` の **D107** である。**★★
★**問いの文言は §12-8 に置いてある**（★**問いに立てなければ返らない** / §9-1）。

| 問い（§12-8） | 回答 |
|---|---|
| **1** 2 台で別々に直したデッキが食い違ったとき、アプリはどう振る舞うべきか | ★**既定は「最新の履歴に合わせる」。**★★**ただし、消える前に知らせる、または消えたことが後から分かるようにする**★★ |
| **2** 同じデッキを 2 台で編集することはあるか | ★★**ある**★★ |

★★**回答 1 を「最新に合わせる」の 1 行に縮めない。**★★
★**縮めると何が落ちるかの正は `docs/決定事項一覧.md` の **D107-3**。★ここに写さない**（§8）。

#### ★★ 回答 1 は 2 段で返った（★経緯を残す）★★

★★**1 度目の答えと 2 度目の答えを 1 行にまとめない。**★★
★**まとめると「利用者が最初から可視性を求めていた」ことになり、★実際に何が効いたかが消える。**

| 段 | 誰が何を言ったか |
|---|---|
| **1** | ★**利用者**: 「最新の履歴に合わせてください」★**とだけ答えた** |
| **2** | ★**相談役**: 「**D105-2** でサーバーは保管庫なので、比べられるのは**端末の時計**である。★**時計のずれで、先に編集したほうが最新と判定されうる。**★**そのとき片方は静かに消える**」と説明した |
| **3** | ★**相談役**: 「**消えてよいか / 消えるのは困るか**」を確認した |
| **4** | ★★**利用者**: 「**消えるのは困る**」★★（→ ★**回答 1 の後半になった**） |

★★**段 2 の説明は、比べる量を「時計」と仮定している。**★★
★**その仮定は方式ではない**（→ ★**(f-6) は開いたままである** / §12-11）。
★★**利用者が言ったのは「最新の★履歴」であって「最新の時刻」ではない。**★★

#### ★ 段 3 は二択だが、§7-8 の型に当たらない（★判定を残す）

★**聞く前に「どちらでもない」が答えになりうるかを確かめている。**
★★**判定の正は §7-8 の 2 つ目の追記**★★（★**成功例として記録した**）。★**ここに写さない。**
★**ただし 1 点だけ書いておく** —— ★★**答えは二択の外に出た**★★（★「消えてよい」でも「消えない」でもなく、
★**「消えてよい。ただし黙っては困る」**が返った）。★**内訳は §7-8。**

#### ★★ 返らなかったもの ★★

| 何 | どう読むか |
|---|---|
| ★**「知らせる」が事前か事後か** | ★★**利用者が「または」で並べた。**★★ → ★**どちらでもよいと言われた**＝ ★**この 2 つの中からの選択は技術判断に降りた** |
| ★**「知らせたうえで選ばせる」か** | ★★**問いには入っていた。★利用者は選ばなかった。**★★ → ★**「不要と決まった」と読まないこと**（★**選ばなかったことは却下ではない**） |
| ★**方式** | ★**聞いていないので返っていない**（§9-1 / §9-5 と同じ読み方）。→ ★**残りの状態は §12-11** |

#### ★ この回で「利用者にしか答えられない論点」は再び 0 になった

★★**`CLAUDE.md` §8 の「利用者にしか答えられない論点は 0 になった」は、
§12-8 が 2 つ見つけた 2026-08-28 の時点で一度偽になっていた。**★★
→ ★**この回答で再び真になった。**
★**型は D-15 の親戚**（★**数を書いた行が、あとから足された論点で偽になる**）。
★★**ここに新しい数を書き直さない。**★★
★**§12-8 が「分離してから数える」手順（§7-1 の新条件）そのものを持っており、
★次に論点が出ればまた偽になる。**

---

## 10. ★ 回答で開いた論点

★★**推測で埋めない。根拠を示せるものだけ書く。**★★
★**方式は 1 つも決めない**（この文書は論点表である / §8）。
★**番号は暫定の `N-x` である** —— ★**決定になったら `docs/決定事項一覧.md` へ移し、ここからは番号で参照する。**

### N-1. ★ D43 と ①(a) —— 矛盾しないが、経路が 2 本になる

★★**矛盾しない**★★（§9-3 の 3）。**D43** は自ら期限を切っている ——

> Phase 4 で配信経路が固まる**まで**、UI 側にネットワーク取得の口を作らない。
> **経路が 2 つあると「どちらから来た画像か」で不具合の切り分けができなくなる。**

★**①(a) はまさにその「配信経路が固まる」ことなので、禁止は解ける。**

★★**しかし D43 が挙げた害そのものは残る。**★★
→ ★**論点は「ローカル経路を残すか、一本化するか」であり、残すなら「どちらから来たか」を出す手段が要る。**

★**器は既にある** ——

| 何 | 状態 |
|---|---|
| `CardImageSource`（抽象 / **D57**） | ★**実装は `LocalDirectoryCardImageSource` の 1 つだけ**。★**足せる形になっている** |
| `imageUrl(cdnBase, imageHash, size)` | ★★**`loveca_core` に既に在るが `lib` からの呼び出しが 0 件**★★（テスト 2 件のみ）。★**同じ関数の doc が D-4 に反する主張をしていた** → **D-33** |

```bash
grep -rn "imageUrl\|cdnBase" --include=*.dart loveca-*/lib loveca-*/test | grep -v '\.g\.dart'
```

### N-2. ★ 差分の単位を決めていない

★**カード単位 / 商品単位 / 画像 1 枚単位 / 段（thumb・normal・large）ごと / 版全体**のどれか。

★★**測定値を書くので、刻み幅・標本・判定手段を併記する**★★（**D-28**）——

| 測ったもの | 値 | 測り方 |
|---|---:|---|
| `manifest.json` の `files` | **25 件**（`cards` 22 / `meta` 3） | `manifest.json` を読み `path` の先頭で数えた |
| うち画像 | ★**0 件** | 同上 |
| `dist/images/{thumb,normal,large}` の実ファイル | 各 **2,527**（計 **7,581**） | ディレクトリを数えた |
| `manifest.json` のサイズ | **3,437 B** | `ls -l` |

★**2026-08-27 に実測。**★**推定はしていない。**

★★**画像を manifest に入れると `version.json` の `manifestHash` の意味が変わる。**★★
`manifestHash` は manifest 全体を指すので、★**画像 1 枚の差し替えで manifest 全体が再取得**になる。
→ ★**「差分で配る」と決めたが、差分の粒度によっては manifest 自体が粒度の下限になる。**

★**D-4 の (a)(b)(c) はこの単位に依存する** —— 案 (c)「配信側に画像リビジョンを持たせる」は
自ら「**差分更新の粒度が『全画像』になる**」と書いている。

### N-3. ★ 版が上がらない限り更新が無言で落ちる → **D-32**

★**実体は `ルール整合性チェック_v1.06.md` の **D-32** に置いた。ここには書かない**（索引と内容を二重に持たない）。
★**A-5（D-4）と同じ失敗の型なので、手当ても同時に見ること。**

### N-4. ★ `loveca_core` の doc が D-4 に反する主張をしていた → **D-33**

★**実体は `ルール整合性チェック_v1.06.md` の **D-33**。**
★★**コメントの訂正は 2026-08-27 に済ませた**★★（★**偽の安全性の主張だったので、案の選択を待たずに消した**）。
★**D-4 の (a)(b)(c) をどう直すかは未決のまま**である —— ★**訂正と判断は別。**

### N-5. ★★ 衝突解決の方式（①(f)）は避けられない ★★

★★**この節は 2 回動いた。★経緯を残す。**★★

★**②の分岐表（§5-1 ②）が書いていた枝** ——
「アカウントを持たない（端末間直結）→ サーバが要らず、(f) が『最後に書いた者勝ち』で済みうる」
★★**この枝は選ばれなかった。**★★
→ ★**(f) は「軽くなるかもしれない未決」から「必ず決めなければならない未決」に変わった。**

→ ★★**2026-08-27（**D105**）にもう一段強まった —— 「避けられない」である。**★★
★**未採番である。**★★**この節（N-5）が ①(f) の論点そのものを持つ。**★★

> ★★**2026-08-28: この 1 語を分解した。★分解の正は §12 である。**★★
> ★**ここに写さない**（★索引と内容を二重に持たない / §8）。
> ★**同時に見るもの**: **N-14**（★★**双方向**★★。★上流ではない / §11-4 / §11-5）/
> **N-10**（★★**双方向**★★ / §12-7）/ **N-11**（★入力になりうる。★**論点 (3) は D106-4 が足した**）。
> ★★**下の「導出」表は入力の一覧ではない**★★ —— ★**「なぜ避けられないか」の導出**である（§12-1）。
> ★**入力の一覧は §11-1。**

#### ★★ 導出（★結論だけ書かない）★★

| 回答 | 何を作るか |
|---|---|
| **D105-1** | ★**オフライン中も編集できる**ので、2 台が同じデッキを別々に直した状態が**作れる** |
| **D105-6** | ★**失敗しても編集を止めない**ので、分岐は失敗が続くあいだ**拡大し続ける** |
| **D105-2** | ★**サーバーは判断しない**ので、突き合わせる主体は**アプリしかいない** |
| **D105-5** | ★**版がずれる**ので、「新しいアプリだけが知っている項目」を古いサーバーが預かる場合がある → ★★**突き合わせの入力が欠けうる**★★（**A-3 型の無言の欠落**） |

★★**D105-4（利用者は複数）はここに入れない。**★★
★**他人のデータは衝突しない。**★**4 が効くのは D105-3（アクセス制御）の側である。**
★**理由が違うものを混ぜない。**

#### ★★ §2-3 の前提は 2 回動いた —— 2 回目で元に戻った ★★

| いつ | §2-3 の記述 | 状態 |
|---|---|---|
| 当初 | 「**Q2**（責務はどの層か）は DB 内の整合の問題で、**同期の相手にも衝突解決にも依存しない**」 | ★**書かれた時点の選択肢は「呼び出し側」と「DAO / エンティティの内側」の 2 つ** |
| 2026-08-27（②/ **D103-3**） | ★**アカウント制になると「サーバーが決める」が 3 つ目の候補として立ちうる** | ★**型は D-15 (l)**。★**走査では出ない** |
| ★★**2026-08-27（**D105-2**）**★★ | ★★**サーバーは判断しない。→ 3 つ目の候補は消えた**★★ | ★**候補は 2 つに戻り、当初の記述が復活する** |

★★**ただし但し書きを 1 つ残す。**★★
★**(f) が新しい列を要求すると「3 つ組」自体が増えうる**（★**N-11** の同定がその候補）。
→ ★**Q2 の答え（層）は動かないが、Q2 が扱う対象は増えうる。**
★**これを書かないと D-15 (h)（doc が実装より広い）になる。**

→ ★**Q2 は本線から外れた**（順序は §11）。

> ★★**2026-08-28（**決定 D107**）: もう一段強まった —— 「事故」ではなく「日常」である。**★★
>
> | いつ | 何と言えたか | 根拠 |
> |---|---|---|
> | 当初 | ★**必須**（②がアカウント制になったため） | **D103-3** |
> | 2026-08-27 | ★**避けられない** | **D105-1** / **-2** / **-5** / **-6**（上の導出表） |
> | ★★**2026-08-28**★★ | ★★**日常である**★★ | ★**D107-2** —— ★**同じデッキを 2 台で編集すると利用者が明示した** |
>
> ★★**D105 までの導出が言えたのは「分岐が★作れる」までである。**★★
> ★**D105-1 は「オフライン中も編集できる」としか言っておらず、★同じデッキかどうかを述べていない**
> （★**§12-8 がこの点を分けて数え、★利用者に聞いた**）。
> → ★**D107-2 が言うのは「実際に★起きる」である。★可能性ではなく頻度が入力になった。**
>
> ★★**順序は動かない。★確かめた**（§11-4 の 2026-08-28 の検算）。★★
> ★**頻度は入力であって上流ではない。**★**「重みが増した」と「順番が変わった」を混ぜないこと**
> （★**§11-5 の「向きと優先度を混ぜない」と同じ作法**）。
>
> ★**回答後の (f-1)〜(f-6) の状態は §12-11。**★**ここに写さない**（★索引と内容を二重に持たない / §8）。

### N-6. ★ 設定の同期範囲を決めていない

★★**利用者が名指ししたのは「既定のエネルギーカード」だけである。★推測で `AppSettings` 全体へ広げない。**★★

★**技術的な事実を 2 つ添える**（★**判断ではない**）——

| # | 事実 |
|---|---|
| 1 | ★★**`AppSettings.distDir` は端末のファイルパスである。**★★ 同期すると相手端末で解決に失敗する —— `DesktopDistLocator` は候補ディレクトリに `version.json` と `manifest.json` が**実在するか**まで確かめる（**D60**）。★**「揃える」対象として素直に扱えない項目が既に 1 つある** |
| 2 | ★★**`settings.json` に `revision` / `updatedAt` / `deviceId` が 1 つも無い。**★★ ★**`AppSettings.toJson` が書くのは 3 項目だけ**。→ ★**D101 の 3 つ組問題が 2 つ目の保管庫で再演する**（§3 の共通の問いが `decks` / `deck_entries` だけの話ではなくなる） |

★★**D60 の見直し条件は「項目が 10 を超える」「型付きの構造が要る」の 2 つだけで、
「同期対象になったとき」を含まない。**★★ → ★**型は D-15 (l)。前提が動いた。**

★★**2026-08-27（**D105-3**）に事実がもう 1 つ増えた。**★★

| # | 事実 |
|---|---|
| 3 | ★★**`settings.json` に「誰の設定か」も無い。**★★ ★**D105-3 はアクセス制御をサーバーに置くと決めた**ので、設定を同期するなら**設定にも所有者が要る**。★**事実 2（3 つ組が無い）とは別の欠落である** —— 2 は**差分検出**の話、3 は**アクセス制御**の話。★**理由が違うものを 1 行にまとめない** |

★**それでも範囲は広げない。**★**利用者が名指ししたのは「既定のエネルギーカード」1 項目だけ**である。

```bash
grep -n "見直す条件" loveca-ui/lib/src/data/app_settings.dart
```

### N-7. ★★ サーバーに何を前提してよいか【★★2026-08-27 回答済み / 決定 **D105**★★】★★

> ★★**5 問とも回答を得た。★回答そのものの正は §9-4、決定の実体は `docs/決定事項一覧.md` の D105。**★★
> ★**ここに回答を写さない**（索引と内容を二重に持たない / §8）。
> ★★**問いの文言は記録として残す**★★ —— ★**何を聞き、何を聞かなかったかが後で効く**
> （§9-1 の「返らなかったもの」がまさにそれで、**問いの文言に入っていなかった**ことが原因だった）。

★**以下は聞く前の記述である。**
★**②で「利用者が用意する」までは決まったが、それが何を保証するかは決まっていなかった。**

> 用意されるサーバーについて、アプリが前提にしてよいことを教えてください。
>
> 1. **常時動いていますか。** 落ちているあいだ、アプリは編集を続けられるべきですか
>    （手元に貯めて後で送る）、それとも編集を止めるべきですか。
> 2. ★**サーバーは「保管庫」ですか、それとも「判断する側」ですか。**
>    2 台が同じデッキを同時に直したとき、**どちらを残すかをサーバーが決めます**か、
>    それとも**アプリが決めた結果をそのまま預かる**だけですか。
> 3. **使うのはあなた 1 人ですか**、それとも他の人も同じサーバーを使いますか。
> 4. **サーバー側のプログラムは、アプリの更新と同時に上げられますか。**
>    古いサーバーに新しいアプリがつながる状況はありますか。
> 5. 通信が失敗したとき、**利用者に見せるべきですか**、黙って後で再試行してよいですか。

★★**問い 2 が最も効く。**★★ ★**これだけが ①(f)（衝突解決）と Q2（責務の層）の両方を決める。**
★**問い 3 は D104 の依存 2（個人利用・非商用）にも掛かる。**

#### ★★ 予測は当たったか —— 半分だけ ★★

★**「問い 2 が最も効く」という予測を、答えが出たあとに検算する**
（★**予測を書いたら結果を突き合わせること**。`docs/決定事項一覧.md` D83 と同じ作法）。

| 予測 | 結果 |
|---|---|
| 問い 2 が ①(f) を決める | ★**当たり。**★**D105-2（アプリ側が解決する）が方式の候補を絞った** |
| 問い 2 が Q2 を決める | ★**当たり。★ただし向きが逆だった** —— ★**候補を選んだのではなく、3 つ目の候補を消して元に戻した**（N-5） |
| ★**問い 3 は D104 に掛かるだけ** | ★★**外れ。**★★ ★**問い 3 は D105-3（アクセス制御）の理由そのものになった**うえ、**N-11（利用者の同定）を新設させた**。★**「法務にだけ掛かる」と読んでいたのが狭かった** |
| ★（予測していなかった） | ★**問い 1・4・5 が N-10 / N-12 / N-13 を新設した。**★**「最も効く 1 問」を当てにいったので、残り 4 問が何を開くかを見ていなかった** |

### N-8. ★★ A-0 の走査は `.dart` しか見ていない ★★

**D-5** の走査は 2 本とも `--include=*.dart` である（`ルール整合性チェック_v1.06.md` の当該節）。
→ ★★**`.md` 側に括弧なしの旧番号が live で残っていても、走査は黙る。**★★

★**実物**（★**判定を挟まず、出た行をそのまま挙げる**）——

| 場所 | 何が書かれているか | どちらの番号か |
|---|---|---|
| `CLAUDE.md` の D-9 の行 | 「`P2`「更新のたびに +1。同期の差分検出に使う」と食い違う」 | ★**同期の設計番号**（= **D101**） |
| `docs/決定事項一覧.md` の D62 の行 | 「`deckId`（UUID v4 / `P1`）」 | ★**同期の設計番号**（= **D100**） |
| 同 D96-4 の 3 | 「`revision` が動き、Phase 4 の同期に意味のない差分が立つ（`P2`）」 | ★**同期の設計番号**（= **D101**） |
| 同 D70 の行 | 「カバーを外せない（`P3` に要る）」 | ★★**UI のペイン番号**★★（`docs/UI設計メモ.md` §2-3） |

★★**最後の 1 行が要点である。**★★ ★**`.md` でも同じ曖昧さが起きる**ので、
★**素の `P1` で機械的に置換すると壊れる**（**D-5** が `.dart` について書いているのと同じ）。

★**機構は既知の型と同一である** —— `CLAUDE.md` §8 が「廃止した語」について
**`abolished_term_test.dart` は `.dart` だけを見ており `.md` を 1 つも読まない**と書いている。
→ ★**D-31（走査を狭めたことが盲点になった）の延長**であり、★**A-0 の範囲に足す。**

### N-9. ★ 原典 §1.2 の 3 行が ② と食い違う

原典 `docs/引き継ぎドキュメント.md` §1.2「確定済み」の技術選定表に、
**認証 / 同期 / CDN** の 3 行がある。★**②「サーバーは利用者が用意する」という前提に立っていない。**

★★**技術名はここに写さない**★★（§6-3 の取り決め）。
★**写せば「台帳の外の決定を、根拠なしに台帳の中へ持ち込む」ことになる。**
★**ここに残すのは「U31 の実物がもう 1 つ増えた」という記録だけである。**

★**CDN の行は自ら「Phase 0 の権利方針判断後に決定」と従属を書いていた** ——
★**その Phase 0 は D104 で解けた**ので、★**この行は「未定のまま前提が動いた」状態にある。**

### N-10. ★ 送信待ちを手元に貯める器が無い（★**D105-1**）

★**D105-1 は「落ちているあいだも編集を続け、手元に貯めて後で送る」と定めた。**
★★**貯める場所が無い。**★★

| # | 事実（★**判断ではない**） |
|---|---|
| 1 | ★**`loveca_db` に送信待ちを表す表が無い**（`loveca-db/lib/src/schema/tables.dart`）。★**`schemaVersion` は 3** |
| 2 | ★**`decks` の 3 つ組（**D101**）は「いつ更新したか」を持つが「送ったか」を持たない** —— ★**未送信は「サーバー側の状態と比べて初めて分かる」形になっている** |

★★**方式は書かない。**★★
★**論点は「送信待ちを明示的に持つか、サーバーとの差分から毎回導くか」**である。
★**N-5（①(f)）と同じ量を使うので、切り離して決めない。**

> ★★**2026-08-28: 向きを確かめた —— 双方向である。**★★
> ★**ここには「同じ量を使う」としか書いておらず、向きを書いていなかった。**
> ★**根拠と実装の事実は §12-7。**★**ここに写さない**（★索引と内容を二重に持たない / §8）。
> → ★★**N-14 と同じ関係であり、並行レーン 4 の「一方向」では足りない。**★★

> ★★**2026-08-28（**決定 D107**）: 器に届く要求が 1 つ増えた。★方式は書かない。**★★
> ★**D107-1 の後半は「消えたことが★後から分かるようにする」を選択肢に含む。**
> → ★★**論点: 後から分かるようにするために、失われた側を残す必要があるか。**★★
> ★**上の事実 2（未送信は「サーバー側の状態と比べて初めて分かる」形になっている）と同じ場所に効く。**
> ★**双方向であることは §12-7 のまま動かない**（★**この追記は向きを変えない。★入力を 1 つ足しただけである**）。

### N-11. ★★ 利用者の同定をどこに持つか（★**D105-3** + **D105-4**）★★

★**D105-3 はアクセス制御をサーバーに置くと決め、D105-4 は利用者が複数だと定めた。**
→ ★★**「誰のデータか」を表す量が要る。**★★

| # | 事実（★**走査と実読で確かめた**） |
|---|---|
| 1 | ★★**`decks` にオーナーを表す列が無い**★★（`loveca-db/lib/src/schema/tables.dart` の `Decks` を実読） |
| 2 | ★★**`lastDeviceId` は端末の ID であって利用者の ID ではない**★★ |
| 3 | ★★**`lastDeviceId` に値を設定する箇所が `lib` に 1 つも無い**★★ —— `loveca-ui/lib/src/data/deck_repository.dart` と `loveca-ui/lib/src/data/energy_fill.dart` は**持ち回るだけ**で、既定は空文字。★**値を入れているのはテストだけ** |

★★**「1 人なら端末の見分けで済んだ」が、D105-4 で済まなくなった。**★★
★**これが §5-1 ②の「`lastDeviceId` が何を指すか」の行き先である。**

★**型は D-20 の系**（宣言だけ在って `lib` に消費者が居ない）。
★**ただし不具合ではない** —— `tables.dart` のコメントが「Phase 4 のための先行対応」と自ら書いている。
★★**D-20 の教訓が効くのはここである**★★ —— ★**先行して置いた列は、
「何を指すか」を決めるまで意味を持たない。★置いたこと自体は実装ではない。**

★★**方式は書かない。**★★ ★**論点は 3 つ** ——
(1) ★**同定の粒度**（利用者 / 端末 / 両方）/ (2) ★**それを `decks` に持つか、サーバーだけが持つか** /
★★**(3)**★★（★**2026-08-27 追記 / D106-4**）★★**Phase 6 の対戦相手の同定と同じ粒度でよいか。**★★
★**(2) は D105-3（アクセス制御はサーバー）と正面から関わる。**

★★**(3) は D106-4（認証は 1 つ）が足した入力である。**★★
★**「認証が 1 つ」は「誰であるかを 1 つの仕組みで確かめる」ことであって、
デッキの所有と対戦相手の同定を同じ粒度で持つことではない。**★**分けて決められる。**
→ ★★**ただし、分けるなら根拠が要る側に回った**★★ ——
★**D106 の前は「別の仕組みかもしれない」で済んでいた。**★**いまは同じ仕組みの上で粒度だけを分ける形になる。**

### N-12. ★ サーバー ↔ アプリの版のずれを吸収する軸が無い（★**D105-5**）

★**D105-5 は「古いサーバーに新しいアプリがつながる状況はある」と定めた。**

★★**`minAppVersion` はこの軸を見ていない。**★★

| 軸 | 仕組み | 状態 |
|---|---|---|
| **配信データ → アプリ** | `minAppVersion`（`build_dist.py` が宣言 / `master_data.dart` の `isAppSupported` が判定） | ★**在る。**★**ただし CLI に露出せず 1.0.0 固定**（**D-7** / **A-6**） |
| ★★**サーバーのプログラム → アプリ**★★ | — | ★★**無い**★★ |
| ★★**アプリ → サーバーのプログラム**★★ | — | ★★**無い**★★ |

★**A-6 は既に発火済みである**（**D103-1** / §1 の A-6 の「依存」欄）。
★★**D105-5 が足したのは発火ではなく軸である。**★★
→ ★**A-6 のやること 4（アプリの版を実態に戻すか）は N-12 と同時に見る。**
→ ★**「新しいアプリだけが知っている項目を古いサーバーが預かる」ときの挙動は N-5 の入力でもある。**

### N-13. ★ 通信の失敗・未送信をどこにどう見せるか（★**D105-6**）

★**D105-6 は「見せる。ただし編集は止めない」と定めた。**★**どこに何を出すかは決まっていない。**

#### ★★ 束 D ではなく Phase 4 である（★判定と根拠）★★

| # | 根拠 |
|---|---|
| 1 | ★★**束 D は盤面 UI 専用である**★★ —— `CLAUDE.md` §8 の索引で **U19 / U26 / U27 / U28 / U29 / U30 は全行が `docs/盤面設計メモ.md` §13** を指し、`docs/相談役への引き継ぎ.md` §5 も「★盤面を次に触るとき」と書いている。★**未送信の表示は盤面ではない**（R2 / R3） |
| 2 | ★**束 D の中身は相互依存で束ねられている**（「測定値を持つ項目は同時に測り直す」）。★**未送信の表示はどの測定値とも共有しない** |
| 3 | ★★**内容が同期設計に従属する**★★ —— 「未送信」が何を指すかは **N-10** と **N-5** が決まらないと書けない。★**「次に盤面を触るとき」では決められない** |

→ ★**決定になった時点で `docs/UI設計メモ.md` へ移す**（★**索引と内容を二重に持たない** / §8）。

#### ★ 器は既にある（★**事実だけ。方式は書かない**）

| 何 | 状態 |
|---|---|
| **D89** の 3 層（帯 / プレースホルダ / 詳細への導線） | ★**dist が解決できていないことを見せる形として実装済み**（`BootGate` 1 箇所 + 設定の問題とデータの問題の撃ち分け + R6 への導線） |
| ★**原則** | ★★**「空」と「失敗」を同じ型で表さない**★★（`docs/相談役への引き継ぎ.md` §6 の教訓 2）。★**D105-6 はこの原則と同じ向き**である |

★★**ただし D89 をそのまま流用してよいとは書かない。**★★
★**D89 は「起動できない」状態を見せるもので、D105-6 は「編集は続く」状態を見せる。**
★**止まる / 止まらないが違う。**

#### ★★ 2026-08-28（**決定 D107**）: 見せるものが 2 つになった ★★

★**D105-6 が立てたのは「通信の失敗」の見せ方だった。**
★★**D107-1 の後半が「衝突が起きたこと」を足した。**★★

★★**理由が違う。★1 行にまとめない**★★（★**根拠は §12-6** —— ★**D105-6 から (f-4) は導けない**）。

| 何を見せるか | 由来 | ★時制 |
|---|---|---|
| ★**通信の失敗 / 未送信** | **D105-6** | ★★**状態**★★（★いま続いている） |
| ★**衝突が起きたこと** | ★**D107-1 の後半** | ★★**出来事**★★（★起きてしまった） |

★★**論点は 2 つ。★方式は書かない。**★★

| # | 論点 |
|---|---|
| **1** | ★★**器を共有するか、分けるか。**★★ ★**共有すると「まだ送れていない」と「上書きされた」が同じ帯に出る。**★**分けると帯が 2 本になる。**★**「器が同じになりうる」ことは §12-6 が既に書いているが、★同じでよいかは決まっていない** |
| **2** | ★★**「消える前に知らせる」を採ると、その瞬間だけ何かを止めることになりうる。**★★ ★**D105-6 の「編集は止めない」は★通信の失敗についてのものである**（§12-6 で分けた）。→ ★**衝突の通知が編集を止めてよいかは、★どこにも決まっていない** |

★★**時制の区別には先例がある。**★★
**D90-3** が「`maxDepth` 到達は Tooltip ではなく**帯**（★**状態であって出来事ではない**）」と書いている。
→ ★**器の選択にこの区別が効く。**★**ただしここでは選ばない**（★**この文書は論点表である** / §8）。

★★**向きは確かめていない。**★★
★**§11-5 は N-13 を並行 4（★一方向）に置いている。**
★**今回「事前に知らせるか、事後に分かるようにするか」が N-13 と (f-4) の残りにまたがった**が、
★★**それが N-13 → N-5 の辺なのか、境界の引き方の問題なのかを確かめていない。**★★
→ ★**図に辺を足していない。**★★**確かめられたら足すこと**★★（§11-7 / §11-4 の末尾に未了として記録した）。

### N-14. ★★ 役割の分離をどう保つか（★**D106-1** + **D106-3**）★★

★**D106-1 は 1 つのサーバーを使うと定め、D106-3 は D105-7 の警告を維持すると定めた。**
★★**同居させたうえで境界を保つ手段が、1 つも決まっていない。**★★

#### ★ 何が問題か（★**事実だけ。判断ではない**）

| # | 事実 |
|---|---|
| 1 | ★★**権威サーバは `loveca_core` を持つ。**★★ `CLAUDE.md` §1 の理由 2 が「`reduce` / `redact` をコピーゼロで再利用する」と定めている。→ ★**リンクするかどうかは論点ではない。★必ずリンクする** |
| 2 | ★★**同じパッケージにデッキ側の判断材料も入っている。**★★ `DeckValidator`（`loveca-core/lib/src/rules/`）/ `Deck` の 3 つ組（**D101**）。→ ★**リンクした時点で呼べる** |
| 3 | ★**D105-2 は「衝突解決はアプリ側」と定めている。**→ ★★**呼べることと、呼んでよいことは別である**★★ |

#### ★★ 論点は 3 つ（★方式は書かない）★★

| # | 論点 |
|---|---|
| ★**(1)** | ★**同じプロセスに同居させるか。**★**同居させると、境界を支えるものが規約だけになる** |
| ★**(2)** | ★★**サーバーが `loveca_core` の何を呼んでよいか。**★★ ★**リンクの可否ではない**（事実 1）。★**線は「どの関数を呼ぶか」に引かれる** |
| ★**(3)** | ★**その線が守られていることを機械で見られるか。**★**規約だけでは腐る**（**D-2** / **D-20** が前例） |

#### ★★ `CLAUDE.md` §1 の境界 1 は柵にならない（★確かめた）★★

★**問い**: 「Phase 6 の権威サーバが同じ `reduce` / `redact` をコピーゼロで再利用する」（`CLAUDE.md` §1 の理由 2）は、
★**デッキ同期にも適用されると読めるか。**

★★**読めない。**★★ ★**実物を引いた結果は 4 つである。**

| # | 根拠 |
|---|---|
| 1 | ★**主語が「Phase 6 の権威サーバ」に限定されている** |
| 2 | ★**目的語が `reduce` / `redact` に限定されている**（★**`DeckValidator` も `Deck` も名指していない**） |
| 3 | ★**理由 1 の軸は「サーバー ↔ アプリ」ではない** —— 「スマホでは合法、**PC では不正**」は**端末どうし**の話である |
| 4 | ★★**§1 自身が 2 つを別物として書いている**★★ —— D-14 の注記が「**Phase 6 の権威サーバや Phase 4 の同期**は踏みうる」と**同じ文で並置**している |

→ ★★**`CLAUDE.md` §1 は 1 文字も直さない。**★★

★★**ただし「読めない」と「禁じている」は違う。**★★
★**§1 は沈黙しているだけで、滑るのを止めてはいない。**
★**滑る経路は §1 の中ではなく `docs/決定事項一覧.md` の D105-2 の理由セルにある** ——
そこは §1 の理由 1 と理由 2 を**並べて引用**しており、こう読める ——

> 二重実装を避ける ⇒ 実装は `loveca_core` 1 つ ⇒ サーバーもそれを使える ⇒ ★**衝突解決もサーバーでやれる**

→ ★★**柵の置き場は §1 ではなく D105-7 / D106-3 の側である。**★★
★**§1 を直して柵にしようとしないこと** —— ★**§1 は `loveca_core` の純粋さの規約であって、
どこで判断が走るかの規約ではない**（★**格が違うものを 1 つにまとめない** / `docs/相談役への引き継ぎ.md` §4-6）。

★★**この「読めない。しかし柵は別の場所に要る」は、聞かれた二択の外にあった。**★★
★**経緯は §7-8**（★相談役の作法）。★**ここに写さない。**

#### ★★ N-5 との噛み合いは双方向である（★並行レーン 4 / 5 とは向きが違う）★★

| 向き | 中身 |
|---|---|
| **N-5 → N-14** | ★**衝突解決の方式が「サーバーが何を知っている必要があるか」を決める** |
| ★★**N-14 → N-5**★★ | ★★**サーバーに許す範囲が、N-5 が選べる方式を狭めうる**★★ |

★**並行レーン 4 / 5 は「本線を待たないが本線と無関係ではない」一方向の依存だった**（§11-5）。
★★**N-14 は両向きである。★同じ棚に置かない。**★★
→ ★**順序上の扱いは §11-4 / §11-5。**★**ここに書かない**（★索引と内容を二重に持たない）。

---

## 11. ★ 束 A の判断順序

★★**依存から機械的に導く。★推奨は書くが、選択肢は出さない。**★★
★**「早く片づく順」ではなく「決め直しが起きない順」である。**

### 11-1. 依存の図

> ★★**2026-08-27 更新（**決定 D105**）。★本線の先頭だった N-7 が閉じた。**★★
> ★**次に来るものを依存から導き直した。★推奨は書くが方式は出さない。**
>
> ★★**2026-08-27 再検算（**決定 D106**）。★本線の先頭は動かなかった。**★★
> ★**「変わらないはず」で済ませず、4 つ確かめた**（★**結果は §11-4 の末尾**）。
> ★**足したのは N-14 だけである。**
>
> ★★**2026-08-28 更新（★N-5 の分解 / §12）。★本線の先頭は動かなかった。**★★
> ★**足したのは `N-10` の `⇄` と `Q2 → A-1` の辺である。**
> ★**N-5 に新しい上流は立っていない**（★**N-10 は双方向であって上流ではない** / §12-7）。
>
> ★★**2026-08-28 再検算（**決定 D107**）。★本線の先頭は動かなかった。**★★
> ★**「変わらないはず」で済ませず、4 つ確かめた**（★**結果は §11-4 の末尾**）。
> ★**足したのは N-5 の「←入力」の行 1 本だけである**（★**辺は 1 本も足していない**）。
> ★★**確かめられなかったものが 1 つある**★★（★**N-13 との境界** / §11-4 の末尾）。

```
[独立レーン]  A-0（D-5 残務）  ── ★依存 0。D-5 自身が「着手前」と定めている
                  └─ ★N-8 で範囲が広がった（.md 側も見る）

[本線]  N-5 ＝ ①(f) 衝突解決の方式（★未採番 / ★★避けられない → 2026-08-28: 日常★★）
          ←入力  D105-1 オフライン編集 / D105-2 サーバーは判断しない
                 D105-5 版がずれる / D105-6 止めない
          ←入力  ★★D107-1 既定は最新の履歴に合わせる＋知らせる / D107-2 衝突は日常★★
          ←入力  ★N-11 同定の粒度（★入力になりうる。11-7 を見よ）
          ⇄     ★★N-14 役割の分離（★双方向。★上流ではない / D106）★★
          ⇄     ★★N-10 貯める器（★双方向。★上流ではない / §12-7）★★ ── ★D105-1 由来
          ├→ Q1  差分検出の正 ─────┐
          └→ Q3  更新と数えるか ────┤
                                     └→ A-1（D-9）/ A-2（D-26）の実装
                                           └→ A-7（U9）
          ★★A-1 は Q2（並行 1）にも依存する。★本線だけでは実装に入れない★★（§12-10）

[並行 1]  ★Q2 責務の層（★候補が 2 つに戻った / D105-2）
              ├→ A-3（D-14）の★向き（★優先度は本線が決める / §4）
              └→ ★A-1（D-9）にも掛かる（★§1 が「Q1 と Q2」と書いている / §12-10）
[並行 2]  N-2 差分の単位 ＋ D-32 版ゲート
              └→ A-5（D-4）の (a)(b)(c) 選択
[並行 3]  N-1 画像経路を一本化するか
          A-4（D-1）/ A-6（D-7）＋ ★N-12 版の軸 ── ★①(a) は満たされた
[並行 4]  ★N-13 見せ方 ── ★D105-6 由来（★★N-10 は 2026-08-28 に本線の ⇄ へ移した / §12-7★★）
[並行 5]  ★N-6 設定の同期範囲 ── ★D105-3（アクセス制御）が掛かる
```

★★**N-7 は図から消えた。★閉じたものは載せない**★★（**A-8** と同じ扱い / 11-6）。

### 11-2. ★★ なぜ A-0 が先頭か ★★

★**依存 0 なのはこれだけである。**
★**D-5 が「Phase 4 の着手前」と定めた理由**は「Phase 4 を実装する人が `P4` とは何かに突き当たる」ことで、
★**同期の設計を始めてからでは、参照の書き換えが同期の実装と混ざる。**

★★**N-8 で範囲が広がった。**★★ `.md` 側も見ること（§10 の N-8）。
★**広げても依存は増えない**（文書の整合であって、同期の方式に何も依存しない）。

### 11-3. ★★ Q2 は本線から外れた（★この節は 2 回動いた）★★

| いつ | 判断 | 根拠 |
|---|---|---|
| 当初 | ★**Q2 は独立に決めてよい** | 候補は「呼び出し側」「DAO / エンティティの内側」の 2 つ |
| 2026-08-27（**D103-3**） | ~~★**「Q2 を最初に」は採らない**~~ | ★**アカウント制になると「サーバーが決める」が 3 つ目の候補として立ちうる**。★**型は D-15 (l)** |
| ★★**2026-08-27（**D105-2**）**★★ | ★★**Q2 は本線から外れる**★★ | ★★**サーバーは保管庫で判断しない。→ 3 つ目の候補は消えた**★★ |

★★**巻き戻しは機械的にできた。**★★
★**2026-08-27 の訂正は「先に決めると N-7 の答え次第で決め直しになる」と、
自分を巻き戻す条件を文中に書いていた**（→ ★**成功例として §7-5 に記録した**）。

★★**但し書きを 1 つ残す。**★★
★**(f) が新しい列を要求すると「3 つ組」自体が増えうる**（★**N-11** の同定がその候補）。
→ ★**Q2 の答え（層）は動かないが、Q2 が扱う対象は増えうる。**
★**これを書かないと D-15 (h)（doc が実装より広い）になる。**

★★**2026-08-27（**決定 D106**）: 3 つ目の候補は消えたままだが、復活の圧力は上がった。**★★
★**サーバーが 1 つになったので、「サーバーが決める」は「ついでにできること」になった。**
→ ★**消えたことと、復活しやすくなったことを混ぜない。**
★**候補は 2 つのままで、Q2 は本線から外れたままである**（★**この行は動かない**）。
★**圧力そのものの扱いは §10 の **N-14**。**★**ここに書かない。**

### 11-4. ★ 本線が N-5（①(f)）から始まる理由

★**「誰が決めるか」は答えが出た**（**D105-2** ＝ アプリ側）。
→ ★★**残っているのは「何を比べるか」であり、それが (f) である。**★★

| 問い | 何を決めるか | 上流 |
|---|---|---|
| ~~**N-7 の問い 2**~~ | ~~サーバーが判断するか、預かるだけか~~ | ★★**2026-08-27 に回答済み**★★（**D105-2**） |
| ★**N-5 ＝ ①(f)** | 衝突をどう解くか | ★★**上流は無い。★本線の先頭である**★★（入力は D105-1 / -2 / -5 / -6。★**N-11 が入力になりうる** / 11-7） |
| **Q1** | 差分検出の正は `revision` か `updatedAt` か 3 つ組か | ★**(f)**（比較に使う量が決まらないと選べない） |
| **Q3** | 利用者の編集でない書き込みを「更新」と数えるか | ★**(f)**（数えると「全端末が同時に全デッキを更新した」ことになる。望ましいかは方式次第） |
| ★**Q2** | 3 つ組を決める責務はどの層か | ★★**外れた**★★（**D105-2** / 上の 11-3）。★**並行レーン 1** |
| ★★**N-14**★★（★**D106**） | 役割の分離をどう保つか | ★★**上流ではない。★双方向である**★★ —— ★**N-5 の方式がサーバーに要る知識を決め、N-14 の許す範囲が N-5 の選べる方式を狭めうる**。→ ★**N-5 と同時に決める。★先に決めない** |
| ★★**N-10**★★（★**2026-08-28**） | 送信待ちを貯める器 | ★★**上流ではない。★双方向である**★★ —— ★**方式が器に何を要求するかを決め、器が何を貯めるかが「比べられる入力」を決める**（★根拠と実装の事実は **§12-7**）。→ ★**N-5 と同時に決める。★先に決めない** |

★★**Q1 と Q3 は (f) の下で互いに独立である。**★★ ★**まとめて決めてよい。**
★**Q2 も (f) と独立になったが、★同時に決める必要は無い**（並行レーンに置いた理由）。

#### ★★ 2026-08-27（**決定 D106**）の検算 —— 先頭は動かなかった ★★

★★**「変わらないはず」で済ませていない。★4 つ確かめた結果である。**★★

| 確かめたこと | 結果 |
|---|---|
| ★**新しい上流が立ったか** | ★★**立っていない。**★★ ★**閉じた 2 件（同一のサーバー / 認証）は元から図に無い** —— §11-1 が「★**閉じたものは載せない**」と定め、**N-7** は既に消えている |
| ★**N-11 が本線の上流へ昇格したか** | ★★**していない。**★★ ★**§11-7 の昇格条件は「(f) が同定の量を使うなら」で、★そこは 1 ミリも動いていない**。★**D106-4 が足したのは N-11 の入力**（論点 (3)）**であって昇格ではない** |
| ★**N-14 が上流か** | ★★**違う。★双方向なので上流にならない**★★（上の表）。→ ★**先頭を奪わない** |
| ★**A-0 は依然 依存 0 か** | ★★**そのまま。**★★ ★**D106 は文書の整合に何も触れていない** |

→ ★★**N-5 は本線の先頭のままである。**★★
★**この 4 つは D106 の時点の数え直しであって、次に何かが動いたらまた数え直すこと**（§11-7）。

#### ★★ 2026-08-28（**決定 D107**）の検算 —— 先頭は動かなかった ★★

★★**「変わらないはず」で済ませていない。★D106 のときと同じ 4 つを確かめた結果である。**★★

| 確かめたこと | 結果 |
|---|---|
| ★**新しい上流が立ったか** | ★★**立っていない。**★★ ★**D107 が埋めたのは N-5 の★入力**（(f-4) の一部 と 使い方の想定）であり、★**入力を埋めることは上流を作らない** |
| ★**N-11 が本線の上流へ昇格したか** | ★★**していない。**★★ ★**§11-7 の昇格条件は「(f) が同定の量を使うなら」で、そこは動いていない。**★★**むしろ D107-2 は「衝突するのは同じ利用者の 2 台」だと言っており、★衝突の両側を分けるのは利用者の同定ではない**★★。→ ★**D107-2 が足したのは N-11 の論点 (1)（同定の粒度）への入力である**（★**D106-4 が論点 (3) に入力を足したときと同じ形** / §12-1） |
| ★**N-14 / N-10 が上流になったか** | ★★**なっていない。**★★ ★**§11-7 の条件は「どちらの方式も決まっていないので、どちらが狭めるかが決まらない」であり、★D107 はどちらの方式も決めていない** |
| ★**A-0 は依然 依存 0 か** | ★★**そのまま。**★★ ★**D107 は文書の整合に何も触れていない** |

→ ★★**N-5 は本線の先頭のままである。**★★
★**この 4 つは D107 の時点の数え直しであって、次に何かが動いたらまた数え直すこと**（§11-7）。

★★**§11-7 に新しい行は足さない。★これも確かめた。**★★
★**D107 が動かしたのは N-5 の★中身**（(f-1)〜(f-6) の状態 / §12-11）**であって、節と節の依存ではない。**
★**A-1 / A-2 への効き方も、既に在る辺（N-5 → Q1 → A-1 / A-2）を通る**（§12-11 の末尾）。

★★**ただし 1 つ、確かめられなかったものがある。**★★
★**「事前に知らせるか、事後に分かるようにするか」は (f-4) の残り（N-5 の中）と N-13（見せ方）にまたがる。**
★★**それが N-13 → N-5 の辺なのか、境界の引き方の問題なのかを確かめていない。**★★
→ ★**図に辺を足していない**（★**確かめていない辺を足すと「依存から機械的に導いた」が別の意味で偽になる**）。
★**§12-4 が「(f-1) と Q1 の間に 1 つ抜けている」と見つけたのと同じ形の疑いである。**
→ ★★**確かめられたら §11-1 に足すこと**★★（§11-7 の「新しい所見が出た場合」）。★**未了として残す。**

### 11-5. ★ 並行レーンが本線を待たない理由

| レーン | 本線と共有するものが無い理由 |
|---|---|
| ★**1: Q2 → A-3（D-14）** | ★★**D105-2 で「サーバーが決める」が消えたので、DB 内の整合の問題に戻った**★★。★**A-3 の「向き」は Q2 から従属して決まる**（★**「優先度」は §4 のとおり本線が決める** —— ★**向きと優先度を混ぜない**） |
| **2: N-2 → A-5（D-4）→ D-32** | ★**配信物の形の話**であり、`decks` / `deck_entries` の 3 つ組に触れない |
| **3: N-1 / A-4（D-1）/ A-6（D-7）＋ N-12** | ★**取り込みと互換性の話。**★**発火条件（①(a)）は満たされた**（★**D43 の期限も ①(a) で解けている**）。★**N-12 は A-6 と同じ「版の互換性」だが軸が違う** —— ★**A-6 は配信データ → アプリ、N-12 はサーバー ↔ アプリ** |
| ★**4: N-13** | ★**見せ方。**★**3 つ組そのものには触れない**。★★**2026-08-28: N-10 をこのレーンから外した**★★（★**双方向だった** / §12-7 / 下の追記） |
| ★**5: N-6**（設定の同期範囲） | ★**別の保管庫の話。**★**D105-3 が掛かる** |

★**ただし A-5 は N-2 と D-32 に依存する**（差分の単位が決まらないと (a)(b)(c) を選べない）。
★★**D-33 は依存に載らない**★★ —— ★**コメントの訂正は済んでいる。残るのは D-4 の案そのものである。**

★★**並行レーン 4 と 5 は「本線を待たない」だけで「本線と無関係」ではない。**★★
★**N-13 が扱う「未送信」の中身は N-5 が決まらないと書けない**（N-13 の判定を見よ）。
★**N-6 は D105-3 が掛かる。**→ ★**着手はできるが、確定は本線のあとになりうる。**
★**「並行」と「独立」を混ぜないこと**（★**独立なのは A-0 だけである** / 11-2）。

★★**N-14 はレーンに載せていない**★★（**決定 D106** / 2026-08-27）。
★**レーン 4 / 5 は「本線を待たないが本線と無関係ではない」という一方向の依存**だが、
★★**N-14 は N-5 と両向きに噛む**★★（§10 の N-14 / 上の 11-4）。
→ ★★**「独立」「並行」に続く 3 つ目の関係である。★同じ棚に置かない。**★★

★★**2026-08-28: 3 つ目の関係に `N-10` が加わった。★レーン 4 から外した。**★★
★**根拠と実装の事実は §12-7**（★**ここに写さない** / §8）。
★★**先例に従っただけである**★★ —— ★**N-14 をレーンに載せなかったのと同じ理由**で、
★**双方向のものをレーンに置くと「並行だから後でよい」と読める。**
★★**「着手できるか」ではなく「関係の向き」で棚を決めている**★★ ——
★**N-10 も N-14 も着手はできる。★確定が本線と切り離せないだけである。**
→ ★**「並行」と「双方向」を混ぜないこと**（★**上の「並行」と「独立」を混ぜない**と同じ作法）。

### 11-6. ★ 順序に載らないもの

| # | なぜ載らないか |
|---|---|
| **A-8**（**U13**） | ★**①(b) で閉じた**（**D103-1**）。★**Phase 4 では何も決めない** |
| **D-33** | ★**訂正済み。**★**残りは A-5 に含まれる** |
| **U32**（番号の突き合わせテスト） | ★**判断項目ではなく検知手段の実装**（§6-2）。★**A-0 と同じ機会に置ける** |

### 11-7. ★★ この順序が古くなる条件 ★★

★**順序は依存から導いてある。★依存が動けば順序も動く。**

| 動くと順序が変わるもの | どう変わるか |
|---|---|
| ~~**N-7 の答え**（サーバーが判断する側だった場合）~~ | ★★**2026-08-27 に答えが出た**★★ —— **判断しない側**だった（**D105-2**）。→ ★**Q2 の候補にサーバーは入らない。この行は閉じた** |
| ★**利用者が (c) の範囲を広げた場合** | ★**N-6 が本線に入る**（`AppSettings` にも 3 つ組が要るなら Q1 / Q2 と同じ問いになる）。★**D105-3 により「誰の設定か」も要るので、広げたときの重みは増えた** |
| ★★**N-11（同定の粒度）の答え**★★ | ★**(f) が同定の量を使うなら N-11 が本線の上流に上がる。**★**いまは「入力になりうる」までしか言えない**（★**方式が決まっていないので、使うかどうかが決まらない**）。★**D106-4 は入力を 1 つ足しただけで、この条件を動かしていない** |
| ★★**N-14（役割の分離）の答え**★★（★**2026-08-27 / D106**） | ★**サーバーに許す範囲が N-5 の選べる方式を狭めた場合、N-14 は「同時」から「上流」に変わる。**★**いまは双方向までしか言えない**（★**どちらの方式も決まっていないので、どちらが狭めるかが決まらない**） |
| ★★**N-10（貯める器）の答え**★★（★**2026-08-28 / §12-7**） | ★**器が貯めるものが N-5 の選べる方式を狭めた場合、N-10 は「同時」から「上流」に変わる。**★**いまは双方向までしか言えない**（★**N-14 とまったく同じ形である** —— ★どちらの方式も決まっていないので、どちらが狭めるかが決まらない） |
| ★**新しい所見が出た場合** | ★**この図に足す。**★**足さないと「依存から機械的に導いた」が偽になる**。★★**2026-08-28 に実際に 2 件足した**★★ —— **N-10 の双方向**（§12-7）と **Q2 → A-1 の辺**（§12-10）。★**この行は空文ではない** |

★★**この節の断定（「依存 0 なのは A-0 だけ」など）は、そのとき数え直すこと。**★★
★**D-15 の型そのものなので、ここに件数を書いていない。**

---

## 12. ★ N-5（①(f)）—— 何を決めれば方式が決まるか

★★**方式を決めない。選択肢も出さない。**★★
★**分けるだけである**（束 A の論点表を作ったときと同じ段取り / §0）。
作成日: 2026-08-28

### 12-1. ★ N-5 の入力を数え直した（★D106 の分）

★★**§10 の N-5 の「導出」表は入力の一覧ではない。**★★
★列見出しが「**何を作るか**」であり、★**「なぜ避けられないか」の導出**である（D105-1 / -2 / -5 / -6）。
★**入力の一覧は §11-1 にある**（★同じ 4 つに加えて **N-11**（入力になりうる）と **N-14**（双方向）が載っている）。
→ ★**同じものが 2 か所にあり、範囲が違う。★片方だけ読むと入力を取り違える。**

★**D106 が何を足したか**（★確かめた）——

| D106 | N-5 への効き方 |
|---|---|
| **D106-1**（同一のサーバー） | ★**直接の入力ではない。**→ **N-14**（★双方向 / §11-4 が「上流ではない」と確かめている） |
| **D106-4**（認証は 1 つ） | ★**N-11 の論点 (3) に入力を足した。**★N-11 は N-5 の「入力になりうる」ので、★**入力側が 1 段動いた**（★昇格ではない / §11-4 の検算どおり） |

→ ★★**D106 は N-5 に新しい直接の入力を足していない。★ただし間接の入力（N-11）の中身を動かした。**★★

★**D105-3 / D105-4 は入力に入れない**（§10 の N-5 が理由を書いている —— 他人のデータは衝突しない）。
★**D106 でもこれは動かない** —— ★**アクセス制御は D106-2 でサーバーの役割として残っており、
「誰のデータか」の分離は同一サーバーになっても消えていない。**

### 12-2. ★ 前提が動いていないか（D-15 (l)）

★**N-5 の本文は D106 より前に書かれている。**★**2 点で古い。**

| # | 何が足りないか |
|---|---|
| 1 | ★**N-5 の本文に N-14 への参照が無い。**★N-14 の側は「N-5 と双方向」と書き、§11-4 / §11-5 も書いているが、★**N-5 から入ると分からない** |
| 2 | ★**N-5 の但し書きは N-11 を挙げるが、D106-4 が N-11 に足した論点 (3) が反映されていない** |

★★**どちらも内容の誤りではない。**★★ ★**書かれていることは今も正しい。★足りないのは参照である。**
→ ★**1 行の相互参照は「索引と内容を二重に持つ」ことにならない**（§8 / N-14 が自分でそう書いている）。

### 12-3. ★★ 分解 —— N-5 は 1 つの問いではない ★★

★**「衝突解決の方式」という 1 語に、答えの出所が違うものが 6 つ入っている。**

| # | 問い | 出所 | いま決まっているか |
|---|---|---|---|
| **(f-1)** | ★**何をもって衝突とするか**（衝突条件） | ★技術判断 | ★**決まっていない**（12-4） |
| **(f-2)** | ★**衝突の粒度** | ★技術判断（★12-5 の事実が入力） | ★**決まっていない** |
| **(f-3)** | ★**解決の主体**（どちらのアプリが解くか） | ★技術判断 | ★**「アプリ側」までは D105-2。★どちらのアプリかは決まっていない** |
| **(f-4)** | ★**自動で解くか、利用者に選ばせるか** | ★★**利用者**★★ | ★**決まっていない**（12-6） |
| **(f-5)** | ★**失われるものをどう扱うか** | ★★**原則は決まっている（D35）**★★ | ★**適用の形だけが未決**（12-9） |
| **(f-6)** | ★**何を比べるか**（比較に使う量） | ★技術判断 | ★**決まっていない。★Q1 の対象そのもの**（12-10） |

★**(f-1) と (f-6) は別である**（12-4）。

### 12-4. ★★ (f-1) と Q1 は混ざっていない —— 間に 1 つ抜けている ★★

★**3 つある。★番号は 2 つしか無い。**

| | 何を問うか | どこに在るか |
|---|---|---|
| ★**差分検出** | 「この端末で、前回同期のあと**このデッキは変わったか**」（★**片側**） | **Q1**（§3-2） |
| ★★**衝突判定**★★ | 「**両側とも**変わったか」（★**両側**） | ★★**どこにも無い**★★ |
| ★**衝突解決** | 「変わっていたらどうするか」 | **N-5**（§11-4 の「衝突をどう解くか」） |

★**走査で確かめた**（★範囲も書く / §7-6）——

```bash
git grep -n "衝突" -- '*.md'
```

★★**同期の衝突を定義した行は 1 つも無い。**★★
★当たるのは `fold` の衝突（`docs/UI技術検証メモ.md`）/ `deckId` の衝突（**D100**）/
条文上の衝突（`docs/PhaseEngine設計メモ.md`）/ 語の衝突（**D-5**）であり、★**どれも別物である**。
★**近いのは D105-2 の括弧「（どちらが新しいか）」と N-7 の問い 2 の「どちらを残すか」だけ**だが、
★★**どちらも問いの文言であって定義ではない**★★（12-6）。

→ ★**(f-1) は Q1 の言い換えではない。**★**Q1 は片側、(f-1) は両側である。**
→ ★★**混ざってはいない。★抜けている。**★★

### 12-5. ★ (f-2) 粒度 —— 実装が持っている事実（★判断ではない）

| # | 事実 | 確かめ方 |
|---|---|---|
| 1 | ★`deck_entries` の主キーは `{deckId, printingId}`。★**`ord` は鍵に入っていない** | `tables.dart` の `DeckEntries` を実読 |
| 2 | ★★`DeckDao.save` は `deck_entries` と `deck_tags` を**毎回「全削除 → 全挿入」**する★★ | `deck_dao.dart` の `save` を実読（★コメントが「増分の保守が要らない」と自ら書いている） |
| 3 | ★★**D101 の 3 つ組が載っているのは `decks` だけ**★★。`deck_entries` / `deck_tags` に時刻も版も無い | `tables.dart` |
| 4 | ★`ord` は「0 始まりの添字」であり、★**列の値ではなくリスト全体の性質**である | `tables.dart` の `ord` のコメント + `save` が `Value(i)` を書く |
| 5 | ★★**`ord` は再計算できない**★★ —— **D99** が**手動並べ替えを残す**と決めている | `docs/決定事項一覧.md` D99 |

→ ★**粒度を「entry 単位」に採ると、事実 3 により比べる量が 1 つも無い。**
→ ★**粒度を「列単位」に採ると、事実 4 / 5 により `ord` だけは列にならない**（★リスト全体で 1 つ）。

★★**これは方式の否定ではない。**★★
★**どちらも「足りない量を新設するか」という問いに変わるだけである**（→ **N-11** の但し書きと同じ形 / §11-3）。

### 12-6. ★★ (f-4) —— D105-6 は通信の話であって衝突の話ではない（★確かめた）★★

| | 何を見せると決めたか |
|---|---|
| **D105-6** | ★**通信の失敗**（★**N-7 の問い 5 は「通信が失敗したとき」と書いている**） |
| ★**(f-4)** | ★★**衝突が起きたこと**★★ |

→ ★★**混ざっていない。★D105-6 から (f-4) は導けない。**★★
★**見せ先の器は同じになりうる**（**N-13**）が、★**見せる理由が違う。**
★**理由が違うものを 1 行にまとめない**（**D105-2** / **D105-3** を分けたのと同じ作法）。

#### ★★ ただし、問いの文言が答えの形を先取りしている（★§7-8 の型）★★

★**N-7 の問い 2** は「**2 台が同じデッキを同時に直したとき、どちらを残すか**」と書き、
★**D105-2 の決定文**は「衝突解決（**どちらが新しいか**）」と書いている。
★★**どちらも「2 つのうち片方を残す」形をしている。**★★

→ ★**字面のまま読むと、両方を残す形が N-5 の選択肢から先に落ちる。**

★★**D105-2 が決めたのは主体である。**★★
★**出所欄が「問い 2 前半」と書いており、前半は「保管庫か判断する側か」である。**
→ ★**括弧の中は問いの文脈を写した説明であって、方式ではない。**
→ ★★**N-5 はこの字面に縛られない。★縛られないことをここに書いておかないと、次に読む人が縛られる。**★★

★**これは D105-2 の訂正ではない。★決定の中身は 1 文字も動かない**（★動いたのは読み方の注記だけ）。

### 12-7. ★★ N-10 は N-5 の下流ではない —— 双方向である（★新しい所見）★★

★**§11-5 は並行 4（N-10 / N-13）を「本線を待たないが本線と無関係ではない」= ★一方向と書いている。**
★**N-10 の本文も「N-5 と同じ量を使うので、切り離して決めない」と書くだけで、向きを書いていない。**

★★**確かめた結果、向きは両方ある。**★★

| 向き | 根拠（★**実装の事実**） |
|---|---|
| **N-5 → N-10** | ★**方式が「基準点との比較」を要求するなら、器は基準点を持たねばならない。**★いま `decks` に基準点は無い（12-5 の事実 3） |
| ★★**N-10 → N-5**★★ | ★★**器が何を貯めるかが、比べられる入力そのものを決める**★★ —— ★`DeckDao.save` は「全削除 → 全挿入」なので（12-5 の事実 2）、★**保存の時点で「何が変わったか」は残っていない。**★器が最終状態だけを貯めるなら、方式は最終状態しか比べられない |

→ ★★**N-14 と同じ関係である**★★（§11-5 が「独立」「並行」に続く**3 つ目**として立てたもの）。
→ ★**並行レーン 4 に置いたままにすると「並行だから後でよい」と読める。**
★**着手は並行でよいが、★N-5 と切り離して確定できない。**

★★**§11-1 の図と §11-5 の表にこれを足さないと「依存から機械的に導いた」が偽になる**★★
（★**§11-7 が自分でそう書いている** —— 「新しい所見が出た場合 → この図に足す」）。

### 12-8. ★★ 利用者にしか答えられないもの —— 分離してから数えた（§7-1 の訂正どおり）★★

★★**先に従属を判定した。★独立と判定できたものだけを数える。**★★

| # | 候補 | 判定 |
|---|---|---|
| **(f-1)** 衝突条件 | ★**技術判断。**★推奨は技術的に出せる | ★**数えない** |
| **(f-2)** 粒度 | ★**技術判断。**★12-5 の事実が入力で、好みでは決まらない | ★**数えない** |
| **(f-3)** 主体 | ★**技術判断** | ★**数えない** |
| **(f-5)** 失われるものの扱い | ★★**既に決まっている（D35）**★★ | ★**数えない**（12-9） |
| **(f-6)** 比べる量 | ★**Q1 の対象。技術判断** | ★**数えない** |
| ★**(f-4)** 自動で解くか、選ばせるか | ★★**利用者。**★★ ★**D105-6 からは導けない**（12-6）。★**条文にも実データにも根拠が無い**（`CLAUDE.md` §7 の表 #1） | ★★**1**★★ |
| ★**使い方の想定** 同じデッキを 2 台で編集するか | ★★**利用者。**★★ ★**D105-1 は「オフラインで編集できる」までしか言っていない**（★同じデッキかどうかは述べていない）。★**「使い方の想定」**（同上 #1） | ★★**2**★★ |

★★**独立と判定できたのは 2 つである。**★★ ★**閾値には触れていない**（§7-1 の新条件）。

#### ★ 問いの文言（★★二択にしない / §7-8★★）

★**記録として残す** —— ★**問いに立てなければ返らない**（§9-1）。

> ★**1.** 2 台で別々に直したデッキが食い違ったとき、アプリはどう振る舞うべきですか。
> **黙って片方に揃えてよい**ですか。**そのつど知らせるべき**ですか。
> **知らせたうえで、どちらを残すか選ばせるべき**ですか。
> ★**「場合による」ならどの場合かも教えてください**（例: 名前だけ違うときと、入っているカードが違うとき）。
>
> ★**2.** **同じデッキを 2 台で編集する**ことはありますか。
> それとも端末ごとに作るデッキは分かれていて、同期は「**別の端末でも見られる**」ためのものですか。
> ★**どちらとも言い切れない**なら、そう教えてください。

★★**二択にしていない。**★★ ★**「どちらでもない」が答えになりうる形にしてある**（§7-8）。

> ★★**2026-08-28: 2 問とも回答を得た（決定 **D107**）。**★★
> ★**回答そのものは §9-6。**★**回答後の (f-1)〜(f-6) の状態は §12-11。**
> ★★**上の表と問いの文言はそのまま残す**★★ —— ★**何を聞いたかが消えると、
> 「返らなかったもの」を「聞いたのに返らなかった」と読み違える**（§9-1 / §9-5 の作法）。
> ★★**問い 1 が「場合による」を許していたことは、(f-2) の入力として §12-11 が使っている。**★★

### 12-9. ★ (f-5) は既に決まっていた（★止まる条件に当たった）

**D35**: 「カードマスタに存在しない printingId を**黙って削除しない**。検出して読み取り専用で示す。
黙って消すとデッキが静かに壊れる」

★**D-22 / 決定 D95 が同じ原則を盤面へ広げている**
（`CLAUDE.md` の D-22 の行が「**決定 D35 を盤面へ広げただけで新しい原則ではない**」と書いている）。

→ ★★**衝突解決で片側の刷りが消えるのは、D35 が禁じた事象を別の機構で起こすことである。**★★
→ ★**決まっていないのは「どう見せるか」だけ**で、それは **N-13** の側にある。

★★**ただし D35 は「消さない」であって「両方残す」ではない。**★★
★**D35 が禁じているのは「黙って」の側である。**★★**方式をここから導かないこと。**★★

> ★★**2026-08-28: 回答が来た。★歯止めは効いていた（★検算）。**★★
>
> ★**利用者は「消えるのは困る」と言い、処方として「知らせる / 後から分かる」を挙げた**（§9-6）。
> ★**「両方残せ」とは言っていない。**
> → ★★**上の「D35 は『消さない』であって『両方残す』ではない」という読みが、独立に裏を取れた。**★★
>
> ★★**ただし D35 の 2 つの要素のうち、及んだのは片方だけである。**★★
>
> | D35 の要素 | 同期の衝突解決に及んだか |
> |---|---|
> | ★**黙って消さない** | ★★**及んだ**★★（**D107-1** の後半） |
> | ★**消さない（読み取り専用で残す）** | ★★**及んでいない**★★ —— ★**D107-1 の前半が「既定は最新の履歴に合わせる」と明示している。★片方は消える** |
>
> → ★★**「D35 が及んだ」を「同期でも消してはならない」と読まないこと。**★★
> ★**上の「衝突解決で片側の刷りが消えるのは、D35 が禁じた事象を別の機構で起こすことである」は、
> ★★『黙って消える』場合に限って読むこと★★**（★**すぐ下の但し書きが既にそう限定している**。★**訂正ではない**）。
>
> ★★**歯止めは維持する。★ここから方式を導かない。**★★
> ★**内訳の正は `docs/決定事項一覧.md` の **D107-4**。★ここに写さない**（§8）。

### 12-10. ★ Q1 / Q2 / Q3 と A-1 / A-2 / A-3 の噛み合いを検算した

| 確かめたこと | 結果 |
|---|---|
| **Q1** は N-5 の下流か | ★**そう。**根拠は §11-4 の表（「比較に使う量が決まらないと選べない」）。★**(f-6) と対になる** |
| **Q3** は N-5 の下流か | ★**そう。**根拠は §11-4（「望ましいかは方式次第」）。★**A-2 の論点 2 が同じことを書いている**（§1） |
| **Q2** は | ★**外れている**（**D105-2** / §11-3）。★**2 回動いて 2 回目で元へ戻った** |
| **A-1 / A-2** は N-5 の下流か | ★**そう。**★★**ただし A-1 は Q2 にも依存する**★★ —— §1 の A-1 の「決まっていないこと」が「**Q1 と Q2**」と書いている。★★**§11-1 の図にその辺が無い**★★（並行 1 は **A-3 だけ**を指している） |
| ★**A-3** は N-5 の下流か | ★★**違う。**★★ §11-1 / §11-5 は **A-3 の「向き」を Q2（並行 1）**に置き、★**本線に従属するのは「優先度」だけ**である（§4 / ★§11-5 が「**向きと優先度を混ぜない**」と書いている） |

→ ★**A-1 は本線（Q1）と並行 1（Q2）の両方が終わるまで実装に入れない。**★**図の辺が 1 本足りない。**

---

### 12-11. ★★ 回答後の (f-1)〜(f-6) —— 何が閉じ、何が残ったか（2026-08-28 / 決定 **D107**）★★

★**回答そのものは §9-6。**★**決定としての実体は `docs/決定事項一覧.md` の **D107** である。**
★**ここに置くのは「6 つの問いのどれがどこまで動いたか」だけである**（★**方式は書かない** / §8）。

#### ★★ (f-4) は完全には閉じていない。★先にそれを書く ★★

★**§12-8 の問い 1 は 3 つを提示した**（★**黙って揃える / そのつど知らせる / 知らせたうえで選ばせる**）。

| # | 何が決まったか |
|---|---|
| **(i)** | ★★**「黙って片方に揃える」は却下された。**★★ ★**利用者が「消えるのは困る」と明示した**（§9-6 の段 4） |
| **(ii)** | ★★**既定の向きは「最新の履歴に合わせる」。**★★ ★**＝ 既定は自動で解く。★利用者の介入を待たない** |
| **(iii)** | ★★**事前に知らせるか、事後に分かるようにするかは決まっていない。**★★ ★**利用者が「または」で並べた** → ★**この 2 つの中からの選択は技術判断に降りた** |
| **(iv)** | ★★**「知らせたうえで選ばせる」を置くかは決まっていない。**★★ ★**問いには入っていたが選ばれなかった。**★★**選ばなかったことを却下と読まない**★★ —— ★**利用者が挙げたのは「消えるのは困る」への処方であって、選択権を否定してはいない** |

→ ★★**「利用者にしか答えられない部分」としては閉じた。★方式としては 1 つに絞れていない。**★★
★**この 2 つを混ぜないこと** —— ★**「聞き直す必要は無い」と「方式が決まった」は別である。**

★★**(iv) の扱い**★★: ★**選ばせる口を足すことは D107 に反しない**（★**既定を (ii) から変えない限り**）。
★**逆に、(iv) を「決まった」として扱うのは D107 を超える。**

#### ★ (f-1) / (f-2) / (f-3) / (f-6) —— 技術判断のまま。★狭まったかを 1 つずつ確かめた

| # | 回答は狭めたか | 根拠 |
|---|---|---|
| **(f-1)** 衝突条件 | ★★**狭めていない。**★★ ★**ただし重みが増した** —— ★**D107-2 により「両側とも変わったか」を問う経路が★実際に走る**。★**§12-4 が「そこに番号が無い」と書いたまま実装に入る危険が上がった** | §12-4 / **D107-2** |
| **(f-2)** 粒度 | ★★**狭めていない。**★★ ★**事実が 1 つ増えただけである** —— ★**問い 1 は「名前だけ違うとき」と「入っているカードが違うとき」を例示して「場合による」を許していた。★利用者は場合分けしなかった。**★★**これは「場合分けが不要と決まった」ではない**★★（★上の (iv) と同じ作法） | §12-8 の問いの文言 / §12-5 |
| **(f-3)** 主体 | ★★**狭めていない。★ただし (f-4) と結びついた。**★★ ★**(iii) で「消える前に知らせる」を採ると、★知らせる相手が居る端末で解く必要が出る。**→ ★**(f-4) の残りが (f-3) を狭めうる** | **D105-2**（主体はアプリ側）/ 上の (iii) |
| ★★**(f-6)** 比べる量★★ | ★★**狭めていない。★「最新」を時刻と読み替えないこと**★★ | ★**下記** |

#### ★★ (f-6) —— 「最新の履歴」は方式ではない ★★

★★**利用者が言ったのは「最新の履歴」である。★「最新の時刻」とは言っていない。**★★

| 誰が | 何と言ったか |
|---|---|
| ★**利用者** | ★★**「最新の履歴に合わせてください」**★★（§9-6 の段 1。★**段 4 のあとも既定として残った**） |
| ★**相談役** | 「比べられるのは**端末の時計**であり、時計のずれで…」（§9-6 の段 2） |

→ ★★**時計は相談役が説明のために置いた仮定であって、利用者の回答ではない。**★★
→ ★**「履歴」は順序の語であり、比べる量を名指していない。**
→ ★★**(f-6) は開いたままである。**★★ ★**方式はここに書かない。**

★★**型は §12-6 と同じである。★これで 2 例目になった。**★★

| # | どこ | 方式に見える字面 | 実際は |
|---|---|---|---|
| **1** | **D105-2** の括弧「（どちらが新しいか）」 | ★**「2 つのうち片方を残す」形** | ★**問いの文脈を写した説明**（§12-6） |
| ★★**2**★★ | ★**D107-1** の「最新の履歴に合わせる」 | ★**「時刻で比べて新しいほうを採る」形** | ★★**利用者の語であって、比べる量の指定ではない**★★ |

→ ★★**回答や問いの文言は、方式の指定ではない。**★★
★**縮めたときに何が落ちるかの正は `docs/決定事項一覧.md` の **D107-3**。★ここに写さない**（§8）。

#### ★ A-1 / A-2 への効き方（★★新しい辺は無い★★）

★**(f-6) が `revision` を含む量を採った場合に限り**、**A-1**（**D-9**）と **A-2**（**D-26**）は
★★**「差分検出の不具合」から「D107-1 の後半が禁じた『黙って消える』の経路」に変わる**★★ ——
★**中身が違うのに `revision` が同じなら、どちらを採っても片方が黙って消える。**

★★**条件つきである。★(f-6) は決まっていないので断定しない。**★★
★★**図に足す辺は無い。**★★ ★**依存は既に在る**（**N-5** → **Q1** → **A-1** / **A-2** / §11-1）。
→ ★**変わるのは A-1 / A-2 が壊すものの★大きさであって、順序ではない**
（★**§11-5 の「向きと優先度を混ぜない」と同じ作法**）。

---

## 13. ★ N-5（①(f)）の案 —— ★★並べるだけ。1 つも選ばない ★★

作成日: 2026-08-29

★★**この節だけが選択肢を出す。**★★ ★**§1〜§12 は論点表のままである**（冒頭の訂正）。
★★**推奨を書かない。★方式を決めない。**★★ ★**書くのは「案」と「代償」と「何に依存するか」だけ。**

★**なぜ今この段に入れるか** —— ★**N-5 の利用者判断が `決定 D107` で揃い、
「利用者にしか答えられない論点」は再び 0 になった**（§9-6）。→ ★**残りはすべて技術判断である。**

★★**扱うのは 4 つだけである**★★ —— **(f-6)** / **(f-2)** / **(f-1)** / **(f-3)**。
★**(f-4) の残り（(iii) 事前か事後か / (iv) 選ばせる口を置くか）は触らない** ——
★**N-13 とまたがり、その境界が確かめられていない**（§11-4 の末尾の未了）。
★**(f-5) は既に決まっている**（**D35** / §12-9）。

---

### 13-1. ★ 入力の点検 —— 揃っていないもの

#### (a) **N-10**（貯める器）—— ★★ 全案が器に何かを要求する。違うのは「何を」だけ ★★

★**§12-7 が「双方向」と確かめたとおりだが、案を並べてみると★それより強い。**

| 何を器に要求するか | その要求を持つ案 |
|---|---|
| ★**前回同期時点の目印**（デッキ 1 つにつきスカラ 1 個） | ★★**選択的に送るすべての案**★★ —— ★**Q1（差分検出）自体が「★前回同期のあと変わったか」を問う**（★**§12-4 の表の字面**）ので、目印なしでは答えられない |
| ★**前回同期時点の内容**（スナップショット） | **(f-6) 案 4** / **(f-1) 案 A** |
| ★**操作の列**（何を足した・減らした・並べ替えた） | **(f-6) 案 6** |
| ★**失われた側の内容**（「後から分かる」の材料） | **(f-1) 案 F** / **(f-4)** の (iii) が「事後」に倒れた場合 |
| ★**何も要求しない** | ★**毎回まるごと送る案だけ**（★差分検出をしない。★代償は通信量と、衝突の頻度が上がること） |

→ ★★**「N-10 が決まらないと選べない案がある」ではなく、「どの案も器に注文を出す」である。**★★
→ ★**N-10 を先に決めると、決めた器が案を落とす。**★**§12-7 の双方向はこの形で効く。**

#### (b) **N-14**（役割の分離）—— ★ サーバーが「比べる」ことを許すか

★**D105-2 は「衝突解決はアプリ側」と決めたが、★「サーバーは一切比較しない」とは言っていない。**
★**その線がどこにも引かれていない**のが **N-14 の論点 (2)** である。

| N-14 が決まらないと選べない案 | 何をサーバーにさせるか |
|---|---|
| **(f-6) 案 7**（サーバー採番の版） | ★**書き込みごとに番号を採る** |
| **(f-1) 案 C**（楽観ロック） | ★★**送られてきた版と手元の版を比べて、古ければ拒む**★★ |
| **(f-3) 案 1**（送信側が解く）を安全にする場合 | ★同上（★読み → 解決 → 書き のあいだに第 3 の書き込みが入るのを防ぐ） |

★**(f-1) 案 F**（上書きされた版をすべて残す）は ★★**N-14 に掛からない**★★ ——
★**保管は保管庫の役割そのものである**（**D106-2** の 3 役割の表）。

#### (c) そのほか

| # | 何 | どの案が依存するか |
|---|---|---|
| **N-11** 論点 (1)（同定の粒度） | ★★**版ベクトルは端末を同定できないと作れない。**★★ ★**実読で確認** —— `lastDeviceId` は `lib` の 4 箇所とも**持ち回るだけ**で、★**値を入れる場所が 1 つも無い**（既定は空文字 / §10 の N-11 の事実 3 が今も真である） | **(f-6) 案 3** / **(f-1) 案 B** |
| ★**§11-7 の昇格条件** | ★**上の 2 案のどちらかを採ると、N-11 は「入力になりうる」から★本線の上流へ昇格する**（★**§11-7 が条件を書いている**）。★**採らなければ昇格しない** | 同上 |
| **(f-4)** の (iii)（事前 / 事後） | ★★**(f-3) の絞り込みができない**★★（§12-11）。→ ★**(f-3) は★並べるところまでである** | **(f-3)** の全案 |
| **N-13** 論点 2 | ★**「衝突の通知が編集を止めてよいか」がどこにも決まっていない。**★**(f-3) の「解決をいつ走らせるか」に直に効く** | **(f-3)** |

---

### 13-2. ★★ (f-6) 何を比べるか ★★

★★**利用者は「最新の★履歴」と言い、時刻とは言っていない**★★（**D107-3** / §12-11）。
→ ★**時計を前提にした案だけを並べない。**★**各案が「最新」を何と読むかを併記する。**

★**物差しは 3 つ** —— (i) **D107-1 後半**（黙って消えない）を満たせるか /
(ii) **§12-5 の実装の事実 5 つ**（特に ★**`ord` が再計算できない**）と噛み合うか / (iii) **代償**。

> ★★**2026-08-29 の警告: 下の 7 案の「D107-1 後半」欄は、物差し (i) の語で揃っていない。**★★
> ★**案 6 だけが「最も精度が高い」と★別の軸の語で書かれている**（ほかの 6 案は「満たせる / 部分的に満たせる /
> 単体では満たせない」）。→ ★★**「満たせる」の字面で横に抜くと案 6 が落ちる。★実際に落ちた**★★
> （★**経緯は §14-2 / 型は `ルール整合性チェック_v1.06.md` **D-35**。★ここに写さない** / §8）。
> → ★★**この表を横に読むときは、字面ではなく意味で読むこと。**★★
> ★**欄そのものは書き換えない** —— ★**案 6 にとっては「最も精度が高い」が最も正確であり、★書いた時点で誤りではない。**

| # | 比べる量 | 新設 | ★「最新」が何になるか |
|---|---|---|---|
| **1** | `decks.updatedAt`（端末の壁時計） | ★**不要** | ★時計が後のほう |
| **2** | `decks.revision`（整数） | ★**不要**（★ただし **A-1** / **A-2** の穴が塞がっていること） | ★★**決まらない**★★ |
| **3** | ★**版ベクトル**（端末ごとのカウンタ） | ★**要る**（列 1 本 or 表 1 つ）＋ ★**端末 ID** | ★**並行なら決まらない** |
| **4** | ★**基準点との 3 者比較**（base / local / remote） | ★**要る**（基準点の器 ＝ **N-10**） | ★**定義しない**（★合成する） |
| **5** | ★**内容ハッシュ** | ★不要 〜 列 1 本 | ★**定義しない**（★順序が無い） |
| **6** | ★**操作の履歴**（編集ログ） | ★**要る**（ログの器 ＝ **N-10**） | ★**並行なら決まらない** |
| **7** | ★**サーバー採番の版 / 到着順** | ★**要る**（サーバー側）＋ アプリ側に「最後に受け取った版」 | ★**後から届いたほう** |

#### 案 1 —— `updatedAt`（端末の壁時計）

- **D107-1 後半**: ★★**単体では満たせない。**★★ ★**「捨てた側が★編集だったか」を判定する材料が無い。**
  内容が違えば鳴らすしかなく、★**相手だけが編集した通常の同期でも鳴る。**
  → ★**鳴りっぱなしの警告は読まれない**（★`CLAUDE.md` §3 の「★**常に非 0 なら誰も読まない**」と同じ型）。
- **§12-5**: ★**事実 3** により deck 単位でしか比べられない → ★粒度は deck 全体に固定。
  ★**`ord` は勝った側のリストごと採るので再計算不要**（★この案の唯一の強み）。
- **代償**: (i) ★★**時計のずれで「先に編集したほうが最新」と判定されうる**★★（★§9-6 の段 2 が既に指摘）。
  ★**実読で確認** —— `loveca-ui/lib/src/data/clock.dart` の `systemClockUtc()` は素の
  `DateTime.now().toUtc()` で、★**NTP を前提にできない。**
  (ii) **D107-2**（衝突は日常）により、★**ずれを踏む回数が増える。**
  (iii) ★★**「最新の履歴」を「最新の時刻」と読み替えることになる**★★ ——
  ★**D107-3 がまさにその読み替えを禁じている。**

> ★★**2026-08-29（決定 **D108-3**）: 代償 (iii) は「案 1 を★単層で使う」ことの代償である。**★★
> ★**2 層に分けて決着層だけに使う場合は当たらない**（★**因果が順序を与えない場面でしか使わないので、
> 置き換える対象そのものが無い**）。★**判定の正は §14-3。★ここに写さない**（§8）。
> ★★**代償 (i)(ii) は 2 層に分けても残る。★性質だけが変わる**★★（同上）。
> → ★**この 3 つを「案 1 の代償」として一括で読まないこと。**

#### 案 2 —— `revision`

- **D107-1 後半**: ★**部分的に満たせる。**「同じ `revision` なのに内容が違う」を衝突として検出できる。
- ★★**ただし「最新」が決まらない。**★★ `DeckRepository.save` は `base.revision + 1`（実読で確認）なので、
  ★**2 台とも 5 → 6 になる。**★**大小比較で順序が付かない場面が必ず出る。**
- **代償**: (i) ★★**A-1（D-9）/ A-2（D-26）の格が上がる**★★ —— §12-11 が
  「(f-6) が `revision` を含む量を採った場合に限り、A-1 / A-2 は『差分検出の不具合』から
  『**黙って消える**の経路』に変わる」と**条件つきで**書いている。★**この案はその条件を成立させる。**
  (ii) ★**決着用に別の量が要る**（★案 1 / 案 7 との合成になる）。
  (iii) ★`revision` が同じでも内容は違いうるので、★**単独では「変わったか」しか言えない。**

#### 案 3 —— 版ベクトル

- **D107-1 後半**: ★★**満たせる。**★★ 「片方が他方を包含するか」で**「更新」と「衝突」を機械的に分けられる**
  → ★**通知が空振りしない。**
- **§12-5**: ★**事実 3** のとおり deck 単位が自然。★★**entry 単位にすると事実 2 と正面衝突する**★★
  （`save` が全削除 → 全挿入なので、★**消して入れ直した行のベクトルを復元できない**）。
- **代償**: (i) ★★**N-11 の論点 (1) が入力になる**★★（★13-1 (c) のとおり `lastDeviceId` は空文字のまま）。
  (ii) ★★**§11-7 により N-11 が本線の上流へ昇格する**★★ —— ★**束 A の順序が動く唯一の案である。**
  (iii) ★**並行なら「最新」が決まらない**ので決着用の量が別に要る。
  (iv) ★端末が増えるとベクトルが伸びる → ★**消えた端末の掃除が新しい論点になる。**

#### 案 4 —— 基準点との 3 者比較

- **D107-1 後半**: ★★**満たせる。**★★ 「両側とも base から変わった」が **(f-1)** の定義になり、
  ★**さらに「何が消えるか」を差分として出せる**（★**事前通知の材料になる 2 案のうちの 1 つ**）。
- **§12-5**: ★★**`ord` と正面からぶつかる。**★★ ★**事実 4 / 5** ——
  両側の変更を合わせる形にすると、★**合わせた結果の `ord` を作らねばならない**が、
  ★**`ord` は再計算できない**（**D99** が手動並べ替えを残した）。
  ★`deckOrderInsertionIndex` は位置を 1 つ決められるが、★**D99 自身が「手動順が混ざったデッキでは
  規則順の正しい位置とは限らない」と書いている。**
- **代償**: (i) ★★**基準点は 2 段ある**★★ —— ★**内容ごと持つ**（「何が消えるか」まで出せる / 保存量が大きい）か、
  ★**ハッシュだけ持つ**（「変わったか」しか言えない / 軽い）。★**同じ「基準点」の語で 2 つを混ぜない。**
  ★★**2026-08-29: 「保存量が大きい」を実測に置き換えた**★★ —— ★**デッキ 1 つあたり
  典型 950 B / 全部 1 枚ずつなら 3,267 B**（★**entries 数の上限 72 は 6.1.1.1 / 6.1.1.3 由来で、
  entries 部分の上限は 3,396 B**）。★★**ただしデッキ 1 つ全体に上限は無い**★★ ——
  ★**`name` / `memo` に長さ制限が 1 つも実装されていない**（走査で 0 件）。
  ★**測り方・標本点・測れなかったもの（デッキの本数）の正は §14-5**（**D-28**）。★**ここに写さない**（§8）。
  (ii) ★**N-10 と同時に決めるしかない**（★§12-7 の双方向そのもの）。
  (iii) ★**基準点を失うと全デッキが衝突になる**（★入れ直し / 端末の乗り換え）。

#### 案 5 —— 内容ハッシュ

- **D107-1 後半**: ★★**単体では満たせない。**★★ 「違う」ことしか言えず**順序が無い。**
- **§12-5**: ★★**`ord` を含めるかで意味が変わる。**★★
  含めると「並べ替えただけ」も衝突になり、★**含めないと並べ替えだけの違いが黙って消える**（D107-1 後半に触れる）。
  ★**実読で確認** —— `Deck.toJson()` は `ord` という**列を持たず、順序をリストの順として持つ**ので、
  素直に取ると `ord` を含む側になる。
- **代償**: (i) ★**補助にしかならない**（★案 1〜4 / 7 のどれかと組んで初めて働く）。
  (ii) ★★**どの `Deck` を掴むかを間違えると別のハッシュになる**★★ —— `applyEnergyFill`（**D96**）は
  盤面を開くときに**保存しない一時的な `Deck`** を作り、末尾にエネルギーを 1 行足す（実読で確認）。
  (iii) ★**器は既にある** —— `DeckDraft._orderedCounts`（★`printingId` と枚数を NUL 文字で連結した文字列の列）が
  ★**`isDirtyAgainst` のために「並び + 枚数」を比べる形で既に作られている**（**D99**）。

#### 案 6 —— 操作の履歴（編集ログ）

- **D107-1 後半**: ★★**最も精度が高い。**★★ 「相手が足した 1 枚が消える」まで言える。
- **§12-5**: ★★**事実 2 が最大の障害**★★ —— `DeckDao.save` は全削除 → 全挿入で、
  ★**コメントが自ら「増分の保守が要らない」と書いている。**
  → ★**保存の時点で「何が変わったか」が残っていない**（★**§12-7 の N-10 → N-5 の向きの根拠そのもの**）。
  ★**逆に、事実 5（`ord` は再計算できない）は解ける** —— ★**並べ替えを操作として記録すれば済む。**
  ★★**`ord` に答えを持つ唯一の案である。**★★
- **代償**: (i) ★**実装量が最も大きい。**
  (ii) ★`DeckDao.save` の「増分の保守が要らない」という設計上の利点を手放す。
  (iii) ★**ログをいつ捨てるかが新しい論点になる。**
  (iv) ★★**利用者の語（「最新の履歴」）に字面が最も近いが、★字面が近いことは根拠ではない**★★
  （★**D107-3** / §12-11 がまさにこの読み替えを禁じている。★**逆向きにも適用する**）。

#### 案 7 —— サーバー採番の版 / 到着順

- **D107-1 後半**: ★★**満たせる。**★★ 「自分が持っている版がサーバーの最新でない」が機械的に分かる。
- **§12-5**: deck 単位。★**`ord` は勝った側ごと採るので問題なし。**
- **代償**: (i) ★★**N-14 に正面から掛かる**★★（13-1 (b)）。
  (ii) ★★**オフライン中は版が採れない**★★（**D105-1**）→ ★**貯めているあいだの端末どうしの順序は
  この量では決まらない。**
  (iii) ★**D105-5**（版がずれる）の影響を受ける。
  (iv) ★★**到着順を「最新」と読むと、1 週間前の編集が今日の編集に勝ちうる**★★
  （★**後から同期した端末が勝つ**）。★**時計のずれとは別の型の逆転である。**

#### ★★ 構造の所見 —— 「最新」が常に決まる量は、因果を見ていない量である ★★

★**上の 7 案を横に読むと 1 本の線が出る。**

| 「最新」が常に一意に決まるか | 案 |
|---|---|
| ★**決まる** | **1**（壁時計）/ **7**（到着順） —— ★★**どちらも因果ではなく★外部の順序である**★★ |
| ★**並行なら決まらない** | **2** / **3** / **6** —— ★**因果を捉える量ほど、まさに衝突の場面で沈黙する** |
| ★**そもそも順序を定義しない** | **4** / **5** |

→ ★★**D107-1 の前半（既定は必ず 1 つに決まること）と後半（黙って消えないこと）は、
物差しに逆向きの注文を出している。**★★
→ ★★**したがって (f-6) は「1 つの量を選ぶ」問いではないかもしれない**★★ ——
**検出用の量**（因果）と**決着用の量**（全順序）を**分ける**形がありうる。
★**§12-4 が「3 つあるのに番号は 2 つ」と見つけたのと同じ形である。**

★★**ただしこれで案 1 / 案 7 が落ちるわけではない。**★★
★**1 つの量で両層を兼ねられる**（★その代償が「検出の空振り」であり、各案に書いてある）。
→ ★★**「2 層に分ける」を前提として書かない。★書けば案 1 / 7 が先に落ちる。**★★
★**§7-8 の型を自分に適用している** —— ★**並べ方が答えの形を決めてはならない。**

---

### 13-3. ★★ (f-2) 粒度 —— 従属する。★案を並べない ★★

★**§12-5 が自分で答えを書いている** ——
「粒度を entry 単位に採ると**事実 3** により比べる量が 1 つも無い」/
「列単位に採ると**事実 4 / 5** により `ord` だけは列にならない」/
→ ★★**「どちらも『足りない量を新設するか』という問いに変わるだけである」**★★。

★**「足りない量を新設するか」は (f-6) の問いそのもの**である。
→ ★★**判定の粒度は (f-6) に従属する。★独立に案を並べない。**★★

#### ★ ただし (f-2) は 2 つを含んでいる

| | 従属するか |
|---|---|
| ★**判定の粒度**（どの単位で「衝突した」と言うか） | ★★**従属する**★★ —— ★**比べる量より細かくは判定できない** |
| ★**解決の粒度**（どの単位で置き換える / 残す） | ★★**上限としてのみ従属する**★★ —— ★**判定より細かくはできないが、粗くはできる** |

→ ★**(f-6) が deck 単位の案（1 / 2 / 3 / 5 / 7）に決まれば、解決の粒度も deck 単位に固定される**（★自由度 0）。
→ ★**案 4 / 6 を採ったときにだけ、解決の粒度に 1 問残る。**
→ ★★**いま並べるべき案は無い。★(f-6) のあとに条件つきで 1 問。**★★

#### ★ 実装の事実を 1 つ足す（★§12-5 の 5 つに無い）

★**事実 1**（主キーは `{deckId, printingId}`）は、★**entry 単位の同定を可能にしている。**
→ ★★**entry 単位に足りないのは「比べる量」だけであって、
「どの行とどの行が対か」は既に決まっている。**★★
★**事実 1 と事実 3 は逆を向いている。★混ぜて「entry 単位は不可能」と読まないこと。**

---

### 13-4. ★★ (f-1) 衝突判定 —— 案の一覧 ★★

★**定義したいのは「★両側とも変わったか」である**（§12-4）。

| # | 何を衝突と呼ぶか | 要る量 | **D107-1 後半** | 代償 |
|---|---|---|---|---|
| **A** | base ≠ local **かつ** base ≠ remote | 基準点（**(f-6) 案 4** / **N-10**） | ★**満たせる**（★空振りしない） | ★基準点の器が要る。★**失うと全件が衝突になる** |
| **B** | どちらの版ベクトルも他方を包含しない | 版ベクトル（**(f-6) 案 3** / **N-11**） | ★**満たせる** | ★★**N-11 が本線の上流へ昇格しうる**★★（§11-7） |
| **C** | 送信時、手元の版とサーバーの版が違う（楽観ロック） | サーバー採番の版（**(f-6) 案 7** / **N-14**） | ★**満たせる**（★**Q1 と組にして初めて両側判定になる** —— ★§12-4 が分けた 2 つがちょうど噛み合う） | ★**N-14 に掛かる。**★オフライン中は版が採れない。★★**押し負けた側にしか通知が立たない**★★ —— ★先に送って後から上書きされた端末は、次に受け取るまで何も知らない |
| **D** | local ≠ remote（★判定せず、常に衝突扱い） | ★**無し**（既存で足りる） | ★**形だけ満たせる** | ★★**編集していない側でも鳴る**★★ → ★**D107-2（日常）と組むと鳴りっぱなしになり、通知が意味を失う**（★**(f-6) 案 1 と同じ型**）。★実装は最も軽い |
| **E** | ★**判定しない**（常に「最新」を採る） | ★**無し** | ★★**満たせない**★★ —— ★**消えたことを検出する材料が無いので、知らせることも後から示すこともできない** | ★★**D107-1 後半と正面から衝突する。★採れない**★★ |
| **F** | ★**判定しない。ただし上書きされた版をすべて残す** | ★**無し**（★サーバー側の保存だけ。★**N-14 に掛からない**） | ★**「後から分かる」は満たせる。**★★**「消える前に知らせる」は満たせない**★★ | ★★**(f-4) の (iii) が「事前」に倒れた瞬間に落ちる**★★（★**触らないと決めた項目に依存する唯一の案**）。★保存量。★**古い版をいつ捨てるかが新しい論点になる**。★見に行く導線が要る（**N-13**） |

★★**案 E を落とすために並べた。**★★
★**「検討していない」と「検討して落ちた」を区別する**（`docs/相談役への引き継ぎ.md` §4-9）。

#### ★★ (f-1) に番号を採るか —— ★採らない（判断した）★★

★**§12-4 が「衝突判定はどこにも無い」と書いたので、番号を採るかを確かめた。**

| 確かめたこと | 結果 |
|---|---|
| ★**番号が本当に無いか** | ★★**在る。★(f-1) がその番号である**★★（★§12-3 が 2026-08-28 に与えた） |
| ★**§12-4 が「どこにも無い」と書いた対象は何か** | ★★**`Q1` / `N-5` の系列（§11-1 の図の節点）に無い、という意味である**★★ |
| ★**図に (f-1) を足す必要があるか** | ★★**無い。**★★ ★**図は (f-1)〜(f-6) を 1 つも載せていない** —— ★**節点の粒度が `N-x` / `Q-x` であって、N-5 の内側ではない。**→ ★**穴ではなく粒度である** |

★★**ただし §12-4 の表に読み違えの罠がある。**★★
★**衝突判定を `Q1`（★N-5 の★下流）と `N-5`（★本線）と同じ表に並べているので、★衝突判定も下流に見える。**
→ ★★**実際は N-5 の★内側である。**★★
★**`Q1` は方式が決まったあとに決める問いだが、★(f-1) は方式の一部として同時に決まる。**

---

### 13-5. ★★ (f-3) 解決の主体 —— 並べるところまで ★★

★**D105-2 が「アプリ側」まで決めている。★どちらのアプリか。**

| # | 誰が解くか | 代償 |
|---|---|---|
| **1** | ★**送信側**（送る前にサーバーの現在の内容を取り、手元で解いてから送る） | (i) ★送信のたびに 読み → 解決 → 書き の往復。(ii) ★★**解決と書き込みのあいだに第 3 の書き込みが入ると取りこぼす**★★ → ★**(f-1) 案 C（楽観ロック）が要り、N-14 に掛かる**。(iii) ★貯めているあいだの編集をどう畳むかが **N-10** 側の論点になる |
| **2** | ★**受信側**（受け取った内容と手元を突き合わせて解く） | (i) ★**サーバーには片方しか残らない**ので、遅れて受け取る端末が「何が消えたか」を知る材料が別に要る（**N-10** / **(f-1) 案 F**）。(ii) ★★**上書きされた側は自分が上書きしたことに気づかない**★★ |
| **3** | ★**両方が独立に解く**（★結果が一致することを保証する） | ★下の「軸 B」—— ★★**決定性の前提が、実データで既に破れている**★★ |
| **4** | ★**サーバーが解く** | ★★**D105-2 が既に否定している。★採れない**★★（★並べるのは案 E と同じ理由） |

★★**絞り込めない理由**★★: §12-11 が「(iii) で『消える前に知らせる』を採ると、
知らせる相手が居る端末で解く必要が出る」と書いている。
→ ★**鋭く言い直すと** —— ★★**「事前に知らせる」を採ると、解決は利用者が応答できる文脈でしか走れない。**★★
→ ★**背景同期と両立しないか、「知らせるまで解決を保留する」器（N-10）が要る。**
→ ★**さらに N-13 の論点 2（衝突の通知が編集を止めてよいか）が未決である。**

#### ★★ (f-3) は 2 つの軸を含んでいる（★所見）★★

| 軸 | 中身 |
|---|---|
| ★**軸 A** | ★**どの端末が解くか**（上の表） |
| ★★**軸 B**★★ | ★**解いた結果を他方がどう受け取るか** —— ★**解決結果を送り返す**（1 回解けば済む）か、★**各端末が独立に解く**（★**決定性が要る**）か |

★★**軸 B に実データ由来の反例がある。**★★
★**「同じ規則を 2 か所で走らせて同じ結果を得る」先例は在る** —— **D99** の比較器は
`loveca-ui` と `loveca-db` の両方から呼ばれる（実読で確認）。
★★**しかしその比較器はマスタを引く。**★★
`deckOrderKeyOf` / `backfillOrd` の `keyOf` は `cards` / `printings` を引き、
★**マスタが空なら全件が `DeckOrderKey.unknown` になる。**
→ ★★**マスタの取り込み状態が端末ごとに違えば、同じ入力から違う結果が出る。**★★
★**これは推測ではない** —— ★**A-2 / D-26 が「マスタ未取り込みの端末では `printingId` 昇順だけになる」と
既に記録している事象そのものである。**
→ ★★**解決の規則がカードマスタを参照するなら、軸 B の「独立に解く」は成立しない。**★★
★**参照しない規則なら成立しうる。★どちらかはまだ決まっていない。**

> ★★**2026-08-29: 相談役が「軸 B の『各端末が独立に解く』は落とす」と推奨したので、上の条件を当てた。**★★
> ★**結果は「★参照しない」** —— ★**(f-6) 案 4 ＋ 案 1 ＋「マージしない」の 3 段は、
> D99 の比較器を 1 度も呼ばない**（★**実読で確認**）。
> → ★★**落とす根拠にならない。★軸 B は決めない**★★（★**(f-3) は決めないという指示のとおり**）。
> ★**呼び出し元の走査と 3 段の内訳の正は §14-7。★ここに写さない**（§8）。
> ★★**この行（「どちらかはまだ決まっていない」）は動かない**★★ ——
> ★**確かめたのは「この推奨なら参照しない」ことであって、★方式が決まったのではない。**

---

### 13-6. ★ この節が §11 に要求すること（★★辺は足さない★★）

★★**(f-6) 案 3 / (f-1) 案 B を採ったときに限り、N-11 が本線の上流へ昇格する。**★★
★**§11-7 の「N-11（同定の粒度）の答え」の行が既にこの条件を書いている**ので、
★★**行は増やさない。**★★

★★**§11-1 の図に辺は 1 本も足していない。**★★
★**この節は案を並べただけで、依存を新しく確かめてはいない。**
★**確かめていない辺を足すと「依存から機械的に導いた」が別の意味で偽になる**（§11-4 の末尾と同じ作法）。

### 13-7. ★ 止まる条件の点検（★記録として残す）

| 条件 | 結果 |
|---|---|
| ★**利用者判断が新たに要ると分かったか** | ★★**要らない。**★★ ★**(f-1) / (f-2) / (f-3) / (f-6) は §12-8 が 4 つとも技術判断と判定済み**で、案を並べても好みで決まる分岐は出なかった。★**「古い版をいつ捨てるか」（(f-1) 案 F）と「消えた端末の掃除」（(f-6) 案 3）は★案を採った場合にだけ開く下流の論点**である |
| ★**N-10 / N-14 が決まらないと案が並べられないか** | ★★**並べられた。**★★ ★**依存は案ごとに明記した**（13-1）。★**落ちた案は無い** |
| ★**既存の決定と矛盾する案しか出ない項目があるか** | ★★**無い。**★★ ★**矛盾するのは (f-1) 案 E（D107-1 後半）と (f-3) 案 4（D105-2）の 2 つだけ**で、どちらも同じ問いに他の案が在る |
| ★**触らないと決めた範囲に踏み込んでいないか** | ★**(f-4) の (iii) / (iv) は決めていない。**★**(f-1) 案 F と (f-3) がそれに★依存することを書いただけである** |

---

## 14. ★ N-5（①(f)）—— 相談役の推奨の検算（2026-08-29 / 決定 **D108**）

★★**この節が検算結果の正である。**★★
★**決定としての実体は `docs/決定事項一覧.md` の **D108** である**（★**骨だけがあちらに在る** / §8）。

★★**推奨は推奨であって決定ではない。**★★
★**6 つ検算した。★2 つが偽だった。★1 つは条件つきである。**
→ ★★**推奨 5 項目のうち、記録したのは 2 項目だけである。**★★

### 14-1. ★ 検算の結果（6 件）

| # | 検算したこと | 結果 |
|---|---|---|
| **4-1** | 「案 3 / 4 / 7 だけが D107-1 後半を単体で満たせる」は正しいか | ★★**偽。★案 6 が落ちている**★★（14-2） |
| **4-2** | 決着用に時計を使うことは **D107-3** の読み替えに当たるか | ★★**当たらない。★ただし理由 3 は過大である**★★（14-3） |
| **4-3** | 「マージしない」は **D107-1** 後半と両立するか | ★**両立する**（14-4） |
| **4-4** | 基準点を内容ごと持つ保存量（★**推測を実測に置き換える**） | ★★**実測した**★★（14-5） |
| **4-5** | §13-3 の「案 4 のときだけ残る 1 問」はこの推奨で閉じるか | ★**閉じる。★ただし検出案が決まってからである**（14-6） |
| **4-6** | 軸 B を落とす根拠（D99 の比較器がマスタを引く）はこの推奨に効くか | ★★**効かない**★★（14-7） |

### 14-2. ★★ 4-1 —— 「案 3 / 4 / 7 だけ」は偽。★案 6 が落ちている ★★

★**§13-2 の 7 案から「D107-1 後半」の欄だけを機械的に抜いた**（★**走査した範囲も書く** / §7-6）——

```bash
grep -n "D107-1 後半" docs/同期設計メモ.md
```

| 案 | §13-2 の「D107-1 後半」欄（★**字面のまま**） |
|---|---|
| **1** | 単体では満たせない |
| **2** | 部分的に満たせる |
| **3** | ★**満たせる** |
| **4** | ★**満たせる** |
| **5** | 単体では満たせない |
| ★★**6**★★ | ★★**「最も精度が高い」**★★ ——「相手が足した 1 枚が消える」まで言える |
| **7** | ★**満たせる** |

★★**「満たせる」の語で抜くと 3 / 4 / 7 になる。★意味で読むと 案 6 も入る。**★★
★**「最も精度が高い」は「満たせる」より★強い主張である**（★**案 4 の「何が消えるかを差分として出せる」より細かい**）。
→ ★★**「だけ」は偽である。★正しくは 3 / 4 / 6 / 7 の 4 つ。**★★

#### ★★ 案 6 は推奨自身の絞り込みを通過する ★★

★**推奨の理由 1 は 2 案を落とした。★同じ基準を案 6 に当てた。**

| 基準（★**推奨自身の言葉**） | 案 3 | 案 7 | ★★**案 6**★★ |
|---|---|---|---|
| **N-11** を本線へ昇格させるか（§11-7） | ★**する**（→ 落ちた） | しない | ★★**しない**★★ |
| **N-14** に掛かるか / **D105-1** と噛み合うか | 掛からない | ★**掛かる**（→ 落ちた） | ★★**掛からない**★★ |
| **N-10** に注文を出すか | 出す | 出す | ★**出す**（★**§13-1 (a) が「どの案も器に注文を出す」と書いており新しい依存ではない** —— ★**案 4 を残した理由がそのまま案 6 にも効く**） |

→ ★★**推奨の絞り込みを一貫して当てると、残るのは {案 4, 案 6} であって {案 4} ではない。**★★

#### ★ 落ちた案の中身（★**評価はしない。★何が未評価かだけ書く**）

★**案 6 と案 4 は `ord` について正反対である。**

| | `ord`（**§12-5 の事実 5** / **D99**） |
|---|---|
| ★**案 4 ＋「マージしない」** | ★**回避する**（★合わせないので合成した `ord` を作らずに済む。★**推奨の理由 4 そのもの**） |
| ★★**案 6**★★ | ★★**解ける**★★（★並べ替えを操作として記録する。★**§13-2 が「`ord` に答えを持つ唯一の案である」と書いている**） |

→ ★**「回避する」と「解ける」は同じではない。**
→ ★★**どちらを採るかは実装量（案 6 の代償 (i)〜(iii)）と `DeckDao.save` の設計（§12-5 の事実 2）との取引であり、★まだ誰も比べていない。**★★
★**この節は比べない**（★**比べると「並べるだけ」の §13 を超える**）。

★★**「検討していない」と「検討して落ちた」を区別する**★★（`docs/相談役への引き継ぎ.md` §4-9）——
★**案 6 はいま「検討していない」側に在る。**

#### ★ 型は新所見 **D-35** に記録した

★**なぜ落ちたかは「見落とし」ではない。**
★**§13-2 が「物差しは 3 つ」と宣言して表を作りながら、★1 案だけ物差しの外の語で書いた**ためである。
→ ★**詳細は `ルール整合性チェック_v1.06.md` **D-35**。★ここに写さない**（§8）。

### 14-3. ★★ 4-2 —— 決着に時計を使うことは D107-3 の読み替えに当たらない ★★

#### ★ まず **D107-3** が何を禁じているかを確かめた

| **D107-3** が言っていること | |
|---|---|
| ★**禁じている** | ★★**利用者の語（「最新の履歴」）から比べる量を導くこと**★★ |
| ★**結論** | ★★**「比べる量（①(f) の (f-6)）は 1 つも決まっていない」**★★ |

→ ★★**「決まっていない」は「開いている」であって「時刻は使えない」ではない。**★★
★**§12-11 の (f-6) の行も「★狭めていない」と書いており、★時刻は候補として残っている。**
★**`docs/決定事項一覧.md` の D107「採らなかった書き方」も、禁じたのは
「★『最新の履歴』から比べる量まで決める」ことであって、★技術判断としての選択ではない。**

#### ★★ 2 層に分けると、読み替えは起こりようがない ★★

★**これが判定の本体である。★「技術判断だから許される」ではない。**

| | 時計が使われる場所 |
|---|---|
| ★**案 1 を単層で使う**（★**§13-2 の案 1 が書いている形**） | ★★**すべての比較**★★ —— ★因果で順序が付く場面でも時計が答える。→ ★**「最新の履歴」が「最新の時刻」に置き換わる。★これが読み替えである** |
| ★★**2 層に分けたときの決着層**★★ | ★★**因果が順序を与えない場面だけ**★★ —— ★検出層が「両側とも変わった」と言った、まさにその場面である。→ ★★**そこには「最新の履歴」という事実が存在しない。★置き換える対象が無い**★★ |

→ ★★**§13-2 の案 1 の代償 (iii) は「案 1 を単層で使う」ことの代償であり、★2 層に分けると解消する。**★★
★**§13-2 の当該箇所に判定を書き添えた**（★**書き添えないと、次に読む人が「案 1 は D107-3 が禁じている」と読み、この決定と食い違う**）。

#### ★★ ただし代償 (i)(ii) は消えない —— 推奨の理由 3 は過大である ★★

★**推奨の理由 3 は「分けると案 1 の弱点が消える。★検出を案 4 が担うので空振りしない」と書いている。**

| 案 1 の代償 | 2 層に分けると | |
|---|---|---|
| ★**検出の空振り**（「D107-1 後半」欄） | ★★**消える**★★ | ★理由 3 が指しているのはこれ |
| ★**(iii) 読み替え** | ★★**消える**★★ | ★**理由 3 は言及していない**（★上の判定） |
| ★**(i) 時計のずれ** | ★★**残る**★★ | ★下記 |
| ★**(ii) D107-2 で頻度が上がる** | ★★**残る**★★ | ★同上 |

→ ★★**3 つのうち 2 つにしか効かない。★「弱点が消える」は言い過ぎである。**★★

★★**ただし (i) の★性質は変わる。★これは受け入れる代償として記録する。**★★

| | 何が起きるか |
|---|---|
| ★**単層のとき** | ★★**因果の誤判定**★★ —— ★**後から編集したほうが負けうる** |
| ★**2 層のときの決着層** | ★★**並行編集どうしの決着が、時計の進み具合に偏る**★★ —— ★**進んだ時計の端末が常に勝つ。★系統的な偏りであって、乱数ではない** |

★**実読で確認**（★§13-2 の案 1 が既に書いているものを追認した）——
`loveca-ui/lib/src/data/clock.dart` の `systemClockUtc()` は `DateTime.now().toUtc()` の 1 行で、★**NTP を前提にできない。**

→ ★**並行編集には「正しいほう」が因果として存在しないので、★どちらを採っても因果には反しない。**
→ ★★**それでも偏りは残る。★D107-1 後半（知らせる / 後から分かる）がその受けである。**★★

### 14-4. ★ 4-3 —— 「マージしない」と **D107-1** 後半は両立する

★**D107-1 後半が要求するのは「知らせる / 後から分かる」であって「残す」ではない**
（**D107-4** —— ★**D35 が及んだのは「黙って」の側だけである**）。

★**消える側の材料が本当に出るかを確かめた。**

| 何が要るか | 手元に在るか |
|---|---|
| ★**消える側の内容そのもの** | ★★**在る**★★ —— ★**解決の時点で local と remote は両方が手元に在る**（★(f-3) の案 1 = 送信側 / 案 2 = 受信側 のどちらでも、★突き合わせる以上 2 つとも手元に無ければ解けない） |
| ★**消える側が「何を変えたか」** | ★★**基準点との差分で出る**★★（★**検出層が案 4 の場合**。★§13-2 の案 4 が「何が消えるかを差分として出せる」と書いている） |

→ ★★**両立する。**★★

★★**ただし「事後」を採るなら、失われた側を★残す器が要る**★★
（★**§13-1 (a) が「失われた側の内容」として既に列挙している**）。
→ ★**これは (f-4) の (iii) の下流であり、★触らないと決めた範囲である**（§13 の冒頭）。
★**両立することと、器が要らないことは別である。**

### 14-5. ★★ 4-4 —— 基準点の保存量を実測した（★推測を置き換えた）★★

★**§13-2 の案 4 の代償 (i) は「保存量が大きい」とだけ書いていた。★数が無い。**
→ ★★**実データで測った。**★★

#### ★ 測り方（**D-28** —— ★**刻み幅・標本点・判定手段を併記する**）

| | |
|---|---|
| ★**何を測ったか** | ★**デッキ 1 つぶんの基準点を「内容ごと」持ったときのバイト数** |
| ★**形** | ★`Deck.toJson()` の形をそのまま UTF-8 の JSON に直列化（★空白なし）。★**`ord` は列を持たず順序はリストの順**（★§13-2 の案 5 が実読で確認済み） |
| ★**入力** | ★**実データの `printingId`**（`loveca-data/data/dist/cards/*.json` の **2,527 刷り**。★長さは最小 11 / 最大 25 / 平均 15.09 / 中央値 15 バイト） |
| ★**標本点** | ★**4 点**（下表）。★**「典型」1 点だけでは上限が分からない** |
| ★**判定手段** | ★直列化した文字列の UTF-8 バイト長。★**推定ではない** |

#### ★ 実測値（2026-08-29）

| # | デッキの形 | entries 数 | ★**デッキ 1 つ** | ★entries 部分だけ |
|---|---|---|---|---|
| **A** | ★**典型** —— メンバー 4 枚 × 12 種 / ライブ 4 枚 × 3 種 / エネルギー 12 枚 × 1 種 | 16 | ★**950 B** | 641 B |
| **B** | ★**中間** —— メンバー 4 枚 × 8 種 ＋ 2 枚 × 8 種 / ライブ 4 枚 × 3 種 / エネルギー 6 枚 × 2 種 | 21 | ★**1,157 B** | 848 B |
| **C** | ★**最悪** —— 全部 1 枚ずつ（48 ＋ 12 ＋ 12） | 72 | ★**3,267 B** | 2,958 B |
| **D** | ★★**entries の上限**★★ —— C の `printingId` を実データの★最長 72 件に差し替え（★**`name` 短・`memo` 空**） | 72 | ★**3,716 B** | ★★**3,396 B**★★ |

★★**72 が entries 数の上限である理由は条文である**★★ —— **6.1.1.1** メンバー 48 ＋ ライブ 12 /
**6.1.1.3** エネルギー 12 で、★**1 エントリが最小 1 枚だから 72 を超えられない。**

#### ★★ 「上限」と呼べるのは entries の部分だけである（★言い過ぎないこと）★★

★**`name` と `memo` に長さの制限が 1 つも無い**（★**走査した** ——
`grep -rn "maxLength\|LengthLimiting\|name\.length\|memo\.length" loveca-ui/lib loveca-core/lib loveca-db/lib`
は **0 件**）。
→ ★★**デッキ 1 つ全体の上限は条文では決まらない。★利用者が長いメモを書けば伸びる。**★★

★**分けて書く。**

| 部分 | 上限 | 何が決めるか |
|---|---|---|
| ★**entries** | ★★**3,396 B**★★（★72 件 × 実データ最長の `printingId`） | ★★**条文**★★（6.1.1.1 / 6.1.1.3） |
| ★**固定部**（`deckId` / 時刻 3 つ / `revision` ほか。★`name` `memo` 空） | ★**263 B** | ★`Deck.toJson()` の形 |
| ★★**`name` / `memo` / `tags`**★★ | ★★**無い**★★ | ★**利用者の入力。★制限が実装されていない** |

→ ★**上の表の「D 3,716 B」は「★短い名前・空のメモ」という条件下の値である。**
★★**「デッキ 1 つは 3,716 B を超えない」と書かないこと。★条件を落とすと偽になる**★★（**D-28** の型）。
→ ★**基準点を内容ごと持つなら、★`memo` を含めるかが 1 つの論点になる**
（★**含めなければ上限が条文で決まる。★ただし「メモだけ違う」衝突が見えなくなる**）。
★**これは案を並べているのではなく、★測って出てきた論点である。★決めない。**

#### ★★ 測れなかったもの —— デッキの本数 ★★

★**利用者の実 DB（`loveca.db`）はこの機械上に存在しない**（★走査した ——
`find /c/Users/hiimo/AppData -name "loveca.db"` で出るのは**テストの一時 DB 2 件だけ**）。
→ ★★**本数は測れない。★推測もしない。**★★
→ ★**総量は「本数 × 上の値」でしかここには書けない。**

★**比較の目安として、同じ機械に在るものを 1 つだけ挙げる**（★**判断はしない**）——
`data/dist/cards/*.json` の合計は **1,510,510 B（約 1.44 MB）**である。

### 14-6. ★ 4-5 —— §13-3 の 1 問は閉じる（★ただし検出案が決まってから）

★**§13-3 は「★案 4 / 6 を採ったときにだけ、解決の粒度に 1 問残る」と書いている。**
★**推奨は「deck 全体。★マージしない。★勝った側のリストごと採る」と答えている。**
→ ★★**問いとしては閉じる。**★★

★**ただし 2 つ書いておく。**

| # | |
|---|---|
| **1** | ★★**この 1 問は検出案が決まってから開く。**★★ ★**検出が案 4 か案 6 かで答えが変わりうる** —— ★**案 6 は `ord` を解けるので、マージが選択肢に残る**（14-2）。→ ★**いま閉じたことにすると、案 6 を選べなくなる** |
| **2** | ★**判定と解決を混ぜない**（★**§13-3 が自分で分けている**）—— ★**案 4 は entry まで差分が出る**ので**判定は entry 単位まで見える**が、★**置き換えは deck 単位**である。★**「何が消えるかを見せる」のは判定側の産物であり、粒度を細かくすることではない** |

### 14-7. ★★ 4-6 —— 軸 B を落とす根拠は、この推奨には効かない ★★

★**§13-5 は条件を正しく書いている** ——
「★★解決の規則がカードマスタを参照するなら、軸 B の『独立に解く』は成立しない。★★
★参照しない規則なら成立しうる。★どちらかはまだ決まっていない。」

→ ★★**だから確かめるべきは「この推奨の解決の規則がマスタを参照するか」である。**★★

#### ★ 実読で確かめた（★走査した範囲も書く / §7-6）

```bash
grep -rn "sortedByDeckOrder\|deckOrderInsertionIndex\|compareDeckOrder\|deckOrderKeyOf\|backfillOrd" \
  loveca-ui/lib loveca-db/lib loveca-core/lib
```

| 呼び出し元 | いつ呼ばれるか |
|---|---|
| `deck_repository.dart` の `sortedByRule` | ★**利用者が「規則順に戻す」を押したとき★だけ**（★doc が明記している） |
| `deck_repository.dart` の `insertionIndexOf` | ★**カードを足したとき** |
| `deck_dao.dart` の `backfillOrd` | ★**`schemaVersion` 3 の移行のとき** |

★★**マスタを引くのは `deckOrderKeyOf`（`loveca-ui`）と `backfillOrd` の `keyOf`（`loveca-db`）で、
どちらも比較器の★入力を作る側である。★比較器そのものは純粋である**★★（`deck_order.dart` の doc が
「★★マスタを引く責務はここに持たせない★★」と書いている）。

#### ★★ 推奨の 3 段は、比較器を 1 度も呼ばない ★★

| 段 | すること | 比較器を呼ぶか |
|---|---|---|
| ★**検出**（案 4） | base と local / base と remote の★内容比較 | ★★**呼ばない**★★ |
| ★**決着**（案 1） | `updatedAt` の大小 | ★★**呼ばない**★★ |
| ★**適用**（マージしない） | ★**勝った側のリストを `ord` ごとそのまま採る** | ★★**呼ばない**★★（★**並べ直さないので `sortedByRule` に用が無い**。★`DeckRepository.save` も並べ直さない —— 実読で確認） |

→ ★★**この推奨の解決の規則はマスタを参照しない。**★★
→ ★★**§13-5 自身の条件に照らすと、軸 B の「独立に解く」は★成立しうる。★落とす根拠にならない。**★★

★**「成立しうる」であって「採る」ではない。**
→ ★★**軸 B は決めない。★(f-3) を決めないという指示のとおりである。**★★
★**落とす根拠が無いことだけを記録する。**

### 14-8. ★ 記録したもの / 止めたもの

| 推奨 | 判定 | |
|---|---|---|
| **(f-6)** ★**2 層に分ける** | ★★**記録した**★★（**D108-1**） | ★**検出案の選択と★独立に成立する** —— ★**検出に残る 3 案（3 / 4 / 6）はいずれも §13-2 の構造の所見で「並行なら決まらない」「そもそも順序を定義しない」側に在り、★単独で全順序を持たない** |
| **(f-6)** ★**決着＝案 1（`updatedAt`）** | ★★**記録した**★★（**D108-2** / **D108-3**） | ★**全順序を持つのは案 1 / 案 7 だけで、★案 7 は D105-1（オフライン中は版が採れない）に掛かる。★選んだのではなく他が落ちた** |
| **(f-6)** ★**検出＝案 4** | ★★**止めた**★★ | ★**4-1 が偽。★案 6 が未評価**（14-2）。★`CLAUDE.md` §7 の止まる条件 **6**（推奨案に自分で不安があるとき） |
| **(f-6)** ★**基準点は内容ごと持つ** | ★★**止めた**★★ | ★**検出＝案 4 に従属する。**★**ただし保存量は実測して §13-2 に書き添えた**（14-5） |
| **(f-2)** ★**deck 全体 / マージしない** | ★★**止めた**★★ | ★**検出案に従属する**（14-6 の 1）。★**両立することだけは確かめた**（14-4） |
| **(f-1)** ★**案 A** | ★★**止めた**★★ | ★**案 A は基準点を要求するので (f-6) 案 4 に従属する**（§13-4 の「要る量」欄） |
| **(f-3)** ★**絞らない** | ★**そのとおり。★決めていない** | ★**指示のとおり** |
| **(f-3)** ★**軸 B を落とす** | ★★**止めた**★★ | ★**4-6 で根拠が成立しない**（14-7） |

#### ★★ この節が §11 に要求すること —— ★辺は足さない ★★

★**§13-6 と同じ判定である。**
★★**§11-1 の図に辺は 1 本も足していない。**★★
★**D108 は (f-6) の内側を 2 層に割っただけで、★節と節の依存を動かしていない。**
★**N-11 の昇格条件（§11-7）も動かない** —— ★**昇格するのは (f-6) 案 3 / (f-1) 案 B を採ったときで、★どちらも採っていない。**

★★**ただし §11-7 に 1 行足す条件が近づいた**★★ ——
★**検出＝案 6 を採ると `DeckDao.save` の書き方（§12-5 の事実 2）が N-10 の器の設計に直結する。**
★**いまは採っていないので足さない**（★**確かめていない辺を足すと「依存から機械的に導いた」が偽になる** / §11-4 の末尾と同じ作法）。

---

## 15. ★ (f-6) 検出層 —— 案 4 の理由 4 点の検算（2026-08-29 / ★★止まった★★）

★★**結論を先に書く。★理由 1 が成り立たない。★止まる条件に当たったので、★案 4 を記録していない。**★★
★**利用者が定めた止まる条件**（「案 4 の理由 1〜4 のいずれかが成り立たない場合」）**に従う。**

★★**「案 4 が誤っている」ではない。★決め手として提示された理由が成り立たない、である。**★★
★**案 6 が優れているという結論でもない**（★**下の 15-2 の末尾**）。

### 15-1. ★ 検算の結果（4 件）

| # | 理由 | 結果 |
|---|---|---|
| **1** | ★**決め手は `ord` ではなく `DeckDao.save` の全削除 → 全挿入をどう扱うか。案 6 は保存の仕組みごと作り替え、束 B の `ord` の書き込みも作り替えることになり D99 / D65 をもう一度触る** | ★★**成り立たない**★★（15-2） |
| **2** | ★**案 4 は `save` に触らない。基準点を別の器に持つだけで既存の実装の上に載る** | ★**成り立つ**（15-3） |
| **3** | ★**`ord` は案 4 ＋ マージしないで回避できる。解ける能力のために `save` を作り替えるのは対価が合わない** | ★★**前半は成り立つ。★後半は理由 1 に従属して崩れる**★★（15-4） |
| **4** | ★**案 6 は「ログをいつ捨てるか」を開く。基準点は 1 デッキにつき 1 つで増え続けない** | ★**成り立つ**（15-5） |

### 15-2. ★★ 理由 1 —— 実装を読んだ。★成り立たない ★★

#### ★ 相談役が置いた前提

★★**「案 6 は保存の仕組みごと作り替える」。**★★
★**根拠は §12-5 の事実 2 / §13-2 の案 6 である** ——
`DeckDao.save` は全削除 → 全挿入なので「★**保存の時点で『何が変わったか』が残っていない**」。

#### ★★ 実装の事実 —— 操作は★上の層に、名前つきで在る ★★

★**実読で確認**（`loveca-ui/lib/src/state/deck_edit_store.dart`）——

| # | 名前つきの操作 | 行 |
|---|---|---|
| 1 | `setName` | `163` |
| 2 | `setMemo` | `165` |
| 3 | `replaceEntries`（★共有形式からの取り込み / D67） | `174` |
| 4 | `applyMeta`（★P3 のメタ編集） | `178` |
| 5 | ★**`addCard`**（★`before` / `edge` つき。★**挿入位置まで持っている**） | `214` |
| 6 | `removeCopy` | `250` |
| 7 | `removeEntry` | `254` |
| 8 | ★★**`moveEntry(printingId, target, edge)`**★★ —— ★**並べ替えそのもの** | `262` |
| 9 | `sortByRule`（★「規則順に戻す」/ D99） | `275` |

→ ★★**9 つとも名前を持っている。★`moveEntry` は §13-2 の案 6 が「並べ替えを操作として記録すれば済む」と書いた、まさにその操作である。**★★
★**9 つはすべて `_apply(next)`（`154`）を通ってドラフトを丸ごと置き換えるが、★呼び出し元は名前つきである。**

#### ★★ 何が誤っていたか —— 層の取り違え ★★

| | |
|---|---|
| ★**§12-5 の事実 2 が言っていること** | ★★**`DeckDao` について**★★の事実である —— ★DAO は entries を受け取って書くだけで、★**「何が変わったか」を作る責務を持たない** |
| ★**相談役が読んだこと** | ★★**システム全体**★★の事実として読んだ —— ★**「どこにも残っていない」** |

→ ★★**操作を DAO から★導出する必要は無い。★上の層が★既に持っている。**★★
→ ★★**したがって「保存の仕組みごと作り替える」は成り立たない。**★★
★**`DeckDao.save` の全削除 → 全挿入も、★束 B で入れた `ord` の書き込み（`deck_dao.dart` の `batch.insertAll`）も、★1 行も触らずに済む道がある。**
→ ★**理由 1 の後半（「D99 / D65 の実装をもう一度触る」）も、★その道では起こらない。**

#### ★★ ただし、その道は無料ではない —— 穴が 2 つある ★★

★★**「作り替えなくてよい」は「代償が無い」ではない。★代償が★別のものになる。**★★
★**実読で確認した穴を 2 つ書く**（★**評価はしない。★案 6 を採るかは決めていない**）。

> ★★**2026-08-29 の警告: 穴は 2 つではない。★3 つ目（`softDelete`）が見つかった。**★★
> ★**この表は書き換えない**（★**書いた時点の実読の記録である** —— D-35 / §7-9 で採った処置と同じ）。
> → ★★**穴の正は §15-7。**★★ ★**下の「`create` / `duplicate` は穴に数えない」の除外理由は
> `softDelete` には当たらない**（★論理削除されるデッキは `revision` > 0 でありうるので、
> ★**比べる相手が存在する**）。★**見落としたのは列挙であって、除外の理由ではない。**

| # | 穴 | 実物 |
|---|---|---|
| ★**(a)** | ★★**R2 の P3 メタ編集が `DeckEditStore` を通らない**★★ —— `deck_list_page.dart:109` が `DeckDraft.of(deck)` をダイアログへ渡し、★**`DeckListStore.saveMeta` → `DeckRepository.save` へ直行する**。★**名前つきの操作を 1 つも経由しない** | `deck_list_store.dart:69` / `deck_list_page.dart:100-110` |
| ★**(b)** | ★★**`backfillOrd` が `save` を通らない**★★ —— `schemaVersion` 3 の移行が全デッキの `ord` を書き換えるが、★**操作としては何も残らない** | `deck_dao.dart:223` / `database.dart:86` |

★★**(b) は既知の形である**★★ —— ★**A-2（**D-26**）が「`ord` の backfill が `revision` を上げない」と
既に記録している、★同じ経路・同じ抜け方**である。
→ ★**新しい問題ではなく、★束 A に既に載っている問題が案 6 でも顔を出す、というだけである。**

★**`create` / `duplicate` も `DeckEditStore` を通らないが、★穴に数えない** ——
★**どちらも新規デッキ（`revision` 0）で、★比べる相手（基準点 / 相手側の版）が存在しない。**

#### ★★ 確かめていないことを 1 つ書く —— ログと本体の同時性 ★★

★★**「作り替えなくてよい」と言えるのは、★書き込みの★中身についてである。**★★
★**ログを本体と★同じトランザクションで書くかは、★確かめていない。**

| 道 | `DeckDao.save` への影響 |
|---|---|
| ★**外側にトランザクションを敷いて `save` とログ追記を包む** | ★**署名も中身も変わらない。**★★**ただし入れ子のトランザクションはこのリポジトリに先例が 0 件**★★（★走査した —— `grep -rn "transaction(" loveca-db/lib` は **7 件**で、★**すべて最も外側**） |
| ★**`save` が操作列を引数で受け取る** | ★★**署名は変わる。**★★ ★**だが全削除 → 全挿入も `ord` の書き込みも変わらない** |

→ ★★**どちらの道でも「保存の仕組みごと作り替える」には★ならない。**★★
★**理由 1 の判定は変わらない。**
★★**ただし「`save` に 1 文字も触れずに済む」とまでは言えない**★★（★**2 つ目の道は署名が変わる**）。
★**測っていないものを「無い」と書かない**（★**§7-6 / D-28 の作法**）。

#### ★★ 何が言えて、何が言えないか ★★

| | |
|---|---|
| ★**言える** | ★★**理由 1 の前提（「案 6 は `save` を作り替える」）は偽である。**★★ ★**実装は別の道を持っている** |
| ★**言えない** | ★★**案 6 のほうが良い、とは言えない。**★★ ★**上の穴 (a)(b) は実在し、★塞ぐ費用を測っていない** |
| ★**言えない** | ★★**案 4 が誤っている、とも言えない。**★★ ★**崩れたのは決め手であって、案そのものではない** |

→ ★★**比較は開いたままである。★**§14-2** が「まだ誰も比べていない」と書いた状態から動いていない。**★★
★**動いたのは「決め手として提示されたものが決め手ではなかった」という 1 点だけである。**

### 15-3. ★ 理由 2 —— 成り立つ

★★**「案 4 は `save` に触らない」は実装と合っている。**★★

| 確かめたこと | 結果 |
|---|---|
| ★**基準点は `save` の時点で書く必要があるか** | ★★**無い。**★★ ★**基準点は「前回同期で合意した内容」なので、★書くのは★同期の時点である**（★保存のたびに更新したら基準点にならない） |
| ★**`DeckDao.save` に変更が要るか** | ★★**要らない。**★★ ★**基準点は別の器（表 1 つ）に持つ。★`decks` / `deck_entries` の書き方は動かない** |
| ★**`ord` の書き込みに触るか** | ★★**触らない**★★（★**§14-7 で確認済み** —— 適用は「勝った側のリストを `ord` ごとそのまま採る」） |

→ ★**「既存の実装の上に載る」は正しい。**

### 15-4. ★ 理由 3 —— 前半は成り立つ / ★後半は理由 1 に従属して崩れる

| | 結果 |
|---|---|
| ★**前半**「`ord` は案 4 ＋ マージしないで回避できる」 | ★★**成り立つ。**★★ ★**§14-7 で実読済み** —— 3 段とも D99 の比較器を呼ばない |
| ★★**後半**★★「解ける能力を得るために `save` を作り替えるのは対価が合わない」 | ★★**崩れる。**★★ ★**「`save` を作り替える」が前提だが、★15-2 のとおり偽である。★対価の片側が消えると、天秤そのものが成立しない** |

★★**後半を「対価が合わない」から「対価が違う」に書き換えれば成り立ちうる**★★ ——
★**案 6 の代償は「`save` の作り替え」ではなく「穴 (a)(b) を塞ぐこと」である。**
→ ★★**ただしその費用を測っていないので、★天秤に載せられない。★書き換えは★推奨ではなく★次に決める人の仕事である。**★★

### 15-5. ★ 理由 4 —— 成り立つ

| 確かめたこと | 結果 |
|---|---|
| ★**案 6 が「ログをいつ捨てるか」を開くか** | ★★**開く。**★★ ★**§13-2 の案 6 の代償 (iii) がそう書いている** |
| ★**案 4 が「基準点を失うと全件が衝突」を持つか** | ★★**持つ。**★★ ★**同 案 4 の代償 (iii)** |
| ★★**非対称か**★★ | ★★**非対称である。**★★ ★**基準点はデッキ 1 つにつき 1 つで、★デッキが増えない限り増えない。★ログは編集のたびに伸びる** |

★★**ただし 4-4 の実測を 1 つ足す。**★★
★**基準点は「増え続けない」が「大きさに上限が無い」** —— ★**`name` / `memo` に長さ制限が 1 つも実装されていない**（§14-5）。
→ ★**「1 デッキにつき 1 つ」は★件数の話であって★容量の話ではない。**
★**それでも非対称の向きは変わらない**（★**基準点はデッキ 1 つぶん / ログは編集回数に比例**）。

### 15-6. ★★ 判定 —— 止まる ★★

★**利用者が定めた止まる条件の 1 つ目に当たった。**

| 止まる条件 | 当たったか |
|---|---|
| ★**案 4 の理由 1〜4 のいずれかが成り立たない** | ★★**当たった**★★（理由 1 / 理由 3 の後半） |
| ★**4-4 の再見積もりで保存量が問題になる** | ★**当たっていない**（★上限が無いことは書いたが、★典型 950 B は問題にならない） |
| ★**止めた 4 件のいずれかが検算で落ちた** | ★★**検算していない**★★（★下記） |

#### ★★ 止めた 4 件には進んでいない ★★

★**利用者の指示は「【案 4 が通ったら】止めた 4 件を順に検算して記録する」である。**
★★**案 4 が通っていないので、★4 件とも手を付けていない。**★★

| 止めた 4 件 | 状態 |
|---|---|
| ★**基準点は内容ごと持つ** | ★**未検算**（★案 4 に従属する） |
| ★**(f-2) deck 全体 / マージしない** | ★**未検算**（★同上） |
| ★**(f-1) 案 A** | ★**未検算**（★同上） |
| ★**軸 B は落とさない / 保留のまま残す** | ★★**これだけは案 4 に従属しない**★★ —— ★**§14-7 で根拠が崩れたことは確認済みで、★「落とさない」は★何も決めないことと同じである**。→ ★**保留のまま残っている。★記録すべき新しい決定が無い** |

→ ★★**`決定 D108` は 2026-08-29 の記録のまま動かない。★D109 は採番していない。**★★

### 15-7. ★ 次に決める人へ（★**この節が渡すもの**）

★★**比較の材料は増えた。★結論は出ていない。**★★

| # | 何が分かったか |
|---|---|
| **1** | ★**案 6 は `DeckDao.save` を作り替えなくてよい。**★**操作は `DeckEditStore` に 9 つ、名前つきで在る**（15-2） |
| **2** | ★★**代わりに穴が 3 つある**★★（★**15-2 は 2 つと書いた。★3 つ目は 2026-08-29 の全走査で出た** / 下の 15-7-1） |
| **3** | ★**案 4 の「`save` に触らない」は正しい**（15-3） |
| **4** | ★**非対称（件数が増えるか）の向きは案 4 に有利**（15-5）。★**ただし容量に上限は無い** |
| ★★**5**★★ | ★★**穴を塞ぐ費用を測った**★★（2026-08-29 / 下の 15-7-1 〜 15-7-4）。→ ★**理由 3 の後半は「対価が違う」の形で書ける**（15-7-5） |

★★**§13-2 の案 6 の欄は書き換えない。**★★
★**「事実 2 が最大の障害」は `DeckDao` についての記述として正しい**（15-2 の表）。
→ ★**直すのは★欄ではなく★読み方である**（★**D-35 / §7-9 と同じ処置**）。

---

#### 15-7-1. ★★ 書き込み経路の全走査 —— 穴は 3 つである（★生の出力を残す）★★

★**走査した範囲も書く**（§7-6）。★**表と一致することを検証にせず、生の出力を残す**（**D-15 (j)** / **D-31**）。

```bash
grep -rnE "(into|update|delete)\(db\.(decks|deckEntries|deckTags)\)" loveca-db/lib
```

→ ★★**5 文 / 3 メソッドだけである。**★★
`DeckDao.save`（`deck_dao.dart:75` / 76・91・94 ＋ batch）/ `DeckDao.softDelete`（`:124`）/ `DeckDao.backfillOrd`（`:255`）。
★**`deck_entries.printingId` に外部キーは無い**（`tables.dart`）ので**マスタ更新が deck 表に触る経路は無い**。
★**`decks` の物理削除は 0 件**なので cascade も起きない（**D102**）。

★**UI 側の入口は 7 本。★9 つの名前つき操作を通らないのは 4 本である。**

| # | 入口 | `updatedAt`（**D108-2** 決着層） | 9 操作を通るか |
|---|---|---|---|
| 1 | `DeckListStore.create` | 動く | 通らない（★**15-2 が除外済み**。★理由も確かめた） |
| 2 | `DeckListStore.saveMeta`（`deck_list_store.dart:69`） | ★動く | ★★**通らない = 穴 (a)**★★ |
| 3 | `DeckListStore.softDelete`（`:61`） | ★動く | ★★**通らない = 穴 (c)**★★ |
| 4 | `DeckListStore.duplicate` | 動く | 通らない（★同上・除外） |
| 5 | `DeckEditStore.save`（`deck_edit_store.dart:290`） | 動く | ★**通る** |
| 6 | `DeckEditStore.softDelete`（`:314`） | ★動く | ★★**通らない = 穴 (c)**★★ |
| 7 | 移行 `database.dart:86` → `backfillOrd` | ★★**動かない**★★ | ★★**通らない = 穴 (b)**★★ |

★★**穴 (c) = `softDelete`。**★★ ★`softDelete()` は `_apply` を呼ばないので、**R2 からも R3 からも 9 操作を通らない。**
★**15-2 の除外理由（「比べる相手が存在しない」）は当たらない** —— 論理削除されるデッキは `revision` > 0 でありうる。
→ ★**(c) は A-1 / D-9 と同じ経路である**（`DeckDao.softDelete`、同一メソッド）。★**(b) が A-2 と同じであるのと同型。**

#### 15-7-2. ★★ 層の表 —— これが (a) と (b)(c) を分ける ★★

| 層 | 通る書き込み | そこに在るもの |
|---|---|---|
| `DeckEditStore` の 9 操作 | ★**5 だけ** | ★★**意図**★★（`moveEntry(printingId, target, edge)` / `addCard(before, edge)` …） |
| `DeckRepository.save(base, draft)`（`:486`） | **2 と 5** | before と after の両方 |
| `DeckDao.save(deck)` | 1 / 2 / 4 / 5 | after だけ |
| ★**どれも通らない** | **3 / 6 / 7** | —— |

→ ★★**「`save` を通らない書き込み」は 2 本**（`softDelete` / `backfillOrd`）で、**どちらも束 A に載っている**★★
（**A-1** / **A-2**。★§3-2 の共通の問い「`save` を通らない書き込みで **D101** の 3 つ組をどう動かすか」がそのまま両方を指す）。
→ ★★**「`save` は通るが 9 操作を通らない」書き込みは 1 本**（`saveMeta`）で、**これだけが束 A に無い**★★。

#### 15-7-3. ★ 穴 (a) —— ★案 6 に固有。★ただし「案 6」ではなく「ログを store 層に置くこと」に固有

★**案 4 の検出は書き込み経路に要求を出さない** —— ★基準点を書くのは同期の時点（**15-3**）で、
検出は**基準点と現在の DB 内容**の比較だからである。★7 本とも `decks` / `deck_entries` に着地する（15-7-1）。
★決着層から見ても穴ではない —— `saveMeta` → `DeckRepository.save` は `updatedAt: _clock()` を書く（`:497`）。

★★**ただし固有なのは案 6 そのものではない。**★★
★`saveMeta`（`deck_list_store.dart:70`）と `DeckEditStore.save`（`deck_edit_store.dart:294`）は
★★**`DeckRepository.save` で合流する。**★★
→ ★**ログを合流点に置けば穴 (a) は存在しない。★ただし採れるのは (before, after) の対だけで、意図は採れない。**
→ ★★**穴 (a) は「意図を採るために合流点より上へ登ること」の代償である。★案 6 の利点と同じ 1 つの選択の表裏である。**★★

★**塞ぐ費用**（★**2 通りとも測った**）——

| | ★**A-i. R2 を `DeckEditStore` 経由にする** | ★**A-ii. `DeckListStore.saveMeta` を記録点にする** |
|---|---|---|
| 触るファイル | `ui/deck/deck_list_page.dart`（`_editMeta` / `:100-110`）/ `state/deck_list_store.dart`（`saveMeta` が不要になる） | `state/deck_list_store.dart` の 1 メソッド |
| 経路 | ★`DeckEditStore` を新規に組む。★`_initialState` は `draftOf` / `validateDraft` / `sectionsOf` だけで **DB へ行かない**（**D55**）。★`draftOf` と `DeckDraft.of` は**フィールド単位で同一**なのでドラフトの中身は変わらない | 変えない |
| ★**挙動の差 1** | `canSave` 門ができる（★**無変更のまま決定しても保存されない** = `revision` +1 が消える）。★**R2 経路の 13 件のうち「決定」を押すのは 8 件で、8 件とも先に何かを変えている**（実読）→ ★**1 件も反転しない** | 無し |
| ★★**挙動の差 2**★★ | ★★**失敗表示が 1 本落ちる**★★ —— 現在は `DeckListStore._act` が `actionError` に写し `deck_list_page.dart:69` が出す。`DeckEditStore` の `actionError` は R2 が描かない。★★**見ているテストが 0 件**★★（`failSave` の使用は R3 の `deck_edit_page_test.dart:334` の 1 件のみ）→ ★**黙って落ちる** | 無し |
| ★**挙動の差 3** | `_act` の `load()` が消えるので一覧の読み直しを明示に足す（★`_openDeck` `:94` と同じ 1 行） | 無し |
| 既存テストへの影響 | `test/ui/deck_meta_dialog_test.dart`（17 件中 ★**13 件が R2 経路**）。★**判定は `saveCalls` / `lastSaved` = `DeckRepository` 層**なので差 1 では落ちない。★**差 2 には受けが無い** | ★**0 件**（署名も経路も変わらない） |
| ★**代償の本体** | ★意図を採る層が 1 つに揃う | ★★**記録点が 2 か所になる**★★ → ★**次に入口が増えたとき同じ穴が開く** |

#### 15-7-4. ★ 穴 (b)(c) と、ログと本体の同時性

★★**穴 (b) は A-2 / D-26 と同じである**★★ —— 経路（`database.dart:86` → `backfillOrd`）も抜け方（`save` を通らない）も同一で、
★**A-2 の「決まっていないこと」(1)（移行を「更新」と数えるか）がそのまま穴 (b) の問いである。**
★**穴 (c) は A-1 / D-9 と同じ**（同一メソッド）。

★★**ただし「決定が共有される」から「費用が共有される」は出ない。**★★
★**前回の測定はここを無条件に書いて誤った**（★**型は §7-10**）。★**正しくは向きが A-2 / A-1 の決め方に依存する** ——

| A-2 / A-1 の決め方 | 案 4 | 案 6 |
|---|---|---|
| ★「移行 / 論理削除を**更新と数える**」 | ★**追加作業なし**（内容比較が既に検出する） | ★**記録点を足す**（ログに操作を残す） |
| ★「**数えない**」 | ★★**追加作業あり**★★ —— ★内容比較は意図に盲目なので、**移行時に基準点を書き換えないと検出が止まらない** | ★**追加作業なし**（ログに残さなければよい） |

→ ★★**どちらの案も一方的には有利にならない。**★★

★**同時性について測れた事実**（走査と実読）——

| | |
|---|---|
| トランザクションを持つ層 | ★`DeckDao.save`（`:75`）と `DeckDao.backfillOrd`（`:245`）**だけ** |
| ★`DeckDao.softDelete` | ★★**トランザクションを持たない**★★（単文 UPDATE） |
| `loveca-db/lib` の `transaction(` | **7 件**。★**呼び出し元を 1 件ずつ辿って全部が最も外側であることを確かめた**（`import` → `_importFile` → `_apply` に囲いは無い）→ ★**入れ子の先例 0 件**（15-2 の主張を独立に追認） |
| ★★`loveca-ui/lib` の `.transaction(`★★ | ★★**0 件**★★（★`DeckRepository._db` は private。★UI は **D55** で DAO より下を呼ばない） |

→ ★★**入れ子の問題が起きるのは `save` だけである。**★★
★`softDelete` は自前のトランザクションを持たないので**新しい最外**を敷けばよく、`backfillOrd` は**既に自前で持っている**ので中に足すだけである。

| 道 | 費用 |
|---|---|
| ★**道 1: 外側に敷いて包む** | ★★**「署名も中身も変わらない」は `DeckDao.save` / `DeckRepository.save` についてしか成り立たない**★★ —— ★ログを store 層で作る以上、**トランザクションを開けるのは `_db` を持つ `DeckRepository` だけ**。→ ★`DeckRepository` に新しい口が要る（★`loveca-ui/lib` に初の `.transaction(` が生える = **D55** の線に触る）か、★**操作列を引数で渡す = 道 2 になる** |
| ★**道 2: `save` が操作列を受け取る** | ★`DeckRepository.save` の呼び出し元 = **lib 2 件**（`deck_edit_store.dart:294` / `deck_list_store.dart:70`）★**＋ test 8 件**（`deck_repository_test.dart` 全 40 件のうち）★**＋ `FakeDeckRepository` の override 1 件**（★これを使うテストファイルは **34 個**だが、直に `save(` を呼ぶのは上の 8 件だけ）。★★**ただしこれだけでは同時性を買えない**★★ —— ★トランザクションを持つのは DAO 側なので、★**`DeckDao.save` も署名を変える**（lib 3 件 / `migration_test.dart` 6 件）か、★**repository で外側に敷く = 道 1 の入れ子に戻る** |
| ★**両道に共通** | ★ログの表 → **`schemaVersion` 3 → 4**（`database.dart:51`）＋ `onUpgrade` の枝 ＋ 移行テスト群（★先例: v2→v3 の群は **12 件** / `migration_test.dart` 全 22 件） |

→ ★★**15-2 の「2 つの道」は独立ではない。★道 2 は道 1 の代わりにならない。**★★

★★**測っていないもの: 入れ子のトランザクションがこの executor で成り立つか。**★★
★**確かめるにはコードを書くしかない**（★**実機も実データも要らない**。★**この指示の範囲では測れない**）。
★**推測で埋めない**（**D-28** の作法）。

#### 15-7-5. ★★ 理由 3 の後半は「対価が違う」の形で書ける ★★

★**§15-4 が「書き換えは★次に決める人の仕事である」と残した分である。**★**大小は比べない**（★それは選ぶことである）。

| | 対価の**種類** |
|---|---|
| ★**案 4** | ★★**器**★★ —— 基準点の表 / 保存量（**§14-5** の実測: 典型 **950 B** / entries 部分の上限 **3,396 B** / ★**デッキ 1 つ全体に上限は無い**）/ ★**失うと全件が衝突**（代償 (iii)） |
| ★★**案 6**★★ | ★★**層**★★ —— ★**意図を採る層まで登ること**。(1) ★**穴 (a) を塞ぐ**（15-7-3 の A-i / A-ii）(2) ★**同時性の手当て**（★`save` についてだけ入れ子か 2 段の署名変更 / 15-7-4）(3) ★**ログをいつ捨てるか**（§13-2 案 6 の代償 (iii) / **15-5**） |

★★**「`DeckDao.save` の作り替え」は案 6 の対価ではない**★★（**15-2** の判定は動かない）。
★★**穴 (b)(c) はどちらの案にも一方的な差を与えない**★★（15-7-4 の表）。

→ ★**書き換えの文言**: ~~「解ける能力を得るために `save` を作り替えるのは対価が合わない」~~
→ ★★**「案 4 は器を要求し、案 6 は層を要求する。★どちらも 0 ではなく、種類が違う」**★★

# 総合ルール ver.1.06 × 既存実装 整合性チェック結果

対象: `引き継ぎドキュメント.md` / `loveca-data` / `loveca-core`
基準: ラブライブ！シリーズ オフィシャルカードゲーム 総合ルール ver.1.06（2026-04-28）

---

## 総評

**引き継ぎ資料のルール解釈は、精査した範囲でほぼ全面的に正しい。**
特に以下の 4 点は、間違えると設計が根本から崩れる箇所であり、正しく押さえられている。

| 論点 | 判定 | 根拠条番号 |
|---|---|---|
| ブレードは「アクティブ状態のメンバー」のみ、ハートは「全メンバー」 | **正** | 8.3.10 / 8.3.14 |
| 解決領域は両プレイヤー共有で 1 つだけ。`ownerId` で絞る必要がある | **正** | 4.14.1 / 8.3.14 |
| 4 枚制限はメインデッキのみ。エネルギーデッキには枚数制限が無い | **正** | 6.1.1.2 / 6.1.1.3 |
| 先攻・後攻がゲーム中に入れ替わる。フェイズにプレイヤー ID を埋めない | **正** | 8.4.13 |
| 1 ターン内で両プレイヤーが行動する | **正** | 7.1.2 |
| リフレッシュは残デッキの「下」へ差し込む | **正** | 10.2.3 |
| ライブカード置き場へのブラフ（ライブ以外を裏向きで置く）は正規戦術 | **正** | 8.2.2 / 8.3.4 |

`DeckValidator` を条文と 1 行ずつ突き合わせたが、**6.1 の解釈に誤りは無い。**

---

## A. 要修正（実装に確実に影響する）

> **対応済み（2026-08-23）。実データで確認した結果、指摘と食い違った点がある。**
>
> | 指摘 | 実測 | 対応 |
> |---|---|---|
> | A-1 `bladeHearts` の DRAW/SCORE でクラッシュ | **実在**。37 種（DRAW 22 / SCORE 15） | `bladeHeartEffects` に型分離。V14 を新設 |
> | A-1 `verify_contract.py` が 5 枚しか見ていない | **そのとおり** | 全 22 商品 1,708 種へ拡張。dist 不在時は終了コード 2 |
> | A-2 `【ターン2回】` が漏れている | **生データにも配信データにも 0 件**。追加しても挙動は変わらない | 予防として追加。実害は `［ターン1回］`（全角角括弧・9 刷り）にあり 4 種を修正 |
> | 「Dart テスト 26 件が未実行」 | **食い違い**。SDK は導入済み（3.11.1）でテストは通っていた | 現在 32 件全通過 |
>
> **レビューに無かった発見（A-3 として対応）**: `_TOKEN_RE` が数字を必須にしていたため、
> `ドロー` / `スコア` / `[ドロー]` のような数字なし表記を無言で捨てており、
> **59 種でブレードハートが欠落していた**（DRAW 22→59 / SCORE 15→37）。
> 色は 5,367 刷り分すべてが数字を伴っており影響なし。詳細は CLAUDE.md §5(7)。

### A-1. `bladeHearts` の `DRAW` / `SCORE` で Dart 側が例外を投げる ★最優先

**現象**

`loveca-data/loveca_data/constants.py` は、ブレードハートの非色アイコンを
色マップと同じ辞書に載せている。

```python
BH_DRAW  = "DRAW"    # 総合ルール 8.3.12.1 カードを 1 枚引く
BH_SCORE = "SCORE"   # 総合ルール 8.4.2.1  スコア +1
```

これは `build_dist.py` により配信 JSON の `bladeHearts` にそのまま出力される。
一方 `loveca-core/lib/src/entities/card.dart` の `HeartColor.fromKey` は
未知のキーで **`ArgumentError` を送出する**。

```dart
_ => throw ArgumentError('unknown heart color: $key'),
```

つまり `bladeHearts: {"DRAW": 1}` を持つカードを 1 枚でも読み込んだ時点で、
**カードマスタのロードがクラッシュする。**

**なぜ今まで発覚しなかったか（2 重の見落とし）**

1. Dart テスト 26 件が Dart SDK 未導入のため一度も実行されていない
2. `verify_contract.py` は色キーの検査（75-78 行）を持っているが、
   参照先が `test/fixtures/cards_BP01.json` の **5 枚だけ**。全 22 商品を見ていない

**実在確認**

引き継ぎ資料の未解決課題 U1 に「ライブの `cost` が `"ドロー1"`」とある。
これは `parse_heart_string` を通って `{"DRAW": 1}` になる経路そのもの。
仮説ではなく**実データに存在する**。

**推奨する直し方（型で分ける）**

配信 JSON の段階で色と効果を別フィールドに分離する。

```jsonc
"bladeHearts":       { "BLUE": 1 },        // 8.3.14 ライブ所有ハートに合算
"bladeHeartEffects": { "DRAW": 1 }         // 8.3.12.1 / 8.4.2.1 の別処理
```

Dart 側は `BladeHeartEffect { draw, score }` を新設して受ける。
理由: 8.3.12.1（ドロー）と 8.4.2.1（スコア）は**ハート集計とは処理タイミングも対象も違う**。
同じ `Map<HeartColor,int>` に同居させること自体が Phase 3a の集計バグの温床になる。

**併せて必要な対策**

`verify_contract.py` の検査対象を `data/dist/cards/*.json` の全 22 ファイルに広げる。
5 枚のフィクスチャで契約を保証したつもりになっていた、というのが今回の本質。

---

### A-2. `【ターン2回】`（全角括弧）がキーワード抽出から漏れている

`constants.py` の `KEYWORD_TOKENS`：

```python
"[ターン1回]": "TURN_1",
"【ターン1回】": "TURN_1",     # 半角・全角の両方がある
"[ターン2回]": "TURN_2",       # ★全角 【ターン2回】 が無い
```

`extract_keywords` は単純な部分一致なので、公式テキストが全角括弧を使っている
カードは `TURN_2` が付かない。ターン 2 回は ver 1.05（2026-03-31）追加で
該当が 6 件しかないため、統計上も気づきにくい。

**修正**: `"【ターン2回】": "TURN_2"` を追加。修正後 `stats` で件数が変わるか確認する。

---

## B. 要確定（設計判断が必要）

### B-1. 「13 フェイズ」の内訳が条文と 1 つ合わない

> **確定済み（2026-08-23）。詳細は `docs/PhaseEngine設計メモ.md`**
>
> **リーフフェイズは 12 個で確定**（7.1.2 / 7.3.3 / 8.1.2）。
> 「13 フェイズ」は誤記であり、条文に 13 番目に相当するフェイズは存在しない。
> コンテナ（先攻通常 / 後攻通常 / ライブ）を含めても 15 で、13 にはならない。
>
> **下記の提案（`turnEnd` を 13 個目のフェイズにする）は撤回する。**
> 8.4.13 と 8.4.14 はライブ勝敗判定フェイズ 8.4 の第 13・第 14 手順であり、
> フェイズに昇格させると 8.1.2 の列挙（4 つ）と条番号の 1 対 1 対応が崩れる。
> 条文はターン境界の誘発を意図的に既存フェイズへ畳み込んでおり
> （「ターンの始めに」は 7.4.2、「ターンの終わりに」は 8.4.10）、
> `turnEnd` を作ると誘発の置き場所が 2 箇所に見える。
> また 8.4.12 は 8.4.9 へ戻るループを持つため、フェイズを単純な前進列にすると表現できない。
>
> **代わりにフェイズの下へ、条番号をそのまま ID とするステップ層を置く。**
> 8.4.13 / 8.4.14 はステップとして実体化する。D25 の受け皿はステップ 8.4.13 が持つ。
> D31「フェイズを 1 つ戻す」は**ステップ単位**に読み替える。
> 8.4.12 のループがあるため、巻き戻しは静的グラフの逆辺ではなく**通過履歴スタック**から行う。
>
> フェイズはロール（先攻/後攻）で定義し、実プレイヤーは `firstPlayerId` から解決する。
> 8.4.13 で入れ替わるため、**フェイズに実プレイヤー ID を埋めない**。

#### ★ 決定 D25 の訂正（2026-08-23）

決定 D25「先攻入れ替えは**手動確定した勝敗**をもとに自動適用」は**厳密には誤り**。

8.4.13 が参照するのは 8.4.6 で決まる勝敗ではなく、
**8.4.7 で実際に成功ライブカード置き場へカードを移動したかどうか**である。

> 8.4.13. **8.4.7 において**、一方のプレイヤーのみが成功ライブカード置き場にカードを
> 移動していた場合、そのプレイヤーが先攻プレイヤーとなり、その相手が後攻プレイヤーとなります。

8.4.7.1 により、**両者がライブに勝利し、かつライブカード置き場に 2 枚あるプレイヤーは
カードを移動しない**。したがって「ライブに勝利した」と「移動した」は一致しない。

具体的にずれる例:

| 状況 | 8.4.6 の勝敗 | 8.4.7 の移動 | 8.4.13 の結果 |
|---|---|---|---|
| 同点。A はライブ置き場に 1 枚、B は 2 枚 | **両者勝利** | A のみ移動（B は 8.4.7.1 で移動しない） | **A が先攻になる** |

D25 の字面（勝敗をもとに判定）だと「両者勝利＝勝者が 1 人に定まらない」ため
先攻は据え置きと判断してしまうが、条文では **A に先攻が移る**。

**正しくは「先攻入れ替えは 8.4.7 の移動実績をもとに自動適用」。**
実装では 8.4.7 の移動実績を記録し、8.4.13 がそれを参照する。
D10「勝敗確定は手動ボタン」で確定するのは 8.4.6 の勝敗であり、**これとは別の情報**である。

---

引き継ぎ資料は一貫して「13 フェイズ」と書いているが、資料自身の列挙は 12 個。

```
先攻通常フェイズ（アクティブ / エネルギー / ドロー / メイン）      4
後攻通常フェイズ（同上）                                          4
ライブフェイズ（セット / 先攻パフォ / 後攻パフォ / ライブ勝敗判定） 4
                                                          合計 12
```

条文（7.3.3 / 8.1.2）も 12 で、13 番目に相当するものは無い。

~~**提案**: 8.4.13（先攻入れ替えの判定）と 8.4.14（ターン終了）を
`turnEnd` という 13 個目のフェイズとして明示的に持つ。
決定 D25「先攻入れ替えは手動確定した勝敗をもとに自動適用」と
D31「フェイズを 1 つ戻すで代用」の受け皿がここに必要なため、
フェイズとして実体化しておくほうが素直。~~ — **撤回済み**（上記の確定ブロックを参照）

### B-2. エネルギーデッキは「順番が管理されない」

4.9.2 は `エネルギーデッキ置き場は…カードの順番は管理されません` と定めるが、
6.2.1.7 と 7.5.2 は `上から 3 枚` `一番上のカード` と書く。条文上の緩い不整合。

**提案**: エネルギーデッキはシャッフルの概念を持たせず、
`DeterministicRng` による無作為 1 枚抽出として実装する。
非公開領域（4.9.2）なので観測上の差は無く、seed 再現性も保てる。

### B-3. 重ね置きの解消先が親子で異なる

> **確定済み（2026-08-23）。詳細は `docs/PhaseEngine設計メモ.md`**
>
> **最初の分岐はカード種別ではなく「移動先がメンバーエリアかどうか」。**
> 4.5.5.3 により、メンバーが**他のメンバーエリア**へ移動する場合は下のカードも
> 重なったまま同時に移動し、解消は起きない。下記の表が効くのは
> 4.5.5.4（メンバーエリア**以外**へ移動する場合）に限られる。
>
> **★解消は即時ではない。** 4.5.5.4.1 / 4.5.5.4.2 はいずれも
> 「そのままそのメンバーエリアに残り、**その後にルール処理により**」と書いており、
> 10.1.2 によりルール処理はチェックタイミングでのみ実行される。
> したがって「上にメンバーが居ない孤児カードがメンバーエリアにある」状態は**正規の中間状態**で、
> メンバーの移動と同時に下のカードを消す実装は条文違反になる。
>
> **10.5.5 の「上記のいずれか」は広義（10.3〜10.5 全体）と読む。**
> 主根拠はエネルギーカードの閉ループ性。4.9.1 →（6.2.1.7 / 7.5.2）→ 4.7 →
> 5.10.1 → 10.5.4 と巡り、5.9.1 のコスト支払いはウェイトにするだけで領域を移動しない。
> 6.1.1.1 によりメインデッキにエネルギーは入らず、控え室側の循環にも合流しない。
> **条文全体で「エネルギーカードを控え室に置く」と書いた条は 1 つも存在しない。**
> 唯一 10.4.1 だけがカード種別に触れず「それ以外のそのメンバーエリアのカード」と書くため
> 閉ループを破りうる。10.5.5 はこれを塞ぐ規定と読むと明確な仕事を持つ。
>
> **【要確認】** 10.4.1 を字面どおり読むと、残す 1 枚の下のスタックまで剥がれる。
> 4.5.5.3 との整合を優先し**残すメンバーのスタックは維持する**と決定した。
> 実装地点に貼るコメント文面を設計メモ §10 に用意してある。

ver 1.06 でメンバーの下に**メンバーカード**も置けるようになった（4.5.5）。
上のメンバーが居なくなったときの行き先が種別で分かれる。

| 取り残されたカード | 行き先 | 条番号 |
|---|---|---|
| メンバーカード | 控え室 | 10.5.3 |
| エネルギーカード | **エネルギーデッキ置き場**（控え室ではない） | 10.5.4 / 10.5.5 |

**エネルギーカードは控え室を経由しない閉ループである。**
Phase 3a の「整理（ルール処理）」コマンドはこの分岐を持つ必要がある。

### B-4. ポジションチェンジ / フォーメーションチェンジ

11.10 / 11.11 で定義されるが、これらは `【】` で囲まれたキーワード能力ではなく
**効果テキスト中の動詞**。`KEYWORD_TOKENS` に無いのは正しい。

ただし盤面 UI では「入れ替え（11.10.2: 移動先のメンバーは元の位置へ）」と
「全員再配置（11.11.2: 1 エリアに 2 人以上は不可）」を
ワンアクションで行える補助コマンドがあると実用性が大きく変わる。
効果の自動処理ではなく**物理操作の補助**なので、大方針に抵触しない。

---

## C. Phase 3a 着手前に確定しておきたい処理順序

条文どおりの順序でないと集計が狂う箇所を抜き出した。

### パフォーマンスフェイズ（8.3）

```
8.3.4   ライブカード置き場を全て表向き → ライブでないカードを控え室へ
8.3.6   この時点でカードが無ければフェイズ終了
8.3.8   「ライブ開始時」の事象が発生（8.3.6 で終了した場合は発生しない / 11.5.2.1）
8.3.10  アクティブ状態のメンバーのブレードを合計
8.3.11  合計ブレード数だけ、メインデッキの一番上を解決領域へ（＝エール）
8.3.12  ★解決領域のブレードハートのドローアイコン分だけカードを引く
8.3.14  ★全メンバーのハート ＋ 解決領域の自分のカードのブレードハート を合計
8.3.15  各ライブカードの必要ハートを満たすか確認（ALL は任意の 1 色として扱う）
```

**ドロー（8.3.12）はハート集計（8.3.14）より前**。逆にすると引いたカードが
その回の集計に影響し得る実装になってしまう。

### リフレッシュはチェックタイミングを待たない（10.2.1）

エール（8.3.11）の途中でメインデッキが尽きた場合、
**その場で処理を中断してリフレッシュし、続きを実行する。**
他のルール処理（10.1.2）と挙動が違うので、実装場所を分ける必要がある。

### 勝利処理は同時到達で引き分け（1.2.1.2）

成功ライブカードが両者同時に 3 枚以上になった場合は引き分け。
決定 D10 の「勝敗確定は手動ボタン」に**引き分けの選択肢**が要る。

---

## D. その他（影響小・記録のみ）

### D-1. `fromKey` の厳格性は運用リスクを抱えている【Phase 4 で判断】

A-1 の修正で `bladeHearts`（色）と `bladeHeartEffects`（DRAW/SCORE）を型で分離し、
`HeartColor.fromKey` / `BladeHeartEffect.fromKey` は未知キーで `throw` する設計を維持した。
契約としてはこれが正しい。生成側が契約を破ったら止まるべきだからである。

**ただし配信データ駆動アプリとしては別のリスクが残る。**
公式が将来 3 つ目の非色アイコンを追加した場合、
新しい配信データを受け取った**既存インストール済みアプリのマスタロードが全滅する**。
アプリ側を更新しない限り復旧できない。

現状の担保は「配信前に `verify_contract.py` が全 22 商品を検査する」ことのみで、
これは**こちらが配信を止められる**という前提に立っている。
Phase 4（認証・同期）で配信経路が固まった時点で、次のどちらを取るか判断する。

| 案 | 挙動 | 代償 |
|---|---|---|
| 現状維持（厳格） | 未知キーで例外。契約違反が即座に露見する | 新アイコン追加で既存アプリが起動不能になりうる |
| 寛容化 | 未知キーは読み飛ばし、件数を記録して警告表示 | 取りこぼしが静かに進行する（A-3 と同じ失敗の型） |

寛容化するなら「読み飛ばした事実を UI に出す」までを含めること。
黙って捨てるのは A-3（数字なし表記を 59 種で無言に捨てていた）と同じ失敗になる。

### D-2. 記録のみ

| 項目 | 内容 | 条番号 |
|---|---|---|
| コストの支払い | 自分のリソースでのみ支払える（ver 1.06 追記） | 9.4.2.1 |
| バトンタッチ | 同一エリアの自分のメンバーを控え室に置きコストを減らす | 9.6.2.3.2 |
| 登場制限 | そのターンにステージ外からステージに来たメンバーが居るエリアは指定不可 | 9.6.2.1.2.1 |
| 正面のエリア | 左サイドの正面は相手の右サイド（ver 1.06 追記）＝鏡像配置がルール上も正 | 4.5.7 |
| グループ名一覧 | 付録 A の 1 行目「ラブライブ！シリーズ オフィシャルカードゲーム」が `OFFICIAL_GROUPS` に無い。V11 は WARN なので実害なし | 付録A |
| 注釈文 | `（）` 内の注釈文はゲームに影響しない。検索対象から外すか要検討 | 2.12.4 |

---

## 修正の適用順序

1. ~~**A-1** `bladeHearts` の型分離~~ — 完了
2. ~~**A-1** `verify_contract.py` を全 22 商品に拡張~~ — 完了（終了コード 2 で限定検査を明示）
3. ~~**A-3** 数字なし表記の取りこぼし修正 + V15~~ — 完了（レビュー時点で未検出だった論点）
4. ~~**A-2** ターン回数制限の括弧ゆれ~~ — 完了
5. ~~検証~~ — 完了。Python 33 件 / Dart 32 件 / `validate --complete` エラー 0 /
   `verify_contract.py` 全 22 商品 1,708 種 / `dataVersion` 2
6. ~~**B-1 / B-3** を確定してから Phase 3a に着手~~ — 完了（`docs/PhaseEngine設計メモ.md`）
7. **Phase 3a（GameState / 集計エンジン）に着手** ← **次はここ**

`build` は `--skip-images` を付けると `imageHash` も空になる。
配信物として使う dist を作るときは付けないこと（Pillow が要る）。

# 総合ルール ver.1.06 × 既存実装 整合性チェック結果

対象: `引き継ぎドキュメント.md` / `loveca-data` / `loveca-core`
基準: ラブライブ！シリーズ オフィシャルカードゲーム 総合ルール ver.1.06（2026-04-28）

---

## 総評

**引き継ぎ資料のルール解釈は、精査した範囲でほぼ全面的に正しい。**
特に以下の 4 点は、間違えると設計が根本から崩れる箇所であり、正しく押さえられている。

| 論点 | 判定 | 根拠条番号 |
|---|---|---|
| ブレードは「アクティブ状態のメンバー」のみ、ハートは「全メンバー」 | **正** | 8.3.10 / 8.3.14 |
| 解決領域は両プレイヤー共有で 1 つだけ。`ownerId` で絞る必要がある | **正** | 4.14.1 / 8.3.14 |
| 4 枚制限はメインデッキのみ。エネルギーデッキには枚数制限が無い | **正** | 6.1.1.2 / 6.1.1.3 |
| 先攻・後攻がゲーム中に入れ替わる。フェイズにプレイヤー ID を埋めない | **正** | 8.4.13 |
| 1 ターン内で両プレイヤーが行動する | **正** | 7.1.2 |
| リフレッシュは残デッキの「下」へ差し込む | **正** | 10.2.3 |
| ライブカード置き場へのブラフ（ライブ以外を裏向きで置く）は正規戦術 | **正** | 8.2.2 / 8.3.4 |

`DeckValidator` を条文と 1 行ずつ突き合わせたが、**6.1 の解釈に誤りは無い。**

---

## A. 要修正（実装に確実に影響する）

> **対応済み（2026-08-23）。実データで確認した結果、指摘と食い違った点がある。**
>
> | 指摘 | 実測 | 対応 |
> |---|---|---|
> | A-1 `bladeHearts` の DRAW/SCORE でクラッシュ | **実在**。37 種（DRAW 22 / SCORE 15） | `bladeHeartEffects` に型分離。V14 を新設 |
> | A-1 `verify_contract.py` が 5 枚しか見ていない | **そのとおり** | 全 22 商品 1,708 種へ拡張。dist 不在時は終了コード 2 |
> | A-2 `【ターン2回】` が漏れている | **生データにも配信データにも 0 件**。追加しても挙動は変わらない | 予防として追加。実害は `［ターン1回］`（全角角括弧・9 刷り）にあり 4 種を修正 |
> | 「Dart テスト 26 件が未実行」 | **食い違い**。SDK は導入済み（3.11.1）でテストは通っていた | 現在 32 件全通過 |
>
> **レビューに無かった発見（A-3 として対応）**: `_TOKEN_RE` が数字を必須にしていたため、
> `ドロー` / `スコア` / `[ドロー]` のような数字なし表記を無言で捨てており、
> **59 種でブレードハートが欠落していた**（DRAW 22→59 / SCORE 15→37）。
> 色は 5,367 刷り分すべてが数字を伴っており影響なし。詳細は CLAUDE.md §5(7)。

### A-1. `bladeHearts` の `DRAW` / `SCORE` で Dart 側が例外を投げる ★最優先

**現象**

`loveca-data/loveca_data/constants.py` は、ブレードハートの非色アイコンを
色マップと同じ辞書に載せている。

```python
BH_DRAW  = "DRAW"    # 総合ルール 8.3.12.1 カードを 1 枚引く
BH_SCORE = "SCORE"   # 総合ルール 8.4.2.1  スコア +1
```

これは `build_dist.py` により配信 JSON の `bladeHearts` にそのまま出力される。
一方 `loveca-core/lib/src/entities/card.dart` の `HeartColor.fromKey` は
未知のキーで **`ArgumentError` を送出する**。

```dart
_ => throw ArgumentError('unknown heart color: $key'),
```

つまり `bladeHearts: {"DRAW": 1}` を持つカードを 1 枚でも読み込んだ時点で、
**カードマスタのロードがクラッシュする。**

**なぜ今まで発覚しなかったか（2 重の見落とし）**

1. Dart テスト 26 件が Dart SDK 未導入のため一度も実行されていない
2. `verify_contract.py` は色キーの検査（75-78 行）を持っているが、
   参照先が `test/fixtures/cards_BP01.json` の **5 枚だけ**。全 22 商品を見ていない

**実在確認**

引き継ぎ資料の未解決課題 U1 に「ライブの `cost` が `"ドロー1"`」とある。
これは `parse_heart_string` を通って `{"DRAW": 1}` になる経路そのもの。
仮説ではなく**実データに存在する**。

**推奨する直し方（型で分ける）**

配信 JSON の段階で色と効果を別フィールドに分離する。

```jsonc
"bladeHearts":       { "BLUE": 1 },        // 8.3.14 ライブ所有ハートに合算
"bladeHeartEffects": { "DRAW": 1 }         // 8.3.12.1 / 8.4.2.1 の別処理
```

Dart 側は `BladeHeartEffect { draw, score }` を新設して受ける。
理由: 8.3.12.1（ドロー）と 8.4.2.1（スコア）は**ハート集計とは処理タイミングも対象も違う**。
同じ `Map<HeartColor,int>` に同居させること自体が Phase 3a の集計バグの温床になる。

**併せて必要な対策**

`verify_contract.py` の検査対象を `data/dist/cards/*.json` の全 22 ファイルに広げる。
5 枚のフィクスチャで契約を保証したつもりになっていた、というのが今回の本質。

---

### A-2. `【ターン2回】`（全角括弧）がキーワード抽出から漏れている

`constants.py` の `KEYWORD_TOKENS`：

```python
"[ターン1回]": "TURN_1",
"【ターン1回】": "TURN_1",     # 半角・全角の両方がある
"[ターン2回]": "TURN_2",       # ★全角 【ターン2回】 が無い
```

`extract_keywords` は単純な部分一致なので、公式テキストが全角括弧を使っている
カードは `TURN_2` が付かない。ターン 2 回は ver 1.05（2026-03-31）追加で
該当が 6 件しかないため、統計上も気づきにくい。

**修正**: `"【ターン2回】": "TURN_2"` を追加。修正後 `stats` で件数が変わるか確認する。

---

### A-3. 数字を伴わないブレードハート表記を無言で捨てていた ★レビューに無かった発見

> **対応済み（2026-08-23）。**

**現象**

`_TOKEN_RE` が数字を必須にしていたため、`ドロー1` は拾えるのに
`ドロー` / `[ドロー]` のような**数字なしの表記が 1 件も拾われていなかった**。
例外も警告も出ず、配信データからブレードハートが欠落した状態で正常終了していた。

**影響**

**59 種**でブレードハートが欠落していた（DRAW 22→59 / SCORE 15→37）。
色は 5,367 刷り分すべてが数字を伴っていたため影響なし。

| 意味 | 実在する書き方 |
|---|---|
| ドロー | `ドロー1` 24 刷り / **`ドロー` 48 刷り** / **`[ドロー]` 3 刷り** |
| スコア | `スコア1` 19 刷り / **`スコア` 27 刷り** / **`[スコア]` 1 刷り** |

同じ `cardNumber` でも刷りごとに書き方が揺れる（`PL!-bp4-022` は `-L` が `スコア`、
`-SECL` が `スコア1`）。`normalize_all` は先勝ちなので、壊れた刷りが勝つと丸ごと落ちる。

**対応**

数値やトークンのパースで数字・括弧を必須にしないようにし、V15 を新設して検出可能にした。
詳細は CLAUDE.md §5-(7)。

**★この項目が「失敗の型」の基準になっている★**

A-3 は、他の箇所から**繰り返し参照される基準点**になっている。
型としては次のとおり。

> **出力に何の痕跡も残さずデータを落とし、呼び出し側が損失を知る手段を持たない。**
> 例外も警告も出ないので、「動いている」ように見えたまま欠落が進行する。

同じ型として扱われている箇所:

| 箇所 | 内容 |
|---|---|
| `fold.dart` の `minTrigramLength` | trigram は 3 文字未満だと**エラーにならず静かに 0 件**を返す。`LIKE` へ切り替えて回避 |
| 下記 **D-1** | 未知キーの寛容化は「取りこぼしが静かに進行する」ので、読み飛ばした事実を UI に出すまでを含める |
| **決定 D50** | `search()` の `limit` 超過が戻り値に痕跡を残していなかった（実データで 534 種）。`truncated` を追加して解消 |

---

## B. 要確定（設計判断が必要）

### B-1. 「13 フェイズ」の内訳が条文と 1 つ合わない

> **確定済み（2026-08-23）。詳細は `docs/PhaseEngine設計メモ.md`**
>
> **リーフフェイズは 12 個で確定**（7.1.2 / 7.3.3 / 8.1.2）。
> 「13 フェイズ」は誤記であり、条文に 13 番目に相当するフェイズは存在しない。
> コンテナ（先攻通常 / 後攻通常 / ライブ）を含めても 15 で、13 にはならない。
>
> **下記の提案（`turnEnd` を 13 個目のフェイズにする）は撤回する。**
> 8.4.13 と 8.4.14 はライブ勝敗判定フェイズ 8.4 の第 13・第 14 手順であり、
> フェイズに昇格させると 8.1.2 の列挙（4 つ）と条番号の 1 対 1 対応が崩れる。
> 条文はターン境界の誘発を意図的に既存フェイズへ畳み込んでおり
> （「ターンの始めに」は 7.4.2、「ターンの終わりに」は 8.4.10）、
> `turnEnd` を作ると誘発の置き場所が 2 箇所に見える。
> また 8.4.12 は 8.4.9 へ戻るループを持つため、フェイズを単純な前進列にすると表現できない。
>
> **代わりにフェイズの下へ、条番号をそのまま ID とするステップ層を置く。**
> 8.4.13 / 8.4.14 はステップとして実体化する。D25 の受け皿はステップ 8.4.13 が持つ。
> D31「フェイズを 1 つ戻す」は**ステップ単位**に読み替える。
> 8.4.12 のループがあるため、巻き戻しは静的グラフの逆辺ではなく**通過履歴スタック**から行う。
>
> フェイズはロール（先攻/後攻）で定義し、実プレイヤーは `firstPlayerId` から解決する。
> 8.4.13 で入れ替わるため、**フェイズに実プレイヤー ID を埋めない**。

#### ★ 決定 D25 の訂正（2026-08-23）

決定 D25「先攻入れ替えは**手動確定した勝敗**をもとに自動適用」は**厳密には誤り**。

8.4.13 が参照するのは 8.4.6 で決まる勝敗ではなく、
**8.4.7 で実際に成功ライブカード置き場へカードを移動したかどうか**である。

> 8.4.13. **8.4.7 において**、一方のプレイヤーのみが成功ライブカード置き場にカードを
> 移動していた場合、そのプレイヤーが先攻プレイヤーとなり、その相手が後攻プレイヤーとなります。

8.4.7.1 により、**両者がライブに勝利し、かつライブカード置き場に 2 枚あるプレイヤーは
カードを移動しない**。したがって「ライブに勝利した」と「移動した」は一致しない。

具体的にずれる例:

| 状況 | 8.4.6 の勝敗 | 8.4.7 の移動 | 8.4.13 の結果 |
|---|---|---|---|
| 同点。A はライブ置き場に 1 枚、B は 2 枚 | **両者勝利** | A のみ移動（B は 8.4.7.1 で移動しない） | **A が先攻になる** |

D25 の字面（勝敗をもとに判定）だと「両者勝利＝勝者が 1 人に定まらない」ため
先攻は据え置きと判断してしまうが、条文では **A に先攻が移る**。

**正しくは「先攻入れ替えは 8.4.7 の移動実績をもとに自動適用」。**
実装では 8.4.7 の移動実績を記録し、8.4.13 がそれを参照する。
D10「勝敗確定は手動ボタン」で確定するのは 8.4.6 の勝敗であり、**これとは別の情報**である。

---

引き継ぎ資料は一貫して「13 フェイズ」と書いているが、資料自身の列挙は 12 個。

```
先攻通常フェイズ（アクティブ / エネルギー / ドロー / メイン）      4
後攻通常フェイズ（同上）                                          4
ライブフェイズ（セット / 先攻パフォ / 後攻パフォ / ライブ勝敗判定） 4
                                                          合計 12
```

条文（7.3.3 / 8.1.2）も 12 で、13 番目に相当するものは無い。

~~**提案**: 8.4.13（先攻入れ替えの判定）と 8.4.14（ターン終了）を
`turnEnd` という 13 個目のフェイズとして明示的に持つ。
決定 D25「先攻入れ替えは手動確定した勝敗をもとに自動適用」と
D31「フェイズを 1 つ戻すで代用」の受け皿がここに必要なため、
フェイズとして実体化しておくほうが素直。~~ — **撤回済み**（上記の確定ブロックを参照）

### B-2. エネルギーデッキは「順番が管理されない」

> **確定済み（2026-08-24 / 決定 D73）。詳細は `docs/盤面設計メモ.md` §2**
>
> **`DeterministicRng` による無作為 1 枚抽出で確定する。**
> 下記の提案どおりだが、**根拠は「観測上の差が無いから」ではない。差は出る。**
>
> ★★**本文が引いていなかった条が 2 つあり、それが結論を決めた。**★★
>
> - **4.9.3**「エネルギーデッキ置き場のカードを他の領域に**複数枚移動する場合、
>   1 枚ずつ移動を行います**」——「無作為 1 枚抽出を N 回」という形が条文の形と一致する。
>   6.2.1.7 の「上から 3 枚」はこの 1 枚ずつの 3 回である
> - **6.2.1.3**「各プレイヤーは自身のエネルギーデッキをエネルギーデッキ置き場に**置きます**」
>   ——★**シャッフルの指示が無い**（6.2.1.2 のメインデッキは「置き、それをシャッフルします」）
>
> ★**現状の実装（`step_engine.dart` の `_drawEnergy` が `deck.first`）は差を生んでいる。**
> 6.2.1.3 がシャッフルしないので `energyDeck` の並びは**デッキリストを並べた順そのもの**であり、
> **「一番上」＝プレイヤーが構築時に決めた順**になる。
> 4.9.2 が「順番は管理されない」と定めて奪っているはずの選択を、実装が返してしまっている。
> 同ファイルの doc「非公開領域 (4.9.2) なので観測上の差は出ない」は**この点で誤り**。
>
> ★**「開始時に 1 回シャッフルして以後は上から取る」案も成立しない。**
> エネルギーカードは控え室を経由しない閉ループ（4.9.1 →（6.2.1.7 / 7.5.2）→ 4.7 →
> 5.10.1 → **10.5.4**）で、**カードはデッキへ戻ってくる**（`docs/PhaseEngine設計メモ.md` §9）。
> 戻すたびに再シャッフルが要り、**挿入位置が観測可能な設計になる。**
> 無作為抽出なら戻す位置を決める必要が最初から無い。
>
> **要る変更**（実装は Phase 3b の着手時。方式は決定 D73 に書いてある）:
> `_drawEnergy` の 1 行 ＋ `GameAction` に `DrawEnergy` を新設（6.2.1.7 と効果由来の抽出が通る）。
> ★`MoveCard`（instanceId 指定）は残す ——「エネルギーデッキを**見て** 1 枚選ぶ」効果に要る。
> **無作為と指定は別の操作**であり、片方に畳まない。
>
> **UI**: エネルギーデッキは**裏面 + 枚数だけ**（4.1.2.2）。
> ★**「上から見る」を出さない** —— 5.7.1 / 5.7.2 / 10.2.2.2 はいずれも
> **メインデッキ置き場**の規定で、エネルギーデッキ置き場を対象にした条は存在しない。

4.9.2 は `エネルギーデッキ置き場は…カードの順番は管理されません` と定めるが、
6.2.1.7 と 7.5.2 は `上から 3 枚` `一番上のカード` と書く。条文上の緩い不整合。

~~**提案**: エネルギーデッキはシャッフルの概念を持たせず、
`DeterministicRng` による無作為 1 枚抽出として実装する。
非公開領域（4.9.2）なので観測上の差は無く、seed 再現性も保てる。~~
— **結論は同じだが理由が違う**（上記の確定ブロックを参照）。
★「非公開領域なので観測上の差は無い」という根拠は**成立しない。**

### B-3. 重ね置きの解消先が親子で異なる

> **確定済み（2026-08-23）。詳細は `docs/PhaseEngine設計メモ.md`**
>
> **最初の分岐はカード種別ではなく「移動先がメンバーエリアかどうか」。**
> 4.5.5.3 により、メンバーが**他のメンバーエリア**へ移動する場合は下のカードも
> 重なったまま同時に移動し、解消は起きない。下記の表が効くのは
> 4.5.5.4（メンバーエリア**以外**へ移動する場合）に限られる。
>
> **★解消は即時ではない。** 4.5.5.4.1 / 4.5.5.4.2 はいずれも
> 「そのままそのメンバーエリアに残り、**その後にルール処理により**」と書いており、
> 10.1.2 によりルール処理はチェックタイミングでのみ実行される。
> したがって「上にメンバーが居ない孤児カードがメンバーエリアにある」状態は**正規の中間状態**で、
> メンバーの移動と同時に下のカードを消す実装は条文違反になる。
>
> **10.5.5 の「上記のいずれか」は広義（10.3〜10.5 全体）と読む。**
> 主根拠はエネルギーカードの閉ループ性。4.9.1 →（6.2.1.7 / 7.5.2）→ 4.7 →
> 5.10.1 → 10.5.4 と巡り、5.9.1 のコスト支払いはウェイトにするだけで領域を移動しない。
> 6.1.1.1 によりメインデッキにエネルギーは入らず、控え室側の循環にも合流しない。
> **条文全体で「エネルギーカードを控え室に置く」と書いた条は 1 つも存在しない。**
> 唯一 10.4.1 だけがカード種別に触れず「それ以外のそのメンバーエリアのカード」と書くため
> 閉ループを破りうる。10.5.5 はこれを塞ぐ規定と読むと明確な仕事を持つ。
>
> **【要確認】** 10.4.1 を字面どおり読むと、残す 1 枚の下のスタックまで剥がれる。
> 4.5.5.3 との整合を優先し**残すメンバーのスタックは維持する**と決定した。
> 実装地点に貼るコメント文面を設計メモ §10 に用意してある。

ver 1.06 でメンバーの下に**メンバーカード**も置けるようになった（4.5.5）。
上のメンバーが居なくなったときの行き先が種別で分かれる。

| 取り残されたカード | 行き先 | 条番号 |
|---|---|---|
| メンバーカード | 控え室 | 10.5.3 |
| エネルギーカード | **エネルギーデッキ置き場**（控え室ではない） | 10.5.4 / 10.5.5 |

**エネルギーカードは控え室を経由しない閉ループである。**
Phase 3a の「整理（ルール処理）」コマンドはこの分岐を持つ必要がある。

### B-4. ポジションチェンジ / フォーメーションチェンジ

> **確定済み（2026-08-24 / 決定 D74）。詳細は `docs/盤面設計メモ.md` §3**
>
> **補助コマンドを置く。★`loveca_core` の変更は要らない。**
>
> | 操作 | 写像 |
> |---|---|
> | ポジションチェンジ（移動先が空） | `MoveMemberBetweenAreas` × 1 |
> | ポジションチェンジ（11.10.2 の入れ替え） | `MoveMemberBetweenAreas` × 2 |
> | フォーメーションチェンジ（11.11.1） | `MoveMemberBetweenAreas` × N |
>
> 成立の根拠 3 つ。
> ① 4.5.5.3（下に重ねられたカードも同時に移動）を `_moveMemberBetweenAreas` が既に満たす。
> ② 途中で 1 エリアに 2 人並ぶ**中間状態は正規**（10.1.2 / `member_area.dart:71-73`）なので
> **任意の置換に到達できる**。
> ③ `stacks` の末尾が 10.4.1 の「最も後から置かれた」と整合する
> （★**このリストを並べ替える操作は作らない**）。
>
> ★**合成の途中で `Tidy` を挟まない。** 挟むと 10.4 が走って中間状態が壊れる。
> ★**1 undo にまとめる** —— `reduce` を N 回回して `GameSession.record` を 1 回だけ呼ぶ
> （`record` は公開 API なので core の変更は不要 / 決定 D78）。
>
> ★★**11.10.2 / 11.11.2 の制約はコマンドの中だけに置く。素のドラッグには課さない。**★★
> どちらも**その効果の中での制約**であって盤面の不変条件ではない。
> 課すと 4.5.5 / 10.4 の中間状態を手で再現できなくなり、
> `MemberArea` が型で表現できるようにした状態（孤児カード / 2 人並んだエリア）を
> **サンドボックスから削ることになる。**
>
> ★**【要確認】** 11.10.2 は移動先のメンバーを**単数**で書いており
> （「移動先にいるメンバーは」）、**2 人以上いる場合を定めていない。**
> 11.11.2 も、既に 2 人並んだエリアがあると「すべてのメンバーを 1 エリア 1 人ずつ」が**不可能**になる。
> → **推測で埋めない。**どちらかのエリアに 2 人以上いる間は**コマンドを無効にし、理由を出す**
> （「先に整理（10.4）してください」）。★**黙って効かないボタンにしない。**
> 公式 Q&A で裏が取れたら再確認する。

11.10 / 11.11 で定義されるが、これらは `【】` で囲まれたキーワード能力ではなく
**効果テキスト中の動詞**。`KEYWORD_TOKENS` に無いのは正しい。

ただし盤面 UI では「入れ替え（11.10.2: 移動先のメンバーは元の位置へ）」と
「全員再配置（11.11.2: 1 エリアに 2 人以上は不可）」を
ワンアクションで行える補助コマンドがあると実用性が大きく変わる。
効果の自動処理ではなく**物理操作の補助**なので、大方針に抵触しない。

---

## C. Phase 3a 着手前に確定しておきたい処理順序

条文どおりの順序でないと集計が狂う箇所を抜き出した。

### パフォーマンスフェイズ（8.3）

```
8.3.4   ライブカード置き場を全て表向き → ライブでないカードを控え室へ
8.3.6   この時点でカードが無ければフェイズ終了
8.3.8   「ライブ開始時」の事象が発生（8.3.6 で終了した場合は発生しない / 11.5.2.1）
8.3.10  アクティブ状態のメンバーのブレードを合計
8.3.11  合計ブレード数だけ、メインデッキの一番上を解決領域へ（＝エール）
8.3.12  ★解決領域のブレードハートのドローアイコン分だけカードを引く
8.3.14  ★全メンバーのハート ＋ 解決領域の自分のカードのブレードハート を合計
8.3.15  各ライブカードの必要ハートを満たすか確認（ALL は任意の 1 色として扱う）
```

**ドロー（8.3.12）はハート集計（8.3.14）より前**。逆にすると引いたカードが
その回の集計に影響し得る実装になってしまう。

### リフレッシュはチェックタイミングを待たない（10.2.1）

エール（8.3.11）の途中でメインデッキが尽きた場合、
**その場で処理を中断してリフレッシュし、続きを実行する。**
他のルール処理（10.1.2）と挙動が違うので、実装場所を分ける必要がある。

### 勝利処理は同時到達で引き分け（1.2.1.2）

成功ライブカードが両者同時に 3 枚以上になった場合は引き分け。
決定 D10 の「勝敗確定は手動ボタン」に**引き分けの選択肢**が要る。

---

## D. その他（影響小・記録のみ）

### D-1. `fromKey` の厳格性は運用リスクを抱えている【Phase 4 で判断】

A-1 の修正で `bladeHearts`（色）と `bladeHeartEffects`（DRAW/SCORE）を型で分離し、
`HeartColor.fromKey` / `BladeHeartEffect.fromKey` は未知キーで `throw` する設計を維持した。
契約としてはこれが正しい。生成側が契約を破ったら止まるべきだからである。

**ただし配信データ駆動アプリとしては別のリスクが残る。**
公式が将来 3 つ目の非色アイコンを追加した場合、
新しい配信データを受け取った**既存インストール済みアプリのマスタロードが全滅する**。
アプリ側を更新しない限り復旧できない。

現状の担保は「配信前に `verify_contract.py` が全 22 商品を検査する」ことのみで、
これは**こちらが配信を止められる**という前提に立っている。
Phase 4（認証・同期）で配信経路が固まった時点で、次のどちらを取るか判断する。

| 案 | 挙動 | 代償 |
|---|---|---|
| 現状維持（厳格） | 未知キーで例外。契約違反が即座に露見する | 新アイコン追加で既存アプリが起動不能になりうる |
| 寛容化 | 未知キーは読み飛ばし、件数を記録して警告表示 | 取りこぼしが静かに進行する（A-3 と同じ失敗の型） |

寛容化するなら「読み飛ばした事実を UI に出す」までを含めること。
黙って捨てるのは A-3（数字なし表記を 59 種で無言に捨てていた）と同じ失敗になる。

**★Phase 2 側の暫定運用（決定 D39・2026-08-23）★**

`loveca_db` の取り込み層は、未知キーの `ArgumentError` を
**商品ファイル単位で隔離**する。失敗したファイルはハッシュを記録せず
（次回再取得される）、既存の行は残し、`import_issues` に記録して
`outstandingImportIssueCount()` から UI が検知できるようにしてある。

**これは上の判断を前倒しするものではない。**
`loveca_core` の `fromKey` は 1 文字も変えていない。
「厳格を維持するか寛容化するか」の本判断は**この項目に残したまま**であり、
Phase 4 で配信経路が固まった時点で改めて決める。

### D-2. リントが 1 つも効いていない【★2026-08-24 解消（Phase 3b / M-B1）】

> ## ★★ 解消（2026-08-24）★★
>
> `loveca-core/analysis_options.yaml` を置き、`lints` を `loveca-db` に揃えて `^6.1.0` にした。
>
> ★★**下に書いてある「接続すると `constant_identifier_names` が `StepId.s8_4_13` を叩く」
> という前提は誤りだった。叩かない。除外設定は 1 つも要らなかった。**★★
>
> ★**0 件を「無い」と読まないため、先に意図的な違反を置いて確かめた**（D-10 と同じ手順）——
> `const FOO_BAR` と `enum { NOT_CAMEL }` は**どちらも指摘が出る**ので
> ルールは有効に効いている。そのうえで `s8_4_13` は出ない
> （先頭が小文字なら以降の数字と下線は lowerCamelCase として通る）。
> **確かめずに「0 件だから除外は不要」と書いていたら、設定が効いていない可能性と
> 区別がつかなかった。**
>
> **実測 7 件**（閾値は測る前に「20 件以下なら同セッションで直す」と宣言してあった）。
>
> | ルール | 件数 | 所在 | 処遇 |
> |---|---:|---|---|
> | `unnecessary_brace_in_string_interps` | 3 | `deck_validator.dart` | 直した（機械的） |
> | `curly_braces_in_flow_control_structures` | 2 | `redact_test.dart` / `reduce_test.dart` | 直した（機械的） |
> | `unnecessary_library_name` | 1 | `loveca_core.dart` | 直した（機械的） |
> | `equal_elements_in_set` | 1 | `game_types_test.dart:567` | ★**意図的な構文。1 行だけ `// ignore:` にし理由をコメントに書いた**（下記） |
>
> ★7 件のうち**設計判断を要したのは 1 件だけ**で、残り 6 件は機械的に直せるものだった。

**現象**

`loveca-core/pubspec.yaml` は `lints: ^4.0.0` を `dev_dependencies` に持つが、
**`loveca-core/analysis_options.yaml` が存在しない。**
Dart のリントは `analysis_options.yaml` の `include` で有効化されるため、
**`loveca-core` ではリントが 1 つも効いていない。**

`dart test` はリントを走らせないので、テストが全通過していても検知されない。
A-1 の「テストが一度も実行されていなかった」と同じ型の見落とし
（**検証しているつもりで検証していない**）である。

> **★現況の更新（2026-08-23）★**
> 執筆時は「リポジトリのどこにも存在しない」と書いていたが、現在は
> **`loveca-db/analysis_options.yaml` と `loveca-ui/analysis_options.yaml` が存在する。**
> `loveca-db` 側には「この設定は loveca-db のみのものであり決定 D-2 を前倒ししない」
> と理由が明記してある（条番号 ID を持つ enum が無いため下記の衝突が起きない）。
> `loveca-ui` は `flutter create` が生成したもの。
> **未接続なのは `loveca-core` だけ**であり、D-2 の論点はそこに残っている。

**接続する場合に衝突する点**

`package:lints/recommended.yaml` の **`constant_identifier_names`** が
`StepId.s8_4_13` 形式の enum 値名を一律に叩く。

この命名は `docs/PhaseEngine設計メモ.md` §12-2 の
「`StepId` は条番号そのもの」という確定に基づいており、
CLAUDE.md §1「ルールに触れるコードには条番号を併記する」の実装形でもある。
条文と 1 対 1 に読めることを優先しているため、**接続と同時に当該ルールの除外設定が要る。**

| 案 | 代償 |
|---|---|
| 除外設定を書いて接続する | 除外の意図をコメントで残す必要がある |
| 値名を lowerCamelCase にする | 条番号との 1 対 1 対応が崩れる。**採らない** |

**判断の時期**

Phase 3b（PC 盤面 UI）で Flutter 側の `analysis_options.yaml` を作る際に、
`loveca-core` の分と揃えて決める。
Flutter 側は `flutter_lints` を使うため、2 つの設定が並ぶことになる。

**★接続時に `// ignore:` が要る実在箇所（2026-08-23 記録）★**

`dart analyze loveca-core` は現在 **1 件**の警告を出す。
これはリントではなく標準アナライザの警告なので、`analysis_options.yaml` の
有無に関わらず出る。**誤検知であり、コードは正しい。**

| 所在 | 警告 | 判定 |
|---|---|---|
| `loveca-core/test/game_types_test.dart:567` | `equal_elements_in_set`（集合リテラル内の 2 要素が等しい） | **誤検知。修正してはいけない** |

該当箇所は `expect({a, b}.length, 1)` で、
**「等価な 2 つの `StepCursor` が集合の中で 1 つに畳まれること」を確かめている。**
つまり要素が等しいことがテストの目的そのものであり、
重複を消すと巻き戻しの履歴スタックが依拠する値等価性の検証が失われる。

リントを接続するときは、この行に `// ignore: equal_elements_in_set` と
**理由のコメント**を添えること。理由を書かずに ignore だけ置くと、
後から「なぜ黙らせたのか」が分からなくなる。

---

### D-3. 記録のみ

| 項目 | 内容 | 条番号 |
|---|---|---|
| コストの支払い | 自分のリソースでのみ支払える（ver 1.06 追記） | 9.4.2.1 |
| バトンタッチ | 同一エリアの自分のメンバーを控え室に置きコストを減らす | 9.6.2.3.2 |
| 登場制限 | そのターンにステージ外からステージに来たメンバーが居るエリアは指定不可 | 9.6.2.1.2.1 |
| 正面のエリア | 左サイドの正面は相手の右サイド（ver 1.06 追記）＝鏡像配置がルール上も正 | 4.5.7 |
| グループ名一覧 | 付録 A の 1 行目「ラブライブ！シリーズ オフィシャルカードゲーム」が `OFFICIAL_GROUPS` に無い。V11 は WARN なので実害なし | 付録A |
| 注釈文 | ~~`（）` 内の注釈文はゲームに影響しない。検索対象から外すか要検討~~ — **決定 D38 で解消（検索対象に含める）**。2.12.4 が定めるのはゲーム上の効果であって索引の対象ではない。実測で該当 44 種・効果テキストの 2.2% | 2.12.4 |

---

### D-4. `imageHash` は原本のハッシュであって配信物のハッシュではない【Phase 4 で判断】

**現象**

`build_images` は **原本 PNG のバイト列**から `imageHash` を作り、
そのハッシュを **3 サイズすべての WebP のファイル名**に使っている。

```python
image_hash = _sha256(src.read_bytes())[:32]                     # 原本 PNG
...
dest = cfg.dist_dir / "images" / name / f"{image_hash}.webp"    # thumb/normal/large が同名
```
（`loveca-data/loveca_data/build_dist.py:59,70`）

したがって **`Config.image_sizes`（`thumb: (200, 80)` / `normal: (500, 85)` / `large: (1000, 90)`）を
変えると、同じ `imageHash` のまま WebP の中身が変わる。**
`loveca-data/loveca_data/config.py:37-42`。

**なぜ今は表面化しないか**

| | 状態 |
|---|---|
| `manifest.json` の `files` | **25 件（`cards/*.json` 22 + `meta/*.json` 3）。画像は 0 件** |
| したがって `planUpdate` | **画像を一切知らない**（差分更新の対象外） |
| 現在の運用 | dist を丸ごと差し替える |

丸ごと差し替えるかぎり、名前が同じでも中身は必ず新しくなる。**現時点で実害は無い。**

**いつ壊れるか**

★**Phase 4 で「画像を差分で配る」経路を作った瞬間に壊れる。**
受け取り側は `{imageHash}.webp` が既にあれば取得を省くので、
`image_sizes` を変えても**古い画質の画像が更新されずに残る。**
しかも**エラーは出ない**（ファイルは存在し、絵も出る）。

★これは **A-3 と同じ「無言」の型**である。違いは落ちるのがデータではなく更新であること。

| | A-3 | D-4 |
|---|---|---|
| 失敗の型 | 痕跡を残さずデータを落とす | **痕跡を残さず更新を落とす** |
| 気づく手段 | 無い | 無い（絵は出る。画質が古いだけ） |
| 起きる時点 | 取り込み時 | **Phase 4 で差分配信を作った時点から** |

**Phase 4 で判断する。案は 3 つ。**

| 案 | 内容 | 代償 |
|---|---|---|
| (a) `image_sizes` を凍結する | 変えたければ全再配信 | 画質の調整が事実上できなくなる |
| (b) `imageHash` を派生物（WebP）のハッシュにする | 名前が段ごとに変わる | ★**`printings.image_hash` は 1 列しかなく、3 段で別名になると持てない。**スキーマと配信形式の変更を伴う |
| (c) 配信側に画像リビジョンを持たせる | `manifest` か `version.json` に画像世代を足す | 差分更新の粒度が「全画像」になる |

★**この判断は `loveca-data` と配信形式に及ぶので、UI 側だけでは決められない。**
`docs/UI設計メモ.md` §5-4 / 決定 D57 が UI 側からこの穴を参照している。

★**UI 側で先に打てる手は 1 つだけ実施済み**（決定 D57）:
**ファイル名を組み立てる場所を `CardImageSource` の 1 箇所に閉じる。**
規約が変わったときの変更点が 1 箇所で済む。

---

### D-5. `P1`〜`P5` の参照先が存在しない【Phase 4 の着手前に判断】

**現象**

同期の設計番号らしき `P1`〜`P5` が**コードコメントから参照されている**が、
**参照先がリポジトリに存在しない。**

★**箇所数を書かない。**M2〜M-B5 で参照は増え続けており（当初 10 → 実測 19）、
**数を書けば必ず古くなる**（D-15 (g)）。**機械で数えられる側が正である。**

```bash
grep -rnE '[（(]P[1-5][）)]' --include=*.dart loveca-*/lib | grep -v '\.g\.dart'
```

★**Phase 4 の完了条件は「0 件」**なので、現在値を記録する必要がそもそも無い。

★**下の表は発見当時（2026-08-23）の記録である。**参照はその後 M2〜M-B5 で増え、
行番号も動いている（`docs/決定事項一覧.md:324` は現在 `:432`）。
★**網羅の正は上の走査コマンドであって、この表ではない。**

| 所在（★発見当時） | 記述 |
|---|---|
| `loveca-core/lib/src/entities/deck.dart:52` | `★UUID v4。連番にすると端末間で衝突する (P1)。` |
| `loveca-core/lib/src/entities/deck.dart:66` | `★更新のたびに +1 (P2)。同期の差分検出に使う。` |
| `loveca-core/lib/src/entities/deck.dart:63` | `★論理削除 (P3)。物理削除すると削除が同期で伝播しない。` |
| `loveca-core/lib/src/entities/deck.dart:70` | `★作成時のカードマスタ版 (P5)。未知カード検出に使う。` |
| `loveca-db/lib/src/schema/tables.dart:246,255,258,262` | 上記 4 つと同文 |
| `loveca-db/lib/src/dao/deck_dao.dart:118` | `★論理削除（P3）。` |
| `docs/決定事項一覧.md:324` | `デッキ作成時の masterDataVersion（P5）から辿る案もある` |

★ほかに `loveca-db/lib/src/schema/database.g.dart` に 4 箇所あるが、
これは `tables.dart` のコメントを写した**生成物**なので手で直す対象ではない。

出典は `引き継ぎドキュメント.md` と思われるが**git 管理外で存在しない**。
`P4` はどこからも参照されておらず、**5 つあるのか、もっとあるのかも分からない。**

**なぜ着手前に判断が要るか**

★**Phase 4（認証・同期）を実装する人は必ずこれに突き当たる。**
参照されている 5 つはいずれも**同期の中核**である。

| # | 参照されている内容 | Phase 4 での役割 |
|---|---|---|
| P1 | `deckId` は UUID v4。連番にすると端末間で衝突する | 識別子の設計 |
| P2 | `revision` は更新のたびに +1 | **差分検出** |
| P3 | 論理削除。物理削除すると削除が同期で伝播しない | **削除の伝播** |
| P5 | `masterDataVersion` は作成時のカードマスタ版 | 未知カード検出（決定 D35） |
| P4 | **不明** | **不明** |

★**「参照先が存在しない」という事実自体が、着手前に解決すべき項目である。**
実装中に気づくと、`P4` が何を定めていたのかを**知らないまま同期を設計することになる。**
`deck.dart` / `tables.dart` の記述から P1・P2・P3・P5 の内容は読み取れるが、
**P4 だけは手がかりが 1 つも無い。**

**判断すべきこと**

| 案 | 内容 | 代償 |
|---|---|---|
| (a) 復元する | 引き継ぎ資料を探し出して P1〜P5 を `決定事項一覧.md` に写す | 資料が見つからなければ成立しない |
| (b) 採番し直す | 参照を `決定 DNN` に置き換え、内容を改めて決める | 上の走査が返す全行の書き換え。**P4 は決め直しになる**。★**語の衝突（下記）があるので機械的な置換は不可** |
| (c) 現状維持 | コメントはそのまま | ★**Phase 4 で「P4 とは何か」に必ず突き当たる。先送りが解決しない** |

★**(c) は採れない。** 判断を Phase 4 の着手前に置くのは、
**同期の設計を始めてからでは (b) の書き換えが同期の実装と混ざる**ためである。

★**推測で埋めないこと**（`docs/決定事項一覧.md` §0 と同じ扱い）。
P1・P2・P3・P5 は内容が読み取れるが、**読み取れることと「そう決まっていた」ことは別**である。
(b) を採るなら「新たに決めた」として採番し直す。

#### ★★ 語の衝突 —— `P1` / `P2` / `P3` は 2 つの体系で使われている（2026-08-26 発見）★★

★**案 (b) の代償はこれまでの記述より大きい。**
`P1`〜`P5`（同期の設計番号）とは**別に**、`docs/UI設計メモ.md` §2-3 が
**ペイン・ダイアログ**に同じ字面を割り当てている。

| 番号 | 同期の設計番号（この項目） | ★UI のペイン番号（§2-3） |
|---|---|---|
| `P1` | `deckId` は UUID v4 | **検証パネル** |
| `P2` | `revision` は更新のたびに +1 | **取り込み失敗の詳細** |
| `P3` | 論理削除 | **デッキのメタ編集** |

★★**1 行に両体系が同居している箇所がある。**★★

```
loveca-ui/lib/src/ui/deck/deck_edit_page.dart:11
  | 論理削除（P3） | ★検証パネル **P1** の常設 |
     ^^ 同期の設計番号        ^^ UI のペイン番号
```

→ ★**素の `P1` で機械的に置換すると UI のペイン名まで壊れる。**
走査は**括弧つきの形**で行うこと（上の走査コマンド）。
★`CLAUDE.md` §8 の索引にも 1 行だけ書いてある。

★**この衝突は D-5 を解決しない。**(b) の見積りが変わるだけである。

**同種の未解決参照**

`P1`〜`P5` だけではない。リポジトリ全体に**同種の系統が複数**（手書き）ある。
★**件数はここに書かない** —— 2026-08-26 の数え直しで **3 系統とも動いていた**
（`設計書 STEP` 7→6 / `P1〜P5` 10→19 / `U1` 2→**0**）。

| 系統 | 箇所 | 判断の要否 |
|---|---:|---|
| `設計書 STEP N §N.N` | 7 | **不要。**「対応関係の記録」であって内容を参照していない |
| `M44` | 1（上記に含む） | 同上 |
| **`P1`〜`P5`** | **10** | ★**要（この項目）** |
| `U1`（引き継ぎ資料の未解決課題） | 2 | 不要。内容は `ルール整合性チェック_v1.06.md` A 節に取り込み済み |

一覧は `docs/UI設計メモ.md` §1-2。

---

---

### D-6. 移行機構（`onUpgrade`）が一度も実行されていない【★2026-08-24 解消】

**現象**

決定 D49 で `schemaVersion` を 2 へ上げ、`MigrationStrategy.onUpgrade` を新設した。
中身は `CardSearchDao.rebuildAll()` 1 本である。

★**`rebuildAll()` そのものは固定されている**
（`loveca-db/test/card_search_test.dart:269,285`「rebuildAll で索引が同じ状態に戻る」
「rebuildAll はデッキに触らない」）。

★★**しかし `onUpgrade` が呼ばれる経路は、どこでも一度も実行されていない。**★★

| 経路 | 走るもの |
|---|---|
| `loveca-db` のテスト | **すべてインメモリの新規 DB** → `onCreate` のみ |
| `loveca-ui` M1 の初回起動 | 新規 DB → `onCreate` のみ |
| `loveca-ui` M1 の 2 回目以降 | `schemaVersion` が同じ → **何も走らない** |

`onUpgrade` を発火させるには **`schemaVersion` 1 のまま作った DB を 2 で開く**必要があり、
そういう DB を作るテストが**存在しない**。

**なぜ問題か**

移行機構は「スキーマを変えた版を配ったときに、既存端末のデッキ（ユーザデータ）を
捨てずに済ませる」ためにある（`database.dart` の doc）。
**その本番の役目が一度も走っていない。**

★これは **D-2（リントが 1 つも効いていない）と同じ型**である。
「仕組みは置いたが、それが働いていることを誰も確かめていない」。
`rebuildAll()` が通ることと、**`onUpgrade` から `rebuildAll()` が呼ばれること**は別である。

★**失敗しても静かとは限らないが、気づくのが最悪のタイミングになる。**
気づくのは「新しい版を配ったあと、利用者の端末で」であり、
そのとき壊れうるのは**作り直せない `decks`**（決定 D11 / D35）である。

**やること（判断ではなく作業）**

`loveca-db` に「v1 相当の DB を作り、v2 で開いて `onUpgrade` が走ることを確かめる」テストを足す。
drift には `NativeDatabase` を `schemaVersion` を偽装して開く直接の口が無いので、
素の `sqlite3` で v1 相当のスキーマを作って `user_version` を 1 にする、
あるいは `card_search` を v1 の列構成で作って開き直す、といった形になる。

#### ★ 解消（2026-08-24）

`loveca-db/test/migration_test.dart` を新設。**10 件**で固定した。
`loveca-db/lib` は変更していない（テストのみ）。スキーマも触っていない。

**v1 の再現方法の判断**

| 案 | 採否 | 理由 |
|---|---|---|
| 生 SQL で v1 スキーマを全部書く | ✗ | ★**実テーブル 16 個は v1 と v2 で同一**なので、複製しても何も検証しない。複製は腐るだけ |
| 過去のスキーマ定義（drift の `schema dump` + `SchemaVerifier`） | ✗ | ★★**drift は `card_search` を知らない。**`customStatement` で作る仮想テーブルは `allSchemaEntities` に載らずスキーマ比較の対象外なので、**v1↔v2 の唯一の差分が見えない。**加えて v1 のスナップショットは現存しない |
| **v2 で作ってから `card_search` だけ巻き戻す（採用）** | ○ | 検証対象にまっすぐ当たる。drift の native 実装は `user_version` プラグマで版を判定する（`drift/lib/native.dart`）ので、1 にして開き直せば `onUpgrade(1, 2)` が走る |

★**この手法が正しいのは「v1 と v2 で実テーブルが同一」だから。**
次に実テーブルを動かす移行を入れたら使えなくなる。テストの doc に条件を明記した。

★**v1 の DDL は現在の定数から導出せず、直書きで凍結した。**
今の定義から `card_number_raw` を引く形にすると、将来また列を足したときに
v1 の定義も一緒に動き、**テストが黙って無意味になる**（決定 D51 と同じ型）。

**固定したこと**

巻き戻し後が v1 の形（5 列 / `user_version` 1）であること（★素の `sqlite3` で覗く。
drift で開くと移行が走ってしまうため）/ v2 で開くと 6 列・`user_version` 2 になること /
★**`decks` が保存されること**（D49 の主張の検証）/ `cards` / `printings` も消えないこと /
★索引が再構築され検索が動くこと（件数一致・trigram 経路・**返る cardNumber が保存表記**）/
★2 回目に開いても `onUpgrade` が走らないこと。

★**番兵方式の依存を、前提そのものをテストにして守った。**
「2 回目は走らない」は索引に番兵行を入れて生存を見る方式だが、
これは **`rebuildAll` が `DROP TABLE` してから建て直す**ことに依存している。
将来 `rebuildAll` が差分更新に変わると、**番兵は生き残りテストは通り続けるが
何も検証しなくなる。** そこで「`rebuildAll` は破壊的である」を独立した test にし、
前提が崩れたら黙って通るのではなく落ちるようにした。

**★テストに歯があることを確認した**

`onUpgrade` の中身を一時的に無効化して走らせたところ **5 件が落ちた**。
★**ただし「`user_version` が 2 になる」だけは通った。**
drift は `onUpgrade` が何もしなくても版を上げるためで、
**この検査単独では移行の中身が走った証拠にならない。**
移行が働いたことを言えるのは「索引が v2 の形になる」「trigram 経路で引ける」
「返る cardNumber が保存表記」の 3 つである。その事実を当該 test のコメントに残した。

#### ★ 同種の穴の洗い出し — 手法の記録

**「仕組みは置いたが、それが働くことを誰も確かめていない」箇所を探す作業は今後も発生する。**
今回どう走査し、何を見落としたかを残す。

**走査した観点**

| # | 観点 | 具体的な探し方 |
|---|---|---|
| 1 | **本番の呼び出し元が 0 の公開 API** | `grep -rn "<名前>" lib test ../他パッケージ/lib` で、`lib/` の定義と `test/` しか出ないもの |
| 2 | **戻り値を誰も読んでいない** | 戻り値の型のフィールド名（例: `wasRecovered`）を全体 grep し、定義箇所しか出ないもの |
| 3 | **doc が「〜で掃除する / 〜が扱う」と書いているのに繋がっていない** | doc コメント中の識別子を grep して呼び出し元を確かめる |
| 4 | **設定・フラグを立てているが効果を確かめていない** | `PRAGMA` / `analysis_options.yaml` / lint など、有効化と効果が別 |
| 5 | **テストが「作られたこと」しか見ていない** | assert が生成物の存在だけで、**利用側まで届くか**を見ていない |

★**観点 5 は今回、自分のテスト自身に見つかった**（`boot_gate_test` の `_pump` が
Notice を描かない builder を使っていた）。**探す側が同じ穴を持つ。**

★★**同じことが D-10 でも起きた。**★★
`.gitignore` の見張りを書いたとき、除外を**ディレクトリ名**で書いてしまい、
**最も警戒すべき `lib/src/rules/build/` を見張り自身が除外していた**（模擬事故を
流しても 0 件を返し、正常だと報告した）。
→ **D-10 の「検知手段自身が同じ罠を踏んだ」を参照。**
**探す道具は対象と同じ材料でできているので、同じ癖がそのまま入り込む。**
検知手段を書いたら、必ず「見つかるはずのもの」を仕込んで動かすこと。
**0 件は「無い」と「見えていない」の区別がつかない。**

**★見落としうるもの（今回実際に誤検知した）**

★**日本語のテスト名は英語の grep に引っかからない。**
外部キーの cascade を「未検証」と判断したが**誤りだった**——
`test/schema_test.dart:155`「カードを消すと刷りとハートも消える」が既に固定していた。
`foreign_keys|cascade|onDelete` で grep したが、**テスト名は日本語で、
本体は drift の Dart API（`delete(...)`）なので英語キーワードが 1 つも無かった。**

→ **対策**: 「未検証」と結論する前に、
**関連するテストファイルの `test(` / `group(` の一覧を目で見る**こと。
このリポジトリのテスト名はすべて日本語なので、**キーワード grep だけでは足りない。**

```bash
grep -n "test(\|group(" loveca-db/test/*.dart
```

**★見つかったもの**

| # | 穴 | 処遇 |
|---|---|---|
| B | `AppSettingsLoad.wasRecovered` が握り潰されていた（`loveca-ui`。観点 2） | **潰した**（`59ac583`） |
| C | `CardImageSource` にテストが無い（観点 5 の予備軍） | **潰した**（`59ac583`） |
| D | `deleteOrphanCards` の本番呼び出し元が 0（観点 1・3） | **D-8 に登録**（挙動変更を伴うため） |

---


---

---

### D-7. `min_app_version` が CLI に露出しておらず 1.0.0 で固定されている【Phase 4 で判断】

**現象**

```python
def build(cfg, result, *, data_version: int,
          min_app_version: str = "1.0.0", with_images: bool = True) -> dict:
    ...
    "minAppVersion": min_app_version,
```
（`loveca-data/loveca_data/build_dist.py:84,196`）

`min_app_version` は**関数の既定値だけで、CLI には露出していない**
（`loveca_data/cli.py` に該当の引数が無い）。
したがって **これまでに作られた dist はすべて `minAppVersion: "1.0.0"` を宣言している。**

`planUpdate` は `remoteVersion.isAppSupported(appVersion)` が偽なら
`UpdateDecision.appTooOld` を返し、**1 ファイルも取り込まずに戻る**
（`loveca-core/lib/src/master/master_data.dart:225`）。

**いつ表面化したか**

Phase 2 後半 M1 の実機起動。`loveca-ui` が自分の版 `0.1.0` を渡したところ
`appTooOld` に落ち、カードが 1 件も入らなかった。

★**`spike` はこの経路を一度も通っていない。**
`loveca-ui/spike/common/spike_db.dart:118` が `appVersion: '1.0.0'` を
**ハードコード**していたためである。決定 D51 が言う
「spike は本実装と食い違っても誰も気づかない」の実例。

**★★ 現在の回避は本来と逆向きである ★★**

M1 では `loveca-ui/pubspec.yaml` の `version` を **`1.0.0` に上げて**回避した。
つまり**配信データに合わせてアプリの版を変えた**。

| | 本来 | 現在の回避 |
|---|---|---|
| `minAppVersion` の意味 | この配信データを読める**アプリの最小版** | — |
| 決まる順序 | **アプリの能力が先。データがそれを宣言する** | **データが先。アプリが合わせる** |

`1.0.0` は**意味を持って設定された値ではなく、関数の既定値がそのまま出ている**だけである。
このアプリは実際に dataVersion 2 の形式を読めるので、
本来 `minAppVersion` はもっと低い値であるべきだった。

★**この前例を残しておく理由。**
書かずに済ませると、次に同じことが起きたときも「アプリの版を上げれば通る」で
片付けられ、**`minAppVersion` という仕組みが恒久的に無意味になる。**
Phase 4 で `minAppVersion` が本当に効く場面（古いアプリを弾く）が来たとき、
判断の根拠が失われている。

**やること（Phase 4 で判断）**

| # | 内容 |
|---|---|
| 1 | `min_app_version` を CLI に露出する（`python -m loveca_data build --min-app-version ...`） |
| 2 | 既定値を実態に合わせる（アプリが実際に読める最小版。現時点なら `0.1.0` 相当） |
| 3 | dist を作り直す。★`--skip-images` は `imageHash` も空にするので**使えない**（CLAUDE.md §7-3）。画像込みで回す |
| 4 | `loveca-ui/pubspec.yaml` と `AppInfo.version` の注記を外し、版を実態に戻すか判断する |

★**判断を Phase 4 に置く理由。** `minAppVersion` は配信経路と一体の仕組みであり、
D-1（`fromKey` の厳格性）と同じく「配信経路が固まった時点」でまとめて決めるのが筋である。

---

---

### D-8. `deleteOrphanCards` の本番呼び出し元が 0【★2026-08-24 方式決定（D63）/ 実装は `loveca_db` を触る時点】

**現象**

`CardDao.deleteExpansion` の doc はこう書いている。

> `cards` は他の商品から参照されうるので消さない
> （どこからも参照されなくなった行は [deleteOrphanCards] で掃除する）。
（`loveca-db/lib/src/dao/card_dao.dart:80-83`）

しかし **`deleteOrphanCards` を呼ぶ本番のコードが 1 つも無い。**
呼んでいるのはテスト 3 箇所だけ（`card_dao_test.dart:196` /
`card_search_test.dart:315` / `deck_dao_test.dart:163`）。

`MasterImporter._deletePath` は `deleteExpansion` しか呼ばない
（`master_importer.dart:295-300`）。

**何が起きるか**

配信のマニフェストから商品ファイルが消えると `planUpdate` が `filesToDelete` に載せ、
`deleteExpansion` が **`printings` だけ**を消す。その結果:

| 残るもの | 帰結 |
|---|---|
| 孤児の `cards` 行 | どの刷りからも参照されないカードが残る |
| 孤児の `card_search` 行 | ★**検索が「存在しない刷りの cardNumber」を返しうる** |

★`deleteExpansion` は索引に触れないので、**索引の掃除も同時に漏れている。**

一覧（`loveca-ui` の投影クエリ）は `printings JOIN cards` なので出てこないが、
**検索（M3）は `card_search` を直接引くので出てくる。**
利用者から見ると「検索に出るのにカードが開けない」になる。

**★今すぐ壊れてはいない**

配信から商品が消えたことは実データでまだ一度も起きていない
（`manifest.json` の 25 ファイルは増える一方）。**潜在的な欠陥である。**

**なぜ今潰さないか**

★これは `MasterImporter` の**挙動変更**であり、D-6 を潰す作業
（＝**検証**であって機能追加ではない）の範囲外だから。
`deleteOrphanCards` をどこで呼ぶかは設計判断を含む。

| 案 | 内容 |
|---|---|
| (a) `MasterImporter.import` の末尾で毎回呼ぶ | 単純。毎回 1 回の全走査が増える |
| (b) `filesToDelete` が空でないときだけ呼ぶ | 必要なときだけ。判断が 1 つ増える |
| (c) `deleteExpansion` の中で呼ぶ | 名前と実体が食い違う（「商品を消す」が「掃除もする」になる） |

★**判断は M3（検索の実装時）に置いた。** 検索が本番の呼び出し元を持つ時点であり、
そこで初めて「存在しない cardNumber が返る」が実害になる。

#### ★ 2026-08-24 の判断（決定 D63）

**採用は案 (b)**（`filesToDelete` が空でないときだけ、削除と同じトランザクション内で呼ぶ）。
**条件つきにする根拠**（孤児が生まれる経路は `deleteExpansion` だけ／毎起動の空振りを増やさない／
案 (c) は名前と実体が食い違う）と、**一度きりの掃除の方式**
（配信の `manifest.json` から商品を消したことがあるかで決める。無ければ掃除経路を作らない）は
`docs/決定事項一覧.md` の **D63 の詳細**に書いた。

★**実装は `loveca_db` の変更を伴うため M3 では行わない。**
M3 が引き受けたのは **UI 側で無言にしないこと**だけである。
検索が返した cardNumber のうち現在のカードデータに刷りが 1 件も無いものを数え、
「一致した N 件のカードを表示できません」として画面に出す
（`loveca-ui/lib/src/state/search_degradation.dart` の `SearchMissingCards`）。

#### ★ 「潜在的」ではなくなった

★★**この経路が実在することを M3 で機械的に確かめた。**★★
`loveca-ui/test/data/card_search_degradation_test.dart` が実 DB に対して
`deleteExpansion` を呼んで**実際に孤児を作り**、検索がその cardNumber を返すこと、
および画面に出る値が立つことを固定している。**出ない側（孤児が無ければ出ないこと）も対で固定した。**
片方だけだと「常に出す」実装でも通ってしまう。

★配信から商品が消えたことがまだ無い、という事実は変わらない。
変わったのは「起きたら気づけるか」であって、「起きるか」ではない。

---

### D-9. `DeckDao.softDelete` が `revision` を上げない【Phase 4 で判断】

**見つけた経緯**: M2（`DeckRepository` の書き込み）で論理削除を通したとき。

`decks.revision` は `loveca-db/lib/src/schema/tables.dart:255` で
**「★更新のたびに +1（P2）。同期の差分検出に使う」**と定められている。
ところが `DeckDao.softDelete`（`loveca-db/lib/src/dao/deck_dao.dart:121-128`）は
`deletedAt` と `updatedAt` だけを書き、**`revision` に触れない。**

```dart
await (db.update(db.decks)..where((d) => d.deckId.equals(deckId))).write(
  DecksCompanion(
    deletedAt: Value(_utc(at)),
    updatedAt: Value(_utc(at)),
  ),                       // ★revision が無い
);
```

`DeckDao.save` は `revision` をそのまま書く形なので、**`revision` を管理するのは
呼び出し側の責務**である（`docs/UI設計メモ.md` §9）。
その前提だと、`softDelete` だけが呼び出し側を通らずに `decks` を書き換えており、
**「更新したのに revision が動かない唯一の経路」**になっている。

| 実測（M2 の実機確認 / 2026-08-24） | |
|---|---:|
| 作成直後 | `revision` 0 |
| 名前を変えて保存 | `revision` **1** |
| 論理削除 | `revision` **1 のまま**（`deletedAt` と `updatedAt` だけ動く） |

**影響はまだ出ていない。** Phase 4 の同期が未実装で、`revision` を読む本番の
利用者がいないため。`updatedAt` は動くので、突き合わせ方によっては拾える。

★**本タスク（M2）では直さない。** 理由は 2 つ。

1. 直す先は `loveca_db` であり、M2 の禁止事項（`loveca_core` / `loveca_db` を変更しない）
2. UI 側で `save()` に迂回して回避すると、**DAO の意図と二重になる。**
   `softDelete` という専用 API がありながら使わない形は、
   「どちらが正か」が曖昧になる点で D49 が案A を却下したのと同じ悪さである

★**現状の挙動はテストで見える形にしてある**
（`loveca-ui/test/data/deck_repository_test.dart` の
「★revision は上がらない（D-9 として記録済み。挙動をここで固定する）」）。
黙って抱えると、Phase 4 で `revision` を信じたときに**差分が出ない削除**として現れる。

**選択肢**

| 案 | 内容 | 代償 |
|---|---|---|
| (a) | `softDelete(deckId, at, revision)` にして呼び出し側から渡す | `save` と同じ形になり一貫する。`loveca_db` の API 変更 |
| (b) | `softDelete` の中で `revision = revision + 1` を SQL で書く | 呼び出し側が要らない。★「層の内側で状態を決めない」方針と食い違う |
| (c) | 同期を `updatedAt` だけで行い `revision` を使わない | P2 の記述と `tables.dart` のコメントを直す必要がある |

★**判断は Phase 4（同期の設計時）に置く。** `revision` と `updatedAt` の
どちらを差分検出の正にするかが決まらないと、(a)〜(c) を選べない。
**D-5（`P1`〜`P5` の参照先が存在しない）と同時に見ること。**
P2 の原文が読めれば「revision は何のためか」が確定する。

---

### D-10. `.gitignore` が本実装を隠していた【★2026-08-24 解消 / 予防は未決】

**A-3 の系譜だが、A-3 より悪い。** A-3 は「痕跡を残さずデータを落とす」だった。
これは**落としたうえで、すべての検出手段が正常を報告する。**

#### 何が起きたか

`.gitignore:7` は取得済みカードデータ（700MB弱）を除くために `data/` と書いてあった。
**先頭に `/` が無いため、どの階層の `data/` にも一致する。**

| 巻き込まれたもの | 件数 |
|---|---:|
| `loveca-ui/lib/src/data/`（M1 のリポジトリ層） | **8 ファイル** |
| `loveca-ui/test/data/`（同テスト） | **4 ファイル** |

M1 は「完了」としてコミットされたが、**クローン直後は `app.dart` が `import` する
`data/app_database.dart` から先が存在せず、ビルドできない状態だった。**

#### ★★ この事故の悪質さ — 検出手段がすべて正常を報告する ★★

**無視されたファイルは `git status` に出ない。** これが核心である。

| 手段 | そのとき何と言うか | 実際 |
|---|---|---|
| `git status` | **clean** | 12 ファイルが未追跡のまま |
| `flutter test` | **97 件 全通過** | 手元にファイルがあるので通る |
| `flutter analyze` | **No issues found** | 同上 |
| 実データで起動 | **`[boot] ok`** | 同上 |
| `git log` / `git show --stat` | 変更が入っているように見える | 隠れたファイルは差分に現れない |

★★**手元では何一つ異常が無い。異常はクローンした先にしか現れない。**★★
CLAUDE.md §3 が「skip が埋もれると検証しているつもりで検証していない状態になる」と
言っているのと同じ構造で、しかも**skip の件数のような手掛かりすら出ない。**

D-2（`loveca-core` にリントが効いていない）や D-6（移行機構が一度も走っていない）は
「仕組みを置いたが働くことを誰も確かめていない」だった。
D-10 は**仕組みが働いていることを確かめても気づけない。**確かめる場所が違うからである。

#### 解消（2026-08-24）

`data/` を **`/loveca-data/data/`** に絞り、隠れていた 12 ファイルをコミットした
（`0dd36f4`）。取得済みカードデータは `loveca-data/.gitignore` が独自に持つ規則で
引き続き無視される（確認済み）。

#### 全数確認（2026-08-24）

無視されているファイル **13,763 件を全数分類**し、事故に当たるものが他に無いことを確かめた。

| 分類 | 件数 |
|---|---:|
| 生成物・依存（`.dart_tool` / `build` / `.venv` / `__pycache__` / `ephemeral` / `spike/.cache`） | 1,747 |
| 実データ `loveca-data/data/`（意図どおり） | 12,008 |
| 公式 PDF（D-C） | 1 |
| IDE / ローカル設定 | 7 |
| ★**分類不能（＝事故の疑い）** | **0** |

**クローン直後にビルドできることを機械的に確認した**（一時ディレクトリへ `git clone`）。

| パッケージ | 結果 |
|---|---|
| `loveca-data` | 33 件 全通過（3 + 30） |
| `loveca-core` | 255 件 全通過 / `dart analyze` は既存の警告 1 件（`test/game_types_test.dart:567` の `equal_elements_in_set`。本タスク由来ではない） |
| `loveca-db` | 118 件通過 + ★**skip 9 件**（すべて実配信データ依存。`data/` はクローンに含まれないため想定どおり） |
| `loveca-ui` | **97 件 全通過 / skip 0** / `flutter analyze` clean |

#### ★ 予防と検知（2026-08-24 に (a)(b) 両方を適用）

解消の時点で、**同じ形の規則が 2 つ残っていた。** `git check-ignore -v` の実測。

| 規則 | 飲み込む例 | 危うさ |
|---|---|---|
| `.gitignore:21` **`build/`** | `loveca-core/lib/src/rules/build/` / `loveca-ui/lib/src/ui/deck/build/` | ★**高い。**このプロジェクトは「デッキ**構築**」を扱うので `build` はソース名として現実的 |
| `loveca-data/.gitignore:4` **`data/`** | `loveca-data/loveca_data/data/` | 中 |

★`loveca-ui/.gitignore:33` は `/build/` とアンカーされており問題なかった。
root の `build/` が効いていたのは `loveca-core` / `loveca-db` に対してだけだが、
両者は `dart` なので `build/` を生成しない（生成物は `.dart_tool/` 配下）。

**検討した手当て**

| 案 | 内容 | 費用 | 効き方 |
|---|---|---|---|
| **(a)** | **規則をすべてアンカーする**（`build/` → 削除または `/loveca-ui/build/`、`data/` → `/data/`） | ほぼ 0 | ★**検知ではなく、事故を起こせなくする。**「漏れうる構造を作らない」（`docs/UI設計メモ.md` §4-3）と同じ考え方 |
| **(b)** | CLAUDE.md §3 に検証コマンドを 1 行足す | 0 | 走らせた人にしか効かない。★**D-2 の前例があり、手動の見張りは忘れられる** |
| **(c)** | `flutter test` の中に置く | 40 行程度 | 自動で走る。★ただしリポジトリ全体の話を 1 パッケージのテストに入れることになり、`git` への依存か `../.gitignore` の読み取りが要る |
| **(d)** | pre-commit フック | 小 | ★**クローン直後に設定が要る**——忘れられる対象がまさにクローン時なので相性が悪い |

**採った手当て: (a) を本命に、(b) を受けに置いた（両方適用）。**

**(a) の結果**（`2d8e4a2`）: 4 つの `.gitignore` のディレクトリ規則をすべて
`/dir/`（1 箇所）か `**/dir/`（意図した再帰）に直し、root の `build/` は削除した。
**素の `dir/` を書かない**という取り決めを root の `.gitignore` 冒頭に理由ごと置いた。
★**素の `dir/` だけが「意図か事故か」判別できない**のが問題の本体である。

★**意図した除外が生き残っていることは、件数ではなく集合で確かめた。**
無視ファイルは前後とも 13,763 件で **diff 0 行**、`loveca-data/data/` の 12,008 件も不変
（外れると 700MB がコミット対象になる）。
アンカー前に飲み込まれていた 3 箇所に新規ファイルを置くと、いずれも `git status` に
`??` として現れることも確かめた。

**(b) の結果**: CLAUDE.md §3 に検証コマンドを置いた。
★**(b) は単独では弱い。** D-2（`loveca-core` にリントが 1 つも効いていなかった）が
前例で、走らせる人がいなければ何も検知しない。**(a) が本命である。**

**検証済みの (b) の中身**（0 件であることを確かめ、模擬事故で検知できることも確かめた）

```bash
git ls-files --others --ignored --exclude-standard \
  | grep -vE "^(loveca-(core|db|ui)/\.dart_tool/|loveca-ui/build/|loveca-ui/windows/flutter/ephemeral/|loveca-ui/spike/\.cache/|loveca-data/\.venv/|loveca-data/data/|\.claude/)" \
  | grep -vE "(^|/)__pycache__/" \
  | grep -E "\.(dart|py|md|ya?ml|json)$"
```

#### ★★ 検知手段自身が同じ罠を踏んだ ★★

**この見張りを書く過程で、2 回、探す側が同じ穴に落ちた。**記録しておく。

**(1) 除外を「ディレクトリ名」で書いて、警戒対象を一緒に見逃した**

最初の版はこう書いていた。

```
| grep -vE "(^|/)(\.dart_tool|build|\.venv|__pycache__|\.idea|ephemeral|\.cache)/"
```

`build` を**名前**で除外している。正当な `loveca-ui/build/` を外すつもりだったが、
**`loveca-core/lib/src/rules/build/` も同時に外していた。**
つまり**この見張りが最も警戒すべき対象を、見張り自身が除外していた。**
模擬事故を流しても 0 件を返し、**正常だと報告した。**

→ **除外はパスで書く。**`^loveca-ui/build/` なら `lib/src/rules/build/` は残る。

★★**これは `.gitignore` の `data/` とまったく同じ間違いである。**★★
「名前で書くと、意図していない階層にも当たる」——
**事故の機構を理解して見張りを書いたのに、見張りを同じ機構で書いていた。**

**(2) 模擬が再現せず、「検知できた」と誤認しかけた**

規則を未アンカーに戻して `git status` を見たが、**何も起きなかった。**
**すでに追跡されているファイルは `.gitignore` の影響を受けない**ためである。
`git ls-files --others` は未追跡ファイルしか見ないので、当然だった。

→ **模擬は「新規の未追跡ファイル」で作る。**
飲み込まれるはずの場所に新しいファイルを置き、`git status` に出ないことを見る。

★この (2) は**見張りの仕様として正しい**（事故の本体は「新しいファイルが
一度も追跡されないまま消える」ことなので、未追跡だけ見れば足りる）。
危ないのは**それを知らずに模擬して「検知できた」と誤認する**ほうである。

#### ★ D-6 と同型である — 探す側が同じ穴を持つ

D-6 の「同種の穴の洗い出し — 手法の記録」に、まったく同じ形の記述がある。

> ★**観点 5 は今回、自分のテスト自身に見つかった**（`boot_gate_test` の `_pump` が
> Notice を描かない builder を使っていた）。**探す側が同じ穴を持つ。**

| | D-6 | D-10 |
|---|---|---|
| 探していた穴 | 「作られたことしか見ていないテスト」 | 「名前で書いた規則が意図しない階層に当たる」 |
| 探す側が踏んだ穴 | 洗い出しに使ったテスト自身が、Notice が**画面まで届くか**を見ていなかった | 見張りの除外を**名前**で書き、警戒対象を除外していた |
| 気づき方 | 自分の書いたテストを読み返した | 模擬事故を流したら 0 件だった |
| 共通する性質 | ★**探す道具が対象と同じ材料でできている**ので、同じ癖がそのまま入り込む | 同上 |

★★**結論: 検知手段を書いたら、必ず「見つかるはずのもの」を仕込んで動かすこと。**★★
0 件という結果は「無い」と「見えていない」の区別がつかない。
D-6 / D-10 のどちらも、**動かして初めて見張り自身の欠陥が出た。**
これは `docs/UI設計メモ.md` §3-4 の「『空』と『失敗』を同じ型で表さない」の、
道具側での言い換えである。

★**D-6 側からもここを参照している**（同節の末尾）。

---

### D-11. デッキの並び順が保存されず、取得経路で並びが違う【★2026-08-24 方式決定（D65）/ 実装は `loveca_db` を次に触るとき】

★★**M4（R3 デッキ編集）の着手時に判明。設計メモにも本書にも書かれていなかった。**★★

#### 事実

| # | 事実 | 出典 |
|---|---|---|
| 1 | `deck_entries` に**順序列が無い**。主キーは `{deckId, printingId}` | `loveca-db/lib/src/schema/tables.dart:289-297` |
| 2 | `DeckDao.byId` は entries を `ORDER BY printing_id` で読む | `loveca-db/lib/src/dao/deck_dao.dart:147` |
| 3 | ★**`DeckDao.all` は entries に `ORDER BY` を持たない** | 同 `:171-175` |
| 4 | R2（デッキ一覧）は `all()` が返した `Deck` をそのまま R3 へ渡す | `loveca-ui/lib/src/ui/deck/deck_list_page.dart:81` |

**帰結は 2 つ。**

- **並べ替えても保存されない。** `DeckDao.save` は `deck.entries` の順で書くが、読み戻しで並べ替えられる
- ★**同じデッキでも取得経路で並びが違いうる。** 2 と 3 が食い違っており、
  `all()` 経由は SQLite の内部順（実質は挿入順）に見えるだけで**保証が無い**

#### なぜ「無言の欠落」の型に近いか

利用者から見ると「並べ替えて保存したのに、開き直すと戻っている」。
**操作が受け付けられたように見えて結果が残らない**という点で A-3 と同じ気味の悪さがある。
ただし**データは失われない**（枚数と刷りは残る）ので、A-3 より軽い。

| | A-3 | D-11 |
|---|---|---|
| 失われるもの | カードの情報そのもの | **並び順だけ** |
| 可逆性 | 不可逆（再取り込みが要る） | 可逆（並べ直せる） |
| 気づけるか | 気づけない | ★**M4 が予告するので気づける** |

#### M4 の手当て（`loveca_db` を変更せずにできること）

1. 開いた直後の並びを**正規化**する（区分順 → `printingId` 昇順）。取得経路の差を画面に持ち込まない
2. 並べ替えたら ★**「並び順はこの画面の中だけです。開き直すとカード番号順に戻ります。」**と出す
3. 並べ替えでは**保存ボタンを光らせない**（押せると「保存したのに戻る」になる）

★**予告の文面が実際と一致していること**が肝で、そのために 1 の正規化規則を
`byId` の並び（`printing_id` 昇順）に合わせてある。

#### 根治

**決定 D65** に方式（`schemaVersion` 2→3 / `ord` 列 / backfill SQL / `all` の `ORDER BY` 追加まで）を
書いてある。**そのとき同じ検討を一からやり直さないための記録である。**
実施時期は **`loveca_db` を次に触るとき**（D-8 / 決定 D63 と同じ時点が自然）。

---

### D-12. `DeckValidator.hasUnknownCards` の doc が UI の編集ポリシーを domain 層に書いている【★2026-08-24 判断済み・2026-08-25 解消】

#### 事実

```dart
/// 未知カードを含む場合、編集は禁止し読み取り専用で表示する。
bool get hasUnknownCards => unknownPrintingIds.isNotEmpty;
```
（`loveca-core/lib/src/rules/deck_validator.dart:82-83`）

M4 は**この記述を採らなかった**（未知の刷りの行だけを読み取り専用にし、デッキ全体は編集できる）。
★**「範囲を超えるので採らない」では、どちらが正か決めないまま実装を進めることになる。**
そこで**どちらが正かを決めた。**

#### 判断: ★**UI の側が正であり、この記述が過剰である**

根拠は 4 つ。

| # | 根拠 |
|---|---|
| 1 | ★**総合ルールの条番号が無い。** 同じファイルの他の制約（6.1.1.1 / 6.1.1.2 / 6.1.1.3）はすべて条番号つきで、ここだけ根拠が無い。CLAUDE.md §1「根拠のない数値・条件をコードに書かない」に反する |
| 2 | ★**presentation の方針を domain 層に書いている。** `loveca_core` は PC・スマホ・**サーバ**が共有する層で、サーバに「編集画面」は無い。「読み取り専用で表示する」はサーバでは意味を持たない |
| 3 | ★**どの実装もこれを満たしたことがない。** `hasUnknownCards` の呼び出し元は grep して **UI の表示 1 箇所とテストのみ**で、編集を禁じるコードは `loveca_core` にも `loveca_db` にも無い。**守られていない記述**である |
| 4 | ★**決定 D35 が要求しているのは「黙って削除しない」であって「編集させない」ではない。** M4 は未知の刷りを**ドラフトに保持したまま保存で書き戻す**（`test/data/deck_repository_test.dart` が固定）ので、D35 の目的は満たしている。デッキ全体の凍結は、利用者が**デッキを直す手段まで奪う**分だけ過剰 |

#### 直す先と時期

`loveca_core` の**コメントの文言**（「編集は禁止し読み取り専用で表示する」→
「未知の刷りは**黙って削除してはならない**。表示は呼び出し側の判断」）。
★M4 は「`loveca_core` / `loveca_db` を変更しない」制約のもとで作業したので触っていなかった。

★★**2026-08-25 に解消した**（決定 **D91** と同じ機会に `loveca_core` を触ったため）。★★
外した理由（上の根拠 4 つ）も一緒にコメントに残してある。
**同じ断定が書き戻されないように、なぜ過剰なのかがその場で読める。**

**時期: `loveca_core` を次に触るとき**（最も早いのは D-2 の `analysis_options.yaml` 追加 / Phase 3b）。

---
### D-13. 直った取り込み失敗が「未解消」のまま永久に残る【★2026-08-24 発見 / 方式決定済み / 実装は `loveca_db` を次に触るとき】

**見つけた経緯**: M6（`import_issues` の出口を作ったとき）。
実 DB に対して `MasterImporter` を走らせるテストを書いていて、
「直したのに件数が戻らない」経路に当たった。
★**その後、実データ + 実機の DB でも再現を確認した**（下記）。

#### 何が起きるか

`MasterStateDao` の「未解消」は `master_state_dao.dart:127-131` で判定する。

```sql
NOT EXISTS (SELECT 1 FROM master_files f
            WHERE f.path = import_issues.path AND f.hash = import_issues.hash)
```

| テーブル | 主キー | 持っているもの |
|---|---|---|
| `master_files` | **`{path}`** | その path の**現在のハッシュ 1 件だけ** |
| `import_issues` | `{path, hash}` | 失敗した**そのときの**ハッシュ |

→ 配信側がファイルを直すと**内容が変わるのでハッシュも変わる。**
取り込みに成功しても `NOT EXISTS` は古いハッシュを探し続けるので、
**記録は永久に未解消のまま残り、消す API も無い**（`delete` は 1 箇所も無い）。

★決定 D39 が「取り込みに成功した時点で自動的に未解消から外れる」と書いたのは、
**同じハッシュで再取得が成功する場合（一時的な読み取り失敗）にしか当てはまらない。**
`loveca-db/test/master_importer_test.dart:298-319` が 0→1→0 を通しているのは、
**直した内容を同じハッシュ文字列で publish しているから**である。

#### 実データでの確認（2026-08-24）

dist を scratch に写して `cards/BP01.json` の `"BLUE"` を `"CYAN"` にし、
`LOVECA_DIST_DIR` で起動した。

| 見たもの | 値 |
|---|---|
| `import_issues` | `cards/BP01.json` / `unknownKey` / `Invalid argument(s): unknown heart color: CYAN` |
| 未解消の判定 | **1** |
| `master_files` の同 path のハッシュ | ★**別のハッシュ**（前回成功した版） |
| `master_states.data_version` | **2 のまま**（D39 の据え置きは効いている） |
| `decks` / `cards` | **2 / 1,708 とも無事**（D39 の「既存の行を消さない」は効いている） |

★**壊れているのは「未解消」の判定だけ**であり、隔離そのものは設計どおり働いている。

#### 当座の手当て（M6 / `loveca_db` は変更しない）

`MasterRepository.outstandingImportIssues()` が `master_files` の現在ハッシュを
一緒に引き、`ImportIssue.supersededByNewerFile` を立てる。
R6 は**「このファイルは、そのあと別の版で取り込めています」**と添える。

★**「いま壊れている」と「壊れた記録が残っている」を混ぜない。**
混ぜると「ずっと壊れている」と誤解され、**警告そのものが無視されるようになる。**

#### ★★ 根治の方式（いま決めておく。D63 / D65 と同じ格）★★

★**「次に `loveca_db` を触るとき」とだけ書くと、そのとき同じ検討を一からやり直すことになる。**

| 案 | 採否 | 理由 |
|---|---|---|
| **(a) 取り込み成功時に同じ path の行を消す（採用）** | ○ | 下記 |
| (b) `resolvedAt` 列を足して履歴として残す | ✗ | `import_issues` は「いま何が読めないか」を出すための表であって履歴ではない。**列と画面の両方が増える** |
| (c) 判定を `path` だけの `NOT EXISTS` にする | ✗ | ★**逆方向に壊れる。** v1 で成功 → v2 で失敗、のとき「解消済み」に見えて**本物の失敗を隠す** |

**採用する方式**

| 項目 | 決めた内容 |
|---|---|
| 変更点 | `MasterStateDao.recordFile(file, at)` の中で `DELETE FROM import_issues WHERE path = ?` を同じトランザクションで実行する |
| 版 | ★**`schemaVersion` は上げない。** 列も索引も変えないので移行が要らない（D65 と違い**ユーザデータに触らない**） |
| 意味 | 「そのファイルが**いまの版で読めた**なら、そのファイルについての過去の失敗は未解消ではない」 |
| 失敗の記録 | ★**捨てても困らない。** 1 回ごとの失敗は起動 Notice が出しており、繰り返しは `occurrenceCount` が持っている |
| 再発 | また壊れれば**新しい行が入る**（`firstSeenAt` も新しくなる）。これは正しい——別の版の別の失敗である |
| 既存テスト | `master_importer_test.dart:298-319`（同じハッシュで 0→1→0）は**そのまま通る** |
| 足すテスト | ★**「別のハッシュで直すと 0 件に戻る」**を足すこと。いま無いのがこの穴の原因である |
| UI 側 | `ImportIssue.supersededByNewerFile` は**不要になる**（消してよい。残っていても害はない） |

★**実施は `loveca_db` を次に触るとき**（D-8 / D-11 と同じ時点が自然）。

---

### D-14. `Deck.copyWith` の既定値違反は UI から到達しなくなったが、本体は未修正【★2026-08-24 記録 / 修正は Phase 4】

CLAUDE.md §1 が既知違反として挙げているのはこれである。

```dart
updatedAt: updatedAt ?? DateTime.now().toUtc(),   // loveca-core/.../deck.dart:96
```

M6 の**決定 D70** で `DeckRepository.save` を明示コンストラクタに変えた結果、
★**`Deck.copyWith` の呼び出し元が `loveca-ui/lib` から 0 件になった。**

```bash
grep -rn "\.copyWith(" loveca-ui/lib | grep -v TextStyle   # Deck.copyWith は出ない
```

★★**これを「解消」と読まないこと。**★★

| 事実 | |
|---|---|
| UI から踏む経路 | **無くなった** |
| `loveca_core` の実装 | ★**変えていない**（変更禁止） |
| 将来踏みうる先 | Phase 6 の権威サーバ / Phase 4 の同期 / `loveca-core` 自身のテスト |

**D-9（`softDelete` が `revision` を上げない）/ D-5（`P1`〜`P5` の参照先不在）と同じ扱い**——
**「片側は塞いだが本体は残っている」**を明示しておく。
解消済みと誤読されると、Phase 4 で見落とす。

★修正の向きは CLAUDE.md §1 が既に定めている:
**「Phase 4（同期）の設計時に呼び出し側から渡す形へ直す」。**

---

### D-15. doc の「列挙による断定」が実装より古い【★2026-08-24 発見・同日 (a)〜(d) を解消 / ★2026-08-25 に (e)〜(h) を発見・同日解消 / ★★**(h) は向きが逆の新型**★★ / ★2026-08-26 に **(i)** を発見・同日解消（★★**(i) は「方針の誤り」でさらに別の型**★★） / ★2026-08-26 に **(j)** を発見・同日解消（★★**(j) は「検証していない完了報告」で、実機で押すまで出ない型**★★） / ★2026-08-26 に **(k)** を発見・同日解消（★★**(k) は「実行できない確認予定」で、(h) の亜種**★★） / ★2026-08-27 に **(l)** を発見・同日解消（★★**(l) は「未実装の決定の前提が、別の決定に動かされていた」型**★★）】

> ## ★★ (a)〜(d) の 4 件は 2026-08-24 に解消した（Phase 3b / M-B1）★★
>
> | 件 | 直した時期 | 結果 |
> |---|---|---|
> | (a)(b) | ★**D73 の実装の直前**（別コミット） | 「3 つ」→「4 つ」（`AdvanceStep` を追加）。D73 が 5 つ目にする**前**に直したので、**「元から誤っていた」ことが履歴に残る** |
> | (c)(d) | M-B1（`loveca-ui/lib` を触ったとき） | 0 箇所であることを grep で確かめてから訂正。`deck_repository.dart` は 6 行目と 445 行目を同時に見た |
>
> ★★**コメントを直しただけにしていない。**★★ (a)(b) は
> `AdvanceStep` が実際に乱数を消費することを `step_engine_test.dart` で固定した
> （同じ盤面に seed だけ変えてメインデッキの並びが変わることを見る。
> 対として同じ seed なら同じであることと、手札まで含めた集合が変わらないことも置いた）。
>
> ★★**§12-4「一致していたもの」のうち 1 つが古くなった**★★——
> 「UI 層で乱数を引くのは `deck_id.dart` 1 箇所」は、M-B1 で
> **`state/board_seed.dart` が 2 箇所目**になった（seed / 6.2.1.4 の無作為）。
> ★**引く場所を増やさないため 1 ファイルに閉じてある**が、**数は 2 である**。
> **この行自体が D-15 の実例になったので、ここに書いておく。**
>
> ★★**2026-08-25 追記: この予告は「予告」のまま 1 日置かれ、`deck_id.dart:30` は
> 「唯一の場所」のまま残っていた。**★★ 下の **(e)** に経緯と処遇を書いた。
> ★**予告は修正ではない。**

#### 何が起きているか

このリポジトリの doc は、**閉じた集合を数え上げる断定**を多用している。

> 「参照透過でないのは乱数を消費する **3 つだけ**」
> 「`Deck.copyWith` を呼ぶのは `save` **1 箇所だけ**である」
> 「`Image` ウィジェットを作る**唯一の場所**」

★**この形は強力だが、実装が増えたときに黙って古くなる。**
`flutter analyze` も `dart test` も検知しない。**次に読む人はその数が古いことを知らない。**

`docs/盤面設計メモ.md` §2 の作業中に `reduce.dart` の断定が実装と食い違っていることに気づき、
**D-6 の「同種の穴の洗い出し — 手法の記録」と同じ走査を doc の断定に対して行った。**

#### 走査

```bash
for p in loveca-core loveca-db loveca-ui; do
  echo "=== $p ==="
  grep -rnE "^\s*(///|//).*((唯一)|([0-9０-９]+\s*(箇所|つ|個|本|件|種|列|通り)\s*(だけ|のみ|ちょうど))|(だけ|のみ)が)" \
    $p/lib --include=*.dart | grep -v "\.g\.dart"
done
```

出た候補を 1 件ずつ**現物と突き合わせる**（grep で数える / enum の値を数える）。

#### ★ 古くなっていたもの（4 件）

| # | 場所 | 断定 | 現状 |
|---|---|---|---|
| **a** | `loveca-core/lib/src/game/reduce.dart:24` | 「参照透過でないのは乱数を消費する **3 つだけ**（`ShuffleZone` / `Refresh` / `DrawCards`）」 | ★**`AdvanceStep` が抜けている** |
| **b** | `loveca-core/lib/src/game/game_action.dart:177` | 「このアクション（`ShuffleZone`）**だけ**が乱数を消費する」 | 同じ穴 |
| **c** | `loveca-ui/lib/src/data/deck_repository.dart:6` | 「`Deck.copyWith` を呼ぶのは `save` **1 箇所だけ**である」 | ★**0 箇所**（決定 D70） |
| **d** | `loveca-ui/lib/src/state/deck_edit_store.dart:250` | 「ここが `Deck.copyWith` を踏む**唯一の入口**」 | 同上。踏んでいない |

★おまけに `docs/決定事項一覧.md` §3 の「**D36〜D71 は使用済み**。次は D72」も、
D72 を採番したときに更新されず残っていた（`CLAUDE.md` §8 だけが更新されていた）。
**同じ数を 2 箇所に書けば必ず食い違う。**→ 正をこの行に定めたうえで D79 まで更新した。

#### ★★ (a)(b) は「これから 4 つ目が増えるから直す」のではない。元から違っていた ★★

`AdvanceStep` は **7.6.2**（`_draw`）と **8.3.11**（`_yell`）で
`Refresher.takeFromMainDeck` を通り、メインデッキが尽きると
**10.2.1 の割り込みリフレッシュ（10.2.3 のシャッフル）で乱数を消費する。**

| いつ書かれたか | 何 |
|---|---|
| **3a-3** | `step_engine.dart` の 7.6.2 / 8.3.11、`refresh.dart` |
| **3a-4** | `reduce.dart` / `game_action.dart` の**この断定** |

→ ★★**断定のほうが後に書かれており、書かれた時点で既に実装と食い違っていた。**★★

**「決定 D73（エネルギーの無作為抽出）で 4 つ目が増えるから直す」と書いてはいけない。**
そう書くと**いつから誤っていたかが失われる。**
D73 はこの誤りを**増やす**が、**作った**のではない。

**実測で裏を取った**（`loveca-core` の `package_config.json` を指した使い捨てスクリプト。
★`loveca_core` は 1 文字も変えていない）。

| # | 検証 | 結果 |
|---|---|---|
| 1 | `AdvanceStep`（カーソル 7.6.2 / メインデッキ空 / 控え室 4 枚）で割り込みリフレッシュが起きる | ○ `refreshCount = 1` |
| 2 | ★seed を変えると `AdvanceStep` 後のメインデッキの並びが変わる（＝ 乱数を消費している） | ○ `w2,w4,w1` / `w1,w4,w2` |

#### ★★ (c)(d) は 1 ファイルの中で矛盾している ★★

`loveca-ui/lib/src/data/deck_repository.dart` の**同じファイル**が、

| 行 | 書いていること |
|---:|---|
| **6** | 「★★ `Deck.copyWith` を呼ぶのは `DeckRepository.save` **1 箇所だけ**である ★★」 |
| **445** | 「★副次的に **`Deck.copyWith` の呼び出し元が UI から 0 件になり**、CLAUDE.md §1 の既知違反に触れる経路そのものが消える」 |

**正反対のことを書いている。**
★★**片方だけ読んだ人が誤る。**★★ 6 行目はファイル頭の library doc なので**先に読まれる。**
直すときは**必ず 2 箇所を同時に見ること。**

#### 一致していたもの（確認済み）

`Zone` 11 種ちょうど / リーフフェイズ 12 個ちょうど / `stepGraph` の分岐 2 箇所（8.3.6・8.4.12）/
`firstPlayerId` の書き換え 1 箇所 / `Image(` 1 箇所 / `FileImage`+`ResizeImage` 1 箇所 /
`Random.secure()` 1 箇所（★**M-B1 で 2 箇所になった**。上記） / `DateTime.now()` 1 箇所 / `path_provider` 1 箇所 /
`Draggable`・`DragTarget` 1 ファイル / `840` の定義 1 箇所 / `LOVECA_SEARCH_LIMIT` 1 箇所 /
`revision + 1` は UI 側 1 箇所 / カードの比の定数 1 箇所 / `dart:io` は `native.dart` と `src/native/` のみ

#### ★★ 検知手段自身が同じ罠を踏んだ（D-10 / D-6 と同型）★★

最初の走査を **`Random(`** と **`Image(`** で書いたため、
**`Random.secure()` と `Image.asset(` 系を 1 件も拾えなかった。**
どちらも **0 件**を返し、**「無い」と読みかけた。**

```bash
# ★直したあと
grep -rnE "Random(\.secure)?\(" loveca-ui/lib --include=*.dart
grep -rnE "\bImage\.(asset|file|network|memory)\(|\bImage\(" loveca-ui/lib --include=*.dart
```

- **D-10**: `.gitignore` の見張りが、警戒対象の `lib/src/rules/build/` を**自分で除外していた**
- **D-6**: 外部キーの cascade を「未検証」と誤判定した。**テスト名が日本語**で英語の grep に掛からなかった
- **D-15（今回）**: **コンストラクタの書き方**が grep から漏れた

★★**探す道具は対象と同じ材料でできているので、同じ癖がそのまま入り込む。**★★
**「〜のみ」と結論する前に、探す語の書き方そのものを疑うこと。**
**0 件は「無い」と「見えていない」の区別がつかない。**

#### 処遇

| 件 | 直す時期 |
|---|---|
| (a)(b) | **決定 D73 の実装と同時**（`loveca_core` を触るとき） |
| (c)(d) | **`loveca-ui/lib` を次に触るとき**（＝ Phase 3b の M-B1） |

★**直すのはコメントの文言だけで、挙動は変わらない**（**D-12** と同じ扱い）。
★**今回は 1 件も直していない。**`docs/盤面設計メモ.md` は設計のみで、
`loveca_core` / `loveca-ui/lib` を変更しない範囲の作業だった。

#### ★ 再発をどう防ぐか — 見張りは置かない

★★**手動の見張りは忘れられる**（CLAUDE.md §3 が **D-2** を前例として明記している）。★★
上の grep を「定期的に流す」規約にしても、流す人がいなければ何も検知しない。

→ **断定を書くときの規約を先に置く。**

| 規約 | 理由 |
|---|---|
| ★**数を書くなら、その数を機械が数えられる形にする** | 「`Zone` は 11 種ちょうど」は enum の値を数えれば分かる。「乱数を消費するのは 3 つ」は**数える手段が無い**（間接的な消費が含まれるため） |
| ★**数える手段が無い断定は、テストで固定するか、書かない** | (a)(b) はまさにこれ。★決定 D73 の実装時に「乱数を消費するアクションの集合」をテストで固定する |
| ★**同じ数を 2 箇所に書かない** | `docs/決定事項一覧.md` §3 と `CLAUDE.md` §8 で「次の D 番号」が食い違ったのがその実例 |

#### ★★ (e) 予告されていたのに直されなかった（2026-08-25 発見・同日解消）★★

**`loveca-ui/lib/src/data/deck_id.dart:30`「★UI 層で乱数を引く唯一の場所。」**
★**書かれた時点では正しかった。**2026-08-24 の走査も「一致していたもの」に数えている。

| いつ | 何が起きたか |
|---|---|
| 2026-08-24 | D-15 の走査。`Random.secure()` 1 件を確認し「一致していたもの」に入れた |
| **同日（M-B1）** | `state/board_seed.dart` が新設され **2 箇所**になった |
| 同日 | ★★**このページ冒頭の引用ブロックと `CLAUDE.md` §8 の D-15 行が「2 箇所になった」と予告した**★★ |
| 2026-08-25（M-B2 / M-B3 / M-B4） | ★**`deck_id.dart:30` は一度も直されなかった** |
| **2026-08-25** | ★「唯一の場所」と「2 箇所目」が同じリポジトリに並んでいることを発見・訂正 |

★★**予告は修正ではない。**★★
(c)(d) は「**1 ファイルの中で**矛盾している」だったが、(e) は **2 ファイルにまたがる**ぶん悪い ——
`board_seed.dart` 側は最初から正しく「2 箇所に留める / もう 1 つは `deck_id.dart`」と書いており、
★**先に開いたほうで結論が変わる。**

#### ★★ (e) の処遇 —— 相互参照では解けない ★★

「2 箇所が相互に参照すれば、片方だけ直されても矛盾が残らないのでは」を検討したが、**解けない。**

★★**「もう 1 つは〜」は 2 という数の言い換えである。**★★
3 箇所目を足すと**両方が同時に偽になる。**
相互参照が買うのは「どちらから読んでも食い違いが見える」ことだけで、**古くなることは防げない。**
★実際、走査を広げたら **3 つ目の写し**（`docs/盤面設計メモ.md` §12-4 の
「UI 層で乱数を引くのは `deck_id.dart` 1 箇所」）が出てきた。
**2 者間の相互参照は最初から足りていなかった。**

→ ★★**数を持つのは正 1 箇所だけにする。**★★`docs/決定事項一覧.md` の D81 詳細に定めた。
コード側は「**ほかに〜がある**」に留めた（★**3 箇所目が増えても偽にならない**書き方）。
★**下の「再発をどう防ぐか」の規約「同じ数を 2 箇所に書かない」を、コードコメントに当てはめただけである。**

★★**根治は機械化だが、今回は入れていない。**★★
`loveca-ui/test/support/source_scan.dart`（★D-15 §12-5 の作法をそのまま実装したもの）を使えば
`test/board/reduce_call_site_test.dart` と同じ形に固定できる。
**入れなかったのは今回の作業範囲が「コメントと文書のみ」だったからで、方式が無いからではない。**
★**置くときは陽性対照を対にすること。**

#### ★★ (f) 同じ数を 2 箇所に書いた実例がまた出た（2026-08-25 発見・同日解消）★★

`CLAUDE.md` §8 の「残っているのは **5 行**」（コード・テストに残る `M-B4` / `M-B5` の行数）。
正である `docs/盤面設計メモ.md` §14-7 は **12 行**で正しく、
★★**そこには「数だけを書くと次に数えた人が『増えた』と誤読する」とまで書いてあった。**★★

→ ★**数を直さず、数を書くのをやめた。**
M-B4 で `CLAUDE.md` から「次の決定番号」そのものを消したのとまったく同じ対処である。
★★**「正はあちら」と書き添えるだけでは足りない。**★★
元の文にも `docs/盤面設計メモ.md` §14-7 への参照は**あった**。それでも古くなった。

#### ★★ (g) テスト件数の表がまた古かった（2026-08-25 発見・同日解消）★★

★**(e)(f) の確認のためにテストを流したら出てきた**（探しに行っていない）。

| パッケージ | `CLAUDE.md` §3 の表 | 実測 |
|---|---:|---:|
| `loveca-data` | 33 | 33 ○ |
| **`loveca-core`** | 293 | ★**307** |
| `loveca-db` | 127（skip 0） | 127（skip 0） ○ |
| **`loveca-ui`** | 590（skip 0） | ★**638**（skip 0） |

表は **M-B3 完了時**のもので、**M-B4 が足したテストで更新されなかった。**
★★**同じ表が 2026-08-25 に一度訂正されており（587 → 590）、
そのとき「テストを足したコミットで必ずここを直すこと」と書き足していた。**★★
→ ★**規約を書いても守られない。**(e) の「予告は修正ではない」と同じ形である。

★★**処遇が (f) と違う。ここは数を消せない。**★★

| | もう 1 つの側 | 採る手 |
|---|---|---|
| **(f)** `CLAUDE.md` §8 の `M-B4` の行数 | ★**文書**（`docs/盤面設計メモ.md` §14-7 が正） | ★**数を消す** |
| **(g)** `CLAUDE.md` §3 のテスト件数 | ★**コードそのもの**（`dart test` / `flutter test` が数える） | ★**消さずに直す**（この表が正） |

★**どちらの手を採るかは「もう 1 つの側が文書か、機械で数えられるものか」で決まる。**

#### ★★ (h) doc が実装より**広く**書かれていた（2026-08-25 発見・同日解消）★★

★★**(a)〜(g) と向きが逆である。初めての型。**★★

`docs/UI設計メモ.md` §4-2 の `CardListRow` 列表が `rarity` を
「表示・**絞り込み**」と書いていたが、**`CardListFilter` に `rarity` の条件は無い。**実装は表示のみ。

★**git で由来を確定した**（「実装が消えた」のか「最初から無かった」のかで型が変わるため）。

| | コミット | 日付 |
|---|---|---|
| doc の列表 | `2d78228`「決定 D59〜D61: M1 の着手条件を埋める」 | **2026-08-23** |
| 実装（`CardListRow` / `CardListFilter`） | `0dd36f4` | **2026-08-24** |
| `filter_panel.dart` に `rarity` が現れたこと | ★**一度も無い**（`git log -S'rarity'` が **0 件**） | — |

→ ★★**doc は「M1 の着手条件」＝設計意図として先に書かれ、実装は表示だけを作った。**★★
**実装が変わって doc が古くなったのではない。doc が最初から実装より広かった。**

★**同じ監査で 2 件目が出た。こちらは (a)〜(g) と同じ従来型である。**
`cardType` の記述が「絞り込み」のままだが、実装は決定 **D72**（`e9d2414` / 2026-08-24
「ライブカードを種別の比で描く」）で **`cardAspectRatioOf` による絵の枠**という用途が増えていた。
★**1 つの表の中に、両方の型が同居していた。**

##### ★★ 走査手法が届かない型である ★★

★**§12-2 の走査コマンドでは出ない。実測で確かめた。**

走査は `lib` の**コメント**（`docs/*.md` まで広げても同じ）から
「唯一」「N つだけ」のような**閉じた集合を数え上げる断定**を拾う形になっている。
`| rarity | String | 表示・絞り込み |` は**数を数えていない**ので、
★**走査の正規表現に 1 文字も当たらない**（当該行に走査を掛けて **0 件**であることを確認した）。

★★**より本質的な問題は、実装側に手がかりが無いことである。**★★

| | (a)〜(g)（従来型） | **(h)（新型）** |
|---|---|---|
| 何が食い違うか | doc の**数**と実装の**数** | doc の**能力の主張**と実装の**不在** |
| 実装側の手がかり | ★**ある。**数えれば出る（`grep` で件数、enum の値の数） | ★★**無い。`filter_panel.dart` に `rarity` が「無い」ことは、grep しても何も返さない**★★ |
| 見つけ方 | 実装を見て doc と比べる | ★**doc の主張を 1 つずつ実装に当たる**（今回は `CardListRow` の **9 列を 1 列ずつ**突き合わせた） |

★★**「無い」ことは走査できない。** 存在するものは数えられるが、
**書かれなかったコードは痕跡を残さない。**★★

##### ★ この型を検知する手段はあるか → ★★**汎用の手段は無い**★★

★**D-20 で `dart analyze` の限界を実測で示したのと同じ形で、ここでも「無い」と書く。**

| 案 | なぜ効かないか |
|---|---|
| §12-2 の走査を広げる | ★**当たらない**（上記で実測）。能力の主張には数量詞が無く、拾う手がかりが無い |
| doc の表に出てくる識別子を実装で `grep` する | ★**`rarity` は `lib` に**存在する**（`CardListRow` の列として）。「絞り込みに使われていない」ことは grep では分からない |
| 「絞り込み」と書かれた列が `CardListFilter` にあるか検査する | ★**この表 1 つには効く**が、doc の行と実装の記号を結ぶ形式的な対応が無いので**一般化できない。**表ごとに検査を書くことになる |

→ ★★**手段が無い以上、「見つけたら直す」しか残らない。**★★
**(h) は探しに行って見つけたのではなく、U21（詳細検索）の調査で
「どの軸が引けるか」を数えていて偶然当たった。**
★**(g) も同じ**（「(e)(f) の確認のためにテストを流したら出てきた」）。
★**この 2 件は「作業のついでに出た」という点で共通しており、
定期的な走査で出る類ではない。**

##### ★★ D-15 の見出しが D-15 の病を踏んでいた ★★

★**(h) を書くために見出しを読んで気づいた。**

この節の見出しは【★2026-08-24 発見・同日 (a)〜(d) を解消 / ★2026-08-25 に **(e)(f)** を発見・同日解消】
であり、★**その下に (g) が既に在るのに (g) が入っていなかった。**
`CLAUDE.md` §8 の D-15 の行も同じだった。

★★**列挙による断定が古くなることを記録している節の見出しが、
まさに列挙による断定で古くなっていた。**★★

★**これは `ルール整合性チェック_v1.06.md` D-6 の「探す側が同じ穴を持つ」と同型である。**
D-6 では**同種の穴を洗い出す手法**そのものが穴を持っていた。
§12-5「検知手段自身が同じ罠を踏んだ」（`docs/盤面設計メモ.md`）も同じ形で、
★**D-10 の「検知手段を書いたら『見つかるはずのもの』を仕込んで動かす」が
文書の見出しにも要る**ことを示している。

→ ★**処遇は (f) と同じにできない。**見出しは「どこまで解消したか」を示す索引なので、
**数（というより符号の並び）を消すと機能を失う。**
**(g) を補い、(h) を足した。**★**次に (i) を足す人は見出しも直すこと。**

##### (h) の処遇

| 直したもの | 内容 |
|---|---|
| `docs/UI設計メモ.md` §4-2 の列表 | `rarity` を「表示（★**絞り込みには使っていない**）」に訂正 / `cardType` に D72 の用途を追記 |
| 同 §4-2 の本文 | ★**「投影に既に来ている」ことは残した。** 詳細検索（**U21**）を入れるとき `rarity` は**追加費用が最も小さい軸**であり、**誤りを消すだけで有用な情報まで消さない** |
| この節の見出し | (g) を補い (h) を足した |
| `CLAUDE.md` §8 の D-15 の行 | 同上 |

#### ★★ (i) doc が**誤った方針**を書いていた（2026-08-26 発見・同日解消 / 決定 D93-2）★★

★★**(h) は「記述の誤り」だったが、(i) は「方針の誤り」である。**★★

**3 つの doc が「脇置き（`OutOfRuleZone.mulliganAside`）の落とし先を盤面に置く」と書いていた。**

| 場所 | 記述 |
|---|---|
| `CLAUDE.md` §8「次の一手」 | 「★**脇置き（6.2.1.6）の落とし先を画面に置くと、M-B5 の 7 つ目の合成経路が通るようになる**」 |
| `docs/盤面設計メモ.md` §7-3 | 「★**画面から通せるのは 6 経路。**脇置き（6.2.1.6）の落とし先は **M-B6** で置く」 |
| `docs/決定事項一覧.md` D90-2 の表 | 同上 |

★★**事実としては正しい**（置けば通る）。**誤っていたのは、それを利点として書いたことである。**★★

`zone.dart` は **M-B6 のはるか前から**こう書いていた ——

> すなわちこの置き場は **6.2.1.6 の手順内にのみ存在し**、置いたカードは同じ手順の中で
> メインデッキ置き場へ戻る。

盤面に恒久の落とし先を置くと、**6.2.1.6 の外で脇置きにカードが残る**という
**条文が定めていない状態**を実装が作れてしまう（**D-B**）。
★U23 が「0 枚を完成と呼ぶのは 6.1.1.3 に反するので解けない」と結論したのと同じ形である。

##### ★ D-15 の系列に入れる判断

★★**先例は `docs/盤面設計メモ.md` §6-1 である。**★★
あそこは D72 の Phase 3b 節が総評の表と食い違っていた件を
「★これも §12（D-15）と同じ型 ——**書いた時点で、既に別の場所に正しい記録があった**」
と分類している。★**今回もまったく同じ形**で、この方針を書いた時点で
`zone.dart` に正しい制約が**既にあった**。→ **(i) として D-15 に入れる。**

| | (a)〜(g) | (h) | ★**(i)** |
|---|---|---|---|
| 何が食い違うか | doc の**数**と実装の**数** | doc の**能力の主張**と実装の**不在** | ★**doc の方針**と、同じリポジトリの**別の場所にある制約** |
| 古くなったのか | ○ 実装が動いて古くなった | ✗ 最初から広かった | ✗ ★**最初から両立していなかった** |
| 走査で出るか | ○ 数を数えれば出る | ✗ 数量詞が無い | ✗ ★**2 つの doc の整合は形式的に検査できない** |

##### ★★ 写像テストだけでは担保が続かなかった（★確かめた）★★

7 つ目の経路（盤の外 → 盤の外）は**画面から通せない**まま残る。
担保は `loveca-ui/test/board/board_move_test.dart` だけだが、
★**このファイルは `reduce` / `ReduceContext` / `GameStore` を 1 つも import していない。**
見ているのは**写像（掴んだ場所 × 落とした場所 → `GameAction`）だけ**である。

→ ★**未検証のまま残っていたもの 3 つ**:

1. その 2 アクションが**実際に `reduce` を通るか**（中継先の手札で札を見つけられるか）
2. ★**中継で `placedIn(card, Zone.hand)` が効く**ので、フリーエリアで**表向き**だった札が
   脇置きで**裏向き**になる —— この観測差を**誰も一度も走らせていない**
3. 合成が**履歴 1 件**に収まるか（他の 6 経路は画面テストが通しているが 7 つ目は通っていない）

★★**「テストがあるから大丈夫」で終わらせなかった。**★★
→ `loveca-ui/test/board/board_move_apply_test.dart` を置き、
**写像の答えを本番の口**（`GameStore.dispatchAll`）へ流す。7 経路とも 1〜3 を固定した。
★**画面から通せるのは 6 経路のまま**である。

##### (i) の処遇

| 直したもの | 内容 |
|---|---|
| `CLAUDE.md` §8 | 「置くと通るようになる」→「★**置かない**（理由つき）」 |
| `docs/盤面設計メモ.md` §7-3 | 「M-B6 で置く」→「★**恒久的に 6 経路**」＋ 写像テストの限界と、それを塞いだ手当て |
| `docs/決定事項一覧.md` D90-2 の表 | 同上 |
| この節の見出し | ★**(i) を足した**（(h) が「次に (i) を足す人は見出しも直すこと」と書いている） |
| `CLAUDE.md` §8 の D-15 の行 | 同上 |

---

#### ★★ (j) 「満たした」と記録した本人が確かめていなかった（2026-08-26 発見・同日解消 / 決定 D94-2）★★

★★**(h) は「記述の誤り」、(i) は「方針の誤り」、(j) は「検証していない完了報告」である。**★★

`docs/盤面設計メモ.md` §14-7 の M-B6 完了表 3 行目が、こう記録していた。

> ★**満たした。**進行バーに「整理する 10.4 / 10.5」。★**当たるものが無いときも黙らない**
> （`TidyFoundNothing`。自動の CT では出さない）

**満たしていなかった。** `BoardTidyLog` の畳み込みが `warnings.isEmpty` を条件に含んでおり、
**10.3 / 10.6 の警告が立っていると「ありませんでした」が出ない。**
押した人は**押す前から在った警告だけを見て、押したボタンの結果を受け取らない。**

★★**「満たした」を書いた時点で、警告がある盤面を 1 度も押していない。**★★
テストも同じ穴を持っていた —— `board_tidy_test.dart` の
「手で押したときは『ありませんでした』が出る」は**警告が無い盤面でしか見ていない。**
★**これは D-10 が戒める形そのもの**（起きない条件で見ると常に通る）で、
**その D-10 を引用して書かれた節が、同じ罠を踏んでいた。**

| | (a)〜(g) | (h) | (i) | ★**(j)** |
|---|---|---|---|---|
| 誤りの所在 | doc の**列挙**が古い | doc の**能力の記述**が最初から広い | doc の**方針**が誤り | doc の**完了報告**が未検証 |
| 実装は | 正しい | 正しい | 正しい | ★**誤っている** |
| 見つけ方 | 走査 | ★走査では出ない | ★形式的に検査できない | ★★**実機で押すまで出ない**★★ |

★★**(j) の見つけ方は他の 3 つと違う。**★★
(a)〜(i) はいずれも**文書を読めば**（あるいは走査すれば）分かるが、
(j) は**実装と doc が同じ誤解を共有している**ので、
**文書と実装を突き合わせても一致してしまう。**
→ **実機で条件を作って押す以外に検知手段が無い。**
★M-B6 の実機確認で 6 件見つかったうち 2 件が実装の誤りだったのは、この性質による。

##### (j) の処遇

| 直したもの | 内容 |
|---|---|
| `loveca-ui/lib/src/state/game_store.dart` | 畳み込みを廃し 4 行を独立に組む（`isEmpty` → `appliedNothing`） |
| `loveca-ui/lib/src/ui/board/board_progress.dart` | Tooltip に**なぜそうなるか**まで書く |
| `docs/盤面設計メモ.md` §14-7 の 3 行目 | 「満たした」に訂正を添える |
| `docs/盤面設計メモ.md` §10-2 | 一般則（抑止できるのは押下が生んだ事実だけ）を足す |
| この節の見出し | ★**(j) を足した**（(h) の「次に足す人は見出しも直すこと」に従う） |
| `CLAUDE.md` §8 の D-15 の行 | 同上 |

★**次に「満たした」と書く人へ**: **条件が成立する盤面を作って実際に押してから書くこと。**
書いた内容をテストで固定するときは、★**その条件が成立している側**で固定すること。

---


#### ★★ (k) 「次に実機で確認する」と書いた項目が、そもそも実行できなかった（2026-08-26 発見・同日解消 / 決定 D95）★★

`docs/盤面設計メモ.md` §16（M-B6 の実機確認で見ていない経路）の **3 番**がこう書いていた。

> **`TidyExcluded`** | カードマスタに無い札が要る。…実機で見るなら dist を欠いた状態を作る。
> ★**このとき D-22（孤児が盤面から消える）も一緒に見えるはず**

★★**成立しない。dist を欠いても盤面が開かない。**★★

`GameSetup.begin`（`loveca-core/lib/src/game/game_setup.dart`）は
`_unknownPrintingsOf` が空でなければ `GameSetupException` を投げる（**決定 D80** / **D35**）。
`GameStore` の `ReduceContext` は開始時と**同じ `catalog.cards`** を使う
（`game_store.dart` / `boot_steps.dart`）。
→ **カタログに無い cardNumber の札が盤面に載る経路が存在しない。**
`ghostCardNumber` は fixture でしか作れない。

★**dist を減らしても駄目である。**カタログは DB から作られ、
`deleteOrphanCards` の本番呼び出し元が 0 件（**D-8**）なので、
dist から商品を消しても DB のカードは残る。

##### ★ D-15 の系列に入れる判断 —— (h) の亜種

| | (h) | ★**(k)** |
|---|---|---|
| 何が食い違うか | doc の**能力の主張**と実装の**不在** | doc の**確認予定**と実装の**不可能性** |
| 向き | 最初から広かった | ★**最初から広かった**（同じ） |
| 対象 | 現在の能力 | ★**将来の作業** |
| 走査で出るか | ✗ 数量詞が無い | ✗ ★**同じ**（「〜できるはず」に数量詞は無い） |

★**(h) と同じ「doc が実装より広い」型**だが、主張しているのが
**現在の能力ではなく将来の作業の実行可能性**である点だけが違う。
→ **(k) として D-15 に入れる。**（新しい D 番号を立てない。型が同じなら系列に入れる）

##### ★★ §16 の残り 5 項目も実行可能性を確かめた ★★

★**「次に確認する」と書いた項目が実行できるかを、誰も確かめていなかった。**
3 番だけを直して他を放置すると、同じ発見をまた繰り返す。→ **6 項目すべてを机上で確認した。**

| # | 実行できるか | 根拠 |
|---|---|---|
| 1 | ★**できる** | 10.5.3 / 10.5.4 は `StackUnderMember`（`moveToMemberSlot` の下半分）→ `DetachFromMember` で孤児を作れる。10.5.2 は `moveToZone` が**種別で拒否しない**ので、手札のメンバーを 4.7 へ落とせる |
| 2 | ★**できる** | 手札の札を解決領域（4.14）へ落として整理を押すだけ |
| 3 | ★★**できない**（この (k)）★★ | 上記のとおり `GameSetup.begin` が投げる |
| 4 | ★**できる。ただし条件を作る手当てが要る** | 測定は実機ではなく `board_min_width_test.dart`。前回 `tidy-notices = 0.0` だったのは **harness が整理を 1 度も押していなかった**ため。押せば出る |
| 5 | ★**できる。ただし前提が誤っている** | 「下段を入れ替える」必要は無い —— ★**`viewerId` を切り替える UI は存在しない**（`BoardPage` の構築時に固定）。ローカル対戦では**両方の袖が同時に描かれる**（`board_layout.dart` の `_Sleeve` 2 つ）ので、そのまま両方見られる |
| 6 | ★**できる** | 合成キー入力で足りる |

★★**3 番は「できない」が、修正の確認は別の経路でできる。**★★
**ライブカードの孤児**（`StackUnderMember` は種別を制限しない）は実機で完全に作れ、
「動かない」「理由が出る」「タグが違う」を実物で確かめられる（**決定 D95**）。
→ ★**この修正が実機で確かめられる唯一の経路である。**

#### ★★ (l) D92（自動進行）の設計が実装より古かった —— 3 件（2026-08-27 発見・同日解消 / 決定 D98-4）★★

★★**M-B7 の実装前に「D92 は端から端まで確定済みか」を確かめた結果である。**★★
**確かめずに「形の話しか残っていない」と読まなかった。**

| # | D92 / §15 の記述 | 実物 | 型 |
|---|---|---|---|
| 1 | D92-6 / §15-10「整理は **CT 単位に 1 行**」 | ★**成立しない。**D95（**2026-08-26**）が `BoardTidyLog.notices` を **1 CT 最大 5 行**にした。★**D92 は 2026-08-25 で、そちらが古い** | ★**(a)〜(g) と同じ**（doc が実装より古い） |
| 2 | D92-3 / §15-5「基準は **`BoardState.tidy.warnings`**」 | ★**その名前は無い。**D93-5 で **`tidies`（`List`）**になり、**どの要素を基準にするかが未定義**になっていた | ★同上 |
| 3 | D92-4 / §15-7 用途 2「**画面から 73 ステップを通す**（D86）と 42（D88）が成立する」 | ★**成立していない。**`board_progress_test.dart` も `board_mode_test.dart` も `_walkOneTurn` が **`store.dispatch(...)` を直接叩く**。画面から 73 ステップを通したテストは**存在しない** | ★★**(h)**（doc が実装より**広い** / 走査では出ない）★★ |

★★**1 と 2 は「決定を書いたあとに別の決定が実装を変えた」型である。**★★
(a)〜(g) は**実装が先に動いて doc が取り残された**が、ここは
**doc（D92）が実装を待っているあいだに、別の決定（D93-5 / D95）が前提を動かした。**
→ ★**未実装の決定も、実装するときに「前提が動いていないか」を確かめること。**
★**M-B6 の持ち越し制約（§14-7 の 4 つ目）は D92 のために置いたのに、
そこで器を変えたことが D92 本文へ反映されていなかった**のが 2 の出どころである。

★**3 は (h) と同じで走査では出ない。**「成立する」という断定に数量詞が無い。
→ ★**「テストが本番の口を通る」と書いたら、そのテストを実際に開いて確かめること。**

##### 処遇

| 直したもの | 内容 |
|---|---|
| `docs/決定事項一覧.md` の D92 の節 | ★**冒頭に訂正の表を置いた**（節の中に埋めない） |
| `docs/盤面設計メモ.md` §15-10-b | 畳み込みの規則（1 の訂正を含む） |
| `docs/盤面設計メモ.md` §15-8 | 押下回数の実測（静的 / 実戦的な盤面） |
| 実装 | 2 は `value.tidies.last.warnings` を基準にした。3 は「`GameStore` の 1 件版を残すこと」へ書き換え、画面からは 1 押下ぶんを見る |
| この節の見出し | ★**(l) を足した**（(h) の「次に足す人は見出しも直すこと」に従う） |

---

##### (k) の処遇

| 直したもの | 内容 |
|---|---|
| `docs/盤面設計メモ.md` §16 の 3 番 | 「dist を欠けば見えるはず」→ ★**「開かないので作れない」**（理由つき）＋ 代わりの経路 |
| `docs/盤面設計メモ.md` §16 | ★**6 項目すべてに実行可能性の欄**を足す（1 つだけ直すと同じ発見を繰り返す） |
| `docs/盤面設計メモ.md` §16 の 5 番 | 「下段を入れ替える」の前提が誤り（切替 UI が無い / 両袖は同時に出る） |
| この節の見出し | ★**(k) を足した**（(h) の「次に足す人は見出しも直すこと」に従う） |
| `CLAUDE.md` §8 の D-15 の行 | 同上 |

★**次に「次回これを見る」と書く人へ**: ★**その経路が実際に到達できるかを確かめてから書くこと。**
到達できないなら、到達できる代わりの経路を同時に書くこと。

---


### D-16. 「乱数を消費するアクション」の列挙が doc で 4 のまま【★2026-08-25 発見・同日解消 / ★同日 走査の盲点を追記 / ★★同日 **同じ列挙にもう 1 件の漏れ**を発見 → **D-19**★★】

★**D-15 とまったく同じ型である。**D-15 (a)(b) を M-B1 で直したとき、
**コード側の 2 箇所（`reduce.dart` / `game_action.dart`）は直したが、
同じ列挙を持つ設計文書 2 箇所は直されなかった。**

| 場所 | 書いてあった数 |
|---|---:|
| `loveca-core/lib/src/game/reduce.dart:24` | **5**（`ShuffleZone` / `Refresh` / `DrawCards` / `AdvanceStep` / ★`DrawEnergy`） |
| `loveca-core/lib/src/game/game_action.dart:177` | **5** |
| ★`docs/盤面設計メモ.md` §8-3 | **4**（`DrawEnergy` が無い） |
| ★`docs/決定事項一覧.md` D78 の詳細 | **4**（同上） |

#### ★★ これは飾りではない ★★

**D78** が定めたのは「**乱数を消費したアクションを undo したときだけ**注記を出す」である。
この列挙どおりに実装すると、**`DrawEnergy` で注記が出ない。**
`DrawEnergy` は **D81 / D87 で「恒久のボタン」と決めた**もので、両プレイヤーの袖にあり、
**プレイヤーが最もよく押すものの 1 つ**である。

★つまり **D-15 の (a)(b) と同じ実害**（「シャッフルは引き直せません」が出るべき場面で出ない）を、
**列挙の写しのほうが持ち続けていた。**

#### いつから誤っていたか

`DrawEnergy` は **M-B1（決定 D73）**で新設され、`energy_deck.dart` の `drawEnergyRandomly` が
`rng.nextInt` を呼ぶ。同じコミットで `reduce.dart` / `game_action.dart` は 5 に直っている。
★**設計文書だけが 4 のまま残った。**

#### 処遇

★**2026-08-25（M-B4 の設計時）に doc 側 2 箇所を 5 に直した。**

★★**手法の記録 —— 列挙を直すときは「同じ列挙の写し」を探すこと。**★★
D-15 の走査コマンドは **`lib` 配下の `///` コメント**しか見ていない。
**設計文書（`docs/*.md`）は走査の対象外だった。**

```bash
grep -rn "ShuffleZone" docs/*.md CLAUDE.md
```

★**コードの断定を直したら、同じ断定が doc に写っていないかを必ず見る。**
★D-15 §12-5 の教訓（探す道具が対象と同じ癖を持つ）に、**探す範囲そのものが狭かった**という形が加わる。

#### ★★ 盲点 —— 原法は `test/` と `docs/*.md` を原理的に見ない（2026-08-25 追記）★★

D-15 の走査は **`<pkg>/lib` 配下の `///` `//` コメント**だけを対象にしている。
つまり次の 3 つは、規約を破っていても**永久に出てこない。**

| 原法が見ないもの | 実害の実例 |
|---|---|
| ★`docs/*.md` / `CLAUDE.md` | D-16 そのもの（列挙の写しが 4 のまま）/ ★**D-15 (f)**（`CLAUDE.md` の「5 行」）/ ★**盤面設計メモ §12-4 の「乱数は `deck_id.dart` 1 箇所」** |
| ★`<pkg>/test` | 断定はテストの doc にも書かれる（`reduce_call_site_test.dart` / `card_image_source_test.dart` / `game_store_test.dart`） |
| ★★**数え上げ語を含まない断定** | 「`BootNotice` \| **R2 の `NoticeBar`**」（D89 で `BootGate` へ一本化したのに 3 写しが残っていた）。★**「唯一」も「N 箇所」も無いので、どんな数え上げ語の正規表現でも当たらない** |

★★**今回 (f) が `CLAUDE.md` で見つかったのは、走査範囲を広げたからである。**★★
原法のままでは `CLAUDE.md` は 1 行も読まれない。

★**広げた走査**（★数詞のあとの `だけ` / `のみ` を必須にしない。範囲に `test/` と文書を足す）

```bash
RE='(唯一)|([0-9０-９][ 　]*(箇所|カ所|か所|つ|個|本|件|種|列|通り|行|ファイル)[ 　]*(だけ|のみ|ちょうど))|([1１][ 　]*(箇所|カ所|か所|本|件|種|行|ファイル))|((だけ|のみ)(が|である))'
for p in loveca-core loveca-db loveca-ui; do
  grep -rnE "^[[:space:]]*(///|//).*($RE)" $p/lib  --include=*.dart | grep -v "\.g\.dart"
  grep -rnE "^[[:space:]]*(///|//).*($RE)" $p/test --include=*.dart      # ★新規
done
grep -rnE "$RE" docs/*.md CLAUDE.md ルール整合性チェック_v1.06.md          # ★新規
```

★★**これでも「数え上げ語を含まない断定」は出ない**★★（上の表の 3 行目）。
そこは D-15 の後半の手順——**唯一と断じている記号を 1 つずつ `grep` で数え直す**——でしか見つからない。
実際 `BootNotice` の 3 写しは `grep -rn "NoticeBar"` で出てきたのであって、
上の正規表現では 1 件も当たらなかった。

#### ★★ 陽性対照を置かなければ「0 件」と「見えていない」の区別がつかなかった ★★

走査を広げる前に、**捕まるはずのもの**を 3 つの範囲それぞれに置いた
（`loveca-ui/lib/src/_scan_ctl_tmp.dart` / `loveca-ui/test/_scan_ctl_tmp.dart` /
`docs/_scan_ctl_tmp.md`。★**走査後に削除。コミットしていない**）。

| 対照 | 広げた走査 | ★**D-15 原法** |
|---|---|---|
| (A) `lib` の「唯一」 | ○ | ○ |
| ★(A2) `lib` の「**1 箇所**」（`だけ` / `のみ` を伴わない） | ○ | ★**×** ——原法は数詞のあとに `だけ\|のみ\|ちょうど` を要求する |
| ★(B) `test/` の断定 | ○ | ★**×**（範囲外） |
| ★(C) `docs/*.md` の断定 | ○ | ★**×**（範囲外） |

★★**(A2) が効いた。**★★`docs/盤面設計メモ.md` §12-4 の
「UI 層で乱数を引くのは `deck_id.dart` **1 箇所**」は `だけ` を伴わないので、
**原法の正規表現をそのまま `docs` に当てても出てこない。**
→ ★★**「範囲を広げる」だけでは足りず、「語の書き方」も同時に広げる必要があった。**★★
§12-5 が言う「探す道具は対象と同じ材料でできている」の **2 度目の実例**である。

#### ★★ 2026-08-25 追記: 4 → 5 に直した列挙は、**5 でも足りていなかった** ★★

M-B5 の実装中に `LookAtTop` が **6 つ目**の消費者であることが分かった。
★**この D-16 の修正そのものが「数え直したのに数え漏らした」ことになる。**
→ **D-19** に分けて記録する（未解消。`loveca_core` を次に触るときに直す）。

★★**ここから引き出せる教訓は「6 に直せ」ではない。**★★
4 → 5 に直した同じ作業が 6 を見落としたのだから、**7 つ目が増えれば同じことが起きる。**
→ M-B5 の実装では **UI 側が列挙を持つのをやめた**（`loveca-ui/lib/src/state/counting_rng.dart`。
`nextInt` の呼び出し回数を数え、差が 0 かどうかで判定する）。
★**列挙を直すのではなく、列挙を要らなくした。**


---

### D-19. 「乱数を消費するアクション」の列挙に `LookAtTop` が無い【★2026-08-25 発見・同日解消】

#### 事実

**`LookAtTop`（総合ルール 10.2.2.2）は乱数を消費する。**列挙 4 箇所すべてに入っていない。

```dart
// loveca-core/lib/src/game/reduce.dart:333-341
ReduceReport _lookAtTop(GameState state, LookAtTop a, ReduceContext ctx) {
  if (!ctx.refresher.needsRefreshForLook(state, a.playerId, a.count)) {
    return ReduceReport(state: state);
  }
  return ReduceReport(
    state: ctx.refresher.refreshPlayer(state, a.playerId),   // ← rng.shuffled を通る
    refreshCount: 1,
  );
}
```

★**条件つきだが、確実に通る枝がある。**
`needsRefreshForLook`（`refresh.dart:39-41`）は `waitingRoom.isNotEmpty` を含み、
`refreshPlayer`（`refresh.dart:50-52`）が早期 return するのは `waitingRoom.isEmpty` のときだけ。
**したがってこの枝に入れば必ず `rng.shuffled` を呼ぶ。**

| 場所 | 書いてある数 | `LookAtTop` |
|---|---:|---|
| `loveca-core/lib/src/game/reduce.dart:24-31` | 5 | **無い**（★未解消） |
| `loveca-core/lib/src/game/game_action.dart:177-192` | 5 | **無い**（★未解消） |
| `docs/盤面設計メモ.md` §8-3 | 5 → ★M-B5 で注記 | — |
| `docs/決定事項一覧.md` D78 | 5 → ★M-B5 で注記 | — |

#### いつから誤っていたか

`_lookAtTop` は **3a-4** で書かれ、列挙も **3a-4** で書かれた。
★**同じ変更の中で、片方が片方を数え漏らしている。**
D-15 (a) が「断定のほうが後に書かれ、書いた時点で実装と食い違っていた」だったのに対し、
ここは**同時に書かれて食い違っていた**。

#### 実害

**D78** は「乱数を消費したアクションを undo したときだけ注記を出す」と定めている。
列挙どおりに実装すると、**10.2.2.2 のリフレッシュを起こした「上から見る」を undo したときに
注記が出ない。**「もう一度シャッフルすると別の結果になる」ことに気づけない。

★**この経路は M-B3 で実装済み**（`board_deck_menu.dart` の「上から N 枚見る」）であり、
**実機で踏める。**

#### ★★ M-B5 で採った手当て — 列挙を要らなくした ★★

UI 側は **`CountingRng`**（`loveca-ui/lib/src/state/counting_rng.dart`）で
`nextInt` の呼び出し回数を数え、**dispatch の前後の差が 0 かどうか**で判定する。
`DeterministicRng` はメソッドが 1 つ、`shuffled` は extension なので、
**包むだけで全経路が数に入る。`loveca_core` の変更は 1 文字も要らない。**

★利点は「漏れなくなる」だけではない ——
**`AdvanceStep` は消費したりしなかったりする**（7.5.2 は必ず / 他はリフレッシュしたときだけ）。
列挙では常に真にせざるを得ず、**「次へ」を戻すたびに注記が出て意味が消える**（M3 の縮退と同じ）。
実測なら**実際に消費したときだけ**出る。

★固定しているテスト:
`loveca-ui/test/state/board_session_test.dart`（6 経路すべて + 純粋なアクションの対）/
`loveca-ui/test/state/counting_rng_test.dart`（包んでも乱数列が変わらないこと）。
★**`_count++` を外すと 3 ファイル 8 件が落ちる**ことを 2026-08-25 に実測した。

#### ★★ 未解消の部分 — core のコメントは 5 のまま残る ★★

**M-B5 は `loveca_core` を変更しない方針だった**ので、上表の 2 箇所は誤ったままである。

★**消すのではなく直す。**この列挙は UI のためではなく
**`reduce` の参照透過性の契約**を core の読者に説明するものなので、
数え方を UI で変えても core 側の意味は残る。

| いつ | 何を |
|---|---|
| ★**済（2026-08-25）** | `reduce.dart` と `game_action.dart` の列挙に **`LookAtTop`** を足した。★**条件つき（10.2.2.2 の不足時のみ）であることも書いた** |

★★**あわせて「列挙方式そのもの」への注記を隣に置いた。**★★
3 → 5 に直した作業（D-15 (b)）が `LookAtTop` を見落とし、
5 → 6 に直したこの作業も **7 つ目で同じことが起きる**。
★**3 度数え直して 3 度とも漏れている**（D-15 → D-16 → D-19）ので、
**「ここを直せば済む」と読ませない**書き方にした ——
判定が要るなら列挙ではなく**観測**を使うこと（決定 **D90-1** / `CountingRng`）。
★`AdvanceStep` と `LookAtTop` は**消費したりしなかったりする**ので、
そもそも列挙では精度が足りない。

★**断定の中身をテストで固定した**（D-15 のときと同じ扱い）——
`loveca-core/test/reduce_test.dart` の「LookAtTop が乱数を消費する（D-19 の裏取り）」。
枚数が足りないときは seed で結果が変わり、足りているときは変わらない。
★**どちらも「リフレッシュが実際に起きた / 起きていない」を先に確かめてから比べる。**


---

### D-20. 属性を定義したのに `lib/` の消費者が 1 人もいない【★2026-08-25 発見・同日一部解消】

#### 事実

`Zone.visibility`（総合ルール 4.1.2 の公開 / 非公開）は **3a-1 から存在していた**。
条番号つきの doc も、11 領域ぶんの値も揃っていた。**だが `lib/` から 1 度も読まれていなかった。**

```bash
# 修正前（bc9cc30）の走査。宣言そのものだけが当たる
git grep -n "\.visibility\b" bc9cc30 -- loveca-core/lib loveca-db/lib loveca-ui/lib
# → loveca-core/lib/src/game/zone.dart:117:    this.visibility,   ← 宣言 1 件のみ
```

読んでいたのは `test/game_types_test.dart` の 3 箇所だけで、
そこも「値が条文どおりか」を確かめるだけだった。
**結果として、総合ルール 4.1.2.1 が 1 度も実装されないまま Phase 3b の M-B5 まで来た**
（`4.1.2.1` / `4.2.2` / `4.2.3` はリポジトリ全体で **0 件**だった。決定 **D91**）。

#### ★ なぜ気づけなかったか

★★**「属性を定義した時期」と「それを使う側を書いた時期」が離れている。**★★

| いつ | 何が起きたか |
|---|---|
| **3a-1** | `zone.dart` に `visibility` / `isOrdered` / `isShared` を持たせた。★**このとき消費者はまだ存在しない**（盤面 UI も `_moveCard` も無い） |
| **3a-2 以降** | `_moveCard` を書いた。**移動先の領域を見る動機がその場に無い**——目の前の仕事は「取り出して差し込む」であり、属性は視界の外にある |
| **M-B1〜M-B5** | 盤面 UI が `isOrdered` は読んだ（帯の撃ち分けに要ったから）。★**`visibility` は読む理由がその場に無かった** |

★**`isOrdered` だけが使われたのは偶然ではない。**「帯を出すか」という
**目に見える仕様**が消費者を呼んだからである。`visibility` に対応する目に見える仕様
（＝ 4.1.2.1 の表示面）は**誰も要求しなかった**ので、属性は置かれたまま眠った。

★★**実データで初めて分かった、のではない。実機で人が触って初めて分かった。**★★
テストは全部通っていた（`loveca-core` 307 / `loveca-ui` 682）。
**属性と実装のあいだに検査が 1 つも無かった**ので、通って当然である。

#### ★ 検知手段はあったか → ★★**無かった（実測で確認した）**★★

`dart analyze` は**公開フィールドの未使用を報告しない。**
模擬事故を仕込んで確かめた ——

```dart
// zone.dart に一時的に足した。どこからも（テストからも）読まない
bool get neverReadAnywhere => isShared && ruleRef.isNotEmpty;
```

→ `dart analyze` は **`No issues found!`**。
`unused_element` / `unused_field` は **private 宣言にしか効かない**ので、
パッケージの公開 API に置いた属性は永久に報告されない。
`lints/recommended.yaml` にも該当するルールは無い。

★走査を使うテスト（`reduce_call_site_test.dart` / `abolished_term_test.dart`）は
**「在ってはいけないもの」を探す**形で、「在るべき消費者が居ない」は見ていない。

#### ★ 他に同種が無いか → ★★**1 件ある。`Zone.isShared`**★★

```bash
grep -rn "\.isShared\b" loveca-core/lib loveca-db/lib loveca-ui/lib
# → loveca-core/lib/src/game/zone.dart:119:    this.isShared = false,   ← 宣言 1 件のみ
```

`isShared`（4.14.1「両プレイヤーが共有して使用する領域が 1 つだけ存在します」）も
**`lib/` の消費者が 0 人**である。読むのは `test/game_types_test.dart` 1 箇所だけ。

★**ただし `visibility` とは事情が違う。**共有であることは
`card_move.dart` が `Zone.resolution` を `ArgumentError` で弾く形と
`GameState.resolution` を独立フィールドに持つ形で**型として表現されている**ので、
`isShared` を読む必要が実際に無い。★**「消費者が 0」＝「不具合」ではない。**
`visibility` が問題だったのは、**条文（4.1.2.1）が要求する振る舞いが実装されていなかった**
からであって、参照数そのものが問題なのではない。

| いつ | 何を |
|---|---|
| 済（2026-08-25） | `visibility` に消費者を作った（`placedIn` / 決定 **D91**） |
| ★**次に `Zone` へ属性を足すとき** | ★**「この属性を読む側を同時に書くか、読まない理由を doc に書くか」を必ずどちらか決める。**★`dart analyze` は助けてくれない（上で実測済み） |

★★**教訓は「属性を持たせただけでは使われない」だけでは足りない。**★★
**「属性が条文由来なら、その条文が要求する振る舞いも同時に実装されたか」を見ること。**
`visibility` は 4.1.2 を写した属性であり、4.1.2 には 4.1.2.1 という
**振る舞いの規定が続いていた**。属性だけを写して振る舞いを写さなかったのが本体である。

---

### D-17. 盤面設計メモ §14-5 の「相手側に何を入れても自分側は同一」が誤り【★2026-08-25 発見・同日解消】

#### 事実

`docs/盤面設計メモ.md` §14-5 は、ソロの相手側に自分と同じデッキを入れる根拠として
こう書いていた ——

> ★★**自分側の初期盤面は、seed が同じなら相手側に何を入れても完全に同一である。**★★
> 6.2.1 は**先攻から順に**処理し（`GameSetup.begin` が `ordered` を作る）、
> ソロでは自分が常に先攻（§14-1）なので、**自分が先に乱数を消費する。**

**6.2.1.7（エネルギー）について誤りである。**

| 手順 | 相手デッキに依るか |
|---|---|
| 6.2.1.2 シャッフル / 6.2.1.5 初期手札 | ★**依らない**（先攻から順に処理される） |
| ★**6.2.1.7 エネルギーを 3 枚** | ★★**依る**★★ |

#### なぜか

★★**6.2.1 は「プレイヤーごと」ではなく「手順ごと」に両プレイヤーを回す**★★（条文どおり）。

```
GameSetup.begin            : A の 6.2.1.2 → B の 6.2.1.2 → …
GameSetup.dealInitialEnergy: A の 6.2.1.7 → B の 6.2.1.7
```

**A の 6.2.1.7 は B のシャッフルより後**なので、B のデッキ枚数が変われば
`rng.shuffled` の消費量が変わり、**A の 4.9.3 の無作為抽出がずれる。**

★**実装は正しい**（条文が手順ごとに定めている / D80 の 2 段構造もそのため）。
★**誤っていたのは断定のほうである。**

#### どうやって見つけたか

★★**「同じはず」で済ませず、テストを書いたら 1 件外れた。**★★
`loveca-core/test/game_setup_test.dart` の
「相手デッキを変えても自分側の instanceId 列が一致する」が、
**メインデッキと手札は一致し、エネルギーだけ一致しなかった。**

★D86 で「同じはず」を実際に測って 1 件ずれた前例があり、**同じ型が再発した。**

#### 実害

★**無い。**ソロは相手側に**必ず自分と同じデッキ**を入れる（D81 の既定）ので、
相手デッキが変わること自体が起きない。**壊れていたのは根拠の書き方だけ**である。

★ただし「空の `Deck` を採らない理由 2 つ」（6.2.1.5 で手札 0 枚 / 7.6.2 が
空のメインデッキで 10.2.2 を踏む）は**そのまま生きている。**
同じデッキを入れる根拠はそちらに寄せる。

#### 処遇

★**盤面設計メモ §14-5 の断定を訂正した**（2026-08-25）。
★**事実をテストで固定した** —— 一致する側（6.2.1.2 / 6.2.1.5）と
一致しない側（6.2.1.7）を**対で**置き、さらに「後攻にすると成立しない」も見る
（**自分が常に先攻であること**が効いていることの確認）。

---

### D-18. `LOVECA_DIST_DIR` の位置づけが D60 と D64 で食い違う【★2026-08-25 発見・同日解消】

#### 事実

| 出典 | 段 1（環境変数 `LOVECA_DIST_DIR`） | 段 2（`settings.json` の `distDir`） |
|---|---|---|
| **D60 本文** / `dist_locator.dart` | 「★**開発と検証**。M1 はこれで動かす」 | 「R6（M6）が書く」 |
| ★**D64 本文** / `CLAUDE.md` §3 | 「D60 が定めた★**本番の設定経路**（段 1）」 | — |

D64 が言いたかったのは「`LOVECA_SEARCH_LIMIT` と違って**検証専用の口ではない**」であり、
**その限りで正しい。**しかし D60 の段の呼び名と読み合わせると、
**「段 1 が本番の経路であって段 2 は補助」と読める。**

#### 何が起きたか

★★**段 2 が一度も使われないまま常態化した。**★★
2026-08-25 の実機調査で `%APPDATA%\com.example\loveca_ui\` には `loveca.db` しか無く、
**`settings.json` が存在しない**ことが分かった。
R6 の書き込み口は M6 で実装済みで動くのに、**誰も一度も書いていない。**
結果として `flutter run` のたびに `LOVECA_DIST_DIR` を付ける運用になっていた。

★**これは D-15 と同じ型である** —— 同じことを 2 箇所に書けば、片方が正から離れる。

#### 処遇

★**文言を直した**（2026-08-25）——

> 段 1 は**開発と検証の口**。ただし `LOVECA_SEARCH_LIMIT` と違い**検証専用ではなく実設定**である。
> ★**本番の設定経路は段 2**（R6 が書く `settings.json`）。

★**運用の判断は `docs/決定事項一覧.md` の D89 への追記**にある
（開発時の既定を変えず、段 2 を一度使わせる。その導線が D89 の帯と
「設定・診断を開く」ボタンである）。

---


### D-21. `dispatchAll` の合成は「報告を持つアクション」を 2 つ以上含められない【★2026-08-25 発見 / ★2026-08-26 に **`tidy` の器だけ**解消（M-B6 / 決定 D93-5）/ ★★**2026-08-27 に全件解消**（M-B7 / 決定 **D98-2**）★★】

#### ★★ 2026-08-27 追記: 残り 2 つも直した（解消）★★

| フィールド | 状態 |
|---|---|
| `refreshCount` | 元から積まれている |
| `tidy` | ★**解消（M-B6 / 決定 D93-5）** —— `BoardState.tidy` を **`tidies`（`List`）**にした |
| ★**`advance`** | ★★**解消（M-B7 / 決定 D98-2）**★★ —— `executed` / `taken` の単数をやめ、**`steps`（`List<BoardStepLog>`）**にした |
| ★**`skipped`** | ★★**解消（M-B7 / 決定 D98-2）**★★ —— 押下ぶん**連結**する |

★★**`cursorBefore` と `taken` が別のステップを指す問題も型ごと消えた。**★★
各 `BoardStepLog` が**自分の `cursor` と `taken` を対で持つ**ので、
「1 件目の入口と N 件目の遷移」という食い違いが**構造的に作れない。**
★**着地先（`cursorAfter`）は新設した** —— `steps` から**導けない**
（最後の遷移がフェイズを終える場合、着地先は次のフェイズの先頭であり、
さらに `skipForward` が飛ばした先になりうる）。**導けないものを導いたことにしない。**

★★**doc の側の「限定の無い書き方」も同時に直した**★★（下の「再発をどう防ぐか」の約束どおり）——
`game_store.dart` の `_run` の doc に「**固定列も自動進行もここを通る**」と書き、
**何を積むかをフィールドごとに表で示してある。**

★★**固定したのは 4 件**★★（`test/board/game_store_test.dart` の
「★★ 進行の報告を全件積む ★★」の群）——
`AdvanceStep` を 2 つ合成すると `steps` が 2 件になる / ★**対**: 1 つなら 1 件のまま /
ソロで飛ばしたカーソルが押下ぶん連結される / ★**対**: ローカル対戦では 1 つも飛ばない。

★**器と本体を混ぜない。**`tidy` を先に直したのは
`docs/盤面設計メモ.md` §14-7 の**持ち越し制約 4 つ目**が「整理ログの器を複数件対応で作れ」と
M-B6 に課していたからであって、D-21 全体を M-B6 へ前倒ししたわけではない。

#### 事実

`GameStore.dispatchAll` は N 個のアクションを `reduce` に通し、`record` を 1 回だけ呼ぶ
（決定 **D90-2** / 盤面設計メモ §8-2）。★**このとき `ReduceReport` の 4 つのフィールドのうち、
積まれるのは `refreshCount` だけで、残り 3 つは「最後の 1 件」で上書きされる。**

```dart
// loveca-ui/lib/src/state/game_store.dart:395-404
for (final action in actions) {
  final report = reduceWithReport(next, action, context: _context);
  next = report.state;
  refreshCount += report.refreshCount;              // ★ここだけ積まれる
  // ★整理と進行は「最後に起きたもの」を採る。起きていなければ前の値を保つ。
  tidy = report.tidy ?? tidy;                       // ★上書き
  advance = report.advance ?? advance;              // ★上書き
  if (report.skipped.isNotEmpty) skipped = report.skipped;   // ★上書き
}
```

`BoardOperationLog` も**単数**である —— `executed` は `StepId?`、`taken` は `StepTransition?`、
`cursorBefore` は**合成全体の開始位置**。★**2 つ以上の `AdvanceStep` を合成すると、
`cursorBefore` は 1 件目の入口、`taken` は N 件目の遷移**になり、
**2 つのフィールドが別のステップを指す。**

#### いつから誤っていたか

★**誤っていない。M-B5（D90-2）の要件は満たしている。**
D90-2 が合成にしたのは**手札を中継する 7 経路**で、いずれも `MoveCard` 系である
（`board_drop.dart:63` / `:71`。走査で確認した）。**`MoveCard` は `tidy` も `advance` も返さない**ので、
上書きが起きる組み合わせが**そもそも存在しない。**

★**問題は実装ではなく doc の側にある。**`game_store.dart` の library doc と D90-2 は

> `reduce` を N 回回して合成状態を作り、`record` を **1 回だけ**呼ぶ。
> これにより **N 個のアクション**が履歴 1 件 = 1 undo になる。

と、**アクションの種類を限定せずに**書いている。★★**doc が実装より広い。**★★
`ルール整合性チェック_v1.06.md` **D-15 (h)**（`CardListRow.rarity` を「絞り込み」と書いていたが
絞り込みは一度も存在しなかった）と**同じ向きの型**である。

#### 走査

```bash
# (1) 上書きしている 3 行と、積んでいる 1 行
grep -n "report.tidy ?? tidy\|report.advance ?? advance\|report.skipped\|refreshCount +=" \
  loveca-ui/lib/src/state/game_store.dart

# (2) ★合成に報告を持つアクションが入っていないこと（= いま実害が無いことの根拠）
grep -rn "dispatchAll" loveca-ui/lib --include=*.dart | grep -v "game_store.dart"
grep -rn "AdvanceStep" loveca-ui/lib --include=*.dart
```

(2) の結果（2026-08-25）: `dispatchAll` を**複数件で**呼ぶのは `board_drop.dart` の **2 か所だけ**で、
どちらも `MoveCard` 系。`AdvanceStep` は `board_progress.dart` の **`store.dispatch(...)`（1 件版）**
からしか渡されていない。★**Tidy は UI から 1 度も dispatch されていない**（M-B6 で口を作る）。

#### 実害

★**現時点では無い。** 上のとおり組み合わせが存在しない。

★★**しかし決定 D92（自動進行）はこれを直接踏む。**★★
「次へ」1 回で **平均 6 ステップ**（見積り。正は `docs/盤面設計メモ.md` §15-8）進むということは、
**1 回の `dispatchAll` に `AdvanceStep` が複数入る**ということである。そのとき

| 落ちるもの | 何が見えなくなるか |
|---|---|
| 途中の `tidy` | ★**10.4 / 10.5 の適用と 10.3 / 10.6 の警告が、最後の CT のぶんしか残らない。**1 押下で CT を 6 回通れば 5 回ぶんが黙って消える |
| 途中の `advance` | ★**8.3.6 の早期終了**（11 ステップ飛ぶ）が、そのあとに別の遷移が起きると読めなくなる |
| 途中の `skipped` | ★ソロで**フェイズを 2 つ以上跨いだとき**、最後のぶんしか出ない（D88 が「黙って飛ばさない」と決めたものが黙る） |

★**D92 の停止規則そのものが「新規の警告で止まる」を含む**（§15-5）ので、
**警告の観測が落ちると停止判定まで誤る。**表示だけの問題ではない。

#### 処遇

| いつ | 何を |
|---|---|
| ★**いま**（2026-08-25） | **記録のみ。**実装は変えない。★1 コミット = 1 論点であり、D92 は**設計を確定するところまで**である |
| ★**M-B6** | ★**器を複数件対応で作る**（`docs/盤面設計メモ.md` §14-7 の持ち越し表 **4 つ目**）。手動 `Tidy` は 1 件しか出さないので、単数で作り込むと M-B7 で作り直しになる |
| ★**M-B7** | ★★**完了（2026-08-27 / 決定 D98-2）**★★ `BoardOperationLog` を**押下 1 回ぶん**へ広げ、`_run` が報告を**積む**ようにした。★`refreshCount` が既に `+=` だったので、**同じ形に揃えるだけ**だった |

#### ★ 再発をどう防ぐか

★★**doc の側を先に直す。**★★ 実装を変えるのは M-B7 だが、**「N 個のアクション」という
限定の無い書き方**が残っている限り、次に合成を使う人が同じ前提で書く。

★**ただし今回は「doc から数量詞を消す」（D-15 (f)）では解けない。**
断定に数量詞が無く、**「アクションの種類を限定していない」という不作為**だからである
（D-15 (h) が「**走査では出ない**」と結論したのと同じ理由。汎用の検知手段は無い）。
→ ★**M-B7 で実装を直したときに doc の限定も同時に外す**、という順で片づける。
★**それまでは、この D-21 が「読んだ人が踏める」ことの記録である。**

---

### D-22. カタログに無い孤児カードが盤面から消える【★2026-08-26 発見・同日解消 / 決定 **D95**】

★★**A-3 より悪い。黙って落とすのではなく、落としたうえで「動かせませんでした」と報告する。**★★

`loveca-core/lib/src/game/rule_process.dart` の `_applyOnce` は、
10.5.3 / 10.5.4（上にメンバーが居ないカード）の処理でこう書いてある。

1. `sendToOwner(orphan)` —— `cards[instance.cardNumber]` が `null` なら
   `excludedCount++` して**そのまま return する**（どの領域にも置かない）
2. 呼び出し側は直後に `orphans = const []` を**無条件で**代入する

→ **カードはメンバーエリアから消え、どの領域にも現れない。**盤面から存在しなくなる。

★**実測**（2026-08-26 / `loveca-ui` のテストから全領域を走査した）——
カタログに無い孤児 1 枚で整理を回すと、
`resolution` / `hand` / `waitingRoom` / `energyDeck` / `mainDeck` / `successLive` /
`liveStage` / `energyField` / `exile` / `freeArea` / `mulliganAside` /
メンバーエリアの `stacks` / `orphans` の**すべてに存在しない。**

★★**同時に 2 つ目の誤りがある。**★★

```dart
applied.add(card?.cardType == CardType.energy
    ? RuleProcessKind.orphanEnergy
    : RuleProcessKind.orphanMember);
```

`card` が `null` のとき、`card?.cardType == CardType.energy` は `false` になるので
**「メンバーカード（10.5.3）を処理した」と記録される。**
種別を判定できていないのに、**推測を事実として報告している。**

★**画面に出る結果**（`loveca-ui`）——
「$ruleRef のチェックタイミングで整理しました: **上にメンバーが居ないメンバーカード 10.5.3**」と
「$ruleRef の整理で **1 枚を動かせませんでした**」が**同時に**出る。
★★**後者は事実に反する。**★★ 動かせなかったのではなく**消えている。**

| | A-3 | ★**D-22** |
|---|---|---|
| 出力 | 痕跡が無い | ★**痕跡はある。内容が誤っている** |
| 読み手 | 欠落に気づけない | ★**「その場に残っている」と誤解する** |

---

## ★★ 解消（2026-08-26 / 決定 D95）★★

### 方針: 動かさず、元の置き場に残す

**条文から導ける。**

- **10.1.2**「ルール処理は…条件を満たしているかを確認し、**満たされている場合に**実行されます」
  → 種別を判定できない札は**条件の成立を確かめられない**ので実行してはならない。
- **4.5.5.4.1 / 4.5.5.4.2**「そのままそのメンバーエリアに**残り**、その後にルール処理により…移動します」
  → 残っている状態は条文が認める**正規の中間状態**であり、`MemberArea.orphans` が既にその型を持つ。
  **新しい状態を発明しない。**
- ★**「種別が分かる領域へ送る」は採れない。**送り先の候補は控え室（メンバー）と
  エネルギーデッキ置き場（エネルギー）だけで、どちらを選んでも**推測して条文を適用**したことになる。
  ルール外の置き場（`OutOfRuleZone`）へ逃がすのは **D-B** に触れる（**決定 D93-2** と同じ理由）。

★★**決定 D35 の原則が片側にしか適用されていなかった。**★★
D35「カードマスタに存在しない printingId を**黙って削除しない**。検出して読み取り専用で示す」は
デッキ側（`DeckValidator` / D71 の複製）では守られていたのに、**盤面では同じ札が消えていた。**
`GameSetup.begin` が未知の刷りで**開始を拒む**のも D35 の系である。
→ 今回の修正は D35 を盤面へ広げるものであり、**新しい原則ではない。**

### ★★ 走査で同じ形がもう 1 か所出た —— 10.4.1 ★★

「引けなかったら置かない」+「元を無条件に消す」の組み合わせを `loveca_core` 全体で当たった。

| 場所 | 形 | 判定 |
|---|---|---|
| **10.4.1 重複メンバー処理** | `sendToOwner(stack.member)` + `beneath.forEach(sendToOwner)` → **`stacks = [survivor]`** | ★★**同じ不具合。D-22 の本文が捉えていなかった 2 か所目**★★ |
| 10.5.3 / 10.5.4 | 上記 | D-22 本体 |
| 10.5.1 / 10.5.2 | `_isInvalidOnLiveStage` / `_isInvalidOnEnergyField` が `card != null && …` で**引けない札を選ばない** | 安全（「選ぶ側」で先に弾く形） |
| `step_engine.dart` `_revealLiveStage`（8.3.4） | `master != null && …` で `else` に落ち、ライブ置き場に**残る** | 安全 |
| `refresh.dart` / `card_move.dart` / `reduce.dart` | カタログを引かない。`reduce` は `orphans` に**足すだけ** | 該当なし |

★★**grep では出ない形である**★★（意味の性質であって字面ではない / **D-15 (h)** と同じ理由）。
→ **走査ではなく不変条件で塞いだ**（下記）。4 か所すべてと将来の追加を同時に見る。

### ★★ 種別を確かめずに断定している箇所は 2 種類あった ★★

条文は種別で行き先を分ける（**10.5.3** メンバー → 控え室 / **10.5.4** エネルギー → エネルギーデッキ置き場）。
**4.5.5** も下に重ねられるカードを**この 2 種別に限る**。

| 入力 | 直す前 | 直したあと |
|---|---|---|
| **カタログを引けない** | 「メンバーカード 10.5.3」と報告し、控え室へ送ろうとして消す | ★動かさず残し、**データの問題**として報告 |
| ★**ライブカードの孤児** | 「メンバーカード 10.5.3」と報告し、控え室へ送る | ★動かさず残し、**条文の問題**として報告 |

★★**ライブ孤児は実機で作れる。**★★ `StackUnderMember`（`board_drag.dart` の下半分ドロップ）に
種別の制限が無い。★**制限は足さない** —— 紙ならライブカードをメンバーの下に置けるので、
禁じると **D-A（サンドボックス）**に反する。**実装が条文より広いのは正しい。**
条文が定めていない状態に**ルール処理を適用しない**ことで整合する。

★★**10.4.1 の枝は格が違う。**★★
10.4.1 は「それ以外のそのメンバーエリアの**カード**を…控え室に置きます」で**種別を条件にしていない**
（10.5.5 が行き先だけを振り替える）。→ **ライブカードでも控え室へ送る。**混ぜないこと。

### ★★ 直したら A-3 に退化しかけた ★★

`tidy()` のループは `if (round.applied.isEmpty) break;` の**前**に除外の記録を取り込んでいなかった。
断定をやめた瞬間に `applied` が空になり、**除外の記録ごと落ちる**（動かせなかったことすら黙る）。
→ 累積器をループの外へ出した。★**対照実験で 5 件落ちることを確かめてある**（下記）。

### 検知手段 —— 「整理で盤面から札が 1 枚も消えない」

`loveca-core/test/rule_process_test.dart` に、全領域を回る走査
（`Zone.values` + `stacks` / `beneath` / `orphans` + `OutOfRuleZone` + 共有 `resolution`）で
**整理の前後の instanceId 多重集合が一致する**ことを固定した。

★★**走査自体が「見えていない」なら常に通る**★★ので（**D-10**）、
**本体を測る前に「対照が判別すること」を確かめた。**

| 実験 | 落ちたテスト |
|---|---:|
| ★**判別確認: 走査を盲目にする** | **7 件**（陽性対照 2 件 + 保存 5 件）|
| **D-22 の元の実装**（種別を断定して無条件に送り、孤児を空にする） | **8 件** |
| 孤児を無条件に消すだけ（断定は直したまま） | **1 件** |
| **10.4.1（2 か所目）で束を無条件に捨てる** | **1 件** |
| **break で空振りラウンドの記録を捨てる** | **5 件** |
| ★**判別確認: 何も変えない（no-op）** | **0 件** |

★★**「孤児を消すだけ」が 1 件しか落ちないのは、2 つの誤りが 1 つの不具合だったから**★★ ——
`tidy()` は `applied` が空のラウンドの `state` を採らないので、
**断定をやめた実装では孤児を空にしても盤面へ書き戻らない。**
消えるのは**断定して `applied` を積んだとき**だけである。→ D-22 の 2 つの誤りは分離できない。

★★**実装中に「増える側」で落ちた。**★★
10.4.1 で束の**中身だけ**が変わったとき、束の**数**の比較で書き戻しを取りこぼし、
**控え室と束の両方に同じ札が居る**状態を作っていた。
★保存の検査は「消える」だけでなく「増える」も見るので捕まえられた。

### 直したもの

| ファイル | 内容 |
|---|---|
| `loveca-core/lib/src/game/rule_process.dart` | `UnmovableReason` / `UnmovableCard` を新設。`excludedCount` / `unknownCardNumbers` → `unmovable`。`sendToOwner` が `CardType?` を返す。`orphanUnmovableReason` を公開（UI が同じ判定を書き直さないため） |
| `loveca-ui/lib/src/state/board_notice.dart` | `TidyExcluded` → `TidyUnknownCard` / `TidyNoRuleForCardType`。`OrphanCardsPresent` → **+ `OrphanCardsStuck`** |
| `loveca-ui/lib/src/ui/board/board_notice_bar.dart` | 文面に「**元の置き場に残っています**」を必ず入れる（無いと消えたと読める。D-22 の出どころ） |
| `loveca-ui/lib/src/ui/board/board_layout.dart` | 孤児のタグを 3 種に分ける（「整理を待っている」と「整理しても動かない」を**盤面で**見分けられること） |
| `loveca-ui/lib/src/ui/board/board_card_menu.dart` | 孤児のメニューが理由と次の一手を出す |

★**`TidyExcluded` の文言は「直す先が決まってから」と保留してあった。**決まったので直した。

★**D-8 / D-11 / D-13 は `loveca_db` 側なので、今回は触っていない。**

---

### D-23. `_saveSettings` がどの項目でも「次の起動から」と言う【★2026-08-26 発見・同日解消（決定 D97 の実装と同時）】

★★**いま実害は無い。だが即時に効く設定項目を 1 つ足した瞬間、画面が嘘をつく。**★★

#### 事実

`loveca-ui/lib/src/ui/settings/settings_page.dart` の `_saveSettings` は、
**どの項目を保存しても**無条件に `_needsRestart = true` を立て、
スナックバーに「保存しました。反映されるのは次の起動からです。」を出す。

★**ところが直上のコメントの理由は `distDir` に限定されている** ——

> ★dist の場所は起動ゲートでしか読まれない（決定 D56 / D60）。
> いま効いたように見せると、次の起動まで嘘をつくことになる。

#### ★ 理由が実装より狭い

| 項目 | 再起動が要るか | 理由の格 |
|---|---|---|
| `distDir` | ★**要る（本質的）** | **D56**「マスタ取り込みは起動ゲートでのみ走らせる」。カタログ全体の再構築を伴う |
| `showParallel` | 要る（★**偶然**） | 一覧が `env.settings` から読んでいるだけ（`card_browse_page.dart:67` / `deck_edit_page.dart:79`）。★**起動ゲートを要求する条件は無い** |

★★**「設定は起動時だけ読む」という規則はそもそも存在しない。**★★
あるのは **D56** という `distDir` だけの制約である。
★**`_needsRestart` はその制約を全項目へ広げてしまっている。**

★**既存 2 項目がどちらも再起動を要するため、いまは正しい答えが出ている。**
★**偶然に助けられているだけで、根拠は 1 項目ぶんしか無い。**

#### ★ 型 —— D-15 (h) の親戚だが**向きが逆**

| | D-15 (a)〜(g) | D-15 (h) | ★**D-23** |
|---|---|---|---|
| 何が食い違うか | doc の**数**と実装の**数** | doc の**能力の主張**と実装の**不在** | ★**コメントの理由**と、**実装が及ぶ範囲** |
| 向き | — | doc が実装より**広い** | ★**理由が実装より狭い** |
| 走査で出るか | ○ 数を数えれば出る | ✗ 数量詞が無い | ✗ ★**数量詞も廃止語も無い。読まないと分からない** |

★**発見の経緯**: 決定 **D97** で「補完に使うエネルギーカード」を `AppSettings` に足すことを決め、
**この項目は再起動が要らない**（補完の 1 回にしか効かず、既存の状態に影響しない）と判断した時点で、
★**「では `_saveSettings` は何を根拠に全項目へ再起動を促しているのか」**を確かめて見つかった。

#### ★★ 解消（2026-08-26 / 決定 D97 の実装と同時）★★

★**3 項目目が入る前に直したので、嘘をつく状態は 1 度も存在しなかった。**

`_saveSettings` に **`needsRestart`** を渡す形にし、`_needsRestart` とスナックバーの
文言をその値で分けた。★**判定を項目名で分岐させていない** —— どちらなのかは
**呼び出し側が知っている**ので呼び出し側が渡す
（`HiddenPile` が「押したとき何が出るか」を持たないのと同じ形 / `CLAUDE.md` §8）。

★**帯とスナックバーの両方を分けた。**
`settings_page.dart` は「★★帯だけでは足りない★★ 帯は一覧の先頭にあり、
下のほうの項目を触った利用者には**見えない**」と自ら書いている。**片方だけ直すとその指摘に反する。**

★**対で固定した**（`loveca-ui/test/ui/settings_page_test.dart`）——
エネルギーの刷りを選び直しても「次の起動から」と言わないこと / ★**対**: `distDir` を
保存したときは言うこと。★片方だけ見ると、**常に「要らない」と言う実装**でも通ってしまう。

★★**`showParallel` は「要る」のまま残してある。**★★
再起動が要る理由は**偶然**（一覧が `env.settings` から読んでいるだけ）だが、
**いまの実装では実際に次の起動まで効かない。**★格が違うことは doc に書き、
**振る舞いは実装に合わせた** —— 先に文言だけ変えると、そちらが嘘になる。

---


## 修正の適用順序

1. ~~**A-1** `bladeHearts` の型分離~~ — 完了
2. ~~**A-1** `verify_contract.py` を全 22 商品に拡張~~ — 完了（終了コード 2 で限定検査を明示）
3. ~~**A-3** 数字なし表記の取りこぼし修正 + V15~~ — 完了（レビュー時点で未検出だった論点）
4. ~~**A-2** ターン回数制限の括弧ゆれ~~ — 完了
5. ~~検証~~ — 完了。`validate --complete` エラー 0 /
   `verify_contract.py` 全 22 商品 1,708 種 / `dataVersion` 2
   （★テスト件数は当時 Python 33 件 / Dart 32 件。**現在の正は CLAUDE.md §3 の表**）
6. ~~**B-1 / B-3** を確定してから Phase 3a に着手~~ — 完了（`docs/PhaseEngine設計メモ.md`）
7. ~~**Phase 3a（GameState / 集計エンジン）に着手**~~ — 完了
8. ~~**Phase 2 第一段階（drift スキーマとマスタ取り込み層）に着手**~~ — 完了
9. ~~**Phase 2 後半（UI）着手前の技術検証**~~ — 完了（`docs/UI技術検証メモ.md` / 決定 D42〜D48）
10. ~~**検索の復元処理の削減と移行機構の新設**~~ — 完了（決定 D49 / D50）
11. ~~**B-2 / B-4** を確定してから Phase 3b に着手~~ — 完了（`docs/盤面設計メモ.md` / 決定 D73〜D79）
12. **Phase 3b（PC 盤面 UI）に着手** — ★着手時に **D-2**（`loveca-core` にリントが 1 つも効いていない）と
    **D-15**（列挙による断定が古くなっている）を読むこと

### D-24. 10.6 の警告が「エール処理中」の札にも立つ【★2026-08-27 発見（M-B7）/ ★**未解消**】

#### 事実

`RuleProcessor.warningsFor` は **解決領域が空でないこと**だけを見て
`RuleProcessWarningKind.invalidResolution`（10.6）を立てる。

```dart
// loveca-core/lib/src/game/rule_process.dart
// 10.6.1: 解決領域にプレイ中・解決中・エール処理中でないカードがある。
// ★「プレイ中 / 解決中」は効果の解決状態であり観測できない (D-A)。
if (state.resolution.isNotEmpty) {
  warnings.add(RuleProcessWarningKind.invalidResolution);
}
```

★★**条文は 3 つを除外しており、そのうち 1 つは観測できる。**★★

> **10.6.1**「解決領域に現在プレイ中または解決中である**またはエール処理中（8.3.11）である**
> 以外のカードが存在する場合、ルール処理として、そのカードをオーナーの控え室に置きます。」

`docs/PhaseEngine設計メモ.md` §11 は**自分でそう書いている** ——
「★**『エール処理中』はカーソルから観測できる**が、『プレイ中 / 解決中』は 9.6 のプレイと
解決の進行状況そのものであり、効果の自動処理を実装しない以上アプリは知りようがない」。
★★**観測できると書いたほうを、実装は観測していない。**★★

#### いつからか / なぜ今まで出なかったか

★**M-B3（決定 D86）から在る。**8.3.11 のエールはメインデッキの札を解決領域へ置き、
**8.4.8 まで置きっぱなし**になる（8.3.12 / 8.3.14 / 8.4.2 がそれを読む）ので、
その間のチェックタイミングでは**必ず**この警告が立つ。

★**M-B6 までは「整理の帯に 1 行出る」だけ**だった。**帯は次の整理まで残る設計**なので、
出っぱなしでも実害として意識されなかった。

★★**M-B7（決定 D92）で「新規の警告」が停止条件になり、初めて挙動として現れた。**★★

#### 実害

| | |
|---|---|
| 押下回数 | ★**1 ターンに 1 回多く止まる**（2 回目以降は基準集合に入るので止まらない / §15-5）。ローカル対戦 12 → **13** / ソロ 7 → **8** |
| 文面 | 「解決領域にカードがあります」と出るが、**エール中は正規の状態**である。「手で処理してください」は誤った指示になる |
| ★格 | ★**誤検出であって、見落としではない。**10.6 を**実行しない**方針（D-A / `docs/PhaseEngine設計メモ.md` §11）は正しく、盤面を壊してはいない |

#### なぜ M-B7 で直さないか

★★**10.6.1 の除外をどこまで観測できると読むかは、M-B7 とは別の論点である。**★★

| 論点 | 中身 |
|---|---|
| 1 | ★**「エール処理中」の範囲が条文から一意に決まらない。**8.3.11 が置き、8.4.8 が片づけるが、**その間ずっとエール処理中なのか**は 8.3.11 の 1 条からは読めない |
| 2 | ★**カーソルで判定すると、`warningsFor` が `cursor` に依存し始める。**いまは**盤面の形だけ**を見る関数である（`tidy` と同じ純粋さ） |
| 3 | ★**手で解決領域へ置いた札と区別できない。**D-A のもとでプレイヤーは効果処理のために解決領域を使う（`docs/PhaseEngine設計メモ.md` §11 が「進行中の作業を破壊する」と書いた経路）。**カーソルで一律に黙らせると、そちらも黙る** |

→ ★**判断の時期: `loveca_core` の `RuleProcessWarningKind` に手を入れるとき**
（**未決 U25**「警告に対象カードを持たせるか」と同時。★**同じ型を触る**）。

#### ★ いまの挙動をテストで固定してある

★★**「直したら押下回数が変わる」ことが検査で分かる形にした。**★★
`loveca-ui/test/board/board_auto_advance_test.dart` の
「★★ 押下回数（★実戦的な盤面 = 動的停止を含む）★★」の群が

- 実戦的な盤面では**静的のみより多く止まる**こと
- ★**余分な停止が 1 ターンに 1 回だけ**であること
- ★**止まった位置が 8.3.11 より後のチェックタイミング**であること

を固定している。★**直すときはこの群が落ちる。**それが合図である。

---

### D-25. §3 の見張りが `.claude/` を構造的に見ていない【★2026-08-27 発見・同日解消】

#### 事実

`CLAUDE.md` §3「意図せず無視されたファイルが無いか」の grep は、
除外リストに **`\.claude/`** を持っていた。

```bash
  | grep -vE "^(loveca-(core|db|ui)/\.dart_tool/|…|loveca-data/data/|\.claude/)" \
```

`^` にぶら下がる選択肢なので実体は **`^\.claude/`** ——
★**`.claude/` 配下は、何が無視されていても 1 件も出ない。**

★★**2026-08-27 に `.claude/commands/bundle-b.md`（束 B の作業指示そのもの）を置いた。**★★
**最も失うと困るものが、最も見えない場所に入った。**

#### ★★ 型は D-10 ではなく D-15 (l) である ★★

★**混ぜないこと。**§3 は自分の中に D-10 由来の教訓を 2 つ書いており、
そのどちらでもない。

| | D-10 / §3 が記録している型 | ★**D-25** |
|---|---|---|
| D-10 本体 | `.gitignore` の**未アンカー規則**が本実装を飲み込んだ | ★**`.gitignore` は関係ない。**飲み込んだのは**見張りの除外リスト**のほう |
| §3 の「除外はパスで書くこと」（最初の版が `(^\|/)build/` だった件） | ★**形式の誤り** —— ディレクトリ**名**で書いたため `lib/src/rules/build/` まで巻き込んだ | ★**形式は正しい。**`^\.claude/` は**パスで書かれており**、§3 自身の規約を守っている |

→ ★★**壊れたのは形式ではなく範囲である。**★★
**書いた時点では正しく、あとから前提が動いた** —— これは **D-15 (l)** と同じ型である。

| | D-15 (l) | ★**D-25** |
|---|---|---|
| 何が古くなったか | 未実装の決定（D92）の**前提** | 見張りの除外リストが仮定していた**「.claude/ に本実装に効くものは無い」** |
| 誰が動かしたか | ★**別の決定**（D93-5 / D95） | ★**別の作業**（2026-08-27 のコミット `69a43f5`） |
| 書いた側の落ち度 | 無い（書いた時点では正しい） | ★**同じく無い** |

★**(a)〜(g) 型ではない。**あれは**実装が動いて doc が取り残された**もので、
ここは**除外リストの前提そのもの**が動いている。

#### いつから誤っていたか

| 日 | 出来事 | そのときの正しさ |
|---|---|---|
| **2026-08-24**（`676a69c`） | §3 に見張りを置き、除外に `\.claude/` を入れた | ★**正しい。**当時 `.claude/` にあったのは `settings.local.json`（グローバル ignore）と `/.claude/worktrees/` だけで、**本実装に効くものは 1 つも無かった** |
| **2026-08-27**（`69a43f5`） | `.claude/commands/bundle-b.md` を置いた | ★★**この瞬間に誤りになった。**★★ 除外を書いた人は何も間違えていない |

★★**「いつ書かれたか」ではなく「いつ前提が動いたか」で日付を持つこと。**★★
`git blame` は 2026-08-24 を指すが、**誤りになったのは 2026-08-27 である。**

#### 処遇 —— 除外を狭める

`.claude/` 全体をやめ、**グローバル ignore が実際に飲む 1 ファイルだけ**を残した。

```
before:  \.claude/
after:   \.claude/settings\.local\.json
```

対応するのは `C:\Users\hiimo/.config/git/ignore` の
**`**/.claude/settings.local.json`（1 行だけ）**である。

#### ★★ 陽性対照 —— 狭めただけでは何も証明していない ★★

★**`settings.local.json` は前後どちらでも除外されるので、テストにならない。**
§3 自身が**「動作確認は新規の未追跡ファイルで行うこと」**と書いているとおりに確かめた。

1. `.gitignore` に一時的に `/.claude/commands/` を足す
2. ★**新規の（追跡されていない）** `.claude/commands/__probe_positive_control.md` を置く
   （★`bundle-b.md` は**追跡済み**なので `.gitignore` の影響を受けない。使えない）
3. `git status` に出ないことを確認 —— **痕跡なく落ちる状態を作れている**
4. 旧 grep と新 grep を両方走らせる
5. `.gitignore` と probe を戻す

| | 結果 |
|---|---|
| 旧（`\.claude/`） | ★**0 件。見逃す** |
| 新（`\.claude/settings\.local\.json`） | ★**`.claude/commands/__probe_positive_control.md` を検知** |

★**出なかったら狭め方が効いていない**、という判定を先に決めてから走らせた。

#### ★ 狭めた結果は 0 件

実データで新 grep を走らせて **0 件**であることを確認した（狭める前と同じ）。
★**いま `.claude/` 配下で無視されているのは `settings.local.json` 1 件だけ**である。

#### ★★ 残る穴が 1 つある —— `/.claude/worktrees/` ★★

★**これは silent hole ではなく noise になる穴である。**格が違うので分けて書く。

root `.gitignore` の `/.claude/worktrees/` は残っており、
**ワークツリーが存在するあいだ、その中の `.dart` / `.md` が全部この見張りに出る。**
実測で確認した（`.claude/worktrees/x/lib/foo.dart` を置くと検知される）。

★**それでも `worktrees/` を除外に足さなかった。**理由は 2 つ。

1. ★★**穴を残す判断は、黙って行ってはならない。**★★ 除外を 1 つ足すたびに
   「そこは永久に見えない」が増える。D-25 はまさにそれで起きた
2. ★**失敗の向きが逆である。**除外すれば**静かに見えなくなる**が、
   除外しなければ**うるさく出る**。root `.gitignore` が
   「予期しない `build/` は**隠すより見えるほうが安全**」と書いているのと同じ判断

→ ★**ワークツリーを常用するようになったら、そのとき改めて判断する。**
そのときも**足す前にここへ理由を書くこと。**

#### 直したもの

| | |
|---|---|
| `CLAUDE.md` §3 の grep | 除外を `\.claude/` → `\.claude/settings\.local\.json` に狭めた |
| `CLAUDE.md` §3 | ★**「除外はパスで書くこと」の隣に「範囲も古くなる」を足した**（形式の話しか無かった） |
| `CLAUDE.md` §8 の未決表 | D-25 の行（済） |

★**次に除外を足す人へ**: ★★**その除外が仮定していることを 1 行で書くこと。**★★
`\.claude/` の仮定は「ここに本実装に効くものは無い」だったが、
**どこにも書かれていなかったので、前提が動いたことに誰も気づけなかった。**

---

### D-26. `ord` の backfill が `revision` を上げず、マスタに依存する【★2026-08-27 記録（束 B）/ ★**いま直さない** / 判断は Phase 4 の束 A】

#### 事実

`schemaVersion` 2 → 3 の `onUpgrade`（決定 D65 / **D99**）は
`DeckDao.backfillOrd()` を呼び、`deck_entries.ord` を規則順で埋める。

★★**この経路は `DeckDao.save` を通らない。**★★

| | |
|---|---|
| 変わるもの | `deck_entries.ord`（**デッキの中身**） |
| 変わらないもの | ★**`decks.revision`** / `decks.updated_at` |

P2 は「`revision` は更新のたびに +1。同期の差分検出に使う」と書いている。
D65 は「`ord` はデッキの中身なので `revision` に乗る。別立ての同期処理は要らない」
と書いたが、**それは `save` を通る書き込みの話**であって、
**移行そのものは `save` を通らない。**

#### ★★ さらに、backfill はマスタに依存する ★★

規則順は `cards.cost` / `cards.score` / `cards.card_type` を引く（決定 D99）。
→ ★**同じデッキでも、端末の `masterDataVersion` が違えば `ord` が違いうる。**

| 端末 | 状態 | 結果 |
|---|---|---|
| A | マスタ取り込み済み | 区分順 → cost / score 降順 |
| B | ★**まだ取り込んでいない** | 全件が「マスタに無い刷り」（決定 D35）→ **`printingId` 昇順だけ** |

★**どちらも `revision` は同じ**なので、Phase 4 の差分検出から見ると
**「同じ版なのに中身が違う」**という状態になる。

#### ★ 壊れはしない

★**null を末尾に落とす規則がある**ので（決定 D99）、値が引けなくても
順序は**全順序として決まる**。落ちないし、行も消えない
（`migration_test.dart` の「マスタが空でも落ちない」が固定している）。
→ ★**症状は「端末間で並びがずれる」であって、データの破損ではない。**

#### なぜ今直さないか

| 論点 | 中身 |
|---|---|
| 1 | ★**`revision` を上げる手を今決めると、D-9 と噛み合わなくなる。**`DeckDao.softDelete` が `revision` を上げないという**逆向きの未決**が既にあり（**D-9**）、両者は「`save` を通らない書き込みで `revision` をどう扱うか」という**同じ問い**である。片方だけ決めると規則が 2 つになる |
| 2 | ★**移行で `revision` を上げると、同期から見て「全端末が同時に全デッキを更新した」ことになる。**それが望ましいかは同期の衝突解決の方式次第で、その方式が **P4**（手がかりが 1 つも無い / **D-5**）である |
| 3 | ★**マスタ依存そのものは避けられない。**規則順が `cost` / `score` を見る以上、引けない端末では違う答えになる。避けるなら「マスタが揃うまで移行しない」だが、それは**起動をマスタ取り込みに従属させる**という別の大きな判断になる |

→ ★**判断の時期: Phase 4 の束 A**（**D-9** / **D-5** / **D-14** と同時。★**同じ問いである**）。

#### ★ 型

★**doc が実装より広い型**（**D-15 (h)**）である。
D65 の「`ord` はデッキの中身なので `revision` に乗る」は**限定が無く**、
**移行経路を含めて読める**が、実装は `save` の経路にしか当てはまらない。
★**走査では出ない**（数量詞も列挙も無い）。

★**発見の経緯**: D99 の backfill を実装しているときに、
「`save` は毎回全削除 → 全挿入なので増分保守が要らない」という D65 の記述を
確かめていて、**移行だけが `save` を通らないこと**に気づいた。

---

★**「次に何をするか」の正は CLAUDE.md §8 の「次の一手」。**
この一覧は本レビューの指摘に対する適用順序の記録であって、作業計画ではない。
食い違ったら CLAUDE.md を優先する。

`build` は `--skip-images` を付けると `imageHash` も空になる。
配信物として使う dist を作るときは付けないこと（Pillow が要る）。

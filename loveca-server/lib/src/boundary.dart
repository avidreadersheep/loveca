/// 線 α —— サーバーが `loveca_core` から呼んでよいものの**許可リスト**。
///
/// ★★ 空で始まる（決定 **D115-6**）★★
///
/// **D115-6** は「線は**禁止ではなく、空で始まる許可リストで引く**」と定めている。
/// 理由は「★**『呼ぶ必要が無い』から『呼んではならない』は導けない**」ことで、
/// ★**空で始めれば、最初の 1 件に必ず理由が付く。**
///
/// ★**Phase 4 のデッキ同期のサーバーは `loveca_core` を 1 つも呼ばない**
/// （**D115-6**）。★**Phase 6 の権威サーバは呼ぶ**（`CLAUDE.md` §1 の境界 1 ——
/// 「同じ `reduce` / `redact` を**コピーゼロで再利用する**」）。
/// → ★**このリストが空でなくなるのは Phase 6 である**、というのが今日の見込みであって、
///   ★**決定ではない**（**D115-6** は時期を 1 文字も述べていない）。
///
/// ---
///
/// ## ★★ 1 件足すときにすること（3 つとも） ★★
///
/// 1. ★**この集合に足す**（★下）。
/// 2. ★**`pubspec.yaml` に `loveca_core` の依存を書く**（決定 **D126-3** —— ★今日は
///    書いていない。★書けば「呼べる」状態になり、リストが空であることを
///    構造で守れなくなる）。
/// 3. ★★**理由をここに書く。**★★ ★**理由の無い許可を作らない**
///    （先例は `loveca-ui/test/docs/legacy_design_number_test.dart` の許可リスト）。
///
/// ★**2 を忘れるとコンパイルが通らない**ので、★**1 と 3 だけを足すことはできない。**
/// ★★**3 を忘れることはできてしまう。★そこは規約である**★★（**D115-5** が
/// 「境界を支えるのは規約ではない」と書いているのは**線そのもの**についてであり、
/// ★**理由を書くことまでは形で守れていない**）。
///
/// ---
///
/// ## ★★ 何を「1 件」と数えるか ★★
///
/// ★**ライブラリの URI そのもの**を数える（`package:loveca_core/loveca_core.dart` の形）。
/// ★**クラスや関数の単位では数えない** —— ★走査が見るのは `import` / `export` の
/// 指示行であり、★**そこから先は機械に見えない**（`ルール整合性チェック_v1.06.md` **D-27** ——
/// ★見えないものを見ていると書かない）。
library;

/// サーバーが `loveca_core` から `import` / `export` してよいライブラリの URI。
///
/// ★★**今日は空である**★★（決定 **D115-6** / **D126-3**）。
/// ★**見張りは `test/core_boundary_test.dart`。**★許可リストとの**完全一致**で見る
/// （★**件数ではなく集合で見る** —— 件数だけだと入れ替わりが通る）。
const Set<String> allowedCoreLibraries = <String>{};

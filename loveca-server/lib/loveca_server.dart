/// ラブカ シミュレーターの同期サーバー（Phase 4 / 決定 **D126**）。
///
/// ★★ このパッケージに口は 1 つも無い ★★
/// 認証の口も、デッキを預かる口も、通信の口も 1 つも無い
/// （`docs/同期設計メモ.md` §38-11 / §44-14）。
/// 中身は**境界の宣言**と、**認証の口の前段**だけである ——
/// パスワードの保存の形（決定 **D129-3**）と、**認証の判定**（決定 **D130-14**）。
/// ★**判定は★★受け取ったあと★★に走り、★保管は★★渡される口★★である**（`docs/同期設計メモ.md` §46-11）。
/// ★★**2026-09-01 訂正: ここには「今日 1 行も動かない」と書いてあった**★★ ——
/// ★`src/password_hash.dart` が入って**偽になった**（★型は **D-15 (l)** —— ★あとから前提が動いた）。
/// → ★**「サーバーが実装できる」と読まないこと。**決まったのは
///   **置き場と言語**であって、**何を送り合うか**ではない。
///
/// ---
///
/// ## ★★ 何であるか（決定 **D105-2** / **D106-1** / **D106-2**）★★
///
/// 1 つのサーバーが 4 つの役割を持つ。★**役割が違うだけで矛盾ではない**（**D106-2**）——
///
/// | 役割 | 中身 |
/// |---|---|
/// | デッキ同期 | ★**保管庫。判断しない**（**D105-2** —— ★衝突解決はアプリ側） |
/// | アクセス制御 | ★**誰のデータかはサーバーが持つ**（**D105-3**） |
/// | Phase 6 の対戦 | ★**権威。`redact` を掛けて配る** |
/// | カードマスタの配信 | ★**配る。判断しない**（**D120-13**） |
///
/// ★**用意して動かすのは利用者（の友人）である**（**D103-3** / **D124-4**）。
/// ★**任意のプログラムを動かせる**ことが分かっている（**D124-2**）。
///
/// ---
///
/// ## ★★ 境界の宣言 —— 4 段。1 段にまとめてはならない（決定 **D126-4**）★★
///
/// ★★**2026-09-01: 「許可」の段に行を 1 つ足した（決定 **D129-3**）。★段は 4 のままである**★★ ——
/// **D126-4** が定めたのは**扱いの種類が 4 つ**であることで、**相手の数ではない**。
/// → ★**訂正ではない**（★`docs/同期設計メモ.md` §44-16）。
///
/// ★★**「禁止」「空で始まる許可リスト」「決めていない」「許可」は別物である。**★★
/// **D115-6** が「★**呼ぶ必要が無い**」から「★**呼んではならない**」は導けないと
/// 定めており、ここはその区別をそのまま段にしてある。
///
/// | 段 | 相手 | 扱い | 根拠 |
/// |---|---|---|---|
/// | ★**禁止** | `package:flutter` / `dart:ui` / `package:loveca_ui` | ★**入れない** | ★**サーバーに画面は無い**（上の 4 役割はどれも描画しない） |
/// | ★**空で始まる許可リスト** | `package:loveca_core` | ★**今日は 0 件。1 件足すには理由が要る** | **D115-6** / **D115-7** |
/// | ★**決めていない** | `package:loveca_db` | ★**今日は依存しない。★「禁止」ではない** | ★サーバーが何で保管するかは未決（`docs/同期設計メモ.md` §35-9） |
/// | ★**許可** | Dart SDK（ファイル・ソケットを含む） | ★**入れてよい** | ★`CLAUDE.md` §1 の禁止は**`loveca_core` に対する制約**である（§2 が `loveca_db` に同じ断りを書いている） |
/// | ★**許可** | ★**外部のパッケージ（pub）** | ★**入れてよい。★ただし 1 件ごとに理由を pubspec に書く** | ★**線 α は `loveca_core` についての線である**（**D115-6**）。★外部のパッケージは**線 α の相手ではない**。★**増やしたこと自体は代償として記録する**（**D115-2**） |
///
/// ★★**3 段目を 1 段目に寄せないこと。**★★
/// 寄せると「サーバーは `loveca_db` を呼んではならない」という**決定**を作ってしまう。
/// ★**決めていない。**
///
/// ---
///
/// ## ★★ 依存の向き —— 6 本とも書く。今日は 0 本である（決定 **D126-3**）★★
///
/// ★**片側だけ書くと、逆向きが黙って通る。**
///
/// | 辺 | 今日 | 将来 | 根拠 |
/// |---|---|---|---|
/// | このパッケージ → `loveca_core` | ★**0 本**（★pubspec にも書かない） | ★Phase 6 で 1 本になりうる | **D115-6** ＋ `CLAUDE.md` §1 の境界 1 |
/// | `loveca_core` → このパッケージ | ★**0 本** | ★★**永久に 0 本**★★ | ★`CLAUDE.md` §1（★純粋 Dart）—— ★サーバーを知ることはファイルとソケットを知ることである |
/// | `loveca_db` → このパッケージ | ★**0 本** | ★**0 本** | ★DB 層はローカルの層である |
/// | `loveca_ui` → このパッケージ | ★**0 本** | ★**0 本** | ★★**通信は HTTP 越しであってコード依存ではない**★★ |
/// | このパッケージ → `loveca_db` | ★**0 本** | ★★**決めていない**★★ | ★上の 3 段目 |
/// | このパッケージ → `loveca_ui` | ★**0 本** | ★★**永久に 0 本**★★ | ★上の 1 段目 |
///
/// ---
///
/// ## ★★ 見張り ★★
///
/// 上の表は**規約ではなく形で守る**（**D115-5** —— ★「境界を支えるのは規約ではない」）。
///
/// - ★**許可リストの正**は [allowedCoreLibraries]（`src/boundary.dart`）。
/// - ★**見張り**は `test/core_boundary_test.dart`（**D115-7** の (c) ＝ 走査テスト）。
/// - ★**pubspec に書かないこと自体**が線 α の一部である（**D126-3**）。
///
/// ★★ この doc は禁止対象・許可リスト対象の字面をどちらも含む（**D-30**）★★
/// 禁止対象を説明する文書は、禁止対象と同じ字面を必ず含む。
/// 上の 2 つの表がまさにそれで、`package:flutter` も `package:loveca_core` も
/// **このファイルの中に実在する。**
/// → ★**走査は `import` / `export` の指示行だけを見る。**素の字面では走査しない。
/// → ★★**この doc 自体が走査テストの標的である**★★ —— 素朴な字面の走査なら
///   **このファイルが当たる**ことと、指示行の走査なら**当たらない**ことを、
///   `test/core_boundary_test.dart` が**対で**固定している（**D-27** —— ★対を置いたら、
///   対象を壊して対が落ちることを実測する）。
library;

export 'src/auth.dart';
export 'src/boundary.dart';
export 'src/password_hash.dart';
export 'src/json_field.dart';
